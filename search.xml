<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Cesium 数据加载实战：从免 npm 引入到视口驱动的增量上球</title>
    <url>/2026/09/09/cesium-data-loading/</url>
    <content><![CDATA[<blockquote>
<p>这篇文章不是 Cesium 入门教程，而是一次真实项目的落地复盘。最近负责的 Vue3 + Vite 项目里有一块三维地图：CesiumJS 1.123，渲染几十类基础设施（变电站、发电厂、线路……）的点线面数据，支持视口内动态加载、拓扑流动网络、实时事件告警。网上 Cesium 教程大多停留在「new 一个 Viewer + GeoJsonDataSource.load 一个文件」，而真实项目里你要回答的问题其实是：<strong>内网离线环境怎么跑、几万个实体怎么不卡、拖动地图时数据怎么增量进出、内存怎么不涨爆</strong>。本文按数据流动的顺序，把这条链路从头到尾拆开讲。所有代码都来自这个真实项目，文末附完整文件地图。</p>
</blockquote>
<h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul>
<li><a href="#sec0">0. 前言：一个真实的 Cesium 项目长什么样</a></li>
<li><a href="#sec1">1. 加载 Cesium 本体：不用 npm、不用 vite-plugin-cesium</a></li>
<li><a href="#sec2">2. Viewer 初始化：先拿到一个”干净”的球</a></li>
<li><a href="#sec3">3. 影像底图加载：配置驱动的多源图层</a></li>
<li><a href="#sec4">4. 业务数据上球：为什么放弃 Entity，改用 Primitive</a></li>
<li><a href="#sec5">5. 视口驱动的增量加载：只请求”看得见”的数据</a></li>
<li><a href="#sec6">6. 让数据”活”起来：实时推送与交互联动</a></li>
<li><a href="#sec7">7. 渲染亮点一瞥：GLSL 流动线、球面弧线、呼吸图标</a></li>
<li><a href="#sec8">8. 写在最后</a></li>
</ul>
<p><a id="sec0"></a></p>
<h2 id="0-前言：一个真实的-Cesium-项目长什么样"><a href="#0-前言：一个真实的-Cesium-项目长什么样" class="headerlink" title="0. 前言：一个真实的 Cesium 项目长什么样"></a>0. 前言：一个真实的 Cesium 项目长什么样</h2><p>先交代背景。项目是一个基础设施三维地图应用：Vue 3.3 + Vite 6 + Pinia 3 + CesiumJS 1.123，部署在<strong>内网离线环境</strong>——这个前提直接决定了后面很多技术选型：不能用 Cesium Ion 的在线资产、不能依赖任何公网瓦片服务、后端接口返回的是<strong>带坐标数组的业务 JSON 而不是标准 GeoJSON 文件</strong>。</p>
<p>数据的流动方向可以用一张图概括，后文所有章节都是对这张图的展开：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">┌────────────────── 静态层（public/）──────────────────┐<br>│  /cesium/          CesiumJS 1.123 构建产物            │<br>│  /config.js        window.baseConfig（运行时配置）     │<br>└────────────┬─────────────────────────────────────────┘<br>             ▼  index.html 里 &lt;script&gt; 全局引入<br>┌────────────── Viewer 初始化（cesiumMap.vue）──────────────┐<br>│  禁用全部默认控件 · 不加载 Ion 默认底图 · 阻断 Ion 请求     │<br>│  camera.setView(baseConfig.cesiumCameraView)              │<br>└────────────┬─────────────────────────────────────────┘<br>             ▼  dispatchEvent(&#x27;viewer-ready&#x27;)<br>┌────────────── 影像底图（home-header）───────────────────────┐<br>│  config.js layers[] → layerFactory.createProvider()      │<br>│  XYZ / WMS / WMTS / ArcGIS → addImageryProvider()       │<br>└────────────┬─────────────────────────────────────────┘<br>             ▼<br>┌────────────── 业务数据管线（use-load-data.ts，核心）──────────┐<br>│  camera.moveEnd(400ms 防抖) → 相机高度过滤(1km~400km)     │<br>│    → computeViewRectangle() 算视口经纬度边界               │<br>│    → POST 接口 &#123; tableIds, boundary &#125;                     │<br>│    → processCoordinates() 坐标解析                        │<br>│    → Primitive 工厂（点/线/面）→ 增量 diff 上球             │<br>└────────────┬─────────────────────────────────────────┘<br>             ▼<br>┌────────────── 渲染与交互 ────────────────────────────────┐<br>│  globalPrimitiveCollection + globalBillboardCollection   │<br>│  悬停/点击/右键状态机 · WebSocket 事件 → 呼吸图标 · flyTo   │<br>└─────────────────────────────────────────────────────────┘<br></code></pre></td></tr></table></figure>

<p>一个值得先说结论的事：这个项目<strong>没有用</strong>地形（<code>CesiumTerrainProvider</code>）、<strong>没有用</strong> 3D Tiles、<strong>没有用</strong> <code>GeoJsonDataSource</code>，也没有 CZML&#x2F;KML。不是不会用，而是数据形态和性能要求决定了「后端 JSON + Primitive API」这条更底层的路线。第 4 节会讲清楚为什么。</p>
<p><a id="sec1"></a></p>
<h2 id="1-加载-Cesium-本体：不用-npm、不用-vite-plugin-cesium"><a href="#1-加载-Cesium-本体：不用-npm、不用-vite-plugin-cesium" class="headerlink" title="1. 加载 Cesium 本体：不用 npm、不用 vite-plugin-cesium"></a>1. 加载 Cesium 本体：不用 npm、不用 vite-plugin-cesium</h2><h3 id="1-1-常规路线的问题"><a href="#1-1-常规路线的问题" class="headerlink" title="1.1 常规路线的问题"></a>1.1 常规路线的问题</h3><p>Cesium 官方推荐的 npm 接入是 <code>npm i cesium</code> + 打包插件（<code>vite-plugin-cesium</code> 或 Vite 官方文档里 copy-webpack-plugin &#x2F; vite-plugin-static-copy 那套）。但 Cesium 的构建产物非常特殊：<code>Build/Cesium/</code> 下除了一个 3MB+ 的 <code>Cesium.js</code>，还有 <strong>Workers、Assets、Widgets、ThirdParty 四个静态目录</strong>，运行时按需加载，一个都不能少。这带来两个痛点：</p>
<ul>
<li>构建器要处理的文件数量暴涨（Workers 目录下几百个文件），dev 冷启动和 HMR 都会被拖慢；</li>
<li>每次构建都要复制一遍静态资源，CI 时间和 <code>dist</code> 体积都难看。</li>
</ul>
<p>而 <code>vite-plugin-cesium</code> 的原理说穿了也很朴素：<strong>把 node_modules 里的构建产物复制到输出目录 + external 掉 cesium 包</strong>。既然如此，不如直接自己来。</p>
<h3 id="1-2-项目的做法：public-目录-全局变量"><a href="#1-2-项目的做法：public-目录-全局变量" class="headerlink" title="1.2 项目的做法：public 目录 + 全局变量"></a>1.2 项目的做法：public 目录 + 全局变量</h3><p>项目的 <code>package.json</code> 里<strong>没有 cesium 依赖</strong>（node_modules 里那份只是为了取构建产物），只留了一个复制脚本：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-punctuation">&#123;</span><br> <span class="hljs-attr">&quot;scripts&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">&#123;</span><br>  <span class="hljs-attr">&quot;copy-cesium&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;cp -r node_modules/cesium/Build/Cesium/* public/cesium/&quot;</span><br> <span class="hljs-punctuation">&#125;</span><br><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure>

<p>装包后手动执行一次 <code>npm run copy-cesium</code>，把产物复制进 <code>public/cesium/</code>。之后 <code>index.html</code> 用最原始的方式引入：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">head</span>&gt;</span><br> <span class="hljs-tag">&lt;<span class="hljs-name">link</span> <span class="hljs-attr">rel</span>=<span class="hljs-string">&quot;stylesheet&quot;</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;/cesium/Widgets/widgets.css&quot;</span> /&gt;</span><br> <span class="hljs-tag">&lt;<span class="hljs-name">script</span> <span class="hljs-attr">src</span>=<span class="hljs-string">&quot;/cesium/Cesium.js&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">script</span>&gt;</span><br> <span class="hljs-tag">&lt;<span class="hljs-name">script</span> <span class="hljs-attr">src</span>=<span class="hljs-string">&quot;/config.js&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">script</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">head</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>这两行 script 各有分工：</p>
<ul>
<li><code>/cesium/Cesium.js</code> 把整个库挂到全局变量 <code>window.Cesium</code> 上；</li>
<li><code>/config.js</code> 是<strong>运行时配置</strong>，往 <code>window.baseConfig</code> 上写后端地址、图层列表、初始相机视角、token——部署后改这一个文件就能换环境，不用重新构建。对内网项目来说这比任何环境变量方案都直观。</li>
</ul>
<p>代价是失去了 ESM 导入，所以补一个全局类型声明文件 <code>global.d.ts</code>（记得在 <code>tsconfig.json</code> 的 <code>include</code> 里加上它）：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-keyword">import</span> <span class="hljs-keyword">type</span> * <span class="hljs-keyword">as</span> <span class="hljs-title class_">Cesium</span> <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;cesium&#x27;</span>;<br><br><span class="hljs-keyword">declare</span> <span class="hljs-variable language_">global</span> &#123;<br> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Window</span> &#123;<br>  <span class="hljs-title class_">Cesium</span>: <span class="hljs-keyword">typeof</span> <span class="hljs-title class_">Cesium</span>;<br>  <span class="hljs-attr">viewer</span>?: <span class="hljs-title class_">Cesium</span>.<span class="hljs-property">Viewer</span>;<br>  <span class="hljs-attr">baseConfig</span>: &#123;<br>   <span class="hljs-attr">baseUrl</span>: <span class="hljs-built_in">string</span>;   <span class="hljs-comment">// 请求 ip</span><br>   <span class="hljs-attr">baseWsUrl</span>: <span class="hljs-built_in">string</span>; <span class="hljs-comment">// ws 请求 ip</span><br>   <span class="hljs-attr">cesiumToken</span>: <span class="hljs-built_in">string</span>;<br>   <span class="hljs-attr">layers</span>?: <span class="hljs-built_in">any</span>;      <span class="hljs-comment">// 地图的图层</span><br>  &#125;;<br> &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>注意第一行 <code>import type * as Cesium from &#39;cesium&#39;</code>——类型可以继续用 npm 包的（devDependencies 里装着，只参与类型检查不参与打包），运行时用的才是 <code>window.Cesium</code>。<strong>类型走 npm，运行走全局</strong>，两头的好处都占了。</p>
<h3 id="1-3-vite-config-ts-里必须加的三个配置"><a href="#1-3-vite-config-ts-里必须加的三个配置" class="headerlink" title="1.3 vite.config.ts 里必须加的三个配置"></a>1.3 vite.config.ts 里必须加的三个配置</h3><figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-keyword">export</span> <span class="hljs-keyword">default</span> <span class="hljs-title function_">defineConfig</span>(&#123;<br> <span class="hljs-comment">// 解决 knockout-3.5.1.js:159 Uncaught ReferenceError: global is not defined</span><br> <span class="hljs-attr">define</span>: &#123;<br>  <span class="hljs-attr">global</span>: <span class="hljs-string">&#x27;globalThis&#x27;</span>,<br>  <span class="hljs-attr">CESIUM_BASE_URL</span>: <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">stringify</span>(<span class="hljs-string">&#x27;/cesium/&#x27;</span>)<br> &#125;,<br> <span class="hljs-attr">worker</span>: &#123;<br>  <span class="hljs-attr">format</span>: <span class="hljs-string">&#x27;es&#x27;</span><br> &#125;<br> <span class="hljs-comment">// ...</span><br>&#125;);<br></code></pre></td></tr></table></figure>

<p>三条各有来历，全是踩坑换来的：</p>
<ol>
<li><strong><code>global: &#39;globalThis&#39;</code></strong>：Cesium 依赖的老版本 knockout 是为 Node&#x2F;Browserify 时代写的，引用了裸的 <code>global</code> 变量，Vite 的 ESM 环境里没有这个东西，直接报 <code>ReferenceError</code>。用 define 把 <code>global</code> 替换成标准的 <code>globalThis</code>。</li>
<li><strong><code>CESIUM_BASE_URL</code></strong>：告诉 Cesium 去哪找 Workers&#x2F;Assets 这些静态资源。因为我们不是从 npm 包解析的，必须显式指到 <code>/cesium/</code>，否则几何计算（<code>PolygonGeometry</code> 创建时会甩给 Worker）会 404。</li>
<li><strong><code>worker.format: &#39;es&#39;</code></strong>：Cesium 的 Worker 需要 ES Module 格式，Vite 默认的 IIFE 会导致 Worker 加载失败。</li>
</ol>
<p>这套方案的净收益：业务代码和 Cesium 彻底解耦，构建产物里不再有 cesium 的 chunk（<code>manualChunks</code> 不用管它），dev server 秒起；升级 Cesium 版本 &#x3D; 覆盖一个目录 + 回归测试，<code>package.json</code> 纹丝不动。</p>
<p><a id="sec2"></a></p>
<h2 id="2-Viewer-初始化：先拿到一个”干净”的球"><a href="#2-Viewer-初始化：先拿到一个”干净”的球" class="headerlink" title="2. Viewer 初始化：先拿到一个”干净”的球"></a>2. Viewer 初始化：先拿到一个”干净”的球</h2><p><code>new Cesium.Viewer()</code> 默认给你的不是一个空球，而是一个”全家桶”：时间轴、动画控件、Ion 默认影像、地理编码搜索框……在离线内网里，这些东西不仅多余，而且<strong>每一个都在偷偷发请求</strong>。所以初始化的第一原则是：全部关掉。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-comment">// src/pages/home/components/cesium-map/cesiumMap.vue（有删减）</span><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">initMap</span> = (<span class="hljs-params"></span>) =&gt; &#123;<br> <span class="hljs-comment">// 用 Cesium 的 Ion 服务进行认证</span><br> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Ion</span>.<span class="hljs-property">defaultAccessToken</span> = cesiumToken;<br> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Ion</span>.<span class="hljs-property">defaultServer</span> = <span class="hljs-variable language_">window</span>.<span class="hljs-property">baseConfig</span>?.<span class="hljs-property">defaultPreventServer</span>; <span class="hljs-comment">// 阻止所有 ion 请求</span><br><br> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">buildModuleUrl</span>.<span class="hljs-title function_">setBaseUrl</span>(<span class="hljs-string">&#x27;/cesium/&#x27;</span>); <span class="hljs-comment">// 👈 关键！指向 public/cesium/</span><br><br> <span class="hljs-keyword">const</span> viewer = <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">Viewer</span>(cesiumContainer.<span class="hljs-property">value</span>, &#123;<br>  <span class="hljs-attr">timeline</span>: <span class="hljs-literal">false</span>,            <span class="hljs-comment">// 时间轴</span><br>  <span class="hljs-attr">animation</span>: <span class="hljs-literal">false</span>,           <span class="hljs-comment">// 动画控制器</span><br>  <span class="hljs-attr">geocoder</span>: <span class="hljs-literal">false</span>,            <span class="hljs-comment">// 地名查找控制器</span><br>  <span class="hljs-attr">homeButton</span>: <span class="hljs-literal">false</span>,          <span class="hljs-comment">// Home 按钮</span><br>  <span class="hljs-attr">sceneModePicker</span>: <span class="hljs-literal">true</span>,      <span class="hljs-comment">// 投影方式控制器（保留 2D/3D 切换）</span><br>  <span class="hljs-attr">baseLayerPicker</span>: <span class="hljs-literal">false</span>,     <span class="hljs-comment">// 图层选择控制器</span><br>  <span class="hljs-attr">navigationHelpButton</span>: <span class="hljs-literal">false</span>,<span class="hljs-comment">// 帮助按钮</span><br>  <span class="hljs-attr">fullscreenButton</span>: <span class="hljs-literal">false</span>,    <span class="hljs-comment">// 全屏按钮</span><br>  <span class="hljs-attr">infoBox</span>: <span class="hljs-literal">false</span>,             <span class="hljs-comment">// 信息框</span><br>  <span class="hljs-attr">selectionIndicator</span>: <span class="hljs-literal">false</span>,  <span class="hljs-comment">// 选择指示器</span><br>  <span class="hljs-attr">imageryLayers</span>: <span class="hljs-literal">false</span>,       <span class="hljs-comment">// 不加载默认底图</span><br>  <span class="hljs-attr">imageryProvider</span>: <span class="hljs-literal">false</span>,<br>  <span class="hljs-attr">scene</span>: &#123; <span class="hljs-attr">msaaSamples</span>: <span class="hljs-number">4</span> &#125;   <span class="hljs-comment">// 多重采样抗锯齿</span><br> &#125;);<br><br> <span class="hljs-comment">// 高分屏下保持清晰度</span><br> <span class="hljs-keyword">if</span> (<span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">FeatureDetection</span>.<span class="hljs-title function_">supportsImageRenderingPixelated</span>()) &#123;<br>  viewer.<span class="hljs-property">resolutionScale</span> = <span class="hljs-variable language_">window</span>.<span class="hljs-property">devicePixelRatio</span>;<br> &#125;<br> <span class="hljs-comment">// 再加一层 FXAA 抗锯齿</span><br> viewer.<span class="hljs-property">scene</span>.<span class="hljs-property">postProcessStages</span>.<span class="hljs-property">fxaa</span>.<span class="hljs-property">enabled</span> = <span class="hljs-literal">true</span>;<br><br> <span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span> = viewer;<br> <span class="hljs-comment">// 初始视角：全部来自运行时配置</span><br> <span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span>.<span class="hljs-property">camera</span>.<span class="hljs-title function_">setView</span>(&#123;<br>  <span class="hljs-attr">destination</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Cartesian3</span>.<span class="hljs-title function_">fromDegrees</span>(...<span class="hljs-variable language_">window</span>.<span class="hljs-property">baseConfig</span>.<span class="hljs-property">cesiumCameraView</span>.<span class="hljs-property">destination</span>),<br>  <span class="hljs-attr">orientation</span>: &#123;<br>   <span class="hljs-attr">heading</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Math</span>.<span class="hljs-title function_">toRadians</span>(<span class="hljs-variable language_">window</span>.<span class="hljs-property">baseConfig</span>.<span class="hljs-property">cesiumCameraView</span>.<span class="hljs-property">orientation</span>.<span class="hljs-property">heading</span>),<br>   <span class="hljs-attr">pitch</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Math</span>.<span class="hljs-title function_">toRadians</span>(<span class="hljs-variable language_">window</span>.<span class="hljs-property">baseConfig</span>.<span class="hljs-property">cesiumCameraView</span>.<span class="hljs-property">orientation</span>.<span class="hljs-property">pitch</span>),<br>   <span class="hljs-attr">roll</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Math</span>.<span class="hljs-title function_">toRadians</span>(<span class="hljs-variable language_">window</span>.<span class="hljs-property">baseConfig</span>.<span class="hljs-property">cesiumCameraView</span>.<span class="hljs-property">orientation</span>.<span class="hljs-property">roll</span>)<br>  &#125;<br> &#125;);<br>&#125;;<br></code></pre></td></tr></table></figure>

<p>几个点单独说：</p>
<p><strong><code>Ion.defaultServer = &#39;undefined&#39;</code>——字符串，不是值</strong>。这是全项目最”邪门”也最有效的一行。<code>config.js</code> 里 <code>defaultPreventServer: &#39;undefined&#39;</code>，赋给 <code>Ion.defaultServer</code> 后所有指向 <code>api.cesium.com</code> 的资产请求都变成了对 <code>undefined/...</code> 的请求，直接快速失败。内网环境没有外网，与其让每个请求等超时，不如让它们立刻死掉。配合 <code>imageryProvider: false</code>，球上是干干净净的椭球面——底图完全由我们自己的图层系统接管（第 3 节）。</p>
<p><strong>初始视角全部配置化</strong>。<code>config.js</code> 里：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-attr">cesiumCameraView</span>: &#123;<br> <span class="hljs-attr">destination</span>: [<span class="hljs-number">121.1</span>, <span class="hljs-number">23.75</span>, <span class="hljs-number">400000</span>], <span class="hljs-comment">// 经度、纬度、高度（米）</span><br> <span class="hljs-attr">orientation</span>: &#123; <span class="hljs-attr">heading</span>: <span class="hljs-number">0</span>, <span class="hljs-attr">pitch</span>: -<span class="hljs-number">90</span>, <span class="hljs-attr">roll</span>: <span class="hljs-number">0</span> &#125;, <span class="hljs-comment">// -90 = 垂直朝下</span><br> <span class="hljs-attr">VIEW_2D_HEIGHT</span>: <span class="hljs-number">5000000</span>,   <span class="hljs-comment">// 2D 模式预设高度</span><br> <span class="hljs-attr">VIEW_3D_HEIGHT</span>: <span class="hljs-number">10000000</span>   <span class="hljs-comment">// 3D 模式预设高度</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>换部署区域只改这三个数字，不碰代码。</p>
<p><strong>2D&#x2F;3D 切换的视角适配</strong>。保留了 <code>sceneModePicker</code>，但 2D 和 3D 需要的视野高度差着一个量级，所以用 <code>postRender</code> 监听场景模式变化，切完自动 <code>flyTo</code> 到各自的预设高度：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">let</span> lastMode = <span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span>.<span class="hljs-property">scene</span>.<span class="hljs-property">mode</span>;<br><span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span>.<span class="hljs-property">scene</span>.<span class="hljs-property">postRender</span>.<span class="hljs-title function_">addEventListener</span>(<span class="hljs-function">() =&gt;</span> &#123;<br> <span class="hljs-keyword">const</span> currentMode = <span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span>.<span class="hljs-property">scene</span>.<span class="hljs-property">mode</span>;<br> <span class="hljs-keyword">if</span> (currentMode !== lastMode) &#123;<br>  lastMode = currentMode;<br>  <span class="hljs-keyword">const</span> targetView = currentMode === <span class="hljs-title class_">Cesium</span>.<span class="hljs-property">SceneMode</span>.<span class="hljs-property">SCENE3D</span> ? globalView : customView;<br>  <span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span>.<span class="hljs-property">camera</span>.<span class="hljs-title function_">flyTo</span>(&#123; ...targetView, <span class="hljs-attr">duration</span>: <span class="hljs-number">0.5</span> &#125;);<br> &#125;<br>&#125;);<br></code></pre></td></tr></table></figure>

<p><strong><code>depthTestAgainstTerrain = false</code></strong>。教程里这句通常教你怎么开（配合地形），我们项目反过来是<strong>显式关掉</strong>：没有地形也没有 3D Tiles 时开启深度检测，实体的空间坐标反而会出现”消失&#x2F;漂移”的诡异问题。没有地形，就别装作有地形。</p>
<p>最后，初始化完成时广播一个自定义事件，让其它组件（图层管理、树组件）等到 viewer 就绪再干活：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">onMounted</span>(<span class="hljs-title function_">async</span> () =&gt; &#123;<br> <span class="hljs-keyword">await</span> <span class="hljs-title function_">nextTick</span>();<br> <span class="hljs-title function_">initMap</span>();<br> <span class="hljs-variable language_">window</span>.<span class="hljs-title function_">dispatchEvent</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">CustomEvent</span>(<span class="hljs-string">&#x27;viewer-ready&#x27;</span>));<br>&#125;);<br></code></pre></td></tr></table></figure>

<p><a id="sec3"></a></p>
<h2 id="3-影像底图加载：配置驱动的多源图层"><a href="#3-影像底图加载：配置驱动的多源图层" class="headerlink" title="3. 影像底图加载：配置驱动的多源图层"></a>3. 影像底图加载：配置驱动的多源图层</h2><p>底图的需求看起来简单，实际约束不少：要支持多种协议（内网自建瓦片的 XYZ、OGC 标准的 WMS&#x2F;WMTS、ArcGIS 服务）、要能勾选开关、透明度亮度可调、层级可控、<strong>用户勾选状态要记住</strong>（下次打开还是上次的组合）、并且全部要配置化——实施人员改 <code>config.js</code> 就能加图层，不用发版。</p>
<h3 id="3-1-图层即配置"><a href="#3-1-图层即配置" class="headerlink" title="3.1 图层即配置"></a>3.1 图层即配置</h3><p><code>config.js</code> 里每个图层是 <code>layers</code> 数组的一项：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-attr">layers</span>: [<br> &#123;<br>  <span class="hljs-attr">id</span>: <span class="hljs-string">&quot;wms_demo&quot;</span>,           <span class="hljs-comment">// 唯一 id，删除/排序都靠它</span><br>  <span class="hljs-attr">title</span>: <span class="hljs-string">&quot;WMS-GeoServer&quot;</span>,   <span class="hljs-comment">// 界面显示名</span><br>  <span class="hljs-attr">type</span>: <span class="hljs-string">&quot;WMS&quot;</span>,              <span class="hljs-comment">// XYZ / WMS / WMTS / ArcGIS 四选一</span><br>  <span class="hljs-attr">visible</span>: <span class="hljs-literal">false</span>,           <span class="hljs-comment">// 初始是否加载</span><br>  <span class="hljs-attr">options</span>: &#123;<br>   <span class="hljs-attr">url</span>: <span class="hljs-string">&quot;https://gibs.earthdata.nasa.gov/wms/epsg4326/best/wms.cgi&quot;</span>,<br>   <span class="hljs-attr">layers</span>: <span class="hljs-string">&#x27;BlueMarble_NextGeneration&#x27;</span>,<br>   <span class="hljs-attr">parameters</span>: &#123; <span class="hljs-attr">transparent</span>: <span class="hljs-literal">false</span>, <span class="hljs-attr">format</span>: <span class="hljs-string">&#x27;image/jpeg&#x27;</span> &#125;,<br>   <span class="hljs-attr">minimumLevel</span>: <span class="hljs-number">0</span>,<br>   <span class="hljs-attr">maximumLevel</span>: <span class="hljs-number">20</span><br>  &#125;,<br>  <span class="hljs-attr">display</span>: &#123; <span class="hljs-attr">alpha</span>: <span class="hljs-number">0.75</span>, <span class="hljs-attr">brightness</span>: <span class="hljs-number">1.2</span>, <span class="hljs-attr">zIndex</span>: <span class="hljs-number">1</span> &#125; <span class="hljs-comment">// 显示控制</span><br> &#125;,<br> &#123;<br>  <span class="hljs-attr">id</span>: <span class="hljs-string">&quot;xyz_osm&quot;</span>,<br>  <span class="hljs-attr">title</span>: <span class="hljs-string">&quot;台湾&quot;</span>,<br>  <span class="hljs-attr">type</span>: <span class="hljs-string">&quot;XYZ&quot;</span>,<br>  <span class="hljs-attr">visible</span>: <span class="hljs-literal">true</span>,<br>  <span class="hljs-attr">options</span>: &#123; <span class="hljs-attr">url</span>: <span class="hljs-string">&quot;http://your-tile-server/maps/tw/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;.jpg&quot;</span>, <span class="hljs-attr">maximumLevel</span>: <span class="hljs-number">20</span> &#125;,<br>  <span class="hljs-attr">display</span>: &#123; <span class="hljs-attr">zIndex</span>: <span class="hljs-number">2</span> &#125;<br> &#125;<br>]<br></code></pre></td></tr></table></figure>

<h3 id="3-2-Provider-工厂"><a href="#3-2-Provider-工厂" class="headerlink" title="3.2 Provider 工厂"></a>3.2 Provider 工厂</h3><p>四种协议对应 Cesium 四种 ImageryProvider，一个 switch 分发完事：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/utils/layerFactory.ts</span><br><span class="hljs-keyword">import</span> <span class="hljs-keyword">type</span> &#123; <span class="hljs-title class_">LayerConfig</span> &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;@/types/cesium&#x27;</span>;<br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">createProvider</span>(<span class="hljs-params"><span class="hljs-attr">cfg</span>: <span class="hljs-title class_">LayerConfig</span></span>) &#123;<br> <span class="hljs-keyword">switch</span> (cfg.<span class="hljs-property">type</span>) &#123;<br>  <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;XYZ&#x27;</span>:<br>   <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">UrlTemplateImageryProvider</span>(cfg.<span class="hljs-property">options</span>);<br>  <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;WMS&#x27;</span>:<br>   <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">WebMapServiceImageryProvider</span>(cfg.<span class="hljs-property">options</span>);<br>  <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;WMTS&#x27;</span>:<br>   <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">WebMapTileServiceImageryProvider</span>(cfg.<span class="hljs-property">options</span>);<br>  <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;ArcGIS&#x27;</span>:<br>   <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">ArcGisMapServerImageryProvider</span>(cfg.<span class="hljs-property">options</span>);<br>  <span class="hljs-attr">default</span>:<br>   <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">`未知图层类型 <span class="hljs-subst">$&#123;(cfg <span class="hljs-keyword">as</span> <span class="hljs-built_in">any</span>).<span class="hljs-keyword">type</span>&#125;</span>`</span>);<br> &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>顺带放一份各家的瓦片地址模板，都是项目里实际用过&#x2F;验证过的，抄作业就能用：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text"># 天地图 WMTS（注记层 cia，需换成自己的 tk）<br>https://t0.tianditu.gov.cn/cia_w/wmts?tk=你的token<br>  layer: &#x27;cia&#x27;, style: &#x27;default&#x27;, tileMatrixSetID: &#x27;w&#x27;, format: &#x27;tiles&#x27;<br># 天地图 XYZ 写法（等价）<br>https://t0.tianditu.gov.cn/DataServer?T=cia_w&amp;x=&#123;x&#125;&amp;y=&#123;y&#125;&amp;l=&#123;z&#125;&amp;tk=你的token<br><br># ArcGIS World_Imagery（注意是 &#123;z&#125;/&#123;y&#125;/&#123;x&#125;，y 在 x 前面！）<br>https://services.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/&#123;z&#125;/&#123;y&#125;/&#123;x&#125;<br><br># 高德（卫片 style=6 / 路网+地名 style=8）<br>http://webst01.is.autonavi.com/appmaptile?style=6&amp;x=&#123;x&#125;&amp;y=&#123;y&#125;&amp;z=&#123;z&#125;<br></code></pre></td></tr></table></figure>

<h3 id="3-3-添加、删除与排序"><a href="#3-3-添加、删除与排序" class="headerlink" title="3.3 添加、删除与排序"></a>3.3 添加、删除与排序</h3><p>图层管理的核心逻辑在头部组件里。添加时把配置里的 <code>display</code> 逐项应用到 <code>ImageryLayer</code> 上，并给图层实例<strong>打两个私有标记</strong>——这是整个模块的小机灵：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-comment">// src/pages/home/components/home-header/index.vue（有删减）</span><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">addLayer</span> = (<span class="hljs-params">cfg: LayerConfig</span>) =&gt; &#123;<br> <span class="hljs-keyword">const</span> provider = <span class="hljs-title function_">createProvider</span>(cfg);<br> <span class="hljs-keyword">const</span> layer = viewer.<span class="hljs-property">imageryLayers</span>.<span class="hljs-title function_">addImageryProvider</span>(provider);<br> <span class="hljs-keyword">if</span> (cfg.<span class="hljs-property">options</span>.<span class="hljs-property">minimumLevel</span> !== <span class="hljs-literal">undefined</span>) layer.<span class="hljs-property">minimumLevel</span> = cfg.<span class="hljs-property">options</span>.<span class="hljs-property">minimumLevel</span>;<br> <span class="hljs-keyword">if</span> (cfg.<span class="hljs-property">options</span>.<span class="hljs-property">maximumLevel</span> !== <span class="hljs-literal">undefined</span>) layer.<span class="hljs-property">maximumLevel</span> = cfg.<span class="hljs-property">options</span>.<span class="hljs-property">maximumLevel</span>;<br><br> <span class="hljs-keyword">const</span> d = cfg.<span class="hljs-property">display</span>;<br> <span class="hljs-keyword">if</span> (d?.<span class="hljs-property">alpha</span> !== <span class="hljs-literal">undefined</span>) layer.<span class="hljs-property">alpha</span> = d.<span class="hljs-property">alpha</span>;<br> <span class="hljs-keyword">if</span> (d?.<span class="hljs-property">brightness</span> !== <span class="hljs-literal">undefined</span>) layer.<span class="hljs-property">brightness</span> = d.<span class="hljs-property">brightness</span>;<br><br> <span class="hljs-comment">/* 关键：打标记，用于后续精准删除 / 排序 */</span><br> layer[<span class="hljs-string">&#x27;__layerId&#x27;</span>] = cfg.<span class="hljs-property">id</span>;<br> layer[<span class="hljs-string">&#x27;__zIndex&#x27;</span>] = d?.<span class="hljs-property">zIndex</span> ?? <span class="hljs-number">0</span>;<br>&#125;;<br></code></pre></td></tr></table></figure>

<ul>
<li><strong>删除</strong>：<code>removeLayer(id)</code> 倒序遍历 <code>imageryLayers</code>，按 <code>__layerId</code> 精确匹配后 <code>imageryLayers.remove(layer, true)</code>。Cesium 的图层对象没有业务 id，不打标记就只能按下标删，一排序就全乱。</li>
<li><strong>排序</strong>：<code>imageryLayers</code> 是个栈式结构（越后添加越在上层），没有直接的 zIndex 概念。项目用两步模拟：先把所有带标记的图层 <code>lowerToBottom</code> 沉底，再按 zIndex 升序依次 <code>raiseToTop</code> 抬起——两轮循环后层级就是 zIndex 的顺序：</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> <span class="hljs-title function_">reorderLayersByZIndex</span> = (<span class="hljs-params"></span>) =&gt; &#123;<br> <span class="hljs-keyword">const</span> list = [];<br> <span class="hljs-keyword">for</span> (<span class="hljs-keyword">let</span> i = <span class="hljs-number">0</span>; i &lt; viewer.<span class="hljs-property">imageryLayers</span>.<span class="hljs-property">length</span>; i++) &#123;<br>  <span class="hljs-keyword">const</span> layer = viewer.<span class="hljs-property">imageryLayers</span>.<span class="hljs-title function_">get</span>(i);<br>  <span class="hljs-keyword">if</span> (layer[<span class="hljs-string">&#x27;__layerId&#x27;</span>]) list.<span class="hljs-title function_">push</span>(&#123; layer, <span class="hljs-attr">z</span>: layer[<span class="hljs-string">&#x27;__zIndex&#x27;</span>] ?? <span class="hljs-number">0</span> &#125;);<br> &#125;<br> list.<span class="hljs-title function_">sort</span>(<span class="hljs-function">(<span class="hljs-params">a, b</span>) =&gt;</span> a.<span class="hljs-property">z</span> - b.<span class="hljs-property">z</span>); <span class="hljs-comment">// Cesium 里越后越上</span><br> list.<span class="hljs-title function_">forEach</span>(<span class="hljs-function">(<span class="hljs-params">&#123; layer &#125;</span>) =&gt;</span> viewer.<span class="hljs-property">imageryLayers</span>.<span class="hljs-title function_">lowerToBottom</span>(layer));<br> list.<span class="hljs-title function_">forEach</span>(<span class="hljs-function">(<span class="hljs-params">&#123; layer &#125;</span>) =&gt;</span> viewer.<span class="hljs-property">imageryLayers</span>.<span class="hljs-title function_">raiseToTop</span>(layer));<br>&#125;;<br></code></pre></td></tr></table></figure>

<ul>
<li><strong>持久化</strong>：每次勾选变化，把整个图层配置数组（含最新的 <code>visible</code>）写进 <code>localStorage(&#39;layerConfig&#39;)</code>；页面初始化时优先读 localStorage、没有才用 <code>config.js</code> 的默认值。这里有个真实的坑，<code>config.js</code> 第一行注释就在警告它：<strong>改了 config.js 的图层配置但浏览器没清缓存，是不会生效的</strong>——localStorage 里的旧配置永远优先。</li>
</ul>
<p>图层组件初始化同样要等 viewer：<code>onMounted</code> 时如果 <code>window.viewer</code> 已存在就直接加载，否则监听 <code>viewer-ready</code> 事件后再按 <code>visible: true</code> 的配置逐个 <code>addLayer</code>。</p>
<p><a id="sec4"></a></p>
<h2 id="4-业务数据上球：为什么放弃-Entity，改用-Primitive"><a href="#4-业务数据上球：为什么放弃-Entity，改用-Primitive" class="headerlink" title="4. 业务数据上球：为什么放弃 Entity，改用 Primitive"></a>4. 业务数据上球：为什么放弃 Entity，改用 Primitive</h2><h3 id="4-1-数据长什么样"><a href="#4-1-数据长什么样" class="headerlink" title="4.1 数据长什么样"></a>4.1 数据长什么样</h3><p>后端接口不返回 GeoJSON 文件，返回的是按设施表分组的业务 JSON：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-punctuation">&#123;</span><br> <span class="hljs-attr">&quot;TRANSFORMER_STATION&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">[</span><br>  <span class="hljs-punctuation">&#123;</span> <span class="hljs-attr">&quot;id&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;xx1&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-attr">&quot;name&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;XX变电站&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-attr">&quot;geoType&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;points&quot;</span><span class="hljs-punctuation">,</span>  <span class="hljs-attr">&quot;geo&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">[</span><span class="hljs-number">120.98</span><span class="hljs-punctuation">,</span> <span class="hljs-number">23.64</span><span class="hljs-punctuation">]</span> <span class="hljs-punctuation">&#125;</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-punctuation">&#123;</span> <span class="hljs-attr">&quot;id&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;xx2&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-attr">&quot;name&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;XX线路&quot;</span><span class="hljs-punctuation">,</span>   <span class="hljs-attr">&quot;geoType&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;lines&quot;</span><span class="hljs-punctuation">,</span>   <span class="hljs-attr">&quot;geo&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">[</span><span class="hljs-punctuation">[</span><span class="hljs-number">120.9</span><span class="hljs-punctuation">,</span> <span class="hljs-number">23.6</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span> <span class="hljs-punctuation">[</span><span class="hljs-number">121.0</span><span class="hljs-punctuation">,</span> <span class="hljs-number">23.7</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">]</span> <span class="hljs-punctuation">&#125;</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-punctuation">&#123;</span> <span class="hljs-attr">&quot;id&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;xx3&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-attr">&quot;name&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;XX园区&quot;</span><span class="hljs-punctuation">,</span>   <span class="hljs-attr">&quot;geoType&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;polygons&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-attr">&quot;geo&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">[</span><span class="hljs-punctuation">[</span><span class="hljs-punctuation">[</span><span class="hljs-number">120.9</span><span class="hljs-punctuation">,</span> <span class="hljs-number">23.6</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span> <span class="hljs-punctuation">[</span><span class="hljs-number">121.0</span><span class="hljs-punctuation">,</span> <span class="hljs-number">23.7</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span> <span class="hljs-punctuation">[</span><span class="hljs-number">120.95</span><span class="hljs-punctuation">,</span> <span class="hljs-number">23.8</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">]</span> <span class="hljs-punctuation">&#125;</span><br> <span class="hljs-punctuation">]</span><br><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure>

<p><code>geoType</code> 决定渲染成点&#x2F;线&#x2F;面，<code>geo</code> 的坐标格式并不统一（多面、单面、GeoJSON 对象、lon&#x2F;lat 字段都有）。这决定了两件事：<code>GeoJsonDataSource.load()</code> 这条捷径走不通；坐标解析必须自己写兼容层。</p>
<h3 id="4-2-Entity-还是-Primitive"><a href="#4-2-Entity-还是-Primitive" class="headerlink" title="4.2 Entity 还是 Primitive"></a>4.2 Entity 还是 Primitive</h3><p>Cesium 渲染业务数据有两条 API 路线：</p>
<table>
<thead>
<tr>
<th></th>
<th>Entity API</th>
<th>Primitive API</th>
</tr>
</thead>
<tbody><tr>
<td>抽象级别</td>
<td>高，声明式（<code>viewer.entities.add({polygon: ...})</code>）</td>
<td>低，手动组装 GeometryInstance + Appearance</td>
</tr>
<tr>
<td>渲染时机</td>
<td>每帧根据属性动态更新</td>
<td>一次性 GPU 资源，静态高效</td>
</tr>
<tr>
<td>性能</td>
<td>几千个实体就开始吃力</td>
<td>数万级实体无压力</td>
</tr>
<tr>
<td>修改样式</td>
<td>直接改属性，立即生效</td>
<td>通常要销毁重建</td>
</tr>
</tbody></table>
<p>项目早期用的就是 Entity + DataSource，实体量上来之后（单视口几千个设施、后面还有拓扑网络叠加）帧率撑不住，于是整体迁移到 Primitive——项目源码里还留着那次迁移的注释痕迹（”换成 primitives”）。迁移的代价是样式交互全要自己管：选中高亮、hover 恢复这些 Entity 白送的能力，都得围绕 <code>scene.pick</code> 重新设计。换来的是渲染性能和完全的细节控制（<code>renderOrder</code>、自定义材质）。</p>
<h3 id="4-3-坐标解析：一层兼容各种后端历史格式"><a href="#4-3-坐标解析：一层兼容各种后端历史格式" class="headerlink" title="4.3 坐标解析：一层兼容各种后端历史格式"></a>4.3 坐标解析：一层兼容各种后端历史格式</h3><figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/pages/home/composables/handleDatas.ts（有删减）</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> <span class="hljs-title function_">processCoordinates</span> = (<span class="hljs-params"><span class="hljs-attr">dataItem</span>: <span class="hljs-built_in">any</span>, <span class="hljs-attr">geometryType</span>: <span class="hljs-title class_">LayerType</span></span>) =&gt; &#123;<br> <span class="hljs-keyword">let</span> <span class="hljs-attr">positions</span>: <span class="hljs-built_in">number</span>[][] = [];<br> <span class="hljs-keyword">const</span> rawGeo = dataItem.<span class="hljs-property">geo</span> || dataItem.<span class="hljs-property">properties</span>.<span class="hljs-property">geo</span>;<br><br> <span class="hljs-keyword">if</span> (geometryType === <span class="hljs-string">&#x27;polygons&#x27;</span>) &#123;<br>  <span class="hljs-comment">// [[[lon, lat]]] → 多面，取第一个面；[[lon, lat]] → 单个面</span><br>  <span class="hljs-keyword">if</span> (<span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(rawGeo) &amp;&amp; <span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(rawGeo[<span class="hljs-number">0</span>]) &amp;&amp; <span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(rawGeo[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>])) &#123;<br>   positions = rawGeo[<span class="hljs-number">0</span>][<span class="hljs-number">0</span>];<br>  &#125; <span class="hljs-keyword">else</span> &#123;<br>   positions = rawGeo;<br>  &#125;<br> &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (geometryType === <span class="hljs-string">&#x27;lines&#x27;</span>) &#123;<br>  positions = rawGeo[<span class="hljs-number">0</span>];<br> &#125; <span class="hljs-keyword">else</span> &#123;<br>  <span class="hljs-comment">// 点：兼容 [lon, lat]、[[lon, lat]]、&#123;coordinates: [...]&#125;、&#123;lon, lat&#125; 四种</span><br>  <span class="hljs-keyword">if</span> (<span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(rawGeo)) &#123;<br>   positions = <span class="hljs-keyword">typeof</span> rawGeo[<span class="hljs-number">0</span>] === <span class="hljs-string">&#x27;number&#x27;</span> ? [rawGeo] : rawGeo;<br>  &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (rawGeo?.<span class="hljs-property">coordinates</span>) &#123;<br>   positions = <span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(rawGeo.<span class="hljs-property">coordinates</span>[<span class="hljs-number">0</span>])<br>    ? rawGeo.<span class="hljs-property">coordinates</span><br>    : [rawGeo.<span class="hljs-property">coordinates</span>];<br>  &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (dataItem.<span class="hljs-property">lon</span> &amp;&amp; dataItem.<span class="hljs-property">lat</span>) &#123;<br>   positions = [[dataItem.<span class="hljs-property">lon</span>, dataItem.<span class="hljs-property">lat</span>]];<br>  &#125;<br> &#125;<br> <span class="hljs-keyword">return</span> &#123; positions &#125;;<br>&#125;;<br></code></pre></td></tr></table></figure>

<p>解析出的经纬度统一转 <code>Cartesian3.fromDegrees</code>，再用 <code>BoundingSphere.fromPoints</code> 求几何中心（面和线需要中心点挂图标&#x2F;标签）：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// BoundingSphere 包围所有顶点，球心即几何中心</span><br><span class="hljs-keyword">const</span> boundingSphere = <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">BoundingSphere</span>.<span class="hljs-title function_">fromPoints</span>(cartesianPositions);<br><span class="hljs-keyword">const</span> cartographic = <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Cartographic</span>.<span class="hljs-title function_">fromCartesian</span>(boundingSphere.<span class="hljs-property">center</span>);<br></code></pre></td></tr></table></figure>

<h3 id="4-4-三种几何的-Primitive-工厂"><a href="#4-4-三种几何的-Primitive-工厂" class="headerlink" title="4.4 三种几何的 Primitive 工厂"></a>4.4 三种几何的 Primitive 工厂</h3><p><strong>点 → BillboardCollection</strong>。点不用 Primitive，用 <code>BillboardCollection</code> 里的一条 billboard：图标 URL 来自后端样式接口（<code>baseApiUrl + style.image</code>），<code>disableDepthTestDistance: Number.POSITIVE_INFINITY</code> 让图标永远不被地形&#x2F;其它几何裁剪——电力设施图标被山体挡住是不能接受的：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/pages/home/composables/pointPrimitive.ts（有删减）</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> <span class="hljs-title function_">createPointPrimitive</span> = (<span class="hljs-params"><span class="hljs-attr">data</span>: <span class="hljs-built_in">any</span>, <span class="hljs-attr">position</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-title class_">Cesium</span>.<span class="hljs-title class_">Cartesian3</span>, <span class="hljs-attr">style</span>: <span class="hljs-built_in">any</span></span>) =&gt; &#123;<br> <span class="hljs-keyword">return</span> &#123;<br>  <span class="hljs-attr">billboardOptions</span>: &#123;<br>   <span class="hljs-attr">id</span>: &#123; <span class="hljs-attr">customAttributes</span>: data &#125;,          <span class="hljs-comment">// 业务数据全塞进 id，拾取时取回</span><br>   <span class="hljs-attr">position</span>: position,<br>   <span class="hljs-attr">image</span>: baseApiUrl + _style.<span class="hljs-property">image</span>,<br>   <span class="hljs-attr">width</span>: _style.<span class="hljs-property">width</span>,<br>   <span class="hljs-attr">height</span>: _style.<span class="hljs-property">height</span>,<br>   <span class="hljs-attr">verticalOrigin</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">VerticalOrigin</span>.<span class="hljs-property">TOP</span>,<br>   <span class="hljs-attr">heightReference</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">HeightReference</span>.<span class="hljs-property">NONE</span>,<br>   <span class="hljs-attr">disableDepthTestDistance</span>: <span class="hljs-title class_">Number</span>.<span class="hljs-property">POSITIVE_INFINITY</span> <span class="hljs-comment">// 👈 避免远处被裁剪</span><br>  &#125;<br> &#125;;<br>&#125;;<br></code></pre></td></tr></table></figure>

<p>注意 <code>id</code> 字段被用来挂 <code>customAttributes</code>——Cesium 拾取（<code>scene.pick</code>）返回的对象会带上这个 id，业务数据就在这一刻”回到”前端手里。这是 Primitive 路线里传递业务上下文的标准手法。</p>
<p><strong>面 → PolygonGeometry + EllipsoidSurfaceAppearance</strong>。面创建完还要在中心点补一个 billboard 图标，并且和面建立<strong>双向关联</strong>（点图标 → <code>associatedPrimitive</code> 指回面，面 → <code>associatedBillboard</code> 指向图标），hover 图标时高亮整个面：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/pages/home/composables/polygonPrimitive.ts（有删减）</span><br><span class="hljs-keyword">const</span> instance = <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">GeometryInstance</span>(&#123;<br> <span class="hljs-attr">geometry</span>: <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">PolygonGeometry</span>(&#123;<br>  <span class="hljs-attr">polygonHierarchy</span>: <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">PolygonHierarchy</span>(positions),<br>  <span class="hljs-attr">vertexFormat</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">PerInstanceColorAppearance</span>.<span class="hljs-property">VERTEX_FORMAT</span>,<br>  <span class="hljs-attr">perPositionHeight</span>: <span class="hljs-literal">false</span>,<br>  <span class="hljs-attr">height</span>: <span class="hljs-number">20</span> <span class="hljs-comment">// 离地高度，避免和底图 z-fighting</span><br> &#125;),<br> <span class="hljs-attr">id</span>: &#123; <span class="hljs-attr">customAttributes</span>: data &#125;<br>&#125;);<br><br><span class="hljs-keyword">const</span> polygon = <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">Primitive</span>(&#123;<br> <span class="hljs-attr">geometryInstances</span>: instance,<br> <span class="hljs-attr">appearance</span>: <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">EllipsoidSurfaceAppearance</span>(&#123;<br>  <span class="hljs-attr">material</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Material</span>.<span class="hljs-title function_">fromType</span>(<span class="hljs-string">&#x27;Color&#x27;</span>, &#123;<br>   <span class="hljs-attr">color</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Color</span>.<span class="hljs-title function_">fromCssColorString</span>(style?.<span class="hljs-property">material</span> || <span class="hljs-string">&#x27;#97FFFF&#x27;</span>)<br>  &#125;),<br>  <span class="hljs-attr">translucent</span>: <span class="hljs-literal">true</span><br> &#125;),<br> <span class="hljs-attr">allowPicking</span>: <span class="hljs-literal">true</span>,    <span class="hljs-comment">// ✅ 显式启用拾取</span><br> <span class="hljs-attr">asynchronous</span>: <span class="hljs-literal">false</span>,   <span class="hljs-comment">// ✅ 同步创建，立即可拾取</span><br> <span class="hljs-attr">renderOrder</span>: <span class="hljs-number">0</span>         <span class="hljs-comment">// 面在底层</span><br>&#125;);<br></code></pre></td></tr></table></figure>

<p><code>asynchronous: false</code> 值得展开一句：默认为 true 时几何在 Web Worker 里分帧构建，创建后的一两帧内 <code>scene.pick</code> 拾取不到它，用户刚画完就点会”点空”。数据量不大时关掉异步，换来即时可交互。</p>
<p><strong>线 → PolylineGeometry + PolylineMaterialAppearance</strong>。线有两个特殊处理：<code>renderOrder: 100</code> 保证线画在面之上；<code>customAttributes.defaultStyle</code> 把当前样式快照存进 primitive，hover 恢复时不用再查样式表：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/pages/home/composables/linePrimitive.ts（有删减）</span><br><span class="hljs-keyword">const</span> polyline = <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">Primitive</span>(&#123;<br> <span class="hljs-attr">geometryInstances</span>: [<span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">GeometryInstance</span>(&#123;<br>  <span class="hljs-attr">geometry</span>: <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">PolylineGeometry</span>(&#123;<br>   positions,<br>   <span class="hljs-attr">width</span>: lineWidth,<br>   <span class="hljs-attr">vertexFormat</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">PolylineMaterialAppearance</span>.<span class="hljs-property">VERTEX_FORMAT</span><br>  &#125;)<br> &#125;)],<br> <span class="hljs-attr">appearance</span>: <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">PolylineMaterialAppearance</span>(&#123; material &#125;),<br> <span class="hljs-attr">renderOrder</span>: <span class="hljs-number">100</span> <span class="hljs-comment">// 线永远在面之上</span><br>&#125;);<br>(polyline <span class="hljs-keyword">as</span> <span class="hljs-built_in">any</span>).<span class="hljs-property">customAttributes</span> = &#123; ...data, defaultStyle &#125;; <span class="hljs-comment">// ✅ 保存默认样式用于恢复</span><br></code></pre></td></tr></table></figure>

<h3 id="4-5-全局集合：实体的”户口”统一管理"><a href="#4-5-全局集合：实体的”户口”统一管理" class="headerlink" title="4.5 全局集合：实体的”户口”统一管理"></a>4.5 全局集合：实体的”户口”统一管理</h3><p>所有 primitive 进出两个全局集合，而不是直接 <code>scene.primitives.add()</code>。集合挂在 scene 上一次，之后只操作集合，销毁、清空、遍历都有唯一入口：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/pages/home/composables/use-load-data.ts</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">let</span> <span class="hljs-attr">globalPrimitiveCollection</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">PrimitiveCollection</span> | <span class="hljs-literal">null</span> = <span class="hljs-literal">null</span>;<br><span class="hljs-keyword">export</span> <span class="hljs-keyword">let</span> <span class="hljs-attr">globalBillboardCollection</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">BillboardCollection</span> | <span class="hljs-literal">null</span> = <span class="hljs-literal">null</span>;<br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">initCollections</span>(<span class="hljs-params"></span>) &#123;<br> <span class="hljs-keyword">if</span> (!globalPrimitiveCollection) &#123;<br>  globalPrimitiveCollection = <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">PrimitiveCollection</span>();<br>  globalPrimitiveCollection.<span class="hljs-property">destroyPrimitives</span> = <span class="hljs-literal">true</span>; <span class="hljs-comment">// 移除时自动销毁内部 primitive</span><br> &#125;<br> <span class="hljs-keyword">if</span> (!globalBillboardCollection) &#123;<br>  globalBillboardCollection = <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">BillboardCollection</span>();<br>  globalBillboardCollection.<span class="hljs-property">depthTest</span> = <span class="hljs-literal">false</span>; <span class="hljs-comment">// 图标始终显示在最上层</span><br> &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><a id="sec5"></a></p>
<h2 id="5-视口驱动的增量加载：只请求”看得见”的数据"><a href="#5-视口驱动的增量加载：只请求”看得见”的数据" class="headerlink" title="5. 视口驱动的增量加载：只请求”看得见”的数据"></a>5. 视口驱动的增量加载：只请求”看得见”的数据</h2><p>这一节是整个项目的核心。设施全量有几十万条，一次性上球既不现实也没必要——<strong>用户看到什么，就加载什么</strong>。</p>
<h3 id="5-1-触发时机：相机静止-400ms-后"><a href="#5-1-触发时机：相机静止-400ms-后" class="headerlink" title="5.1 触发时机：相机静止 400ms 后"></a>5.1 触发时机：相机静止 400ms 后</h3><p>拖动地图的过程中相机在连续变化，直接监听 <code>moveEnd</code> 会连环触发请求。两层过滤：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// use-load-data.ts · setupClickHandler 内</span><br><span class="hljs-keyword">let</span> <span class="hljs-attr">loadDebounce</span>: <span class="hljs-built_in">number</span> | <span class="hljs-literal">null</span> = <span class="hljs-literal">null</span>;<br><span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span>?.<span class="hljs-property">camera</span>.<span class="hljs-property">moveEnd</span>.<span class="hljs-title function_">addEventListener</span>(<span class="hljs-function">() =&gt;</span> &#123;<br> <span class="hljs-keyword">if</span> (loadDebounce) <span class="hljs-built_in">clearTimeout</span>(loadDebounce);<br> loadDebounce = <span class="hljs-variable language_">window</span>.<span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-title function_">handleCameraMoveEnd</span>();<br> &#125;, <span class="hljs-number">400</span>);<br>&#125;);<br></code></pre></td></tr></table></figure>

<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/pages/home/composables/handleEventPicks.ts</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> <span class="hljs-title function_">handleCameraMoveEnd</span> = (<span class="hljs-params"></span>) =&gt; &#123;<br> <span class="hljs-keyword">if</span> (cesiumlStore.<span class="hljs-property">isTopologyMode</span>) <span class="hljs-keyword">return</span>; <span class="hljs-comment">// 拓扑展示模式不打扰</span><br><br> <span class="hljs-keyword">const</span> height = <span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span>?.<span class="hljs-property">camera</span>.<span class="hljs-property">positionCartographic</span>.<span class="hljs-property">height</span>;<br> <span class="hljs-keyword">if</span> (<span class="hljs-number">1000</span> &lt; height &amp;&amp; height &lt;= <span class="hljs-number">400000</span>) &#123; <span class="hljs-comment">// 只在 1km~400km 高度之间加载</span><br>  <span class="hljs-title function_">loadData</span>();<br> &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<p>高度窗口是个业务决策：低于 1km 视口太小查不出东西还频繁触发；高于 400km（约卫星视角）视口覆盖太广，后端空间查询代价大，干脆不加载。</p>
<h3 id="5-2-视口边界：computeViewRectangle-及其三个分支"><a href="#5-2-视口边界：computeViewRectangle-及其三个分支" class="headerlink" title="5.2 视口边界：computeViewRectangle 及其三个分支"></a>5.2 视口边界：computeViewRectangle 及其三个分支</h3><p>请求参数需要”当前视口的经纬度矩形”。Cesium 提供了现成的 <code>camera.computeViewRectangle()</code>，但 2D&#x2F;3D&#x2F;Columbus 三种模式下它的行为和可靠性不同，所以 store 里做了分支处理：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/store/cesium.ts（有删减）</span><br><span class="hljs-keyword">async</span> <span class="hljs-title function_">getCartographicLimitsData</span>(<span class="hljs-params"></span>) &#123;<br> <span class="hljs-keyword">const</span> currentMode = <span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span>?.<span class="hljs-property">scene</span>.<span class="hljs-property">mode</span>;<br> <span class="hljs-keyword">const</span> is2DOrCVMode = currentMode === <span class="hljs-number">1</span> <span class="hljs-comment">/* SCENE2D */</span> || currentMode === <span class="hljs-number">2</span> <span class="hljs-comment">/* COLUMBUS_VIEW */</span>;<br><br> <span class="hljs-keyword">if</span> (is2DOrCVMode) <span class="hljs-keyword">return</span> <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">get2DViewportBounds</span>();<br> <span class="hljs-keyword">return</span> <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">get3DViewportBounds</span>();<br>&#125;<br><br><span class="hljs-title function_">get3DViewportBounds</span>(<span class="hljs-params"></span>) &#123;<br> <span class="hljs-keyword">let</span> rectangle = <span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span>?.<span class="hljs-property">camera</span>.<span class="hljs-title function_">computeViewRectangle</span>();<br> <span class="hljs-keyword">if</span> (!rectangle) <span class="hljs-keyword">return</span> <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">calculateViewportBoundsFallback</span>(); <span class="hljs-comment">// 计算失败 → 兜底</span><br><br> <span class="hljs-variable language_">this</span>.<span class="hljs-property">cartographicLimitsData</span> = &#123;<br>  <span class="hljs-attr">lowerLeftJing</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Math</span>.<span class="hljs-title function_">toDegrees</span>(rectangle.<span class="hljs-property">west</span>),<br>  <span class="hljs-attr">lowerLeftWei</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Math</span>.<span class="hljs-title function_">toDegrees</span>(rectangle.<span class="hljs-property">south</span>),<br>  <span class="hljs-attr">upperRightJing</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Math</span>.<span class="hljs-title function_">toDegrees</span>(rectangle.<span class="hljs-property">east</span>),<br>  <span class="hljs-attr">upperRightWei</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Math</span>.<span class="hljs-title function_">toDegrees</span>(rectangle.<span class="hljs-property">north</span>)<br> &#125;;<br> <span class="hljs-keyword">return</span> <span class="hljs-variable language_">this</span>.<span class="hljs-property">cartographicLimitsData</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>2D 分支除了同样的转换，还多两道修正：视口范围太小（&lt; 30°）时以中心为准扩到 30°，太大（&gt; 180°）时压回 180°——2D 模式下 <code>computeViewRectangle</code> 在极端缩放时会给出不合理的边界。</p>
<p><strong>兜底方案</strong>是按相机高度查表估一个范围（0.05° ~ 30° 共九档），当 <code>computeViewRectangle</code> 返回 undefined（相机朝天上、贴地等极端姿态）时顶上：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-title function_">calculateViewportBoundsFallback</span>(<span class="hljs-params"></span>) &#123;<br> <span class="hljs-keyword">const</span> &#123; longitude, latitude, height &#125; = <span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span>.<span class="hljs-property">camera</span>.<span class="hljs-property">positionCartographic</span>;<br> <span class="hljs-keyword">let</span> range = <span class="hljs-number">1.0</span>;<br> <span class="hljs-keyword">if</span> (height &lt; <span class="hljs-number">10000</span>) range = <span class="hljs-number">0.05</span>;         <span class="hljs-comment">// 非常近的视角</span><br> <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (height &lt; <span class="hljs-number">100000</span>) range = <span class="hljs-number">1.0</span>;    <span class="hljs-comment">// 中近视角</span><br> <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (height &lt; <span class="hljs-number">1000000</span>) range = <span class="hljs-number">3.0</span>;   <span class="hljs-comment">// 远视角</span><br> <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (height &lt; <span class="hljs-number">10000000</span>) range = <span class="hljs-number">15.0</span>; <span class="hljs-comment">// 全球视角</span><br> <span class="hljs-keyword">else</span> range = <span class="hljs-number">30.0</span>;<br> <span class="hljs-comment">// 以相机正下方为中心，range 为半径构造矩形……</span><br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="5-3-请求与组装"><a href="#5-3-请求与组装" class="headerlink" title="5.3 请求与组装"></a>5.3 请求与组装</h3><p>边界 + 勾选的设施类型（tableIds）作为参数发给后端，由后端做空间过滤：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/api/cesiumApi.ts</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">getFacilityPrimitives</span>(<span class="hljs-params"><span class="hljs-attr">data</span>: <span class="hljs-built_in">any</span></span>) &#123;<br> <span class="hljs-keyword">return</span> request&lt;<span class="hljs-built_in">any</span>, <span class="hljs-title class_">ResponseData</span>&gt;(&#123;<br>  <span class="hljs-attr">url</span>: <span class="hljs-string">`/<span class="hljs-subst">$&#123;managementModel&#125;</span>/aggregation/get/range/table`</span>,<br>  <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;POST&#x27;</span>,<br>  data <span class="hljs-comment">// &#123; tableIds: [...勾选的设施类型], boundary: &#123; lowerLeftJing, lowerLeftWei, upperRightJing, upperRightWei &#125; &#125;</span><br> &#125;);<br>&#125;<br><br><span class="hljs-comment">// use-load-data.ts</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> <span class="hljs-title function_">loadData</span> = <span class="hljs-keyword">async</span> (<span class="hljs-params"></span>) =&gt; &#123;<br> <span class="hljs-keyword">await</span> cesiumlStore.<span class="hljs-title function_">getCartographicLimitsData</span>(); <span class="hljs-comment">// 算视口边界</span><br> <span class="hljs-keyword">const</span> _data = &#123;<br>  <span class="hljs-attr">tableIds</span>: facilitiesManage.<span class="hljs-property">facilitiesCheckedList</span>,<br>  <span class="hljs-attr">boundary</span>: cesiumlStore.<span class="hljs-property">cartographicLimitsData</span><br> &#125;;<br> <span class="hljs-keyword">if</span> (_data.<span class="hljs-property">tableIds</span>.<span class="hljs-property">length</span> &gt; <span class="hljs-number">0</span> &amp;&amp; <span class="hljs-title class_">Object</span>.<span class="hljs-title function_">values</span>(cesiumlStore.<span class="hljs-property">cartographicLimitsData</span>).<span class="hljs-title function_">some</span>(<span class="hljs-function"><span class="hljs-params">v</span> =&gt;</span> v !== <span class="hljs-number">0</span>)) &#123;<br>  <span class="hljs-keyword">const</span> res = <span class="hljs-keyword">await</span> <span class="hljs-title function_">getFacilityPrimitives</span>(_data);<br>  <span class="hljs-keyword">if</span> (res.<span class="hljs-property">code</span> == <span class="hljs-number">200</span>) <span class="hljs-keyword">await</span> <span class="hljs-title function_">loadDataAsPrimitives</span>(res.<span class="hljs-property">data</span> || &#123;&#125;);<br> &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<h3 id="5-4-增量-diff：不闪、不重、不漏"><a href="#5-4-增量-diff：不闪、不重、不漏" class="headerlink" title="5.4 增量 diff：不闪、不重、不漏"></a>5.4 增量 diff：不闪、不重、不漏</h3><p><code>loadDataAsPrimitives</code> 是整个管线里最精华的函数。它的前身有两个，都在源码注释里留着”尸体”，正好构成一部踩坑进化史：</p>
<ul>
<li><strong>v1：全量加载 + 清理</strong>——每次把缓存里”不在本次结果中”的实体移除，新实体加入。问题是每次请求间隔里实体有增有减，界面出现可见的闪烁；</li>
<li><strong>v2：清空重载</strong>——干脆每次清空所有实体重新创建。闪烁更严重（线实体创建瞬间还会”抖动”），而且反复创建销毁 GeometryInstance 性能浪费；</li>
<li><strong>v3（现行）：增量 diff</strong>——三步走，只动真正变化的部分：</li>
</ul>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// use-load-data.ts（有删减）</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> <span class="hljs-title function_">loadDataAsPrimitives</span> = <span class="hljs-keyword">async</span> (<span class="hljs-params"><span class="hljs-attr">data</span>: <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-built_in">any</span>&gt;</span>) =&gt; &#123;<br> <span class="hljs-keyword">if</span> (!<span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span> || <span class="hljs-variable language_">window</span>.<span class="hljs-property">viewer</span>.<span class="hljs-title function_">isDestroyed</span>()) <span class="hljs-keyword">return</span>;<br> <span class="hljs-title function_">initCollections</span>();<br><br> <span class="hljs-comment">// ① 构建本次视口应该显示的 ID 集合（按设施类型分组）</span><br> <span class="hljs-keyword">const</span> currentIdsByType = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Map</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-title class_">Set</span>&lt;<span class="hljs-built_in">string</span>&gt;&gt;();<br> <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> [key, list] <span class="hljs-keyword">of</span> <span class="hljs-title class_">Object</span>.<span class="hljs-title function_">entries</span>(data)) &#123;<br>  <span class="hljs-keyword">if</span> (<span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(list)) &#123;<br>   <span class="hljs-keyword">const</span> ids = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Set</span>(list.<span class="hljs-title function_">filter</span>(<span class="hljs-function"><span class="hljs-params">item</span> =&gt;</span> item.<span class="hljs-property">id</span>).<span class="hljs-title function_">map</span>(<span class="hljs-function"><span class="hljs-params">item</span> =&gt;</span> item.<span class="hljs-property">id</span>));<br>   currentIdsByType.<span class="hljs-title function_">set</span>(key, ids);<br>  &#125;<br> &#125;<br><br> <span class="hljs-comment">// ② 移除&quot;已离开视口/被取消勾选&quot;的实体</span><br> <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> [key, existingMap] <span class="hljs-keyword">of</span> rawDataMap) &#123;<br>  <span class="hljs-keyword">const</span> currentIds = currentIdsByType.<span class="hljs-title function_">get</span>(key) || <span class="hljs-keyword">new</span> <span class="hljs-title class_">Set</span>();<br>  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> [id, obj] <span class="hljs-keyword">of</span> existingMap.<span class="hljs-title function_">entries</span>()) &#123;<br>   <span class="hljs-keyword">if</span> (!currentIds.<span class="hljs-title function_">has</span>(id)) &#123;<br>    <span class="hljs-keyword">if</span> (obj.<span class="hljs-property">primitive</span> &amp;&amp; globalPrimitiveCollection?.<span class="hljs-title function_">contains</span>(obj.<span class="hljs-property">primitive</span>)) &#123;<br>     globalPrimitiveCollection.<span class="hljs-title function_">remove</span>(obj.<span class="hljs-property">primitive</span>);<br>    &#125;<br>    <span class="hljs-keyword">if</span> (obj.<span class="hljs-property">billboard</span> &amp;&amp; globalBillboardCollection?.<span class="hljs-title function_">contains</span>(obj.<span class="hljs-property">billboard</span>)) &#123;<br>     globalBillboardCollection.<span class="hljs-title function_">remove</span>(obj.<span class="hljs-property">billboard</span>);<br>    &#125;<br>    existingMap.<span class="hljs-title function_">delete</span>(id); <span class="hljs-comment">// 缓存同步删除</span><br>   &#125;<br>  &#125;<br> &#125;<br><br> <span class="hljs-comment">// ③ 处理新数据 —— processGeoToPrimitive 内部按 id 跳过已存在的，只有新 id 会走创建流程</span><br> <span class="hljs-keyword">const</span> processed = <span class="hljs-keyword">await</span> <span class="hljs-title function_">processGeoToPrimitive</span>(data);<br> processed.<span class="hljs-title function_">forEach</span>(<span class="hljs-function"><span class="hljs-params">item</span> =&gt;</span> &#123;<br>  <span class="hljs-keyword">if</span> (item.<span class="hljs-property">primitive</span> &amp;&amp; globalPrimitiveCollection) globalPrimitiveCollection.<span class="hljs-title function_">add</span>(item.<span class="hljs-property">primitive</span>);<br>  <span class="hljs-keyword">if</span> (item.<span class="hljs-property">billboardOptions</span> &amp;&amp; globalBillboardCollection) &#123;<br>   <span class="hljs-keyword">const</span> billboard = globalBillboardCollection.<span class="hljs-title function_">add</span>(item.<span class="hljs-property">billboardOptions</span>);<br>   <span class="hljs-keyword">if</span> (item.<span class="hljs-property">primitive</span>) &#123;<br>    (item.<span class="hljs-property">primitive</span> <span class="hljs-keyword">as</span> <span class="hljs-built_in">any</span>).<span class="hljs-property">customAttributes</span>.<span class="hljs-property">associatedBillboard</span> = billboard; <span class="hljs-comment">// 双向关联</span><br>   &#125;<br>   <span class="hljs-comment">// billboard 引用回填缓存……</span><br>  &#125;<br> &#125;);<br><br> treeObj.<span class="hljs-property">treeData</span> = <span class="hljs-keyword">await</span> <span class="hljs-title function_">processTreeData</span>(); <span class="hljs-comment">// 左侧设施树同步更新</span><br>&#125;;<br></code></pre></td></tr></table></figure>

<p>去重的关键在 <code>processGeoToPrimitive</code> 里的这一行——缓存结构 <code>rawDataMap: Map&lt;表名, Map&lt;id, {primitive, billboard}&gt;&gt;</code> 天然就是去重表：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> dataItem <span class="hljs-keyword">of</span> value) &#123;<br> <span class="hljs-keyword">if</span> (!dataItem.<span class="hljs-property">id</span>) <span class="hljs-keyword">continue</span>;<br> <span class="hljs-comment">// ⚠️ 关键：如果已存在，跳过创建（避免重复）</span><br> <span class="hljs-keyword">if</span> (existingData.<span class="hljs-title function_">has</span>(dataItem.<span class="hljs-property">id</span>)) <span class="hljs-keyword">continue</span>;<br> <span class="hljs-comment">// ……解析坐标 → 求 center → 按 geoType 分发到三个工厂函数</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>效果：相机平移时，离开视口的实体被精准移除、新进入的实体被创建、视口内已有的实体<strong>一个都不动</strong>——没有闪烁，没有重复请求渲染，内存里也永远只有”看得见”的那批。</p>
<h3 id="5-5-缓存治理：勾选、卸载与定时清理"><a href="#5-5-缓存治理：勾选、卸载与定时清理" class="headerlink" title="5.5 缓存治理：勾选、卸载与定时清理"></a>5.5 缓存治理：勾选、卸载与定时清理</h3><p>设施类型在左侧树上可勾选&#x2F;取消，联动逻辑同样走增量化：新增的 typeCode 立即请求加载，取消的 typeCode 从两个全局集合移除并记入 <code>loadedFacilitiesCache</code>（记下取消时间）。因为”取消勾选只是隐藏，用户很可能马上又勾回来”，数据并不立刻销毁；真正释放靠定时任务——每分钟扫一遍缓存，超过 5 分钟（<code>MAX_CACHE_DURATION</code>）没被重新勾选的类型才真正清掉：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// handleDatas.ts（有删减）</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> <span class="hljs-title function_">startCleanupTask</span> = (<span class="hljs-params"></span>) =&gt; &#123;<br> <span class="hljs-built_in">setInterval</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-keyword">const</span> now = <span class="hljs-title class_">Date</span>.<span class="hljs-title function_">now</span>();<br>  cesiumlStore.<span class="hljs-property">loadedFacilitiesCache</span>.<span class="hljs-title function_">forEach</span>(<span class="hljs-function">(<span class="hljs-params">value, typeCode</span>) =&gt;</span> &#123;<br>   <span class="hljs-keyword">if</span> (now - value.<span class="hljs-property">loadTime</span> &gt; cesiumlStore.<span class="hljs-property">MAX_CACHE_DURATION</span>) &#123;<br>    <span class="hljs-comment">// 释放该类型数据 &amp; 从设施树移除 &amp; 删除缓存记录</span><br>   &#125;<br>  &#125;);<br> &#125;, <span class="hljs-number">1</span> * <span class="hljs-number">60</span> * <span class="hljs-number">1000</span>);<br>&#125;;<br></code></pre></td></tr></table></figure>

<p>这是典型的<strong>空间换时间 + 延迟回收</strong>：勾选切换是高频操作，5 分钟内恢复就免请求；长时间不用则彻底回收，内存不无限增长。</p>
<p><a id="sec6"></a></p>
<h2 id="6-让数据”活”起来：实时推送与交互联动"><a href="#6-让数据”活”起来：实时推送与交互联动" class="headerlink" title="6. 让数据”活”起来：实时推送与交互联动"></a>6. 让数据”活”起来：实时推送与交互联动</h2><h3 id="6-1-WebSocket-事件-→-呼吸图标"><a href="#6-1-WebSocket-事件-→-呼吸图标" class="headerlink" title="6.1 WebSocket 事件 → 呼吸图标"></a>6.1 WebSocket 事件 → 呼吸图标</h3><p>后端通过 WebSocket 推送事件变更，前端收到任何消息都重新拉取”进行中事件列表”，把事件关联的设施渲染成<strong>呼吸告警图标</strong>：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/pages/home/index.vue（有删减）</span><br><span class="hljs-keyword">const</span> &#123; messages &#125; = <span class="hljs-title function_">useWebSocket</span>(<span class="hljs-variable language_">window</span>.<span class="hljs-property">baseConfig</span>?.<span class="hljs-property">baseWsUrl</span> + <span class="hljs-string">&#x27;/ws/event&#x27;</span>);<br><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">getEventList</span> = <span class="hljs-keyword">async</span> (<span class="hljs-params"></span>) =&gt; &#123;<br> <span class="hljs-keyword">const</span> res = <span class="hljs-keyword">await</span> <span class="hljs-title function_">getRunningEventList</span>();<br> <span class="hljs-keyword">if</span> (res.<span class="hljs-property">code</span> === <span class="hljs-number">200</span>) &#123;<br>  eventList.<span class="hljs-property">value</span> = res.<span class="hljs-property">data</span>;<br>  <span class="hljs-title function_">createEventPrimitive</span>(eventList.<span class="hljs-property">value</span>); <span class="hljs-comment">// 事件设施上球</span><br> &#125;<br>&#125;;<br><br><span class="hljs-title function_">watch</span>(messages, <span class="hljs-function">() =&gt;</span> <span class="hljs-title function_">getEventList</span>(), &#123; <span class="hljs-attr">deep</span>: <span class="hljs-literal">true</span> &#125;); <span class="hljs-comment">// 收到推送 → 刷新</span><br></code></pre></td></tr></table></figure>

<p><code>createEventPrimitive</code> 的实现有几个细节值得说：图标用独立的 <code>BillboardCollection</code> 承载并 <code>raiseToTop</code> 保证压在所有设施图标之上；每个 billboard 打上 <code>type: &#39;eventFlag&#39;</code> 标记，点击事件的处理器里优先识别它、走事件信息框分支；多个设施同时告警时动画起始时间错开（<code>animationTime: facilityIndex * 0.5</code>），避免”齐步走”的呆板感。</p>
<p>呼吸动画本体就是一个 <code>requestAnimationFrame</code> 循环 + 正弦函数驱动 scale：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/pages/home/composables/eventPrimitive.ts（有删减）</span><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">startAnimation</span>(<span class="hljs-params">billboardArray</span>) &#123;<br> <span class="hljs-keyword">const</span> <span class="hljs-title function_">animate</span> = (<span class="hljs-params"></span>) =&gt; &#123;<br>  <span class="hljs-keyword">const</span> time = performance.<span class="hljs-title function_">now</span>() * <span class="hljs-number">0.001</span>;<br>  billboardArray.<span class="hljs-title function_">forEach</span>(<span class="hljs-function"><span class="hljs-params">item</span> =&gt;</span> &#123;<br>   <span class="hljs-comment">// sin 波形映射到 [0,1]，再放大成 [scaleBase, scaleBase + scaleRange]</span><br>   <span class="hljs-keyword">const</span> progress = <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">sin</span>(time * item.<span class="hljs-property">animationSpeed</span> + item.<span class="hljs-property">animationTime</span>) * <span class="hljs-number">0.5</span> + <span class="hljs-number">0.5</span>;<br>   item.<span class="hljs-property">billboard</span>.<span class="hljs-property">scale</span> = item.<span class="hljs-property">scaleBase</span> + item.<span class="hljs-property">scaleRange</span> * progress;<br>  &#125;);<br>  animationFrameId = <span class="hljs-title function_">requestAnimationFrame</span>(animate);<br> &#125;;<br> <span class="hljs-title function_">animate</span>();<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="6-2-拾取与高亮状态机"><a href="#6-2-拾取与高亮状态机" class="headerlink" title="6.2 拾取与高亮状态机"></a>6.2 拾取与高亮状态机</h3><p><code>scene.pick</code> 拿到 primitive 后，样式状态由一个四态状态机管理：<code>normal / moveIn / click / rightclick</code>。悬停高亮、移出恢复、点击选中（保持高亮，右键取消）、右键弹菜单。线的样式切换因为 Primitive 不可变，走的是<strong>销毁重建</strong>：改线宽 &#x3D; 用缓存的原坐标 + 新样式新建一个 GeometryInstance 替换旧的——这也是 Primitive 路线的代价之一，好在有 <code>defaultStyle</code> 快照，恢复不难。</p>
<h3 id="6-3-flyTo-与屏幕坐标联动"><a href="#6-3-flyTo-与屏幕坐标联动" class="headerlink" title="6.3 flyTo 与屏幕坐标联动"></a>6.3 flyTo 与屏幕坐标联动</h3><p>Vue 弹窗（信息框、右键菜单）要贴着三维实体显示，靠的是 <code>SceneTransforms.wgs84ToWindowCoordinates</code>（新版叫 <code>worldToWindowCoordinates</code>）把实体坐标投到屏幕坐标，再交给 Vue 层绝对定位。飞行定位则是 <code>camera.flyTo</code> 的 complete 回调里做这一次投影——飞行结束后实体才在视口内，此时算屏幕坐标才是准的。</p>
<p><a id="sec7"></a></p>
<h2 id="7-渲染亮点一瞥：GLSL-流动线、球面弧线、呼吸图标"><a href="#7-渲染亮点一瞥：GLSL-流动线、球面弧线、呼吸图标" class="headerlink" title="7. 渲染亮点一瞥：GLSL 流动线、球面弧线、呼吸图标"></a>7. 渲染亮点一瞥：GLSL 流动线、球面弧线、呼吸图标</h2><p>这三个特效是拓扑展示模式的门面，各讲一个知识点。</p>
<h3 id="7-1-自定义-GLSL-流动线材质"><a href="#7-1-自定义-GLSL-流动线材质" class="headerlink" title="7.1 自定义 GLSL 流动线材质"></a>7.1 自定义 GLSL 流动线材质</h3><p>Cesium 的 <code>Material</code> 体系支持注入自定义 shader：把 GLSL 源码和默认 uniforms 注册进 <code>Material._materialCache</code>，之后就能像内置材质一样 <code>Material.fromType(&#39;PolylineFlow&#39;, {...})</code> 使用：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/pages/home/composables/linePrimitive.ts（有删减）</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">registerFlowLineMaterial</span>(<span class="hljs-params"></span>) &#123;<br> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Material</span>.<span class="hljs-property">PolylineFlowType</span> = <span class="hljs-string">&#x27;PolylineFlow&#x27;</span>;<br> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Material</span>.<span class="hljs-property">PolylineFlowSource</span> = <span class="hljs-string">`</span><br><span class="hljs-string">  uniform vec4 color;</span><br><span class="hljs-string">  uniform vec4 glowColor; // 光影颜色</span><br><span class="hljs-string">  uniform float speed;</span><br><span class="hljs-string">  uniform float percent;</span><br><span class="hljs-string">  uniform float gradient;</span><br><span class="hljs-string"></span><br><span class="hljs-string">  czm_material czm_getMaterial(czm_materialInput materialInput) &#123;</span><br><span class="hljs-string">   czm_material material = czm_getDefaultMaterial(materialInput);</span><br><span class="hljs-string">   float st = materialInput.st.s;             // 沿线方向的纹理坐标 0→1</span><br><span class="hljs-string">   float time = czm_frameNumber * speed / 1000.0;</span><br><span class="hljs-string">   float currentPos = fract(time - st);       // 流动的&quot;光头&quot;位置</span><br><span class="hljs-string">   float trailPos = smoothstep(0.0, percent, currentPos);          // 拖尾</span><br><span class="hljs-string">   float glowPos = smoothstep(0.0, gradient * percent, currentPos)</span><br><span class="hljs-string">                 * smoothstep(percent, percent * (1.0 - gradient), currentPos); // 辉光</span><br><span class="hljs-string"></span><br><span class="hljs-string">   material.diffuse = color.rgb;</span><br><span class="hljs-string">   material.alpha = 0.6;</span><br><span class="hljs-string">   material.emission = glowPos * glowColor.rgb;</span><br><span class="hljs-string">   return material;</span><br><span class="hljs-string">  &#125;</span><br><span class="hljs-string"> `</span>;<br><br> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Material</span>.<span class="hljs-property">_materialCache</span>.<span class="hljs-title function_">addMaterial</span>(<span class="hljs-string">&#x27;PolylineFlow&#x27;</span>, &#123;<br>  <span class="hljs-attr">fabric</span>: &#123;<br>   <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;PolylineFlow&#x27;</span>,<br>   <span class="hljs-attr">uniforms</span>: &#123;<br>    <span class="hljs-attr">color</span>: <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">Color</span>(<span class="hljs-number">0.1</span>, <span class="hljs-number">0.8</span>, <span class="hljs-number">0.4</span>, <span class="hljs-number">0.8</span>),<br>    <span class="hljs-attr">glowColor</span>: <span class="hljs-keyword">new</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-title class_">Color</span>(<span class="hljs-number">1.0</span>, <span class="hljs-number">1.0</span>, <span class="hljs-number">0.0</span>, <span class="hljs-number">1.0</span>),<br>    <span class="hljs-attr">speed</span>: <span class="hljs-number">5.0</span>, <span class="hljs-attr">percent</span>: <span class="hljs-number">0.15</span>, <span class="hljs-attr">gradient</span>: <span class="hljs-number">0.4</span><br>   &#125;,<br>   <span class="hljs-attr">source</span>: <span class="hljs-variable language_">window</span>.<span class="hljs-property">Cesium</span>.<span class="hljs-property">Material</span>.<span class="hljs-property">PolylineFlowSource</span><br>  &#125;,<br>  <span class="hljs-attr">translucent</span>: <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123; <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>; &#125;<br> &#125;);<br>&#125;<br></code></pre></td></tr></table></figure>

<p>流动感的核心就两行：<code>czm_frameNumber</code>（Cesium 内置的帧计数 uniform，每帧自增）驱动时间，<code>fract(time - st)</code> 让”亮段”沿线坐标 st 周期性扫过。注册一次全局可用，拓扑模式下的线全部换成这个材质，网络”能量流动”的观感就出来了。</p>
<h3 id="7-2-球面抛物线弧线（航线）"><a href="#7-2-球面抛物线弧线（航线）" class="headerlink" title="7.2 球面抛物线弧线（航线）"></a>7.2 球面抛物线弧线（航线）</h3><p>两点间的航线不能画直线（会穿过地球），要在球面上撑一条弧线。做法：对起点终点的<strong>单位方向向量</strong>做球面插值得到路径点，再用抛物线公式 <code>h * 4t * (1 - t)</code> 沿弧顶法线抬升——t&#x3D;0.5 时恰好达到最大高度 h：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// linePrimitive.ts（有删减）</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">createArcPositions</span>(<span class="hljs-params">start, end, segments = <span class="hljs-number">50</span>, heightFromEllipsoid = <span class="hljs-number">20000</span></span>) &#123;<br> <span class="hljs-keyword">const</span> positions = [];<br> <span class="hljs-keyword">const</span> startDir = <span class="hljs-title class_">Cesium</span>.<span class="hljs-property">Cartesian3</span>.<span class="hljs-title function_">normalize</span>(start, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Cesium</span>.<span class="hljs-title class_">Cartesian3</span>());<br> <span class="hljs-keyword">const</span> endDir = <span class="hljs-title class_">Cesium</span>.<span class="hljs-property">Cartesian3</span>.<span class="hljs-title function_">normalize</span>(end, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Cesium</span>.<span class="hljs-title class_">Cartesian3</span>());<br> <span class="hljs-keyword">let</span> midDir = <span class="hljs-title class_">Cesium</span>.<span class="hljs-property">Cartesian3</span>.<span class="hljs-title function_">add</span>(startDir, endDir, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Cesium</span>.<span class="hljs-title class_">Cartesian3</span>());<br> <span class="hljs-title class_">Cesium</span>.<span class="hljs-property">Cartesian3</span>.<span class="hljs-title function_">normalize</span>(midDir, midDir); <span class="hljs-comment">// 弧顶法线方向</span><br> <span class="hljs-keyword">const</span> earthRadius = <span class="hljs-title class_">Cesium</span>.<span class="hljs-property">Ellipsoid</span>.<span class="hljs-property">WGS84</span>.<span class="hljs-property">maximumRadius</span>;<br><br> <span class="hljs-keyword">for</span> (<span class="hljs-keyword">let</span> i = <span class="hljs-number">0</span>; i &lt;= segments; i++) &#123;<br>  <span class="hljs-keyword">const</span> t = i / segments;<br>  <span class="hljs-keyword">let</span> dir = <span class="hljs-title class_">Cesium</span>.<span class="hljs-property">Cartesian3</span>.<span class="hljs-title function_">lerp</span>(startDir, endDir, t, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Cesium</span>.<span class="hljs-title class_">Cartesian3</span>());<br>  <span class="hljs-title class_">Cesium</span>.<span class="hljs-property">Cartesian3</span>.<span class="hljs-title function_">normalize</span>(dir, dir);<br>  <span class="hljs-keyword">const</span> surfacePoint = <span class="hljs-title class_">Cesium</span>.<span class="hljs-property">Cartesian3</span>.<span class="hljs-title function_">multiplyByScalar</span>(dir, earthRadius, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Cesium</span>.<span class="hljs-title class_">Cartesian3</span>());<br>  <span class="hljs-keyword">const</span> arcHeight = heightFromEllipsoid * <span class="hljs-number">4.0</span> * t * (<span class="hljs-number">1.0</span> - t); <span class="hljs-comment">// 抛物线</span><br>  positions.<span class="hljs-title function_">push</span>(<span class="hljs-title class_">Cesium</span>.<span class="hljs-property">Cartesian3</span>.<span class="hljs-title function_">add</span>(<br>   surfacePoint,<br>   <span class="hljs-title class_">Cesium</span>.<span class="hljs-property">Cartesian3</span>.<span class="hljs-title function_">multiplyByScalar</span>(midDir, arcHeight, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Cesium</span>.<span class="hljs-title class_">Cartesian3</span>())<br>  ));<br> &#125;<br> <span class="hljs-keyword">return</span> positions;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>工程上还有两个防御性细节：分段数按两点距离自适应（<code>distance / 100000</code>，夹在 20~200 之间），近距离航线不至于棱角分明、远距离不至于顶点爆炸；两点几乎对跖（midDir 趋近零向量）时用叉乘另取法线，避免 normalize 除零。</p>
<h3 id="7-3-交互式画多边形"><a href="#7-3-交互式画多边形" class="headerlink" title="7.3 交互式画多边形"></a>7.3 交互式画多边形</h3><p>区域圈选功能用的是 Entity API 的另一件武器——<code>CallbackProperty</code>：把预览面的 <code>hierarchy</code> 属性定义成一个回调函数，鼠标每点一个新顶点，回调返回的形状立即变化，<strong>不用重建实体</strong>就得到”橡皮筋”跟手效果。绘制完成后把顶点 <code>Cartesian3</code> 转回 WGS84 经纬度数组交给上层业务，预览用的临时实体随即销毁。绘制期间会置一个全局 <code>isDrawingPolygon</code> 标志，右键菜单等交互全部让路——两个交互系统共存时，状态互斥一定要显式管理。</p>
<p><a id="sec8"></a></p>
<h2 id="8-写在最后"><a href="#8-写在最后" class="headerlink" title="8. 写在最后"></a>8. 写在最后</h2><p>把整条链路的选型收成一张表：</p>
<table>
<thead>
<tr>
<th>需求</th>
<th>方案</th>
<th>关键文件</th>
</tr>
</thead>
<tbody><tr>
<td>内网离线 + 快速构建</td>
<td>public 静态引入 + 全局变量，<code>global: &#39;globalThis&#39;</code> + <code>CESIUM_BASE_URL</code></td>
<td><code>index.html</code>、<code>vite.config.ts</code>、<code>global.d.ts</code></td>
</tr>
<tr>
<td>阻断外网请求</td>
<td><code>Ion.defaultServer = &#39;undefined&#39;</code> + 不加载默认底图</td>
<td><code>cesiumMap.vue</code>、<code>config.js</code></td>
</tr>
<tr>
<td>多源底图配置化</td>
<td>layers 配置 + Provider 工厂 + <code>__layerId</code>&#x2F;<code>__zIndex</code> 标记 + localStorage 持久化</td>
<td><code>config.js</code>、<code>layerFactory.ts</code>、<code>home-header/index.vue</code></td>
</tr>
<tr>
<td>海量业务数据渲染</td>
<td>后端 JSON → 坐标兼容解析 → 点 Billboard &#x2F; 线面 Primitive 工厂</td>
<td><code>handleDatas.ts</code>、<code>pointPrimitive.ts</code>、<code>linePrimitive.ts</code>、<code>polygonPrimitive.ts</code></td>
</tr>
<tr>
<td>只加载看得见的数据</td>
<td><code>computeViewRectangle</code> 视口边界（2D&#x2F;3D&#x2F;兜底三分支）+ 高度窗口 + 400ms 防抖</td>
<td><code>store/cesium.ts</code>、<code>handleEventPicks.ts</code></td>
</tr>
<tr>
<td>拖动不闪烁、内存不涨</td>
<td>增量 diff（rawDataMap 按 id 去重）+ 取消勾选延迟回收（5 分钟定时清理）</td>
<td><code>use-load-data.ts</code>、<code>handleDatas.ts</code></td>
</tr>
<tr>
<td>实时告警</td>
<td>WebSocket 推送 → requestAnimationFrame 正弦呼吸图标</td>
<td><code>home/index.vue</code>、<code>eventPrimitive.ts</code></td>
</tr>
<tr>
<td>拓扑&#x2F;航线特效</td>
<td>自定义 GLSL 材质注入 <code>_materialCache</code>、球面插值 + 抛物线抬升</td>
<td><code>linePrimitive.ts</code></td>
</tr>
</tbody></table>
<p>以及这个项目<strong>刻意没用</strong>的东西和原因，同样是选型的一部分：</p>
<ul>
<li><strong>地形</strong>：没有地形数据源，<code>depthTestAgainstTerrain</code> 显式关闭，避免无地形时的坐标漂移；</li>
<li><strong>3D Tiles</strong>：没有倾斜摄影&#x2F;建筑模型需求；</li>
<li><strong>GeoJsonDataSource</strong>：数据是业务 JSON 不是 GeoJSON，且 Entity&#x2F;DataSource 路线扛不住实体量；</li>
<li><strong>CZML&#x2F;KML</strong>：没有时序驱动需求。</li>
</ul>
<p><strong>一句话记住</strong>：Cesium 项目的数据加载，本质是回答”<strong>数据从哪来、什么时候来、来了怎么高效上球、走了怎么干净下球</strong>“——视口驱动回答了”什么时候”，增量 diff 和缓存治理回答了”怎么高效”和”怎么干净”，剩下的才是”从哪来”的工程问题。</p>
<h3 id="文件地图"><a href="#文件地图" class="headerlink" title="文件地图"></a>文件地图</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">infrastructure-data-plus/<br>├── index.html                        # 全局引入 /cesium/Cesium.js 与 /config.js<br>├── vite.config.ts                    # define(global/CESIUM_BASE_URL) + worker es<br>├── global.d.ts                       # window.Cesium / viewer / baseConfig 类型<br>├── public/<br>│   ├── config.js                     # 运行时配置：图层、相机视角、后端地址<br>│   └── cesium/                       # CesiumJS 1.123 构建产物（copy-cesium 脚本复制）<br>└── src/<br>    ├── utils/layerFactory.ts         # XYZ/WMS/WMTS/ArcGIS Provider 工厂<br>    ├── api/cesiumApi.ts              # 视口范围查询 / 样式 / 拓扑接口<br>    ├── store/cesium.ts               # 视口边界计算、选中实体、缓存状态<br>    ├── types/cesium.ts               # 样式常量、图层类型、图标映射<br>    └── pages/home/<br>        ├── index.vue                 # WebSocket → 事件列表 → 呼吸图标<br>        ├── components/<br>        │   ├── cesium-map/cesiumMap.vue    # Viewer 初始化 / 2D-3D 切换<br>        │   └── home-header/index.vue       # 图层勾选 / 排序 / 持久化<br>        └── composables/<br>            ├── use-load-data.ts      # ★ 数据管线核心：loadData → 增量 diff<br>            ├── handleDatas.ts        # 坐标解析 / 中心点 / 勾选联动 / 定时清理<br>            ├── pointPrimitive.ts     # 点 → Billboard<br>            ├── linePrimitive.ts      # 线 → Polyline + GLSL 流动材质 + 弧线<br>            ├── polygonPrimitive.ts   # 面 → Polygon + 中心图标双向关联<br>            ├── eventPrimitive.ts     # 告警呼吸图标<br>            ├── handleEventPicks.ts   # 悬停/点击/右键 + 高度过滤<br>            ├── use-map-style.ts      # 四态样式状态机<br>            ├── use-flyTo-map.ts      # 飞行定位 + 屏幕坐标投影<br>            └── drawPolygon.ts        # CallbackProperty 交互画多边形<br></code></pre></td></tr></table></figure>

<h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><ul>
<li><a href="https://cesium.com/learn/cesiumjs-learn/cesium-for-absolute-beginners/">CesiumJS 官方文档 - Quickstart（构建产物的构成）</a></li>
<li><a href="https://cesium.com/learn/cesiumjs/ref-doc/global.html#ImageryProvider">CesiumJS 官方文档 - ImageryProvider 体系</a></li>
<li><a href="https://vitejs.dev/config/shared-options.html">Vite 官方文档 - define 与 Worker 配置</a></li>
</ul>
]]></content>
      <categories>
        <category>GIS 与三维可视化</category>
      </categories>
      <tags>
        <tag>Cesium</tag>
        <tag>GIS</tag>
        <tag>三维可视化</tag>
        <tag>Vue3</tag>
        <tag>Vite</tag>
      </tags>
  </entry>
  <entry>
    <title>前端工程师的 AI 入门：从流式接口到落地一个 AI 应用</title>
    <url>/2026/09/09/ai-from-streaming/</url>
    <content><![CDATA[<blockquote>
<p>这篇文章不是概念科普，而是一次真实的落地复盘。最近我在自己的 Vue3 后台管理项目（vue3_admin）里完整实现了一个 AI 智能问答模块：支持 SSE 流式打字机输出、多轮对话记忆、Function Calling 工具调用、可视化组件渲染、会话持久化。本文把这条链路从头到尾拆开讲——<strong>为什么需要流式、前端怎么手写 SSE 解析器、Function Calling 的两轮循环到底转了什么、以及从 demo 到可用应用之间隔着哪些工程细节</strong>。所有代码都来自这个真实模块，文末附完整文件地图。</p>
</blockquote>
<h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul>
<li><a href="#sec0">0. 前言：前端工程师离 AI 应用有多远</a></li>
<li><a href="#sec1">1. 认识流式接口：AI 的回复为什么是”蹦”出来的</a></li>
<li><a href="#sec2">2. 服务端：把模型的流转发给浏览器</a></li>
<li><a href="#sec3">3. 前端：手写一个 SSE 解析器</a></li>
<li><a href="#sec4">4. 从”流”到”聊天界面”：状态编排层</a></li>
<li><a href="#sec5">5. 让 AI 用上你的函数：Function Calling</a></li>
<li><a href="#sec6">6. 落地成应用：demo 和工程之间隔着什么</a></li>
<li><a href="#sec7">7. 展望：MCP 离我们还有多远</a></li>
<li><a href="#sec8">8. 写在最后</a></li>
</ul>
<p><a id="sec0"></a></p>
<h2 id="0-前言：前端工程师离-AI-应用有多远"><a href="#0-前言：前端工程师离-AI-应用有多远" class="headerlink" title="0. 前言：前端工程师离 AI 应用有多远"></a>0. 前言：前端工程师离 AI 应用有多远</h2><p>打开 ChatGPT 或者任何 AI 对话产品，仔细观察它的交互，你会发现两个本质特征：</p>
<ol>
<li><strong>回复是流式的</strong>——文字一个字一个字”蹦”出来，而不是等几秒后一次性出现；</li>
<li><strong>AI 会用工具</strong>——问”今天天气”它去调天气 API，问”帮我算一下”它去调计算器，最后把结果组织成人话告诉你。</li>
</ol>
<p>这两件事分别对应两套技术：<strong>SSE（Server-Sent Events）流式传输</strong>和 <strong>Function Calling（工具调用）</strong>。它们也是前端工程师切入 AI 应用开发的最佳入口——不需要懂模型训练，不需要会调参，需要的恰恰是我们最熟悉的东西：HTTP 协议、异步编程、状态管理、组件渲染。</p>
<p>本文按照我实际搭建这个模块的顺序展开，技术栈：</p>
<ul>
<li><strong>前端</strong>：Vue 3 + TypeScript + Vite（<code>src/views/aiChatroom/</code>）</li>
<li><strong>服务端</strong>：Node.js + Express 5 + OpenAI 官方 SDK（<code>server/</code>）</li>
<li><strong>模型</strong>：任何 OpenAI 兼容网关（官方 API、DeepSeek 等均可，只需改一个 <code>baseUrl</code>）</li>
</ul>
<p>先给一张全链路图，后文的所有章节都是对这张图的展开：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">┌─────────────────────── 浏览器 ────────────────────────┐<br>│  chatRoom.vue        （UI 编排：组件接线 + 页面骨架）  │<br>│      ↓                                               │<br>│  useAgentChat.ts     （状态：气泡 / 块 / 竞态防护）    │<br>│      ↓                                               │<br>│  useSSEChat.ts       （传输：fetch 发请求 + 读流解析） │<br>└────────┬──────────────────────────────────────────────┘<br>         │  POST /api/chat-room-agent<br>         │  body: &#123; messages: [...对话历史], stream: true &#125;<br>         ▼<br>┌─────────────────────── Node 服务 ─────────────────────┐<br>│  routes/chat-room-agent.js   （HTTP 进出 + SSE 翻译）  │<br>│      ↓                                               │<br>│  chatRoomAgentService.js     （Agent 工具循环 ≤4 轮）  │<br>│      ↓  stream: true                                │<br>│  OpenAI 兼容网关（官方 / DeepSeek / 中转站…）          │<br>└──────────────────────────────────────────────────────┘<br></code></pre></td></tr></table></figure>

<p><a id="sec1"></a></p>
<h2 id="1-认识流式接口：AI-的回复为什么是”蹦”出来的"><a href="#1-认识流式接口：AI-的回复为什么是”蹦”出来的" class="headerlink" title="1. 认识流式接口：AI 的回复为什么是”蹦”出来的"></a>1. 认识流式接口：AI 的回复为什么是”蹦”出来的</h2><h3 id="1-1-LLM-的生成方式决定了交互形态"><a href="#1-1-LLM-的生成方式决定了交互形态" class="headerlink" title="1.1 LLM 的生成方式决定了交互形态"></a>1.1 LLM 的生成方式决定了交互形态</h3><p>大语言模型的本质是”逐 token 预测下一个 token”——一段 500 字的回答，是模型一个词一个词生成出来的。假设生成完整体需要 8 秒，那么：</p>
<ul>
<li><strong>非流式</strong>：用户盯着加载动画 8 秒，然后”啪”地出现一大段文字；</li>
<li><strong>流式</strong>：首个 token 通常几百毫秒就到，用户第 1 秒就开始阅读，文字边生成边展示。</li>
</ul>
<p>体验差距是量级性的。这就是为什么几乎所有 AI 产品都用流式——<strong>不是炫技，是把模型的生成过程原样暴露给用户</strong>。</p>
<h3 id="1-2-SSE：为”服务器单向推流”而生的协议"><a href="#1-2-SSE：为”服务器单向推流”而生的协议" class="headerlink" title="1.2 SSE：为”服务器单向推流”而生的协议"></a>1.2 SSE：为”服务器单向推流”而生的协议</h3><p>实现”服务器不断推、客户端不断收”，标准答案就是 SSE。它不是一个新协议，就是<strong>普通 HTTP 响应</strong>，只是约定了三件事：</p>
<ol>
<li>响应头声明 <code>Content-Type: text/event-stream</code>，告诉客户端”后面的 body 是事件流，别等它结束”；</li>
<li>每条消息以 <code>data:</code> 开头，以<strong>空行（两个换行 <code>\n\n</code>）结尾</strong>；</li>
<li>连接一直保持，直到服务端主动关闭。</li>
</ol>
<p>我的模块里，一次流式响应的原始报文长这样：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Content-Type: text/event-stream; charset=utf-8<br>Cache-Control: no-cache, no-transform<br>Connection: keep-alive<br><br>data: &#123;&quot;type&quot;:&quot;text&quot;,&quot;content&quot;:&quot;你&quot;&#125;<br><br>data: &#123;&quot;type&quot;:&quot;text&quot;,&quot;content&quot;:&quot;好&quot;&#125;<br><br>data: &#123;&quot;type&quot;:&quot;agent_status&quot;,&quot;phase&quot;:&quot;start&quot;,&quot;name&quot;:&quot;get_current_time&quot;&#125;<br><br>data: [DONE]<br></code></pre></td></tr></table></figure>

<p>有一个容易误解的点：<strong><code>data: [DONE]</code> 不是 SSE 标准的一部分</strong>，它是 OpenAI 风格的流结束约定（DeepSeek 等兼容网关都沿用了）。SSE 标准里流结束就是连接关闭，但实践中”显式结束标记 + 关连接”更可靠，前端也不需要额外处理”连接断了到底是答完了还是出错了”的歧义。</p>
<h3 id="1-3-SSE-vs-WebSocket-vs-轮询"><a href="#1-3-SSE-vs-WebSocket-vs-轮询" class="headerlink" title="1.3 SSE vs WebSocket vs 轮询"></a>1.3 SSE vs WebSocket vs 轮询</h3><table>
<thead>
<tr>
<th>方案</th>
<th>方向</th>
<th>协议成本</th>
<th>适用场景</th>
</tr>
</thead>
<tbody><tr>
<td>轮询</td>
<td>客户端反复拉</td>
<td>低，但延迟高、空请求多</td>
<td>兼容性兜底</td>
</tr>
<tr>
<td><strong>SSE</strong></td>
<td><strong>服务器 → 客户端单向</strong></td>
<td><strong>就是 HTTP，无需升级协议</strong></td>
<td><strong>AI 流式回复、通知推送</strong></td>
</tr>
<tr>
<td>WebSocket</td>
<td>双向</td>
<td>需要协议升级、心跳、重连逻辑</td>
<td>聊天室、协作编辑</td>
</tr>
</tbody></table>
<p>AI 对话场景里，客户端只在开头发一次请求，之后全是服务器往回推——<strong>典型的单向流，SSE 是刚好够用的方案</strong>。WebSocket 的双向能力和额外复杂度在这里是浪费。</p>
<h3 id="1-4-那为什么大厂-API-不直接用-SSE-标准-EventSource？"><a href="#1-4-那为什么大厂-API-不直接用-SSE-标准-EventSource？" class="headerlink" title="1.4 那为什么大厂 API 不直接用 SSE 标准 EventSource？"></a>1.4 那为什么大厂 API 不直接用 SSE 标准 EventSource？</h3><p>浏览器其实内置了 SSE 客户端 <code>EventSource</code>，但 AI 场景几乎没人直接用它，原因在下一章展开（剧透：它只能发 GET）。业界主流做法和我模块里一样：<strong>用 <code>fetch</code> 发 POST，然后手动解析流</strong>——这也正是前端工程师最值得吃透的一段代码。</p>
<p><a id="sec2"></a></p>
<h2 id="2-服务端：把模型的流转发给浏览器"><a href="#2-服务端：把模型的流转发给浏览器" class="headerlink" title="2. 服务端：把模型的流转发给浏览器"></a>2. 服务端：把模型的流转发给浏览器</h2><p>先说结论：服务端做的事情本质上是**”二道贩子”**——把模型网关推过来的流式 chunk，翻译成 SSE 格式推给浏览器。Node 侧用 Express 实现，总共分两步。</p>
<h3 id="2-1-最简版：三十行跑通流式接口"><a href="#2-1-最简版：三十行跑通流式接口" class="headerlink" title="2.1 最简版：三十行跑通流式接口"></a>2.1 最简版：三十行跑通流式接口</h3><p>这是我模块的第一个版本（<code>server/routes/chat.js</code>），保留至今作为教学对照，核心逻辑只有三段：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// server/routes/chat.js（节选）</span><br><span class="hljs-keyword">const</span> &#123; prompt, stream = <span class="hljs-literal">false</span> &#125; = req.<span class="hljs-property">body</span><br><span class="hljs-comment">// OpenAI SDK：stream:true 时返回&quot;异步可迭代对象&quot;，</span><br><span class="hljs-comment">// 可以直接 for await——每循环一次拿到一个增量 chunk</span><br><span class="hljs-keyword">const</span> response = <span class="hljs-keyword">await</span> <span class="hljs-title function_">createChatCompletion</span>(&#123; prompt, stream &#125;)<br><br><span class="hljs-keyword">if</span> (stream) &#123;<br>  <span class="hljs-comment">// SSE 标准响应头</span><br>  res.<span class="hljs-title function_">setHeader</span>(<span class="hljs-string">&#x27;Content-Type&#x27;</span>, <span class="hljs-string">&#x27;text/event-stream&#x27;</span>)<br>  res.<span class="hljs-title function_">setHeader</span>(<span class="hljs-string">&#x27;Cache-Control&#x27;</span>, <span class="hljs-string">&#x27;no-cache&#x27;</span>)<br>  res.<span class="hljs-title function_">setHeader</span>(<span class="hljs-string">&#x27;Connection&#x27;</span>, <span class="hljs-string">&#x27;keep-alive&#x27;</span>)<br><br>  <span class="hljs-comment">// for await 每次拿到一个增量 chunk，delta.content 是这一小段文字</span><br>  <span class="hljs-keyword">for</span> <span class="hljs-keyword">await</span> (<span class="hljs-keyword">const</span> chunk <span class="hljs-keyword">of</span> response) &#123;<br>    <span class="hljs-keyword">const</span> content = chunk.<span class="hljs-property">choices</span>[<span class="hljs-number">0</span>]?.<span class="hljs-property">delta</span>?.<span class="hljs-property">content</span> || <span class="hljs-string">&#x27;&#x27;</span><br>    <span class="hljs-keyword">if</span> (content) &#123;<br>      <span class="hljs-comment">// data: 后面加空格，两个换行 = 一条消息结束</span><br>      res.<span class="hljs-title function_">write</span>(<span class="hljs-string">`data: <span class="hljs-subst">$&#123;<span class="hljs-built_in">JSON</span>.stringify(&#123; content &#125;)&#125;</span>\n\n`</span>)<br>    &#125;<br>  &#125;<br>  res.<span class="hljs-title function_">end</span>()<br>&#125;<br></code></pre></td></tr></table></figure>

<p>三个关键点：</p>
<ul>
<li>OpenAI SDK（以及所有兼容网关）在 <code>stream: true</code> 时返回的是一个 <strong>异步可迭代对象</strong>，<code>for await...of</code> 每次迭代拿到一个 chunk，chunk 里的增量文字在 <code>choices[0].delta.content</code> 上；</li>
<li>每个 chunk 不一定有内容（有的只带角色信息），用可选链 + 空串兜底；</li>
<li><code>res.write()</code> 只是写入缓冲，什么时候真正到达浏览器由底层决定——这也是后面 <code>flushHeaders()</code> 存在的原因。</li>
</ul>
<h3 id="2-2-生产版要补的细节"><a href="#2-2-生产版要补的细节" class="headerlink" title="2.2 生产版要补的细节"></a>2.2 生产版要补的细节</h3><p>真正给聊天页面用的接口（<code>server/routes/chat-room-agent.js</code>）在最简版之上补了四类细节。首先是把 SSE 写操作封装成两个函数，避免格式错误：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// server/routes/chat-room-agent.js</span><br><span class="hljs-comment">// 写一条 SSE 消息：必须是 `data: JSON\n\n` 格式（两个换行=一条消息结束）</span><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">writeSse</span>(<span class="hljs-params">res, data</span>) &#123;<br>  res.<span class="hljs-title function_">write</span>(<span class="hljs-string">`data: <span class="hljs-subst">$&#123;<span class="hljs-built_in">JSON</span>.stringify(data)&#125;</span>\n\n`</span>)<br>&#125;<br><br><span class="hljs-comment">// 写结束标记：前端解析到 [DONE] 就知道流正常结束了</span><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">writeDone</span>(<span class="hljs-params">res</span>) &#123;<br>  res.<span class="hljs-title function_">write</span>(<span class="hljs-string">&#x27;data: [DONE]\n\n&#x27;</span>)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>然后是响应头”四件套”和<strong>立即刷新</strong>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// server/routes/chat-room-agent.js（节选）</span><br>res.<span class="hljs-title function_">status</span>(<span class="hljs-number">200</span>)<br><span class="hljs-comment">// ① 声明事件流（带 charset，中文场景必须有）</span><br>res.<span class="hljs-title function_">setHeader</span>(<span class="hljs-string">&#x27;Content-Type&#x27;</span>, <span class="hljs-string">&#x27;text/event-stream; charset=utf-8&#x27;</span>)<br><span class="hljs-comment">// ② 禁缓存；no-transform 禁止代理压缩改写</span><br>res.<span class="hljs-title function_">setHeader</span>(<span class="hljs-string">&#x27;Cache-Control&#x27;</span>, <span class="hljs-string">&#x27;no-cache, no-transform&#x27;</span>)<br><span class="hljs-comment">// ③ 保持长连接，直到服务端主动 res.end()</span><br>res.<span class="hljs-title function_">setHeader</span>(<span class="hljs-string">&#x27;Connection&#x27;</span>, <span class="hljs-string">&#x27;keep-alive&#x27;</span>)<br>res.<span class="hljs-title function_">setHeader</span>(<span class="hljs-string">&#x27;X-Request-Id&#x27;</span>, requestId)<br><br><span class="hljs-comment">// 立即把响应头发出去：让前端尽早进入&quot;接收流&quot;状态</span><br><span class="hljs-comment">//（不然后面的 res.write 会攒到缓冲区，首字延迟变高）</span><br><span class="hljs-keyword">if</span> (<span class="hljs-keyword">typeof</span> res.<span class="hljs-property">flushHeaders</span> === <span class="hljs-string">&#x27;function&#x27;</span>) &#123;<br>  res.<span class="hljs-title function_">flushHeaders</span>()<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>flushHeaders()</code> 是很多人会漏掉的一行：不调用它，响应头可能攒在缓冲区里和第一批数据一起走，白白增加首字延迟。</p>
<p>最后是<strong>架构上的关键设计——回调嫁接</strong>。路由层不写业务，业务层（<code>chatRoomAgentService.js</code>）只暴露三个回调：<code>onText</code>（模型产出一段文字）、<code>onToolStart</code> &#x2F; <code>onToolEnd</code>（工具执行前后）。路由层负责把回调”翻译”成 SSE 事件：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// server/routes/chat-room-agent.js（节选）</span><br><span class="hljs-keyword">await</span> <span class="hljs-title function_">runChatRoomAgent</span>(&#123;<br>  input,<br>  <span class="hljs-attr">signal</span>: abortController.<span class="hljs-property">signal</span>,<br><br>  <span class="hljs-comment">// 模型每产出一段文字，立刻推一条 SSE（打字机效果的来源）</span><br>  <span class="hljs-attr">onText</span>: <span class="hljs-title function_">async</span> (text) =&gt; &#123;<br>    <span class="hljs-title function_">writeSse</span>(res, &#123; <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;text&#x27;</span>, <span class="hljs-attr">content</span>: text &#125;)<br>  &#125;,<br><br>  <span class="hljs-comment">// 工具生命周期 → agent_status 事件：</span><br>  <span class="hljs-comment">// 前端在气泡里渲染&quot;正在调用 xx 工具…&quot;状态条</span><br>  <span class="hljs-attr">onToolStart</span>: <span class="hljs-title function_">async</span> (&#123; name &#125;) =&gt; &#123;<br>    <span class="hljs-title function_">writeSse</span>(res, &#123; <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;agent_status&#x27;</span>, <span class="hljs-attr">phase</span>: <span class="hljs-string">&#x27;start&#x27;</span>, name &#125;)<br>  &#125;,<br>  <span class="hljs-attr">onToolEnd</span>: <span class="hljs-title function_">async</span> (&#123; name &#125;) =&gt; &#123;<br>    <span class="hljs-title function_">writeSse</span>(res, &#123; <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;agent_status&#x27;</span>, <span class="hljs-attr">phase</span>: <span class="hljs-string">&#x27;end&#x27;</span>, name &#125;)<br>  &#125;,<br>&#125;)<br><span class="hljs-title function_">writeDone</span>(res)<br></code></pre></td></tr></table></figure>

<p>这个设计还有一个附赠的好处：<strong>非流式模式几乎免费</strong>。业务层根本不知道 HTTP 的存在，非流式只是把 <code>onText</code> 攒进一个字符串最后 <code>res.json()</code> 一次性返回——同一套 Agent 循环天然兼容两种输出形态，curl &#x2F; Postman 调试时特别方便。</p>
<h3 id="2-3-四个我亲自踩过的坑"><a href="#2-3-四个我亲自踩过的坑" class="headerlink" title="2.3 四个我亲自踩过的坑"></a>2.3 四个我亲自踩过的坑</h3><p><strong>坑 1：SSE 响应头必须在第一条 <code>data:</code> 之前设置。</strong><br>听起来是废话，但配合 Express 的错误处理中间件就容易翻车：如果第一次 <code>res.write</code> 之前抛了错，你还能改状态码返回 500；一旦写过头，一切就晚了——这就是坑 2。</p>
<p><strong>坑 2：流开始后出错，状态码永远是 200，只能用”错误事件”传错。</strong><br>响应头 200 已经发给浏览器了，中途模型挂了怎么办？不能再 <code>res.status(500)</code>，只能把错误文案包成一条专用 SSE 事件发给前端，再正常收尾：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// server/routes/chat-room-agent.js（节选）</span><br><span class="hljs-keyword">if</span> (!abortController.<span class="hljs-property">signal</span>.<span class="hljs-property">aborted</span>) &#123;<br>  <span class="hljs-keyword">const</span> mapped = <span class="hljs-title function_">mapChatRoomAgentError</span>(error)<br>  <span class="hljs-comment">// 状态码改不了了，只能用 error 事件把文案推给前端（前端标红气泡）</span><br>  <span class="hljs-title function_">writeSse</span>(res, &#123; <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;error&#x27;</span>, <span class="hljs-attr">content</span>: mapped.<span class="hljs-property">message</span> &#125;)<br>  <span class="hljs-title function_">writeDone</span>(res)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>注意细节：<code>aborted</code> 为 true 说明是用户自己点了停止，不算错误、不推送。前端也要有对应的处理——收到 <code>type:&#39;error&#39;</code> 的事件时把气泡标记成错误态。</p>
<p><strong>坑 3：监听客户端断开，要监听 <code>res</code> 的 close，不是 <code>req</code> 的。</strong><br>用户点”停止生成”或直接关页面时，服务端应该立刻取消对上游模型的请求（不取消就白烧 token）。直觉写法是 <code>req.on(&#39;close&#39;, ...)</code>，但在 Node 22 实测中这是个假阳性陷阱：<strong><code>req</code> 的 close 在请求体被 <code>express.json()</code> 读完之后就会触发</strong>（请求进来约 2ms），和客户端断不断开毫无关系——用它判断”提前断开”会把完全正常的请求也 abort 掉。正确姿势：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// server/routes/chat-room-agent.js（节选）</span><br><span class="hljs-comment">// res 的 close 在响应结束时触发：正常答完时 writableEnded=true，</span><br><span class="hljs-comment">// 客户端半路断开时 writableEnded=false——用它区分才可靠</span><br>res.<span class="hljs-title function_">on</span>(<span class="hljs-string">&#x27;close&#x27;</span>, <span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-keyword">if</span> (!res.<span class="hljs-property">writableEnded</span>) &#123;<br>    abortController.<span class="hljs-title function_">abort</span>()<br>  &#125;<br>&#125;)<br></code></pre></td></tr></table></figure>

<p><strong>坑 4：OpenAI SDK 的配置项是 <code>baseUrl</code>，不是 <code>baseURL</code>。</strong><br>SDK 的构造参数是全小写的 <code>baseUrl</code>，写错大小写不会报错，只会静默回落到官方地址——如果你在用中转网关，症状就是”怎么改配置都提示 Key 无效”。我的服务端通过 <code>OPENAI_BASE_URL</code> 环境变量统一配置，指向任何 OpenAI 兼容服务（官方、DeepSeek、中转站）都不用改代码。</p>
<p><a id="sec3"></a></p>
<h2 id="3-前端：手写一个-SSE-解析器"><a href="#3-前端：手写一个-SSE-解析器" class="headerlink" title="3. 前端：手写一个 SSE 解析器"></a>3. 前端：手写一个 SSE 解析器</h2><h3 id="3-1-为什么不用-EventSource"><a href="#3-1-为什么不用-EventSource" class="headerlink" title="3.1 为什么不用 EventSource"></a>3.1 为什么不用 EventSource</h3><p>浏览器原生的 <code>EventSource</code> 有三个硬伤，在 AI 对话场景全部踩中：</p>
<table>
<thead>
<tr>
<th>能力</th>
<th>EventSource</th>
<th>我们需要的</th>
</tr>
</thead>
<tbody><tr>
<td>请求方法</td>
<td>只能 GET</td>
<td><strong>POST</strong>（要发 JSON 请求体）</td>
</tr>
<tr>
<td>请求体</td>
<td>不能带 body</td>
<td><strong>完整对话历史</strong>（多轮记忆的来源）</td>
</tr>
<tr>
<td>自定义头</td>
<td>不能设置</td>
<td><code>Authorization</code> 等</td>
</tr>
</tbody></table>
<p>所以主流方案是：<strong><code>fetch</code> 发 POST，拿到 <code>response.body</code> 这个字节流，自己解析 SSE 格式</strong>。这也是整个模块里我觉得最值得逐行吃透的一段代码（<code>src/views/aiChatroom/utils/useSSEChat.ts</code>，节选）：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// 传输层：发起请求 + 解析 SSE 流，整个模块里唯一碰网络的代码</span><br><span class="hljs-comment">// 用 fetch 而不是原生 EventSource：EventSource 只支持 GET、</span><br><span class="hljs-comment">// 不能自定义请求头/请求体，而这里要 POST JSON</span><br><span class="hljs-keyword">const</span> response = <span class="hljs-keyword">await</span> <span class="hljs-title function_">fetch</span>(url, &#123;<br>  <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;POST&#x27;</span>,<br>  <span class="hljs-attr">headers</span>: &#123; <span class="hljs-string">&#x27;Content-Type&#x27;</span>: <span class="hljs-string">&#x27;application/json&#x27;</span> &#125;,<br>  <span class="hljs-comment">// messages：完整对话历史（含本次新消息），服务端优先读它——</span><br>  <span class="hljs-comment">// 这是多轮&quot;记忆&quot;的来源；不传 history 时退化为单条消息</span><br>  <span class="hljs-attr">body</span>: <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">stringify</span>(&#123;<br>    message,<br>    <span class="hljs-attr">messages</span>: history?.<span class="hljs-property">length</span> ? history : [&#123; <span class="hljs-attr">role</span>: <span class="hljs-string">&#x27;user&#x27;</span>, <span class="hljs-attr">content</span>: message &#125;],<br>    <span class="hljs-attr">stream</span>: <span class="hljs-literal">true</span>,<br>  &#125;),<br>  <span class="hljs-comment">// abort 信号：外部调 abortController.abort() 时 fetch 抛 AbortError</span><br>  <span class="hljs-attr">signal</span>: abortController?.<span class="hljs-property">signal</span>,<br>&#125;)<br><br><span class="hljs-comment">// 拿到字节流的 reader：每次 read() 返回一小段 Uint8Array，</span><br><span class="hljs-comment">// 何时有数据、每段多大都由网络决定，需要自己解码拼装</span><br><span class="hljs-keyword">const</span> reader = response.<span class="hljs-property">body</span>.<span class="hljs-title function_">getReader</span>()<br><span class="hljs-keyword">const</span> decoder = <span class="hljs-keyword">new</span> <span class="hljs-title class_">TextDecoder</span>(<span class="hljs-string">&#x27;utf-8&#x27;</span>)<br><span class="hljs-keyword">let</span> buffer = <span class="hljs-string">&#x27;&#x27;</span><br><br><span class="hljs-keyword">let</span> finished = <span class="hljs-literal">false</span><br><span class="hljs-keyword">while</span> (!finished) &#123;<br>  <span class="hljs-keyword">const</span> &#123; done, value &#125; = <span class="hljs-keyword">await</span> reader.<span class="hljs-title function_">read</span>()<br>  <span class="hljs-keyword">if</span> (done) &#123;<br>    finished = <span class="hljs-literal">true</span><br>    <span class="hljs-keyword">break</span><br>  &#125;<br><br>  <span class="hljs-comment">// stream:true 允许多字节中文/emoji 被拆在两个 chunk 里，</span><br>  <span class="hljs-comment">// TextDecoder 会记住未完成的字节，等下一段拼上再输出</span><br>  <span class="hljs-keyword">const</span> chunk = decoder.<span class="hljs-title function_">decode</span>(value, &#123; <span class="hljs-attr">stream</span>: <span class="hljs-literal">true</span> &#125;)<br>  buffer += chunk<br><br>  <span class="hljs-comment">// 按行切开处理，最后一段可能是不完整的行，</span><br>  <span class="hljs-comment">// 留在 buffer 里等下一个 chunk 拼上（流式解析的经典细节）</span><br>  <span class="hljs-keyword">const</span> lines = buffer.<span class="hljs-title function_">split</span>(<span class="hljs-string">&#x27;\n&#x27;</span>)<br>  buffer = lines.<span class="hljs-title function_">pop</span>() || <span class="hljs-string">&#x27;&#x27;</span><br><br>  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> line <span class="hljs-keyword">of</span> lines) &#123;<br>    <span class="hljs-keyword">const</span> trimmedLine = line.<span class="hljs-title function_">trim</span>()<br>    <span class="hljs-keyword">if</span> (!trimmedLine) <span class="hljs-keyword">continue</span><br><br>    <span class="hljs-comment">// SSE 协议：有效内容行以 &quot;data:&quot; 开头</span><br>    <span class="hljs-keyword">if</span> (trimmedLine.<span class="hljs-title function_">startsWith</span>(<span class="hljs-string">&#x27;data:&#x27;</span>)) &#123;<br>      <span class="hljs-keyword">const</span> dataStr = trimmedLine.<span class="hljs-title function_">replace</span>(<span class="hljs-regexp">/^data:\s*/</span>, <span class="hljs-string">&#x27;&#x27;</span>).<span class="hljs-title function_">trim</span>()<br><br>      <span class="hljs-comment">// [DONE]：与服务端约定的流结束标记</span><br>      <span class="hljs-keyword">if</span> (dataStr === <span class="hljs-string">&#x27;[DONE]&#x27;</span>) &#123;<br>        <span class="hljs-title function_">onComplete</span>()<br>        isLoading.<span class="hljs-property">value</span> = <span class="hljs-literal">false</span><br>        <span class="hljs-keyword">return</span><br>      &#125;<br><br>      <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-comment">// 正常情况：一条 JSON 反序列化成一个块</span><br>        <span class="hljs-keyword">const</span> <span class="hljs-attr">data</span>: <span class="hljs-title class_">Partial</span>&lt;<span class="hljs-title class_">SSEData</span>&gt; = <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">parse</span>(dataStr)<br>        <span class="hljs-title function_">onChunk</span>(&#123; <span class="hljs-attr">type</span>: data.<span class="hljs-property">type</span> || <span class="hljs-string">&#x27;text&#x27;</span>, ...data &#125; <span class="hljs-keyword">as</span> <span class="hljs-title class_">SSEData</span>)<br>      &#125; <span class="hljs-keyword">catch</span> &#123;<br>        <span class="hljs-comment">// 兜底：JSON 解析失败时按纯文本处理，不让一条坏消息打断整条流</span><br>        <span class="hljs-title function_">onChunk</span>(&#123; <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;text&#x27;</span>, <span class="hljs-attr">content</span>: dataStr &#125;)<br>      &#125;<br>    &#125;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这段 60 行不到的代码里有三个值得单独拎出来讲的细节，它们是所有”流式解析”的通用套路：</p>
<p><strong>细节一：<code>decoder.decode(value, { stream: true })</code>。</strong><br>网络 chunk 是按字节切的，不按字符切。一个中文字占 3 字节、一个 emoji 占 4 字节，完全可能被切在两个 chunk 里——不带 <code>stream: true</code> 时，前半截会被解码成乱码。带上之后 <code>TextDecoder</code> 会记住未完成的字节序列，等下一段到达后拼起来再输出。</p>
<p><strong>细节二：<code>lines.pop()</code> 半行缓冲。</strong><br>SSE 按行解析，但一个 chunk 里最后一行很可能是半截的（比如 <code>data: {&quot;type&quot;:&quot;te</code>）。所以按 <code>\n</code> 切行之后，把最后一段 <code>pop()</code> 出来留在 <code>buffer</code> 里，等下一个 chunk 拼上再处理。没有这一步，JSON.parse 会随机失败。</p>
<p><strong>细节三：单条消息解析失败，降级但不中断。</strong><br>一条 <code>data:</code> 消息 JSON.parse 失败时，按纯文本兜底处理，而不是抛错断流——网络世界的格言是”坏一条消息可以，坏一整条流不行”。</p>
<h3 id="3-2-中断：AbortController-与”静默的-AbortError”"><a href="#3-2-中断：AbortController-与”静默的-AbortError”" class="headerlink" title="3.2 中断：AbortController 与”静默的 AbortError”"></a>3.2 中断：AbortController 与”静默的 AbortError”</h3><p>“停止生成”按钮的实现只有两行核心代码：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// src/views/aiChatroom/utils/useSSEChat.ts</span><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">stopChat</span> = (<span class="hljs-params"></span>) =&gt; &#123;<br>  <span class="hljs-keyword">if</span> (currentAbortController) &#123;<br>    currentAbortController.<span class="hljs-title function_">abort</span>()<br>    currentAbortController = <span class="hljs-literal">null</span><br>  &#125;<br>  isLoading.<span class="hljs-property">value</span> = <span class="hljs-literal">false</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>abort()</code> 一石三鸟：fetch 抛出 <code>AbortError</code>、<code>reader.read()</code> 停止、服务端收到连接断开后沿着第 2 章坑 3 的链路取消对上游模型的请求——<strong>中断信号贯穿了整个三层链路</strong>。</p>
<p>一个容易忽略的处理：abort 触发的 <code>AbortError</code> 不应该走错误分支。用户主动停止是正常操作，不该弹出红色报错：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts">&#125; <span class="hljs-keyword">catch</span> (err) &#123;<br>  <span class="hljs-keyword">if</span> (err <span class="hljs-keyword">instanceof</span> <span class="hljs-title class_">Error</span>) &#123;<br>    <span class="hljs-comment">// 用户主动停止属于正常行为，不算错误，不打扰上层</span><br>    <span class="hljs-keyword">if</span> (err.<span class="hljs-property">name</span> === <span class="hljs-string">&#x27;AbortError&#x27;</span>) &#123;<br>      <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;Request aborted&#x27;</span>)<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>      error.<span class="hljs-property">value</span> = err<br>      <span class="hljs-title function_">onError</span>(err)<br>    &#125;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><a id="sec4"></a></p>
<h2 id="4-从”流”到”聊天界面”：状态编排层"><a href="#4-从”流”到”聊天界面”：状态编排层" class="headerlink" title="4. 从”流”到”聊天界面”：状态编排层"></a>4. 从”流”到”聊天界面”：状态编排层</h2><p>到这里，”收流”已经解决了。但 <code>onChunk</code> 给你的只是一个个碎片 <code>{&quot;type&quot;:&quot;text&quot;,&quot;content&quot;:&quot;你&quot;}</code>，而用户看到的是一个带打字机动画的聊天气泡——中间隔着一层<strong>状态编排</strong>。我的模块把这层单独抽成了 <code>useAgentChat.ts</code>，整体分三层：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">chatRoom.vue     UI 编排层：组件接线、页面骨架，不含网络逻辑<br>    ↓<br>useAgentChat.ts  状态层：SSE 流 → messages 数组的变化<br>    ↓<br>useSSEChat.ts    传输层：唯一碰网络的代码，只认 SSEData<br></code></pre></td></tr></table></figure>

<p>分层的判定标准很朴素：<strong>传输层不认识”气泡”（ChatMessage），UI 层不认识”网络”</strong>。改 SSE 协议只动传输层，改界面只动 UI 层。</p>
<h3 id="4-1-占位气泡：打字机效果的真相"><a href="#4-1-占位气泡：打字机效果的真相" class="headerlink" title="4.1 占位气泡：打字机效果的真相"></a>4.1 占位气泡：打字机效果的真相</h3><p>发送消息时先 push 一条<strong>空的 streaming 气泡</strong>，之后流里收到的所有内容都累积到这条消息上：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// src/views/aiChatroom/composables/useAgentChat.ts（节选）</span><br><span class="hljs-comment">// ② 再占位一条&quot;空回复&quot;：气泡先出现，type=&#x27;streaming&#x27; 显示三点动画，</span><br><span class="hljs-comment">// 之后流里收到的内容全部累积到这条消息上</span><br><span class="hljs-keyword">const</span> assistantId = <span class="hljs-title class_">Date</span>.<span class="hljs-title function_">now</span>() + <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">random</span>()<br>messages.<span class="hljs-property">value</span>.<span class="hljs-title function_">push</span>(&#123;<br>  <span class="hljs-attr">id</span>: assistantId,<br>  <span class="hljs-attr">role</span>: <span class="hljs-string">&#x27;assistant&#x27;</span>,<br>  <span class="hljs-attr">content</span>: <span class="hljs-string">&#x27;&#x27;</span>,<br>  <span class="hljs-attr">blocks</span>: [],<br>  <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;streaming&#x27;</span>,<br>  <span class="hljs-attr">timestamp</span>: <span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>().<span class="hljs-title function_">toISOString</span>(),<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>所谓打字机效果，没有任何 <code>setInterval</code> 定时器魔法，就是**”占位气泡 + 每个 chunk 触发一次响应式更新”**——Vue 的响应式系统天然适合流式渲染。<code>type</code> 是气泡的状态机：<code>streaming</code>（显示三点动画）→ <code>complete</code>（显示复制&#x2F;重新生成按钮）或 <code>error</code>（标红）。</p>
<h3 id="4-2-相邻-text-增量必须合并成一个块"><a href="#4-2-相邻-text-增量必须合并成一个块" class="headerlink" title="4.2 相邻 text 增量必须合并成一个块"></a>4.2 相邻 text 增量必须合并成一个块</h3><p>这是我踩过的最隐蔽的一个坑。最初我每收到一个 chunk 就 push 一个新块，结果markdown 渲染完全错乱——因为每个字都成了独立的 markdown 块，代码块被拦腰截断、列表序号全部重排。修复方式只有几行：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// src/views/aiChatroom/composables/useAgentChat.ts（节选）</span><br><span class="hljs-keyword">const</span> last = blocks[blocks.<span class="hljs-property">length</span> - <span class="hljs-number">1</span>]<br><span class="hljs-comment">// 真流式下文字逐段到达：相邻 text 增量必须合并进同一块。</span><br><span class="hljs-comment">// 否则每个字都是独立 markdown 块——换行错乱、代码块被拦腰截断</span><br><span class="hljs-keyword">if</span> (data.<span class="hljs-property">type</span> === <span class="hljs-string">&#x27;text&#x27;</span> &amp;&amp; last?.<span class="hljs-property">type</span> === <span class="hljs-string">&#x27;text&#x27;</span>) &#123;<br>  last.<span class="hljs-property">content</span> = (last.<span class="hljs-property">content</span> || <span class="hljs-string">&#x27;&#x27;</span>) + (data.<span class="hljs-property">content</span> || <span class="hljs-string">&#x27;&#x27;</span>)<br>&#125; <span class="hljs-keyword">else</span> &#123;<br>  blocks.<span class="hljs-title function_">push</span>(&#123; <span class="hljs-comment">/* ... */</span> &#125;)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>渲染侧则简单粗暴：每个 text 块<strong>整块 full re-parse</strong>（markdown-it）。你可能会担心性能——实际上现代设备对几百字文本做 markdown 解析是微秒级的，每个 chunk 重解析一次完全无压力。流式 markdown 的”未闭合代码块闪烁”问题也顺带解决了：markdown-it 对不完整内容会容错渲染，下个 chunk 到达后整体重渲染自然补全。</p>
<h3 id="4-3-竞态防护：给每个请求发一张”门票”"><a href="#4-3-竞态防护：给每个请求发一张”门票”" class="headerlink" title="4.3 竞态防护：给每个请求发一张”门票”"></a>4.3 竞态防护：给每个请求发一张”门票”</h3><p>场景：用户点了”重新生成”，旧请求的 SSE 还在路上。如果不做防护，旧流的迟到数据会写进新对话的气泡里。解法是 requestId”门票”——发请求时领号，回调进来先验票：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// src/views/aiChatroom/composables/useAgentChat.ts（节选）</span><br><span class="hljs-comment">// ── 竞态防护三件套 ──</span><br><span class="hljs-keyword">let</span> <span class="hljs-attr">activeRequestId</span>: <span class="hljs-built_in">number</span> | <span class="hljs-literal">null</span> = <span class="hljs-literal">null</span> <span class="hljs-comment">// 当前有效请求的&quot;门票号&quot;</span><br><span class="hljs-keyword">let</span> <span class="hljs-attr">activeAbortController</span>: <span class="hljs-title class_">AbortController</span> | <span class="hljs-literal">null</span> = <span class="hljs-literal">null</span><br><span class="hljs-keyword">let</span> <span class="hljs-attr">activeAssistantId</span>: <span class="hljs-built_in">number</span> | <span class="hljs-literal">null</span> = <span class="hljs-literal">null</span> <span class="hljs-comment">// 正在流式输出的气泡 id</span><br><br><span class="hljs-keyword">const</span> requestId = <span class="hljs-title class_">Date</span>.<span class="hljs-title function_">now</span>() + <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">random</span>()<br>activeRequestId = requestId<br><br><span class="hljs-comment">// 每个 chunk 回调进来第一件事：验票</span><br><span class="hljs-attr">onChunk</span>: <span class="hljs-function">(<span class="hljs-params"><span class="hljs-attr">data</span>: <span class="hljs-title class_">SSEData</span></span>) =&gt;</span> &#123;<br>  <span class="hljs-comment">// 门票对不上 = 这是已被停止/取代的旧请求，丢弃迟到数据</span><br>  <span class="hljs-keyword">if</span> (activeRequestId !== requestId) <span class="hljs-keyword">return</span><br>  <span class="hljs-comment">// ...</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>onComplete</code> &#x2F; <code>onError</code> 同样先验票再操作。加上重新生成时的处理（找到上一条用户消息、截断其后所有消息、原样重发），一套完整的多轮交互就稳了。</p>
<h3 id="4-4-多轮记忆：把历史原样发回去"><a href="#4-4-多轮记忆：把历史原样发回去" class="headerlink" title="4.4 多轮记忆：把历史原样发回去"></a>4.4 多轮记忆：把历史原样发回去</h3><p>模型本身没有记忆，所谓”多轮对话”，就是<strong>每次请求都把之前的对话历史原样带上</strong>。组装逻辑：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// src/views/aiChatroom/composables/useAgentChat.ts（节选）</span><br><span class="hljs-comment">// 过滤出「有文字的 user 消息」和「已完成且有内容的 assistant 消息」，</span><br><span class="hljs-comment">// streaming 占位与 error 气泡天然被排除。</span><br><span class="hljs-comment">// 取最后 20 条：服务端校验有上限，留出安全余量</span><br><span class="hljs-keyword">const</span> <span class="hljs-attr">history</span>: <span class="hljs-title class_">ApiChatMessage</span>[] = messages.<span class="hljs-property">value</span><br>  .<span class="hljs-title function_">filter</span>(<br>    <span class="hljs-function">(<span class="hljs-params">item</span>) =&gt;</span><br>      (item.<span class="hljs-property">role</span> === <span class="hljs-string">&#x27;user&#x27;</span> &amp;&amp; item.<span class="hljs-property">content</span>.<span class="hljs-title function_">trim</span>()) ||<br>      (item.<span class="hljs-property">role</span> === <span class="hljs-string">&#x27;assistant&#x27;</span> &amp;&amp; item.<span class="hljs-property">type</span> === <span class="hljs-string">&#x27;complete&#x27;</span> &amp;&amp; item.<span class="hljs-property">content</span>.<span class="hljs-title function_">trim</span>()),<br>  )<br>  .<span class="hljs-title function_">map</span>(<span class="hljs-function">(<span class="hljs-params">item</span>) =&gt;</span> (&#123; <span class="hljs-attr">role</span>: item.<span class="hljs-property">role</span>, <span class="hljs-attr">content</span>: item.<span class="hljs-property">content</span> &#125;))<br>  .<span class="hljs-title function_">slice</span>(-<span class="hljs-number">20</span>)<br></code></pre></td></tr></table></figure>

<p>两个细节：只发 <code>role + content</code> 的最小结构（id、时间戳等 UI 字段不透传给模型）；截取最近 20 条控制上下文长度和 token 成本。</p>
<h3 id="4-5-blocks-化渲染：气泡里不只有文字"><a href="#4-5-blocks-化渲染：气泡里不只有文字" class="headerlink" title="4.5 blocks 化渲染：气泡里不只有文字"></a>4.5 blocks 化渲染：气泡里不只有文字</h3><p>最后一块拼图：AI 的回复不只是文字——工具调用过程、图片、交互组件都要出现在同一条气泡里。我的设计是把回复建模为<strong>块（block）序列</strong>，三条数据主线：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">SSEData（线上格式，一条 data: 消息）<br>   ↓ useAgentChat 翻译<br>MessageBlock（气泡内的一块：text / image / component / agent_status）<br>   ↓ 组成<br>ChatMessage（完整气泡：id / role / blocks / type 状态机）<br></code></pre></td></tr></table></figure>

<p>渲染器 <code>InteractiveRenderer.vue</code> 按 <code>block.type</code> 分发：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs vue">&lt;!-- src/views/aiChatroom/components/InteractiveRenderer.vue（节选） --&gt;<br>&lt;template v-for=&quot;(block, index) in blocks&quot; :key=&quot;index&quot;&gt;<br>  &lt;!-- 文本块：markdown 渲染 --&gt;<br>  &lt;div v-if=&quot;block.type === &#x27;text&#x27;&quot; class=&quot;text-block&quot;<br>       v-html=&quot;renderedMarkdown(block.content || &#x27;&#x27;)&quot;&gt;&lt;/div&gt;<br><br>  &lt;!-- 组件块：AI 下发的交互组件，白名单 + 懒加载 --&gt;<br>  &lt;component v-else-if=&quot;block.type === &#x27;component&#x27; &amp;&amp; block.component&quot;<br>             :is=&quot;resolveComponent(block.component)&quot; v-bind=&quot;block.props&quot; /&gt;<br><br>  &lt;!-- 工具状态块：Agent 循环里&quot;正在调用 xx 工具&quot;的过程提示 --&gt;<br>  &lt;div v-else-if=&quot;block.type === &#x27;agent_status&#x27;&quot; class=&quot;agent-status-block&quot;&gt;<br>    &lt;LoadingOutlined v-if=&quot;block.phase === &#x27;start&#x27;&quot; spin /&gt;<br>    &lt;CheckCircleOutlined v-else /&gt;<br>    &lt;span&gt;&#123;&#123; block.phase === &#x27;start&#x27; ? `正在调用 $&#123;block.name&#125;…` : `已调用 $&#123;block.name&#125;` &#125;&#125;&lt;/span&gt;<br>  &lt;/div&gt;<br>&lt;/template&gt;<br></code></pre></td></tr></table></figure>

<p>这个设计的妙处在于<strong>数据驱动</strong>：气泡里”能出现什么”由块类型决定，服务端将来新增一种块（比如视频、表格），前端只需要在渲染器里加一个分支，上层链路一行不改。<code>component</code> 块还有一道白名单（<code>componentMap</code> 查表，查不到降级为空 div），既防止渲染崩溃，也杜绝了任意组件注入。</p>
<p><a id="sec5"></a></p>
<h2 id="5-让-AI-用上你的函数：Function-Calling"><a href="#5-让-AI-用上你的函数：Function-Calling" class="headerlink" title="5. 让 AI 用上你的函数：Function Calling"></a>5. 让 AI 用上你的函数：Function Calling</h2><p>流式解决的是”说得快”，Function Calling 解决的是”说得对”——你问”现在几点”，模型的训练数据里没有答案，它只能一本正经地编。Function Calling 让模型可以<strong>请求调用你注册的函数</strong>，拿到真实结果后再组织回答。</p>
<h3 id="5-1-核心机制：模型只”下订单”，不执行任何东西"><a href="#5-1-核心机制：模型只”下订单”，不执行任何东西" class="headerlink" title="5.1 核心机制：模型只”下订单”，不执行任何东西"></a>5.1 核心机制：模型只”下订单”，不执行任何东西</h3><p>这是理解 FC 最重要的心智模型：<strong>模型从头到尾没有执行任何代码</strong>。它做的只是在回复里说一句”我想调用 <code>get_weather</code>，参数是 <code>{&quot;city&quot;:&quot;北京&quot;}</code>“，执行永远发生在你的代码里（前端或后端）。所谓 Function Calling，本质是一套**”工具声明格式 + 消息协议”**的约定。</p>
<p>一个工具的声明分两部分，<strong>同名对应、声明发给模型、实现留在本地</strong>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// server/modules/chat-room-agent/agentTool.js（节选）</span><br><span class="hljs-keyword">const</span> toolDefinitions = [<br>  &#123;<br>    <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;function&#x27;</span>,               <span class="hljs-comment">// OpenAI FC 固定包装层</span><br>    <span class="hljs-attr">function</span>: &#123;<br>      <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;get_current_time&#x27;</span>,     <span class="hljs-comment">// 必须与 toolHandlers 的键完全一致</span><br>      <span class="hljs-attr">description</span>:<br>        <span class="hljs-string">&#x27;获取指定时区的当前时间。当用户询问现在几点、当前日期或当地时间时使用。&#x27;</span>,<br>      <span class="hljs-comment">// parameters 就是 JSON Schema：模型按这个格式&quot;生成&quot;参数</span><br>      <span class="hljs-attr">parameters</span>: &#123;<br>        <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;object&#x27;</span>,<br>        <span class="hljs-attr">properties</span>: &#123;<br>          <span class="hljs-attr">timezone</span>: &#123;<br>            <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;string&#x27;</span>,<br>            <span class="hljs-attr">description</span>: <span class="hljs-string">&#x27;IANA 时区名称，例如 Asia/Shanghai、Asia/Tokyo&#x27;</span>,<br>          &#125;,<br>        &#125;,<br>        <span class="hljs-attr">required</span>: [<span class="hljs-string">&#x27;timezone&#x27;</span>],<br>        <span class="hljs-attr">additionalProperties</span>: <span class="hljs-literal">false</span>,<br>      &#125;,<br>    &#125;,<br>  &#125;,<br>]<br></code></pre></td></tr></table></figure>

<p>实践心得：<strong><code>description</code> 的质量直接决定触发准确率</strong>。模型是靠读这段文字来判断”什么时候该用这个工具”的，把典型问法写进去，误触发会显著减少。</p>
<h3 id="5-2-两轮循环：一次工具调用的完整时序"><a href="#5-2-两轮循环：一次工具调用的完整时序" class="headerlink" title="5.2 两轮循环：一次工具调用的完整时序"></a>5.2 两轮循环：一次工具调用的完整时序</h3><p>把工具发给模型后，一次完整的工具调用是”两轮请求”：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">你的代码                                    模型（LLM）<br>    │  ① messages + tools（工具说明书）        │<br>    │     tool_choice: &#x27;auto&#x27;                 │<br>    │ ──────────────────────────────────────► │<br>    │                                         │ 模型：这题我需要调工具<br>    │  ② assistant 消息，带 tool_calls：      │<br>    │     &#123; name:&#x27;get_current_time&#x27;,          │<br>    │       arguments:&#x27;&#123;&quot;timezone&quot;:           │<br>    │         &quot;Asia/Shanghai&quot;&#125;&#x27; &#125;             │<br>    │ ◄────────────────────────────────────── │<br>    │                                         │<br>    │  ③ 你在本地执行 handler，拿到真实结果     │<br>    │  ④ messages 追加两条：                   │<br>    │     - assistant（含 tool_calls 原样回填）│<br>    │     - tool（含 tool_call_id + 结果）     │<br>    │                                         │<br>    │  ⑤ 完整 messages 再次请求（可不再带 tools）│<br>    │ ──────────────────────────────────────► │<br>    │  ⑥ assistant: &quot;现在是 2026 年 9 月…&quot;    │<br>    │ ◄────────────────────────────────────── │<br></code></pre></td></tr></table></figure>

<p>三个协议细节最容易踩坑：</p>
<ol>
<li><strong><code>arguments</code> 是 JSON 字符串，不是对象</strong>。模型返回的 <code>tool_calls[0].function.arguments</code> 是 <code>&#39;{&quot;timezone&quot;:&quot;Asia/Shanghai&quot;}&#39;</code> 这样的一坨字符串，必须自己 <code>JSON.parse</code>；</li>
<li><strong>回填 tool 结果前，必须先把带 <code>tool_calls</code> 的 assistant 消息 push 进历史</strong>。OpenAI 协议要求 tool 消息必须紧跟发起调用的 assistant 消息，缺了它网关直接 400——这是 FC 接入率最高的报错；</li>
<li><strong><code>tool_call_id</code> 是回执编号</strong>。一次回复可能同时调多个工具（”北京和东京几点？”），每个结果靠 <code>tool_call_id</code> 对号入座。</li>
</ol>
<h3 id="5-3-前端教学版：把两轮循环写直白"><a href="#5-3-前端教学版：把两轮循环写直白" class="headerlink" title="5.3 前端教学版：把两轮循环写直白"></a>5.3 前端教学版：把两轮循环写直白</h3><p>我的模块里有一版前端直连模型的两轮循环（<code>src/views/aiChatroom/utils/useSkillsChat.ts</code>），因为逻辑完全摊平、没有服务端参与，特别适合理解协议本身：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// src/views/aiChatroom/utils/useSkillsChat.ts（节选）</span><br><span class="hljs-comment">// ── 第一轮：带工具说明书请求 ──</span><br><span class="hljs-keyword">const</span> firstResponse = <span class="hljs-keyword">await</span> <span class="hljs-title function_">fetch</span>(<span class="hljs-variable constant_">DEEPSEEK_CHAT_URL</span>, &#123;<br>  <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;POST&#x27;</span>,<br>  <span class="hljs-attr">headers</span>: &#123;<br>    <span class="hljs-string">&#x27;Content-Type&#x27;</span>: <span class="hljs-string">&#x27;application/json&#x27;</span>,<br>    <span class="hljs-title class_">Authorization</span>: <span class="hljs-string">`Bearer <span class="hljs-subst">$&#123;apiKey.value&#125;</span>`</span>,<br>  &#125;,<br>  <span class="hljs-attr">body</span>: <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">stringify</span>(&#123;<br>    model,<br>    <span class="hljs-attr">messages</span>: conversationForApi,<br>    <span class="hljs-attr">tools</span>: toolsForApi,     <span class="hljs-comment">// 技能声明（由 ChatSkill 转换而来）</span><br>    <span class="hljs-attr">tool_choice</span>: <span class="hljs-string">&#x27;auto&#x27;</span>,    <span class="hljs-comment">// 模型自己决定调不调</span><br>  &#125;),<br>&#125;)<br><span class="hljs-keyword">const</span> firstData = <span class="hljs-keyword">await</span> firstResponse.<span class="hljs-title function_">json</span>()<br><span class="hljs-keyword">const</span> assistantMsg = firstData.<span class="hljs-property">choices</span>[<span class="hljs-number">0</span>].<span class="hljs-property">message</span><br>messages.<span class="hljs-property">value</span>.<span class="hljs-title function_">push</span>(assistantMsg)   <span class="hljs-comment">// ← 坑 2：先回填 assistant 消息</span><br><br><span class="hljs-comment">// ── 中间：模型要调工具？在本地执行 ──</span><br><span class="hljs-keyword">if</span> (assistantMsg.<span class="hljs-property">tool_calls</span>?.<span class="hljs-property">length</span>) &#123;<br>  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> toolCall <span class="hljs-keyword">of</span> assistantMsg.<span class="hljs-property">tool_calls</span>) &#123;<br>    <span class="hljs-keyword">let</span> <span class="hljs-attr">parsedArgs</span>: <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-built_in">unknown</span>&gt; = &#123;&#125;<br>    <span class="hljs-keyword">try</span> &#123;<br>      parsedArgs = <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">parse</span>(toolCall.<span class="hljs-property">function</span>.<span class="hljs-property">arguments</span>)  <span class="hljs-comment">// ← 坑 1：字符串！</span><br>    &#125; <span class="hljs-keyword">catch</span> &#123; parsedArgs = &#123;&#125; &#125;<br>    <span class="hljs-keyword">const</span> result = <span class="hljs-keyword">await</span> <span class="hljs-title function_">executeSkill</span>(options.<span class="hljs-property">skills</span>, toolCall.<span class="hljs-property">function</span>.<span class="hljs-property">name</span>, parsedArgs)<br>    messages.<span class="hljs-property">value</span>.<span class="hljs-title function_">push</span>(&#123;<br>      <span class="hljs-attr">role</span>: <span class="hljs-string">&#x27;tool&#x27;</span>,<br>      <span class="hljs-attr">content</span>: result,<br>      <span class="hljs-attr">tool_call_id</span>: toolCall.<span class="hljs-property">id</span>,   <span class="hljs-comment">// ← 坑 3：回执编号</span><br>    &#125;)<br>  &#125;<br><br>  <span class="hljs-comment">// ── 第二轮：带着工具结果再请求一次，拿最终回答 ──</span><br>  <span class="hljs-keyword">const</span> secondResponse = <span class="hljs-keyword">await</span> <span class="hljs-title function_">fetch</span>(<span class="hljs-variable constant_">DEEPSEEK_CHAT_URL</span>, &#123;<br>    <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;POST&#x27;</span>,<br>    <span class="hljs-attr">headers</span>: &#123; <span class="hljs-comment">/* ... */</span> &#125;,<br>    <span class="hljs-attr">body</span>: <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">stringify</span>(&#123; model, <span class="hljs-attr">messages</span>: messages.<span class="hljs-property">value</span> &#125;), <span class="hljs-comment">// 含 tool 结果的完整历史</span><br>  &#125;)<br>  <span class="hljs-keyword">const</span> finalMsg = (<span class="hljs-keyword">await</span> secondResponse.<span class="hljs-title function_">json</span>()).<span class="hljs-property">choices</span>[<span class="hljs-number">0</span>].<span class="hljs-property">message</span><br>  messages.<span class="hljs-property">value</span>.<span class="hljs-title function_">push</span>(&#123; <span class="hljs-attr">role</span>: <span class="hljs-string">&#x27;assistant&#x27;</span>, <span class="hljs-attr">content</span>: finalMsg.<span class="hljs-property">content</span> ?? <span class="hljs-string">&#x27;&#x27;</span> &#125;)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>注意这版代码里工具是<strong>在浏览器里执行的</strong>——教学上清晰，但意味着 API Key 和工具实现全部暴露在前端。生产怎么办？看下一节的后端版。</p>
<h3 id="5-4-ChatSkill：让工具声明变成”填表题”"><a href="#5-4-ChatSkill：让工具声明变成”填表题”" class="headerlink" title="5.4 ChatSkill：让工具声明变成”填表题”"></a>5.4 ChatSkill：让工具声明变成”填表题”</h3><p>如果每个工具都要手写一遍 OpenAI tools 格式，太啰嗦。我抽象了一个 <code>ChatSkill</code> 契约，四件套：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// src/views/aiChatroom/skills/types.ts</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">ChatSkill</span> &#123;<br>  <span class="hljs-attr">name</span>: <span class="hljs-built_in">string</span>          <span class="hljs-comment">// 工具名（模型用它下订单）</span><br>  <span class="hljs-attr">description</span>: <span class="hljs-built_in">string</span>   <span class="hljs-comment">// 说明书（模型靠它决定何时触发）</span><br>  <span class="hljs-attr">category</span>?: <span class="hljs-built_in">string</span>     <span class="hljs-comment">// 分类（仅 UI 展示用）</span><br>  <span class="hljs-attr">inputSchema</span>: <span class="hljs-title class_">SkillInputSchema</span>  <span class="hljs-comment">// JSON Schema 参数声明</span><br>  <span class="hljs-attr">handler</span>: <span class="hljs-function">(<span class="hljs-params"><span class="hljs-attr">args</span>: <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-built_in">unknown</span>&gt;</span>) =&gt;</span> <span class="hljs-title class_">Promise</span>&lt;<span class="hljs-built_in">string</span>&gt;  <span class="hljs-comment">// 本地实现</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>再配一个转换函数，把 ChatSkill 映射成 OpenAI tools 格式——<code>inputSchema</code> 直接落到 <code>parameters</code> 位置：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// src/views/aiChatroom/skills/index.ts</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">skillsToOpenAITools</span>(<span class="hljs-params"><span class="hljs-attr">skills</span>: <span class="hljs-title class_">ChatSkill</span>[]</span>): <span class="hljs-title class_">OpenAIToolDefinition</span>[] &#123;<br>  <span class="hljs-keyword">return</span> skills.<span class="hljs-title function_">map</span>(<span class="hljs-function">(<span class="hljs-params">skill</span>) =&gt;</span> (&#123;<br>    <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;function&#x27;</span>,<br>    <span class="hljs-attr">function</span>: &#123;<br>      <span class="hljs-attr">name</span>: skill.<span class="hljs-property">name</span>,<br>      <span class="hljs-attr">description</span>: skill.<span class="hljs-property">description</span>,<br>      <span class="hljs-attr">parameters</span>: skill.<span class="hljs-property">inputSchema</span>,<br>    &#125;,<br>  &#125;))<br>&#125;<br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">executeSkill</span>(<span class="hljs-params">skills, skillName, args</span>): <span class="hljs-title class_">Promise</span>&lt;<span class="hljs-built_in">string</span>&gt; &#123;<br>  <span class="hljs-keyword">const</span> skill = skills.<span class="hljs-title function_">find</span>(<span class="hljs-function">(<span class="hljs-params">item</span>) =&gt;</span> item.<span class="hljs-property">name</span> === skillName)<br>  <span class="hljs-keyword">if</span> (!skill) <span class="hljs-keyword">return</span> <span class="hljs-string">`错误：未找到名为「<span class="hljs-subst">$&#123;skillName&#125;</span>」的技能`</span><br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">await</span> skill.<span class="hljs-title function_">handler</span>(args)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>于是新增一个工具就变成”填表题”，比如这个 mock 天气技能：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// src/views/aiChatroom/skills/commonSkills.ts（节选）</span><br>&#123;<br>  <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;get_weather&#x27;</span>,<br>  <span class="hljs-attr">description</span>: <span class="hljs-string">&#x27;获取指定城市的实时天气信息&#x27;</span>,<br>  <span class="hljs-attr">inputSchema</span>: &#123;<br>    <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;object&#x27;</span>,<br>    <span class="hljs-attr">properties</span>: &#123;<br>      <span class="hljs-attr">city</span>: &#123; <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;string&#x27;</span>, <span class="hljs-attr">description</span>: <span class="hljs-string">&#x27;城市名称，如北京、上海&#x27;</span> &#125;,<br>    &#125;,<br>    <span class="hljs-attr">required</span>: [<span class="hljs-string">&#x27;city&#x27;</span>],<br>  &#125;,<br>  <span class="hljs-attr">handler</span>: <span class="hljs-title function_">async</span> (args) =&gt; &#123;<br>    <span class="hljs-keyword">const</span> <span class="hljs-attr">weatherMap</span>: <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-built_in">string</span>&gt; = &#123;<br>      北京: <span class="hljs-string">&#x27;晴，24°C，湿度 40%&#x27;</span>,<br>      上海: <span class="hljs-string">&#x27;阴，28°C，湿度 70%&#x27;</span>,<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-string">`<span class="hljs-subst">$&#123;args.city&#125;</span> 天气：<span class="hljs-subst">$&#123;weatherMap[args.city] ?? <span class="hljs-string">&#x27;晴转多云，22°C&#x27;</span>&#125;</span>`</span><br>  &#125;,<br>&#125;<br></code></pre></td></tr></table></figure>

<p>我的模块里用这个契约注册了 11 个技能：4 个通用演示（天气&#x2F;计算&#x2F;翻译&#x2F;搜索，全 mock）+ 7 个读取真实项目数据的业务技能（大屏目标查询、KPI、告警列表、飞行路线建议、路由导航等）。<strong>业务技能和通用技能走的是同一套契约</strong>——这就是抽象的力量。</p>
<h3 id="5-5-生产版：后端-Agent-循环"><a href="#5-5-生产版：后端-Agent-循环" class="headerlink" title="5.5 生产版：后端 Agent 循环"></a>5.5 生产版：后端 Agent 循环</h3><p>真正给聊天页面用的版本，工具循环跑在服务端（<code>server/modules/chat-room-agent/chatRoomAgentService.js</code>）。相比教学版的两轮，它有三个升级：</p>
<p><strong>升级一：循环不是固定两轮，而是”直到模型不再要工具”，并设最大轮数防死循环：</strong></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// server/modules/chat-room-agent/chatRoomAgentService.js（节选）</span><br><span class="hljs-comment">// 核心循环：round 从 0 数到 maxToolRounds-1（默认 4），防止无限循环</span><br><span class="hljs-keyword">for</span> (<span class="hljs-keyword">let</span> round = <span class="hljs-number">0</span>; round &lt; config.<span class="hljs-property">agent</span>.<span class="hljs-property">maxToolRounds</span>; round += <span class="hljs-number">1</span>) &#123;<br>  <span class="hljs-keyword">const</span> response = <span class="hljs-keyword">await</span> <span class="hljs-title function_">requestAgentModel</span>(&#123; messages, tools, model, signal &#125;)<br>  <span class="hljs-comment">// 读流转发文字增量、拼装 tool_calls</span><br>  <span class="hljs-keyword">const</span> &#123; assistantMessage, usage &#125; = <span class="hljs-keyword">await</span> <span class="hljs-title function_">consumeAgentStream</span>(response, onText)<br><br>  <span class="hljs-keyword">const</span> toolCalls = assistantMessage.<span class="hljs-property">tool_calls</span> || []<br><br>  <span class="hljs-comment">// 分支 A：没有 tool_calls = 模型认为可以直接回答了，循环出口</span><br>  <span class="hljs-keyword">if</span> (toolCalls.<span class="hljs-property">length</span> === <span class="hljs-number">0</span>) &#123;<br>    <span class="hljs-keyword">return</span> &#123; <span class="hljs-attr">content</span>: assistantMessage.<span class="hljs-property">content</span> || <span class="hljs-string">&#x27;&#x27;</span>, <span class="hljs-attr">rounds</span>: round + <span class="hljs-number">1</span>, <span class="hljs-attr">usedTools</span>: round &gt; <span class="hljs-number">0</span> &#125;<br>  &#125;<br><br>  <span class="hljs-comment">// 分支 B：先回填 assistant.tool_calls 消息（OpenAI 协议要求），</span><br>  <span class="hljs-comment">// 再逐个执行工具、把结果 push 成 tool 消息</span><br>  messages.<span class="hljs-title function_">push</span>(&#123;<br>    <span class="hljs-attr">role</span>: <span class="hljs-string">&#x27;assistant&#x27;</span>,<br>    <span class="hljs-attr">content</span>: assistantMessage.<span class="hljs-property">content</span> || <span class="hljs-literal">null</span>,<br>    <span class="hljs-attr">tool_calls</span>: toolCalls,<br>  &#125;)<br>  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> toolCall <span class="hljs-keyword">of</span> toolCalls) &#123;<br>    <span class="hljs-keyword">await</span> <span class="hljs-title function_">onToolStart</span>(&#123; <span class="hljs-attr">name</span>: toolCall.<span class="hljs-property">function</span>?.<span class="hljs-property">name</span>, <span class="hljs-attr">callId</span>: toolCall.<span class="hljs-property">id</span> &#125;)<br>    <span class="hljs-keyword">const</span> toolResultMessage = <span class="hljs-keyword">await</span> <span class="hljs-title function_">executeToolCall</span>(toolCall)<br>    messages.<span class="hljs-title function_">push</span>(toolResultMessage)<br>    <span class="hljs-keyword">await</span> <span class="hljs-title function_">onToolEnd</span>(&#123; <span class="hljs-attr">name</span>: toolCall.<span class="hljs-property">function</span>?.<span class="hljs-property">name</span>, <span class="hljs-attr">callId</span>: toolCall.<span class="hljs-property">id</span> &#125;)<br>  &#125;<br>  <span class="hljs-comment">// for 循环结束后回到顶部：带着工具结果再次请求模型</span><br>&#125;<br><span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">&#x27;Agent 工具调用超过最大轮数&#x27;</span>)<br></code></pre></td></tr></table></figure>

<p>比如”帮我对比北京和东京的时间，再换算成纽约时间”这类问题，模型可能连续调用工具两三次，固定两轮的写法就歇菜了。</p>
<p><strong>升级二：工具执行前过白名单，报错不打断循环：</strong></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// server/modules/chat-room-agent/agentTool.js（节选）</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">executeAgentTool</span>(<span class="hljs-params">name, rawArguments</span>) &#123;<br>  <span class="hljs-comment">// ① 白名单查表：toolHandlers 里没有这个名字就直接拒绝，</span><br>  <span class="hljs-comment">//    防止模型&quot;幻觉&quot;出一个不存在的函数名（或被提示注入诱导）</span><br>  <span class="hljs-keyword">const</span> handler = toolHandlers[name]<br>  <span class="hljs-keyword">if</span> (!handler) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">`未注册的工具：<span class="hljs-subst">$&#123;name&#125;</span>`</span>)<br><br>  <span class="hljs-comment">// ② 模型返回的 arguments 是 JSON 字符串（不是对象），需要手动 parse</span><br>  <span class="hljs-keyword">const</span> argumentsObject = <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">parse</span>(rawArguments || <span class="hljs-string">&#x27;&#123;&#125;&#x27;</span>)<br><br>  <span class="hljs-comment">// ③ 交给对应的 handler 执行（handler 内部还会再做一层参数校验）</span><br>  <span class="hljs-keyword">return</span> <span class="hljs-title function_">handler</span>(argumentsObject)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>而<strong>工具执行报错</strong>时，不 throw 打断循环，而是把错误也包成一条 tool 消息回填给模型——让模型自己决定怎么向用户解释（换个参数重试，或道歉说明）。这比直接给用户看一坨 stack trace 优雅得多：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// server/modules/chat-room-agent/chatRoomAgentService.js（节选）</span><br>&#125; <span class="hljs-keyword">catch</span> (error) &#123;<br>  <span class="hljs-comment">// 关键设计：工具报错不 throw 打断循环，而是&quot;把错误也告诉模型&quot;，</span><br>  <span class="hljs-comment">// 让模型自己决定怎么向用户解释（比如换个时区重试或道歉说明）</span><br>  <span class="hljs-keyword">return</span> &#123;<br>    <span class="hljs-attr">role</span>: <span class="hljs-string">&#x27;tool&#x27;</span>,<br>    <span class="hljs-attr">tool_call_id</span>: toolCall.<span class="hljs-property">id</span>,<br>    <span class="hljs-attr">content</span>: <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">stringify</span>(&#123; <span class="hljs-attr">error</span>: error.<span class="hljs-property">message</span> &#125;),<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>升级三：流式模式下拼装 tool_calls。</strong> 这是整个 FC 链路最”脏”的活：模型决定调工具时，<code>tool_calls</code> 也是<strong>以增量的形式分片到达</strong>的——<code>id</code> 和 <code>name</code> 只在首个片段出现，<code>arguments</code> 字符串被随机切断：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">片段1 &#123;index:0, id:&#x27;call_x&#x27;, function:&#123;name:&#x27;get_current_time&#x27;, arguments:&#x27;&#123;&quot;time&#x27;&#125;&#125;<br>片段2 &#123;index:0,                    function:&#123;arguments:&#x27;zone&quot;:&quot;Asia/Shanghai&quot;&#125;&#x27;&#125;&#125;<br></code></pre></td></tr></table></figure>

<p><code>index</code> 是每个调用的”工位号”（一次可能调多个工具），拼装逻辑：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// server/modules/chat-room-agent/chatRoomAgentService.js（节选）</span><br><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">consumeAgentStream</span>(<span class="hljs-params">stream, onText</span>) &#123;<br>  <span class="hljs-keyword">let</span> content = <span class="hljs-string">&#x27;&#x27;</span><br>  <span class="hljs-keyword">const</span> toolCalls = []   <span class="hljs-comment">// toolCalls[index]：按&quot;工位号&quot;对号入座</span><br>  <span class="hljs-keyword">let</span> usage = <span class="hljs-literal">null</span><br><br>  <span class="hljs-keyword">for</span> <span class="hljs-keyword">await</span> (<span class="hljs-keyword">const</span> chunk <span class="hljs-keyword">of</span> stream) &#123;<br>    <span class="hljs-keyword">const</span> delta = chunk.<span class="hljs-property">choices</span>?.[<span class="hljs-number">0</span>]?.<span class="hljs-property">delta</span><br>    <span class="hljs-keyword">if</span> (!delta) <span class="hljs-keyword">continue</span><br><br>    <span class="hljs-comment">// ① 文字增量：立刻转发给路由层（马上写成一条 SSE → 打字机效果）</span><br>    <span class="hljs-keyword">if</span> (delta.<span class="hljs-property">content</span>) &#123;<br>      content += delta.<span class="hljs-property">content</span><br>      <span class="hljs-keyword">await</span> <span class="hljs-title function_">onText</span>(delta.<span class="hljs-property">content</span>)<br>    &#125;<br><br>    <span class="hljs-comment">// ② 工具调用增量：骨架不存在先建（首个片段带 id 和 name），</span><br>    <span class="hljs-comment">//    后续片段只追加 arguments 字符串片段</span><br>    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> part <span class="hljs-keyword">of</span> delta.<span class="hljs-property">tool_calls</span> || []) &#123;<br>      <span class="hljs-keyword">const</span> &#123; index, id, <span class="hljs-attr">function</span>: fn &#125; = part<br>      <span class="hljs-keyword">if</span> (!toolCalls[index]) &#123;<br>        toolCalls[index] = &#123; <span class="hljs-attr">id</span>: <span class="hljs-string">&#x27;&#x27;</span>, <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;function&#x27;</span>, <span class="hljs-attr">function</span>: &#123; <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;&#x27;</span>, <span class="hljs-attr">arguments</span>: <span class="hljs-string">&#x27;&#x27;</span> &#125; &#125;<br>      &#125;<br>      <span class="hljs-keyword">if</span> (id) toolCalls[index].<span class="hljs-property">id</span> = id<br>      <span class="hljs-comment">// 用 += 而不是 = ：个别网关会把 name 也拆成多段</span><br>      <span class="hljs-keyword">if</span> (fn?.<span class="hljs-property">name</span>) toolCalls[index].<span class="hljs-property">function</span>.<span class="hljs-property">name</span> += fn.<span class="hljs-property">name</span><br>      <span class="hljs-keyword">if</span> (fn?.<span class="hljs-property">arguments</span>) toolCalls[index].<span class="hljs-property">function</span>.<span class="hljs-property">arguments</span> += fn.<span class="hljs-property">arguments</span><br>    &#125;<br>  &#125;<br><br>  <span class="hljs-comment">// 拼装成与 stream:false 的 message 同构的对象</span><br>  <span class="hljs-keyword">return</span> &#123; <span class="hljs-attr">assistantMessage</span>: &#123; <span class="hljs-attr">role</span>: <span class="hljs-string">&#x27;assistant&#x27;</span>, <span class="hljs-attr">content</span>: content || <span class="hljs-literal">null</span>,<br>          ...(toolCalls.<span class="hljs-property">length</span> ? &#123; <span class="hljs-attr">tool_calls</span>: toolCalls &#125; : &#123;&#125;) &#125;, usage &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>看懂这段，你就理解了”流式 + Function Calling”叠加态的全部复杂性。而文字增量在这段代码里第一优先级就被 <code>onText</code> 转发出去了——<strong>用户不会因为模型在调工具就看不到已生成的文字</strong>，体验和工程在这里汇合。</p>
<h3 id="5-6-工具到底该在前端执行还是后端执行？"><a href="#5-6-工具到底该在前端执行还是后端执行？" class="headerlink" title="5.6 工具到底该在前端执行还是后端执行？"></a>5.6 工具到底该在前端执行还是后端执行？</h3><table>
<thead>
<tr>
<th>维度</th>
<th>前端执行（教学版）</th>
<th>后端执行（生产版）</th>
</tr>
</thead>
<tbody><tr>
<td>API Key</td>
<td>暴露在浏览器（致命）</td>
<td>只存在服务端 <code>.env</code></td>
</tr>
<tr>
<td>工具能力</td>
<td>只能碰浏览器环境（localStorage、页面跳转）</td>
<td>可以碰数据库、内网、文件系统</td>
</tr>
<tr>
<td>适合场景</td>
<td>教学 demo、纯本地小工具</td>
<td>一切正经应用</td>
</tr>
<tr>
<td>中间过程可见性</td>
<td>天然可见</td>
<td>需要 agent_status 事件专门下发</td>
</tr>
</tbody></table>
<p>一个真实的反面教材：我教学版的演示页图省事，把一个测试 Key 硬编码在了前端源码里。这类 Key 一旦发布就等于公开（打包产物、浏览器 DevTools 里都看得到），<strong>任何出现在前端的 Key 都应该视为已泄露、立即作废</strong>。生产架构里，前端只跟自己的后端说话，Key 和工具循环全部收在服务端。</p>
<p><a id="sec6"></a></p>
<h2 id="6-落地成应用：demo-和工程之间隔着什么"><a href="#6-落地成应用：demo-和工程之间隔着什么" class="headerlink" title="6. 落地成应用：demo 和工程之间隔着什么"></a>6. 落地成应用：demo 和工程之间隔着什么</h2><h3 id="6-1-接口的演进：同一个问题的三次回答"><a href="#6-1-接口的演进：同一个问题的三次回答" class="headerlink" title="6.1 接口的演进：同一个问题的三次回答"></a>6.1 接口的演进：同一个问题的三次回答</h3><p>我的服务端现在并存三个对话接口，恰好记录了这条链路的演进：</p>
<table>
<thead>
<tr>
<th>接口</th>
<th>定位</th>
<th>关键能力</th>
</tr>
</thead>
<tbody><tr>
<td><code>POST /api/chat</code></td>
<td>第一版：单轮直通</td>
<td>最简 SSE 转发，无历史、无工具、无结束标记</td>
</tr>
<tr>
<td><code>POST /api/openai-example/chat</code></td>
<td>第二版：规范示例</td>
<td>补上 <code>[DONE]</code>、charset、abort 接线、requestId</td>
</tr>
<tr>
<td><code>POST /api/chat-room-agent</code></td>
<td>生产版：Agent</td>
<td>多轮历史 + 工具循环 + agent_status 事件 + 错误事件</td>
</tr>
</tbody></table>
<p>保留旧版本不是冗余——<strong>对照着重读三版代码，能看到每个设计决策是为什么出现的</strong>，这比直接看最终形态收获大得多。</p>
<h3 id="6-2-后端分层与配置集中"><a href="#6-2-后端分层与配置集中" class="headerlink" title="6.2 后端分层与配置集中"></a>6.2 后端分层与配置集中</h3><p>服务端按”上层只调下层”分了四层：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">server.js    启动入口（listen、端口占用处理）<br>app.js       应用工厂（中间件、CORS 白名单、路由挂载、统一错误兜底）<br>routes/      路由层：HTTP 进出 + SSE 翻译，不写业务<br>modules/     业务层：调模型、跑 Agent 循环<br>config.js    配置中心：唯一有权读 process.env 的文件<br></code></pre></td></tr></table></figure>

<p>配置集中是一条硬规矩：业务代码不允许直接读 <code>process.env</code>，统一从 <code>config.js</code> 拿。<code>.env</code> 里的关键配置项：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">PORT=3000<br>OPENAI_API_KEY=...        # 模型密钥，只存在服务端<br>OPENAI_BASE_URL=...       # 兼容网关地址（官方 / DeepSeek / 中转站）<br>OPENAI_MODEL=...          # 默认模型<br>CORS_ORIGIN=http://localhost:8080   # 前端来源白名单<br>AGENT_MAX_TOOL_ROUNDS=4   # Agent 循环最大轮数<br></code></pre></td></tr></table></figure>

<p>CORS 用的是白名单函数式校验（<code>app.js</code> 里 <code>cors()</code> 的 <code>origin</code> 回调）：无 Origin 的 curl&#x2F;Postman 放行、命中白名单放行、其余拒绝——比 <code>origin: &#39;*&#39;</code> 安全，比写死单个 origin 灵活。</p>
<h3 id="6-3-一个真实业务技能长什么样"><a href="#6-3-一个真实业务技能长什么样" class="headerlink" title="6.3 一个真实业务技能长什么样"></a>6.3 一个真实业务技能长什么样</h3><p>通用技能（天气&#x2F;计算）是 mock，业务技能是动真格的。以”查询大屏目标状态”为例，它的 handler 直接读取项目里 TW 大屏的真实数据源，模型问到时返回的是<strong>此刻的真实数据</strong>；另一个 <code>search_demo_route</code> 技能的输入数据来自对路由配置的扁平化目录——用户说”打开说明 &#x2F;twScreen 那个页面”，模型查目录、生成可点击的路由卡片（component 块）直接渲染在气泡里。</p>
<p>这就是 Function Calling 落地的完整闭环：<strong>自然语言 → 模型选工具 → 真实数据 → 结构化块 → 交互组件</strong>。</p>
<h3 id="6-4-安全清单"><a href="#6-4-安全清单" class="headerlink" title="6.4 安全清单"></a>6.4 安全清单</h3><p>最后把散落在各章的安全点收拢成一张清单，每一条都对应真实代码里的防御：</p>
<ol>
<li><strong>API Key 只放服务端 <code>.env</code></strong>（前端直连模型仅限教学演示）；</li>
<li><strong>markdown 渲染必须净化</strong>：<code>markdown-it</code> 开了 <code>html:true</code> + <code>v-html</code> 渲染模型输出 &#x3D; 渲染不可信内容，生产必须过 DOMPurify 防 XSS；</li>
<li><strong>工具执行白名单查表</strong>：模型”幻觉”出的函数名直接拒绝，防提示注入；</li>
<li><strong>工具入参先校验再使用</strong>：永远不要信任模型生成的参数（我的 <code>get_current_time</code> handler 第一行就是校验 <code>timezone</code> 是非空字符串）；</li>
<li><strong>CORS 白名单</strong>而非全放开；</li>
<li><strong>服务端消息校验</strong>：角色白名单（<code>system/user/assistant/tool</code>）、条数上限、单条长度上限——既是安全也是省钱（防超长上下文烧 token）。</li>
</ol>
<p><a id="sec7"></a></p>
<h2 id="7-展望：MCP-离我们还有多远"><a href="#7-展望：MCP-离我们还有多远" class="headerlink" title="7. 展望：MCP 离我们还有多远"></a>7. 展望：MCP 离我们还有多远</h2><p>写到这你可能听说过 MCP（Model Context Protocol）并感到焦虑：Function Calling 会不会很快被淘汰？</p>
<p>先把概念对齐。FC 和 MCP 解决的是同一件事的不同侧面：</p>
<table>
<thead>
<tr>
<th>维度</th>
<th>Function Calling</th>
<th>MCP</th>
</tr>
</thead>
<tbody><tr>
<td>是什么</td>
<td>模型 API 的一个参数协议</td>
<td>一套完整的客户端-服务器协议</td>
</tr>
<tr>
<td>工具从哪来</td>
<td>每个应用自己声明、写死在代码里</td>
<td>服务器统一注册，客户端动态发现</td>
</tr>
<tr>
<td>工具在哪执行</td>
<td>你自己的进程里</td>
<td>独立的 MCP 服务器进程里</td>
</tr>
<tr>
<td>生态</td>
<td>各家 API 格式略有差异</td>
<td>统一协议，工具可跨应用复用</td>
</tr>
</tbody></table>
<p>一个比喻：<strong>FC 是”点菜”，MCP 是”美食广场”</strong>——FC 里每家餐厅（应用）各自印菜单；MCP 把工具做成独立的档口（MCP Server），任何应用（MCP Client）进来都能发现并点单。</p>
<p>我的模块里有两个 “MCP 风格”的演示页，但说实话：<strong>它们不是 MCP，是用 FC 模拟 MCP 的思想</strong>——把工具声明 JSON Schema 化、声明与执行分离、可以动态增删技能。真正的 MCP 还有一整套传输层（stdio &#x2F; SSE transport）、会话管理和发现机制。理解了 FC 再看 MCP，会发现它不过是把”工具说明书”从你的代码里搬到了协议层——<strong>FC 是地基，MCP 是地基上的标准化</strong>。先吃透 FC，MCP 只是多学一层皮。</p>
<p><a id="sec8"></a></p>
<h2 id="8-写在最后"><a href="#8-写在最后" class="headerlink" title="8. 写在最后"></a>8. 写在最后</h2><p>回头看，这个模块跑通的东西可以压缩成两条心智模型：</p>
<blockquote>
<p><strong>流式 &#x3D; 增量协议 + 状态机。</strong> 服务端把生成过程切成 chunk 逐条推送（<code>data: JSON\n\n</code> + <code>[DONE]</code>），前端用”占位气泡 + 增量合并 + 响应式更新”把流翻译成界面。所有细节——多字节解码、半行缓冲、竞态门票、错误事件——都是这两个词的注脚。</p>
</blockquote>
<blockquote>
<p><strong>Function Calling &#x3D; 声明与执行分离 + 两轮循环。</strong> 模型只读说明书（tools）下订单（tool_calls），执行永远在你的代码里（handler），结果以 <code>role:&#39;tool&#39;</code> + <code>tool_call_id</code> 回填，再要一轮请求拿最终回答。安全的关键就一句话：<strong>白名单 + 不信任模型给的任何参数</strong>。</p>
</blockquote>
<p>前端工程师做 AI 应用，新东西其实只有这两条协议；剩下的——分层、状态管理、组件设计、错误处理——都是我们的老本行。这也是我做完这个模块最大的感受：<strong>AI 应用开发是前端的延伸，不是转行</strong>。</p>
<h3 id="附：完整文件地图"><a href="#附：完整文件地图" class="headerlink" title="附：完整文件地图"></a>附：完整文件地图</h3><table>
<thead>
<tr>
<th>文件</th>
<th>职责</th>
</tr>
</thead>
<tbody><tr>
<td><code>src/views/aiChatroom/chatRoom.vue</code></td>
<td>UI 编排层：组件接线 + 页面骨架</td>
</tr>
<tr>
<td><code>src/views/aiChatroom/composables/useAgentChat.ts</code></td>
<td>状态层：流 → 气泡、竞态防护、多轮历史</td>
</tr>
<tr>
<td><code>src/views/aiChatroom/composables/useChatSession.ts</code></td>
<td>会话层：多对话切换 + localStorage 持久化</td>
</tr>
<tr>
<td><code>src/views/aiChatroom/utils/useSSEChat.ts</code></td>
<td>传输层：fetch + 手写 SSE 解析（唯一碰网络）</td>
</tr>
<tr>
<td><code>src/views/aiChatroom/utils/useSkillsChat.ts</code></td>
<td>前端版 FC 两轮循环（教学）</td>
</tr>
<tr>
<td><code>src/views/aiChatroom/skills/types.ts</code></td>
<td>ChatSkill 契约定义</td>
</tr>
<tr>
<td><code>src/views/aiChatroom/skills/index.ts</code></td>
<td>skillsToOpenAITools &#x2F; executeSkill 桥接</td>
</tr>
<tr>
<td><code>src/views/aiChatroom/skills/commonSkills.ts</code></td>
<td>通用技能（天气&#x2F;计算&#x2F;翻译&#x2F;搜索）</td>
</tr>
<tr>
<td><code>src/views/aiChatroom/skills/projectSkills.ts</code></td>
<td>业务技能（大屏数据&#x2F;路由导航）</td>
</tr>
<tr>
<td><code>src/views/aiChatroom/types/chat.ts</code></td>
<td>三条数据主线：SSEData → MessageBlock → ChatMessage</td>
</tr>
<tr>
<td><code>src/views/aiChatroom/components/InteractiveRenderer.vue</code></td>
<td>块渲染器：text&#x2F;image&#x2F;component&#x2F;agent_status 分发</td>
</tr>
<tr>
<td><code>server/routes/chat.js</code></td>
<td>v1：单轮流式直通（教学保留）</td>
</tr>
<tr>
<td><code>server/routes/chat-room-agent.js</code></td>
<td>v3：Agent 接口（SSE 翻译 + 中断传播）</td>
</tr>
<tr>
<td><code>server/modules/chat-room-agent/chatRoomAgentService.js</code></td>
<td>Agent 核心循环 + 流式 tool_calls 拼装</td>
</tr>
<tr>
<td><code>server/modules/chat-room-agent/agentTool.js</code></td>
<td>工具说明书 + 白名单执行</td>
</tr>
<tr>
<td><code>server/config.js</code></td>
<td>配置中心（唯一读 process.env 的文件）</td>
</tr>
</tbody></table>
<h3 id="跑起来"><a href="#跑起来" class="headerlink" title="跑起来"></a>跑起来</h3><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 服务端（需要 .env 里配置 OPENAI_API_KEY / OPENAI_BASE_URL）</span><br><span class="hljs-built_in">cd</span> server &amp;&amp; npm install &amp;&amp; npm start    <span class="hljs-comment"># localhost:3000</span><br><br><span class="hljs-comment"># 前端</span><br>npm install &amp;&amp; npm run dev               <span class="hljs-comment"># localhost:8080，进入 /chat 路由</span><br></code></pre></td></tr></table></figure>

<p>如果你也想系统性走一遍这条链路，我在项目里维护了一份按阶段划分的学习路线（<code>docs/learning/智能问答学习路线.md</code>），从画数据流图开始，到亲手新增一个技能结束。<strong>最好的入门方式，永远是动手改一版自己的</strong>。</p>
]]></content>
      <categories>
        <category>AI 应用开发</category>
      </categories>
      <tags>
        <tag>Vue3</tag>
        <tag>AI</tag>
        <tag>SSE</tag>
        <tag>Function Calling</tag>
        <tag>Node.js</tag>
        <tag>LLM</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS 工程化演进：从命名地狱到原子化与原生回归</title>
    <url>/2026/09/09/css-engineering/</url>
    <content><![CDATA[<p>CSS 工程化的工具十几年代际更替，但它们都在回应同一件事：<strong>CSS 没有模块系统。</strong> 接手过老项目的人都见过 <code>.btn</code> 全站几十处引用、三层 <code>!important</code> 叠罗汉——不是谁的代码写得烂，是「所有样式共享一个全局命名空间」这个默认假设，在项目规模化之后必然崩塌。</p>
<p>把所有方案放进一个坐标系，整部演进史一目了然：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">CSS 的三个原生缺陷                 各时代的回应<br>──────────────────────────────────────────────────────<br>全局作用域（谁都能覆盖谁）     →   BEM / CSS Modules / scoped / @scope<br>级联失控（特异性军备竞赛）     →   ITCSS / @layer / :where()<br>死代码（删了组件不敢删样式）   →   原子化 CSS 的按需生成<br></code></pre></td></tr></table></figure>

<span id="more"></span>

<h2 id="一、问题的根源：CSS-生来没有「工程」这根筋"><a href="#一、问题的根源：CSS-生来没有「工程」这根筋" class="headerlink" title="一、问题的根源：CSS 生来没有「工程」这根筋"></a>一、问题的根源：CSS 生来没有「工程」这根筋</h2><p>CSS 诞生于 1996 年，服务的是「文档」：几十个页面、一个作者。现代应用是几万个组件、几十个开发者——设计假设和现实差了三个数量级，于是有三个原生缺陷。</p>
<p><strong>全局作用域</strong>：每条规则都注册进同一个命名空间，任何规则都可能命中任何元素。JS 2015 年就有了 ES Module，CSS 的「模块」至今仍靠社区方案模拟——这是整条演进史的主线。</p>
<p><strong>级联失控</strong>：胜负由 <code>!important</code> &gt; 特异性 &gt; 源顺序三层博弈决定，而特异性没有上限，最后大家一起摆烂：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-class">.card</span> <span class="hljs-selector-class">.title</span> &#123; <span class="hljs-attribute">color</span>: gray; &#125;              <span class="hljs-comment">/* 0-2-0 */</span><br><span class="hljs-selector-id">#app</span> <span class="hljs-selector-class">.title</span>  &#123; <span class="hljs-attribute">color</span>: gray; &#125;              <span class="hljs-comment">/* 1-1-0，id 一出彻底失控 */</span><br><span class="hljs-selector-class">.card</span> <span class="hljs-selector-class">.title</span> &#123; <span class="hljs-attribute">color</span>: red <span class="hljs-meta">!important</span>; &#125;    <span class="hljs-comment">/* 终局：全员 important */</span><br></code></pre></td></tr></table></figure>

<p><strong>死代码</strong>：CSS 与 DOM 是字符串匹配的松耦合——删了组件，没人敢确定样式是否还有别处在用；构建器也无法 tree-shaking（选择器命中谁只有运行时知道）。样式表体积只增不减。</p>
<p>一句话：<strong>CSS 缺的不是功能，是模块系统——没有边界、没有依赖关系、没有可回收性。</strong> 后面所有方案都是在给这三个洞打补丁。</p>
<h2 id="二、靠纪律：命名规范（BEM）"><a href="#二、靠纪律：命名规范（BEM）" class="headerlink" title="二、靠纪律：命名规范（BEM）"></a>二、靠纪律：命名规范（BEM）</h2><p>思路很朴素：语言不给我作用域，就让命名承担唯一性。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">article</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;card&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">h3</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;card__title&quot;</span>&gt;</span>标题<span class="hljs-tag">&lt;/<span class="hljs-name">h3</span>&gt;</span>                            <span class="hljs-comment">&lt;!-- Element 用 __ --&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">button</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;card__btn card__btn--disabled&quot;</span>&gt;</span>删除<span class="hljs-tag">&lt;/<span class="hljs-name">button</span>&gt;</span>  <span class="hljs-comment">&lt;!-- 状态用 -- --&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">article</span>&gt;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-class">.card</span>                &#123; <span class="hljs-attribute">padding</span>: <span class="hljs-number">16px</span>; &#125;                      <span class="hljs-comment">/* Block */</span><br><span class="hljs-selector-class">.card__btn--disabled</span> &#123; <span class="hljs-attribute">opacity</span>: .<span class="hljs-number">4</span>; <span class="hljs-attribute">pointer-events</span>: none; &#125;  <span class="hljs-comment">/* 永远只写单类选择器 */</span><br></code></pre></td></tr></table></figure>

<p>两个机制层面的巧思：<strong>扁平化特异性</strong>（只写 0-1-0 的单类选择器，从机制上杜绝竞赛）、<strong>命名即文档</strong>（<code>card__btn--disabled</code> 不看上下文就知道来历）。OOCSS、SMACSS、ITCSS 同一思路，本质都是<strong>没有任何强制力的约定</strong>。</p>
<p>局限也就出在这儿：新人一句 <code>.card .card__btn</code>，特异性就涨了；跨项目复用还是复制粘贴。纪律方案的天花板是「团队永远不犯错」——这不是工程，是修行。</p>
<h2 id="三、靠预处理：Sass——优化「写」，不改变「跑」"><a href="#三、靠预处理：Sass——优化「写」，不改变「跑」" class="headerlink" title="三、靠预处理：Sass——优化「写」，不改变「跑」"></a>三、靠预处理：Sass——优化「写」，不改变「跑」</h2><p>瞄准的是编写体验：没有变量、没有复用单元、几千行文件全靠人肉导航。</p>
<figure class="highlight scss"><table><tr><td class="code"><pre><code class="hljs scss"><span class="hljs-variable">$primary</span>: <span class="hljs-number">#1677ff</span>;                        <span class="hljs-comment">// 变量：设计值一处定义</span><br><br><span class="hljs-selector-class">.card</span> &#123;<br>  <span class="hljs-attribute">padding</span>: <span class="hljs-number">16px</span>;<br>  &amp;__title &#123; <span class="hljs-attribute">color</span>: <span class="hljs-variable">$primary</span>; &#125;           <span class="hljs-comment">// 嵌套 + &amp;：与 BEM 天作之合</span><br>  &amp;<span class="hljs-selector-pseudo">:hover</span>  &#123; <span class="hljs-attribute">box-shadow</span>: <span class="hljs-number">0</span> <span class="hljs-number">2px</span> <span class="hljs-number">8px</span> <span class="hljs-built_in">rgba</span>(<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>, .<span class="hljs-number">15</span>); &#125;<br>&#125;<br><br><span class="hljs-keyword">@use</span> <span class="hljs-string">&quot;components/button&quot;</span>;                 <span class="hljs-comment">// 文件级拆分，带命名空间引入</span><br></code></pre></td></tr></table></figure>

<p>（另有 mixin 提供带参数的样式片段。）但要看清编译产物：嵌套、变量全部被摊平成普通的全局规则。<strong>Sass 优化的是「编写时」的体验，完全不改变「运行时」的结果</strong>——产物依旧是一张全局样式表，三大缺陷一个没解决；嵌套随手一写还是后代选择器，特异性反而悄悄上涨。它是舒适层，不是答案。</p>
<p>（后话：变量和嵌套如今都进了原生 CSS，见第七节——预处理器的增量价值正在归零。）</p>
<h2 id="四、靠构建：CSS-Modules——作用域终于有了"><a href="#四、靠构建：CSS-Modules——作用域终于有了" class="headerlink" title="四、靠构建：CSS Modules——作用域终于有了"></a>四、靠构建：CSS Modules——作用域终于有了</h2><p>先定位 PostCSS：<strong>CSS 界的 Babel</strong>（解析成 AST → 插件转换 → 序列化回去），Autoprefixer、cssnano 都跑在它上面；CSS Modules 也构建在这层（webpack 的 css-loader、Vite 的 <code>.module.css</code> 文件名约定）。</p>
<p>CSS Modules 的核心思想：<strong>语言层没有作用域，那就在构建层造一个——把每个文件的类名改写成全局唯一的名字。</strong></p>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-comment">/* Button.module.css */</span><br><span class="hljs-selector-class">.btn</span>     &#123; <span class="hljs-attribute">padding</span>: <span class="hljs-number">8px</span> <span class="hljs-number">16px</span>; &#125;<br><span class="hljs-selector-class">.primary</span> &#123; composes: btn; <span class="hljs-attribute">background</span>: <span class="hljs-number">#1677ff</span>; &#125;  <span class="hljs-comment">/* composes：组合而非复制 */</span><br>:<span class="hljs-built_in">global</span>(.reset-all) &#123; <span class="hljs-attribute">all</span>: unset; &#125;               <span class="hljs-comment">/* 逃生舱：显式声明全局规则 */</span><br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">import</span> styles <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;./Button.module.css&#x27;</span>;<br><br><span class="hljs-comment">// 类名被 hash 改写，import 拿到「原始名 → 改写名」的映射表：</span><br><span class="hljs-comment">// styles.primary =&gt; &quot;Button_primary_3zK9 Button_btn_1xY2&quot;</span><br><span class="language-xml"><span class="hljs-tag">&lt;<span class="hljs-name">button</span> <span class="hljs-attr">className</span>=<span class="hljs-string">&#123;styles.primary&#125;</span>&gt;</span>确定<span class="hljs-tag">&lt;/<span class="hljs-name">button</span>&gt;</span></span>;<br></code></pre></td></tr></table></figure>

<p>编译产物里 <code>.btn</code> 变成 <code>.Button_btn_1xY2</code>——两个文件各有一个 <code>.btn</code>，hash 后各管各的，<strong>冲突在机制上不可能</strong>；样式第一次有了 import 依赖关系，「删组件能不能删样式」变成可分析的问题；<code>composes</code> 用组合替代复制，比 Sass mixin 的摊平干净得多。</p>
<p>代价：类名变成黑话（调试靠 devtools 源码映射）、动态拼接要写 <code>styles[variant]</code>、CSS 与 JS 从松耦合变成强绑定。</p>
<h2 id="五、靠-JS：CSS-in-JS-与-Vue-scoped——样式跟着组件走"><a href="#五、靠-JS：CSS-in-JS-与-Vue-scoped——样式跟着组件走" class="headerlink" title="五、靠 JS：CSS-in-JS 与 Vue scoped——样式跟着组件走"></a>五、靠 JS：CSS-in-JS 与 Vue scoped——样式跟着组件走</h2><p>组件化时代的诛心之问：BEM 的「Block」和组件分明是一回事，为什么人肉维护两套对应关系？于是样式直接搬进组件。</p>
<p><strong>运行时派（styled-components）</strong>：样式即组件，类名运行时生成（hash 串）注入 <code>&lt;style&gt;</code> 标签，样式拿到完整 JS 表达力（读 props、读主题：<code>background: ${props =&gt; …}</code>）、按组件注入零死代码。代价同样硬核：首屏要先执行 JS 才有样式（性能与 CLS 受拖累）、SSR 要服务端收集关键样式、与 RSC 不兼容——React 团队已明确不推荐新项目使用，此派退潮。</p>
<p><strong>零运行时派（vanilla-extract &#x2F; linaria &#x2F; PandaCSS）</strong>：写法长得像 CSS-in-JS（TS 里写样式、传变量），但样式在<strong>编译期</strong>就被抽成静态 CSS 文件。思路完成回归：<strong>表达力归 JS，交付物归 CSS。</strong></p>
<p><strong>Vue 的中庸答案（SFC scoped）</strong>——给选择器加尾巴，给元素盖印章：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">源码                          编译产物<br>&lt;style scoped&gt;                .btn[data-v-7ba5bd90] &#123; … &#125;<br>  .btn &#123; … &#125;<br><br>&lt;button class=&quot;btn&quot;&gt;          &lt;button class=&quot;btn&quot; data-v-7ba5bd90&gt;<br></code></pre></td></tr></table></figure>

<p>和 CSS Modules 同代同思路（构建期作用域），只是隔离单位从「文件」变成「组件的 DOM 归属」。两个高频细节：</p>
<ul>
<li><strong>子组件的根元素会被同时打上父组件的 hash</strong>——方便父级微调子组件根样式，也是「为什么子组件吃到了父组件样式」这个经典困惑的来源；</li>
<li><strong>scoped 只隔离「出去」，不拦截「进来」</strong>：全局样式照样命中你的组件，特异性竞赛依然存在——它是好的默认起点，不是完备的隔离方案。</li>
</ul>
<p>动态样式交给 Vue 3.2+ 的 <code>v-bind()</code>，编译产物就是 CSS 变量（第七节）。</p>
<h2 id="六、靠原子化：Tailwind-UnoCSS——不命名，就没有命名冲突"><a href="#六、靠原子化：Tailwind-UnoCSS——不命名，就没有命名冲突" class="headerlink" title="六、靠原子化：Tailwind &#x2F; UnoCSS——不命名，就没有命名冲突"></a>六、靠原子化：Tailwind &#x2F; UnoCSS——不命名，就没有命名冲突</h2><p>前几代方案有一个共同软肋：都需要「人为命名」，而冲突、死代码、不一致恰恰都从命名来。原子化掀了这个前提：<strong>不命名</strong>——每个类只做一件小事，类名即样式描述，由工具生成、全局唯一：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">button</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;px-4 py-2 rounded-md bg-blue-600 text-white hover:bg-blue-700&quot;</span>&gt;</span>确定<span class="hljs-tag">&lt;/<span class="hljs-name">button</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>工程价值在三件事：</p>
<ol>
<li><strong>JIT 按需生成，死 CSS 被机制性消灭</strong>：只为源码中实际出现的类名字符串生成规则，源码里没有的类产物里就不存在——样式体积随「当页用到的原子类」收敛，而非随项目年龄增长；</li>
<li><strong>设计 token 内建为刻度</strong>：<code>p-2</code>&#x2F;<code>p-4</code> 来自间距刻度、<code>blue-600</code> 来自色板——没有「随手写 13px」的自由，一致性靠工具而不是靠 review 吼；</li>
<li><strong>与组件化天作之合</strong>：HTML 住在组件里，样式写在 HTML 上，等于「删组件即删样式」，工程化追求的局部可推理性在这里闭环。</li>
</ol>
<p>争议同样真实：类名串长、学习曲线。实践结论：原子类只写在<strong>组件模板</strong>里（组件本身就是封装边界），长类名串收敛进组件内部；散在页面级 HTML 上才是灾难。UnoCSS 是同思路的另一实现（preset 可组合）；Tailwind v4 底层已改用原生 <code>@layer</code> 保证 utilities 覆盖组件样式——正是第七节的主角。</p>
<h2 id="七、原生反击：标准正在逐个收回工程化诉求"><a href="#七、原生反击：标准正在逐个收回工程化诉求" class="headerlink" title="七、原生反击：标准正在逐个收回工程化诉求"></a>七、原生反击：标准正在逐个收回工程化诉求</h2><p>一条清晰的规律：<strong>社区方案每验证一个需求，标准就吸收一个。</strong></p>
<p><strong>CSS 变量</strong>——运行时的变量：可继承、可按选择器覆盖、可被 JS 读写，这是 Sass 变量没有的东西：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-pseudo">:root</span> &#123; <span class="hljs-attr">--primary</span>: <span class="hljs-number">#1677ff</span>; &#125;<br><span class="hljs-selector-class">.btn</span> &#123; <span class="hljs-attribute">background</span>: <span class="hljs-built_in">var</span>(--primary); &#125;<br><span class="hljs-selector-attr">[data-theme=<span class="hljs-string">&quot;dark&quot;</span>]</span> &#123; <span class="hljs-attr">--primary</span>: <span class="hljs-number">#4096ff</span>; &#125;  <span class="hljs-comment">/* 切主题 = 换一组变量值 */</span><br></code></pre></td></tr></table></figure>

<p><strong>@layer</strong>——把优先级从「特异性竞赛」变成「层声明顺序」：层序即胜负，与层内选择器写法无关。ITCSS 讲了十年的分层第一次成为语言机制：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-keyword">@layer</span> reset, base, components, utilities;   <span class="hljs-comment">/* 后声明的层胜出 */</span><br><br><span class="hljs-keyword">@layer</span> components &#123; <span class="hljs-selector-id">#app</span> <span class="hljs-selector-class">.btn</span> &#123; <span class="hljs-attribute">background</span>: gray; &#125; &#125;    <span class="hljs-comment">/* 1-1-0 也没用 */</span><br><span class="hljs-keyword">@layer</span> utilities  &#123; <span class="hljs-selector-class">.bg-blue</span> &#123; <span class="hljs-attribute">background</span>: <span class="hljs-number">#1677ff</span>; &#125; &#125;  <span class="hljs-comment">/* 单类照样赢 */</span><br></code></pre></td></tr></table></figure>

<p><strong>@scope</strong>——原生作用域，还带一个「洞」（donut scoping）。「组件嵌套同款组件导致选择器越界」这个 BEM 时代的老大难，标准直接给了语法（2024 年起三大内核全绿）：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-keyword">@scope</span> (.card) to (.card__content) &#123;<br>  <span class="hljs-selector-tag">img</span> &#123; <span class="hljs-attribute">border-radius</span>: <span class="hljs-number">8px</span>; &#125;  <span class="hljs-comment">/* 只命中 .card 内、.card__content 之外的 img */</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>原生嵌套与 :where()</strong>——Sass 的两大卖点进了标准（2023 年起主流全绿）；<code>:where()</code> 特异性归零，默认样式不怕被业务样式覆盖不了。</p>
<p>判断：2026 年的新项目，「CSS 变量 + <code>@layer</code> 分层 + 原生嵌套 + 一套 utility 约定」已能覆盖过去一整套工具链的大部分诉求；工具链的剩余价值收敛到两件事——<strong>token 的约束力</strong>（Tailwind）和<strong>构建期作用域</strong>（Modules&#x2F;scoped），而后者 <code>@scope</code> 正在路上。</p>
<h2 id="八、选型地图"><a href="#八、选型地图" class="headerlink" title="八、选型地图"></a>八、选型地图</h2><table>
<thead>
<tr>
<th>方案</th>
<th>解决的核心问题</th>
<th>代价 &#x2F; 风险</th>
<th>适用场景</th>
</tr>
</thead>
<tbody><tr>
<td>命名规范（BEM&#x2F;ITCSS）</td>
<td>命名冲突（靠约定）</td>
<td>无强制力，全靠人遵守</td>
<td>无构建链路的老项目、静态站</td>
</tr>
<tr>
<td>预处理器（Sass&#x2F;Less）</td>
<td>编写效率（变量&#x2F;复用）</td>
<td>产物仍是全局 CSS</td>
<td>任何时代的舒适层，不再是关键决策</td>
</tr>
<tr>
<td>CSS Modules</td>
<td>作用域（构建期 hash）</td>
<td>类名黑话、与 JS 强绑定</td>
<td>React 生态、中大型应用</td>
</tr>
<tr>
<td>Vue scoped</td>
<td>作用域（组件 DOM 归属）</td>
<td>只隔离「出去」，不拦截「进来」</td>
<td>Vue SFC 的默认答案</td>
</tr>
<tr>
<td>CSS-in-JS</td>
<td>作用域 + 动态样式全表达力</td>
<td>运行时成本、SSR、RSC 摩擦</td>
<td>强主题组件库，优先零运行时实现</td>
</tr>
<tr>
<td>原子化（Tailwind&#x2F;UnoCSS）</td>
<td>死代码 + 一致性 token</td>
<td>类名串、学习曲线</td>
<td>组件化项目、追求交付效率的团队</td>
</tr>
<tr>
<td>原生新特性</td>
<td>作用域 + 优先级 + 变量</td>
<td>需要较新的浏览器基线</td>
<td>新项目的默认基座</td>
</tr>
</tbody></table>
<p>几条决策直觉：</p>
<ul>
<li><strong>先看框架再选方案</strong>：Vue 默认 scoped（复杂设计系统再叠 UnoCSS）；React 在 CSS Modules 和 Tailwind 之间二选一基本不会错；做组件库才需要认真评估零运行时 CSS-in-JS；</li>
<li><strong>团队越大越选「约束型」方案</strong>：刻度、分层都是用机制换一致性——工程化的意义是让一致性不依赖个人自觉；</li>
<li><strong>存量项目别推倒重来</strong>：用 <code>@layer</code> 把旧样式垫在底层、新代码写新层，两套体系共存且优先级明确，按页面粒度推进；</li>
<li><strong>要硬隔离只有 Shadow DOM &#x2F; iframe</strong>：微前端、嵌入第三方页面的组件（客服气泡、支付控件），类名 hash 不够——Shadow DOM 内外样式互相不可见（可继承属性除外），才是物理边界；</li>
<li><strong>比工具更根本的是设计 token</strong>：颜色、间距、字号的刻度统一了，用什么工具都乱不到哪去；刻度没统一，什么工具都救不回来。</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>演进一览，也是全文的坐标系：</p>
<table>
<thead>
<tr>
<th>阶段</th>
<th>代表</th>
<th>靠什么</th>
<th>解决了</th>
<th>留下了</th>
</tr>
</thead>
<tbody><tr>
<td>纪律</td>
<td>BEM &#x2F; ITCSS</td>
<td>人</td>
<td>命名冲突</td>
<td>无强制力</td>
</tr>
<tr>
<td>预处理</td>
<td>Sass</td>
<td>编译器</td>
<td>编写效率</td>
<td>产物仍全局</td>
</tr>
<tr>
<td>构建</td>
<td>CSS Modules &#x2F; scoped</td>
<td>构建器</td>
<td>作用域</td>
<td>类名黑话</td>
</tr>
<tr>
<td>运行时</td>
<td>styled-components</td>
<td>JS</td>
<td>作用域 + 动态</td>
<td>性能 &#x2F; SSR</td>
</tr>
<tr>
<td>原子化</td>
<td>Tailwind &#x2F; UnoCSS</td>
<td>生成器</td>
<td>死代码 + 一致性</td>
<td>可读性争议</td>
</tr>
<tr>
<td>原生</td>
<td>@layer &#x2F; @scope</td>
<td>标准</td>
<td>以上全部（渐进）</td>
<td>浏览器基线</td>
</tr>
</tbody></table>
<p>面试速答：</p>
<ul>
<li><strong>CSS 工程化到底在解决什么？</strong> CSS 没有模块系统，三个原生缺陷：全局作用域、级联失控、死代码。所有方案都是对这三个问题的不同回应。</li>
<li><strong>CSS Modules 的原理？</strong> 构建期把类名改写为「文件路径 + hash」的全局唯一名，JS import 得到映射表；冲突在机制上不可能，composes 提供组合能力。</li>
<li><strong>Vue scoped 的原理和局限？</strong> 编译期给组件内元素和选择器附加同 hash 的 <code>data-v-xxx</code> 属性；只隔离「出去」不拦截「进来」，子组件根元素会额外带父组件的 hash。</li>
<li><strong>@layer 解决什么？</strong> 把优先级从特异性竞赛变成层声明顺序——层序即胜负，与层内选择器写法无关。</li>
<li><strong>为什么 Tailwind 没有死 CSS？</strong> JIT 只为源码中实际出现的类名字符串生成规则，没出现就不生成。</li>
</ul>
<p><strong>一句话记住</strong>：CSS 工程化的每一步，都是在给一门没有模块系统的语言补模块系统——先靠人（命名规范），再靠编译（Modules&#x2F;scoped），最后标准自己来（@layer&#x2F;@scope）；具体工具会退潮，但「作用域、级联、死代码」这三个原生缺陷，是理解一切 CSS 工程化方案的坐标系。</p>
<!-- ## 参考

- [MDN：级联层 @layer](https://developer.mozilla.org/zh-CN/docs/Web/CSS/@layer)
- [MDN：@scope](https://developer.mozilla.org/zh-CN/docs/Web/CSS/@scope)
- [MDN：使用 CSS 自定义属性](https://developer.mozilla.org/zh-CN/docs/Web/CSS/Using_CSS_custom_properties)
- [MDN：CSS 嵌套](https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_nesting)
- [CSS Modules 规范](https://github.com/css-modules/css-modules)
- [Vue 文档：SFC CSS 功能（scoped / v-bind / CSS Modules）](https://cn.vuejs.org/api/sfc-css-features.html)
- [Tailwind CSS：Core Concepts](https://tailwindcss.com/docs/utility-first)
- [UnoCSS 文档](https://unocss.dev/) -->
]]></content>
      <categories>
        <category>前端工程化</category>
        <category>css</category>
      </categories>
      <tags>
        <tag>CSS</tag>
        <tag>CSS Modules</tag>
        <tag>CSS-in-JS</tag>
        <tag>Tailwind</tag>
        <tag>工程化</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS 中的层叠上下文与 BFC：那些年「莫名其妙」的样式问题</title>
    <url>/2026/09/09/css-bfc/</url>
    <content><![CDATA[<p>写 CSS 时总会撞上一些「莫名其妙」的问题：</p>
<ul>
<li>明明 <code>z-index: 9999</code>，元素还是被盖住；</li>
<li>子元素 <code>z-index</code> 再大，也压不过隔壁的父元素；</li>
<li>子元素设 <code>margin-top</code>，反而是父元素被推下去了；</li>
<li>子元素一 <code>float</code>，父元素高度直接归零；</li>
<li>给父元素随手加个 <code>overflow: hidden</code>，上面的问题居然全好了——然后下拉菜单又神秘消失。</li>
</ul>
<p>这些问题表面毫无关联，底层都指向两个概念：<strong>层叠上下文（Stacking Context）</strong> 和 <strong>BFC（Block Formatting Context）</strong>。名字很像，职责完全不同——一个管层级，一个管布局：</p>
<table>
<thead>
<tr>
<th>概念</th>
<th>一句话</th>
<th>类比</th>
<th>管什么</th>
</tr>
</thead>
<tbody><tr>
<td>BFC</td>
<td>独立的块级布局环境</td>
<td>房间：东西怎么摆</td>
<td>高度塌陷、margin 折叠、浮动环绕</td>
</tr>
<tr>
<td>层叠上下文</td>
<td>独立的图层世界</td>
<td>楼层：谁在上面</td>
<td>z-index、覆盖关系</td>
</tr>
</tbody></table>
<span id="more"></span>

<h2 id="一、层叠上下文：z-index-不是全局排行榜"><a href="#一、层叠上下文：z-index-不是全局排行榜" class="headerlink" title="一、层叠上下文：z-index 不是全局排行榜"></a>一、层叠上下文：z-index 不是全局排行榜</h2><h3 id="坑-1：z-index-9999-还是被盖住"><a href="#坑-1：z-index-9999-还是被盖住" class="headerlink" title="坑 1：z-index: 9999 还是被盖住"></a>坑 1：z-index: 9999 还是被盖住</h3><figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;box-a&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;child-a&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;box-b&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-class">.box-a</span>   &#123; <span class="hljs-attribute">position</span>: relative; <span class="hljs-attribute">z-index</span>: <span class="hljs-number">1</span>; &#125;<br><span class="hljs-selector-class">.child-a</span> &#123; <span class="hljs-attribute">position</span>: relative; <span class="hljs-attribute">z-index</span>: <span class="hljs-number">9999</span>; &#125;<br><span class="hljs-selector-class">.box-b</span>   &#123; <span class="hljs-attribute">position</span>: relative; <span class="hljs-attribute">z-index</span>: <span class="hljs-number">2</span>; &#125;<br></code></pre></td></tr></table></figure>

<p>结果：<code>child-a</code> 的 9999 输给了 <code>box-b</code> 的 2。</p>
<p>误解在于把 z-index 当成「全局排行榜」。实际规则是：<strong>z-index 的比较只发生在同一个层叠上下文内部；不同上下文之间，先由各自的父级上下文分胜负</strong>：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">页面（根层叠上下文）<br>├── box-a  z-index: 1   ← 先比这一层：1 &lt; 2，box-a 整组输掉<br>│   └── child-a z-index: 9999   ← 只在 box-a 内部有效<br>└── box-b  z-index: 2<br></code></pre></td></tr></table></figure>

<p><code>child-a</code> 的 9999 从来没有和 <code>box-b</code> 的 2 同台比过——先把 box-a 和 box-b 比出胜负，输的那一组整体压在下面，组内 z-index 再大也只是「矮子里的将军」。</p>
<p>所以排查 z-index 的第一问不是「数值够不够大」，而是：<strong>这个元素属于哪个层叠上下文？</strong> 一个典型场景——侧边栏里的弹层被内容区盖住：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-class">.sidebar</span> &#123; <span class="hljs-attribute">position</span>: relative; <span class="hljs-attribute">z-index</span>: <span class="hljs-number">1</span>; &#125;  <span class="hljs-comment">/* menu 的父级 */</span><br><span class="hljs-selector-class">.menu</span>    &#123; <span class="hljs-attribute">position</span>: absolute; <span class="hljs-attribute">z-index</span>: <span class="hljs-number">9999</span>; &#125;<br><span class="hljs-selector-class">.content</span> &#123; <span class="hljs-attribute">position</span>: relative; <span class="hljs-attribute">z-index</span>: <span class="hljs-number">2</span>; &#125;<br></code></pre></td></tr></table></figure>

<p>正解是提 <code>.sidebar</code> 的 z-index，而不是继续给 <code>.menu</code> 加 9。</p>
<h3 id="谁创建了层叠上下文"><a href="#谁创建了层叠上下文" class="headerlink" title="谁创建了层叠上下文"></a>谁创建了层叠上下文</h3><table>
<thead>
<tr>
<th>来源</th>
<th>例子</th>
</tr>
</thead>
<tbody><tr>
<td>根元素</td>
<td><code>html</code> 自带最外层上下文</td>
</tr>
<tr>
<td>定位 + z-index 非 auto</td>
<td><code>position: relative/absolute</code> + <code>z-index: 1</code></td>
</tr>
<tr>
<td>fixed &#x2F; sticky</td>
<td><code>position: fixed</code> 无条件创建</td>
</tr>
<tr>
<td>opacity &lt; 1</td>
<td><code>opacity: .9</code>（等于 1 不会）</td>
</tr>
<tr>
<td>transform &#x2F; filter &#x2F; perspective</td>
<td>值非 none 即创建</td>
</tr>
<tr>
<td>flex &#x2F; grid 子项 + z-index 非 auto</td>
<td>最常被忽略的一类</td>
</tr>
<tr>
<td>显式声明</td>
<td><code>isolation: isolate</code>（推荐：无副作用地建上下文）</td>
</tr>
</tbody></table>
<h3 id="坑-2：随手加个-transform，fixed-失灵"><a href="#坑-2：随手加个-transform，fixed-失灵" class="headerlink" title="坑 2：随手加个 transform，fixed 失灵"></a>坑 2：随手加个 transform，fixed 失灵</h3><p>容器上有 <code>transform: translateZ(0)</code>，里面的 <code>position: fixed</code> 弹窗表现异常——因为 transform 除了创建层叠上下文，还会成为 fixed 后代元素的<strong>包含块</strong>，fixed 从此不再相对视口定位。别为了「GPU 加速」随手给祖先元素加 transform。</p>
<p>z-index 不生效的排查顺序：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 生效条件：是定位元素或 flex/grid 子项吗？<br>② 两个元素在同一个层叠上下文里吗？<br>③ 沿祖先链找上下文创建者：z-index / transform / opacity / filter / fixed<br>④ 该比的是两个父级上下文，不是这两个数字<br></code></pre></td></tr></table></figure>

<h2 id="二、BFC：一个独立的块级布局环境"><a href="#二、BFC：一个独立的块级布局环境" class="headerlink" title="二、BFC：一个独立的块级布局环境"></a>二、BFC：一个独立的块级布局环境</h2><p>BFC 的心智模型：页面里一个个「房间」，房间内部的浮动、margin、高度计算自成一统，与外部隔离。三条核心规则，对应三个经典场景：</p>
<h3 id="坑-3：float-让父元素高度塌陷"><a href="#坑-3：float-让父元素高度塌陷" class="headerlink" title="坑 3：float 让父元素高度塌陷"></a>坑 3：float 让父元素高度塌陷</h3><figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;parent&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;child&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-class">.parent</span> &#123; <span class="hljs-attribute">background</span>: <span class="hljs-number">#eee</span>; &#125;<br><span class="hljs-selector-class">.child</span>  &#123; <span class="hljs-attribute">width</span>: <span class="hljs-number">200px</span>; <span class="hljs-attribute">height</span>: <span class="hljs-number">100px</span>; <span class="hljs-attribute">float</span>: left; &#125;  <span class="hljs-comment">/* 脱离普通文档流 */</span><br></code></pre></td></tr></table></figure>

<p><code>float</code> 脱离文档流，父元素计算高度时看不见它 → <code>parent</code> 高度 ≈ 0。</p>
<p>修复：让父元素建立 BFC——<strong>BFC 计算高度时，会把内部的浮动子元素也纳入</strong>：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-class">.parent</span> &#123; <span class="hljs-attribute">display</span>: flow-root; &#125;  <span class="hljs-comment">/* 首选：为建 BFC 而生，无副作用 */</span><br><span class="hljs-comment">/* overflow: hidden 也能解决，但有裁剪副作用，见坑 6 */</span><br></code></pre></td></tr></table></figure>

<h3 id="坑-4：子元素的-margin-把父元素顶下去"><a href="#坑-4：子元素的-margin-把父元素顶下去" class="headerlink" title="坑 4：子元素的 margin 把父元素顶下去"></a>坑 4：子元素的 margin 把父元素顶下去</h3><figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-class">.parent</span> &#123; <span class="hljs-attribute">background</span>: <span class="hljs-number">#eee</span>; &#125;<br><span class="hljs-selector-class">.child</span>  &#123; <span class="hljs-attribute">margin-top</span>: <span class="hljs-number">50px</span>; &#125;  <span class="hljs-comment">/* 视觉效果：parent 整体下沉，child 紧贴顶边 */</span><br></code></pre></td></tr></table></figure>

<p>这是 <strong>margin collapse（外边距折叠）</strong>：父子之间没有 border、padding、行内内容或 BFC 隔离时，两者的垂直 margin 合并成一个，作用到了父元素外侧。修复还是给父级建 BFC（或加 border&#x2F;padding 隔离）：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-class">.parent</span> &#123; <span class="hljs-attribute">display</span>: flow-root; &#125;<br></code></pre></td></tr></table></figure>

<p>补全折叠的另一半：<strong>相邻兄弟元素</strong>的上&#x2F;下 margin 也会合并（取大者）——这是设计行为不是 bug，想要每个间隙都生效，用 padding 或 BFC 隔开。</p>
<h3 id="坑-5：后面的元素被-float-兄弟环绕"><a href="#坑-5：后面的元素被-float-兄弟环绕" class="headerlink" title="坑 5：后面的元素被 float 兄弟环绕"></a>坑 5：后面的元素被 float 兄弟环绕</h3><figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-class">.left</span>    &#123; <span class="hljs-attribute">width</span>: <span class="hljs-number">200px</span>; <span class="hljs-attribute">height</span>: <span class="hljs-number">300px</span>; <span class="hljs-attribute">float</span>: left; &#125;<br><span class="hljs-selector-class">.content</span> &#123; <span class="hljs-attribute">height</span>: <span class="hljs-number">300px</span>; <span class="hljs-attribute">background</span>: <span class="hljs-number">#eee</span>; &#125;  <span class="hljs-comment">/* 普通块级：内容环绕浮动元素 */</span><br></code></pre></td></tr></table></figure>

<p>BFC 的第三条规则：<strong>BFC 的区域不与外部浮动重叠</strong>。给 <code>.content</code> 建 BFC（<code>display: flow-root</code>），两者即并排分立——经典「自适应两栏布局」的原理正是这一条。</p>
<h3 id="创建-BFC-的方式"><a href="#创建-BFC-的方式" class="headerlink" title="创建 BFC 的方式"></a>创建 BFC 的方式</h3><table>
<thead>
<tr>
<th>方式</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td><code>display: flow-root</code></td>
<td>首选：专门为建 BFC 设计，无副作用</td>
</tr>
<tr>
<td><code>overflow</code> 非 <code>visible</code></td>
<td>传统做法，附带裁剪 &#x2F; 滚动条</td>
</tr>
<tr>
<td><code>float</code> &#x2F; <code>position: absolute</code> &#x2F; <code>fixed</code></td>
<td>元素自身建立 BFC</td>
</tr>
<tr>
<td><code>display: flex</code> &#x2F; <code>grid</code></td>
<td>容器行为上等价 BFC（包含浮动、隔离 margin）</td>
</tr>
</tbody></table>
<h3 id="坑-6：overflow-hidden-为什么「包治百病」又「暗藏杀机」"><a href="#坑-6：overflow-hidden-为什么「包治百病」又「暗藏杀机」" class="headerlink" title="坑 6：overflow: hidden 为什么「包治百病」又「暗藏杀机」"></a>坑 6：overflow: hidden 为什么「包治百病」又「暗藏杀机」</h3><p>它会创建 BFC，于是高度塌陷、margin 折叠、浮动环绕一起消失——「诶，好了」。但它同时<strong>裁剪溢出内容</strong>：dropdown、tooltip、popover 超出父级边界的部分直接不见——这类「元素消失了」的问题，九成不是 z-index，而是被某个祖先的 overflow 裁掉了。原则：要 BFC 就写 <code>flow-root</code>，真的要裁剪才写 <code>overflow: hidden</code>。</p>
<h2 id="三、排查模型：三层漏斗"><a href="#三、排查模型：三层漏斗" class="headerlink" title="三、排查模型：三层漏斗"></a>三、排查模型：三层漏斗</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">元素表现不对<br>  ├─ 位置 / 高度 / margin 不对  → 布局层：BFC / float / margin 折叠<br>  ├─ 位置对，但被盖住           → 层叠层：stacking context / z-index<br>  └─ 压根看不见                 → 裁剪层：祖先 overflow；定位层：包含块<br></code></pre></td></tr></table></figure>

<p>配套的 DevTools 手法：选中元素后沿祖先链逐层向上看，重点找 <code>transform</code>、<code>opacity</code>、<code>filter</code>、<code>z-index</code>、<code>overflow</code>——层叠上下文和裁剪的元凶都在这条链上。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>症状速查：</p>
<table>
<thead>
<tr>
<th>症状</th>
<th>元凶</th>
<th>解法</th>
</tr>
</thead>
<tbody><tr>
<td>z-index 再大也被盖</td>
<td>父级层叠上下文输了</td>
<td>提父级上下文的 z-index，或把弹层挪出该上下文</td>
</tr>
<tr>
<td>fixed 定位失灵</td>
<td>祖先有 transform</td>
<td>去掉 transform，或把元素挪到外层</td>
</tr>
<tr>
<td>父元素高度塌陷</td>
<td>float 脱流</td>
<td>父级 <code>display: flow-root</code></td>
</tr>
<tr>
<td>子 margin 顶飞父元素</td>
<td>margin 折叠</td>
<td>父级建 BFC，或 border&#x2F;padding 隔离</td>
</tr>
<tr>
<td>元素被 float 兄弟环绕</td>
<td>非 BFC 与浮动重叠</td>
<td>自身建 BFC（自适应两栏的原理）</td>
</tr>
<tr>
<td>dropdown 消失</td>
<td>祖先 overflow 裁剪</td>
<td>改 overflow，或把弹层挂到 body</td>
</tr>
</tbody></table>
<p>面试速答：</p>
<ul>
<li><strong>BFC 是什么？</strong> 块级格式化上下文，一个独立的块级布局环境：内部浮动参与高度计算、垂直 margin 不与外部折叠、区域不与外部浮动重叠。</li>
<li><strong>怎么创建 BFC？</strong> 首选 <code>display: flow-root</code>；<code>overflow</code> 非 visible、float、绝对定位、flex&#x2F;grid 容器也可以。</li>
<li><strong>层叠上下文的比较规则？</strong> z-index 只在同一层叠上下文内比较；上下文之间由各自的 z-index 分胜负，子上下文整体跟随父级，无法突破。</li>
<li><strong>为什么 <code>overflow: hidden</code> 能修高度塌陷？</strong> 它创建了 BFC，而 BFC 计算高度时纳入浮动子元素——但注意它还会裁剪溢出内容。</li>
<li><strong>z-index 不生效怎么排查？</strong> 先确认生效条件（定位元素 &#x2F; flex、grid 子项），再沿祖先链找上下文创建者（transform &#x2F; opacity &#x2F; filter &#x2F; z-index），最后确认比较对象是否在同一层上下文。</li>
</ul>
<p><strong>一句话记住</strong>：BFC 管「怎么布局」，层叠上下文管「谁在上面」——前者是房间，后者是楼层；楼层的胜负在楼层之间决定，房间里的 z-index 再大，也出不了自家那层楼。</p>
<!-- ## 参考

- [MDN：层叠上下文](https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_positioned_layout/Stacking_context)
- [MDN：块格式化上下文](https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_display/Block_formatting_context)
- [MDN：掌握外边距折叠](https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_box_model/Mastering_margin_collapsing)
- [CSS 规范：CSS Positioning / CSS Display](https://www.w3.org/Style/CSS/specs) -->
]]></content>
      <categories>
        <category>前端基础</category>
        <category>css</category>
      </categories>
      <tags>
        <tag>CSS</tag>
        <tag>前端基础</tag>
      </tags>
  </entry>
  <entry>
    <title>现代布局选型指南：Flex / Grid / 容器查询</title>
    <url>/2026/09/09/css-modern-layout/</url>
    <content><![CDATA[<p>刚写页面那几年，我的布局知识就两样：float + position。后来 Flexbox 出了，就开始「flex 一把梭」——导航用它，列表用它，页面骨架也用它。真正逼我去补课的是三个场景：</p>
<ol>
<li><strong>工具栏</strong>：一行按钮，最后一个「导出」要贴右。flex 加一个 <code>margin-left: auto</code> 就优雅解决。</li>
<li><strong>商品卡片列表</strong>：<code>flex-wrap</code> 写的网格，最后一行只剩 2 个卡片，被 <code>flex-grow</code> 拉成两倍宽，和上一行完全对不齐。换成 Grid 一行代码解决。</li>
<li><strong>同一个卡片组件</strong>：放在主区域有 400px+，塞进侧边栏只剩 260px。媒体查询感知的是<strong>视口</strong>，视口没变，组件却「住」进了小房间——这时需要容器查询。</li>
</ol>
<p>这三个场景对应三套工具，而它们不是竞争关系，是三层不同的抽象。web.dev 的 Learn CSS 课程里有个说法我一直拿来当心智模型：<strong>Flex 是 content-out（从内容出发，往外分配空间），Grid 是 layout-in（先定轨道结构，再把内容放进去）</strong>。容器查询则更进一步：组件不再问「视口多宽」，而是问「我自己家多宽」。</p>
<span id="more"></span>

<h2 id="一、Flex：一维空间分配器"><a href="#一、Flex：一维空间分配器" class="headerlink" title="一、Flex：一维空间分配器"></a>一、Flex：一维空间分配器</h2><p>先把 Flex 的分配算法说透，因为后面大部分「玄学」都出在这里。</p>
<p>一行 flex 的空间计算只有三步：</p>
<ol>
<li>每个 item 按 <code>flex-basis</code> 得到一个<strong>假想主尺寸</strong>（<code>basis: auto</code> 时取内容宽度）；</li>
<li><strong>剩余空间</strong> &#x3D; 容器宽度 − Σ假想尺寸 − gap。剩余为正，按 <code>flex-grow</code> 的比例加权分配；为负，按 <code>flex-shrink</code> 加权回收；</li>
<li>回收有下限——item 的默认 <code>min-width: auto</code>（内容最小宽度），<strong>压不破内容</strong>，这正是多数「压不动」问题的根源。</li>
</ol>
<p>注意第 2 步：grow 只按比例分，shrink 却要乘上 basis 加权（内容越宽让得越多）。所以「等分」这件事，起点比 grow 更重要。看四个关键字的真值表：</p>
<table>
<thead>
<tr>
<th>写法</th>
<th>展开为</th>
<th>行为</th>
</tr>
</thead>
<tbody><tr>
<td><code>flex: 1</code></td>
<td><code>1 1 0%</code></td>
<td>起点归零，完全等分，<strong>内容不参与</strong></td>
</tr>
<tr>
<td><code>flex: auto</code></td>
<td><code>1 1 auto</code></td>
<td>先按内容，再均摊剩余空间</td>
</tr>
<tr>
<td><code>flex: none</code></td>
<td><code>0 0 auto</code></td>
<td>定宽，不参与分配</td>
</tr>
<tr>
<td><code>flex: initial</code>（默认）</td>
<td><code>0 1 auto</code></td>
<td>按内容，只挤不涨</td>
</tr>
</tbody></table>
<h3 id="坑-1：以为-flex-1-和-flex-auto-等价"><a href="#坑-1：以为-flex-1-和-flex-auto-等价" class="headerlink" title="坑 1：以为 flex: 1 和 flex: auto 等价"></a>坑 1：以为 <code>flex: 1</code> 和 <code>flex: auto</code> 等价</h3><figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">style</span>&gt;</span><span class="language-css"></span><br><span class="language-css">  <span class="hljs-selector-class">.row</span> &#123; <span class="hljs-attribute">display</span>: flex; <span class="hljs-attribute">gap</span>: <span class="hljs-number">8px</span>; <span class="hljs-attribute">margin-bottom</span>: <span class="hljs-number">8px</span>; &#125;</span><br><span class="language-css">  <span class="hljs-selector-class">.row</span> &gt; <span class="hljs-selector-tag">div</span> &#123; <span class="hljs-attribute">background</span>: <span class="hljs-number">#eee</span>; <span class="hljs-attribute">padding</span>: <span class="hljs-number">8px</span> <span class="hljs-number">4px</span>; <span class="hljs-attribute">text-align</span>: center; &#125;</span><br><span class="language-css">  <span class="hljs-selector-class">.a</span> &gt; <span class="hljs-selector-tag">div</span> &#123; <span class="hljs-attribute">flex</span>: <span class="hljs-number">1</span>; &#125;    <span class="hljs-comment">/* 1 1 0%：起点是 0，三分天下 */</span></span><br><span class="language-css">  <span class="hljs-selector-class">.b</span> &gt; <span class="hljs-selector-tag">div</span> &#123; <span class="hljs-attribute">flex</span>: auto; &#125; <span class="hljs-comment">/* 1 1 auto：起点是内容宽，长内容永远更宽 */</span></span><br><span class="language-css"></span><span class="hljs-tag">&lt;/<span class="hljs-name">style</span>&gt;</span><br><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;row a&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>短<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>我是比较长的那一项内容<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>中<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;row b&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>短<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>我是比较长的那一项内容<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>中<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>上面一行三个格子严格等宽；下面一行长内容的格子更宽。要做「平分」，<code>flex: 1</code>（basis 归零）才是对的；要让内容保留差异再分剩余，才用 <code>flex: auto</code>。我在一个后台项目里见过满屏的 <code>flex: auto</code> 导致三栏表单宽窄不一，改成 <code>flex: 1</code> 立刻齐了。</p>
<h3 id="坑-2：min-width-auto-——-flex-里最经典的「文字压不动」"><a href="#坑-2：min-width-auto-——-flex-里最经典的「文字压不动」" class="headerlink" title="坑 2：min-width: auto —— flex 里最经典的「文字压不动」"></a>坑 2：min-width: auto —— flex 里最经典的「文字压不动」</h3><figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">style</span>&gt;</span><span class="language-css"></span><br><span class="language-css">  <span class="hljs-selector-class">.outer</span> &#123;</span><br><span class="language-css">    <span class="hljs-attribute">display</span>: flex;</span><br><span class="language-css">    <span class="hljs-attribute">width</span>: <span class="hljs-number">320px</span>;                 <span class="hljs-comment">/* 容器故意给窄 */</span></span><br><span class="language-css">    <span class="hljs-attribute">border</span>: <span class="hljs-number">1px</span> dashed red;</span><br><span class="language-css">  &#125;</span><br><span class="language-css">  <span class="hljs-selector-class">.outer</span> &gt; <span class="hljs-selector-class">.text</span> &#123;</span><br><span class="language-css">    <span class="hljs-attribute">flex</span>: <span class="hljs-number">1</span>;</span><br><span class="language-css">    <span class="hljs-attribute">white-space</span>: nowrap;          <span class="hljs-comment">/* 想做单行省略号 */</span></span><br><span class="language-css">    <span class="hljs-attribute">overflow</span>: hidden;</span><br><span class="language-css">    <span class="hljs-attribute">text-overflow</span>: ellipsis;</span><br><span class="language-css">  &#125;</span><br><span class="language-css">  <span class="hljs-selector-class">.outer</span> &gt; <span class="hljs-selector-class">.fixed</span> &#123; <span class="hljs-attribute">flex</span>: none; <span class="hljs-attribute">width</span>: <span class="hljs-number">120px</span>; <span class="hljs-attribute">background</span>: <span class="hljs-number">#eee</span>; &#125;</span><br><span class="language-css"></span><br><span class="language-css">  <span class="hljs-selector-class">.fixed-row</span> &gt; <span class="hljs-selector-class">.text</span> &#123; <span class="hljs-attribute">min-width</span>: <span class="hljs-number">0</span>; &#125;  <span class="hljs-comment">/* 修复：解除内容最小宽度限制 */</span></span><br><span class="language-css"></span><span class="hljs-tag">&lt;/<span class="hljs-name">style</span>&gt;</span><br><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;outer&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;text&quot;</span>&gt;</span>这一行很长很长的标题本来应该被省略号截断<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;fixed&quot;</span>&gt;</span>固定 120px<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;outer fixed-row&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;text&quot;</span>&gt;</span>这一行很长很长的标题本来应该被省略号截断<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;fixed&quot;</span>&gt;</span>固定 120px<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>第一个盒子：文字的 <code>min-content</code> 宽度成了 item 的隐形下限（<code>min-width: auto</code>），省略号失效，固定列被挤出去。第二个盒子加了一行 <code>min-width: 0</code>，立刻正常。</p>
<p><strong>为什么规范这么设计？</strong> 默认保住内容完整（比如一段文字不被压到 0 宽），是更安全的兜底。代价是：当你明确要「内容让路」时，必须手动解除。同理，设 <code>overflow: hidden</code> 也会让最小宽度归零——所以「加个 overflow: hidden 就好了」背后的原理是同一件事。</p>
<h3 id="一个被低估的技巧：margin-auto"><a href="#一个被低估的技巧：margin-auto" class="headerlink" title="一个被低估的技巧：margin: auto"></a>一个被低估的技巧：margin: auto</h3><p>flex 容器里，<code>auto</code> margin 会吸收该轴上的全部剩余空间。工具栏场景一行搞定：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">style</span>&gt;</span><span class="language-css"></span><br><span class="language-css">  <span class="hljs-selector-class">.toolbar</span> &#123; <span class="hljs-attribute">display</span>: flex; <span class="hljs-attribute">gap</span>: <span class="hljs-number">8px</span>; &#125;</span><br><span class="language-css">  <span class="hljs-selector-class">.toolbar</span> <span class="hljs-selector-class">.export</span> &#123; <span class="hljs-attribute">margin-left</span>: auto; &#125;  <span class="hljs-comment">/* 把剩余空间全吸到自己左边 */</span></span><br><span class="language-css"></span><span class="hljs-tag">&lt;/<span class="hljs-name">style</span>&gt;</span><br><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;toolbar&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">button</span>&gt;</span>新建<span class="hljs-tag">&lt;/<span class="hljs-name">button</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">button</span>&gt;</span>删除<span class="hljs-tag">&lt;/<span class="hljs-name">button</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">button</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;export&quot;</span>&gt;</span>导出<span class="hljs-tag">&lt;/<span class="hljs-name">button</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>比 <code>justify-content: space-between</code> 干净的地方在于：不需要包一层、也不影响其他按钮的间距逻辑。</p>
<h2 id="二、Grid：先画格子，再放东西"><a href="#二、Grid：先画格子，再放东西" class="headerlink" title="二、Grid：先画格子，再放东西"></a>二、Grid：先画格子，再放东西</h2><p>Grid 的思路反过来：<strong>先把轨道（track）结构定好，再把 item 放进格子</strong>。它和 Flex 的底层分配思想其实是同一套——<code>fr</code> 就是「剩余空间的份数」：先扣除固定轨道和 gap，剩余部分按 fr 比例分。区别在于 Flex 把空间分给 item，Grid 把空间分给轨道。</p>
<h3 id="一行代码的响应式网格"><a href="#一行代码的响应式网格" class="headerlink" title="一行代码的响应式网格"></a>一行代码的响应式网格</h3><p>卡片列表这种场景，Grid 有个杀手级写法：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-class">.grid</span> &#123;<br>  <span class="hljs-attribute">display</span>: grid;<br>  <span class="hljs-attribute">grid-template-columns</span>: <span class="hljs-built_in">repeat</span>(auto-fill, <span class="hljs-built_in">minmax</span>(<span class="hljs-number">220px</span>, <span class="hljs-number">1</span>fr));<br>  <span class="hljs-attribute">gap</span>: <span class="hljs-number">16px</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>不带一个媒体查询，就实现了「容器够宽就多放一列，不够就少放，每列不窄于 220px」。拆开看它的计算过程（假设容器 920px、gap 16px）：</p>
<ol>
<li><code>auto-fill</code>：尽可能多地创建轨道。n 列放得下的条件是 <code>n × 220 + (n − 1) × 16 ≤ 920</code>，n &#x3D; 3 时是 692px ✓，n &#x3D; 4 时 928px ✗ → 建 3 根轨道；</li>
<li><code>minmax(220px, 1fr)</code>：每根轨道最小 220px、最大取剩余空间份数。剩余 920 − 692 &#x3D; 228px，平摊给 3 根轨道各 76px → 最终每列约 296px。</li>
</ol>
<p>改窗口宽度，这套算术自动重算——这就是「响应式不需要断点」的出处。</p>
<h3 id="坑-3：flex-wrap-的最后一行问题"><a href="#坑-3：flex-wrap-的最后一行问题" class="headerlink" title="坑 3：flex-wrap 的最后一行问题"></a>坑 3：flex-wrap 的最后一行问题</h3><p>回到开头说的商品列表。两种写法放一起看：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">style</span>&gt;</span><span class="language-css"></span><br><span class="language-css">  <span class="hljs-selector-class">.wrap</span>, <span class="hljs-selector-class">.grid</span> &#123; <span class="hljs-attribute">width</span>: <span class="hljs-number">600px</span>; <span class="hljs-attribute">gap</span>: <span class="hljs-number">16px</span>; <span class="hljs-attribute">margin-bottom</span>: <span class="hljs-number">16px</span>; &#125;</span><br><span class="language-css"></span><br><span class="language-css">  <span class="hljs-comment">/* 写法一：flex-wrap */</span></span><br><span class="language-css">  <span class="hljs-selector-class">.wrap</span> &#123; <span class="hljs-attribute">display</span>: flex; <span class="hljs-attribute">flex-wrap</span>: wrap; &#125;</span><br><span class="language-css">  <span class="hljs-selector-class">.wrap</span> &gt; <span class="hljs-selector-tag">div</span> &#123; <span class="hljs-attribute">flex</span>: <span class="hljs-number">1</span> <span class="hljs-number">1</span> <span class="hljs-number">160px</span>; <span class="hljs-attribute">background</span>: <span class="hljs-number">#cfe</span>; <span class="hljs-attribute">padding</span>: <span class="hljs-number">24px</span> <span class="hljs-number">0</span>; <span class="hljs-attribute">text-align</span>: center; &#125;</span><br><span class="language-css"></span><br><span class="language-css">  <span class="hljs-comment">/* 写法二：grid auto-fill */</span></span><br><span class="language-css">  <span class="hljs-selector-class">.grid</span> &#123; <span class="hljs-attribute">display</span>: grid; <span class="hljs-attribute">grid-template-columns</span>: <span class="hljs-built_in">repeat</span>(auto-fill, <span class="hljs-built_in">minmax</span>(<span class="hljs-number">160px</span>, <span class="hljs-number">1</span>fr)); &#125;</span><br><span class="language-css">  <span class="hljs-selector-class">.grid</span> &gt; <span class="hljs-selector-tag">div</span> &#123; <span class="hljs-attribute">background</span>: <span class="hljs-number">#fec</span>; <span class="hljs-attribute">padding</span>: <span class="hljs-number">24px</span> <span class="hljs-number">0</span>; <span class="hljs-attribute">text-align</span>: center; &#125;</span><br><span class="language-css"></span><span class="hljs-tag">&lt;/<span class="hljs-name">style</span>&gt;</span><br><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;wrap&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>1<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>2<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>3<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>4<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>5<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;grid&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>1<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>2<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>3<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>4<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>5<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>同样是 5 个卡片、每行 3 个：flex 版<strong>每一行独立分配</strong>剩余空间，第一行 3 个各约 189px，最后一行 2 个各 292px——列完全对不上。grid 版所有轨道全局统一，最后一行只是留了一个空轨道，列宽和上面严格一致。</p>
<p>这就是「卡片列表选 grid 而不是 flex-wrap」的根本原因：<strong>flex 的分配单位是行，grid 的分配单位是整个容器</strong>。</p>
<h3 id="auto-fill-vs-auto-fit"><a href="#auto-fill-vs-auto-fit" class="headerlink" title="auto-fill vs auto-fit"></a>auto-fill vs auto-fit</h3><p>这两个关键字只差一件事：<strong>空轨道折叠不折叠</strong>。<code>auto-fit</code> 会把没放东西的轨道折叠成 0（于是仅有的几个 item 会被拉伸占满整行），<code>auto-fill</code> 保留空轨道占位。item 数量少、希望它们撑满容器时用 <code>auto-fit</code>；做常规卡片网格、希望列宽稳定时用 <code>auto-fill</code>。item 足够多时两者表现一致。</p>
<h3 id="页面骨架：template-areas-让结构一眼可读"><a href="#页面骨架：template-areas-让结构一眼可读" class="headerlink" title="页面骨架：template-areas 让结构一眼可读"></a>页面骨架：template-areas 让结构一眼可读</h3><p>Grid 的另一主场是页面级骨架，<code>grid-template-areas</code> 可以按「画户型图」的方式写：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-class">.page</span> &#123;<br>  <span class="hljs-attribute">display</span>: grid;<br>  <span class="hljs-attribute">grid-template-areas</span>:<br>    <span class="hljs-string">&quot;header header&quot;</span><br>    <span class="hljs-string">&quot;sidebar main&quot;</span><br>    <span class="hljs-string">&quot;footer footer&quot;</span>;<br>  <span class="hljs-attribute">grid-template-columns</span>: <span class="hljs-number">240px</span> <span class="hljs-number">1</span>fr;<br>  <span class="hljs-attribute">grid-template-rows</span>: auto <span class="hljs-number">1</span>fr auto;<br>  <span class="hljs-attribute">min-height</span>: <span class="hljs-number">100vh</span>;<br>&#125;<br><span class="hljs-selector-class">.page</span> &gt; <span class="hljs-selector-tag">header</span>  &#123; <span class="hljs-attribute">grid-area</span>: header; &#125;<br><span class="hljs-selector-class">.page</span> &gt; <span class="hljs-selector-tag">aside</span>   &#123; <span class="hljs-attribute">grid-area</span>: sidebar; &#125;<br><span class="hljs-selector-class">.page</span> &gt; <span class="hljs-selector-tag">main</span>    &#123; <span class="hljs-attribute">grid-area</span>: main; &#125;<br><span class="hljs-selector-class">.page</span> &gt; <span class="hljs-selector-tag">footer</span>  &#123; <span class="hljs-attribute">grid-area</span>: footer; &#125;<br></code></pre></td></tr></table></figure>

<p>要把 sidebar 从左边挪到右边，改一处字符串（<code>&quot;main sidebar&quot;</code>）就够，不用动任何子元素的属性。</p>
<h3 id="坑-4：1fr-的隐形下限（和坑-2-是亲兄弟）"><a href="#坑-4：1fr-的隐形下限（和坑-2-是亲兄弟）" class="headerlink" title="坑 4：1fr 的隐形下限（和坑 2 是亲兄弟）"></a>坑 4：1fr 的隐形下限（和坑 2 是亲兄弟）</h3><p><code>1fr</code> 展开是 <code>minmax(auto, 1fr)</code>——轨道同样<strong>压不破内容的最小宽度</strong>。一根 <code>1fr</code> 轨道里塞了长单词、长表格，整行照样被撑爆。解法和坑 2 如出一辙，只是换了写法：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-comment">/* 长内容不会撑爆列的写法 */</span><br><span class="hljs-attribute">grid-template-columns</span>: <span class="hljs-built_in">minmax</span>(<span class="hljs-number">0</span>, <span class="hljs-number">1</span>fr) <span class="hljs-built_in">minmax</span>(<span class="hljs-number">0</span>, <span class="hljs-number">1</span>fr);<br></code></pre></td></tr></table></figure>

<p>Flex 和 Grid 在「默认保内容、手动才让路」这件事上完全一致——理解了其中一个，另一个的「玄学」也就消失了。</p>
<p>顺带一提 <strong>subgrid</strong>（Chrome 117+ 已支持）：子网格可以继承父网格的轨道，一张卡片网格里想让所有卡片的「标题行 &#x2F; 正文 &#x2F; 按钮」分别横向对齐，让卡片内部也用 <code>grid-template-rows: subgrid</code> 就行，不再需要 JS 去量高度。</p>
<h2 id="三、容器查询：组件知道自己住哪了"><a href="#三、容器查询：组件知道自己住哪了" class="headerlink" title="三、容器查询：组件知道自己住哪了"></a>三、容器查询：组件知道自己住哪了</h2><p>媒体查询统治响应式十几年，但它有个结构性缺陷：<strong>它感知的是视口</strong>。组件化时代，同一个组件会被放进主区域、侧边栏、弹窗、Grid 网格——家的大小千差万别，视口却纹丝不动。</p>
<p>容器查询三步上手：</p>
<ol>
<li>给「容器」声明尺寸 containment：<code>container-type: inline-size</code>（最常用，只约束行内方向）；</li>
<li>后代组件里写 <code>@container (min-width: 400px) { … }</code>，条件针对<strong>最近的祖先容器</strong>；</li>
<li>想要更精细，可用容器查询单位 <code>cqi</code>（容器行内宽的 1%）：<code>font-size: clamp(0.9rem, 4cqi, 1.2rem)</code>。</li>
</ol>
<p>一个最小 demo——<strong>同一个卡片组件，放进两个不同宽度的壳</strong>，零媒体查询：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">style</span>&gt;</span><span class="language-css"></span><br><span class="language-css">  <span class="hljs-selector-class">.box</span> &#123; <span class="hljs-attribute">border</span>: <span class="hljs-number">1px</span> dashed <span class="hljs-number">#999</span>; <span class="hljs-attribute">margin-bottom</span>: <span class="hljs-number">12px</span>; &#125;</span><br><span class="language-css">  <span class="hljs-selector-class">.narrow</span> &#123; <span class="hljs-attribute">width</span>: <span class="hljs-number">240px</span>; &#125;</span><br><span class="language-css">  <span class="hljs-selector-class">.wide</span>   &#123; <span class="hljs-attribute">width</span>: <span class="hljs-number">520px</span>; &#125;</span><br><span class="language-css"></span><br><span class="language-css">  <span class="hljs-selector-class">.card</span> &#123;</span><br><span class="language-css">    <span class="hljs-attribute">container-type</span>: inline-size;   <span class="hljs-comment">/* 卡片自己是容器 */</span></span><br><span class="language-css">    <span class="hljs-attribute">display</span>: flex;</span><br><span class="language-css">    <span class="hljs-attribute">flex-direction</span>: column;        <span class="hljs-comment">/* 默认（家窄）：竖排 */</span></span><br><span class="language-css">    <span class="hljs-attribute">gap</span>: <span class="hljs-number">12px</span>;</span><br><span class="language-css">    <span class="hljs-attribute">padding</span>: <span class="hljs-number">12px</span>;</span><br><span class="language-css">    <span class="hljs-attribute">border</span>: <span class="hljs-number">1px</span> solid <span class="hljs-number">#ddd</span>;</span><br><span class="language-css">  &#125;</span><br><span class="language-css">  <span class="hljs-selector-class">.thumb</span> &#123; <span class="hljs-attribute">background</span>: <span class="hljs-number">#ccc</span>; <span class="hljs-attribute">aspect-ratio</span>: <span class="hljs-number">16</span> / <span class="hljs-number">9</span>; &#125;</span><br><span class="language-css"></span><br><span class="language-css">  <span class="hljs-keyword">@container</span> (<span class="hljs-attribute">min-width</span>: <span class="hljs-number">400px</span>) &#123;  <span class="hljs-comment">/* 家够宽：横排 */</span></span><br><span class="language-css">    <span class="hljs-selector-class">.card</span> &#123; <span class="hljs-attribute">flex-direction</span>: row; <span class="hljs-attribute">align-items</span>: center; &#125;</span><br><span class="language-css">    <span class="hljs-selector-class">.thumb</span> &#123; <span class="hljs-attribute">width</span>: <span class="hljs-number">180px</span>; <span class="hljs-attribute">flex</span>: none; <span class="hljs-attribute">aspect-ratio</span>: <span class="hljs-number">1</span>; &#125;</span><br><span class="language-css">  &#125;</span><br><span class="language-css"></span><span class="hljs-tag">&lt;/<span class="hljs-name">style</span>&gt;</span><br><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;box narrow&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;card&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;thumb&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>标题：容器查询让组件自己适配环境<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;box wide&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;card&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;thumb&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>标题：容器查询让组件自己适配环境<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>窄壳里竖排、宽壳里横排——组件的响应式逻辑从「页面」下沉到了「组件」，从此组件在哪里都能活。</p>
<p>它和第二节的 auto-fill 网格是天然搭档：网格负责「每列多宽」，容器查询负责「落在这列里的卡片长什么样」，整条链路没有一个媒体查询。</p>
<h3 id="注意点：inline-size-containment-有代价"><a href="#注意点：inline-size-containment-有代价" class="headerlink" title="注意点：inline-size containment 有代价"></a>注意点：inline-size containment 有代价</h3><p><code>container-type: inline-size</code> 意味着<strong>容器自身的宽度不能再由内容撑开</strong>，必须由外部（父级、自身 width 或 Grid&#x2F;Flex 分配）决定。把一个原本 <code>fit-content</code> 的元素设成容器，它可能突然「塌」了——这是容器查询最高频的意外。记住：先有确定的家，家才可被查询。</p>
<p>兼容性上，Chrome&#x2F;Edge 105+、Safari 16+、Firefox 110+（2023 年起全绿），2026 年可以放心用于生产。</p>
<h2 id="四、选型总结"><a href="#四、选型总结" class="headerlink" title="四、选型总结"></a>四、选型总结</h2><table>
<thead>
<tr>
<th>场景</th>
<th>首选</th>
<th>一句话理由</th>
</tr>
</thead>
<tbody><tr>
<td>导航 &#x2F; 工具栏（一行，部分定宽部分伸缩）</td>
<td>Flex</td>
<td>一维分配，<code>margin: auto</code> 吸剩余空间</td>
</tr>
<tr>
<td>图标 + 文字之类的小型行内结构</td>
<td>Flex</td>
<td>最短路径</td>
</tr>
<tr>
<td>卡片 &#x2F; 商品网格（多行多列，要求列对齐）</td>
<td>Grid</td>
<td>轨道全局统一，最后一行不变形</td>
</tr>
<tr>
<td>页面骨架（页头 &#x2F; 侧栏 &#x2F; 主区 &#x2F; 页脚）</td>
<td>Grid</td>
<td><code>template-areas</code> 结构即代码</td>
</tr>
<tr>
<td>表单行（label 定宽，输入框占满）</td>
<td>Flex</td>
<td>定宽 + <code>flex: 1</code> 的标准组合</td>
</tr>
<tr>
<td>组件要适配所在容器而非视口</td>
<td>容器查询</td>
<td>响应式下沉到组件</td>
</tr>
</tbody></table>
<p>顺带几条面试速答（都是上面的结论压缩版）：</p>
<ul>
<li><strong><code>flex: 1</code> 展开是什么？</strong> <code>1 1 0%</code>——basis 归零所以严格等分；不等分的是 <code>flex: auto</code>（basis 为内容宽）。</li>
<li><strong>为什么 flex 里加 <code>overflow: hidden</code> 能修溢出？</strong> overflow 非 visible 时，最小宽度限制（<code>min-width: auto</code>）归零，等价于 <code>min-width: 0</code>。</li>
<li><strong><code>1fr</code> 的最小值是多少？</strong> <code>auto</code>（内容最小宽）；防撑爆要写 <code>minmax(0, 1fr)</code>。</li>
<li><strong>auto-fill 和 auto-fit 的区别？</strong> 空轨道是否折叠；item 少且要撑满用 auto-fit。</li>
<li><strong>容器查询和媒体查询的本质区别？</strong> 感知对象不同：视口 vs 最近祖先容器。</li>
</ul>
<p><strong>一句话记住</strong>：Flex 从内容出发往外分空间，Grid 从轨道出发往里放内容，容器查询让组件不再看视口脸色——三者是三层抽象，按「分空间、画格子、认环境」各取所长。</p>
<!-- ## 参考

- [MDN：Flexbox 基本概念](https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_flexible_box_layout/Basic_concepts_of_flexbox)
- [CSS-Tricks: A Complete Guide to Flexbox](https://css-tricks.com/snippets/css/a-guide-to-flexbox/)
- [MDN：Grid 布局基本概念](https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_grid_layout/Basic_concepts_of_grid_layout)
- [CSS-Tricks: A Complete Guide to CSS Grid](https://css-tricks.com/snippets/css/complete-guide-grid/)
- [web.dev: Learn CSS — Layout](https://web.dev/learn/css/layout)（content-out / layout-in 的出处）
- [web.dev: CSS Container Queries](https://web.dev/articles/css-container-queries)
- 规范：[CSS Flexbox 1](https://www.w3.org/TR/css-flexbox-1/) / [CSS Grid 2](https://www.w3.org/TR/css-grid-2/) / [CSS Containment 3](https://www.w3.org/TR/css-contain-3/) -->
]]></content>
      <categories>
        <category>前端基础</category>
        <category>css</category>
      </categories>
      <tags>
        <tag>css</tag>
      </tags>
  </entry>
  <entry>
    <title>数据大屏的数据加载体系：运行时配置、Adapter 级 Mock 与推送即拉取</title>
    <url>/2026/09/09/data-platform-data-loading/</url>
    <content><![CDATA[<p>在一个数据可视化大屏项目里，「请求接口渲染数据」这件事被环境逼成了一套体系。三个现实约束：</p>
<ul>
<li><strong>后端是多微服务</strong>（事件、设施、图层、用户……），联调排期永远对不齐，前端必须能在后端没就绪时把整个大屏跑起来；</li>
<li><strong>部署环境经常换地址</strong>——今天对接测试网关，明天切演示环境，后天要带去客户内网。每次改 <code>baseURL</code> 都要重新打包发版，不可接受；</li>
<li><strong>数据要「实时」</strong>——新事件发生后端会推送，但推送消息怎么变成列表和地图上的点，中间有讲究。</li>
</ul>
<p>这篇文章按数据从后端到像素的路径，把这个项目的数据加载方案拆开讲：<strong>运行时配置 → 请求封装 → Mock 拦截 → API 组织 → 数据流转 → 实时推送 → 按需加载</strong>。技术栈是 Vue 3 + Pinia + Axios，方案本身与框架关系不大。</p>
<span id="more"></span>

<h2 id="一、运行时配置：改地址不重新构建"><a href="#一、运行时配置：改地址不重新构建" class="headerlink" title="一、运行时配置：改地址不重新构建"></a>一、运行时配置：改地址不重新构建</h2><p>先解决「换环境就重新打包」的问题。思路是把配置从「构建时」挪到「运行时」：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-comment">&lt;!-- index.html：同步加载，保证在任何业务代码执行前就绪 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">script</span> <span class="hljs-attr">src</span>=<span class="hljs-string">&quot;/config.js&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">script</span>&gt;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// public/config.js：跟静态资源一起部署，不参与构建</span><br><span class="hljs-variable language_">window</span>.<span class="hljs-property">__APP_CONFIG__</span> = &#123;<br>  <span class="hljs-attr">api</span>: &#123;<br>    <span class="hljs-attr">baseURL</span>: <span class="hljs-string">&#x27;https://api.example.com&#x27;</span>,  <span class="hljs-comment">// 网关地址</span><br>    <span class="hljs-attr">timeout</span>: <span class="hljs-number">30000</span>,<br>    <span class="hljs-attr">mock</span>: <span class="hljs-literal">false</span>,                          <span class="hljs-comment">// Mock 总开关</span><br>  &#125;,<br>  <span class="hljs-attr">ws</span>:  &#123; <span class="hljs-attr">enabled</span>: <span class="hljs-literal">true</span>, <span class="hljs-attr">pushUrl</span>: <span class="hljs-string">&#x27;wss://api.example.com/ws/push&#x27;</span>, <span class="hljs-attr">reconnectInterval</span>: <span class="hljs-number">5000</span> &#125;,<br>  <span class="hljs-attr">sse</span>: &#123; <span class="hljs-attr">enabled</span>: <span class="hljs-literal">true</span>, <span class="hljs-attr">url</span>: <span class="hljs-string">&#x27;/api/ai/chat&#x27;</span>, <span class="hljs-attr">mock</span>: <span class="hljs-literal">true</span> &#125;,<br>  <span class="hljs-comment">// 地图中心点、底图列表、瓦片服务前缀……也都在这里</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>读取时与代码里的<strong>默认配置深度合并</strong>，<code>config.js</code> 加载失败也有兜底：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">getConfig</span>(<span class="hljs-params"></span>): <span class="hljs-title class_">AppConfig</span> &#123;<br>  <span class="hljs-keyword">const</span> win = <span class="hljs-variable language_">window</span> <span class="hljs-keyword">as</span> <span class="hljs-built_in">unknown</span> <span class="hljs-keyword">as</span> &#123; <span class="hljs-attr">__APP_CONFIG__</span>?: <span class="hljs-title class_">Partial</span>&lt;<span class="hljs-title class_">AppConfig</span>&gt; &#125;<br>  <span class="hljs-keyword">if</span> (!win.<span class="hljs-property">__APP_CONFIG__</span>) <span class="hljs-keyword">return</span> defaultConfig<br>  <span class="hljs-keyword">return</span> &#123;<br>    <span class="hljs-attr">api</span>:       &#123; ...defaultConfig.<span class="hljs-property">api</span>, ...win.<span class="hljs-property">__APP_CONFIG__</span>.<span class="hljs-property">api</span> &#125;,<br>    <span class="hljs-attr">ws</span>:        &#123; ...defaultConfig.<span class="hljs-property">ws</span>, ...win.<span class="hljs-property">__APP_CONFIG__</span>.<span class="hljs-property">ws</span> &#125;,<br>    <span class="hljs-attr">sse</span>:       &#123; ...defaultConfig.<span class="hljs-property">sse</span>, ...win.<span class="hljs-property">__APP_CONFIG__</span>.<span class="hljs-property">sse</span> &#125;,<br>    <span class="hljs-comment">// ...</span><br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这个方案有三个细节值得抠：</p>
<ol>
<li><code>&lt;script&gt;</code> 必须放在应用 bundle <strong>之前</strong>同步加载（不用 <code>defer</code>），否则应用初始化时配置还没注入；</li>
<li>深度合并只做了一层——够用的前提下不做通用 <code>deepMerge</code>，配置结构变了让 TS 类型直接报错，比静默合并错位更早暴露问题；</li>
<li>Vite 生态里更常见的做法是多个 <code>.env.xxx</code> 文件 + <code>import.meta.env</code>，但那是<strong>构建时注入</strong>：<code>vite build</code> 出来的产物里地址已经写死。运行时配置牺牲了一点点加载顺序上的心智负担，换来「同一份产物跑任意环境」——对内网交付项目，这笔账必须这么算。</li>
</ol>
<h2 id="二、请求封装：契约、解包、错误分类"><a href="#二、请求封装：契约、解包、错误分类" class="headerlink" title="二、请求封装：契约、解包、错误分类"></a>二、请求封装：契约、解包、错误分类</h2><p>Axios 封装是每个项目的保留节目，这里说三个设计点。</p>
<p><strong>统一响应契约</strong>。后端所有接口遵循 <code>{ success, code, message, data }</code> 结构，响应拦截器负责校验和<strong>解包</strong>——业务代码拿到的永远是净数据：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts">service.<span class="hljs-property">interceptors</span>.<span class="hljs-property">response</span>.<span class="hljs-title function_">use</span>(<br>  <span class="hljs-function">(<span class="hljs-params">response</span>) =&gt;</span> &#123;<br>    <span class="hljs-keyword">const</span> res = response.<span class="hljs-property">data</span> <span class="hljs-keyword">as</span> <span class="hljs-title class_">ApiResponse</span><br>    <span class="hljs-comment">// HTTP 200 但业务失败（success=false 或 code 异常）也走 reject</span><br>    <span class="hljs-keyword">if</span> (!res.<span class="hljs-property">success</span> || (res.<span class="hljs-property">code</span> !== <span class="hljs-number">0</span> &amp;&amp; res.<span class="hljs-property">code</span> !== <span class="hljs-number">200</span>)) &#123;<br>      <span class="hljs-keyword">return</span> <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">reject</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(res.<span class="hljs-property">message</span> || <span class="hljs-string">&#x27;请求失败&#x27;</span>))<br>    &#125;<br>    <span class="hljs-keyword">return</span> res.<span class="hljs-property">data</span>   <span class="hljs-comment">// 直接返回 data 字段</span><br>  &#125;,<br>  <span class="hljs-function">(<span class="hljs-params">error</span>) =&gt;</span> &#123; <span class="hljs-comment">/* 见下文错误分类 */</span> &#125;<br>)<br></code></pre></td></tr></table></figure>

<p>业务失败和 HTTP 失败统一成一条 <code>Promise.reject(Error)</code> 流，组件层 <code>catch</code> 到的永远是带中文 message 的 Error，不用每个调用点再判断一次 <code>res.success</code>。</p>
<p><strong>错误分类比统一提示更重要</strong>。错误拦截器把「同一长得很像的 error」拆成用户能看懂的几种：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">if</span> (error.<span class="hljs-property">response</span>) &#123;<br>  <span class="hljs-keyword">const</span> &#123; status &#125; = error.<span class="hljs-property">response</span><br>  <span class="hljs-keyword">if</span> (status === <span class="hljs-number">401</span>) &#123;                       <span class="hljs-comment">// 登录过期：清凭证 + 跳登录页</span><br>    <span class="hljs-title function_">clearAuth</span>()<br>    <span class="hljs-variable language_">window</span>.<span class="hljs-property">location</span>.<span class="hljs-property">href</span> = <span class="hljs-string">&#x27;/login&#x27;</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">reject</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">&#x27;登录已过期，请重新登录&#x27;</span>))<br>  &#125;<br>  <span class="hljs-keyword">if</span> (status === <span class="hljs-number">403</span>) <span class="hljs-keyword">return</span> <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">reject</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">&#x27;没有权限执行此操作&#x27;</span>))<br>  <span class="hljs-keyword">return</span> <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">reject</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(error.<span class="hljs-property">response</span>.<span class="hljs-property">data</span>?.<span class="hljs-property">message</span> || <span class="hljs-string">`请求错误 (<span class="hljs-subst">$&#123;status&#125;</span>)`</span>))<br>&#125;<br><span class="hljs-keyword">if</span> (error.<span class="hljs-property">code</span> === <span class="hljs-string">&#x27;ECONNABORTED&#x27;</span>) <span class="hljs-keyword">return</span> <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">reject</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">&#x27;请求超时，请稍后重试&#x27;</span>))<br><span class="hljs-keyword">if</span> (axios.<span class="hljs-title function_">isCancel</span>(error))          <span class="hljs-keyword">return</span> <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">reject</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">&#x27;请求已取消&#x27;</span>))<br><span class="hljs-keyword">if</span> (!<span class="hljs-variable language_">window</span>.<span class="hljs-property">navigator</span>.<span class="hljs-property">onLine</span>)       <span class="hljs-keyword">return</span> <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">reject</span>(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">&#x27;网络连接已断开&#x27;</span>))<br></code></pre></td></tr></table></figure>

<p>注意 401 里做了<strong>副作用</strong>（清 token、跳转）——这类「全局性、只该做一次」的处理放拦截器，比每个组件各写一遍可靠得多。</p>
<p><strong>Token 注入</strong>放在请求拦截器，来源区分 localStorage（记住我）和 sessionStorage（会话级），这是登录模块的约定，请求层只管读。</p>
<h2 id="三、Mock-拦截：换-adapter，而不是换函数"><a href="#三、Mock-拦截：换-adapter，而不是换函数" class="headerlink" title="三、Mock 拦截：换 adapter，而不是换函数"></a>三、Mock 拦截：换 adapter，而不是换函数</h2><p>前端脱离后端开发，常见方案有三档：</p>
<table>
<thead>
<tr>
<th>方案</th>
<th>做法</th>
<th>问题</th>
</tr>
</thead>
<tbody><tr>
<td>注释切换</td>
<td>业务代码里 <code>if (mock) return fakeData</code></td>
<td>mock 逻辑散落各处，上线前忘删就是事故</td>
</tr>
<tr>
<td>代理工具</td>
<td>Mock.js 劫持 XHR &#x2F; MSW 拦截 Service Worker</td>
<td>引新依赖；MSW 需要注册 worker，环境受限时麻烦</td>
</tr>
<tr>
<td><strong>adapter 替换</strong></td>
<td>请求拦截器里把 axios 的 adapter 换成假实现</td>
<td>零依赖、零业务侵入，请求根本不发出</td>
</tr>
</tbody></table>
<p>这个项目用的第三档。Axios 的 <code>adapter</code> 是真正发请求的底层函数，在<strong>请求拦截器</strong>里替换它：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">if</span> (mock) &#123;<br>  service.<span class="hljs-property">interceptors</span>.<span class="hljs-property">request</span>.<span class="hljs-title function_">use</span>(<span class="hljs-function">(<span class="hljs-params">config</span>) =&gt;</span> &#123;<br>    <span class="hljs-keyword">const</span> mockData = <span class="hljs-title function_">mockAdapter</span>(config.<span class="hljs-property">url</span>, config.<span class="hljs-property">method</span>, config.<span class="hljs-property">params</span>, config.<span class="hljs-property">data</span>)<br>    <span class="hljs-keyword">if</span> (mockData !== <span class="hljs-literal">undefined</span>) &#123;<br>      config.<span class="hljs-property">adapter</span> = <span class="hljs-function">() =&gt;</span>                 <span class="hljs-comment">// 命中 mock：替换 adapter，直接 resolve 假响应</span><br>        <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">resolve</span>(&#123;<br>          <span class="hljs-attr">data</span>: &#123; <span class="hljs-attr">success</span>: <span class="hljs-literal">true</span>, <span class="hljs-attr">code</span>: <span class="hljs-number">0</span>, <span class="hljs-attr">data</span>: mockData, <span class="hljs-attr">message</span>: <span class="hljs-string">&#x27;ok&#x27;</span> &#125;,<br>          <span class="hljs-attr">status</span>: <span class="hljs-number">200</span>, <span class="hljs-attr">statusText</span>: <span class="hljs-string">&#x27;OK&#x27;</span>, <span class="hljs-attr">headers</span>: &#123;&#125;, config,<br>        &#125;)<br>    &#125;<br>    <span class="hljs-keyword">return</span> config                            <span class="hljs-comment">// 未命中：正常发出真实请求</span><br>  &#125;)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>路由表用类似后端框架的写法，支持 <code>:param</code> 占位符：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">const</span> <span class="hljs-attr">mockRoutes</span>: <span class="hljs-title class_">MockRoute</span>[] = [<br>  &#123; <span class="hljs-attr">pattern</span>: <span class="hljs-string">&#x27;event-service/event/list&#x27;</span>, <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;post&#x27;</span>, <span class="hljs-attr">handler</span>: <span class="hljs-function">(<span class="hljs-params">_p, _q, body</span>) =&gt;</span> &#123;<br>      <span class="hljs-comment">// 与真实后端行为一致：category/severity 过滤 + pageNum/pageSize 分页</span><br>      <span class="hljs-keyword">let</span> list = [...mockEvents]<br>      <span class="hljs-keyword">if</span> (<span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(body?.<span class="hljs-property">category</span>) &amp;&amp; body.<span class="hljs-property">category</span>.<span class="hljs-property">length</span> &gt; <span class="hljs-number">0</span>) &#123;<br>        list = list.<span class="hljs-title function_">filter</span>(<span class="hljs-function"><span class="hljs-params">e</span> =&gt;</span> body.<span class="hljs-property">category</span>.<span class="hljs-title function_">includes</span>(e.<span class="hljs-property">category</span>))<br>      &#125;<br>      <span class="hljs-keyword">const</span> start = ((<span class="hljs-title class_">Number</span>(body.<span class="hljs-property">pageNum</span>) || <span class="hljs-number">1</span>) - <span class="hljs-number">1</span>) * (<span class="hljs-title class_">Number</span>(body.<span class="hljs-property">pageSize</span>) || <span class="hljs-number">10</span>)<br>      <span class="hljs-keyword">return</span> &#123; <span class="hljs-attr">records</span>: list.<span class="hljs-title function_">slice</span>(start, start + <span class="hljs-number">10</span>), <span class="hljs-attr">total</span>: list.<span class="hljs-property">length</span>, <span class="hljs-comment">/* ... */</span> &#125;<br>  &#125;&#125;,<br>  &#123; <span class="hljs-attr">pattern</span>: <span class="hljs-string">&#x27;event-service/news/list/:eventId&#x27;</span>, <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;get&#x27;</span>, <span class="hljs-attr">handler</span>: <span class="hljs-function">(<span class="hljs-params">p</span>) =&gt;</span> <span class="hljs-title function_">findNewsBy</span>(p.<span class="hljs-property">eventId</span>) &#125;,<br>]<br></code></pre></td></tr></table></figure>

<p>两个关键设计：</p>
<ol>
<li><strong>mock 行为与真实后端对齐</strong>（过滤、分页、响应结构都按接口文档实现），联调切换时前端逻辑一行不改，只有数据源变了——这正是 adapter 方案的价值：切换点在传输层，不在业务层；</li>
<li><strong>未命中的路由放行真实请求</strong>并打 warn，允许「一半接口真实、一半接口 mock」的渐进联调状态，而不是全有或全无。</li>
</ol>
<h2 id="四、API-层组织：微服务前缀集中管理"><a href="#四、API-层组织：微服务前缀集中管理" class="headerlink" title="四、API 层组织：微服务前缀集中管理"></a>四、API 层组织：微服务前缀集中管理</h2><p>后端按微服务拆分，网关按<strong>路径前缀</strong>路由。前缀常量集中一处定义，避免拼写漂移：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// api/commonApi.ts —— 服务前缀唯一事实来源</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> eventService = <span class="hljs-string">&#x27;event-service&#x27;</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> facilityService = <span class="hljs-string">&#x27;facility-service&#x27;</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> authService = <span class="hljs-string">&#x27;auth-service&#x27;</span><br><br><span class="hljs-comment">// api/event.ts —— 按业务域拆文件</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">getEventList</span>(<span class="hljs-params"><span class="hljs-attr">params</span>?: <span class="hljs-title class_">EventListParams</span></span>) &#123;<br>  <span class="hljs-keyword">return</span> <span class="hljs-title function_">post</span>(<span class="hljs-string">`/<span class="hljs-subst">$&#123;eventService&#125;</span>/event/list`</span>, &#123; pageNum, pageSize, ...filters &#125;)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>API 文件只做三件事：拼路径、传参数、<strong>把后端结构翻译成前端类型</strong>。以事件列表为例，后端字段和前端模型并不一致——<code>description</code> → <code>summary</code>，关键词&#x2F;实体字段是 PostgreSQL jsonb 的包装结构（<code>{ null: false, value: &quot;[...]&quot; }</code>，值是序列化字符串）：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">/** PG jsonb 包装字段 → string[] */</span><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">parsePgJsonArray</span>(<span class="hljs-params"><span class="hljs-attr">field</span>: <span class="hljs-title class_">PgJsonField</span> | <span class="hljs-built_in">string</span>[] | <span class="hljs-literal">undefined</span></span>): <span class="hljs-built_in">string</span>[] &#123;<br>  <span class="hljs-keyword">if</span> (!field) <span class="hljs-keyword">return</span> []<br>  <span class="hljs-keyword">if</span> (<span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(field)) <span class="hljs-keyword">return</span> field.<span class="hljs-title function_">map</span>(<span class="hljs-title class_">String</span>)   <span class="hljs-comment">// 兼容后端已展开的形态</span><br>  <span class="hljs-keyword">if</span> (field.<span class="hljs-property">null</span> || !field.<span class="hljs-property">value</span>) <span class="hljs-keyword">return</span> []<br>  <span class="hljs-keyword">try</span> &#123;<br>    <span class="hljs-keyword">const</span> parsed = <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">parse</span>(field.<span class="hljs-property">value</span>)             <span class="hljs-comment">// 值是 JSON 字符串，要二次解析</span><br>    <span class="hljs-keyword">return</span> <span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(parsed) ? parsed.<span class="hljs-title function_">map</span>(<span class="hljs-title class_">String</span>) : []<br>  &#125; <span class="hljs-keyword">catch</span> &#123; <span class="hljs-keyword">return</span> [] &#125;<br>&#125;<br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">mapBackendEvent</span>(<span class="hljs-params"><span class="hljs-attr">raw</span>: <span class="hljs-title class_">BackendEventInfo</span></span>): <span class="hljs-title class_">EventItem</span> &#123;<br>  <span class="hljs-keyword">return</span> &#123;<br>    <span class="hljs-attr">id</span>: raw.<span class="hljs-property">id</span>,<br>    <span class="hljs-attr">title</span>: raw.<span class="hljs-property">title</span>,<br>    <span class="hljs-attr">summary</span>: raw.<span class="hljs-property">description</span>,          <span class="hljs-comment">// 字段改名收在这一层</span><br>    <span class="hljs-attr">tags</span>: <span class="hljs-title function_">parsePgJsonArray</span>(raw.<span class="hljs-property">keywords</span>),<br>    <span class="hljs-comment">// ...</span><br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这类「脏活」必须圈死在 API 层——store 和组件永远只见过滤干净的前端类型。后端分页结构在不同接口间还漂移（有的叫 <code>records</code>、有的叫 <code>list</code>、有的叫 <code>rows</code>），同样在 API 层做兼容：<code>res.records ?? res.list ?? res.rows ?? res.data</code>。</p>
<h2 id="五、完整数据流：以事件列表为例"><a href="#五、完整数据流：以事件列表为例" class="headerlink" title="五、完整数据流：以事件列表为例"></a>五、完整数据流：以事件列表为例</h2><p>把前面的积木串起来，一个大屏面板的数据流长这样：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">EventPanel onMounted<br>  └─ Promise.all([loadCategories(), loadSeverities()])   // 筛选项也是接口下发的<br>       └─ loadEvents()<br>            └─ getEventList(&#123; pageNum: 1, pageSize: 10, category, severity, keyword &#125;)<br>                 └─ axios（拦截器：mock 判断 → token → 响应解包）<br>                      └─ mapBackendEvent 字段映射 + jsonb 解析<br>                           └─ store: events = res.list<br>                                ├─ computed filteredEvents        // 多类别/严重度/关键词/视口过滤<br>                                │    └─ EventList v-for 渲染卡片<br>                                │         └─ 滚动距底 80px → loadMoreEvents() 追加下一页<br>                                └─ MapContainer watch(filteredEvents)<br>                                     └─ GeoJSON source.setData()  // 地图增量更新，图层不重建<br></code></pre></td></tr></table></figure>

<p>三个细节：</p>
<ul>
<li><strong>筛选在哪层做？</strong> 服务端筛选（接口参数）和客户端筛选（computed）并存：翻页和关键词搜索走接口（数据量大、要分页），视口过滤、会话态标记在 computed 里做（变化频繁、不值得发请求）；</li>
<li><strong>地图更新用 <code>setData</code> 而不是重建图层</strong>：GeoJSON source 支持 <code>setData</code> 原地换数据，图层定义只建一次，数据变化只触发一次重绘；</li>
<li><strong>滚动懒加载的守卫</strong>：<code>loadMoreEvents</code> 开头检查 <code>isLoadingMore || !hasMoreEvents</code>，防止滚动事件连打导致重复请求和页码错乱。</li>
</ul>
<h2 id="六、实时数据：推送即拉取"><a href="#六、实时数据：推送即拉取" class="headerlink" title="六、实时数据：推送即拉取"></a>六、实时数据：推送即拉取</h2><p>新事件由后端通过 WebSocket 推送。最容易想到的写法是「推送里带什么数据就渲染什么」——<strong>恰恰是错的</strong>。推送消息里往往只有摘要（比如 <code>{ totalCount: 5 }</code>），直接渲染会绕过 API 层的字段映射和 store 的筛选状态；用户此刻正筛选着「突发级事件」，一条不匹配的推送直接塞进列表就是脏数据。</p>
<p>这个项目的策略是<strong>推送即拉取（push-then-pull）</strong>：WS 消息只当「数据变了」的信号，收到后带着当前筛选条件重拉第一页：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> <span class="hljs-title function_">handleMessage</span>(<span class="hljs-params"><span class="hljs-attr">event</span>: <span class="hljs-title class_">MessageEvent</span></span>) &#123;<br>  <span class="hljs-keyword">const</span> parsed = <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">parse</span>(event.<span class="hljs-property">data</span>)<br>  <span class="hljs-keyword">const</span> totalCount = <span class="hljs-title class_">Number</span>(parsed?.<span class="hljs-property">data</span>?.<span class="hljs-property">totalCount</span> ?? <span class="hljs-number">0</span>)<br>  <span class="hljs-keyword">if</span> (totalCount &gt; <span class="hljs-number">0</span>) &#123;<br>    <span class="hljs-keyword">if</span> (refreshTimer) <span class="hljs-built_in">clearTimeout</span>(refreshTimer)<br>    refreshTimer = <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;        <span class="hljs-comment">// 500ms 节流：连发多条推送只拉一次</span><br>      eventStore.<span class="hljs-title function_">refreshEvents</span>()            <span class="hljs-comment">// 重拉第一页，pageSize 取已加载数量</span><br>    &#125;, <span class="hljs-number">500</span>)<br>    <span class="hljs-title class_">ElNotification</span>(&#123; <span class="hljs-attr">title</span>: <span class="hljs-string">`新事件 (<span class="hljs-subst">$&#123;totalCount&#125;</span> 条)`</span>, <span class="hljs-comment">/* ... */</span> &#125;)<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这样推送数据永远走与手动刷新完全相同的链路（接口 → 映射 → store → computed → 视图），筛选一致性免费获得。代价是多一次 HTTP 往返，但大屏场景事件频率不高，这点延迟无所谓。<strong>结论：推送当信号用，别当数据用。</strong></p>
<p>断线重连用<strong>指数退避</strong>，每次失败间隔 ×1.5，封顶 60s，连接成功后归零——避免后端重启时前端疯狂重连：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> <span class="hljs-title function_">scheduleReconnect</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">if</span> (manualClosed || !wsConfig.<span class="hljs-property">enabled</span>) <span class="hljs-keyword">return</span><br>  <span class="hljs-keyword">const</span> delay = <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">min</span>(wsConfig.<span class="hljs-property">reconnectInterval</span> * <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">pow</span>(<span class="hljs-number">1.5</span>, reconnectAttempts), <span class="hljs-number">60000</span>)<br>  reconnectAttempts++<br>  reconnectTimer = <span class="hljs-built_in">setTimeout</span>(connect, delay)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>组件卸载（<code>onUnmounted</code>）时置 <code>manualClosed = true</code> 再 close，防止主动关闭也触发重连。AI 对话的流式回复则走 SSE：原生 <code>fetch</code> + <code>ReadableStream.getReader()</code> 手动按行解析 <code>data:</code> 帧（POST 请求用不了原生 EventSource），同样由 config.js 的 <code>sse.mock</code> 开关一键切到「逐字输出」的假流。</p>
<h2 id="七、按需加载清单"><a href="#七、按需加载清单" class="headerlink" title="七、按需加载清单"></a>七、按需加载清单</h2><p>大屏首屏压力大，「什么时候才发起请求&#x2F;渲染」需要刻意设计。这个项目的清单：</p>
<table>
<thead>
<tr>
<th>手段</th>
<th>实现</th>
<th>收益</th>
</tr>
</thead>
<tbody><tr>
<td>组件级懒加载</td>
<td>面板用 <code>defineAsyncComponent</code> 挂载</td>
<td>首屏 bundle 不含次要面板</td>
</tr>
<tr>
<td>初始化单例</td>
<td><code>_homeInitPromise</code> 缓存 Promise，首页与面板共享</td>
<td>两个组件同时触发初始化只发一次请求</td>
</tr>
<tr>
<td>勾选驱动拉瓦片</td>
<td>用户勾选图层才请求瓦片地址，取消勾选只隐藏不销毁</td>
<td>不看的图层零请求</td>
</tr>
<tr>
<td>滚动懒加载</td>
<td>列表距底 80px 触发下一页</td>
<td>首屏不拉全量</td>
</tr>
<tr>
<td>弹框懒加载</td>
<td>详情弹框 <code>watch(visible)</code> 打开时才请求关联新闻</td>
<td>关联数据用不到就不拉</td>
</tr>
<tr>
<td>防抖节流</td>
<td>搜索 400ms、视口同步 300ms + 变化阈值、WS 刷新 500ms</td>
<td>高频交互不放大请求量</td>
</tr>
<tr>
<td>会话级缓存</td>
<td>全局样式配置进 sessionStorage</td>
<td>刷新页面不重拉样式</td>
</tr>
</tbody></table>
<p>其中「初始化单例」容易被忽略：首页 <code>onMounted</code> 和图层面板 <code>onMounted</code> 都会触发初始化请求，用 Promise 缓存挡住第二次——比「谁先挂载谁负责」的君子协定可靠得多。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>四种数据源的切换矩阵：</p>
<table>
<thead>
<tr>
<th>数据源</th>
<th>开关位置</th>
<th>生效方式</th>
<th>典型场景</th>
</tr>
</thead>
<tbody><tr>
<td>真实 HTTP</td>
<td><code>api.mock: false</code></td>
<td>正常请求</td>
<td>联调&#x2F;生产</td>
</tr>
<tr>
<td>Mock 数据</td>
<td><code>api.mock: true</code> + mockRoutes 命中</td>
<td>替换 axios adapter，请求不发出</td>
<td>后端未就绪</td>
</tr>
<tr>
<td>WS 推送</td>
<td><code>ws.enabled</code></td>
<td>推送当信号，节流后带条件重拉</td>
<td>事件实时更新</td>
</tr>
<tr>
<td>SSE 流</td>
<td><code>sse.mock</code></td>
<td>fetch 流解析 &#x2F; setTimeout 假流</td>
<td>AI 对话</td>
</tr>
</tbody></table>
<p><strong>一句话记住</strong>：数据加载体系的目标是让「换环境、换数据源、上实时」都不侵入业务代码——配置在运行时注入，Mock 在传输层拦截，字段映射圈死在 API 层，推送只当信号不当数据，剩下的组件就只管对着干净的前端类型渲染。</p>
]]></content>
      <categories>
        <category>前端工程化</category>
      </categories>
      <tags>
        <tag>axios</tag>
        <tag>websocket</tag>
        <tag>sse</tag>
        <tag>前端工程化</tag>
      </tags>
  </entry>
  <entry>
    <title>前端权限控制实战：从 token 到 v-auth 指令，数据权限放在哪一层？</title>
    <url>/2026/09/09/front-end-permission/</url>
    <content><![CDATA[<blockquote>
<p>这是真实项目复盘系列的第二篇。这个 Vue3 + Vite + Pinia + Ant Design Vue 的后台系统里，权限是典型的”隐形基础设施”：平时没人注意它，直到某天测试提了个 bug——“为什么这个角色看不到导出按钮”。本文把这套权限体系完整拆开：<strong>登录凭证怎么存、权限码怎么建模、按钮和菜单怎么按权限显隐、角色权限在哪个界面配出来</strong>，以及一个几乎所有后台项目都会遇到、也最容易做错的问题——<strong>数据权限（数据范围过滤）到底放在前端还是后端</strong>。先说结论：这个项目把数据权限完全交给了后端，前端只做了功能层（菜单&#x2F;按钮）权限。为什么这样选、边界在哪，第 7 节详细展开。</p>
</blockquote>
<h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul>
<li><a href="#sec0">0. 前言：前端权限到底防谁</a></li>
<li><a href="#sec1">1. 登录与凭证：token 从哪来，权限快照存哪去</a></li>
<li><a href="#sec2">2. 权限数据模型：两张 Map 与「模块:动作」权限码</a></li>
<li><a href="#sec3">3. 判断核心：hasBtnAuth &#x2F; hasAuth 双函数</a></li>
<li><a href="#sec4">4. 双指令：v-auth 与 v-menuAuth</a></li>
<li><a href="#sec5">5. 三层显隐实战：模块入口 → Tab → 按钮</a></li>
<li><a href="#sec6">6. 权限的配置面：角色管理与权限树</a></li>
<li><a href="#sec7">7. 数据权限：这一层前端做了什么、该做什么</a></li>
<li><a href="#sec8">8. 短板与改进清单</a></li>
<li><a href="#sec9">9. 写在最后</a></li>
</ul>
<p><a id="sec0"></a></p>
<h2 id="0-前言：前端权限到底防谁"><a href="#0-前言：前端权限到底防谁" class="headerlink" title="0. 前言：前端权限到底防谁"></a>0. 前言：前端权限到底防谁</h2><p>先把一个立场立住，后面所有设计才立得住：<strong>前端权限从来不是安全机制，而是体验机制</strong>。</p>
<p>用户打开 DevTools 就能改 localStorage、就能直接 curl 你的接口。所以前端权限真正回答的不是”怎么阻止未授权操作”，而是”<strong>怎么让不同角色看到与身份相符的界面，不误操作、不困惑</strong>“。真正的安全边界永远在后端——每一个接口都要自己校验”这个 token 有没有资格调我”。</p>
<p>理解了这一点，这个项目的很多”非主流”选择就顺理成章了。整个权限体系分三层：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">┌──────────── 认证层：你是谁 ────────────────────────┐<br>│ login → token → localStorage                       │<br>│ axios 拦截器：Authorization: Bearer &lt;token&gt;          │<br>│ 响应 code 401/601 → 踢回 /login（后端兜底）          │<br>└──────────────┬─────────────────────────────────────┘<br>               ▼<br>┌──────────── 功能层：你能看到什么、点什么 ──────────────┐<br>│ GET /sysPermission/getPermissionByUser（靠 token 识别）│<br>│   ├─ buttonPermissionMap → localStorage.btnObj       │<br>│   └─ menuPermissionMap  → localStorage.menuObj       │<br>│ 判断：hasBtnAuth / hasAuth（同步读 localStorage）      │<br>│ 消费：v-auth（按钮） · v-menuAuth（模块入口）           │<br>│       hasAuth + v-if（Tab） · hasAuth（下拉菜单项）    │<br>└──────────────┬─────────────────────────────────────┘<br>               ▼<br>┌──────────── 数据层：你能拿到哪些数据 ──────────────────┐<br>│ 前端：无显式实现（部门=用户属性，地区=业务筛选条件）     │<br>│ 实际：后端按 token 过滤数据范围，前端只负责渲染          │<br>└─────────────────────────────────────────────────────┘<br></code></pre></td></tr></table></figure>

<p>一个和主流后台模板（vue-element-admin 那套）很不一样的前提：<strong>这个系统没有多路由页面</strong>。整个应用只有 <code>/login</code> 和 <code>/home</code> 两个路由，所有业务模块（设施管理、指标体系、评估任务、用户管理……）都是 <code>/home</code> 内部通过 <code>moduleStore.activeModule</code> + <code>v-if</code> 切换的组件。所以经典方案里的”动态路由 + addRoute + permission store”在这完全没有用武之地——<strong>没有路由可以管权限，权限管的是组件</strong>。这个前提让整个权限方案变得非常薄，也暴露了它最本质的形态。</p>
<p><a id="sec1"></a></p>
<h2 id="1-登录与凭证：token-从哪来，权限快照存哪去"><a href="#1-登录与凭证：token-从哪来，权限快照存哪去" class="headerlink" title="1. 登录与凭证：token 从哪来，权限快照存哪去"></a>1. 登录与凭证：token 从哪来，权限快照存哪去</h2><h3 id="1-1-登录链路"><a href="#1-1-登录链路" class="headerlink" title="1.1 登录链路"></a>1.1 登录链路</h3><p>登录逻辑收在 Pinia 的 user store 里，流程是清晰的四步：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/store/user.ts（有删减）</span><br><span class="hljs-keyword">async</span> <span class="hljs-title function_">login</span>(<span class="hljs-params"><span class="hljs-attr">params</span>: <span class="hljs-built_in">any</span></span>) &#123;<br> <span class="hljs-title function_">sysUserlogin</span>(params).<span class="hljs-title function_">then</span>(<span class="hljs-function"><span class="hljs-params">res</span> =&gt;</span> &#123;<br>  <span class="hljs-keyword">if</span> (res.<span class="hljs-property">code</span> == <span class="hljs-number">200</span>) &#123;<br>   <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">setToken</span>(res.<span class="hljs-property">data</span>.<span class="hljs-property">token</span>);              <span class="hljs-comment">// ① 存 token</span><br>   <span class="hljs-keyword">let</span> userInfo = &#123;<br>    <span class="hljs-attr">username</span>: res.<span class="hljs-property">data</span>.<span class="hljs-property">username</span>,<br>    <span class="hljs-attr">displayName</span>: res.<span class="hljs-property">data</span>.<span class="hljs-property">displayName</span><br>   &#125;;<br>   <span class="hljs-title function_">sysPermissionList</span>().<span class="hljs-title function_">then</span>(<span class="hljs-function"><span class="hljs-params">res</span> =&gt;</span> &#123;            <span class="hljs-comment">// ② 拉权限</span><br>    <span class="hljs-keyword">if</span> (res.<span class="hljs-property">code</span> == <span class="hljs-number">200</span> &amp;&amp; res.<span class="hljs-property">data</span>) &#123;<br>     <span class="hljs-variable language_">localStorage</span>.<span class="hljs-title function_">setItem</span>(<span class="hljs-string">&#x27;btnObj&#x27;</span>, <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">stringify</span>(res.<span class="hljs-property">data</span>.<span class="hljs-property">buttonPermissionMap</span>));<br>     <span class="hljs-variable language_">localStorage</span>.<span class="hljs-title function_">setItem</span>(<span class="hljs-string">&#x27;menuObj&#x27;</span>, <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">stringify</span>(res.<span class="hljs-property">data</span>.<span class="hljs-property">menuPermissionMap</span>));<br>     <span class="hljs-variable language_">localStorage</span>.<span class="hljs-title function_">setItem</span>(<span class="hljs-string">&#x27;userInfo&#x27;</span>, <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">stringify</span>(userInfo));  <span class="hljs-comment">// ③ 存用户信息</span><br>     router.<span class="hljs-title function_">push</span>(<span class="hljs-string">&#x27;/home&#x27;</span>);            <span class="hljs-comment">// ④ 跳首页</span><br>    &#125;<br>   &#125;);<br>  &#125;<br> &#125;);<br>&#125;<br></code></pre></td></tr></table></figure>

<p>注意两个设计点：</p>
<p><strong>权限接口不带参数</strong>。<code>GET /sysPermission/getPermissionByUser</code> 连 userId 都不传——后端从请求头的 token 里识别用户。这是正确的做法：<strong>前端声明的身份不可信，凭证里的身份才可信</strong>。同理，登录响应里也只有 <code>username/displayName/token</code>，没有角色列表。</p>
<p><strong>权限快照存 localStorage，而不是 Pinia state</strong>。这是这个项目最值得说道的取舍。store&#x2F;user.ts 的 state 里只有 <code>token</code> 和 <code>userInfo</code>，权限码两张 Map 完全不进 store。为什么？因为权限的消费方是<strong>自定义指令</strong>——指令的 <code>mounted</code> 钩子在组件 setup 之外执行，拿不到组件实例的 store 上下文，如果权限在 Pinia 里，指令就得在模块顶层手动初始化 pinia 再取 store（这个项目在 directive&#x2F;index.ts 顶部确实 <code>import pinia from &#39;@/store&#39;</code> 做了这件事，但只用于引入结构）。localStorage 是同步全局可读的，指令里一行 <code>localStorage.getItem(&#39;btnObj&#39;)</code> 就能拿到，代价是<strong>权限快照与登录态耦合在浏览器存储里</strong>——第 8 节会讲这个代价。</p>
<h3 id="1-2-请求层：token-注入与-401-兜底"><a href="#1-2-请求层：token-注入与-401-兜底" class="headerlink" title="1.2 请求层：token 注入与 401 兜底"></a>1.2 请求层：token 注入与 401 兜底</h3><p>axios 拦截器承担全部凭证工作：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/service/service.ts（有删减）</span><br><span class="hljs-keyword">const</span> getToken = <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>): <span class="hljs-built_in">string</span> &#123;<br> <span class="hljs-keyword">return</span> <span class="hljs-variable language_">window</span>.<span class="hljs-property">localStorage</span>.<span class="hljs-title function_">getItem</span>(<span class="hljs-string">&#x27;token&#x27;</span>) || <span class="hljs-string">&#x27;&#x27;</span>;<br>&#125;;<br><br>service.<span class="hljs-property">interceptors</span>.<span class="hljs-property">request</span>.<span class="hljs-title function_">use</span>(<span class="hljs-function"><span class="hljs-params">config</span> =&gt;</span> &#123;<br> <span class="hljs-keyword">if</span> (<span class="hljs-title function_">getToken</span>()) &#123;<br>  config.<span class="hljs-property">headers</span>.<span class="hljs-property">Authorization</span> = <span class="hljs-string">&#x27;Bearer &#x27;</span> + <span class="hljs-title function_">getToken</span>();  <span class="hljs-comment">// 有 token 则加 token</span><br> &#125;<br> <span class="hljs-keyword">return</span> config;<br>&#125;);<br><br>service.<span class="hljs-property">interceptors</span>.<span class="hljs-property">response</span>.<span class="hljs-title function_">use</span>(<span class="hljs-function"><span class="hljs-params">response</span> =&gt;</span> &#123;<br> <span class="hljs-keyword">const</span> &#123; data &#125; = response;<br><br> <span class="hljs-comment">// 文件流（excel / octet-stream）直接放行</span><br> <span class="hljs-comment">// ...</span><br><br> <span class="hljs-keyword">if</span> (data.<span class="hljs-property">code</span> === <span class="hljs-number">401</span> || data.<span class="hljs-property">code</span> === <span class="hljs-number">601</span>) &#123;<br>  router.<span class="hljs-title function_">replace</span>(&#123; <span class="hljs-attr">path</span>: <span class="hljs-string">&#x27;/login&#x27;</span> &#125;);   <span class="hljs-comment">// 唯一的登录态失效处理</span><br>  <span class="hljs-keyword">return</span> <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">reject</span>(data);<br> &#125;<br> <span class="hljs-keyword">if</span> (data.<span class="hljs-property">code</span> !== <span class="hljs-number">200</span>) &#123;<br>  message.<span class="hljs-title function_">error</span>(data.<span class="hljs-property">msg</span> || <span class="hljs-string">&#x27;未知错误&#x27;</span>);<br>  <span class="hljs-keyword">return</span> <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">reject</span>(data);<br> &#125;<br> <span class="hljs-keyword">return</span> data;<br>&#125;);<br></code></pre></td></tr></table></figure>

<p>值得注意 <code>validateStatus: status &gt;= 200 &amp;&amp; status &lt;= 500</code>——把 4xx&#x2F;5xx 也当”成功”响应交给业务层，让后端业务码（200&#x2F;401&#x2F;601）成为唯一的状态语言。这样 401 跳转、错误提示的逻辑都收敛在响应拦截器一处。</p>
<h3 id="1-3-一个”缺失”的路由守卫"><a href="#1-3-一个”缺失”的路由守卫" class="headerlink" title="1.3 一个”缺失”的路由守卫"></a>1.3 一个”缺失”的路由守卫</h3><p>这是整个权限体系里最让熟悉 vue-element-admin 的人意外的地方：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/router/index.ts（完整）</span><br>router.<span class="hljs-title function_">beforeEach</span>(<span class="hljs-function"><span class="hljs-params">to</span> =&gt;</span> &#123;<br> <span class="hljs-title function_">setTitle</span>(to);   <span class="hljs-comment">// 只设置 document.title</span><br>&#125;);<br></code></pre></td></tr></table></figure>

<p><strong>没有 token 校验、没有白名单、没有重定向</strong>。未登录直接访问 <code>/#/home</code> 不会被拦截——页面会正常渲染，只是因为 <code>btnObj/menuObj</code> 不存在而全部按钮被禁用，第一个接口请求回来 401 再被踢回 <code>/login</code>。</p>
<p>这算 bug 吗？功能上不算（后端兜底闭环了），体验上算（用户会看到一屏禁用按钮闪一下）。改进方案第 8 节给出，就三行代码的事。</p>
<p><a id="sec2"></a></p>
<h2 id="2-权限数据模型：两张-Map-与「模块-动作」权限码"><a href="#2-权限数据模型：两张-Map-与「模块-动作」权限码" class="headerlink" title="2. 权限数据模型：两张 Map 与「模块:动作」权限码"></a>2. 权限数据模型：两张 Map 与「模块:动作」权限码</h2><p>后端下发的权限长这样（项目里留有一份完整的权限码清单 mock，可以直接当文档看）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-punctuation">&#123;</span><br> <span class="hljs-attr">&quot;buttonPermissionMap&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">&#123;</span><br>  <span class="hljs-attr">&quot;set&quot;</span><span class="hljs-punctuation">:</span>      <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;set:view&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;set:delete&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;set:add&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;set:edit&quot;</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-attr">&quot;model&quot;</span><span class="hljs-punctuation">:</span>    <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;model:delete&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;model:view&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;model:edit&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;model:add&quot;</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-attr">&quot;rules&quot;</span><span class="hljs-punctuation">:</span>    <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;rules:view&quot;</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-attr">&quot;workflow&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;workflow:view&quot;</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-attr">&quot;plan&quot;</span><span class="hljs-punctuation">:</span>     <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;plan:add&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;plan:edit&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;plan:delete&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;plan:view&quot;</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-attr">&quot;facility&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;facility:add&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;facility:view&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;facility:edit&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;facility:delete&quot;</span><span class="hljs-punctuation">]</span><br> <span class="hljs-punctuation">&#125;</span><span class="hljs-punctuation">,</span><br> <span class="hljs-attr">&quot;menuPermissionMap&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">&#123;</span><br>  <span class="hljs-attr">&quot;indicator&quot;</span><span class="hljs-punctuation">:</span>  <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;indicator:set&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;indicator:model&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;indicator:rules&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;indicator:workflow&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;indicator:system&quot;</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-attr">&quot;assess&quot;</span><span class="hljs-punctuation">:</span>     <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;assess:assessTask&quot;</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-attr">&quot;management&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;management:facility&quot;</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-attr">&quot;user&quot;</span><span class="hljs-punctuation">:</span>       <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;user:userManagement&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;user:permissionManagement&quot;</span><span class="hljs-punctuation">]</span><br> <span class="hljs-punctuation">&#125;</span><br><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure>

<p>模型要点：</p>
<ol>
<li><strong>权限码命名规范是 <code>模块:动作</code></strong>。<code>plan:edit</code> 一眼可读：plan 模块的编辑权。不带 <code>:</code> 的是模块级权限（如 <code>user</code>），控制模块入口的可见性。</li>
<li><strong>按钮权限和菜单权限是两张独立的 Map</strong>，而不是一棵树。后端数据库里它们是一棵权限树（第 6 节），下发时按用途拍平成两组。前端消费时互不干扰：按钮判断查 <code>btnObj</code>，菜单&#x2F;Tab 判断查 <code>menuObj</code>。</li>
<li><strong>分组结构是”模块 → 权限码数组”</strong>，判断时不需要知道权限码属于哪个模块——遍历所有模块找就是了。这带来一个好处：前端写 <code>v-auth=&quot;&#39;plan:edit&#39;&quot;</code> 时不用关心后端把它归在哪个 key 下，耦合度更低。</li>
</ol>
<p>为什么菜单和按钮要分开？因为它们的<strong>失效表现不同</strong>：菜单&#x2F;Tab 没权限应该不可见或不可进入，按钮没权限可以是”可见但禁用”。同一个权限模型，两种交互策略。</p>
<p><a id="sec3"></a></p>
<h2 id="3-判断核心：hasBtnAuth-hasAuth-双函数"><a href="#3-判断核心：hasBtnAuth-hasAuth-双函数" class="headerlink" title="3. 判断核心：hasBtnAuth &#x2F; hasAuth 双函数"></a>3. 判断核心：hasBtnAuth &#x2F; hasAuth 双函数</h2><p>两个纯函数是所有权限判断的唯一出口，都在 <code>src/directive/index.ts</code>：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> hasBtnAuth = (<span class="hljs-attr">permission</span>: <span class="hljs-built_in">string</span>): <span class="hljs-function"><span class="hljs-params">boolean</span> =&gt;</span> &#123;<br> <span class="hljs-keyword">const</span> btnStr = <span class="hljs-variable language_">localStorage</span>.<span class="hljs-title function_">getItem</span>(<span class="hljs-string">&#x27;btnObj&#x27;</span>);<br> <span class="hljs-keyword">if</span> (!btnStr) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br><br> <span class="hljs-keyword">try</span> &#123;<br>  <span class="hljs-keyword">const</span> btnObj = <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">parse</span>(btnStr);<br><br>  <span class="hljs-comment">// 情况1: 权限是扁平数组</span><br>  <span class="hljs-keyword">if</span> (<span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(btnObj)) &#123;<br>   <span class="hljs-keyword">return</span> btnObj.<span class="hljs-title function_">includes</span>(permission);<br>  &#125;<br><br>  <span class="hljs-comment">// 情况2: 权限是分组对象（当前格式）</span><br>  <span class="hljs-keyword">if</span> (<span class="hljs-keyword">typeof</span> btnObj === <span class="hljs-string">&#x27;object&#x27;</span> &amp;&amp; btnObj !== <span class="hljs-literal">null</span>) &#123;<br>   <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> moduleKey <span class="hljs-keyword">in</span> btnObj) &#123;<br>    <span class="hljs-keyword">const</span> permissions = btnObj[moduleKey];<br>    <span class="hljs-keyword">if</span> (<span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(permissions) &amp;&amp; permissions.<span class="hljs-title function_">includes</span>(permission)) &#123;<br>     <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>    &#125;<br>   &#125;<br>   <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>  &#125;<br>  <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br> &#125; <span class="hljs-keyword">catch</span> (e) &#123;<br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">error</span>(<span class="hljs-string">&#x27;❌ 解析 btnObj 失败，请检查 localStorage 格式:&#x27;</span>, e);<br>  <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br> &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<p><code>hasAuth</code>（查 menuObj）多一个分支：<strong>不带 <code>:</code> 的参数按”模块级权限”处理</strong>——只要 menuObj 里存在这个模块 key 就算有权限：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> hasAuth = (<span class="hljs-attr">permission</span>: <span class="hljs-built_in">string</span>): <span class="hljs-function"><span class="hljs-params">boolean</span> =&gt;</span> &#123;<br> <span class="hljs-keyword">const</span> menuStr = <span class="hljs-variable language_">localStorage</span>.<span class="hljs-title function_">getItem</span>(<span class="hljs-string">&#x27;menuObj&#x27;</span>);<br> <span class="hljs-keyword">if</span> (!menuStr) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br> <span class="hljs-keyword">const</span> menuObj = <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">parse</span>(menuStr);<br><br> <span class="hljs-comment">// 情况1: permission 是模块名（不包含 &#x27;:&#x27;），如 &#x27;user&#x27;</span><br> <span class="hljs-keyword">if</span> (!permission.<span class="hljs-title function_">includes</span>(<span class="hljs-string">&#x27;:&#x27;</span>)) &#123;<br>  <span class="hljs-keyword">return</span> <span class="hljs-title class_">Object</span>.<span class="hljs-property"><span class="hljs-keyword">prototype</span></span>.<span class="hljs-property">hasOwnProperty</span>.<span class="hljs-title function_">call</span>(menuObj, permission);<br> &#125;<br> <span class="hljs-comment">// 情况2: 细粒度权限，如 &#x27;indicator:set&#x27;，遍历所有模块匹配</span><br> <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> moduleKey <span class="hljs-keyword">in</span> menuObj) &#123;<br>  <span class="hljs-keyword">const</span> permissions = menuObj[moduleKey];<br>  <span class="hljs-keyword">if</span> (<span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(permissions) &amp;&amp; permissions.<span class="hljs-title function_">includes</span>(permission)) <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br> &#125;<br> <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>&#125;;<br></code></pre></td></tr></table></figure>

<p>三个值得吸收的细节：</p>
<ul>
<li><strong>兼容”扁平数组”和”分组对象”两种数据格式</strong>。这不是过度设计——后端接口结构演进过一轮（mock 文件里两种格式的注释都留着），兼容分支让前端在格式切换期不用发版。对权限这种横切所有页面的基础设施，<strong>消费端对数据格式的容忍度应该拉满</strong>。</li>
<li><strong>fail-closed：任何解析失败、数据缺失都返回 false</strong>。权限判断的默认值只能是”无权限”。<code>!btnStr return false</code>、<code>catch return false</code>，方向一致。</li>
<li><strong>try&#x2F;catch 包住 JSON.parse</strong>。localStorage 是用户可污染的存储，损坏的 JSON 不应该让整页指令抛错白屏。</li>
</ul>
<p><a id="sec4"></a></p>
<h2 id="4-双指令：v-auth-与-v-menuAuth"><a href="#4-双指令：v-auth-与-v-menuAuth" class="headerlink" title="4. 双指令：v-auth 与 v-menuAuth"></a>4. 双指令：v-auth 与 v-menuAuth</h2><p>指令在 main.ts 里一行注册：<code>authDirective(app)</code>。两个指令的实现几乎一样，差异只在判断函数：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/directive/index.ts（有删减）</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> <span class="hljs-title function_">authDirective</span> = (<span class="hljs-params"><span class="hljs-attr">app</span>: <span class="hljs-built_in">any</span></span>) =&gt; &#123;<br> <span class="hljs-comment">// 按钮级权限</span><br> app.<span class="hljs-title function_">directive</span>(<span class="hljs-string">&#x27;auth&#x27;</span>, &#123;<br>  <span class="hljs-title function_">mounted</span>(<span class="hljs-params"><span class="hljs-attr">el</span>: <span class="hljs-built_in">any</span>, <span class="hljs-attr">options</span>: <span class="hljs-built_in">any</span></span>) &#123;<br>   <span class="hljs-keyword">if</span> (!<span class="hljs-title function_">hasBtnAuth</span>(options.<span class="hljs-property">value</span>)) &#123;<br>    el.<span class="hljs-title function_">setAttribute</span>(<span class="hljs-string">&#x27;disabled&#x27;</span>, <span class="hljs-string">&#x27;disabled&#x27;</span>);<br>    el.<span class="hljs-property">style</span>.<span class="hljs-property">pointerEvents</span> = <span class="hljs-string">&#x27;none&#x27;</span>;   <span class="hljs-comment">// 防点击</span><br>    el.<span class="hljs-property">style</span>.<span class="hljs-property">opacity</span> = <span class="hljs-string">&#x27;0.6&#x27;</span>;          <span class="hljs-comment">// 视觉禁用</span><br>   &#125;<br>  &#125;<br> &#125;);<br><br> <span class="hljs-comment">// 菜单/Tab 级权限</span><br> app.<span class="hljs-title function_">directive</span>(<span class="hljs-string">&#x27;menuAuth&#x27;</span>, &#123;<br>  <span class="hljs-title function_">mounted</span>(<span class="hljs-params"><span class="hljs-attr">el</span>: <span class="hljs-built_in">any</span>, <span class="hljs-attr">options</span>: <span class="hljs-built_in">any</span></span>) &#123;<br>   <span class="hljs-keyword">const</span> &#123; <span class="hljs-attr">value</span>: permission &#125; = options;<br>   <span class="hljs-keyword">if</span> (<span class="hljs-keyword">typeof</span> permission !== <span class="hljs-string">&#x27;string&#x27;</span>) &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">warn</span>(<span class="hljs-string">&#x27;⚠️ v-menuAuth 必须传入字符串权限标识&#x27;</span>);<br>    <span class="hljs-keyword">return</span>;<br>   &#125;<br>   <span class="hljs-keyword">if</span> (!<span class="hljs-title function_">hasAuth</span>(permission)) &#123;<br>    el.<span class="hljs-title function_">setAttribute</span>(<span class="hljs-string">&#x27;disabled&#x27;</span>, <span class="hljs-string">&#x27;disabled&#x27;</span>);<br>    el.<span class="hljs-property">style</span>.<span class="hljs-property">pointerEvents</span> = <span class="hljs-string">&#x27;none&#x27;</span>;<br>    el.<span class="hljs-property">style</span>.<span class="hljs-property">opacity</span> = <span class="hljs-string">&#x27;0.6&#x27;</span>;<br>   &#125;<br>  &#125;<br> &#125;);<br>&#125;;<br></code></pre></td></tr></table></figure>

<p><strong>“禁用三件套”是这个方案的一个明确选择</strong>：<code>disabled</code> 属性 + <code>pointerEvents: none</code> + <code>opacity: 0.6</code>。源码注释里留着被放弃的另一个方案：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// if (!btnObj || !btnObj.includes(options.value)) &#123;</span><br><span class="hljs-comment">//  // 如果在用户信息 buttons 数组当中没有，就从 DOM 上干掉</span><br><span class="hljs-comment">//  el.parentNode.removeChild(el);</span><br><span class="hljs-comment">// &#125;</span><br></code></pre></td></tr></table></figure>

<p><strong>移除 DOM vs 禁用</strong>，两种策略的取舍：</p>
<table>
<thead>
<tr>
<th></th>
<th>移除 DOM（removeChild）</th>
<th>禁用三件套</th>
</tr>
</thead>
<tbody><tr>
<td>用户感知</td>
<td>“功能不存在”</td>
<td>“功能存在但无权用”</td>
</tr>
<tr>
<td>布局</td>
<td>兄弟元素位移，布局跳动</td>
<td>稳定</td>
</tr>
<tr>
<td>恢复</td>
<td>几乎不可逆（节点已丢）</td>
<td>改 style 即可</td>
</tr>
<tr>
<td>适用</td>
<td>导航型（不该知道的入口）</td>
<td>操作型（增删改按钮）</td>
</tr>
</tbody></table>
<p>这个项目最终统一用禁用，理由是后台场景里用户通常<strong>需要知道”有这个功能，但我没权限”</strong>——配上 tooltip 说明原因，比入口凭空消失的体验好；同时 DOM 稳定对自动化测试也更友好。代价是”藏起来的秘密入口”会暴露在 DOM 里——但回到第 0 节的立论：前端权限本来就不承担保密职责。</p>
<p>还有一个容易踩的坑值得点出：<strong>指令只在 <code>mounted</code> 执行一次</strong>。权限快照是登录后一次性写入 localStorage、会话内不变的，所以”一次判断”够用；但如果你的系统支持”不刷新页面热更新权限”，<code>mounted</code> 一次性的指令就不够了，需要用 <code>updated</code> 钩子或在权限变化时强制重渲染（比如给 router-view 加 key）。</p>
<p><a id="sec5"></a></p>
<h2 id="5-三层显隐实战：模块入口-→-Tab-→-按钮"><a href="#5-三层显隐实战：模块入口-→-Tab-→-按钮" class="headerlink" title="5. 三层显隐实战：模块入口 → Tab → 按钮"></a>5. 三层显隐实战：模块入口 → Tab → 按钮</h2><h3 id="5-1-第一层：顶部模块入口（v-menuAuth）"><a href="#5-1-第一层：顶部模块入口（v-menuAuth）" class="headerlink" title="5.1 第一层：顶部模块入口（v-menuAuth）"></a>5.1 第一层：顶部模块入口（v-menuAuth）</h3><p>顶部导航的每个模块入口挂指令，权限码就是模块名：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-comment">&lt;!-- src/pages/home/components/home-header/index.vue（有删减） --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;home-tittle-bg&quot;</span></span><br><span class="hljs-tag"> <span class="hljs-attr">:class</span>=<span class="hljs-string">&quot;[&#x27;cp&#x27;, isActive(&#x27;targetFacilityVisible&#x27;) ? &#x27;acitve&#x27; : &#x27;&#x27;]&quot;</span></span><br><span class="hljs-tag"> @<span class="hljs-attr">click.stop</span>=<span class="hljs-string">&quot;setHearderActiveModule($event, &#x27;targetFacilityVisible&#x27;)&quot;</span></span><br><span class="hljs-tag"> <span class="hljs-attr">v-menuAuth</span>=<span class="hljs-string">&quot;&#x27;management&#x27;&quot;</span>&gt;</span><br> 目标设施管理<br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;home-tittle-bg&quot;</span> <span class="hljs-attr">...</span> <span class="hljs-attr">v-menuAuth</span>=<span class="hljs-string">&quot;&#x27;indicator&#x27;&quot;</span>&gt;</span>指标体系管理<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><br><span class="hljs-comment">&lt;!-- 下拉菜单项则直接用 v-if + hasAuth --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">a-menu-item</span> <span class="hljs-attr">v-if</span>=<span class="hljs-string">&quot;hasAuth(&#x27;user&#x27;)&quot;</span> @<span class="hljs-attr">click.stop</span>=<span class="hljs-string">&quot;setHearderActiveModule($event, &#x27;userInfoManage&#x27;)&quot;</span>&gt;</span><br> 用户管理<br><span class="hljs-tag">&lt;/<span class="hljs-name">a-menu-item</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>同一个位置能看到两种写法并存：独立入口用指令（禁用置灰），下拉菜单项用 <code>v-if</code>（直接不渲染）。因为 Ant Design 的 <code>a-menu-item</code> 上叠 disabled 属性和行内样式会与其自身样式打架，<code>v-if</code> 反而干净——<strong>指令不是唯一答案，合适的容器用合适的方式</strong>。</p>
<h3 id="5-2-第二层：模块内-Tab（hasAuth-首个可见-Tab-自动选中）"><a href="#5-2-第二层：模块内-Tab（hasAuth-首个可见-Tab-自动选中）" class="headerlink" title="5.2 第二层：模块内 Tab（hasAuth + 首个可见 Tab 自动选中）"></a>5.2 第二层：模块内 Tab（hasAuth + 首个可见 Tab 自动选中）</h3><p>每个模块内部的 Tab 页签也按权限显隐。这里有个容易被忽略的体验问题：<strong>如果默认激活的 Tab 恰好没权限，页面会白屏</strong>。项目的解法是一段 watch，自动选中第一个有权限的 Tab：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// src/pages/IndexManagement/index.vue（有删减）</span><br><span class="hljs-keyword">const</span> activeKey = ref&lt;<span class="hljs-built_in">string</span>&gt;(<span class="hljs-string">&#x27;1&#x27;</span>); <span class="hljs-comment">// 1 指标集 2 指标管理 3 模型库 4 规则库 5 节点库</span><br><br><span class="hljs-keyword">const</span> <span class="hljs-variable constant_">ALL_TAB_KEYS</span> = [<span class="hljs-string">&#x27;1&#x27;</span>, <span class="hljs-string">&#x27;2&#x27;</span>, <span class="hljs-string">&#x27;3&#x27;</span>, <span class="hljs-string">&#x27;4&#x27;</span>, <span class="hljs-string">&#x27;5&#x27;</span>] <span class="hljs-keyword">as</span> <span class="hljs-keyword">const</span>;<br><br><span class="hljs-comment">// 判断每个 tab 是否可见（必须和模板中的 v-if 条件一致！）</span><br><span class="hljs-keyword">const</span> isTabVisible = (<span class="hljs-attr">key</span>: <span class="hljs-built_in">string</span>): <span class="hljs-function"><span class="hljs-params">boolean</span> =&gt;</span> &#123;<br> <span class="hljs-keyword">switch</span> (key) &#123;<br>  <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;1&#x27;</span>: <span class="hljs-keyword">return</span> <span class="hljs-title function_">hasAuth</span>(<span class="hljs-string">&#x27;indicator:set&#x27;</span>);<br>  <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;2&#x27;</span>: <span class="hljs-keyword">return</span> <span class="hljs-title function_">hasAuth</span>(<span class="hljs-string">&#x27;indicator:system&#x27;</span>);<br>  <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;3&#x27;</span>: <span class="hljs-keyword">return</span> <span class="hljs-title function_">hasAuth</span>(<span class="hljs-string">&#x27;indicator:model&#x27;</span>);<br>  <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;4&#x27;</span>: <span class="hljs-keyword">return</span> <span class="hljs-title function_">hasAuth</span>(<span class="hljs-string">&#x27;indicator:rules&#x27;</span>);<br>  <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;5&#x27;</span>: <span class="hljs-keyword">return</span> <span class="hljs-title function_">hasAuth</span>(<span class="hljs-string">&#x27;indicator:workflow&#x27;</span>);<br>  <span class="hljs-attr">default</span>: <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br> &#125;<br>&#125;;<br><br><span class="hljs-comment">// 🔁 监听权限变化（或页面初始化），自动设置 activeKey</span><br><span class="hljs-title function_">watch</span>(<br> <span class="hljs-function">() =&gt;</span> [<span class="hljs-title function_">hasAuth</span>(<span class="hljs-string">&#x27;indicator:set&#x27;</span>), <span class="hljs-title function_">hasAuth</span>(<span class="hljs-string">&#x27;indicator:system&#x27;</span>), <span class="hljs-title function_">hasAuth</span>(<span class="hljs-string">&#x27;indicator:model&#x27;</span>),<br>        <span class="hljs-title function_">hasAuth</span>(<span class="hljs-string">&#x27;indicator:rules&#x27;</span>), <span class="hljs-title function_">hasAuth</span>(<span class="hljs-string">&#x27;indicator:workflow&#x27;</span>)],<br> <span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-keyword">const</span> firstVisibleKey = <span class="hljs-variable constant_">ALL_TAB_KEYS</span>.<span class="hljs-title function_">find</span>(<span class="hljs-function"><span class="hljs-params">key</span> =&gt;</span> <span class="hljs-title function_">isTabVisible</span>(key));<br>  <span class="hljs-keyword">if</span> (firstVisibleKey &amp;&amp; !<span class="hljs-title function_">isTabVisible</span>(activeKey.<span class="hljs-property">value</span>)) &#123;<br>   activeKey.<span class="hljs-property">value</span> = firstVisibleKey;  <span class="hljs-comment">// 只在当前 key 不可见时重置，不打扰用户手动切换</span><br>  &#125;<br> &#125;,<br> &#123; <span class="hljs-attr">immediate</span>: <span class="hljs-literal">true</span> &#125;<br>);<br></code></pre></td></tr></table></figure>

<p>模板侧就是简单的 <code>v-if</code>：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">a-tabs</span> <span class="hljs-attr">v-model:activeKey</span>=<span class="hljs-string">&quot;activeKey&quot;</span> @<span class="hljs-attr">change</span>=<span class="hljs-string">&quot;handleChange&quot;</span>&gt;</span><br> <span class="hljs-tag">&lt;<span class="hljs-name">a-tab-pane</span> <span class="hljs-attr">key</span>=<span class="hljs-string">&quot;1&quot;</span> <span class="hljs-attr">tab</span>=<span class="hljs-string">&quot;指标集&quot;</span>   <span class="hljs-attr">v-if</span>=<span class="hljs-string">&quot;hasAuth(&#x27;indicator:set&#x27;)&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">indexSetList</span> <span class="hljs-attr">v-if</span>=<span class="hljs-string">&quot;activeKey == &#x27;1&#x27;&quot;</span> /&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">a-tab-pane</span>&gt;</span><br> <span class="hljs-tag">&lt;<span class="hljs-name">a-tab-pane</span> <span class="hljs-attr">key</span>=<span class="hljs-string">&quot;2&quot;</span> <span class="hljs-attr">tab</span>=<span class="hljs-string">&quot;指标管理&quot;</span> <span class="hljs-attr">v-if</span>=<span class="hljs-string">&quot;hasAuth(&#x27;indicator:system&#x27;)&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">indexManageTree</span> <span class="hljs-attr">v-if</span>=<span class="hljs-string">&quot;activeKey == &#x27;2&#x27;&quot;</span> /&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">a-tab-pane</span>&gt;</span><br> <span class="hljs-tag">&lt;<span class="hljs-name">a-tab-pane</span> <span class="hljs-attr">key</span>=<span class="hljs-string">&quot;3&quot;</span> <span class="hljs-attr">tab</span>=<span class="hljs-string">&quot;模型库&quot;</span>   <span class="hljs-attr">v-if</span>=<span class="hljs-string">&quot;hasAuth(&#x27;indicator:model&#x27;)&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">model-base</span> <span class="hljs-attr">v-if</span>=<span class="hljs-string">&quot;activeKey == &#x27;3&#x27;&quot;</span> /&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">a-tab-pane</span>&gt;</span><br> <span class="hljs-comment">&lt;!-- ... --&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">a-tabs</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>注意内层还套了一层 <code>v-if=&quot;activeKey == &#39;1&#39;&quot;</code>——Tab 切走时<strong>销毁</strong>内容组件而不是仅隐藏，配合 <code>onBeforeUnmount</code> 里 <code>store.$reset()</code>，保证每个 Tab 进入时都是干净状态。权限显隐和状态清理是两件事，但经常在同一处代码交织。</p>
<p>这段代码也有个维护性隐患值得一提：<code>isTabVisible</code> 里的权限判断<strong>必须手动和模板里的 v-if 保持一致</strong>（代码注释里自己加了感叹号提醒）。权限项增删时要改两处，漏一处就是”页面空白但 Tab 还在”。更稳的做法是把权限码收进 Tab 配置数组，<code>v-for</code> 渲染——单一数据源，天然同步。</p>
<h3 id="5-3-第三层：按钮（v-auth）"><a href="#5-3-第三层：按钮（v-auth）" class="headerlink" title="5.3 第三层：按钮（v-auth）"></a>5.3 第三层：按钮（v-auth）</h3><p>全项目约 60 处使用，模式高度统一：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">a-button</span> <span class="hljs-attr">v-auth</span>=<span class="hljs-string">&quot;&#x27;plan:add&#x27;&quot;</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;primary&quot;</span>&gt;</span>新增<span class="hljs-tag">&lt;/<span class="hljs-name">a-button</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">a-button</span> <span class="hljs-attr">v-auth</span>=<span class="hljs-string">&quot;&#x27;plan:edit&#x27;&quot;</span>&gt;</span>编辑<span class="hljs-tag">&lt;/<span class="hljs-name">a-button</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">a-button</span> <span class="hljs-attr">v-auth</span>=<span class="hljs-string">&quot;&#x27;plan:delete&#x27;&quot;</span> <span class="hljs-attr">danger</span>&gt;</span>删除<span class="hljs-tag">&lt;/<span class="hljs-name">a-button</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>卡片操作栏、列表行内操作、详情页按钮，一律 <code>v-auth=&quot;&#39;模块:动作&#39;&quot;</code>。没有任何页面在<strong>逻辑代码里</strong>判断按钮权限后再调接口——按钮禁用了，入口就断了，这是”指令管显隐、后端管校验”的干净分工。</p>
<p><a id="sec6"></a></p>
<h2 id="6-权限的配置面：角色管理与权限树"><a href="#6-权限的配置面：角色管理与权限树" class="headerlink" title="6. 权限的配置面：角色管理与权限树"></a>6. 权限的配置面：角色管理与权限树</h2><p>前面都是消费端，现在看权限从哪配出来。用户管理模块有两个 Tab：用户管理 + 权限管理（实际是角色管理），标准的 RBAC 三件套：用户—角色—权限。</p>
<p><strong>角色配置弹窗</strong>是配置面的核心：Element Plus 的 <code>el-tree</code> 复选树渲染整棵权限树，勾选结果随角色一起保存：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-comment">&lt;!-- src/pages/UserManage/components/permissionModel.vue（有删减） --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">ModuleItem</span> <span class="hljs-attr">title</span>=<span class="hljs-string">&quot;权限配置&quot;</span>&gt;</span><br> <span class="hljs-tag">&lt;<span class="hljs-name">el-tree</span></span><br><span class="hljs-tag">  <span class="hljs-attr">ref</span>=<span class="hljs-string">&quot;treeRef&quot;</span></span><br><span class="hljs-tag">  <span class="hljs-attr">:data</span>=<span class="hljs-string">&quot;treeData&quot;</span></span><br><span class="hljs-tag">  <span class="hljs-attr">show-checkbox</span></span><br><span class="hljs-tag">  <span class="hljs-attr">node-key</span>=<span class="hljs-string">&quot;permissionId&quot;</span></span><br><span class="hljs-tag">  <span class="hljs-attr">default-expand-all</span></span><br><span class="hljs-tag">  <span class="hljs-attr">:default-checked-keys</span>=<span class="hljs-string">&quot;formState.permissionIdList&quot;</span></span><br><span class="hljs-tag">  <span class="hljs-attr">:props</span>=<span class="hljs-string">&quot;treeProps&quot;</span></span><br><span class="hljs-tag"> /&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">ModuleItem</span>&gt;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript"><span class="hljs-comment">// 同文件（有删减）</span><br><span class="hljs-comment">// 权限树来自后端</span><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">getPermissionTree</span> = (<span class="hljs-params"></span>) =&gt; &#123;<br> <span class="hljs-title function_">sysPermissionTree</span>().<span class="hljs-title function_">then</span>(<span class="hljs-function"><span class="hljs-params">res</span> =&gt;</span> &#123;<br>  <span class="hljs-keyword">if</span> (res.<span class="hljs-property">code</span> == <span class="hljs-number">200</span> &amp;&amp; res.<span class="hljs-property">data</span>) &#123;<br>   <span class="hljs-title function_">handleTree</span>(res.<span class="hljs-property">data</span>);<br>   treeData.<span class="hljs-property">value</span> = res.<span class="hljs-property">data</span>;<br>  &#125;<br> &#125;);<br>&#125;;<br><br><span class="hljs-comment">// 提交：勾选的节点 id 集合就是角色的权限清单</span><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">handleOk</span> = (<span class="hljs-params"></span>) =&gt; &#123;<br> <span class="hljs-keyword">if</span> (treeRef.<span class="hljs-property">value</span>!.<span class="hljs-title function_">getCheckedKeys</span>(<span class="hljs-literal">false</span>).<span class="hljs-property">length</span> == <span class="hljs-number">0</span>) &#123;<br>  message.<span class="hljs-title function_">error</span>(<span class="hljs-string">&#x27;请选择权限&#x27;</span>);<br>  <span class="hljs-keyword">return</span>;<br> &#125;<br> roleInfoForm.<span class="hljs-property">value</span>.<span class="hljs-title function_">validate</span>().<span class="hljs-title function_">then</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-title function_">emit</span>(<span class="hljs-string">&#x27;sendRole&#x27;</span>, &#123;<br>   ...formState,<br>   <span class="hljs-attr">permissionIdList</span>: treeRef.<span class="hljs-property">value</span>!.<span class="hljs-title function_">getCheckedKeys</span>(<span class="hljs-literal">false</span>)<br>  &#125;);<br> &#125;);<br>&#125;;<br></code></pre></td></tr></table></figure>

<p>几个实现细节：</p>
<ul>
<li><strong>权限树是后端的单一事实源</strong>。树的结构（<code>permissionLevel</code> 1 级&#x2F;2 级）、命名全部来自 <code>GET /sysPermission/getPermissionTree</code>，前端只做展示和回显（编辑时 <code>getRoleDetail</code> 带回 <code>permissionIdList</code> 填进 <code>default-checked-keys</code>）。<strong>前端永远不该硬编码权限清单</strong>——项目里那份 <code>utils/roleCode.ts</code> 的权限码 mock 全项目零引用，只当设计文档存在，这是对的位置。</li>
<li><code>node-key=&quot;permissionId&quot;</code> + <code>getCheckedKeys(false)</code>：参数 <code>false</code> 表示”不含半选父节点”，只提交真正勾中的末级权限，父节点由后端推导，避免”勾了父带出全部子”的歧义数据。</li>
<li><strong>用户侧挂角色</strong>：用户新增&#x2F;编辑弹窗里是角色<strong>多选</strong>（<code>roleIdList</code> 必填）+ 部门单选。一个用户多个角色，权限并集——这是 RBAC 的标准语义。</li>
</ul>
<p>至此闭环完成：管理员在权限树勾出角色 → 用户绑定角色 → 用户登录后 <code>getPermissionByUser</code> 把并集权限拍平成两张 Map 下发 → 前端指令&#x2F;函数消费。</p>
<p><a id="sec7"></a></p>
<h2 id="7-数据权限：这一层前端做了什么、该做什么"><a href="#7-数据权限：这一层前端做了什么、该做什么" class="headerlink" title="7. 数据权限：这一层前端做了什么、该做什么"></a>7. 数据权限：这一层前端做了什么、该做什么</h2><p>用户的需求原话是”数据权限”。把这一节留到最后，是因为它需要先辨析概念——<strong>这个项目在前端没有任何数据权限实现，而这是一个合理的架构选择，不是缺失</strong>。</p>
<h3 id="7-1-先辨析：三个容易混淆的东西"><a href="#7-1-先辨析：三个容易混淆的东西" class="headerlink" title="7.1 先辨析：三个容易混淆的东西"></a>7.1 先辨析：三个容易混淆的东西</h3><p>调查过程中发现的三个”疑似数据权限”，逐一甄别：</p>
<table>
<thead>
<tr>
<th>现象</th>
<th>实质</th>
<th>是数据权限吗</th>
</tr>
</thead>
<tbody><tr>
<td>设施列表的地区级联筛选（国家&#x2F;省&#x2F;市&#x2F;县）</td>
<td>查询表单的业务条件，<strong>任何用户都能任意选</strong></td>
<td>❌ 是筛选，不是权限</td>
</tr>
<tr>
<td>用户资料里的部门字段（departmentId）</td>
<td>用户的一个<strong>属性</strong>，没有任何接口按它过滤数据</td>
<td>❌ 是属性，不是权限</td>
</tr>
<tr>
<td>内置用户 <code>record.sign</code> 置灰编辑&#x2F;删除按钮</td>
<td><strong>数据状态保护</strong>（内置数据不可改），与操作者身份无关</td>
<td>❌ 是保护，不是权限</td>
</tr>
<tr>
<td>全后端按 token 过滤返回的数据范围</td>
<td><strong>真·数据权限</strong>：同一接口，不同 token 拿到不同数据</td>
<td>✅</td>
</tr>
</tbody></table>
<p>一句话区分：<strong>筛选是用户主动选择看什么，权限是系统决定你能看什么</strong>。前者的控制权在用户手里，后者的控制权在身份里。全项目搜索 <code>dataScope</code>&#x2F;<code>数据范围</code> 为零结果——数据范围过滤没有前端参与的显式实现，完全由后端在接口层按 token 隐式完成。</p>
<h3 id="7-2-为什么”前端零实现”是对的"><a href="#7-2-为什么”前端零实现”是对的" class="headerlink" title="7.2 为什么”前端零实现”是对的"></a>7.2 为什么”前端零实现”是对的</h3><p>回到第 0 节的立论就能推出：数据权限如果做在前端（比如前端拿到数据后按部门过滤再展示），等于<strong>把过滤逻辑放在了攻击者可修改的环境里</strong>——改一行 JS 就能看到全量数据。数据权限的<strong>执行</strong>只有一个安全的位置：后端。前端连”参与”都不应该。</p>
<p>这个项目的形态是数据权限最薄的样子：登录拿到 token → 每个请求带着 token → 后端认 token 给数据。前端甚至不知道数据被过滤过。优点是简单、绝对安全；缺点也明显——<strong>前端无从给出”为什么这里没数据”的提示</strong>（用户看到空列表，分不清是没数据还是没权限），以及交互上无法做”只能看自己部门”之类的默认约束。</p>
<h3 id="7-3-如果要做得更好：前端的正确角色"><a href="#7-3-如果要做得更好：前端的正确角色" class="headerlink" title="7.3 如果要做得更好：前端的正确角色"></a>7.3 如果要做得更好：前端的正确角色</h3><p>主流的增强方案（比如若依的数据权限：按角色配”全部&#x2F;本部门&#x2F;本部门及以下&#x2F;仅本人”的 dataScope）里，前端的参与方式仍然是<strong>辅助而非执行</strong>：</p>
<ol>
<li><strong>后端把 dataScope 配置随权限接口下发</strong>（比如 <code>{ dataScope: &#39;DEPT_BELOW&#39;, deptId: 103 }</code>）；</li>
<li>前端用它做<strong>呈现的礼貌</strong>：查询表单的部门树默认定位到自己部门、禁选越权节点；列表的敏感列（成本、手机号）按权限隐藏；</li>
<li><strong>过滤本身永远在后端</strong>——前端那步只是让 UI 不提供”注定返回空结果”的选项。</li>
</ol>
<p>判断一个”前端数据权限”实现是否合格，就一条标准：<strong>把它整段删掉，系统安全性不下降</strong>。删掉后只是体验变差（能选到无权看的选项、发出注定失败的请求），数据一条都不该多出来。</p>
<p><a id="sec8"></a></p>
<h2 id="8-短板与改进清单"><a href="#8-短板与改进清单" class="headerlink" title="8. 短板与改进清单"></a>8. 短板与改进清单</h2><p>复盘不能只讲好的。按风险排序：</p>
<ol>
<li><p><strong>401 处理不彻底</strong>。响应拦截器里 <code>router.replace(&#39;/login&#39;)</code> 之后直接 reject，<strong>没有清掉 localStorage 里的 token&#x2F;btnObj&#x2F;menuObj</strong>。用户被踢回登录页后重新登录另一个账号，期间带着旧权限快照渲染了一屏；更糟的是如果 token 过期但快照还在，部分页面会出现”按钮都在、点击全 401”的灵异状态。改进：401 分支先 <code>localStorage.clear()</code> 再跳转，登录页 <code>onMounted</code> 顺手清一次。</p>
</li>
<li><p><strong>路由守卫缺登录态校验</strong>。未登录直达 <code>/#/home</code> 会看到一屏禁用按钮 + 一串 401 报错。改进只需三行：</p>
 <figure class="highlight typescript"><table><tr><td class="code"><pre><code class="hljs typescript">router.<span class="hljs-title function_">beforeEach</span>(<span class="hljs-function"><span class="hljs-params">to</span> =&gt;</span> &#123;<br> <span class="hljs-title function_">setTitle</span>(to);<br> <span class="hljs-keyword">if</span> (to.<span class="hljs-property">path</span> !== <span class="hljs-string">&#x27;/login&#x27;</span> &amp;&amp; !<span class="hljs-variable language_">localStorage</span>.<span class="hljs-title function_">getItem</span>(<span class="hljs-string">&#x27;token&#x27;</span>)) &#123;<br>  <span class="hljs-keyword">return</span> <span class="hljs-string">&#x27;/login&#x27;</span>;<br> &#125;<br>&#125;);<br></code></pre></td></tr></table></figure>
</li>
<li><p><strong>权限快照无时效机制</strong>。权限存在 localStorage，会话内不刷新。管理员中途改了某用户权限，该用户不重新登录就不生效（系统配置文件里那句”改了配置必须清空浏览器 localStorage”的注释是同一类问题）。改进：快照加时间戳，超时重拉；或后端权限变更时通过已有的 WebSocket 通道推一个 <code>permission-changed</code> 事件强制刷新。</p>
</li>
<li><p><strong>明文密码回显</strong>。用户编辑弹窗的详情回显里带着 <code>cleartextPassword</code> 字段并填进表单——详情接口不该返回明文密码，编辑时留空表示”不修改”即可。同类问题：登录表单预填了演示账号密码，上线前应移除。</p>
</li>
<li><p><strong>Tab 权限判断双写</strong>。<code>isTabVisible</code> 与模板 <code>v-if</code> 人工保持同步（5.2 节），应收敛为 Tab 配置数组单一数据源。</p>
</li>
</ol>
<p>这些短板有一个共同背景：它们全部<strong>不影响安全性</strong>（后端兜底闭环了），只影响体验和可维护性——这反过来印证了第 0 节立论的正确性：前端权限做错了顶多难用，不会不安全；后端权限做错了才是事故。</p>
<p><a id="sec9"></a></p>
<h2 id="9-写在最后"><a href="#9-写在最后" class="headerlink" title="9. 写在最后"></a>9. 写在最后</h2><table>
<thead>
<tr>
<th>需求</th>
<th>方案</th>
<th>关键文件</th>
</tr>
</thead>
<tbody><tr>
<td>登录凭证</td>
<td>login → token → localStorage，axios 注入 <code>Bearer</code></td>
<td><code>store/user.ts</code>、<code>service/service.ts</code></td>
</tr>
<tr>
<td>权限下发</td>
<td><code>getPermissionByUser</code> 靠 token 识别，两张 Map 拍平下发</td>
<td><code>api/userApi.ts</code></td>
</tr>
<tr>
<td>权限存储</td>
<td>localStorage 快照（指令在 setup 外同步消费）</td>
<td><code>store/user.ts</code></td>
</tr>
<tr>
<td>权限模型</td>
<td><code>模块:动作</code> 权限码，按钮&#x2F;菜单两张独立 Map</td>
<td><code>utils/roleCode.ts</code>（文档）</td>
</tr>
<tr>
<td>按钮显隐</td>
<td><code>v-auth</code> 指令 + 禁用三件套</td>
<td><code>directive/index.ts</code></td>
</tr>
<tr>
<td>模块入口</td>
<td><code>v-menuAuth</code> 指令 &#x2F; <code>hasAuth</code> + v-if</td>
<td><code>home-header/index.vue</code></td>
</tr>
<tr>
<td>Tab 显隐</td>
<td><code>hasAuth</code> + v-if + 首个可见 Tab 自动选中</td>
<td><code>IndexManagement/index.vue</code></td>
</tr>
<tr>
<td>权限配置</td>
<td>角色绑定权限树（el-tree 勾选 → permissionIdList）</td>
<td><code>UserManage/components/permissionModel.vue</code></td>
</tr>
<tr>
<td>数据权限</td>
<td>前端零实现，后端按 token 过滤（正确位置）</td>
<td>—</td>
</tr>
</tbody></table>
<p><strong>一句话记住</strong>：前端权限是一套”体验协议”——用 <code>模块:动作</code> 的权限码约定语言，用指令和函数做显隐，用 localStorage 存快照；而数据权限的执行只有一个正确位置，是后端。前端在这一层最好的动作是”呈现的礼貌”，不是”过滤的执行”。</p>
<h3 id="文件地图"><a href="#文件地图" class="headerlink" title="文件地图"></a>文件地图</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">infrastructure-data-plus/src/<br>├── main.ts                           # authDirective(app) 注册双指令<br>├── router/index.ts                   # 路由守卫（仅 setTitle，无鉴权）<br>├── directive/index.ts                # ★ 权限核心：hasBtnAuth / hasAuth / v-auth / v-menuAuth<br>├── store/user.ts                     # 登录链路：token + 权限快照落 localStorage<br>├── service/service.ts                # axios 拦截器：Bearer 注入 + 401/601 踢回登录<br>├── api/userApi.ts                    # 登录/用户/角色/权限树全部接口<br>├── utils/roleCode.ts                 # 权限码清单 mock（零引用，当设计文档）<br>└── pages/<br>    ├── login/index.vue               # 登录表单<br>    ├── home/components/home-header/  # 模块入口 v-menuAuth / 菜单项 hasAuth<br>    ├── IndexManagement/index.vue     # Tab 权限 + 首个可见 Tab 自动选中<br>    ├── AttackMeans/index.vue         # 同款 Tab 权限方案<br>    ├── TargetFacilityManagement/     # v-auth 按钮权限（facility:*）<br>    └── UserManage/<br>        ├── index.vue                 # 用户管理 + 权限管理双 Tab<br>        ├── components/userList.vue       # 用户列表（角色筛选/启停）<br>        ├── components/userInfoModel.vue  # 用户新增编辑（角色多选 + 部门）<br>        ├── components/permissionList.vue # 角色列表<br>        └── components/permissionModel.vue # ★ 角色权限树配置（el-tree）<br></code></pre></td></tr></table></figure>

<h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><ul>
<li><a href="https://panjiachen.github.io/vue-element-admin-site/zh/guide/essentials/permission.html">vue-element-admin 权限方案（动态路由&#x2F;指令的先驱实践）</a></li>
<li><a href="https://cn.vuejs.org/guide/reusability/custom-directives.html">Vue 3 官方文档 - 自定义指令</a></li>
<li><a href="https://pinia.vuejs.org/zh/">Pinia 官方文档</a></li>
<li><a href="https://en.wikipedia.org/wiki/Role-based_access_control">RBAC 模型：NIST&#x2F;维基百科</a></li>
</ul>
]]></content>
      <categories>
        <category>Vue 进阶</category>
      </categories>
      <tags>
        <tag>Vue3</tag>
        <tag>axios</tag>
        <tag>RBAC</tag>
        <tag>Pinia</tag>
      </tags>
  </entry>
  <entry>
    <title>封装一个 Element Plus 通用表格组件：配置驱动、插槽透传与单选模拟</title>
    <url>/2026/09/09/element-plus-table-component/</url>
    <content><![CDATA[<p>写过中后台的同学都有体感：列表页的模板代码一半是 <code>&lt;el-table-column&gt;</code> 的复制粘贴，另一半是 <code>&lt;el-pagination&gt;</code> 的事件处理。一个页面十几列，<code>loading</code>、空状态、序号列、多选框、操作列插槽……每个页面把这些再写一遍，改一个列宽要去模板里翻半天。</p>
<p>之前项目里沉淀了一个基于 Element Plus 的通用表格组件，最近又在新项目里用一个「后端下发表头」的动态列场景喂了它一波，正好把完整代码和设计思路整理出来。组件不大，两百多行，但里面有几个值得抠的设计点：<strong>配置驱动、插槽透传、单选模拟、跨页多选、分页状态归属</strong>。</p>
<span id="more"></span>

<h2 id="一、API-设计：三个-prop、三个事件、一个-expose"><a href="#一、API-设计：三个-prop、三个事件、一个-expose" class="headerlink" title="一、API 设计：三个 prop、三个事件、一个 expose"></a>一、API 设计：三个 prop、三个事件、一个 expose</h2><p>先看组件对外的契约，封装一个组件最重要的就是先把 API 定稳：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">props<br>  data     : any[]          行数据<br>  header   : HeaderItem[]   列配置（JSON 声明，见下表）<br>  config   : TableConfig    表格行为配置（loading/total/多选/序号…）<br><br>emits<br>  pageChange      &#123; pageNum, pageSize &#125;   翻页或改页大小（内置分页）<br>  sortChange      &#123; prop, order &#125;         排序（已归一化为 asc/desc/&#x27;&#x27;）<br>  selectionChange rows                    多选/单选变化<br><br>expose<br>  pageReset()     分页重置回第一页并触发 pageChange<br>  tableRefs       el-table 实例引用（父组件需要时能直接调原生方法）<br></code></pre></td></tr></table></figure>

<p><code>header</code> 数组每一项的字段：</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>作用</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td><code>key</code> &#x2F; <code>title</code></td>
<td>字段名 &#x2F; 列标题</td>
<td>对应 el-table-column 的 <code>prop</code> &#x2F; <code>label</code></td>
</tr>
<tr>
<td><code>isCheck</code></td>
<td>是否显示该列</td>
<td>computed 里过滤掉 <code>false</code> 的列</td>
</tr>
<tr>
<td><code>colWidth</code> &#x2F; <code>minWidth</code></td>
<td>固定宽 &#x2F; 最小宽</td>
<td>二选一</td>
</tr>
<tr>
<td><code>fixed</code></td>
<td>固定列</td>
<td><code>&#39;left&#39;</code> &#x2F; <code>&#39;right&#39;</code>，操作列常用</td>
</tr>
<tr>
<td><code>sort</code></td>
<td>是否可排序</td>
<td>透传 <code>sortable</code></td>
</tr>
<tr>
<td><code>slot</code></td>
<td>自定义单元格插槽名</td>
<td>命中则该列渲染具名插槽</td>
</tr>
<tr>
<td><code>headerSlot</code></td>
<td>自定义表头插槽名</td>
<td>同上，渲染在 <code>#header</code></td>
</tr>
<tr>
<td><code>formatter</code></td>
<td>格式化函数</td>
<td>直接透传 el-table-column 的 <code>formatter</code></td>
</tr>
</tbody></table>
<p><code>config</code> 的字段：<code>loading</code>、<code>total</code>、<code>rowKey</code>、<code>isSelection</code>（多选列）、<code>isSerialNo</code>（序号列）、<code>isRadio</code>（单选模式）、<code>isBorder</code>、<code>noPagination</code>（关掉分页）。</p>
<p>这个设计的目标是：<strong>常规页面零模板代码</strong>——列声明在 JS 里，数据驱动渲染；需要自定义的格子通过插槽escape hatch出去，不会把组件做成大杂烩。</p>
<h2 id="二、模板：配置驱动的列渲染-插槽透传"><a href="#二、模板：配置驱动的列渲染-插槽透传" class="headerlink" title="二、模板：配置驱动的列渲染 + 插槽透传"></a>二、模板：配置驱动的列渲染 + 插槽透传</h2><p>完整模板（Vue 3 <code>&lt;script setup&gt;</code> + Element Plus）：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">template</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;table-container&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">el-table</span></span><br><span class="hljs-tag">      <span class="hljs-attr">:data</span>=<span class="hljs-string">&quot;data&quot;</span></span><br><span class="hljs-tag">      <span class="hljs-attr">:border</span>=<span class="hljs-string">&quot;setBorder&quot;</span></span><br><span class="hljs-tag">      <span class="hljs-attr">v-bind</span>=<span class="hljs-string">&quot;$attrs&quot;</span></span><br><span class="hljs-tag">      <span class="hljs-attr">:row-key</span>=<span class="hljs-string">&quot;config?.rowKey || &#x27;id&#x27;&quot;</span></span><br><span class="hljs-tag">      <span class="hljs-attr">stripe</span></span><br><span class="hljs-tag">      <span class="hljs-attr">style</span>=<span class="hljs-string">&quot;width: 100%&quot;</span></span><br><span class="hljs-tag">      <span class="hljs-attr">v-loading</span>=<span class="hljs-string">&quot;config.loading&quot;</span></span><br><span class="hljs-tag">      @<span class="hljs-attr">selection-change</span>=<span class="hljs-string">&quot;onSelectionChange&quot;</span></span><br><span class="hljs-tag">      @<span class="hljs-attr">sort-change</span>=<span class="hljs-string">&quot;sortChange&quot;</span></span><br><span class="hljs-tag">      <span class="hljs-attr">ref</span>=<span class="hljs-string">&quot;tableRefs&quot;</span></span><br><span class="hljs-tag">      <span class="hljs-attr">:class</span>=<span class="hljs-string">&quot;[config?.isRadio ? &#x27;show-radio&#x27; : &#x27;&#x27;]&quot;</span></span><br><span class="hljs-tag">    &gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">el-table-column</span></span><br><span class="hljs-tag">        <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;selection&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:reserve-selection</span>=<span class="hljs-string">&quot;true&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">align</span>=<span class="hljs-string">&quot;center&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">width</span>=<span class="hljs-string">&quot;40&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">v-if</span>=<span class="hljs-string">&quot;config.isSelection&quot;</span></span><br><span class="hljs-tag">      /&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">el-table-column</span></span><br><span class="hljs-tag">        <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;index&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">label</span>=<span class="hljs-string">&quot;序号&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">align</span>=<span class="hljs-string">&quot;center&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">width</span>=<span class="hljs-string">&quot;60&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">v-if</span>=<span class="hljs-string">&quot;config.isSerialNo&quot;</span></span><br><span class="hljs-tag">      /&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">el-table-column</span></span><br><span class="hljs-tag">        <span class="hljs-attr">v-for</span>=<span class="hljs-string">&quot;(item, index) in setHeader&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:key</span>=<span class="hljs-string">&quot;index&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:show-overflow-tooltip</span>=<span class="hljs-string">&quot;item.type === &#x27;image&#x27; ? false : true&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:prop</span>=<span class="hljs-string">&quot;item.key&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:width</span>=<span class="hljs-string">&quot;item.colWidth&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:min-width</span>=<span class="hljs-string">&quot;item.minWidth&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:label</span>=<span class="hljs-string">&quot;item.title&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:fixed</span>=<span class="hljs-string">&quot;item.fixed&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:sortable</span>=<span class="hljs-string">&quot;item.sort&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:formatter</span>=<span class="hljs-string">&quot;item.formatter&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">align</span>=<span class="hljs-string">&quot;center&quot;</span></span><br><span class="hljs-tag">      &gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">template</span> <span class="hljs-attr">v-if</span>=<span class="hljs-string">&quot;item.headerSlot&quot;</span> #<span class="hljs-attr">header</span>&gt;</span><br>          <span class="hljs-tag">&lt;<span class="hljs-name">slot</span> <span class="hljs-attr">:name</span>=<span class="hljs-string">&quot;item.headerSlot&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">slot</span>&gt;</span><br>        <span class="hljs-tag">&lt;/<span class="hljs-name">template</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">template</span> <span class="hljs-attr">v-if</span>=<span class="hljs-string">&quot;item.slot&quot;</span> <span class="hljs-attr">v-slot</span>=<span class="hljs-string">&quot;scope&quot;</span>&gt;</span><br>          <span class="hljs-tag">&lt;<span class="hljs-name">slot</span> <span class="hljs-attr">:name</span>=<span class="hljs-string">&quot;item.slot&quot;</span> <span class="hljs-attr">v-bind</span>=<span class="hljs-string">&quot;scope&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">slot</span>&gt;</span><br>        <span class="hljs-tag">&lt;/<span class="hljs-name">template</span>&gt;</span><br>      <span class="hljs-tag">&lt;/<span class="hljs-name">el-table-column</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">template</span> #<span class="hljs-attr">empty</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">el-empty</span> <span class="hljs-attr">description</span>=<span class="hljs-string">&quot;暂无数据&quot;</span> /&gt;</span><br>      <span class="hljs-tag">&lt;/<span class="hljs-name">template</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">el-table</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;table-footer mt15&quot;</span> <span class="hljs-attr">v-if</span>=<span class="hljs-string">&quot;!config.noPagination&quot;</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">el-pagination</span></span><br><span class="hljs-tag">        <span class="hljs-attr">v-model:current-page</span>=<span class="hljs-string">&quot;state.page.pageNum&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">v-model:page-size</span>=<span class="hljs-string">&quot;state.page.pageSize&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:pager-count</span>=<span class="hljs-string">&quot;5&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:page-sizes</span>=<span class="hljs-string">&quot;[10, 20, 30]&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">:total</span>=<span class="hljs-string">&quot;config.total&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">layout</span>=<span class="hljs-string">&quot;total, sizes, prev, pager, next, jumper&quot;</span></span><br><span class="hljs-tag">        <span class="hljs-attr">background</span></span><br><span class="hljs-tag">        @<span class="hljs-attr">size-change</span>=<span class="hljs-string">&quot;onHandleSizeChange&quot;</span></span><br><span class="hljs-tag">        @<span class="hljs-attr">current-change</span>=<span class="hljs-string">&quot;onHandleCurrentChange&quot;</span></span><br><span class="hljs-tag">      &gt;</span><br>      <span class="hljs-tag">&lt;/<span class="hljs-name">el-pagination</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">template</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>四个值得停下来的点：</p>
<p><strong>1. <code>v-bind=&quot;$attrs&quot;</code> 保留原生能力。</strong> 封装最容易翻车的是「包了一层，原生 API 全丢了」——父组件想设 <code>height</code>、想监听 <code>row-click</code> 怎么办？<code>$attrs</code> 一行透传，el-table 的所有 props 和事件照常可用。组件不是墙，是门。</p>
<p><strong>2. 多选列的 <code>:reserve-selection=&quot;true&quot;</code>。</strong> 默认情况下翻页会丢失已勾选的行——el-table 的多选是「当前页多选」。开启 reserve-selection 后配合 <code>row-key</code>，跨页勾选会累积保留。这是多选场景最容易漏的配置。</p>
<p><strong>3. 插槽透传的动态具名插槽。</strong> 列配置里声明 <code>slot: &#39;action&#39;</code>，模板里 <code>&lt;slot :name=&quot;item.slot&quot; v-bind=&quot;scope&quot;&gt;</code> 把作用域插槽转发出去——父组件用普通的 <code>&lt;template #action=&quot;scope&quot;&gt;</code> 就能自定义任意格子。<strong>配置声明「哪列要自定义」，插槽决定「怎么自定义」</strong>，两层的关注点是分开的。<code>v-bind=&quot;scope&quot;</code> 把 <code>{ row, column, $index }</code> 原样带出去，父组件拿到的和直接写 el-table-column 一样。</p>
<p><strong>4. 空状态统一。</strong> <code>&lt;template #empty&gt;</code> 里放 <code>el-empty</code>，所有页面「暂无数据」的视觉一致，不用每个页面各配一次。</p>
<h2 id="三、脚本：单选模拟、排序归一化、分页状态归属"><a href="#三、脚本：单选模拟、排序归一化、分页状态归属" class="headerlink" title="三、脚本：单选模拟、排序归一化、分页状态归属"></a>三、脚本：单选模拟、排序归一化、分页状态归属</h2><figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts">&lt;script setup lang=<span class="hljs-string">&quot;ts&quot;</span>&gt;<br><span class="hljs-keyword">import</span> &#123; reactive, computed, nextTick, ref, watch &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;vue&#x27;</span><br><br><span class="hljs-keyword">declare</span> <span class="hljs-keyword">type</span> <span class="hljs-title class_">EmptyObjectType</span>&lt;T = <span class="hljs-built_in">any</span>&gt; = &#123; [<span class="hljs-attr">key</span>: <span class="hljs-built_in">string</span>]: T &#125;<br><span class="hljs-keyword">declare</span> <span class="hljs-keyword">type</span> <span class="hljs-title class_">RefType</span>&lt;T = <span class="hljs-built_in">any</span>&gt; = T | <span class="hljs-literal">null</span><br><br><span class="hljs-keyword">const</span> props = <span class="hljs-title function_">defineProps</span>(&#123;<br>  <span class="hljs-attr">data</span>:   &#123; <span class="hljs-attr">type</span>: <span class="hljs-title class_">Array</span>&lt;<span class="hljs-title class_">EmptyObjectType</span>&gt;, <span class="hljs-attr">default</span>: <span class="hljs-function">() =&gt;</span> [] &#125;,<br>  <span class="hljs-attr">header</span>: &#123; <span class="hljs-attr">type</span>: <span class="hljs-title class_">Array</span>&lt;<span class="hljs-title class_">EmptyObjectType</span>&gt;, <span class="hljs-attr">default</span>: <span class="hljs-function">() =&gt;</span> [] &#125;,<br>  <span class="hljs-attr">config</span>: &#123; <span class="hljs-attr">type</span>: <span class="hljs-title class_">Object</span>, <span class="hljs-attr">default</span>: <span class="hljs-function">() =&gt;</span> &#123;&#125; &#125;,<br>  <span class="hljs-attr">printName</span>: &#123; <span class="hljs-attr">type</span>: <span class="hljs-title class_">String</span>, <span class="hljs-attr">default</span>: <span class="hljs-function">() =&gt;</span> <span class="hljs-string">&#x27;&#x27;</span> &#125;,<br>&#125;)<br><br><span class="hljs-keyword">const</span> emit = <span class="hljs-title function_">defineEmits</span>([<span class="hljs-string">&#x27;pageChange&#x27;</span>, <span class="hljs-string">&#x27;sortChange&#x27;</span>, <span class="hljs-string">&#x27;selectionChange&#x27;</span>])<br><br><span class="hljs-keyword">const</span> tableRefs = ref&lt;<span class="hljs-title class_">RefType</span>&gt;(<span class="hljs-literal">null</span>)<br><span class="hljs-keyword">const</span> state = <span class="hljs-title function_">reactive</span>(&#123;<br>  <span class="hljs-attr">page</span>: &#123; <span class="hljs-attr">pageNum</span>: <span class="hljs-number">1</span>, <span class="hljs-attr">pageSize</span>: <span class="hljs-number">10</span> &#125;,<br>  <span class="hljs-attr">selectlist</span>: [] <span class="hljs-keyword">as</span> <span class="hljs-title class_">EmptyObjectType</span>[],<br>&#125;)<br><br><span class="hljs-comment">// 外部把 clearSelection 置 true → 清空勾选（布尔脉冲）</span><br><span class="hljs-title function_">watch</span>(<br>  <span class="hljs-function">() =&gt;</span> props.<span class="hljs-property">config</span>?.<span class="hljs-property">clearSelection</span>,<br>  <span class="hljs-function">(<span class="hljs-params">val</span>) =&gt;</span> &#123;<br>    <span class="hljs-keyword">if</span> (val === <span class="hljs-literal">true</span>) &#123;<br>      <span class="hljs-title function_">nextTick</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>        tableRefs.<span class="hljs-property">value</span>?.<span class="hljs-title function_">clearSelection</span>()<br>      &#125;)<br>    &#125;<br>  &#125;,<br>)<br><br><span class="hljs-comment">// 排序：把 el-table 的 descending/ascending 归一化成后端约定的 desc/asc</span><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">sortChange</span> = (<span class="hljs-params"><span class="hljs-attr">data</span>: &#123; column: <span class="hljs-built_in">any</span>; prop: <span class="hljs-built_in">string</span>; order: <span class="hljs-built_in">any</span> &#125;</span>) =&gt; &#123;<br>  <span class="hljs-keyword">switch</span> (data?.<span class="hljs-property">order</span>) &#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;descending&#x27;</span>: data.<span class="hljs-property">order</span> = <span class="hljs-string">&#x27;desc&#x27;</span>; <span class="hljs-keyword">break</span><br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;ascending&#x27;</span>:  data.<span class="hljs-property">order</span> = <span class="hljs-string">&#x27;asc&#x27;</span>;  <span class="hljs-keyword">break</span><br>    <span class="hljs-attr">default</span>:           data.<span class="hljs-property">order</span> = <span class="hljs-string">&#x27;&#x27;</span><br>  &#125;<br>  <span class="hljs-title function_">emit</span>(<span class="hljs-string">&#x27;sortChange&#x27;</span>, data)<br>&#125;<br><br><span class="hljs-keyword">const</span> setBorder = <span class="hljs-title function_">computed</span>(<span class="hljs-function">() =&gt;</span> !!props.<span class="hljs-property">config</span>.<span class="hljs-property">isBorder</span>)<br><span class="hljs-keyword">const</span> setHeader = <span class="hljs-title function_">computed</span>(<span class="hljs-function">() =&gt;</span> props.<span class="hljs-property">header</span>.<span class="hljs-title function_">filter</span>(<span class="hljs-function">(<span class="hljs-params">v</span>) =&gt;</span> v.<span class="hljs-property">isCheck</span>))<br><br><span class="hljs-keyword">const</span> cur = <span class="hljs-title function_">ref</span>(&#123;&#125;)<br><span class="hljs-comment">// 多选回调 / 单选模式：用多选模拟</span><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">onSelectionChange</span> = (<span class="hljs-params"><span class="hljs-attr">val</span>: <span class="hljs-title class_">EmptyObjectType</span>[]</span>) =&gt; &#123;<br>  <span class="hljs-keyword">if</span> (props.<span class="hljs-property">config</span>?.<span class="hljs-property">isRadio</span>) &#123;<br>    cur.<span class="hljs-property">value</span> = val[val.<span class="hljs-property">length</span> - <span class="hljs-number">1</span>]<br>    val.<span class="hljs-property">length</span> &gt; <span class="hljs-number">1</span> &amp;&amp; tableRefs.<span class="hljs-property">value</span>.<span class="hljs-title function_">toggleRowSelection</span>(val[<span class="hljs-number">0</span>])  <span class="hljs-comment">// 取消上一行</span><br>    state.<span class="hljs-property">selectlist</span> = val.<span class="hljs-property">length</span> &gt; <span class="hljs-number">1</span> ? [val[<span class="hljs-number">1</span>]] : val<br>    <span class="hljs-title function_">emit</span>(<span class="hljs-string">&#x27;selectionChange&#x27;</span>, state.<span class="hljs-property">selectlist</span>)<br>  &#125; <span class="hljs-keyword">else</span> &#123;<br>    state.<span class="hljs-property">selectlist</span> = val<br>    <span class="hljs-title function_">emit</span>(<span class="hljs-string">&#x27;selectionChange&#x27;</span>, val)<br>  &#125;<br>&#125;<br><br><span class="hljs-comment">// 分页</span><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">onHandleSizeChange</span> = (<span class="hljs-params"><span class="hljs-attr">val</span>: <span class="hljs-built_in">number</span></span>) =&gt; &#123;<br>  state.<span class="hljs-property">page</span>.<span class="hljs-property">pageSize</span> = val<br>  <span class="hljs-title function_">emit</span>(<span class="hljs-string">&#x27;pageChange&#x27;</span>, state.<span class="hljs-property">page</span>)<br>&#125;<br><span class="hljs-keyword">const</span> <span class="hljs-title function_">onHandleCurrentChange</span> = (<span class="hljs-params"><span class="hljs-attr">val</span>: <span class="hljs-built_in">number</span></span>) =&gt; &#123;<br>  state.<span class="hljs-property">page</span>.<span class="hljs-property">pageNum</span> = val<br>  <span class="hljs-title function_">emit</span>(<span class="hljs-string">&#x27;pageChange&#x27;</span>, state.<span class="hljs-property">page</span>)<br>&#125;<br><span class="hljs-comment">// 搜索时：分页还原成第一页</span><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">pageReset</span> = (<span class="hljs-params"></span>) =&gt; &#123;<br>  state.<span class="hljs-property">page</span>.<span class="hljs-property">pageNum</span> = <span class="hljs-number">1</span><br>  state.<span class="hljs-property">page</span>.<span class="hljs-property">pageSize</span> = <span class="hljs-number">10</span><br>  <span class="hljs-title function_">emit</span>(<span class="hljs-string">&#x27;pageChange&#x27;</span>, state.<span class="hljs-property">page</span>)<br>&#125;<br><br><span class="hljs-title function_">defineExpose</span>(&#123; pageReset, tableRefs &#125;)<br>&lt;/script&gt;<br></code></pre></td></tr></table></figure>

<h3 id="单选模拟：Element-Plus-没有-radio-列"><a href="#单选模拟：Element-Plus-没有-radio-列" class="headerlink" title="单选模拟：Element Plus 没有 radio 列"></a>单选模拟：Element Plus 没有 radio 列</h3><p>Element Plus（截至 2.x）没有内置单选列。社区通用解法就是<strong>用多选列模拟</strong>，配合一段 CSS 把表头的全选框藏掉：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">if</span> (props.<span class="hljs-property">config</span>?.<span class="hljs-property">isRadio</span>) &#123;<br>  <span class="hljs-comment">// 新选中的行永远是 val 的最后一项</span><br>  cur.<span class="hljs-property">value</span> = val[val.<span class="hljs-property">length</span> - <span class="hljs-number">1</span>]<br>  <span class="hljs-comment">// 勾选了第二行时，把上一行的勾选状态取消</span><br>  val.<span class="hljs-property">length</span> &gt; <span class="hljs-number">1</span> &amp;&amp; tableRefs.<span class="hljs-property">value</span>.<span class="hljs-title function_">toggleRowSelection</span>(val[<span class="hljs-number">0</span>])<br>  state.<span class="hljs-property">selectlist</span> = val.<span class="hljs-property">length</span> &gt; <span class="hljs-number">1</span> ? [val[<span class="hljs-number">1</span>]] : val<br>&#125;<br></code></pre></td></tr></table></figure>

<figure class="highlight scss"><table><tr><td class="code"><pre><code class="hljs scss"><span class="hljs-selector-class">.show-radio</span> &#123;<br>  <span class="hljs-comment">// 单选模式下隐藏表头的全选框</span><br>  :<span class="hljs-built_in">deep</span>(.el-table__header .el-checkbox) &#123;<br>    <span class="hljs-attribute">display</span>: none;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这个 hack 的巧处在 <code>selection-change</code> 回调的时序：用户每次点击一行，新勾选的行<strong>追加在数组末尾</strong>，于是「数组长度大于 1」意味着「切换了选择」，把 <code>val[0]</code>（旧行）取消勾选即可。看起来别扭，但它完全建立在 el-table 的公开行为上，比自己造一套 radio 列（要处理高亮、行点击、数据回显）省得多。</p>
<h3 id="排序归一化：边界翻译收在组件里"><a href="#排序归一化：边界翻译收在组件里" class="headerlink" title="排序归一化：边界翻译收在组件里"></a>排序归一化：边界翻译收在组件里</h3><p>el-table 的 <code>sort-change</code> 事件给的是 <code>descending</code> &#x2F; <code>ascending</code>，而绝大多数后端接口要的是 <code>desc</code> &#x2F; <code>asc</code>。这种「两个世界的方言差异」就该由封装层翻译——组件的使用者拿到的永远是能直接拼进请求参数的值，每个页面不用再写一遍 switch。</p>
<h3 id="分页状态归属：一个反直觉的取舍"><a href="#分页状态归属：一个反直觉的取舍" class="headerlink" title="分页状态归属：一个反直觉的取舍"></a>分页状态归属：一个反直觉的取舍</h3><p>注意分页的 <code>pageNum/pageSize</code> 放在<strong>组件内部的 state</strong> 里，而不是 props。这意味着：</p>
<ul>
<li>父组件不需要维护一份分页状态，翻页只管接 <code>pageChange</code> 事件、带着新页码发请求；</li>
<li>但父组件<strong>不能</strong>直接改页码——搜索后想回第一页，得调 <code>defineExpose</code> 出去的 <code>pageReset()</code>。</li>
</ul>
<p>这是「受控&#x2F;非受控」的经典权衡。选择非受控是因为列表页的真实数据流是单向的：翻页事件 → 改请求参数 → 拉数据 → 回填 <code>data</code>&#x2F;<code>total</code>。分页状态留在组件内，父组件的 <code>param.pageNum</code> 只作为<strong>请求参数的快照</strong>存在，两边不会打架。代价是像「删除最后一页最后一条后自动前翻一页」这种场景，父组件只能间接操作——这是我会记在下次改进清单上的一条。</p>
<p><code>watch(clearSelection)</code> 也是个有意思的约定：外部把 <code>config.clearSelection</code> 置 <code>true</code>，组件在 <code>nextTick</code> 后清空勾选。因为 <code>true → true</code> 不触发 watch，实际用的时候外部得先切回 <code>false</code> 再置 <code>true</code>，形成一个「布尔脉冲」——能用，但语义不如暴露一个 <code>clearSelection()</code> 方法直白，同样是改进项。</p>
<h2 id="四、实战：动态表头-操作列插槽"><a href="#四、实战：动态表头-操作列插槽" class="headerlink" title="四、实战：动态表头 + 操作列插槽"></a>四、实战：动态表头 + 操作列插槽</h2><p>实际项目里一个列表页用起来是这样的（以设施列表页为例，节选）：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">Table</span> <span class="hljs-attr">ref</span>=<span class="hljs-string">&quot;tableRef&quot;</span> <span class="hljs-attr">v-bind</span>=<span class="hljs-string">&quot;state.tableData&quot;</span> @<span class="hljs-attr">pageChange</span>=<span class="hljs-string">&quot;onTablePageChange&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">template</span> #<span class="hljs-attr">action</span>=<span class="hljs-string">&quot;scope&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">el-button</span> <span class="hljs-attr">link</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;primary&quot;</span> <span class="hljs-attr">size</span>=<span class="hljs-string">&quot;small&quot;</span> @<span class="hljs-attr">click</span>=<span class="hljs-string">&quot;openEditDialog(scope.row)&quot;</span>&gt;</span><br>      编辑<br>    <span class="hljs-tag">&lt;/<span class="hljs-name">el-button</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">el-popconfirm</span> <span class="hljs-attr">title</span>=<span class="hljs-string">&quot;此操作将永久删除该设施，是否继续?&quot;</span> @<span class="hljs-attr">confirm</span>=<span class="hljs-string">&quot;handleDelete(scope.row)&quot;</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">template</span> #<span class="hljs-attr">reference</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">el-button</span> <span class="hljs-attr">link</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;danger&quot;</span> <span class="hljs-attr">size</span>=<span class="hljs-string">&quot;small&quot;</span>&gt;</span>删除<span class="hljs-tag">&lt;/<span class="hljs-name">el-button</span>&gt;</span><br>      <span class="hljs-tag">&lt;/<span class="hljs-name">template</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">el-popconfirm</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">template</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">Table</span>&gt;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">const</span> state = <span class="hljs-title function_">reactive</span>(&#123;<br>  <span class="hljs-attr">tableData</span>: &#123;<br>    <span class="hljs-attr">data</span>: [] <span class="hljs-keyword">as</span> <span class="hljs-built_in">any</span>[],<br>    <span class="hljs-attr">header</span>: [] <span class="hljs-keyword">as</span> <span class="hljs-built_in">any</span>[],<br>    <span class="hljs-attr">config</span>: &#123;<br>      <span class="hljs-attr">total</span>: <span class="hljs-number">0</span>,<br>      <span class="hljs-attr">loading</span>: <span class="hljs-literal">false</span>,<br>      <span class="hljs-attr">rowKey</span>: <span class="hljs-string">&#x27;id&#x27;</span>,<br>      <span class="hljs-attr">isSerialNo</span>: <span class="hljs-literal">true</span>,     <span class="hljs-comment">// 序号列</span><br>      <span class="hljs-attr">noPagination</span>: <span class="hljs-literal">false</span>,<br>    &#125;,<br>    <span class="hljs-attr">param</span>: &#123; <span class="hljs-attr">pageNum</span>: <span class="hljs-number">1</span>, <span class="hljs-attr">pageSize</span>: <span class="hljs-number">10</span>, <span class="hljs-attr">keyword</span>: <span class="hljs-string">&#x27;&#x27;</span> &#125;,<br>  &#125;,<br>&#125;)<br><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">getTableData</span> = (<span class="hljs-params"></span>) =&gt; &#123;<br>  state.<span class="hljs-property">tableData</span>.<span class="hljs-property">config</span>.<span class="hljs-property">loading</span> = <span class="hljs-literal">true</span><br>  <span class="hljs-title function_">getFacilityList</span>(&#123;<br>    ...state.<span class="hljs-property">tableData</span>.<span class="hljs-property">param</span>,<br>    <span class="hljs-attr">pageNum</span>: state.<span class="hljs-property">tableData</span>.<span class="hljs-property">param</span>.<span class="hljs-property">pageNum</span>,<br>    <span class="hljs-attr">pageSize</span>: state.<span class="hljs-property">tableData</span>.<span class="hljs-property">param</span>.<span class="hljs-property">pageSize</span>,<br>  &#125;).<span class="hljs-title function_">then</span>(<span class="hljs-function">(<span class="hljs-params"><span class="hljs-attr">result</span>: <span class="hljs-built_in">any</span></span>) =&gt;</span> &#123;<br>    <span class="hljs-comment">// 亮点：表头由后端下发（dataIndex/title），前端只补操作列</span><br>    <span class="hljs-keyword">const</span> <span class="hljs-attr">head</span>: <span class="hljs-built_in">any</span>[] = result.<span class="hljs-property">head</span> || []<br>    state.<span class="hljs-property">tableData</span>.<span class="hljs-property">header</span> = [<br>      ...head.<span class="hljs-title function_">map</span>(<span class="hljs-function">(<span class="hljs-params"><span class="hljs-attr">col</span>: <span class="hljs-built_in">any</span></span>) =&gt;</span> (&#123;<br>        <span class="hljs-attr">key</span>: col.<span class="hljs-property">dataIndex</span> || col.<span class="hljs-property">key</span>,<br>        <span class="hljs-attr">title</span>: col.<span class="hljs-property">title</span> || col.<span class="hljs-property">label</span>,<br>        <span class="hljs-attr">isCheck</span>: col.<span class="hljs-property">isCheck</span> !== <span class="hljs-literal">false</span>,<br>        <span class="hljs-attr">minWidth</span>: col.<span class="hljs-property">minWidth</span> || col.<span class="hljs-property">width</span> || <span class="hljs-number">120</span>,<br>        <span class="hljs-attr">slot</span>: col.<span class="hljs-property">slot</span> || <span class="hljs-literal">undefined</span>,<br>      &#125;)),<br>      &#123; <span class="hljs-attr">key</span>: <span class="hljs-string">&#x27;action&#x27;</span>, <span class="hljs-attr">colWidth</span>: <span class="hljs-number">180</span>, <span class="hljs-attr">title</span>: <span class="hljs-string">&#x27;操作&#x27;</span>, <span class="hljs-attr">slot</span>: <span class="hljs-string">&#x27;action&#x27;</span>, <span class="hljs-attr">fixed</span>: <span class="hljs-string">&#x27;right&#x27;</span>, <span class="hljs-attr">isCheck</span>: <span class="hljs-literal">true</span> &#125;,<br>    ]<br>    state.<span class="hljs-property">tableData</span>.<span class="hljs-property">data</span> = result.<span class="hljs-property">data</span> || []<br>    state.<span class="hljs-property">tableData</span>.<span class="hljs-property">config</span>.<span class="hljs-property">total</span> = <span class="hljs-title class_">Number</span>(result.<span class="hljs-property">total</span>) || <span class="hljs-number">0</span><br>  &#125;).<span class="hljs-title function_">finally</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>    state.<span class="hljs-property">tableData</span>.<span class="hljs-property">config</span>.<span class="hljs-property">loading</span> = <span class="hljs-literal">false</span><br>  &#125;)<br>&#125;<br><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">onSearch</span>(<span class="hljs-params"></span>) &#123;<br>  tableRef.<span class="hljs-property">value</span>?.<span class="hljs-title function_">pageReset</span>()   <span class="hljs-comment">// 搜索 = 回第一页重新拉取</span><br>&#125;<br><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">onTablePageChange</span> = (<span class="hljs-params"><span class="hljs-attr">page</span>: &#123; pageNum: <span class="hljs-built_in">number</span>; pageSize: <span class="hljs-built_in">number</span> &#125;</span>) =&gt; &#123;<br>  state.<span class="hljs-property">tableData</span>.<span class="hljs-property">param</span>.<span class="hljs-property">pageNum</span> = page.<span class="hljs-property">pageNum</span><br>  state.<span class="hljs-property">tableData</span>.<span class="hljs-property">param</span>.<span class="hljs-property">pageSize</span> = page.<span class="hljs-property">pageSize</span><br>  <span class="hljs-title function_">getTableData</span>()<br>&#125;<br></code></pre></td></tr></table></figure>

<p>两个实践模式：</p>
<ol>
<li><strong><code>v-bind=&quot;state.tableData&quot;</code> 一把传三 prop。</strong> <code>data/header/config</code> 打包在一个 reactive 对象里，模板上只有一行——「这一个对象就是这张表的全部状态」，查问题的时候只看一个地方；</li>
<li><strong>动态表头是配置驱动的隐藏福利。</strong> 因为列本来就是 JSON，后端下发 <code>head</code> 数组时前端只需做一层字段名归一化（<code>dataIndex → key</code>）再拼上固定的操作列。「表格列配置」从模板代码变成了<strong>数据</strong>，可以从接口来、可以存用户偏好、可以做列显隐设置——这些都是写死 <code>&lt;el-table-column&gt;</code> 时不敢想的。</li>
</ol>
<p>这个组件在项目里被用户、角色、设施三个管理页复用。当前页面主要用到了序号列、分页和插槽；多选、单选、排序是按通用中后台场景预置的能力，在别的项目里已经过实战，这里一并保留。</p>
<h2 id="五、设计复盘：好的与该改的"><a href="#五、设计复盘：好的与该改的" class="headerlink" title="五、设计复盘：好的与该改的"></a>五、设计复盘：好的与该改的</h2><table>
<thead>
<tr>
<th>维度</th>
<th>做法</th>
<th>评价</th>
</tr>
</thead>
<tbody><tr>
<td>原生能力</td>
<td><code>v-bind=&quot;$attrs&quot;</code> 透传</td>
<td>✅ 封装不阉割，el-table 全量 API 可用</td>
</tr>
<tr>
<td>自定义格子</td>
<td>列配置声明 slot + 动态插槽转发</td>
<td>✅ 配置管「哪列」，插槽管「怎么渲染」</td>
</tr>
<tr>
<td>跨页多选</td>
<td><code>reserve-selection</code> + <code>row-key</code></td>
<td>✅ 一行配置解决翻页丢勾选</td>
</tr>
<tr>
<td>单选</td>
<td>多选模拟 + CSS 藏表头全选框</td>
<td>⚠️ 可用，但依赖 selection 数组时序，升级 Element Plus 要回归测试</td>
</tr>
<tr>
<td>排序</td>
<td>descending → desc 归一化</td>
<td>✅ 方言翻译收进组件</td>
</tr>
<tr>
<td>分页</td>
<td>非受控 + <code>pageReset()</code> 暴露</td>
<td>⚠️ 搜索回第一页顺手，但父组件无法直接控制页码</td>
</tr>
<tr>
<td>清空勾选</td>
<td>watch 布尔脉冲</td>
<td>❌ 应该直接 expose 一个 <code>clearSelection()</code> 方法</td>
</tr>
<tr>
<td>类型</td>
<td><code>EmptyObjectType</code>、config 无类型</td>
<td>❌ 该用泛型 <code>TableProps&lt;T&gt;</code> + <code>HeaderItem</code> 接口约束，IDE 提示和防错都缺</td>
</tr>
</tbody></table>
<p>复盘里最后两条是诚实账：<code>config</code> 是个无类型 Object，字段拼错不会有任何提示，纯靠文档和人肉记忆。理想形态是导出 <code>HeaderItem</code>、<code>TableConfig</code> 接口并让 <code>data</code> 支持泛型推导——列少的时候无所谓，列多、多人协作时类型就是第一生产力。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">封装前（每页）                        封装后（每页）<br>─────────────────                    ─────────────────<br>&lt;el-table&gt; + loading     模板 ~60 行   &lt;Table v-bind=&quot;tableData&quot;&gt;  1 行<br>&lt;el-table-column&gt; ×N                  + 需要自定义的具名插槽<br>&lt;el-pagination&gt; + 2 个事件            header: JSON 声明列<br>空状态 / 序号列 / 多选列               翻页只接 pageChange 一个事件<br></code></pre></td></tr></table></figure>

<p><strong>一句话记住</strong>：表格组件封装的本质不是「包一层」，而是把<strong>稳定的骨架</strong>（列渲染、分页、选择、空态）收进组件，把<strong>易变的血肉</strong>（哪些列、列怎么自定义、数据从哪来）留给配置和插槽——判断封装好坏的标准就一条：用的人还有没有机会碰到 el-table 本身解决不了的问题（<code>$attrs</code> 和 <code>defineExpose</code> 就是留给这个的）。</p>
]]></content>
      <categories>
        <category>Vue 进阶</category>
      </categories>
      <tags>
        <tag>vue3</tag>
        <tag>element-plus</tag>
        <tag>组件封装</tag>
        <tag>typescript</tag>
        <tag>中后台</tag>
      </tags>
  </entry>
  <entry>
    <title>手写防抖与节流：30 行核心代码，讲清两件事</title>
    <url>/2026/09/10/javascript-debounce-throttle/</url>
    <content><![CDATA[<p><code>scroll</code>、<code>resize</code>、<code>input</code>、<code>mousemove</code> 这类事件一秒能触发几十上百次，处理器跟不上就是卡顿。防抖和节流做的事情一样——<strong>降频</strong>，但策略相反：</p>
<blockquote>
<p><strong>防抖（debounce）：等你停了我再做。</strong> 每次触发都重新计时，只有安静了 <code>wait</code> 毫秒才执行，持续触发期间一次都不跑。<br><strong>节流（throttle）：再急也按固定节奏做。</strong> 不管触发多频繁，每个 <code>wait</code> 窗口内最多执行一次。</p>
</blockquote>
<p>电梯类比：防抖是「有人进来就重新等，没人再进才关门」；节流是「观光车每 10 分钟发一班，到点就走」。</p>
<span id="more"></span>

<h2 id="一、手写防抖"><a href="#一、手写防抖" class="headerlink" title="一、手写防抖"></a>一、手写防抖</h2><h3 id="基础版"><a href="#基础版" class="headerlink" title="基础版"></a>基础版</h3><p>核心就一招：<strong>用闭包存住定时器，每次触发先把上一个清掉</strong>。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">function</span> <span class="hljs-title function_">debounce</span>(<span class="hljs-params">fn, wait</span>) &#123;<br>  <span class="hljs-keyword">let</span> timer = <span class="hljs-literal">null</span>;<br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> (<span class="hljs-params">...args</span>) &#123;<br>    <span class="hljs-keyword">if</span> (timer) <span class="hljs-built_in">clearTimeout</span>(timer);   <span class="hljs-comment">// 上一次作废，重新计时</span><br>    timer = <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>      fn.<span class="hljs-title function_">apply</span>(<span class="hljs-variable language_">this</span>, args);           <span class="hljs-comment">// this 指向事件目标，参数透传</span><br>    &#125;, wait);<br>  &#125;;<br>&#125;<br><br><span class="hljs-comment">// 用法</span><br>input.<span class="hljs-title function_">addEventListener</span>(<span class="hljs-string">&#x27;input&#x27;</span>, <span class="hljs-title function_">debounce</span>(search, <span class="hljs-number">300</span>));<br></code></pre></td></tr></table></figure>

<p>两个容易被问的细节：</p>
<ul>
<li><strong>外层必须是普通 <code>function</code></strong>：<code>this</code> 才能拿到绑定事件的元素；内层用箭头函数继承这个 <code>this</code>。</li>
<li><strong><code>fn.apply(this, args)</code></strong>：不写的话 <code>fn</code> 里的 <code>this</code> 是 <code>window</code>、<code>event</code> 也丢了。</li>
</ul>
<h3 id="完善版：支持立即执行-取消"><a href="#完善版：支持立即执行-取消" class="headerlink" title="完善版：支持立即执行 + 取消"></a>完善版：支持立即执行 + 取消</h3><p>搜索框更理想的体验是<strong>第一次输入立刻搜，之后的连击被防抖吞掉</strong>——这就是 <code>immediate</code> 选项。组件卸载时还得能取消等待中的任务，防止「人已下车还执行回调」。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">function</span> <span class="hljs-title function_">debounce</span>(<span class="hljs-params">fn, wait, immediate = <span class="hljs-literal">false</span></span>) &#123;<br>  <span class="hljs-keyword">let</span> timer = <span class="hljs-literal">null</span>;<br><br>  <span class="hljs-keyword">const</span> debounced = <span class="hljs-keyword">function</span> (<span class="hljs-params">...args</span>) &#123;<br>    <span class="hljs-keyword">if</span> (timer) <span class="hljs-built_in">clearTimeout</span>(timer);<br><br>    <span class="hljs-keyword">if</span> (immediate) &#123;<br>      <span class="hljs-keyword">const</span> callNow = !timer;                    <span class="hljs-comment">// 没有等待中的任务 → 这次立刻执行</span><br>      timer = <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> (timer = <span class="hljs-literal">null</span>), wait); <span class="hljs-comment">// 定时器只用来&quot;锁&quot;住冷却期</span><br>      <span class="hljs-keyword">if</span> (callNow) fn.<span class="hljs-title function_">apply</span>(<span class="hljs-variable language_">this</span>, args);<br>    &#125; <span class="hljs-keyword">else</span> &#123;<br>      timer = <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> fn.<span class="hljs-title function_">apply</span>(<span class="hljs-variable language_">this</span>, args), wait);<br>    &#125;<br>  &#125;;<br><br>  debounced.<span class="hljs-property">cancel</span> = <span class="hljs-function">() =&gt;</span> &#123;                     <span class="hljs-comment">// 取消：清掉定时器并复位</span><br>    <span class="hljs-built_in">clearTimeout</span>(timer);<br>    timer = <span class="hljs-literal">null</span>;<br>  &#125;;<br>  <span class="hljs-keyword">return</span> debounced;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>immediate</code> 分支的巧妙之处：定时器到点后<strong>不执行 fn，只把 <code>timer</code> 置空</strong>，它退化成一把「冷却锁」——锁着的时候（<code>timer</code> 非空）不执行，锁开了才放行下一次。</p>
<h2 id="二、手写节流"><a href="#二、手写节流" class="headerlink" title="二、手写节流"></a>二、手写节流</h2><h3 id="时间戳版：首次立即执行，末尾不补"><a href="#时间戳版：首次立即执行，末尾不补" class="headerlink" title="时间戳版：首次立即执行，末尾不补"></a>时间戳版：首次立即执行，末尾不补</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">function</span> <span class="hljs-title function_">throttle</span>(<span class="hljs-params">fn, wait</span>) &#123;<br>  <span class="hljs-keyword">let</span> prev = <span class="hljs-number">0</span>;                          <span class="hljs-comment">// 上次执行时间，0 保证首次立即执行</span><br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> (<span class="hljs-params">...args</span>) &#123;<br>    <span class="hljs-keyword">const</span> now = <span class="hljs-title class_">Date</span>.<span class="hljs-title function_">now</span>();<br>    <span class="hljs-keyword">if</span> (now - prev &gt;= wait) &#123;            <span class="hljs-comment">// 距上次执行够久了，放行</span><br>      fn.<span class="hljs-title function_">apply</span>(<span class="hljs-variable language_">this</span>, args);<br>      prev = now;<br>    &#125;<br>  &#125;;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>缺点：停止触发前，最后一次可能刚好卡在窗口里，不会被补执行。</p>
<h3 id="定时器版：首次延迟执行，末尾补一次"><a href="#定时器版：首次延迟执行，末尾补一次" class="headerlink" title="定时器版：首次延迟执行，末尾补一次"></a>定时器版：首次延迟执行，末尾补一次</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">function</span> <span class="hljs-title function_">throttle</span>(<span class="hljs-params">fn, wait</span>) &#123;<br>  <span class="hljs-keyword">let</span> timer = <span class="hljs-literal">null</span>;<br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> (<span class="hljs-params">...args</span>) &#123;<br>    <span class="hljs-keyword">if</span> (timer) <span class="hljs-keyword">return</span>;                   <span class="hljs-comment">// 已有任务在排队，直接丢弃本次</span><br>    timer = <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>      fn.<span class="hljs-title function_">apply</span>(<span class="hljs-variable language_">this</span>, args);<br>      timer = <span class="hljs-literal">null</span>;                      <span class="hljs-comment">// 释放，允许排下一个任务</span><br>    &#125;, wait);<br>  &#125;;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>缺点：第一次也要等 <code>wait</code> 才执行。</p>
<h3 id="合并版：首尾都执行"><a href="#合并版：首尾都执行" class="headerlink" title="合并版：首尾都执行"></a>合并版：首尾都执行</h3><p>用「剩余时间」把两种策略拼起来：窗口已过就立即执行，还在窗口内就排一个 <code>remaining</code> 毫秒后的兜底任务。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">function</span> <span class="hljs-title function_">throttle</span>(<span class="hljs-params">fn, wait</span>) &#123;<br>  <span class="hljs-keyword">let</span> timer = <span class="hljs-literal">null</span>;<br>  <span class="hljs-keyword">let</span> prev = <span class="hljs-number">0</span>;<br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> (<span class="hljs-params">...args</span>) &#123;<br>    <span class="hljs-keyword">const</span> remaining = wait - (<span class="hljs-title class_">Date</span>.<span class="hljs-title function_">now</span>() - prev);<br><br>    <span class="hljs-keyword">if</span> (remaining &lt;= <span class="hljs-number">0</span>) &#123;                <span class="hljs-comment">// 窗口已过：立即执行</span><br>      <span class="hljs-built_in">clearTimeout</span>(timer);<br>      timer = <span class="hljs-literal">null</span>;<br>      fn.<span class="hljs-title function_">apply</span>(<span class="hljs-variable language_">this</span>, args);<br>      prev = <span class="hljs-title class_">Date</span>.<span class="hljs-title function_">now</span>();<br>    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (!timer) &#123;                 <span class="hljs-comment">// 窗口内：补一次尾部执行</span><br>      timer = <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>        fn.<span class="hljs-title function_">apply</span>(<span class="hljs-variable language_">this</span>, args);<br>        prev = <span class="hljs-title class_">Date</span>.<span class="hljs-title function_">now</span>();<br>        timer = <span class="hljs-literal">null</span>;<br>      &#125;, remaining);<br>    &#125;<br>  &#125;;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="三、一张表选型"><a href="#三、一张表选型" class="headerlink" title="三、一张表选型"></a>三、一张表选型</h2><p>假设持续触发 1 分钟、<code>wait</code> 为 1 秒：</p>
<table>
<thead>
<tr>
<th></th>
<th>防抖 debounce</th>
<th>节流 throttle</th>
</tr>
</thead>
<tbody><tr>
<td>执行时机</td>
<td>停止触发 <code>wait</code> 后</td>
<td>固定周期到了就执行</td>
</tr>
<tr>
<td>1 分钟内的执行次数</td>
<td><strong>1 次</strong>（只认最后一次）</td>
<td><strong>约 60 次</strong>（稀释频率）</td>
</tr>
<tr>
<td>典型场景</td>
<td>搜索联想、<code>resize</code> 后重算布局、表单校验、防重复提交</td>
<td>滚动加载&#x2F;吸顶、<code>mousemove</code> 跟随、按钮防连点、视频进度上报</td>
</tr>
</tbody></table>
<p>判断口诀：<strong>只关心最终结果的用防抖（结果型），过程中必须周期响应的用节流（过程型）。</strong> 拿不准就想电梯和观光车。</p>
<!-- ## 四、面试速答清单

- 防抖：闭包 + `clearTimeout` 重置定时器；节流：时间戳差值判断或定时器占位。
- 三个共性细节：普通 `function` 保留 `this`、`apply` 透传 `args`、返回新函数而非原地改。
- 防抖进阶：`immediate`（首刷 + 冷却锁）、`cancel`（组件卸载时调用）。
- 节流两版的差异：时间戳版首执行尾不补，定时器版首延迟尾补一次，合并版兼得。
- 生产环境直接用 `lodash` 的 `debounce` / `throttle`，它们还处理了 `maxWait`、`leading/trailing` 开关等边界。 -->
]]></content>
      <categories>
        <category>前端基础</category>
        <category>javascript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
        <tag>性能优化</tag>
      </tags>
  </entry>
  <entry>
    <title>HTML 语义化总结：页面骨架、易混标签与无障碍实践</title>
    <url>/2026/09/09/html-semantics/</url>
    <content><![CDATA[<p>语义化一句话：<strong>用标签表达「这是什么」，而不是「这长什么样」。</strong> 页面最终长什么样是 CSS 的事，但 HTML 还有一层身份——一份结构化文档，读者除了浏览器，还有读屏软件和搜索引擎爬虫。它们没有视觉，只能靠标签理解页面：<code>nav</code> 是导航、<code>h1</code> 是主题、<code>article</code> 是正文。用 <code>div</code> + class 描述一切，等于把这本书写成了「机器读不出来的图片」。</p>
<p>本篇是技术总结，直接进正题。</p>
<span id="more"></span>

<h2 id="一、语义化解决什么"><a href="#一、语义化解决什么" class="headerlink" title="一、语义化解决什么"></a>一、语义化解决什么</h2><table>
<thead>
<tr>
<th>受益方</th>
<th>靠什么理解页面</th>
<th>语义化带来什么</th>
</tr>
</thead>
<tbody><tr>
<td>读屏软件用户</td>
<td>标签的角色（landmark、heading）</td>
<td>可一键跳转导航&#x2F;正文、标题可当目录朗读</td>
</tr>
<tr>
<td>搜索引擎爬虫</td>
<td>文档结构（h1、article、time）</td>
<td>抓得住页面主题与内容权重</td>
</tr>
<tr>
<td>未来的维护者</td>
<td>结构本身</td>
<td>标签即文档，不用读 class 猜意图</td>
</tr>
</tbody></table>
<p>反例长这样——人眼能看懂，机器什么都读不出来：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;top-header&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;nav-box&quot;</span>&gt;</span>…<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;main-wrap&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;big-title&quot;</span>&gt;</span>为什么语义化重要<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br></code></pre></td></tr></table></figure>

<h2 id="二、页面骨架：结构级标签"><a href="#二、页面骨架：结构级标签" class="headerlink" title="二、页面骨架：结构级标签"></a>二、页面骨架：结构级标签</h2><p>一张典型的内容页结构：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">┌ &lt;header&gt; ── 页头：站名 / logo / 全局导航<br>│   └ &lt;nav&gt; ── 主要导航链接组<br>├ &lt;main&gt; ──── 主内容区（整页唯一）<br>│   ├ &lt;article&gt; ─ 独立成篇的内容<br>│   │   ├ &lt;header&gt; ─ 这篇文章的头（标题 / 时间）<br>│   │   ├ &lt;section&gt; ─ 有标题的主题分区<br>│   │   └ &lt;footer&gt; ─ 这篇文章的尾（作者 / 联系方式）<br>│   └ &lt;aside&gt; ── 与主内容间接相关：侧栏 / 相关阅读<br>└ &lt;footer&gt; ── 页脚：版权 / 备案 / 联系方式<br></code></pre></td></tr></table></figure>

<p>对应的最小骨架：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">body</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">header</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">h1</span>&gt;</span>眉眼如初<span class="hljs-tag">&lt;/<span class="hljs-name">h1</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">nav</span> <span class="hljs-attr">aria-label</span>=<span class="hljs-string">&quot;主导航&quot;</span>&gt;</span>…<span class="hljs-tag">&lt;/<span class="hljs-name">nav</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">header</span>&gt;</span><br><br>  <span class="hljs-tag">&lt;<span class="hljs-name">main</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">article</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">header</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">h2</span>&gt;</span>HTML 语义化总结<span class="hljs-tag">&lt;/<span class="hljs-name">h2</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">time</span> <span class="hljs-attr">datetime</span>=<span class="hljs-string">&quot;2026-09-09&quot;</span>&gt;</span>9 月 9 日<span class="hljs-tag">&lt;/<span class="hljs-name">time</span>&gt;</span><br>      <span class="hljs-tag">&lt;/<span class="hljs-name">header</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">section</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">h3</span>&gt;</span>第一节<span class="hljs-tag">&lt;/<span class="hljs-name">h3</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">p</span>&gt;</span>…<span class="hljs-tag">&lt;/<span class="hljs-name">p</span>&gt;</span><br>      <span class="hljs-tag">&lt;/<span class="hljs-name">section</span>&gt;</span><br>      <span class="hljs-tag">&lt;<span class="hljs-name">footer</span>&gt;</span>作者：<span class="hljs-tag">&lt;<span class="hljs-name">address</span>&gt;</span>me@example.com<span class="hljs-tag">&lt;/<span class="hljs-name">address</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">footer</span>&gt;</span><br>    <span class="hljs-tag">&lt;/<span class="hljs-name">article</span>&gt;</span><br><br>    <span class="hljs-tag">&lt;<span class="hljs-name">aside</span>&gt;</span>相关阅读…<span class="hljs-tag">&lt;/<span class="hljs-name">aside</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">main</span>&gt;</span><br><br>  <span class="hljs-tag">&lt;<span class="hljs-name">footer</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">small</span>&gt;</span>© 2026 眉眼如初<span class="hljs-tag">&lt;/<span class="hljs-name">small</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">footer</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">body</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>三条硬规则：</p>
<ol>
<li><strong><code>main</code> 整页唯一</strong>，且不能嵌在 <code>article</code> &#x2F; <code>aside</code> &#x2F; <code>nav</code> &#x2F; <code>header</code> &#x2F; <code>footer</code> 里；</li>
<li><strong><code>header</code> &#x2F; <code>footer</code> 不止页头页脚</strong>：<code>article</code>、<code>section</code> 内部也可以有，表示「这一块的头和尾」；</li>
<li><strong>语义标签自带隐式 ARIA 角色</strong>：顶层 <code>header</code> → <code>banner</code>、<code>footer</code> → <code>contentinfo</code>、<code>nav</code> → <code>navigation</code>、<code>main</code> → <code>main</code>——读屏软件据此生成「地标」，用户可以一键跳过导航直达正文（嵌套在 article 里的 header 则退化回普通节点，这正是「顶层」限定的含义）。</li>
</ol>
<p>标题层级：<code>h1</code>–<code>h6</code> 不跳级、整页一个 <code>h1</code> 最稳。HTML5 曾允许每个 <code>section</code> 各配一个 <code>h1</code>，但那套 outline 算法已被规范移除，别再依赖。</p>
<h2 id="三、易混标签辨析"><a href="#三、易混标签辨析" class="headerlink" title="三、易混标签辨析"></a>三、易混标签辨析</h2><table>
<thead>
<tr>
<th>问题</th>
<th>判据</th>
<th>结论</th>
</tr>
</thead>
<tbody><tr>
<td><code>section</code> 还是 <code>div</code></td>
<td>能不能给它起个像样的标题</td>
<td>有主题、配标题的分区用 <code>section</code>；纯样式&#x2F;JS 挂钩的容器用 <code>div</code></td>
</tr>
<tr>
<td><code>article</code> 还是 <code>section</code></td>
<td>拿出来单独发（RSS、卡片）还成不成立</td>
<td>文章、评论、商品卡用 <code>article</code>；页面里的一章用 <code>section</code></td>
</tr>
<tr>
<td>链接组要不要包 <code>nav</code></td>
<td>用户会不会想「跳过 &#x2F; 跳到」它</td>
<td>主导航、面包屑用；页脚的一串备案链接不用</td>
</tr>
<tr>
<td><code>aside</code> 是不是侧边栏</td>
<td>拿掉它正文是否依然完整</td>
<td>判据是「与主内容间接相关」，不是视觉位置——正文里插入的引申阅读框也是 <code>aside</code></td>
</tr>
<tr>
<td><code>strong</code> 还是 <code>b</code></td>
<td>是否要传达「重要」</td>
<td>重要&#x2F;紧急&#x2F;警告用 <code>strong</code>（读屏会加重语气）；只是视觉上吸引注意（关键词、引导句）用 <code>b</code></td>
</tr>
<tr>
<td><code>em</code> 还是 <code>i</code></td>
<td>强调是否改变句义</td>
<td>「我<em>就</em>是说…」这类改句义的强调用 <code>em</code>；术语、外语、内心戏用 <code>i</code></td>
</tr>
</tbody></table>
<p>一句话版：<strong>结构标签问「它是什么」，文本标签问「读者该怎么理解它」。</strong></p>
<h2 id="四、文本级语义速查"><a href="#四、文本级语义速查" class="headerlink" title="四、文本级语义速查"></a>四、文本级语义速查</h2><table>
<thead>
<tr>
<th>标签</th>
<th>含义</th>
<th>典型场景</th>
</tr>
</thead>
<tbody><tr>
<td><code>code</code> &#x2F; <code>pre</code></td>
<td>代码（行内 &#x2F; 块级）</td>
<td>行内片段；块级代码保持格式</td>
</tr>
<tr>
<td><code>abbr</code></td>
<td>缩写，<code>title</code> 给全称</td>
<td><code>&lt;abbr title=&quot;Cascading Style Sheets&quot;&gt;CSS&lt;/abbr&gt;</code></td>
</tr>
<tr>
<td><code>cite</code></td>
<td>作品标题（<strong>不是作者</strong>）</td>
<td>书名、影名、论文名</td>
</tr>
<tr>
<td><code>mark</code></td>
<td>当前语境中的「相关」高亮</td>
<td>搜索结果关键词标亮</td>
</tr>
<tr>
<td><code>del</code> &#x2F; <code>ins</code></td>
<td>文档修订（删除 &#x2F; 插入）</td>
<td>价格变更、changelog</td>
</tr>
<tr>
<td><code>s</code></td>
<td>不再准确但保留原文</td>
<td>原价划线（区别于 <code>del</code>：不是修订，是失效）</td>
</tr>
<tr>
<td><code>small</code></td>
<td>附属细则</td>
<td>版权声明、免责条款</td>
</tr>
<tr>
<td><code>time</code></td>
<td>机器可读时间，值放 <code>datetime</code></td>
<td><code>&lt;time datetime=&quot;2026-09-09&quot;&gt;昨天&lt;/time&gt;</code></td>
</tr>
<tr>
<td><code>figure</code> &#x2F; <code>figcaption</code></td>
<td>被正文引用的独立单元及其说明</td>
<td>图表、截图、代码示例</td>
</tr>
<tr>
<td><code>address</code></td>
<td>最近 <code>article</code> &#x2F; <code>body</code> 的作者联系方式</td>
<td>不是随便一个收货地址</td>
</tr>
<tr>
<td><code>dfn</code> &#x2F; <code>var</code> &#x2F; <code>kbd</code> &#x2F; <code>samp</code></td>
<td>定义术语 &#x2F; 变量 &#x2F; 按键 &#x2F; 程序输出</td>
<td>教程文档常用</td>
</tr>
</tbody></table>
<h2 id="五、表单：语义化的重灾区"><a href="#五、表单：语义化的重灾区" class="headerlink" title="五、表单：语义化的重灾区"></a>五、表单：语义化的重灾区</h2><p>表单是读屏用户受影响最大的区域，四条底线：</p>
<ol>
<li><strong>每个输入框都有 <code>label</code></strong>，用 <code>for</code>&#x2F;<code>id</code> 关联——点击 label 聚焦输入框，读屏才知道「这个框是干嘛的」；<code>placeholder</code> 对比度低、输入后消失，<strong>不能替代 label</strong>；</li>
<li><strong><code>input type</code> 用对</strong>：<code>email</code> &#x2F; <code>url</code> &#x2F; <code>tel</code> &#x2F; <code>number</code> &#x2F; <code>date</code> 会唤起对应的移动端键盘并带原生校验，白送的能力；</li>
<li><strong>相关选项用 <code>fieldset</code> + <code>legend</code> 分组</strong>：单选组共用一个问题时，<code>legend</code> 就是那道题；</li>
<li><strong><code>autocomplete</code> 写上 token</strong>：<code>name</code> &#x2F; <code>email</code> &#x2F; <code>one-time-code</code>，浏览器自动填充对所有人都是效率，对读屏用户更是。</li>
</ol>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">fieldset</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">legend</span>&gt;</span>订阅方式<span class="hljs-tag">&lt;/<span class="hljs-name">legend</span>&gt;</span><br><br>  <span class="hljs-tag">&lt;<span class="hljs-name">label</span> <span class="hljs-attr">for</span>=<span class="hljs-string">&quot;email&quot;</span>&gt;</span>邮箱<span class="hljs-tag">&lt;/<span class="hljs-name">label</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">input</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;email&quot;</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;email&quot;</span> <span class="hljs-attr">autocomplete</span>=<span class="hljs-string">&quot;email&quot;</span> <span class="hljs-attr">required</span>&gt;</span><br><br>  <span class="hljs-tag">&lt;<span class="hljs-name">label</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">input</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;radio&quot;</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;freq&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;w&quot;</span>&gt;</span> 每周<span class="hljs-tag">&lt;/<span class="hljs-name">label</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">label</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">input</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;radio&quot;</span> <span class="hljs-attr">name</span>=<span class="hljs-string">&quot;freq&quot;</span> <span class="hljs-attr">value</span>=<span class="hljs-string">&quot;d&quot;</span>&gt;</span> 每天<span class="hljs-tag">&lt;/<span class="hljs-name">label</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">fieldset</span>&gt;</span><br></code></pre></td></tr></table></figure>

<h2 id="六、ARIA：语义标签的兜底层"><a href="#六、ARIA：语义标签的兜底层" class="headerlink" title="六、ARIA：语义标签的兜底层"></a>六、ARIA：语义标签的兜底层</h2><p>ARIA 第一法则：<strong>能用原生语义标签，就不加 ARIA。</strong> 原生标签自带键盘行为和焦点管理，ARIA 只改变读屏的「读法」，不补行为——这是假按钮最大的坑：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-comment">&lt;!-- 假按钮：读屏知道它是按钮了，但没有焦点、Enter/Space 不激活、不能提交表单，</span><br><span class="hljs-comment">     tabindex、keydown、样式全要手动补 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">role</span>=<span class="hljs-string">&quot;button&quot;</span> <span class="hljs-attr">tabindex</span>=<span class="hljs-string">&quot;0&quot;</span> <span class="hljs-attr">onkeydown</span>=<span class="hljs-string">&quot;handleKeys(event)&quot;</span>&gt;</span>收藏<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><br><span class="hljs-comment">&lt;!-- 真按钮：以上全部原生自带 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">button</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;button&quot;</span>&gt;</span>收藏<span class="hljs-tag">&lt;/<span class="hljs-name">button</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>原生没有对应物时，ARIA 才登场，常用的就这几个：</p>
<ul>
<li><code>aria-label</code>：无文字的图标按钮（按钮内只有 SVG）；</li>
<li><code>aria-expanded</code>：折叠菜单 &#x2F; 弹层的展开状态；</li>
<li><code>aria-current=&quot;page&quot;</code>：导航里的当前页高亮。</li>
</ul>
<p>顺带 <code>img</code> 的 <code>alt</code> 三规则：信息图<strong>描述内容</strong>（不是「一张图片」）；装饰图 <code>alt=&quot;&quot;</code>（空值表示跳过，不是省略属性）；在链接&#x2F;按钮里的图<strong>描述动作</strong>。</p>
<h2 id="七、常见误区"><a href="#七、常见误区" class="headerlink" title="七、常见误区"></a>七、常见误区</h2><ul>
<li><strong>语义化 ≠ 消灭 <code>div</code></strong>：无语义容器正是 <code>div</code> 的本职，错的只是「用 div 表达一切」；</li>
<li><strong>「长得一样」≠「语义一样」</strong>：<code>b</code> 和 <code>strong</code> 默认都是粗体，但读屏读法不同；把 <code>h1</code> 样式改成 12px 不改变它是页面主题；</li>
<li><strong>语义标签不自带样式</strong>：<code>section</code> &#x2F; <code>nav</code> 基本就是个 block，该写的 CSS 一行不少——语义化买的是含义，不是外观；</li>
<li><strong>工作流上：先圈结构，再写样式</strong>：拿到设计稿先标出 <code>header</code> &#x2F; <code>main</code> &#x2F; <code>aside</code> &#x2F; <code>article</code>，再考虑 class 和样式，顺序反了必然 div 一把梭。</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>场景速查：</p>
<table>
<thead>
<tr>
<th>要放的东西</th>
<th>用什么</th>
</tr>
</thead>
<tbody><tr>
<td>页头 &#x2F; 页脚</td>
<td><code>header</code> &#x2F; <code>footer</code></td>
</tr>
<tr>
<td>导航链接组</td>
<td><code>nav</code></td>
</tr>
<tr>
<td>主内容区</td>
<td><code>main</code>（整页唯一）</td>
</tr>
<tr>
<td>一篇独立内容</td>
<td><code>article</code></td>
</tr>
<tr>
<td>有标题的主题分区</td>
<td><code>section</code></td>
</tr>
<tr>
<td>纯样式 &#x2F; 脚本容器</td>
<td><code>div</code></td>
</tr>
<tr>
<td>重要 &#x2F; 强调的文字</td>
<td><code>strong</code> &#x2F; <code>em</code></td>
</tr>
<tr>
<td>图 + 说明</td>
<td><code>figure</code> + <code>figcaption</code></td>
</tr>
<tr>
<td>时间</td>
<td><code>time[datetime]</code></td>
</tr>
<tr>
<td>图标按钮</td>
<td><code>button</code> + <code>aria-label</code></td>
</tr>
<tr>
<td>一组相关表单项</td>
<td><code>fieldset</code> + <code>legend</code></td>
</tr>
</tbody></table>
<p>面试速答：</p>
<ul>
<li><strong>为什么需要语义化？</strong> HTML 的读者除了浏览器还有读屏软件和爬虫；标签传达结构与重要性，决定它们如何理解和朗读页面，同时让人一眼看懂结构。</li>
<li><strong><code>section</code> 和 <code>div</code> 怎么选？</strong> 有主题、能配上标题的分区用 <code>section</code>；只为样式或脚本挂钩的通用容器用 <code>div</code>。</li>
<li><strong><code>strong</code> 和 <code>b</code> 的区别？</strong> <code>strong</code> 传达重要性（读屏加重语气），<code>b</code> 只是视觉上引起注意；<code>em</code>&#x2F;<code>i</code> 同理。</li>
<li><strong>ARIA 和语义标签的关系？</strong> 原生优先——自带行为与焦点；ARIA 是没有原生对应物时的兜底，只改可读性不补行为。</li>
<li><strong><code>img</code> 的 <code>alt</code> 怎么写？</strong> 信息图描述内容，装饰图写空 <code>alt=&quot;&quot;</code>，链接&#x2F;按钮内的图描述动作。</li>
</ul>
<p><strong>一句话记住</strong>：HTML 是写给三类读者的文档——人看结构、读屏读角色、爬虫抓重点；先选对标签，再谈样式，ARIA 只做兜底。</p>
<!-- ## 参考

- [MDN：HTML 元素参考](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element)
- [HTML Living Standard](https://html.spec.whatwg.org/)
- [WAI-ARIA Authoring Practices Guide (APG)](https://www.w3.org/WAI/ARIA/apg/)
- [web.dev：Learn Accessibility](https://web.dev/learn/accessibility)
- [Using ARIA：First Rule of ARIA](https://www.w3.org/WAI/ARIA/apg/practices/read-me-first/) -->
]]></content>
      <categories>
        <category>前端基础</category>
        <category>html</category>
      </categories>
      <tags>
        <tag>前端基础</tag>
        <tag>html</tag>
        <tag>语义化</tag>
        <tag>SEO</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 闭包：从作用域到数据私有化</title>
    <url>/2026/09/10/javascript-closure/</url>
    <content><![CDATA[<h1 id="JavaScript-闭包：从作用域到数据私有化"><a href="#JavaScript-闭包：从作用域到数据私有化" class="headerlink" title="JavaScript 闭包：从作用域到数据私有化"></a>JavaScript 闭包：从作用域到数据私有化</h1><blockquote>
<p>闭包是 JavaScript 中一个非常重要的概念。<br>它看起来像是“函数记住了外部变量”，但如果只记住这句话，很容易在遇到实际代码时产生疑惑。</p>
<p>真正理解闭包，需要先理解 <strong>作用域、作用域链以及函数执行时的变量查找过程</strong>。</p>
</blockquote>
<hr>
<h2 id="一、什么是闭包？"><a href="#一、什么是闭包？" class="headerlink" title="一、什么是闭包？"></a>一、什么是闭包？</h2><p>先看一个最简单的例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">outer</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">const</span> name = <span class="hljs-string">&#x27;Tom&#x27;</span><br><br>  <span class="hljs-keyword">function</span> <span class="hljs-title function_">inner</span>(<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(name)<br>  &#125;<br><br>  <span class="hljs-keyword">return</span> inner<br>&#125;<br><br><span class="hljs-keyword">const</span> fn = <span class="hljs-title function_">outer</span>()<br><br><span class="hljs-title function_">fn</span>()<br></code></pre></td></tr></table></figure>

<p>执行结果：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Tom<br></code></pre></td></tr></table></figure>

<p>这里有一个值得注意的地方：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> fn = <span class="hljs-title function_">outer</span>()<br></code></pre></td></tr></table></figure>

<p><code>outer()</code> 执行完成以后，按直觉来说，<code>outer</code> 的执行环境应该结束了。</p>
<p>但是：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">fn</span>()<br></code></pre></td></tr></table></figure>

<p>依然可以访问：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> name = <span class="hljs-string">&#x27;Tom&#x27;</span><br></code></pre></td></tr></table></figure>

<p>这就是闭包最典型的表现。</p>
<p>可以简单理解为：</p>
<blockquote>
<p><strong>当一个函数能够访问并使用它定义时所在作用域中的变量，即使这个外部作用域已经执行结束，这种现象就形成了闭包。</strong></p>
</blockquote>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">outer</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">let</span> count = <span class="hljs-number">0</span><br><br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123;<br>    count++<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(count)<br>  &#125;<br>&#125;<br><br><span class="hljs-keyword">const</span> increment = <span class="hljs-title function_">outer</span>()<br><br><span class="hljs-title function_">increment</span>() <span class="hljs-comment">// 1</span><br><span class="hljs-title function_">increment</span>() <span class="hljs-comment">// 2</span><br><span class="hljs-title function_">increment</span>() <span class="hljs-comment">// 3</span><br></code></pre></td></tr></table></figure>

<p>这里 <code>increment</code> 一直可以访问 <code>outer</code> 中的 <code>count</code>。</p>
<p>因此：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">outer()<br>  ↓<br>创建 count<br>  ↓<br>创建内部函数<br>  ↓<br>返回内部函数<br>  ↓<br>outer 执行结束<br>  ↓<br>increment 仍然可以访问 count<br></code></pre></td></tr></table></figure>

<p>这就是闭包。</p>
<hr>
<h1 id="二、理解闭包之前，先理解作用域"><a href="#二、理解闭包之前，先理解作用域" class="headerlink" title="二、理解闭包之前，先理解作用域"></a>二、理解闭包之前，先理解作用域</h1><p>闭包并不是一个孤立的概念，它建立在 JavaScript 的<strong>词法作用域</strong>之上。</p>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> name = <span class="hljs-string">&#x27;global&#x27;</span><br><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">outer</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">const</span> name = <span class="hljs-string">&#x27;outer&#x27;</span><br><br>  <span class="hljs-keyword">function</span> <span class="hljs-title function_">inner</span>(<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(name)<br>  &#125;<br><br>  <span class="hljs-title function_">inner</span>()<br>&#125;<br><br><span class="hljs-title function_">outer</span>()<br></code></pre></td></tr></table></figure>

<p>输出：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">outer<br></code></pre></td></tr></table></figure>

<p>为什么？</p>
<p>因为 <code>inner</code> 定义在 <code>outer</code> 内部。</p>
<p>当 <code>inner</code> 查找：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">name<br></code></pre></td></tr></table></figure>

<p>时，会按照它定义时的作用域关系进行查找：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">inner<br> ↓<br>outer<br> ↓<br>global<br></code></pre></td></tr></table></figure>

<p>这就是<strong>作用域链</strong>。</p>
<hr>
<h1 id="三、作用域链是什么？"><a href="#三、作用域链是什么？" class="headerlink" title="三、作用域链是什么？"></a>三、作用域链是什么？</h1><p>可以把作用域链理解成：</p>
<blockquote>
<p><strong>变量查找时，一层一层向外寻找的路径。</strong></p>
</blockquote>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> a = <span class="hljs-number">1</span><br><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">outer</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">const</span> b = <span class="hljs-number">2</span><br><br>  <span class="hljs-keyword">function</span> <span class="hljs-title function_">inner</span>(<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-keyword">const</span> c = <span class="hljs-number">3</span><br><br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(a)<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(b)<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(c)<br>  &#125;<br><br>  <span class="hljs-title function_">inner</span>()<br>&#125;<br><br><span class="hljs-title function_">outer</span>()<br></code></pre></td></tr></table></figure>

<p><code>inner</code> 中访问三个变量：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">a<br>b<br>c<br></code></pre></td></tr></table></figure>

<p>查找过程大致可以理解为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">inner 作用域<br> ├── c ✅<br> │<br> ↓<br><br>outer 作用域<br> ├── b ✅<br> │<br> ↓<br><br>全局作用域<br> ├── a ✅<br></code></pre></td></tr></table></figure>

<p>所以：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">inner<br></code></pre></td></tr></table></figure>

<p>能够访问：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">inner 自己的变量<br>↓<br>outer 的变量<br>↓<br>更外层作用域的变量<br>↓<br>全局变量<br></code></pre></td></tr></table></figure>

<p>但是反过来不成立。</p>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">outer</span>(<span class="hljs-params"></span>) &#123;<br><br>  <span class="hljs-keyword">function</span> <span class="hljs-title function_">inner</span>(<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-keyword">const</span> name = <span class="hljs-string">&#x27;Tom&#x27;</span><br>  &#125;<br><br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(name)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这里：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(name)<br></code></pre></td></tr></table></figure>

<p>无法访问 <code>inner</code> 中的 <code>name</code>。</p>
<p>因为作用域链的方向是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">内层 → 外层<br></code></pre></td></tr></table></figure>

<p>而不是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">外层 → 内层<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="四、闭包是怎么形成的？"><a href="#四、闭包是怎么形成的？" class="headerlink" title="四、闭包是怎么形成的？"></a>四、闭包是怎么形成的？</h1><p>看一个经典例子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">createCounter</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">let</span> count = <span class="hljs-number">0</span><br><br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123;<br>    count++<br><br>    <span class="hljs-keyword">return</span> count<br>  &#125;<br>&#125;<br><br><span class="hljs-keyword">const</span> counter = <span class="hljs-title function_">createCounter</span>()<br></code></pre></td></tr></table></figure>

<p>此时发生了什么？</p>
<h2 id="1-执行-createCounter"><a href="#1-执行-createCounter" class="headerlink" title="1. 执行 createCounter"></a>1. 执行 createCounter</h2><p>创建：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">count = <span class="hljs-number">0</span><br></code></pre></td></tr></table></figure>

<p>同时创建内部函数：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123;<br>  count++<br>  <span class="hljs-keyword">return</span> count<br>&#125;<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="2-返回内部函数"><a href="#2-返回内部函数" class="headerlink" title="2. 返回内部函数"></a>2. 返回内部函数</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123;<br>  count++<br>  <span class="hljs-keyword">return</span> count<br>&#125;<br></code></pre></td></tr></table></figure>

<p>然后：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> counter = <span class="hljs-title function_">createCounter</span>()<br></code></pre></td></tr></table></figure>

<p>此时：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">counter<br>  ↓<br>内部函数<br>  ↓<br>createCounter 的作用域<br>  ↓<br>count<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="3-createCounter-执行结束"><a href="#3-createCounter-执行结束" class="headerlink" title="3. createCounter 执行结束"></a>3. createCounter 执行结束</h2><p>虽然：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">createCounter</span>()<br></code></pre></td></tr></table></figure>

<p>已经执行结束，但是内部函数还在被：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">counter<br></code></pre></td></tr></table></figure>

<p>引用。</p>
<p>因此它依赖的外部变量：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">count<br></code></pre></td></tr></table></figure>

<p>不能被简单地回收。</p>
<p>所以：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">counter</span>()<br></code></pre></td></tr></table></figure>

<p>仍然能够访问：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">count<br></code></pre></td></tr></table></figure>

<p>并修改它。</p>
<hr>
<h1 id="五、闭包的核心：函数-外部作用域"><a href="#五、闭包的核心：函数-外部作用域" class="headerlink" title="五、闭包的核心：函数 + 外部作用域"></a>五、闭包的核心：函数 + 外部作用域</h1><p>可以把闭包简单抽象成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">闭包<br>=<br>函数<br>+<br>函数能够访问的外部词法环境<br></code></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">outer</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">const</span> name = <span class="hljs-string">&#x27;Tom&#x27;</span><br><br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">inner</span>(<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(name)<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这里：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">inner 函数<br>+<br>outer 作用域<br></code></pre></td></tr></table></figure>

<p>形成了闭包关系。</p>
<p>因此不要把闭包理解成一个特殊的语法。</p>
<p>它实际上是：</p>
<blockquote>
<p><strong>函数与其词法作用域之间的一种关系。</strong></p>
</blockquote>
<hr>
<h1 id="六、为什么函数执行结束后，变量还存在？"><a href="#六、为什么函数执行结束后，变量还存在？" class="headerlink" title="六、为什么函数执行结束后，变量还存在？"></a>六、为什么函数执行结束后，变量还存在？</h1><p>这是很多人第一次理解闭包时最容易产生疑惑的地方。</p>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">outer</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">let</span> count = <span class="hljs-number">0</span><br><br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-keyword">return</span> ++count<br>  &#125;<br>&#125;<br><br><span class="hljs-keyword">const</span> fn = <span class="hljs-title function_">outer</span>()<br></code></pre></td></tr></table></figure>

<p>有人会认为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">outer 执行结束<br>↓<br>count 应该销毁<br></code></pre></td></tr></table></figure>

<p>但实际上不能简单这么理解。</p>
<p>因为：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> fn = <span class="hljs-title function_">outer</span>()<br></code></pre></td></tr></table></figure>

<p>得到的函数仍然需要：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">count<br></code></pre></td></tr></table></figure>

<p>所以 JavaScript 的垃圾回收机制会判断：</p>
<blockquote>
<p>这个变量是否还有对象引用？</p>
</blockquote>
<p>如果闭包仍然引用它，就不能回收。</p>
<p>因此：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">outer()<br>  ↓<br>创建 count<br>  ↓<br>创建 inner<br>  ↓<br>inner 引用 count<br>  ↓<br>返回 inner<br>  ↓<br>fn 引用 inner<br>  ↓<br>count 仍然可达<br></code></pre></td></tr></table></figure>

<p>所以 <code>count</code> 依然存在。</p>
<hr>
<h1 id="七、闭包最典型的用途：保存状态"><a href="#七、闭包最典型的用途：保存状态" class="headerlink" title="七、闭包最典型的用途：保存状态"></a>七、闭包最典型的用途：保存状态</h1><p>闭包非常适合保存一些需要长期存在的状态。</p>
<p>例如计数器：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">createCounter</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">let</span> count = <span class="hljs-number">0</span><br><br>  <span class="hljs-keyword">return</span> &#123;<br>    <span class="hljs-title function_">increment</span>(<span class="hljs-params"></span>) &#123;<br>      count++<br>    &#125;,<br><br>    <span class="hljs-title function_">decrement</span>(<span class="hljs-params"></span>) &#123;<br>      count--<br>    &#125;,<br><br>    <span class="hljs-title function_">getCount</span>(<span class="hljs-params"></span>) &#123;<br>      <span class="hljs-keyword">return</span> count<br>    &#125;<br>  &#125;<br>&#125;<br><br><span class="hljs-keyword">const</span> counter = <span class="hljs-title function_">createCounter</span>()<br><br>counter.<span class="hljs-title function_">increment</span>()<br>counter.<span class="hljs-title function_">increment</span>()<br><br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(counter.<span class="hljs-title function_">getCount</span>()) <span class="hljs-comment">// 2</span><br></code></pre></td></tr></table></figure>

<p>这里：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">count<br></code></pre></td></tr></table></figure>

<p>没有暴露出去。</p>
<p>外部无法直接：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">counter.<span class="hljs-property">count</span> = <span class="hljs-number">100</span><br></code></pre></td></tr></table></figure>

<p>修改真正的 <code>count</code>。</p>
<p>只有通过：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">increment</span>()<br><span class="hljs-title function_">decrement</span>()<br><span class="hljs-title function_">getCount</span>()<br></code></pre></td></tr></table></figure>

<p>操作。</p>
<p>因此闭包可以帮助实现一种简单的：</p>
<blockquote>
<p><strong>数据私有化。</strong></p>
</blockquote>
<hr>
<h1 id="八、闭包实现数据私有化"><a href="#八、闭包实现数据私有化" class="headerlink" title="八、闭包实现数据私有化"></a>八、闭包实现数据私有化</h1><p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">createUser</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">let</span> username = <span class="hljs-string">&#x27;Tom&#x27;</span><br>  <span class="hljs-keyword">let</span> password = <span class="hljs-string">&#x27;123456&#x27;</span><br><br>  <span class="hljs-keyword">return</span> &#123;<br>    <span class="hljs-title function_">getUsername</span>(<span class="hljs-params"></span>) &#123;<br>      <span class="hljs-keyword">return</span> username<br>    &#125;,<br><br>    <span class="hljs-title function_">changePassword</span>(<span class="hljs-params">newPassword</span>) &#123;<br>      password = newPassword<br>    &#125;<br>  &#125;<br>&#125;<br><br><span class="hljs-keyword">const</span> user = <span class="hljs-title function_">createUser</span>()<br><br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(user.<span class="hljs-title function_">getUsername</span>())<br></code></pre></td></tr></table></figure>

<p>外部只能通过提供的方法访问数据。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">            createUser<br>                │<br>    ┌───────────┴───────────┐<br>    ↓                       ↓<br>username                password<br>    │                       │<br>    └───────────┬───────────┘<br>                ↓<br>             闭包<br>                ↓<br>      getUsername / changePassword<br></code></pre></td></tr></table></figure>

<p>这和直接暴露：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> user = &#123;<br>  <span class="hljs-attr">username</span>: <span class="hljs-string">&#x27;Tom&#x27;</span>,<br>  <span class="hljs-attr">password</span>: <span class="hljs-string">&#x27;123456&#x27;</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>相比，多了一层控制。</p>
<hr>
<h1 id="九、闭包与工厂函数"><a href="#九、闭包与工厂函数" class="headerlink" title="九、闭包与工厂函数"></a>九、闭包与工厂函数</h1><p>闭包经常和工厂函数一起使用。</p>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">createPerson</span>(<span class="hljs-params">name</span>) &#123;<br>  <span class="hljs-keyword">return</span> &#123;<br>    <span class="hljs-title function_">sayHello</span>(<span class="hljs-params"></span>) &#123;<br>      <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">`Hello, <span class="hljs-subst">$&#123;name&#125;</span>`</span>)<br>    &#125;<br>  &#125;<br>&#125;<br><br><span class="hljs-keyword">const</span> person1 = <span class="hljs-title function_">createPerson</span>(<span class="hljs-string">&#x27;Tom&#x27;</span>)<br><span class="hljs-keyword">const</span> person2 = <span class="hljs-title function_">createPerson</span>(<span class="hljs-string">&#x27;Jack&#x27;</span>)<br><br>person1.<span class="hljs-title function_">sayHello</span>()<br>person2.<span class="hljs-title function_">sayHello</span>()<br></code></pre></td></tr></table></figure>

<p>输出：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Hello, Tom<br>Hello, Jack<br></code></pre></td></tr></table></figure>

<p>两个对象拥有不同的状态。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">person1<br>  ↓<br>sayHello<br>  ↓<br>name = Tom<br><br><br>person2<br>  ↓<br>sayHello<br>  ↓<br>name = Jack<br></code></pre></td></tr></table></figure>

<p>这也是闭包非常典型的应用方式。</p>
<hr>
<h1 id="十、闭包与定时器"><a href="#十、闭包与定时器" class="headerlink" title="十、闭包与定时器"></a>十、闭包与定时器</h1><p>闭包在异步代码中也非常常见。</p>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">createTimer</span>(<span class="hljs-params">name</span>) &#123;<br>  <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(name)<br>  &#125;, <span class="hljs-number">1000</span>)<br>&#125;<br><br><span class="hljs-title function_">createTimer</span>(<span class="hljs-string">&#x27;任务完成&#x27;</span>)<br></code></pre></td></tr></table></figure>

<p>这里：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(name)<br>&#125;, <span class="hljs-number">1000</span>)<br></code></pre></td></tr></table></figure>

<p>中的回调函数使用了外部的：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">name<br></code></pre></td></tr></table></figure>

<p>因此回调函数形成了对外部变量的引用。</p>
<p>即使：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">createTimer</span>()<br></code></pre></td></tr></table></figure>

<p>已经执行结束：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">name<br></code></pre></td></tr></table></figure>

<p>依然需要被回调函数访问。</p>
<p>所以它会继续存在到不再需要为止。</p>
<hr>
<h1 id="十一、经典问题：循环-闭包"><a href="#十一、经典问题：循环-闭包" class="headerlink" title="十一、经典问题：循环 + 闭包"></a>十一、经典问题：循环 + 闭包</h1><p>闭包最经典的坑之一，就是循环中的变量。</p>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">for</span> (<span class="hljs-keyword">var</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">3</span>; i++) &#123;<br>  <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(i)<br>  &#125;, <span class="hljs-number">0</span>)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>结果：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">3<br>3<br>3<br></code></pre></td></tr></table></figure>

<p>为什么？</p>
<p>因为 <code>var</code> 不会为每一次循环创建一个新的块级作用域。</p>
<p>这些回调函数访问的是同一个：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">i<br></code></pre></td></tr></table></figure>

<p>循环结束以后：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">i = 3<br></code></pre></td></tr></table></figure>

<p>所以定时器执行时，看到的都是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">3<br></code></pre></td></tr></table></figure>

<p>可以理解为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">      i<br>      │<br> ┌────┼────┐<br> ↓    ↓    ↓<br>回调1 回调2 回调3<br></code></pre></td></tr></table></figure>

<p>三个回调共享同一个 <code>i</code>。</p>
<hr>
<h1 id="十二、使用-let-为什么可以解决？"><a href="#十二、使用-let-为什么可以解决？" class="headerlink" title="十二、使用 let 为什么可以解决？"></a>十二、使用 let 为什么可以解决？</h1><p>把：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">var</span><br></code></pre></td></tr></table></figure>

<p>换成：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">let</span><br></code></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">for</span> (<span class="hljs-keyword">let</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">3</span>; i++) &#123;<br>  <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(i)<br>  &#125;, <span class="hljs-number">0</span>)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>结果：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">0<br>1<br>2<br></code></pre></td></tr></table></figure>

<p>因为 <code>let</code> 具有块级作用域。</p>
<p>每一次循环都可以理解为拥有自己的变量绑定：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">第 1 次循环<br>i = 0<br>  ↓<br>回调1<br><br><br>第 2 次循环<br>i = 1<br>  ↓<br>回调2<br><br><br>第 3 次循环<br>i = 2<br>  ↓<br>回调3<br></code></pre></td></tr></table></figure>

<p>因此每个回调访问的是对应的 <code>i</code>。</p>
<hr>
<h1 id="十三、也可以通过函数创建独立作用域"><a href="#十三、也可以通过函数创建独立作用域" class="headerlink" title="十三、也可以通过函数创建独立作用域"></a>十三、也可以通过函数创建独立作用域</h1><p>在 <code>let</code> 出现之前，可以利用闭包解决这个问题：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">for</span> (<span class="hljs-keyword">var</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">3</span>; i++) &#123;<br>  (<span class="hljs-keyword">function</span> (<span class="hljs-params">index</span>) &#123;<br>    <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>      <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(index)<br>    &#125;, <span class="hljs-number">0</span>)<br>  &#125;)(i)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>输出：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">0<br>1<br>2<br></code></pre></td></tr></table></figure>

<p>这里通过立即执行函数：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">(<span class="hljs-keyword">function</span> (<span class="hljs-params">index</span>) &#123;<br><br>&#125;)(i)<br></code></pre></td></tr></table></figure>

<p>每次创建一个新的函数作用域。</p>
<p>因此：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">第一次<br>index = 0<br><br><br>第二次<br>index = 1<br><br><br>第三次<br>index = 2<br></code></pre></td></tr></table></figure>

<p>每个回调都闭包保存了自己的 <code>index</code>。</p>
<p>不过现代 JavaScript 中通常直接使用 <code>let</code>，代码更加简单。</p>
<hr>
<h1 id="十四、闭包不等于“变量永远不会被回收”"><a href="#十四、闭包不等于“变量永远不会被回收”" class="headerlink" title="十四、闭包不等于“变量永远不会被回收”"></a>十四、闭包不等于“变量永远不会被回收”</h1><p>这是理解闭包时一个很重要的误区。</p>
<p>闭包确实可能延长变量的生命周期，但并不是：</p>
<blockquote>
<p>只要形成闭包，变量就永远存在。</p>
</blockquote>
<p>垃圾回收机制关注的是：</p>
<blockquote>
<p><strong>对象是否仍然可达。</strong></p>
</blockquote>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">outer</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">const</span> data = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Array</span>(<span class="hljs-number">1000000</span>)<br><br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(data.<span class="hljs-property">length</span>)<br>  &#125;<br>&#125;<br><br><span class="hljs-keyword">let</span> fn = <span class="hljs-title function_">outer</span>()<br></code></pre></td></tr></table></figure>

<p>此时：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">fn<br></code></pre></td></tr></table></figure>

<p>仍然引用内部函数。</p>
<p>内部函数又引用：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">data<br></code></pre></td></tr></table></figure>

<p>所以：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">fn<br> ↓<br>内部函数<br> ↓<br>data<br></code></pre></td></tr></table></figure>

<p><code>data</code> 仍然可达。</p>
<p>如果后来：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">fn = <span class="hljs-literal">null</span><br></code></pre></td></tr></table></figure>

<p>那么这条引用链可能被断开。</p>
<p>如果没有其他引用：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">data<br></code></pre></td></tr></table></figure>

<p>就有机会被垃圾回收。</p>
<p>所以：</p>
<blockquote>
<p><strong>闭包不是内存泄漏，错误地长期持有闭包引用才可能导致内存无法及时释放。</strong></p>
</blockquote>
<hr>
<h1 id="十五、闭包可能带来的内存问题"><a href="#十五、闭包可能带来的内存问题" class="headerlink" title="十五、闭包可能带来的内存问题"></a>十五、闭包可能带来的内存问题</h1><p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">let</span> fn<br><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">create</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">const</span> bigData = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Array</span>(<span class="hljs-number">1000000</span>).<span class="hljs-title function_">fill</span>(<span class="hljs-string">&#x27;data&#x27;</span>)<br><br>  fn = <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(bigData.<span class="hljs-property">length</span>)<br>  &#125;<br>&#125;<br><br><span class="hljs-title function_">create</span>()<br></code></pre></td></tr></table></figure>

<p>此时：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">fn<br> ↓<br>匿名函数<br> ↓<br>bigData<br></code></pre></td></tr></table></figure>

<p>只要：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">fn<br></code></pre></td></tr></table></figure>

<p>一直存在：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">bigData<br></code></pre></td></tr></table></figure>

<p>就可能一直保持可达状态。</p>
<p>如果这个闭包被长期保存，尤其是：</p>
<ul>
<li>全局变量</li>
<li>定时器</li>
<li>事件监听器</li>
<li>缓存</li>
<li>长生命周期对象</li>
</ul>
<p>就需要注意引用是否应该继续存在。</p>
<p>例如事件监听：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> button = <span class="hljs-variable language_">document</span>.<span class="hljs-title function_">querySelector</span>(<span class="hljs-string">&#x27;#button&#x27;</span>)<br><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">createHandler</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">const</span> data = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Array</span>(<span class="hljs-number">1000000</span>).<span class="hljs-title function_">fill</span>(<span class="hljs-string">&#x27;data&#x27;</span>)<br><br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(data.<span class="hljs-property">length</span>)<br>  &#125;<br>&#125;<br><br><span class="hljs-keyword">const</span> handler = <span class="hljs-title function_">createHandler</span>()<br><br>button.<span class="hljs-title function_">addEventListener</span>(<span class="hljs-string">&#x27;click&#x27;</span>, handler)<br></code></pre></td></tr></table></figure>

<p>如果组件销毁时仍然保留监听器，就可能让相关对象继续保持引用。</p>
<p>因此在合适的时候应该解除监听：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">button.<span class="hljs-title function_">removeEventListener</span>(<span class="hljs-string">&#x27;click&#x27;</span>, handler)<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="十六、闭包的常见应用场景"><a href="#十六、闭包的常见应用场景" class="headerlink" title="十六、闭包的常见应用场景"></a>十六、闭包的常见应用场景</h1><p>闭包并不是为了“炫技”，实际开发中非常常见。</p>
<h2 id="1-保存状态"><a href="#1-保存状态" class="headerlink" title="1. 保存状态"></a>1. 保存状态</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">createCounter</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">let</span> count = <span class="hljs-number">0</span><br><br>  <span class="hljs-keyword">return</span> <span class="hljs-function">() =&gt;</span> ++count<br>&#125;<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="2-数据私有化"><a href="#2-数据私有化" class="headerlink" title="2. 数据私有化"></a>2. 数据私有化</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">createUser</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">let</span> password = <span class="hljs-string">&#x27;123456&#x27;</span><br><br>  <span class="hljs-keyword">return</span> &#123;<br>    <span class="hljs-title function_">getPassword</span>(<span class="hljs-params"></span>) &#123;<br>      <span class="hljs-keyword">return</span> password<br>    &#125;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="3-工厂函数"><a href="#3-工厂函数" class="headerlink" title="3. 工厂函数"></a>3. 工厂函数</h2><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">createPerson</span>(<span class="hljs-params">name</span>) &#123;<br>  <span class="hljs-keyword">return</span> &#123;<br>    <span class="hljs-title function_">sayHello</span>(<span class="hljs-params"></span>) &#123;<br>      <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(name)<br>    &#125;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="4-防抖"><a href="#4-防抖" class="headerlink" title="4. 防抖"></a>4. 防抖</h2><p>防抖函数本质上也大量使用了闭包保存定时器：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">debounce</span>(<span class="hljs-params">fn, delay</span>) &#123;<br>  <span class="hljs-keyword">let</span> timer = <span class="hljs-literal">null</span><br><br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> (<span class="hljs-params">...args</span>) &#123;<br>    <span class="hljs-built_in">clearTimeout</span>(timer)<br><br>    timer = <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>      fn.<span class="hljs-title function_">apply</span>(<span class="hljs-variable language_">this</span>, args)<br>    &#125;, delay)<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这里：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">timer<br></code></pre></td></tr></table></figure>

<p>被返回的函数持续引用。</p>
<p>因此每次调用时，都可以访问之前的：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">timer<br></code></pre></td></tr></table></figure>

<p>这就是闭包。</p>
<hr>
<h2 id="5-节流"><a href="#5-节流" class="headerlink" title="5. 节流"></a>5. 节流</h2><p>节流同样可以利用闭包保存状态：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">throttle</span>(<span class="hljs-params">fn, delay</span>) &#123;<br>  <span class="hljs-keyword">let</span> lastTime = <span class="hljs-number">0</span><br><br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> (<span class="hljs-params">...args</span>) &#123;<br>    <span class="hljs-keyword">const</span> now = <span class="hljs-title class_">Date</span>.<span class="hljs-title function_">now</span>()<br><br>    <span class="hljs-keyword">if</span> (now - lastTime &gt;= delay) &#123;<br>      lastTime = now<br>      fn.<span class="hljs-title function_">apply</span>(<span class="hljs-variable language_">this</span>, args)<br>    &#125;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这里：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">lastTime<br></code></pre></td></tr></table></figure>

<p>就是通过闭包保存的状态。</p>
<hr>
<h1 id="十七、闭包与模块化"><a href="#十七、闭包与模块化" class="headerlink" title="十七、闭包与模块化"></a>十七、闭包与模块化</h1><p>闭包还可以实现简单的模块模式。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> counter = (<span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-keyword">let</span> count = <span class="hljs-number">0</span><br><br>  <span class="hljs-keyword">return</span> &#123;<br>    <span class="hljs-title function_">increment</span>(<span class="hljs-params"></span>) &#123;<br>      count++<br>    &#125;,<br><br>    <span class="hljs-title function_">getCount</span>(<span class="hljs-params"></span>) &#123;<br>      <span class="hljs-keyword">return</span> count<br>    &#125;<br>  &#125;<br>&#125;)()<br></code></pre></td></tr></table></figure>

<p>使用：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">counter.<span class="hljs-title function_">increment</span>()<br>counter.<span class="hljs-title function_">increment</span>()<br><br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(counter.<span class="hljs-title function_">getCount</span>())<br></code></pre></td></tr></table></figure>

<p>但是外部无法直接访问：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">count<br></code></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(counter.<span class="hljs-property">count</span>)<br></code></pre></td></tr></table></figure>

<p>得到：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">undefined<br></code></pre></td></tr></table></figure>

<p>因为 <code>count</code> 并不是对象公开的属性。</p>
<hr>
<h1 id="十八、闭包和普通函数有什么区别？"><a href="#十八、闭包和普通函数有什么区别？" class="headerlink" title="十八、闭包和普通函数有什么区别？"></a>十八、闭包和普通函数有什么区别？</h1><p>并不是所有函数都需要特别强调“闭包”。</p>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">add</span>(<span class="hljs-params">a, b</span>) &#123;<br>  <span class="hljs-keyword">return</span> a + b<br>&#125;<br></code></pre></td></tr></table></figure>

<p>它当然也存在函数自己的作用域。</p>
<p>但我们通常不会特意把它称为闭包。</p>
<p>闭包真正值得关注的是：</p>
<blockquote>
<p><strong>函数离开了原来的作用域之后，仍然依赖并访问那个外部作用域中的变量。</strong></p>
</blockquote>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">outer</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">const</span> name = <span class="hljs-string">&#x27;Tom&#x27;</span><br><br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">inner</span>(<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(name)<br>  &#125;<br>&#125;<br><br><span class="hljs-keyword">const</span> fn = <span class="hljs-title function_">outer</span>()<br><br><span class="hljs-title function_">fn</span>()<br></code></pre></td></tr></table></figure>

<p>这里的 <code>inner</code> 就具有典型的闭包特征。</p>
<hr>
<h1 id="十九、用一张图理解闭包"><a href="#十九、用一张图理解闭包" class="headerlink" title="十九、用一张图理解闭包"></a>十九、用一张图理解闭包</h1><p>整个过程可以简化成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">function outer() &#123;<br>    const count = 0<br><br>    return function inner() &#123;<br>        console.log(count)<br>    &#125;<br>&#125;<br>        │<br>        │ 执行<br>        ↓<br>┌─────────────────────┐<br>│ outer 作用域         │<br>│                     │<br>│ count = 0           │<br>│                     │<br>│ inner ──────────────┼────┐<br>└─────────────────────┘    │<br>                           │<br>                           ↓<br>                     ┌───────────┐<br>                     │ inner函数 │<br>                     └───────────┘<br>                           │<br>                           │ 引用<br>                           ↓<br>                         count<br></code></pre></td></tr></table></figure>

<p>最终：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> fn = <span class="hljs-title function_">outer</span>()<br></code></pre></td></tr></table></figure>

<p>得到：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">fn<br> ↓<br>inner<br> ↓<br>outer 作用域<br> ↓<br>count<br></code></pre></td></tr></table></figure>

<p>这就是闭包关系。</p>
<hr>
<h1 id="二十、如何真正理解闭包？"><a href="#二十、如何真正理解闭包？" class="headerlink" title="二十、如何真正理解闭包？"></a>二十、如何真正理解闭包？</h1><p>可以从三个层次去理解。</p>
<h2 id="第一层：现象"><a href="#第一层：现象" class="headerlink" title="第一层：现象"></a>第一层：现象</h2><p>函数可以访问定义位置外部的变量：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">outer</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">let</span> count = <span class="hljs-number">0</span><br><br>  <span class="hljs-keyword">return</span> <span class="hljs-function">() =&gt;</span> ++count<br>&#125;<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="第二层：原因"><a href="#第二层：原因" class="headerlink" title="第二层：原因"></a>第二层：原因</h2><p>JavaScript 使用词法作用域。</p>
<p>函数在定义的时候，就确定了自己能够访问哪些外部作用域。</p>
<p>因此内部函数即使之后被返回，也依然可以访问外部变量。</p>
<hr>
<h2 id="第三层：结果"><a href="#第三层：结果" class="headerlink" title="第三层：结果"></a>第三层：结果</h2><p>当外部作用域中的变量仍然被内部函数引用时，这些变量的生命周期可能被延长。</p>
<p>因此闭包可以：</p>
<ul>
<li>保存状态</li>
<li>实现数据私有化</li>
<li>创建独立作用域</li>
<li>实现防抖、节流等工具函数</li>
<li>构建工厂函数和模块模式</li>
</ul>
<p>同时也需要注意：</p>
<ul>
<li>不要无意义地长期保存闭包</li>
<li>注意事件监听器的解绑</li>
<li>注意定时器的清理</li>
<li>注意大型对象是否被闭包长期引用</li>
</ul>
<hr>
<h1 id="二十一、总结"><a href="#二十一、总结" class="headerlink" title="二十一、总结"></a>二十一、总结</h1><p>闭包可以浓缩成一句话：</p>
<blockquote>
<p><strong>闭包就是函数与其词法作用域之间形成的持续引用关系，使函数即使离开原来的执行环境，也仍然能够访问其中的变量。</strong></p>
</blockquote>
<p>理解闭包时，不需要死记：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">函数 + 外部变量 = 闭包<br></code></pre></td></tr></table></figure>

<p>更重要的是理解整个过程：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">词法作用域<br>    ↓<br>作用域链<br>    ↓<br>函数可以访问外部变量<br>    ↓<br>内部函数被返回 / 保存<br>    ↓<br>仍然引用外部作用域<br>    ↓<br>形成闭包<br>    ↓<br>外部变量生命周期可能延长<br></code></pre></td></tr></table></figure>

<p>而闭包真正有价值的地方，就是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">保存状态<br>    +<br>控制数据访问<br>    +<br>延长变量生命周期<br></code></pre></td></tr></table></figure>

<p>因此，闭包并不是 JavaScript 中一个“特殊的语法”，而是<strong>作用域、函数以及引用关系共同产生的一种语言机制</strong>。</p>
<p>当能够从这个角度理解闭包之后，很多看起来独立的问题——例如防抖、节流、循环中的异步回调、数据私有化、工厂函数——其实都可以归结到同一个概念上。</p>
<hr>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>闭包</tag>
      </tags>
  </entry>
  <entry>
    <title>事件循环全景：调用栈、宏任务、微任务与渲染帧</title>
    <url>/2026/09/09/javascript-event-loop/</url>
    <content><![CDATA[<p>我对事件循环的误解持续了很多年：最早以为「setTimeout 就是开个线程」，后来背下口诀「宏先微后」，直到连翻三次车：</p>
<ol>
<li><strong>loading 不出现</strong>：点击 → 显示转圈 → 同步计算 300ms → 隐藏转圈。转圈从头到尾没出现过。</li>
<li><strong>顺序背不动</strong>：<code>Promise.then</code> 和 <code>setTimeout(fn, 0)</code> 谁先我能背，但代码里混进一个 <code>await</code>，顺序立刻说不清。</li>
<li><strong>Node 玄学</strong>：同一段代码里 <code>setTimeout(fn, 0)</code> 和 <code>setImmediate</code>，先跑谁居然「看机器心情」。</li>
</ol>
<p>后来才明白，这是同一个机制的三种表现。<strong>事件循环不是 JavaScript 的特性，而是宿主环境（浏览器、Node）的调度机制</strong>——JS 引擎只负责一块调用栈，「什么时候跑哪段异步代码」由宿主说了算。</p>
<span id="more"></span>

<h2 id="一、发动机：调用栈、任务队列与循环算法"><a href="#一、发动机：调用栈、任务队列与循环算法" class="headerlink" title="一、发动机：调用栈、任务队列与循环算法"></a>一、发动机：调用栈、任务队列与循环算法</h2><p>先立地基：JS 单线程，同一时刻调用栈里只有一段代码在跑，同步代码执行到栈空为止。「异步」从来不是并行，而是<strong>推迟</strong>。推迟出去的部分由宿主收着（定时器、DOM 事件、网络都不在 JS 线程上干等），时机一到，宿主把回调包装成一个<strong>任务</strong>（task，俗称宏任务）放进任务队列，事件循环按固定算法一个个调回 JS 线程：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">while (true) &#123;<br>  task = 任务队列中最老的一个任务;      // 一轮只取一个<br>  执行 task;                           // 调用栈随之清空<br>  while (微任务队列非空) &#123;<br>    执行最老的微任务;                   // 清空过程中新入队的也要执行<br>  &#125;<br>  if (本轮有渲染机会) &#123;                 // 只有浏览器有，大致对齐垂直同步信号<br>    执行 requestAnimationFrame 回调;<br>    样式计算 → 布局 → 绘制 → 合成;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>最小的例子：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;script start&#x27;</span>);<br><span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;timeout&#x27;</span>), <span class="hljs-number">0</span>);<br><span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">resolve</span>().<span class="hljs-title function_">then</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;promise&#x27;</span>));<br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;script end&#x27;</span>);<br><br><span class="hljs-comment">// 输出：script start → script end → promise → timeout</span><br></code></pre></td></tr></table></figure>

<p>整段脚本是<strong>第一个宏任务</strong>：同步执行到末尾，<code>then</code> 回调入微任务队列、<code>setTimeout</code> 回调入宏任务队列；脚本任务结束后先清微任务（<code>promise</code>），下一轮循环才取宏任务（<code>timeout</code>）。</p>
<h3 id="坑-1：「宏任务队列」其实不止一条"><a href="#坑-1：「宏任务队列」其实不止一条" class="headerlink" title="坑 1：「宏任务队列」其实不止一条"></a>坑 1：「宏任务队列」其实不止一条</h3><p>任务队列按<strong>任务源</strong>分多条（定时器一条、DOM 事件一条、网络一条），每轮从哪个源取由浏览器决定。所以「宏任务 vs 微任务」的准确区别不是「两个队列谁在前」，而是<strong>调度方式</strong>：宏任务一轮取一个、任务之间可插渲染；微任务每个宏任务之后<strong>必清空</strong>。后面所有的「玄学顺序」都是这条的推论。</p>
<h2 id="二、微任务：每个宏任务后的「必清空」清单"><a href="#二、微任务：每个宏任务后的「必清空」清单" class="headerlink" title="二、微任务：每个宏任务后的「必清空」清单"></a>二、微任务：每个宏任务后的「必清空」清单</h2><p>微任务来源一张表列全：</p>
<table>
<thead>
<tr>
<th>来源</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>then / catch / finally</code></td>
<td>回调进微任务队列</td>
</tr>
<tr>
<td><code>await</code> 之后的续体</td>
<td>本质就是 then 回调</td>
</tr>
<tr>
<td><code>queueMicrotask(fn)</code></td>
<td>显式入队</td>
</tr>
<tr>
<td><code>MutationObserver</code></td>
<td>DOM 变更回调（Vue 时代前的 nextTick 实现）</td>
</tr>
</tbody></table>
<p>两条铁律：<strong>每个宏任务结束后立刻清空</strong>（清空过程中新入队的也要清）；<strong>清完之前没有渲染，也没有下一个宏任务</strong>。</p>
<h3 id="await-的本质：微任务的语法糖"><a href="#await-的本质：微任务的语法糖" class="headerlink" title="await 的本质：微任务的语法糖"></a>await 的本质：微任务的语法糖</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">a</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;a-1&#x27;</span>);<br>  <span class="hljs-keyword">await</span> <span class="hljs-title function_">b</span>();              <span class="hljs-comment">// 暂停点：a 把「后面的代码」交给微任务，自己先返回</span><br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;a-2&#x27;</span>);<br>&#125;<br><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">b</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;b&#x27;</span>);<br>&#125;<br><br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;start&#x27;</span>);<br><span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;timeout&#x27;</span>), <span class="hljs-number">0</span>);<br><span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">resolve</span>()<br>  .<span class="hljs-title function_">then</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;then-1&#x27;</span>))<br>  .<span class="hljs-title function_">then</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;then-2&#x27;</span>));<br><span class="hljs-title function_">a</span>();<br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;end&#x27;</span>);<br><br><span class="hljs-comment">// 输出：start → a-1 → b → end → then-1 → a-2 → then-2 → timeout</span><br></code></pre></td></tr></table></figure>

<p>拆解：<code>a()</code> 同步执行到第一个 await——打印 <code>a-1</code>，<code>b()</code> 也是同步调用打印 <code>b</code>；<code>a-2</code> 及之后的部分作为「b 结果的 then 回调」进微任务队列；同步代码继续打印 <code>end</code>；清微任务：<code>then-1</code>（执行后挂出 <code>then-2</code>）→ <code>a-2</code> → <code>then-2</code>；最后宏任务 <code>timeout</code>。记法一句：<strong>await 后面的代码 ≈ 写在 then 里的代码</strong>——在哪个 await 暂停，就从那里进微任务队列。</p>
<h3 id="坑-2：混进-await-之后顺序就背不动了？自测一题"><a href="#坑-2：混进-await-之后顺序就背不动了？自测一题" class="headerlink" title="坑 2：混进 await 之后顺序就背不动了？自测一题"></a>坑 2：混进 await 之后顺序就背不动了？自测一题</h3><p>不背口诀，纯用第一节的算法推：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;1&#x27;</span>);<br><span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;2&#x27;</span>);<br>  <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">resolve</span>().<span class="hljs-title function_">then</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;3&#x27;</span>));<br>&#125;, <span class="hljs-number">0</span>);<br><span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">resolve</span>()<br>  .<span class="hljs-title function_">then</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;4&#x27;</span>);<br>    <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;5&#x27;</span>), <span class="hljs-number">0</span>);<br>  &#125;)<br>  .<span class="hljs-title function_">then</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;6&#x27;</span>));<br><span class="hljs-title function_">queueMicrotask</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;7&#x27;</span>));<br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;8&#x27;</span>);<br></code></pre></td></tr></table></figure>

<p>按轮次推演：</p>
<table>
<thead>
<tr>
<th>轮次</th>
<th>发生什么</th>
<th>输出</th>
</tr>
</thead>
<tbody><tr>
<td>宏任务 1（脚本）</td>
<td>打印 1、8；<code>2</code> 入宏任务；<code>then(4)</code>、<code>7</code> 入微任务</td>
<td>1、8</td>
</tr>
<tr>
<td>微任务批次 1</td>
<td><code>then(4)</code>：打印 4，<code>5</code> 入宏任务，返回后 <code>then(6)</code> 入队；依次执行 <code>7</code>、<code>6</code></td>
<td>4、7、6</td>
</tr>
<tr>
<td>宏任务 2（timeout-2）</td>
<td>打印 2；<code>3</code> 入微任务</td>
<td>2</td>
</tr>
<tr>
<td>微任务批次 2 → 宏任务 3</td>
<td>执行 <code>3</code>；下一轮取 <code>5</code></td>
<td>3、5</td>
</tr>
</tbody></table>
<p>最终输出：<strong>1 → 8 → 4 → 7 → 6 → 2 → 3 → 5</strong>。推的时候永远只问三个问题：栈空了吗？微任务队列还有吗？宏任务该取哪一个了？——比任何口诀都可靠。</p>
<h2 id="三、setTimeout-fn-0-的真相与渲染帧"><a href="#三、setTimeout-fn-0-的真相与渲染帧" class="headerlink" title="三、setTimeout(fn, 0) 的真相与渲染帧"></a>三、setTimeout(fn, 0) 的真相与渲染帧</h2><p><strong>「0 毫秒」从来不是 0</strong>：HTML 规范规定定时器嵌套层级超过 5 后，小于 4ms 的延时一律按 4ms 算；后台标签页更狠，直接钳到 1 秒起步。所以 <code>setTimeout(fn, 0)</code> 的真实语义是「尽快，但至少让出本轮」。</p>
<h3 id="坑-3：loading-不出现——渲染只发生在任务之间"><a href="#坑-3：loading-不出现——渲染只发生在任务之间" class="headerlink" title="坑 3：loading 不出现——渲染只发生在任务之间"></a>坑 3：loading 不出现——渲染只发生在任务之间</h3><p>回到开头翻的第一辆车：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">btn.<span class="hljs-title function_">addEventListener</span>(<span class="hljs-string">&#x27;click&#x27;</span>, <span class="hljs-function">() =&gt;</span> &#123;<br>  spinner.<span class="hljs-property">style</span>.<span class="hljs-property">display</span> = <span class="hljs-string">&#x27;block&#x27;</span>;   <span class="hljs-comment">// 想让用户先看见转圈</span><br>  <span class="hljs-title function_">heavyWork</span>();                       <span class="hljs-comment">// 同步计算 300ms，阻塞整个任务</span><br>  spinner.<span class="hljs-property">style</span>.<span class="hljs-property">display</span> = <span class="hljs-string">&#x27;none&#x27;</span>;<br>&#125;);<br></code></pre></td></tr></table></figure>

<p>事件回调是一整个宏任务，而样式计算、布局、绘制都发生在<strong>任务之间</strong>——浏览器只看得到这个任务的最终状态（<code>none</code>），中间那次「显示」从未被画出来。修复的关键是把任务切开，让一帧真正提交：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js">btn.<span class="hljs-title function_">addEventListener</span>(<span class="hljs-string">&#x27;click&#x27;</span>, <span class="hljs-title function_">async</span> () =&gt; &#123;<br>  spinner.<span class="hljs-property">style</span>.<span class="hljs-property">display</span> = <span class="hljs-string">&#x27;block&#x27;</span>;<br>  <span class="hljs-comment">// 双 rAF：第一个 rAF 在本帧渲染步骤开始时执行（此时还没画），</span><br>  <span class="hljs-comment">// 它里面排的 rAF 落在下一帧——那时上一帧已经画完提交了</span><br>  <span class="hljs-keyword">await</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Promise</span>(<span class="hljs-function"><span class="hljs-params">r</span> =&gt;</span> <span class="hljs-title function_">requestAnimationFrame</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-title function_">requestAnimationFrame</span>(r)));<br>  <span class="hljs-title function_">heavyWork</span>();<br>  spinner.<span class="hljs-property">style</span>.<span class="hljs-property">display</span> = <span class="hljs-string">&#x27;none&#x27;</span>;<br>&#125;);<br></code></pre></td></tr></table></figure>

<p>rAF 的准确位置：不是普通宏任务——回调跑在「更新渲染」这一步里、<strong>样式计算和布局之前</strong>，专为「绘制前最后一刻改样式」设计。这也是双 rAF 的原理：只等一帧不够，因为第一个 rAF 回调执行时那一帧还没画。</p>
<h3 id="坑-4：微任务递归，页面冻结"><a href="#坑-4：微任务递归，页面冻结" class="headerlink" title="坑 4：微任务递归，页面冻结"></a>坑 4：微任务递归，页面冻结</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">function</span> <span class="hljs-title function_">loop</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">resolve</span>().<span class="hljs-title function_">then</span>(loop);   <span class="hljs-comment">// 无限续微任务</span><br>&#125;<br><span class="hljs-title function_">loop</span>();<br></code></pre></td></tr></table></figure>

<p>微任务清空之前没有渲染机会，这个循环让事件循环永远出不了当前轮次，页面直接冻结；换成 <code>setTimeout(loop, 0)</code> 就没事。引出一个容易被忽略的结论：<strong>能「让出 UI」的从来不是异步，而是宏任务</strong>——微任务让得再频繁，渲染和输入响应一帧都等不到。</p>
<h2 id="四、Node-js：libuv-的六个阶段"><a href="#四、Node-js：libuv-的六个阶段" class="headerlink" title="四、Node.js：libuv 的六个阶段"></a>四、Node.js：libuv 的六个阶段</h2><p>Node 的事件循环由 libuv 实现，宏任务来源从「定时器 &#x2F; DOM &#x2F; 网络」换成六个阶段：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">   ┌────────────────────────────────┐<br>┌─►│ timers         setTimeout/Interval│<br>│  ├────────────────────────────────┤<br>│  │ pending callbacks  系统级回调    │<br>│  ├────────────────────────────────┤<br>│  │ idle, prepare     内部使用       │<br>│  ├────────────────────────────────┤<br>│  │ poll            I/O 事件、取新事件│<br>│  ├────────────────────────────────┤<br>│  │ check           setImmediate    │<br>│  ├────────────────────────────────┤<br>│  │ close callbacks  close 事件回调  │<br>└──┴────────────────────────────────┘<br>   每执行完一个宏任务回调：先清空 nextTick 队列，再清空 Promise 微任务队列<br></code></pre></td></tr></table></figure>

<p>两个与浏览器的差异：</p>
<ul>
<li><strong>微任务清理时机</strong>：Node 11 起与浏览器对齐——每个宏任务回调之后清一次（更早版本攒到阶段结束才清）；</li>
<li><strong>process.nextTick 是独立队列</strong>，优先级比 Promise 微任务还高，递归同样能饿死循环——绝大多数场景用 <code>queueMicrotask</code>（本轮后尽快）或 <code>setImmediate</code>（下轮 check 阶段）更好。</li>
</ul>
<h3 id="坑-5：setImmediate-和-setTimeout-0-谁先？——「不一定」"><a href="#坑-5：setImmediate-和-setTimeout-0-谁先？——「不一定」" class="headerlink" title="坑 5：setImmediate 和 setTimeout(0) 谁先？——「不一定」"></a>坑 5：setImmediate 和 setTimeout(0) 谁先？——「不一定」</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;timeout&#x27;</span>));<br><span class="hljs-title function_">setImmediate</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;immediate&#x27;</span>));<br><span class="hljs-comment">// 主模块里顺序不定：同一台机器两次运行都可能不同</span><br></code></pre></td></tr></table></figure>

<p>原因在 timers 阶段的到期判断有约 1ms 阈值：进入事件循环时如果进程启动够快、0ms 定时器还没「到期」，循环就先绕过 timers 走到 check 阶段——<strong>顺序取决于进程启动耗时，本质是竞态</strong>。但放进 I&#x2F;O 回调里顺序就恒定了：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> fs = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;fs&#x27;</span>);<br>fs.<span class="hljs-title function_">readFile</span>(__filename, <span class="hljs-function">() =&gt;</span> &#123;                  <span class="hljs-comment">// 回调在 poll 阶段执行</span><br>  <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;timeout&#x27;</span>));      <span class="hljs-comment">// 排进下一轮的 timers</span><br>  <span class="hljs-title function_">setImmediate</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;immediate&#x27;</span>));  <span class="hljs-comment">// 本轮 poll 之后的 check 阶段</span><br>&#125;);<br><span class="hljs-comment">// 恒定输出：immediate → timeout</span><br></code></pre></td></tr></table></figure>

<p>poll 之后紧跟 check，而 timers 要等下一轮——这就是「I&#x2F;O 回调内部想尽快执行用 setImmediate」的由来。</p>
<h2 id="五、工程实践：把长任务切开"><a href="#五、工程实践：把长任务切开" class="headerlink" title="五、工程实践：把长任务切开"></a>五、工程实践：把长任务切开</h2><p>超过 50ms 的同步任务会明显伤害输入响应和帧率（RAIL 模型的经验值）。渲染只发生在任务之间，标准解法就是<strong>主动切</strong>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// 1) setTimeout：最常用，但有 4ms 钳制、优先级偏低</span><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">yieldByTimeout</span> = (<span class="hljs-params"></span>) =&gt; <span class="hljs-keyword">new</span> <span class="hljs-title class_">Promise</span>(<span class="hljs-function"><span class="hljs-params">r</span> =&gt;</span> <span class="hljs-built_in">setTimeout</span>(r, <span class="hljs-number">0</span>));<br><br><span class="hljs-comment">// 2) MessageChannel：无钳制、触发快，React 调度器的同款思路</span><br><span class="hljs-keyword">const</span> channel = <span class="hljs-keyword">new</span> <span class="hljs-title class_">MessageChannel</span>();<br><span class="hljs-keyword">const</span> <span class="hljs-title function_">yieldByMessage</span> = (<span class="hljs-params"></span>) =&gt; <span class="hljs-keyword">new</span> <span class="hljs-title class_">Promise</span>(<span class="hljs-function"><span class="hljs-params">resolve</span> =&gt;</span> &#123;<br>  channel.<span class="hljs-property">port1</span>.<span class="hljs-property">onmessage</span> = <span class="hljs-function">() =&gt;</span> <span class="hljs-title function_">resolve</span>();<br>  channel.<span class="hljs-property">port2</span>.<span class="hljs-title function_">postMessage</span>(<span class="hljs-literal">null</span>);<br>&#125;);<br><br><span class="hljs-comment">// 3) scheduler.yield()：原生让出、保留任务优先级（Chrome/Edge 129+，用前检测）</span><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">yieldToMain</span> = (<span class="hljs-params"></span>) =&gt;<br>  <span class="hljs-keyword">typeof</span> scheduler !== <span class="hljs-string">&#x27;undefined&#x27;</span> &amp;&amp; <span class="hljs-string">&#x27;yield&#x27;</span> <span class="hljs-keyword">in</span> scheduler<br>    ? scheduler.<span class="hljs-title function_">yield</span>()<br>    : <span class="hljs-title function_">yieldByMessage</span>();<br></code></pre></td></tr></table></figure>

<p>用法上按时间片切，而不是按条数切：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">process</span>(<span class="hljs-params">items, work</span>) &#123;<br>  <span class="hljs-keyword">let</span> chunkStart = performance.<span class="hljs-title function_">now</span>();<br>  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> item <span class="hljs-keyword">of</span> items) &#123;<br>    <span class="hljs-title function_">work</span>(item);<br>    <span class="hljs-keyword">if</span> (performance.<span class="hljs-title function_">now</span>() - chunkStart &gt; <span class="hljs-number">40</span>) &#123;   <span class="hljs-comment">// 每片约 40ms</span><br>      <span class="hljs-keyword">await</span> <span class="hljs-title function_">yieldToMain</span>();                       <span class="hljs-comment">// 让出：给渲染和输入留窗口</span><br>      chunkStart = performance.<span class="hljs-title function_">now</span>();<br>    &#125;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>注意和 <code>await Promise.resolve()</code> 的区别：那样根本没让出——微任务清空之前既不渲染也不响应输入。<strong>让出主线程 &#x3D; 进入下一个宏任务</strong>，这句是本节的全部内容。</p>
<h2 id="六、总结"><a href="#六、总结" class="headerlink" title="六、总结"></a>六、总结</h2><p>速查表：</p>
<table>
<thead>
<tr>
<th>API &#x2F; 来源</th>
<th>归属</th>
<th>执行时机</th>
</tr>
</thead>
<tbody><tr>
<td>setTimeout &#x2F; setInterval</td>
<td>宏任务</td>
<td>一轮取一个；嵌套 ≥5 层后最小 4ms，后台页钳到 1s</td>
</tr>
<tr>
<td>DOM 事件 &#x2F; I&#x2F;O &#x2F; MessageChannel</td>
<td>宏任务</td>
<td>任务源之间的优先级由宿主定</td>
</tr>
<tr>
<td>then &#x2F; await 续体 &#x2F; queueMicrotask</td>
<td>微任务</td>
<td>每个宏任务之后，队列必清空</td>
</tr>
<tr>
<td>requestAnimationFrame</td>
<td>渲染步骤</td>
<td>渲染机会到来时、样式计算之前</td>
</tr>
<tr>
<td>setImmediate（Node）</td>
<td>check 阶段宏任务</td>
<td>本轮 poll 之后</td>
</tr>
<tr>
<td>process.nextTick（Node）</td>
<td>独立微任务队列</td>
<td>比 Promise 微任务更早清空</td>
</tr>
</tbody></table>
<!-- 面试速答：

- **setTimeout(fn, 0) 是立即执行吗？** 不是。至少让出到下一个宏任务，且嵌套 5 层后被钳到 4ms。
- **await 后面的代码什么时候跑？** 作为微任务，在本轮宏任务结束后、渲染和下一个宏任务之前。
- **宏任务和微任务的本质区别？** 调度方式：宏任务一轮取一个、之间可渲染；微任务每轮后必清空、清完前不渲染。
- **为什么微任务读到的状态比 setTimeout 回调「新」？** 它跑在渲染和后续任务之前，中间没有任何人插队。
- **怎么让浏览器先画一帧再干重活？** 双 rAF 等上一帧提交，或把重活按时间片拆成多个宏任务。
- **Node 里 setImmediate 和 setTimeout(0) 谁先？** 主模块里是竞态、顺序不定；I/O 回调里 setImmediate 恒先。 -->

<p><strong>一句话记住</strong>：同步代码一口气跑完 → 清空微任务 → 也许渲染 → 下一个宏任务——浏览器如此；Node 只是换了一套宏任务的来源（六个阶段）和两条更早清空的队列（nextTick、微任务），主循环铁律不变。</p>
<!-- ## 参考

- [WHATWG HTML Standard — Event loops](https://html.spec.whatwg.org/multipage/webappapis.html#event-loops)
- [MDN：事件循环](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Event_loop)
- [MDN：使用 queueMicrotask()](https://developer.mozilla.org/zh-CN/docs/Web/API/HTML_DOM_API/Microtask_guide)
- [Jake Archibald：Tasks, microtasks, queues and schedules](https://jakearchibald.com/2015/tasks-microtasks-queues-and-schedules/)
- [Philip Roberts：What the heck is the event loop anyway?（JSConf EU 2014）](https://www.youtube.com/watch?v=8aGhZQkoFbQ)
- [Node.js 官方文档：The Event Loop, Timers, and process.nextTick](https://nodejs.org/en/learn/asynchronous-work/event-loop-timers-and-nexttick)
- [web.dev：Optimize long tasks](https://web.dev/articles/optimize-long-tasks) -->
]]></content>
      <categories>
        <category>前端基础</category>
        <category>javascript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
        <tag>事件循环</tag>
        <tag>node.js</tag>
      </tags>
  </entry>
  <entry>
    <title>Promise 全景：从回调地狱、状态机到微任务与链式调用</title>
    <url>/2026/09/09/javascript-promise/</url>
    <content><![CDATA[<p>我以前对 Promise 的理解就一句话：<strong>解决回调地狱的。</strong> 这话没错，但远远不够——直到被一连串问题问住：</p>
<ul>
<li><code>new Promise()</code> 里的代码为什么是<strong>同步执行</strong>的？</li>
<li>明明已经 resolve 了，<code>.then()</code> 为什么还是不立即跑？</li>
<li><code>then</code> 里 <code>return 123</code>，下一个 <code>then</code> 怎么就拿到了 <code>123</code>？<code>throw</code> 的错为什么后面的 <code>catch</code> 接得住？</li>
<li><code>async/await</code> 和 Promise 到底什么关系？</li>
</ul>
<p>后来才想明白，Promise 本质是一套抽象：</p>
<blockquote>
<p><strong>用状态机表示「未来才产生的结果」，用 then &#x2F; catch &#x2F; finally 描述后续处理，用链式 Promise 传递值与错误，再交给微任务机制调度执行。</strong></p>
</blockquote>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Promise<br>├── 状态管理    pending → fulfilled / rejected<br>├── 结果传递    resolve(value) / reject(reason)<br>├── 后续处理    then / catch / finally<br>├── 链式调用    then 返回新 Promise<br>├── 任务调度    reaction → 微任务<br>└── 组合能力    all / allSettled / race / any<br></code></pre></td></tr></table></figure>

<span id="more"></span>

<h2 id="一、从回调地狱说起：Promise-是「未来结果的容器」"><a href="#一、从回调地狱说起：Promise-是「未来结果的容器」" class="headerlink" title="一、从回调地狱说起：Promise 是「未来结果的容器」"></a>一、从回调地狱说起：Promise 是「未来结果的容器」</h2><p>回调时代把三个串行请求连起来，缩进和错误处理一起失控：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-title function_">getUser</span>(<span class="hljs-function">(<span class="hljs-params">user</span>) =&gt;</span> &#123;<br>  <span class="hljs-title function_">getUserDetail</span>(user.<span class="hljs-property">id</span>, <span class="hljs-function">(<span class="hljs-params">detail</span>) =&gt;</span> &#123;<br>    <span class="hljs-title function_">getOrders</span>(detail.<span class="hljs-property">id</span>, <span class="hljs-function">(<span class="hljs-params">orders</span>) =&gt;</span> &#123;<br>      <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(orders);   <span class="hljs-comment">// 想统一处理错误？每层都要写一遍</span><br>    &#125;);<br>  &#125;);<br>&#125;);<br></code></pre></td></tr></table></figure>

<p>Promise 的解法：<strong>把「未来会得到的结果」封装成一个对象</strong>。<code>fetch(&#39;/api/user&#39;)</code> 立刻返回一个 promise——现在还没有结果，未来要么 fulfilled（用户数据）、要么 rejected（错误原因）。此后的所有问题，都变成「怎么管理这个对象」。</p>
<h2 id="二、状态机：Promise-的核心不是-then，是状态"><a href="#二、状态机：Promise-的核心不是-then，是状态" class="headerlink" title="二、状态机：Promise 的核心不是 then，是状态"></a>二、状态机：Promise 的核心不是 then，是状态</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">pending ──resolve──► fulfilled（保存 value）<br>   │<br>   └──reject───────► rejected（保存 reason）<br></code></pre></td></tr></table></figure>

<p>最重要的一条规则：<strong>状态只能从 pending 变一次，之后不可逆</strong>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">new</span> <span class="hljs-title class_">Promise</span>(<span class="hljs-function">(<span class="hljs-params">resolve, reject</span>) =&gt;</span> &#123;<br>  <span class="hljs-title function_">resolve</span>(<span class="hljs-string">&#x27;A&#x27;</span>);<br>  <span class="hljs-title function_">resolve</span>(<span class="hljs-string">&#x27;B&#x27;</span>);   <span class="hljs-comment">// 无效：状态已定格</span><br>  <span class="hljs-title function_">reject</span>(<span class="hljs-string">&#x27;C&#x27;</span>);    <span class="hljs-comment">// 无效</span><br>&#125;);<br><span class="hljs-comment">// 最终：fulfilled，value = &#x27;A&#x27;</span><br></code></pre></td></tr></table></figure>

<p>而 <code>resolve</code> &#x2F; <code>reject</code> 做的事是完整的一条链：<strong>改变状态 → 保存结果 → 触发已注册的 reactions → 把它们排进微任务队列</strong>。最后一步，就是「then 为什么是异步的」的答案。</p>
<h2 id="三、执行时机：executor-同步，then-是微任务"><a href="#三、执行时机：executor-同步，then-是微任务" class="headerlink" title="三、执行时机：executor 同步，then 是微任务"></a>三、执行时机：executor 同步，then 是微任务</h2><p>Promise 最容易误解的地方——「哪部分是异步的」：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;1&#x27;</span>);<br><span class="hljs-keyword">new</span> <span class="hljs-title class_">Promise</span>(<span class="hljs-function">(<span class="hljs-params">resolve</span>) =&gt;</span> &#123;<br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;2&#x27;</span>);                    <span class="hljs-comment">// executor：同步执行</span><br>  <span class="hljs-title function_">resolve</span>(<span class="hljs-string">&#x27;ok&#x27;</span>);<br>&#125;).<span class="hljs-title function_">then</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;3&#x27;</span>));       <span class="hljs-comment">// 回调：进微任务队列</span><br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;4&#x27;</span>);<br><br><span class="hljs-comment">// 输出：1 → 2 → 4 → 3</span><br></code></pre></td></tr></table></figure>

<p><strong>executor 是同步执行的</strong>，不会因为写在 <code>new Promise</code> 里就变异步；真正异步的是 <code>then</code> 的回调——即使 Promise 已经 settled，回调也要等当前同步代码跑完、轮到微任务时才执行。加上 setTimeout 就凑成经典面试题：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;1&#x27;</span>);<br><span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;2&#x27;</span>), <span class="hljs-number">0</span>);           <span class="hljs-comment">// 宏任务</span><br><span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">resolve</span>().<span class="hljs-title function_">then</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;3&#x27;</span>));  <span class="hljs-comment">// 微任务</span><br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;4&#x27;</span>);<br><br><span class="hljs-comment">// 输出：1 → 4 → 3 → 2（微任务在当前任务后清空，宏任务等下一轮）</span><br></code></pre></td></tr></table></figure>

<h2 id="四、链式调用：then-永远返回一个新-Promise"><a href="#四、链式调用：then-永远返回一个新-Promise" class="headerlink" title="四、链式调用：then 永远返回一个新 Promise"></a>四、链式调用：then 永远返回一个新 Promise</h2><p>链式调用的基础：<code>then</code> 同时做两件事——注册当前 Promise 的后续处理，<strong>创建并返回一个新的 Promise</strong>。所以 <code>.then().then()</code> 连的从来不是同一个对象。新 Promise 的状态由回调的返回值决定，规则只有三条：</p>
<table>
<thead>
<tr>
<th>then 回调里</th>
<th>新 Promise</th>
</tr>
</thead>
<tbody><tr>
<td><code>return 123</code>（普通值）</td>
<td>fulfilled，value &#x3D; 123（≈ <code>Promise.resolve(123)</code>）</td>
</tr>
<tr>
<td><code>return promise2</code></td>
<td>等待并跟随 promise2 的状态</td>
</tr>
<tr>
<td><code>throw new Error()</code></td>
<td>rejected，reason &#x3D; 该错误</td>
</tr>
</tbody></table>
<p>两个高频推论：</p>
<ul>
<li><strong>值穿透</strong>：<code>.then()</code> 不传回调时值不会被吃掉，<code>Promise.resolve(100).then().then(v =&gt; v)</code> 一路传到底——空 then 等价于 <code>v =&gt; v</code>；</li>
<li><strong>错误沿链传播</strong>：链中某一步 rejected 后，中间没有 onRejected 的 then 全部跳过，错误一路传到最近的 <code>.catch</code>。<code>catch</code> 就是 <code>then(undefined, onRejected)</code> 的语法糖——工程上推荐 <code>then / catch / finally</code> 分开写而不是用 then 的第二个参数，因为前者能兜住整条链。</li>
</ul>
<p><code>finally</code> 无论成败都执行且不改变值，典型用途是关 loading：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-title function_">showLoading</span>();<br><span class="hljs-title function_">fetch</span>(<span class="hljs-string">&#x27;/api/user&#x27;</span>)<br>  .<span class="hljs-title function_">then</span>(<span class="hljs-function">(<span class="hljs-params">res</span>) =&gt;</span> res.<span class="hljs-title function_">json</span>())<br>  .<span class="hljs-title function_">catch</span>(<span class="hljs-variable language_">console</span>.<span class="hljs-property">error</span>)<br>  .<span class="hljs-title function_">finally</span>(hideLoading);   <span class="hljs-comment">// 成败都走到</span><br></code></pre></td></tr></table></figure>

<h2 id="五、微任务：Promise-和-Event-Loop-的分工"><a href="#五、微任务：Promise-和-Event-Loop-的分工" class="headerlink" title="五、微任务：Promise 和 Event Loop 的分工"></a>五、微任务：Promise 和 Event Loop 的分工</h2><p>Promise 本身不是事件循环。分工是：<strong>Promise 负责状态、结果和 reactions；宿主的事件循环负责什么时候执行它们</strong>——reaction 被排进微任务队列，每个宏任务结束后清空。</p>
<p>一个容易忽视的细节：链上的 then 是<strong>逐个</strong>入队的——第一个 then 的回调执行完、它返回的新 Promise settled 后，第二个 then 的回调才入队：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">resolve</span>().<span class="hljs-title function_">then</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-number">1</span>)).<span class="hljs-title function_">then</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-number">2</span>));<br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-number">3</span>);<br><br><span class="hljs-comment">// 输出：3 → 1 → 2</span><br><span class="hljs-comment">// 同步打印 3 → 微任务 1 打印 1，第二个回调此时才入队 → 微任务 2 打印 2</span><br></code></pre></td></tr></table></figure>

<p>所以链上每个 then 各占一个微任务位——「微任务清空过程中新入队的也要清」这条事件循环规则，日常来源就是它。</p>
<h2 id="六、组合-API：批量管理多个-Promise"><a href="#六、组合-API：批量管理多个-Promise" class="headerlink" title="六、组合 API：批量管理多个 Promise"></a>六、组合 API：批量管理多个 Promise</h2><p>先看它们解决的真实问题。三个互不依赖的请求串行 await，总耗时是三者之和；<code>Promise.all</code> 并发发起，总耗时约等于最慢的那个：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// 串行：T = T1 + T2 + T3</span><br><span class="hljs-keyword">const</span> user = <span class="hljs-keyword">await</span> <span class="hljs-title function_">getUser</span>();<br><span class="hljs-keyword">const</span> goods = <span class="hljs-keyword">await</span> <span class="hljs-title function_">getGoods</span>();<br><span class="hljs-keyword">const</span> orders = <span class="hljs-keyword">await</span> <span class="hljs-title function_">getOrders</span>();<br><br><span class="hljs-comment">// 并发：T = max(T1, T2, T3)</span><br><span class="hljs-keyword">const</span> [user, goods, orders] = <span class="hljs-keyword">await</span> <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">all</span>([<span class="hljs-title function_">getUser</span>(), <span class="hljs-title function_">getGoods</span>(), <span class="hljs-title function_">getOrders</span>()]);<br></code></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>API</th>
<th>成功条件</th>
<th>失败条件</th>
<th>典型场景</th>
</tr>
</thead>
<tbody><tr>
<td><code>Promise.all</code></td>
<td>全部 fulfilled</td>
<td>任意一个 rejected</td>
<td>并发请求</td>
</tr>
<tr>
<td><code>Promise.allSettled</code></td>
<td>全部结束即 resolve</td>
<td>永不因单个失败 reject</td>
<td>批量任务要逐个结果</td>
</tr>
<tr>
<td><code>Promise.race</code></td>
<td>第一个 settle 的定结果</td>
<td>同左</td>
<td>请求超时控制</td>
</tr>
<tr>
<td><code>Promise.any</code></td>
<td>第一个 fulfilled</td>
<td>全部 rejected</td>
<td>多源容灾取最快成功</td>
</tr>
</tbody></table>
<p>三个细节常被问：</p>
<ol>
<li><code>all</code> 的结果<strong>按输入顺序</strong>排列，不是完成顺序——B 先回来也排在第二位；</li>
<li><code>all</code> 一个失败立即 rejected，但<strong>不会自动取消</strong>其他已发出的请求——要取消得配 <code>AbortController</code>；</li>
<li><code>race</code> 同样只是「取先到的结果」，不会取消落败者。</li>
</ol>
<h2 id="七、async-await：Promise-之上的语法糖"><a href="#七、async-await：Promise-之上的语法糖" class="headerlink" title="七、async&#x2F;await：Promise 之上的语法糖"></a>七、async&#x2F;await：Promise 之上的语法糖</h2><p>async&#x2F;await 不是 Promise 的替代品，而是<strong>建立在 Promise 之上的语法机制</strong>。两条核心规则，一个例子全占了：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">foo</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;A&#x27;</span>);<br>  <span class="hljs-keyword">await</span> <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">resolve</span>();<br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;B&#x27;</span>);   <span class="hljs-comment">// ≈ 写在 then 里的代码</span><br>&#125;<br><span class="hljs-title function_">foo</span>();<br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;C&#x27;</span>);<br><br><span class="hljs-comment">// 输出：A → C → B</span><br></code></pre></td></tr></table></figure>

<ol>
<li><strong>async 函数一定返回 Promise</strong>：<code>return 123</code> 会被包装成 <code>Promise.resolve(123)</code>；</li>
<li><strong>await 暂停的是当前 async 函数，不是 JS 线程</strong>：函数在 await 处让出，把后面的代码当作「await 值的 then 回调」排进微任务，调用处继续同步执行——所以 B 在 C 之后。</li>
</ol>
<p>串行陷阱也出自这里：循环里逐个 <code>await</code> 是串行，要并发就先 <code>Promise.all</code> 再取值。</p>
<h2 id="八、手写-Promise：骨架与真正的难点"><a href="#八、手写-Promise：骨架与真正的难点" class="headerlink" title="八、手写 Promise：骨架与真正的难点"></a>八、手写 Promise：骨架与真正的难点</h2><p>手写是检验理解的最好方式。内部最少需要这些：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">class</span> <span class="hljs-title class_">MyPromise</span> &#123;<br>  <span class="hljs-title function_">constructor</span>(<span class="hljs-params">executor</span>) &#123;<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">status</span> = <span class="hljs-string">&#x27;pending&#x27;</span>;<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">value</span> = <span class="hljs-literal">undefined</span>;    <span class="hljs-comment">// fulfilled 时保存结果</span><br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">reason</span> = <span class="hljs-literal">undefined</span>;   <span class="hljs-comment">// rejected 时保存原因</span><br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">callbacks</span> = [];       <span class="hljs-comment">// pending 期 then 注册的回调，先存起来</span><br>    <span class="hljs-comment">// executor 同步执行；resolve/reject 里：改状态、存结果、依次异步触发 callbacks</span><br>  &#125;<br>  <span class="hljs-title function_">then</span>(<span class="hljs-params">onFulfilled, onRejected</span>) &#123; <span class="hljs-comment">/* 返回新 Promise，难点见下 */</span> &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>为什么需要 callbacks 数组：<code>p.then(f1); p.then(f2);</code> 在 pending 期注册的回调，要等 <code>resolve</code> 时再依次触发。</p>
<p>真正的难点不在状态机，而在 <strong>then 的返回值解析</strong>（规范叫 Promise Resolution Procedure）：回调可能 return 普通值、return 一个 Promise、return 一个 thenable（<code>{ then(resolve) {} }</code>）、甚至 throw——每种情况新 Promise 怎么变，全在这一个函数里。把这段写对，才算真懂 then 链。</p>
<h2 id="九、常见误区速查"><a href="#九、常见误区速查" class="headerlink" title="九、常见误区速查"></a>九、常见误区速查</h2><table>
<thead>
<tr>
<th>误区</th>
<th>事实</th>
</tr>
</thead>
<tbody><tr>
<td>Promise 的代码是异步的</td>
<td>executor 同步执行；异步的是 then 的回调</td>
</tr>
<tr>
<td><code>Promise.resolve().then(fn)</code> 立即执行</td>
<td>fn 进微任务，同步代码之后才跑</td>
</tr>
<tr>
<td>Promise &#x3D; 线程 &#x2F; Web Worker</td>
<td>只是结果抽象，干活的仍是宿主（网络、定时器……）</td>
</tr>
<tr>
<td>async&#x2F;await 绕开了 Promise</td>
<td>全部建立在 Promise 机制之上</td>
</tr>
<tr>
<td><code>Promise.all</code> 失败会取消其他请求</td>
<td>不会；取消需要 AbortController</td>
</tr>
</tbody></table>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>一张图收束全文：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">pending ──resolve──► fulfilled(value)<br>   │                    │<br>   └──reject──────► rejected(reason)<br>                        │<br>             状态改变 → reaction → 微任务 → then 回调执行<br>                        │<br>          ┌─────────────┼─────────────┐<br>      return 值      return P       throw<br>          │             │             │<br>    新 P fulfilled  等待并跟随 P   新 P rejected<br>          └─────────────┼─────────────┘<br>                     then...（链继续）<br></code></pre></td></tr></table></figure>

<!-- 面试速答：

- **Promise 的本质？** 表示异步操作最终结果的对象：状态机保存结果，then / catch / finally 注册后续，reaction 排进微任务执行，then 返回新 Promise 形成链。
- **`new Promise` 里的代码同步还是异步？** 同步。异步的是 then 的回调，即使 Promise 已 settled 也要等微任务时机。
- **then 的三种返回值规则？** 普通值 → 新 Promise fulfilled；返回 Promise → 等待并跟随；throw → rejected，错误沿链传播到最近的 catch。
- **catch 为什么能接住前面的错？** then 回调 throw 会让它返回的新 Promise rejected，错误沿链向后传播直到遇到 onRejected；catch 就是 `then(undefined, onRejected)`。
- **async/await 和 Promise 的关系？** 语法糖：async 函数必返回 Promise，await 后的代码等价于 then 回调，暂停的是当前函数而非线程。 -->

<p><strong>一句话记住</strong>：Promise &#x3D; 状态机 + 链式传递 + 微任务调度——状态存结果，链传值与错，微任务定时机；async&#x2F;await 只是这套机制的衣服。</p>
<!-- ## 参考资料

- [ECMAScript 规范：Promise Objects](https://tc39.es/ecma262/multipage/control-abstraction-objects.html)
- [MDN：Using Promises](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Using_promises)
- [MDN：await](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/await)
- [Promises/A+ 规范](https://promises-aplus.promises-spec/) -->
]]></content>
      <categories>
        <category>前端基础</category>
        <category>javascript</category>
      </categories>
      <tags>
        <tag>javascript</tag>
        <tag>Promise</tag>
        <tag>异步</tag>
        <tag>Event Loop</tag>
        <tag>async/await</tag>
      </tags>
  </entry>
  <entry>
    <title>大屏地图的图层管理实战：MapLibre GL 动态样式、两阶段渲染与叠放排序</title>
    <url>/2026/09/09/maplibre-layer-management/</url>
    <content><![CDATA[<p>最近在一个数据可视化大屏项目里负责地图模块，需求乍看很朴素：左侧一棵图层树，勾选哪类设施，地图上就渲染哪类要素。但做下去发现坑一个接一个：</p>
<ul>
<li>设施有<strong>几十类</strong>，点、线、面混杂，样式（颜色&#x2F;图标&#x2F;线宽）全部由后端配置下发，前端写死不可能；</li>
<li>矢量瓦片是<strong>异步加载</strong>的，「瓦片里到底有哪些要素类型」要等加载完才知道——直接等它，图层会白屏好几百毫秒；</li>
<li>大面积面要素挂图标，放大后<strong>每个瓦片上都长出一个重复图标</strong>；</li>
<li>一切换底图样式，所有业务图层<strong>集体消失</strong>（MapLibre 的 <code>setStyle</code> 会重置整个样式）；</li>
<li>把 MapLibre 实例塞进 Vue 的 <code>ref</code>，地图<strong>随机报错</strong>，内部数据结构被 Proxy 拆得面目全非。</li>
</ul>
<p>这篇文章把这些问题的解法按主题拆开讲清楚。技术栈是 <strong>Vue 3 + Pinia + MapLibre GL（配 PMTiles 协议）</strong>，但思路对任何 WebGL 地图引擎（Mapbox GL &#x2F; MapLibre &#x2F; 高性能 Canvas 地图）都通用。</p>
<span id="more"></span>

<h2 id="一、整体架构：不写图层基类，用组合式工厂"><a href="#一、整体架构：不写图层基类，用组合式工厂" class="headerlink" title="一、整体架构：不写图层基类，用组合式工厂"></a>一、整体架构：不写图层基类，用组合式工厂</h2><p>先回答一个设计问题：几十种图层，要不要抽象一个 <code>BaseLayer</code> 基类，子类重写 <code>addToMap</code> &#x2F; <code>removeFromMap</code>？</p>
<p>我们的答案是不写 class 继承，而是把图层系统拆成<strong>职责单一的工厂函数</strong>，在 Pinia setup store 里组合：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">useLayerStore (图层主 Store)<br> ├── state      : layers / graphLayers 两棵树 + activeTab + 搜索词<br> ├── computed   : filteredLayers / checkedIds / activeLayerCount<br> ├── styleCache   = createStyleCache()     全局样式解析缓存<br> ├── persistence  = createPersistence()    localStorage 勾选/展开持久化<br> ├── mapOps       = createMapLayers()      MapLibre 图层操控（核心）<br> └── searchFilter = createKeywordFilter()  关键词过滤（纯函数）<br></code></pre></td></tr></table></figure>

<p><code>createMapLayers</code> 通过<strong>依赖注入</strong>拿到它需要的东西，而不是自己 <code>import</code> store——这让它可以独立单测：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">const</span> mapOps = <span class="hljs-title function_">createMapLayers</span>(&#123;<br>  layers,                    <span class="hljs-comment">// 图层树（响应式）</span><br>  graphLayers,               <span class="hljs-comment">// 拓扑树（响应式）</span><br>  <span class="hljs-attr">getCache</span>: styleCache.<span class="hljs-property">getCache</span>,          <span class="hljs-comment">// 样式缓存快照</span><br>  findNodeInBoth,            <span class="hljs-comment">// 两棵树按 ID 查节点</span><br>  <span class="hljs-attr">getActiveTab</span>: <span class="hljs-function">() =&gt;</span> activeTab.<span class="hljs-property">value</span>,<br>  <span class="hljs-attr">getFacilityOrderIds</span>: <span class="hljs-function">() =&gt;</span> facilityOrderIds.<span class="hljs-property">value</span>,<br>  ready,                     <span class="hljs-comment">// 两棵树加载完成的 Promise</span><br>&#125;)<br></code></pre></td></tr></table></figure>

<p>选择组合而非继承的理由很实际：图层系统的变化维度是「样式解析 &#x2F; 持久化 &#x2F; 地图操作 &#x2F; 过滤」四个<strong>正交方向</strong>，继承树只能沿一条轴展开，最终必然长出 <code>BaseLayer → VectorLayer → StyledVectorLayer</code> 这种每次改样式都要动基类的结构。组合模式下每个工厂只对自己的 Map 负责，核心纯函数（瓦片 URL 解析、排序合并）还能直接跑单测。</p>
<h2 id="二、数据模型：一棵树跑通-UI-和地图两个世界"><a href="#二、数据模型：一棵树跑通-UI-和地图两个世界" class="headerlink" title="二、数据模型：一棵树跑通 UI 和地图两个世界"></a>二、数据模型：一棵树跑通 UI 和地图两个世界</h2><p>后端返回的是三级树：一级分类（电力&#x2F;交通）→ 二级分组 → 三级叶子设施。前端转成统一的 <code>LayerNode</code>：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">export</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">LayerNode</span> &#123;<br>  <span class="hljs-attr">id</span>: <span class="hljs-built_in">string</span><br>  <span class="hljs-attr">name</span>: <span class="hljs-built_in">string</span><br>  <span class="hljs-attr">nodeType</span>: <span class="hljs-string">&#x27;group&#x27;</span> | <span class="hljs-string">&#x27;layer&#x27;</span><br>  <span class="hljs-attr">visible</span>: <span class="hljs-built_in">boolean</span>          <span class="hljs-comment">// 勾选状态</span><br>  <span class="hljs-attr">opacity</span>: <span class="hljs-built_in">number</span><br>  <span class="hljs-attr">children</span>?: <span class="hljs-title class_">LayerNode</span>[]<br>  <span class="hljs-attr">sourceLayer</span>?: <span class="hljs-built_in">string</span>      <span class="hljs-comment">// 矢量瓦片的 source-layer 名</span><br>  <span class="hljs-comment">// ---- 运行时：MapLibre 绑定字段（渲染后由 store 写回） ----</span><br>  <span class="hljs-attr">_mapLayerIds</span>?: <span class="hljs-built_in">string</span>[]   <span class="hljs-comment">// 该节点在地图上的所有 MapLibre layer ID</span><br>  <span class="hljs-attr">_mapSourceAdded</span>?: <span class="hljs-built_in">boolean</span> <span class="hljs-comment">// source 是否已添加（隐藏时不销毁）</span><br>  <span class="hljs-attr">_centroidMeta</span>?: &#123; ... &#125;   <span class="hljs-comment">// 面要素质心图层的运行时状态</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>关键设计是那几个下划线开头的<strong>运行时绑定字段</strong>：一棵树同时服务图层面板（勾选、搜索、计数）和地图（真正的 layer ID 列表）。「用户勾选的是树节点，地图操作的是 layer ID」这层映射关系就收在节点自己身上，不用再维护一张 id 映射表。</p>
<p>瓦片协议上同时支持两种源，按后端下发的字段自动分流：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">if</span> (item.<span class="hljs-property">pmtiles</span>) &#123;<br>  <span class="hljs-comment">// PMTiles：单文件瓦片，注册自定义协议后直接当 vector source 用</span><br>  map.<span class="hljs-title function_">addSource</span>(sourceId, &#123; <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;vector&#x27;</span>, <span class="hljs-attr">url</span>: <span class="hljs-string">`pmtiles://<span class="hljs-subst">$&#123;base&#125;</span><span class="hljs-subst">$&#123;path&#125;</span>`</span> &#125;)<br>&#125; <span class="hljs-keyword">else</span> &#123;<br>  <span class="hljs-comment">// GeoServer TMS PBF 瓦片</span><br>  map.<span class="hljs-title function_">addSource</span>(sourceId, &#123; <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;vector&#x27;</span>, tiles, <span class="hljs-attr">scheme</span>: <span class="hljs-string">&#x27;tms&#x27;</span>, <span class="hljs-attr">tileSize</span>: <span class="hljs-number">512</span> &#125;)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>PMTiles 值得一提：它是把整包瓦片打成一个文件的格式，配合官方库注册 <code>pmtiles://</code> 协议即可无缝接入 MapLibre，私有化部署时比维护一套瓦片服务省事得多：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">let</span> registered = <span class="hljs-literal">false</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">ensurePmtilesProtocol</span>(<span class="hljs-params"><span class="hljs-attr">maplibre</span>: <span class="hljs-keyword">typeof</span> maplibregl</span>) &#123;<br>  <span class="hljs-keyword">if</span> (registered) <span class="hljs-keyword">return</span>          <span class="hljs-comment">// 幂等注册</span><br>  <span class="hljs-keyword">const</span> &#123; <span class="hljs-title class_">Protocol</span> &#125; = <span class="hljs-keyword">await</span> <span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;pmtiles&#x27;</span>)  <span class="hljs-comment">// 动态 import，不进首屏包</span><br>  maplibre.<span class="hljs-title function_">addProtocol</span>(<span class="hljs-string">&#x27;pmtiles&#x27;</span>, <span class="hljs-keyword">new</span> <span class="hljs-title class_">Protocol</span>().<span class="hljs-property">tile</span>)<br>  registered = <span class="hljs-literal">true</span><br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="三、动态样式：样式不写死，按-type-过滤建图层"><a href="#三、动态样式：样式不写死，按-type-过滤建图层" class="headerlink" title="三、动态样式：样式不写死，按 type 过滤建图层"></a>三、动态样式：样式不写死，按 type 过滤建图层</h2><p>几十类设施样式由后端「全局样式配置」下发，结构大致是 <code>Record&lt;typeKey, 样式规则[]&gt;</code>——key 是瓦片要素 <code>properties.type</code> 的值（比如 <code>transformer_station</code>），值是渲染规则（<code>code</code> 决定渲染成点&#x2F;线&#x2F;面，<code>config</code> 决定颜色图标线宽）。</p>
<p>拿到配置后先<strong>预编译</strong>成 MapLibre 的 layer spec（<code>createStyleCache</code> 的职责）：point + 图标 → symbol 图层、point 无图标 → circle、line → line、polygon → fill + 边界线 outline，同时把 SVG 图标预渲染成 <code>ImageData</code> 备用。渲染时按 typeKey 建<strong>带 filter 的独立图层</strong>：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> [typeKey, entries] <span class="hljs-keyword">of</span> styleCache) &#123;<br>  <span class="hljs-keyword">const</span> filter = [<span class="hljs-string">&#x27;==&#x27;</span>, [<span class="hljs-string">&#x27;get&#x27;</span>, <span class="hljs-string">&#x27;type&#x27;</span>], typeKey]<br>  <span class="hljs-keyword">const</span> layerId = <span class="hljs-string">`<span class="hljs-subst">$&#123;nodeId&#125;</span>__<span class="hljs-subst">$&#123;typeKey&#125;</span>`</span><br>  map.<span class="hljs-title function_">addLayer</span>(&#123; <span class="hljs-attr">id</span>: layerId, <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;fill&#x27;</span>, <span class="hljs-attr">source</span>: sourceId,<br>                 <span class="hljs-string">&#x27;source-layer&#x27;</span>: sourceLayer, filter, ...layerSpec &#125;)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>图层 ID 用 <code>${nodeId}__${typeKey}</code> 这样的命名约定串起来，一个设施图层勾选后实际产生的所有 layer（多条样式、面边界线 <code>-outline</code>、质心 <code>_centroid</code>）都能靠前缀反查回来——后面处理显隐、排序、销毁时全靠这份「户口」。</p>
<h2 id="四、两阶段渲染：先上菜，再校菜"><a href="#四、两阶段渲染：先上菜，再校菜" class="headerlink" title="四、两阶段渲染：先上菜，再校菜"></a>四、两阶段渲染：先上菜，再校菜</h2><p>这是整个模块里最值得写的一笔。矛盾在于：</p>
<ul>
<li>瓦片是<strong>按需异步加载</strong>的，<code>addSource</code> 之后要等一会儿才能查到里面的要素；</li>
<li>但样式缓存的 key 集合我们是<strong>预先知道</strong>的（全局样式配置就摆在那）；</li>
<li>用户勾选图层后盯着地图等，白屏超过几百毫秒体验就很差。</li>
</ul>
<p>方案是两阶段：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Phase 1（立即渲染，~100ms）<br>  addSource → 注册图标 → 用样式缓存的全部 typeKey 建 filter 图层<br>  特征先按缓存假设渲染，用户立刻看到带样式的要素<br><br>Phase 2（后台精炼，requestIdleCallback）<br>  探测瓦片里实际存在的 type 集合与几何类型<br>  与缓存假设 diff：一致 → 什么都不做；不一致 → 拆掉重建<br></code></pre></td></tr></table></figure>

<p>探测用 <code>map.querySourceFeatures</code> 轮询，带<strong>指数退避</strong>和超时兜底：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">const</span> <span class="hljs-variable constant_">MAX_ATTEMPTS</span> = <span class="hljs-number">10</span><br><span class="hljs-keyword">const</span> <span class="hljs-variable constant_">TIMEOUT_MS</span> = <span class="hljs-number">3000</span><br><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">getBackoffInterval</span> = (<span class="hljs-params"></span>) =&gt; &#123;<br>  <span class="hljs-keyword">if</span> (attempt &lt;= <span class="hljs-number">2</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">150</span><br>  <span class="hljs-keyword">if</span> (attempt &lt;= <span class="hljs-number">5</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">300</span><br>  <span class="hljs-keyword">return</span> <span class="hljs-number">500</span><br>&#125;<br><br><span class="hljs-keyword">const</span> <span class="hljs-title function_">tryCollect</span> = (<span class="hljs-params"></span>) =&gt; &#123;<br>  <span class="hljs-keyword">const</span> features = map.<span class="hljs-title function_">querySourceFeatures</span>(sourceId, &#123; sourceLayer &#125;)<br>  <span class="hljs-keyword">if</span> (features.<span class="hljs-property">length</span> &gt; <span class="hljs-number">0</span>) &#123;<br>    <span class="hljs-comment">// 收集 properties.type 集合 + 每个 type 的几何类型（Point/LineString/Polygon）</span><br>    <span class="hljs-title function_">resolve</span>(&#123; types, typeGeometries &#125;)<br>    <span class="hljs-keyword">return</span><br>  &#125;<br>  <span class="hljs-keyword">if</span> (attempt &lt; <span class="hljs-variable constant_">MAX_ATTEMPTS</span>) timer = <span class="hljs-built_in">setTimeout</span>(tryCollect, <span class="hljs-title function_">getBackoffInterval</span>())<br>  <span class="hljs-keyword">else</span> <span class="hljs-title function_">resolve</span>(<span class="hljs-literal">null</span>)   <span class="hljs-comment">// 超时兜底：维持 Phase 1 的渲染结果</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>精炼阶段的 diff 逻辑克制到「多一步都不做」：类型集合一致、几何类型与渲染 code 匹配（<code>GEOM_TO_CODE</code> 映射：<code>Point → {point, circle}</code>、<code>Polygon → {polygon, fill}</code>）、且渲染图层确实有要素命中——三条全过就<strong>不重建</strong>，避免无谓的移除&#x2F;重加造成闪烁。</p>
<p>这个模式的本质是把「<strong>乐观渲染 + 后台校验</strong>」搬到了地图场景：与其等真相出来再画，不如先按最可信的假设画出来，真相到了只在偏差时纠正。类似的思路在离线优先的编辑器、React 的并发渲染里都能看到影子。</p>
<h2 id="五、面要素的图标重复：质心图层方案"><a href="#五、面要素的图标重复：质心图层方案" class="headerlink" title="五、面要素的图标重复：质心图层方案"></a>五、面要素的图标重复：质心图层方案</h2><p>面要素（比如一片行政区、一个园区）想标注图标，直接在 vector source 上加 symbol 图层会踩坑：<strong>MapLibre 会在每个包含该要素的瓦片里各放一个图标</strong>。低缩放时一个瓦片能罩住整个面，看不出来；一放大，面横跨 6 个瓦片，图标就排成了 6 个。</p>
<p>解法是把图标从瓦片渲染里拿出来，放到<strong>独立 GeoJSON source</strong> 上：</p>
<ol>
<li><code>querySourceFeatures</code> 取出面要素几何，计算每个面的<strong>质心</strong>（外环顶点坐标平均）；</li>
<li>质心点写进独立的 GeoJSON source，symbol 图层挂在质心 source 上——一个面永远只有一个图标；</li>
<li>监听 <code>sourcedata</code> 事件，<strong>新瓦片到达时增量补充</strong>新出现的面质心（150ms 防抖，<code>seenIds</code> 集合去重）。</li>
</ol>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts">map.<span class="hljs-title function_">on</span>(<span class="hljs-string">&#x27;sourcedata&#x27;</span>, <span class="hljs-function">(<span class="hljs-params">e</span>) =&gt;</span> &#123;<br>  <span class="hljs-keyword">if</span> (e.<span class="hljs-property">sourceId</span> !== sourceId || !e.<span class="hljs-property">isSourceLoaded</span> || !e.<span class="hljs-property">tile</span>) <span class="hljs-keyword">return</span><br>  <span class="hljs-comment">// 防抖后：querySourceFeatures → 算新面质心 → geoSource.setData(prev.concat(newFeats))</span><br>&#125;)<br></code></pre></td></tr></table></figure>

<h2 id="六、显隐、叠放、透明度：三个小专题"><a href="#六、显隐、叠放、透明度：三个小专题" class="headerlink" title="六、显隐、叠放、透明度：三个小专题"></a>六、显隐、叠放、透明度：三个小专题</h2><h3 id="显隐：隐藏不销毁"><a href="#显隐：隐藏不销毁" class="headerlink" title="显隐：隐藏不销毁"></a>显隐：隐藏不销毁</h3><p>取消勾选只把图层 <code>visibility</code> 设为 <code>none</code>，<strong>source 原地保留</strong>：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> <span class="hljs-title function_">setLayerVisibility</span>(<span class="hljs-params">map, layerIds, visible</span>) &#123;<br>  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> id <span class="hljs-keyword">of</span> layerIds) &#123;<br>    map.<span class="hljs-title function_">setLayoutProperty</span>(id, <span class="hljs-string">&#x27;visibility&#x27;</span>, visible ? <span class="hljs-string">&#x27;visible&#x27;</span> : <span class="hljs-string">&#x27;none&#x27;</span>)<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>再次勾选时直接切回 visible，瓦片零请求、秒出。代价是内存里留着 source，但大屏场景图层总量有限，这笔账划算。显示时配一段 350ms 的 rAF 渐入动画（easeOutCubic 缓动），并用 <code>_previouslyShown</code> 集合记录「首次加载才动画，再次显示直接出」，避免每次勾选都闪一下。</p>
<h3 id="叠放：MapLibre-没有-z-index"><a href="#叠放：MapLibre-没有-z-index" class="headerlink" title="叠放：MapLibre 没有 z-index"></a>叠放：MapLibre 没有 z-index</h3><p>WebGL 地图的图层顺序 &#x3D; <strong>addLayer 的先后顺序</strong>，想调整只有 <code>map.moveLayer(id)</code>（不传第二个参数即置顶）一条路。我们的设施叠放顺序是后端下发的全局配置（<code>orderIds[0]</code> &#x3D; 最上层），应用方式是「<strong>从最底层开始逐个置顶</strong>」：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> <span class="hljs-title function_">applyFacilityOrderToMap</span>(<span class="hljs-params">map</span>) &#123;<br>  <span class="hljs-keyword">const</span> orderedLeaves = <span class="hljs-title function_">getOrderedFacilityLeaves</span>(layers, graphLayers, orderIds)<br>  <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> node <span class="hljs-keyword">of</span> orderedLeaves) &#123;          <span class="hljs-comment">// 数组顺序 = 从下到上</span><br>    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> layerId <span class="hljs-keyword">of</span> <span class="hljs-title function_">collectNodeMapLayerIds</span>(node)) &#123;<br>      <span class="hljs-keyword">if</span> (map.<span class="hljs-title function_">getLayer</span>(layerId)) map.<span class="hljs-title function_">moveLayer</span>(layerId)  <span class="hljs-comment">// 逐个置顶</span><br>    &#125;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>坑在于图层是<strong>异步</strong>创建的——两阶段渲染的 Phase 2 可能随时重建图层，质心层还会延迟 200ms 追加，每次都会破坏顺序。所以所有会新增图层的路径末尾统一调一个 <strong>50ms 防抖的重排</strong>，保证最终状态收敛到全局排序。</p>
<h3 id="透明度：别覆盖样式默认值"><a href="#透明度：别覆盖样式默认值" class="headerlink" title="透明度：别覆盖样式默认值"></a>透明度：别覆盖样式默认值</h3><p>面图层样式里常写着 <code>fill-opacity: 0.25</code> 这种「设计师调好的半透明」。如果用户拖滑块设 0.8，直接 <code>setPaintProperty(0.8)</code> 就把原设计覆盖了；再拖回 1.0，图变成实心——原始基准丢了。</p>
<p>解法是<strong>基准透明度缓存</strong>：首次读某图层时把它的原始 opacity 记下来，之后永远写 <code>userOpacity × base</code>：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">const</span> _baseOpacityCache = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Map</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-built_in">number</span>&gt;()<br><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">applyOpacity</span>(<span class="hljs-params">map, layerId, userOpacity</span>) &#123;<br>  <span class="hljs-keyword">const</span> base = <span class="hljs-title function_">ensureBaseOpacity</span>(map, layerId)  <span class="hljs-comment">// 首次读取并缓存原始值</span><br>  <span class="hljs-keyword">const</span> effective = <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">max</span>(<span class="hljs-number">0</span>, <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">min</span>(<span class="hljs-number">1</span>, userOpacity * base))<br>  map.<span class="hljs-title function_">setPaintProperty</span>(layerId, <span class="hljs-string">`<span class="hljs-subst">$&#123;layer.<span class="hljs-keyword">type</span>&#125;</span>-opacity`</span>, effective)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>图层销毁时同步清缓存（<code>clearLayerBaseOpacityCache</code>），否则重建后的图层会拿旧基准。</p>
<h2 id="七、Vue-与地图实例的相处：markRaw-toRaw"><a href="#七、Vue-与地图实例的相处：markRaw-toRaw" class="headerlink" title="七、Vue 与地图实例的相处：markRaw + toRaw"></a>七、Vue 与地图实例的相处：markRaw + toRaw</h2><p>MapLibre 的 Map 实例内部有大量 <code>Set</code>&#x2F;<code>Map</code> 和 WebGL 资源。放进 Vue 的 <code>ref</code> 会被 <strong>深度 Proxy 代理</strong>，读写属性都过一遍 Proxy，轻则性能劣化，重则 MapLibre 内部依赖引用相等性的逻辑直接失效（Proxy 包装后的对象 <code>!==</code> 原对象）。</p>
<p>处理三件套：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// 1. 存储用 shallowRef + markRaw</span><br><span class="hljs-keyword">const</span> mapInstance = shallowRef&lt;<span class="hljs-title class_">MapLibreMap</span> | <span class="hljs-literal">null</span>&gt;(<span class="hljs-literal">null</span>)<br><span class="hljs-keyword">function</span> <span class="hljs-title function_">setMapInstance</span>(<span class="hljs-params"><span class="hljs-attr">map</span>: <span class="hljs-title class_">MapLibreMap</span> | <span class="hljs-literal">null</span></span>) &#123;<br>  mapInstance.<span class="hljs-property">value</span> = map ? <span class="hljs-title function_">markRaw</span>(map) : <span class="hljs-literal">null</span>   <span class="hljs-comment">// markRaw 阻止深度代理</span><br>&#125;<br><br><span class="hljs-comment">// 2. 使用前 toRaw 兜底（防中间环节被 reactive 污染）</span><br><span class="hljs-keyword">const</span> map = <span class="hljs-title function_">toRaw</span>(mapInstance.<span class="hljs-property">value</span>)<br><br><span class="hljs-comment">// 3. 传给地图 API 的节点对象同样 toRaw</span><br><span class="hljs-title function_">renderNodeLayers</span>(map, <span class="hljs-title function_">toRaw</span>(node))<br></code></pre></td></tr></table></figure>

<p>一句话记住：<strong>地图实例和一切会进地图 API 的对象，都要待在响应式系统之外</strong>。真的需要响应式的（中心点、缩放级别），单独用基本类型 ref 同步一份。</p>
<h2 id="八、底图切换：setStyle-是一次「大清洗」"><a href="#八、底图切换：setStyle-是一次「大清洗」" class="headerlink" title="八、底图切换：setStyle 是一次「大清洗」"></a>八、底图切换：setStyle 是一次「大清洗」</h2><p><code>map.setStyle(newStyle)</code> 会<strong>替换整个样式</strong>，所有手动 <code>addLayer/addSource</code> 的业务图层全部蒸发。正确姿势是把它当成一次受控的销毁重建：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> <span class="hljs-title function_">switchMapStyle</span>(<span class="hljs-params">map, styleId</span>) &#123;<br>  <span class="hljs-keyword">const</span> snapshot = layerStore.<span class="hljs-property">layers</span><br>  layerStore.<span class="hljs-title function_">removeAllFromMap</span>(map)          <span class="hljs-comment">// ① 主动清干净业务层（含事件监听）</span><br>  map.<span class="hljs-title function_">setStyle</span>(newStyle, &#123; <span class="hljs-attr">diff</span>: <span class="hljs-literal">true</span> &#125;)    <span class="hljs-comment">// ② diff 模式：能复用的底层不动</span><br>  map.<span class="hljs-title function_">once</span>(<span class="hljs-string">&#x27;style.load&#x27;</span>, <span class="hljs-function">() =&gt;</span> &#123;<br>    <span class="hljs-title function_">requestAnimationFrame</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>      <span class="hljs-title function_">registerLayerIcons</span>(map, <span class="hljs-title function_">flattenLeafNodes</span>(snapshot))  <span class="hljs-comment">// ③ 重注册图标</span><br>        .<span class="hljs-title function_">then</span>(<span class="hljs-function">() =&gt;</span> layerStore.<span class="hljs-title function_">syncAfterLoad</span>(map))         <span class="hljs-comment">// ④ 按勾选状态重渲染</span><br>    &#125;)<br>  &#125;)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>两个细节：<code>{ diff: true }</code> 让 MapLibre 尽量增量替换而非全部重载；底图 style JSON 提前用原生 <code>fetch</code> 预取并缓存（<code>force-cache</code>），切换时省一次网络往返。另外图标也要重注册——<code>addImage</code> 注册的图片同样活在 style 里，<code>setStyle</code> 后一样会丢。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>整套图层管理的生命周期一图流：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">初始化     并行拉取：图层树 + 全局样式 + 叠放顺序（样式进 sessionStorage 免重复请求）<br>  ↓<br>勾选图层   树节点 visible = true（localStorage 持久化）<br>  ↓         ↓<br>首次加载   fetchLayerTiles → addSource → Phase 1 立即渲染（缓存 key）<br>  ↓         Phase 2 idle 探测 → diff → 必要时重建 → 质心图层 → 防抖重排<br>  ↓<br>再次勾选   visibility 直接切回，渐入动画，零瓦片请求<br>  ↓<br>样式变更   保留 source，只拆重建 layer<br>  ↓<br>切底图     removeAll → setStyle(diff) → style.load 后重注册图标 + 全量恢复<br></code></pre></td></tr></table></figure>

<p>问题速查表：</p>
<table>
<thead>
<tr>
<th>问题</th>
<th>根因</th>
<th>解法</th>
</tr>
</thead>
<tbody><tr>
<td>图层样式要等瓦片加载完才出来</td>
<td>渲染 key 依赖瓦片数据</td>
<td>两阶段：缓存 key 乐观渲染 + idle 探测校验</td>
</tr>
<tr>
<td>面要素图标放大后重复</td>
<td>symbol 按瓦片放置</td>
<td>质心 GeoJSON source + sourcedata 增量刷新</td>
</tr>
<tr>
<td>图层顺序被异步加载打乱</td>
<td>moveLayer 顺序 &#x3D; 添加顺序</td>
<td>全局排序数组 + 新增图层后 50ms 防抖重排</td>
</tr>
<tr>
<td>透明度滑块毁掉样式默认值</td>
<td>setPaintProperty 覆盖基准</td>
<td>基准透明度缓存，写 userOpacity × base</td>
</tr>
<tr>
<td>切底图业务图层全丢</td>
<td>setStyle 重置整个样式</td>
<td>主动清除 → diff 切换 → style.load 后恢复</td>
</tr>
<tr>
<td>地图实例放 ref 随机报错</td>
<td>深度 Proxy 破坏内部结构</td>
<td>shallowRef + markRaw + toRaw 三件套</td>
</tr>
</tbody></table>
<p><strong>一句话记住</strong>：大屏图层管理的核心不是「往地图上加图层」，而是管理<strong>树节点与 MapLibre layer 之间的映射</strong>——渲染可以乐观、校验放后台，但每个 layer 的出生（ID 命名）、户籍（<code>_mapLayerIds</code>）、销毁（source 保留策略）都必须在掌控之中。</p>
]]></content>
      <categories>
        <category>Vue 进阶</category>
      </categories>
      <tags>
        <tag>vue3</tag>
        <tag>pinia</tag>
        <tag>maplibre</tag>
        <tag>gis</tag>
        <tag>前端架构</tag>
      </tags>
  </entry>
  <entry>
    <title>TCP、HTTP、WebSocket 与 SSE：一根管道上的四种用法</title>
    <url>/2026/09/10/network-tcp-http-websocket-sse/</url>
    <content><![CDATA[<p>这四个名字经常一起出现，但它们并不在同一层面上竞争：</p>
<blockquote>
<p><strong>TCP 是传输层的「管道」</strong>：保证字节可靠、有序地到达，但对内容的格式一无所知。<strong>HTTP、SSE、WebSocket 都是应用层协议</strong>，跑在同一根 TCP 管道上——区别只在于怎么用这根管道：HTTP 用报文规则切出「一问一答」，SSE 让一次响应永远不结束从而单向推送，WebSocket 握手后换成自己的帧协议实现全双工。</p>
</blockquote>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">应用层    HTTP(一问一答)   SSE(单向事件流)   WebSocket(全双工帧)<br>              │               │                  │<br>传输层        └───────────── TCP(可靠字节流) ─────────────┘<br>网络层                          IP(尽力送达)<br></code></pre></td></tr></table></figure>

<span id="more"></span>

<h2 id="一、TCP：一根没有消息边界的字节流"><a href="#一、TCP：一根没有消息边界的字节流" class="headerlink" title="一、TCP：一根没有消息边界的字节流"></a>一、TCP：一根没有消息边界的字节流</h2><p>IP 层只管「尽力送达」，丢包、乱序、重复都不负责。TCP 在其上补齐三件事：</p>
<ul>
<li><strong>连接</strong>：握手同步双方的起始序号，挥手协商关闭；</li>
<li><strong>可靠</strong>：每个字节都有编号，接收方逐段确认（ACK），超时未确认就重传；</li>
<li><strong>不撑死对方</strong>：滑动窗口做流量控制（别超过接收方缓冲区），拥塞窗口做拥塞控制（别超过网络承载力）。</li>
</ul>
<h3 id="1-1-先搞懂-seq-和-ack，握手才看得懂"><a href="#1-1-先搞懂-seq-和-ack，握手才看得懂" class="headerlink" title="1.1 先搞懂 seq 和 ack，握手才看得懂"></a>1.1 先搞懂 seq 和 ack，握手才看得懂</h3><p>TCP 报文头里最容易「背了就忘」的就是这两个数字，其实语义很简单：</p>
<ul>
<li><strong>seq（序号）</strong>：本报文段携带的数据，从字节流的<strong>第几个字节</strong>开始编号；</li>
<li><strong>ack（确认号）</strong>：你发来的数据我已经按序收到了 <code>ack - 1</code> 字节，<strong>期望你下一个从 <code>ack</code> 开始发</strong>。</li>
</ul>
<p>数据传输阶段两者如何走，一个例子就够：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">客户端                                服务端<br>  │ ── seq=1001, 数据 &quot;Hello&quot; ──► │    发 5 个字节<br>  │ ◄─ ack=1006 ──────────────── │    1001+5，&quot;前 1005 个都收到了，下次从 1006 发&quot;<br></code></pre></td></tr></table></figure>

<p>「确认 &#x3D; 收到的最后字节序号 + 1」这条规则，在握手和挥手里会反复出现。</p>
<h3 id="1-2-三次握手：交换的不是数据，是初始序号"><a href="#1-2-三次握手：交换的不是数据，是初始序号" class="headerlink" title="1.2 三次握手：交换的不是数据，是初始序号"></a>1.2 三次握手：交换的不是数据，是初始序号</h3><p>建立连接前，双方对彼此的编号体系一无所知。握手要做的事：<strong>交换初始序号（ISN），并让双方都确认「自己和对方的收发能力都正常」</strong>。带具体数字走一遍：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">客户端                                       服务端（LISTEN）<br>  │                                            │<br>  │ ──① SYN, seq=1000 ──────────────────────► │   &quot;请求连接，我的数据从 1000 开始编号&quot;<br>  │   SYN_SENT                                 │   SYN_RCVD<br>  │                                            │<br>  │ ◄─② SYN+ACK, seq=5000, ack=1001 ────────── │   &quot;同意；我的数据从 5000 开始，<br>  │   ESTABLISHED                              │      你的 1000 我收到了(占一个序号)&quot;<br>  │                                            │<br>  │ ──③ ACK, ack=5001 ──────────────────────► │   ESTABLISHED<br></code></pre></td></tr></table></figure>

<p>每一步之后，「谁确认了什么」：</p>
<table>
<thead>
<tr>
<th>报文</th>
<th>客户端已确认</th>
<th>服务端已确认</th>
</tr>
</thead>
<tbody><tr>
<td>① SYN</td>
<td>—（还什么都不知道）</td>
<td>客户端能发、自己能收</td>
</tr>
<tr>
<td>② SYN+ACK</td>
<td>自己能发能收、服务端能发能收</td>
<td>（同①）</td>
</tr>
<tr>
<td>③ ACK</td>
<td>（同②）</td>
<td>自己能发、客户端能收 → 双方齐全</td>
</tr>
</tbody></table>
<p><strong>为什么不是两次</strong>：走到②就停的话，服务端已经单方面建立连接，但它发出的 <code>SYN+ACK</code> 有没有被客户端收到，永远没有验证——包丢了它就只能傻等。另一个经典场景：网络里滞留的<strong>旧 SYN</strong>（历史连接的残包）迟到抵达服务端，两次握手下服务端会直接建连白占资源；三次握手下客户端发现 <code>ack</code> 跟自己的序号对不上，回 RST 拒绝。</p>
<p><strong>为什么不是四次</strong>：② 里服务端的「确认对方」(ACK) 和「介绍自己」(SYN) 没有理由分开发，合并成一个包；三次已经让双方的确认闭环，多一次纯浪费。</p>
<p><strong>为什么握手包也要占序号</strong>：SYN 和 FIN 虽然不携带数据，但都是「改变字节流状态」的关键报文，各占一个序号，所以②里的 ack 是对方 seq + 1。丢包重传和数据包共用同一套 seq&#x2F;ack 体系——比如③丢了，服务端会重传 <code>SYN+ACK</code>；若客户端已经直接开始发数据，数据报文自带的 <code>ack=5001</code> 同样能让服务端进入 ESTABLISHED。</p>
<h3 id="1-3-四次挥手：FIN-只关一个方向"><a href="#1-3-四次挥手：FIN-只关一个方向" class="headerlink" title="1.3 四次挥手：FIN 只关一个方向"></a>1.3 四次挥手：FIN 只关一个方向</h3><p>TCP 是<strong>全双工</strong>的——两个方向各自独立收发，所以关闭也要<strong>两边各关各的</strong>。FIN 的语义不是「断开连接」，而是「<strong>我的数据发完了</strong>」：只关写方向，读方向还开着（半关闭）。用代码验证这一点：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> net = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;net&#x27;</span>);<br><span class="hljs-keyword">const</span> <span class="hljs-title function_">log</span> = (<span class="hljs-params">who, msg</span>) =&gt; <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">`[<span class="hljs-subst">$&#123;who&#125;</span>] <span class="hljs-subst">$&#123;msg&#125;</span>`</span>);<br><br><span class="hljs-comment">// allowHalfOpen: true —— Node 默认收到 FIN 会自动回 FIN，</span><br><span class="hljs-comment">// 关掉这个默认行为，才能看到协议本来的半关闭</span><br><span class="hljs-keyword">const</span> server = net.<span class="hljs-title function_">createServer</span>(&#123; <span class="hljs-attr">allowHalfOpen</span>: <span class="hljs-literal">true</span> &#125;, <span class="hljs-function">(<span class="hljs-params">socket</span>) =&gt;</span> &#123;<br>  socket.<span class="hljs-title function_">on</span>(<span class="hljs-string">&#x27;data&#x27;</span>, <span class="hljs-function">() =&gt;</span> &#123;&#125;);            <span class="hljs-comment">// 消费数据，&#x27;end&#x27; 才会触发</span><br>  socket.<span class="hljs-title function_">write</span>(<span class="hljs-string">&#x27;服务端：连接刚建立时发的数据\n&#x27;</span>);<br>  socket.<span class="hljs-title function_">on</span>(<span class="hljs-string">&#x27;end&#x27;</span>, <span class="hljs-function">() =&gt;</span> &#123;                <span class="hljs-comment">// 收到客户端 FIN：它不再发了</span><br>    <span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;server&#x27;</span>, <span class="hljs-string">&#x27;收到 FIN，但我这边还有话没说完&#x27;</span>);<br>    socket.<span class="hljs-title function_">write</span>(<span class="hljs-string">&#x27;收到你的FIN，我这边还有话没说完\n&#x27;</span>);   <span class="hljs-comment">// 半关闭下照常发送</span><br>    <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> socket.<span class="hljs-title function_">end</span>(<span class="hljs-string">&#x27;说完了，我也关\n&#x27;</span>), <span class="hljs-number">200</span>); <span class="hljs-comment">// 这才是我的 FIN</span><br>  &#125;);<br>&#125;);<br><br>server.<span class="hljs-title function_">listen</span>(<span class="hljs-number">3000</span>, <span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-keyword">const</span> client = net.<span class="hljs-title function_">connect</span>(<span class="hljs-number">3000</span>, <span class="hljs-function">() =&gt;</span> &#123;<br>    client.<span class="hljs-title function_">end</span>(<span class="hljs-string">&#x27;客户端：我的数据发完了，先关写端\n&#x27;</span>);     <span class="hljs-comment">// 数据 + FIN 一起发出</span><br>  &#125;);<br>  client.<span class="hljs-title function_">on</span>(<span class="hljs-string">&#x27;data&#x27;</span>, <span class="hljs-function">(<span class="hljs-params">c</span>) =&gt;</span> <span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;client&#x27;</span>, <span class="hljs-string">&#x27;仍能收到: &#x27;</span> + c.<span class="hljs-title function_">toString</span>().<span class="hljs-title function_">trim</span>()));<br>&#125;);<br></code></pre></td></tr></table></figure>

<p>运行输出（真实运行结果）：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">[client] 已连接，发完数据就关写端<br>[client] 仍能收到: 服务端：连接刚建立时发的数据<br>[server] 收到 FIN，但我这边还有话没说完<br>[client] 仍能收到: 收到你的FIN，我这边还有话没说完<br>[client] 仍能收到: 说完了，我也关<br>[client] 连接完全关闭<br></code></pre></td></tr></table></figure>

<p>客户端发出 FIN 之后，<strong>还在持续收到服务端的数据</strong>——这就是「半关闭」，也是四次挥手的全部秘密。对着状态机走一遍（序号接上文数据阶段的 1006 &#x2F; 5003）：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">客户端                                       服务端<br>  │ ──① FIN, seq=1006 ─────────────────────► │   &quot;我的数据发完了&quot;<br>  │   FIN_WAIT_1                              │   CLOSE_WAIT（注意：还能发数据！）<br>  │ ◄─② ACK, ack=1007 ────────────────────── │   &quot;知道了&quot;<br>  │   FIN_WAIT_2（半关闭：只收不发）           │<br>  │            ……服务端把剩余数据发完……        │<br>  │ ◄─③ FIN, seq=5003 ─────────────────────── │   &quot;我也发完了&quot;<br>  │   TIME_WAIT                               │   LAST_ACK<br>  │ ──④ ACK, ack=5004 ──────────────────────► │<br>  │   等 2MSL 后 → CLOSED                      │   收到 ACK → CLOSED<br></code></pre></td></tr></table></figure>

<p><strong>为什么是四次而不是三次</strong>：收到对方的 FIN，ACK 必须<strong>立刻</strong>回（这是 TCP 的确认义务）；但自己的 FIN 意味着「我发完了」，要<strong>等应用层把剩余数据写完</strong>才能发。②和③中间隔着「剩余数据」的发送窗口，没法像握手的 SYN+ACK 那样合并成一个包。上面的运行输出里，②和③之间隔了服务端的两条消息——间隔里发生的事，正是多出那一次挥手的原因。</p>
<p><strong>为什么主动方要等 2MSL（TIME_WAIT）</strong>：</p>
<ol>
<li>若④这个 ACK 丢了，服务端会重传 FIN；客户端若已经 CLOSED 就无法应答，服务端将永远停在 LAST_ACK。等 2MSL &#x3D; ACK 去程最多 1 个 MSL + 重传 FIN 回程最多再 1 个 MSL，兜住这次重传；</li>
<li>让本连接的旧报文在网络上自然消亡，防止污染之后复用同一四元组（同 IP 同端口）的新连接。</li>
</ol>
<h3 id="1-4-字节流无边界：应用层协议的起点"><a href="#1-4-字节流无边界：应用层协议的起点" class="headerlink" title="1.4 字节流无边界：应用层协议的起点"></a>1.4 字节流无边界：应用层协议的起点</h3><p>TCP 对上层只交付「一串连续字节」，<strong>写入几次和读取几次没有对应关系</strong>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> net = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;net&#x27;</span>);<br><span class="hljs-comment">// 服务端：连着写两条&quot;消息&quot;</span><br>net.<span class="hljs-title function_">createServer</span>(<span class="hljs-function">(<span class="hljs-params">socket</span>) =&gt;</span> &#123;<br>  socket.<span class="hljs-title function_">write</span>(<span class="hljs-string">&#x27;hello&#x27;</span>);<br>  socket.<span class="hljs-title function_">write</span>(<span class="hljs-string">&#x27;world&#x27;</span>);<br>  socket.<span class="hljs-title function_">end</span>();<br>&#125;).<span class="hljs-title function_">listen</span>(<span class="hljs-number">3001</span>);<br><br><span class="hljs-comment">// 客户端</span><br><span class="hljs-keyword">const</span> client = net.<span class="hljs-title function_">connect</span>(<span class="hljs-number">3001</span>);<br>client.<span class="hljs-title function_">on</span>(<span class="hljs-string">&#x27;data&#x27;</span>, <span class="hljs-function">(<span class="hljs-params">c</span>) =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">`收到 &quot;<span class="hljs-subst">$&#123;c&#125;</span>&quot;`</span>));<br></code></pre></td></tr></table></figure>

<p>运行输出——两次 <code>write</code> 合并成了一次 <code>data</code>（也可能反过来被拆开，取决于网络）：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">第 1 次 data 事件, 长度 10: &quot;helloworld&quot;<br></code></pre></td></tr></table></figure>

<p>这不是 bug，是字节流的本性（即所谓粘包&#x2F;拆包）。于是得到贯穿全文的主线：<strong>每个应用层协议的第一件事，都是在无边界的字节流上定义「消息边界」</strong>。接下来三个协议的报文设计，本质是在回答同一个问题：一段字节，从哪开始、到哪结束。</p>
<h2 id="二、HTTP：用报文规则切出「一问一答」"><a href="#二、HTTP：用报文规则切出「一问一答」" class="headerlink" title="二、HTTP：用报文规则切出「一问一答」"></a>二、HTTP：用报文规则切出「一问一答」</h2><p>HTTP 的边界方案浓缩在一条请求的解析规则里：<strong>头部以 <code>\r\n\r\n</code> 结束，正文长度由 <code>Content-Length</code> 声明</strong>。在裸 TCP 上手写一个解析器，规则立刻具象化：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> net = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;net&#x27;</span>);<br><br>net.<span class="hljs-title function_">createServer</span>(<span class="hljs-function">(<span class="hljs-params">socket</span>) =&gt;</span> &#123;<br>  <span class="hljs-keyword">let</span> buf = <span class="hljs-title class_">Buffer</span>.<span class="hljs-title function_">alloc</span>(<span class="hljs-number">0</span>);                       <span class="hljs-comment">// 字节流先攒起来</span><br>  socket.<span class="hljs-title function_">on</span>(<span class="hljs-string">&#x27;data&#x27;</span>, <span class="hljs-function">(<span class="hljs-params">chunk</span>) =&gt;</span> &#123;<br>    buf = <span class="hljs-title class_">Buffer</span>.<span class="hljs-title function_">concat</span>([buf, chunk]);<br>    <span class="hljs-keyword">const</span> idx = buf.<span class="hljs-title function_">indexOf</span>(<span class="hljs-string">&#x27;\r\n\r\n&#x27;</span>);           <span class="hljs-comment">// 头部结束标记</span><br>    <span class="hljs-keyword">if</span> (idx === -<span class="hljs-number">1</span>) <span class="hljs-keyword">return</span>;                        <span class="hljs-comment">// 头没到齐：继续等</span><br>    <span class="hljs-keyword">const</span> head = buf.<span class="hljs-title function_">subarray</span>(<span class="hljs-number">0</span>, idx).<span class="hljs-title function_">toString</span>();<br>    <span class="hljs-keyword">const</span> len = <span class="hljs-title class_">Number</span>(<span class="hljs-regexp">/Content-Length: (\d+)/i</span>.<span class="hljs-title function_">exec</span>(head)?.[<span class="hljs-number">1</span>] ?? <span class="hljs-number">0</span>);<br>    <span class="hljs-keyword">if</span> (buf.<span class="hljs-property">length</span> - idx - <span class="hljs-number">4</span> &lt; len) <span class="hljs-keyword">return</span>;        <span class="hljs-comment">// body 没到齐：继续等</span><br>    <span class="hljs-keyword">const</span> body = buf.<span class="hljs-title function_">subarray</span>(idx + <span class="hljs-number">4</span>, idx + <span class="hljs-number">4</span> + len).<span class="hljs-title function_">toString</span>();<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;--- 解析出完整请求 ---\n&#x27;</span> + head + <span class="hljs-string">&#x27;\n[body] &#x27;</span> + body);<br>    socket.<span class="hljs-title function_">end</span>(<span class="hljs-string">&#x27;HTTP/1.1 200 OK\r\nContent-Length: 2\r\n\r\nok&#x27;</span>);<br>  &#125;);<br>&#125;).<span class="hljs-title function_">listen</span>(<span class="hljs-number">3002</span>);<br><br><span class="hljs-comment">// 客户端：故意分两次写，模拟 TCP 分段到达</span><br><span class="hljs-keyword">const</span> client = net.<span class="hljs-title function_">connect</span>(<span class="hljs-number">3002</span>, <span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-keyword">const</span> body = <span class="hljs-string">&#x27;&#123;&quot;user&quot;:&quot;a&quot;,&quot;pass&quot;:&quot;b&quot;&#125;&#x27;</span>;<br>  <span class="hljs-keyword">const</span> head = <span class="hljs-string">`POST /api/login HTTP/1.1\r\nHost: localhost\r\nContent-Length: <span class="hljs-subst">$&#123;Buffer.byteLength(body)&#125;</span>\r\n\r\n`</span>;<br>  client.<span class="hljs-title function_">write</span>(head);<br>  <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> client.<span class="hljs-title function_">write</span>(body), <span class="hljs-number">100</span>);       <span class="hljs-comment">// 100ms 后 body 才到</span><br>&#125;);<br></code></pre></td></tr></table></figure>

<p>运行输出——头和 body 分两次到达，解析器靠缓冲 + 边界规则拼出完整请求：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">--- 解析出完整请求 ---<br>POST /api/login HTTP/1.1<br>Host: localhost<br>Content-Length: 23<br>[body] &#123;&quot;user&quot;:&quot;a&quot;,&quot;pass&quot;:&quot;b&quot;&#125;<br></code></pre></td></tr></table></figure>

<p>调试这段代码时踩过一个坑，恰好证明这套规则的分量：<code>Content-Length</code> 手写成了 26 而实际 body 是 23 字节，解析器<strong>永远等不齐第 24~26 字节</strong>，连接一挂到底。边界声明差一个字节，整条连接就死掉。</p>
<p>在此之上是 HTTP 的模型与演进：</p>
<ul>
<li><strong>一问一答 + 无状态</strong>：请求永远由客户端发起，协议不记得上一个请求是谁，会话靠 Cookie &#x2F; Token 补；</li>
<li><strong>keep-alive</strong>（HTTP&#x2F;1.1 默认）：一次对话结束不拆 TCP 连接，下一个请求直接复用。能复用的前提正是边界明确——解析器必须准确知道上一个响应在哪里结束；</li>
<li><strong>HTTP&#x2F;1.1 队头阻塞</strong>：一条连接上对话必须排队。HTTP&#x2F;2 的解法是把报文拆成<strong>二进制帧</strong>、标记流 ID 交错传输（多路复用）——注意这步和 WebSocket 殊途同归，都转向「帧 + 长度字段」的边界方案；TCP 层的队头阻塞要等 HTTP&#x2F;3 换 QUIC 才解决。</li>
</ul>
<p>这个模型的能力边界也由此清楚：<strong>服务端无法主动开口</strong>——这是下面两个协议存在的原因。</p>
<h2 id="三、SSE：让一次响应永远不结束"><a href="#三、SSE：让一次响应永远不结束" class="headerlink" title="三、SSE：让一次响应永远不结束"></a>三、SSE：让一次响应永远不结束</h2><p>SSE（Server-Sent Events）的思路简单：<strong>正常发起 GET 请求，服务端不写 Content-Length，让响应一直「没结束」，有新消息就往里写一段</strong>。Node.js 十几行实现一个行情推送：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> http = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;http&#x27;</span>);<br><br>http.<span class="hljs-title function_">createServer</span>(<span class="hljs-function">(<span class="hljs-params">req, res</span>) =&gt;</span> &#123;<br>  <span class="hljs-comment">// 断线重连时浏览器自动带上最后收到的事件 id</span><br>  <span class="hljs-keyword">let</span> id = <span class="hljs-title class_">Number</span>(req.<span class="hljs-property">headers</span>[<span class="hljs-string">&#x27;last-event-id&#x27;</span>] || <span class="hljs-number">0</span>);<br><br>  res.<span class="hljs-title function_">writeHead</span>(<span class="hljs-number">200</span>, &#123;<br>    <span class="hljs-string">&#x27;Content-Type&#x27;</span>: <span class="hljs-string">&#x27;text/event-stream&#x27;</span>,<br>    <span class="hljs-string">&#x27;Cache-Control&#x27;</span>: <span class="hljs-string">&#x27;no-cache&#x27;</span>,<br>  &#125;);<br><br>  <span class="hljs-keyword">const</span> timer = <span class="hljs-built_in">setInterval</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>    <span class="hljs-keyword">const</span> price = (<span class="hljs-number">100</span> + <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">random</span>() * <span class="hljs-number">10</span>).<span class="hljs-title function_">toFixed</span>(<span class="hljs-number">2</span>);<br>    res.<span class="hljs-title function_">write</span>(<span class="hljs-string">`id: <span class="hljs-subst">$&#123;++id&#125;</span>\nevent: price\ndata: <span class="hljs-subst">$&#123;price&#125;</span>\n\n`</span>);<br>  &#125;, <span class="hljs-number">1000</span>);<br><br>  req.<span class="hljs-title function_">on</span>(<span class="hljs-string">&#x27;close&#x27;</span>, <span class="hljs-function">() =&gt;</span> <span class="hljs-built_in">clearInterval</span>(timer));   <span class="hljs-comment">// 客户端断开必须清理</span><br>&#125;).<span class="hljs-title function_">listen</span>(<span class="hljs-number">3000</span>);<br></code></pre></td></tr></table></figure>

<p>线上的数据流（<code>Transfer-Encoding: chunked</code> 分块，永不主动结束）：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">HTTP/1.1 200 OK<br>Content-Type: text/event-stream<br><br>id: 1<br>event: price<br>data: 104.37<br>                                  ← 空行 = 一个事件结束，这就是 SSE 的消息边界<br>id: 2<br>event: price<br>data: 107.91<br><br></code></pre></td></tr></table></figure>

<p>格式规则：每行是 <code>字段: 值</code>，支持 <code>id</code> &#x2F; <code>event</code> &#x2F; <code>data</code> &#x2F; <code>retry</code>（重连等待毫秒数）；<strong>事件之间用空行分隔</strong>。客户端是原生 API：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> es = <span class="hljs-keyword">new</span> <span class="hljs-title class_">EventSource</span>(<span class="hljs-string">&#x27;http://localhost:3000&#x27;</span>);<br>es.<span class="hljs-title function_">addEventListener</span>(<span class="hljs-string">&#x27;price&#x27;</span>, <span class="hljs-function">(<span class="hljs-params">e</span>) =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;最新价格&#x27;</span>, e.<span class="hljs-property">data</span>));<br></code></pre></td></tr></table></figure>

<p>重连机制值得一读：连接断开后 <code>EventSource</code> 自动重试，并把最后收到的 <code>id</code> 放进 <code>Last-Event-ID</code> 请求头——服务端读这个头就能续传（上面服务端代码第一行）。断点续传是协议内建的。能力边界：<strong>单向</strong>（客户端要说话得另发普通请求）、<strong>纯文本</strong>（二进制要 Base64，体积膨胀 1&#x2F;3）、HTTP&#x2F;1.1 下浏览器对同域只开 6 条 TCP 连接（HTTP&#x2F;2 多路复用后无此顾虑）。</p>
<h2 id="四、WebSocket：握手换协议，边界靠帧"><a href="#四、WebSocket：握手换协议，边界靠帧" class="headerlink" title="四、WebSocket：握手换协议，边界靠帧"></a>四、WebSocket：握手换协议，边界靠帧</h2><p>要双向实时（聊天、协同编辑、联机游戏），「SSE + 普通请求」太别扭。WebSocket 的做法更彻底：<strong>先用 HTTP 发起一次「换协议」谈判，成功后这条 TCP 连接不再说 HTTP</strong>。</p>
<h3 id="4-1-握手：一次特殊的-HTTP-请求"><a href="#4-1-握手：一次特殊的-HTTP-请求" class="headerlink" title="4.1 握手：一次特殊的 HTTP 请求"></a>4.1 握手：一次特殊的 HTTP 请求</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">GET /chat HTTP/1.1<br>Upgrade: websocket                          ← 请求升级协议<br>Connection: Upgrade<br>Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ== ← 随机 16 字节的 Base64<br><br>HTTP/1.1 101 Switching Protocols            ← 101：协议切换成功<br>Sec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo=<br></code></pre></td></tr></table></figure>

<p><code>Sec-WebSocket-Accept</code> 是一次防篡改校验，算法固定，用 Node 起手就能复现：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> crypto = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;crypto&#x27;</span>);<br><span class="hljs-keyword">const</span> <span class="hljs-variable constant_">GUID</span> = <span class="hljs-string">&#x27;258EAFA5-E914-47DA-95CA-C5AB0DC85B11&#x27;</span>;   <span class="hljs-comment">// 协议规定的固定串</span><br><span class="hljs-keyword">const</span> accept = crypto<br>  .<span class="hljs-title function_">createHash</span>(<span class="hljs-string">&#x27;sha1&#x27;</span>)<br>  .<span class="hljs-title function_">update</span>(<span class="hljs-string">&#x27;dGhlIHNhbXBsZSBub25jZQ==&#x27;</span> + <span class="hljs-variable constant_">GUID</span>)          <span class="hljs-comment">// key + GUID 做 SHA-1</span><br>  .<span class="hljs-title function_">digest</span>(<span class="hljs-string">&#x27;base64&#x27;</span>);<br><span class="hljs-comment">// → s3pPLMBiTxaQ9kYGzzhZRbK+xOo=，与响应头一致</span><br><span class="hljs-comment">// 双方各自计算比对，确保中间的代理没有篡改握手</span><br></code></pre></td></tr></table></figure>

<h3 id="4-2-帧：逐字节分析边界方案"><a href="#4-2-帧：逐字节分析边界方案" class="headerlink" title="4.2 帧：逐字节分析边界方案"></a>4.2 帧：逐字节分析边界方案</h3><p>握手之后，字节流上跑的换成 WebSocket 帧。先看真实字节——服务端发一个 <code>&quot;Hello&quot;</code>，线上就是这 7 个：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">81 05 48 65 6c 6c 6f<br>│  │  └────────┬────────┘<br>│  │        &quot;Hello&quot; 的 ASCII<br>│  └ Payload len = 5（无掩码）<br>└ FIN=1 且 opcode=0x1（文本帧，单帧即完整消息）<br></code></pre></td></tr></table></figure>

<p>帧头各字段的职责：</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>FIN</td>
<td>1 表示消息的最后一帧（大消息可分片）</td>
</tr>
<tr>
<td>opcode</td>
<td><code>0x1</code> 文本、<code>0x2</code> 二进制、<code>0x0</code> 延续帧、<code>0x8</code> 关闭、<code>0x9</code> ping、<code>0xA</code> pong</td>
</tr>
<tr>
<td>MASK</td>
<td>是否掩码。<strong>客户端发的帧必须掩码，服务端发的不掩码</strong>（防中间代理被恶意客户端欺骗解析）</td>
</tr>
<tr>
<td>Payload len</td>
<td>7 位存长度；不够用时置 126 &#x2F; 127，再跟 2 或 8 字节扩展长度</td>
</tr>
</tbody></table>
<p>按这套规则手写解码器，粘包在此天然解决——读完帧头知道长度，收满即一条完整消息：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">function</span> <span class="hljs-title function_">decodeFrame</span>(<span class="hljs-params">buf</span>) &#123;<br>  <span class="hljs-keyword">const</span> fin = (buf[<span class="hljs-number">0</span>] &amp; <span class="hljs-number">0x80</span>) !== <span class="hljs-number">0</span>;       <span class="hljs-comment">// 最高位</span><br>  <span class="hljs-keyword">const</span> opcode = buf[<span class="hljs-number">0</span>] &amp; <span class="hljs-number">0x0f</span>;            <span class="hljs-comment">// 低 4 位</span><br>  <span class="hljs-keyword">const</span> masked = (buf[<span class="hljs-number">1</span>] &amp; <span class="hljs-number">0x80</span>) !== <span class="hljs-number">0</span>;<br>  <span class="hljs-keyword">let</span> len = buf[<span class="hljs-number">1</span>] &amp; <span class="hljs-number">0x7f</span>;<br>  <span class="hljs-keyword">let</span> offset = <span class="hljs-number">2</span>;<br>  <span class="hljs-keyword">if</span> (len === <span class="hljs-number">126</span>) &#123; len = buf.<span class="hljs-title function_">readUInt16BE</span>(offset); offset += <span class="hljs-number">2</span>; &#125;<br>  <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (len === <span class="hljs-number">127</span>) &#123; len = <span class="hljs-title class_">Number</span>(buf.<span class="hljs-title function_">readBigUInt64BE</span>(offset)); offset += <span class="hljs-number">8</span>; &#125;<br>  <span class="hljs-keyword">let</span> mask = <span class="hljs-literal">null</span>;<br>  <span class="hljs-keyword">if</span> (masked) &#123; mask = buf.<span class="hljs-title function_">subarray</span>(offset, offset + <span class="hljs-number">4</span>); offset += <span class="hljs-number">4</span>; &#125;<br>  <span class="hljs-keyword">const</span> payload = buf.<span class="hljs-title function_">subarray</span>(offset, offset + len);<br>  <span class="hljs-keyword">if</span> (mask) <span class="hljs-keyword">for</span> (<span class="hljs-keyword">let</span> i = <span class="hljs-number">0</span>; i &lt; payload.<span class="hljs-property">length</span>; i++) payload[i] ^= mask[i % <span class="hljs-number">4</span>];  <span class="hljs-comment">// 异或解掩码</span><br>  <span class="hljs-keyword">return</span> &#123; fin, opcode, masked, <span class="hljs-attr">data</span>: payload.<span class="hljs-title function_">toString</span>() &#125;;<br>&#125;<br><br><span class="hljs-comment">// 验证：服务端帧 &quot;Hello&quot;（无掩码）</span><br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-title function_">decodeFrame</span>(<span class="hljs-title class_">Buffer</span>.<span class="hljs-title function_">from</span>([<span class="hljs-number">0x81</span>, <span class="hljs-number">0x05</span>, ...<span class="hljs-title class_">Buffer</span>.<span class="hljs-title function_">from</span>(<span class="hljs-string">&#x27;Hello&#x27;</span>)])));<br></code></pre></td></tr></table></figure>

<p>运行输出（含一个真实构造的掩码客户端帧 <code>81 82 37fa213d 7f93</code>，第二字节 <code>82</code> 的最高位就是 MASK&#x3D;1）：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">服务端帧 : 810548656c6c6f<br>解码结果 : &#123; fin: true, opcode: 1, masked: false, data: &#x27;Hello&#x27; &#125;<br>客户端帧 : 818237fa213d7f93<br>解码结果 : &#123; fin: true, opcode: 1, masked: true, data: &#x27;Hi&#x27; &#125;<br></code></pre></td></tr></table></figure>

<p>心跳也是帧：一方发 opcode&#x3D;0x9 的 ping，另一方必须回 0xA 的 pong，超时无响应即可判定连接已死。实际开发用 <code>ws</code> 库，不必手撕：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> &#123; <span class="hljs-title class_">WebSocketServer</span> &#125; = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;ws&#x27;</span>);          <span class="hljs-comment">// 服务端</span><br><span class="hljs-keyword">const</span> wss = <span class="hljs-keyword">new</span> <span class="hljs-title class_">WebSocketServer</span>(&#123; <span class="hljs-attr">port</span>: <span class="hljs-number">3000</span> &#125;);<br>wss.<span class="hljs-title function_">on</span>(<span class="hljs-string">&#x27;connection&#x27;</span>, <span class="hljs-function">(<span class="hljs-params">ws</span>) =&gt;</span> &#123;<br>  ws.<span class="hljs-title function_">on</span>(<span class="hljs-string">&#x27;message&#x27;</span>, <span class="hljs-function">(<span class="hljs-params">data</span>) =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;收到&#x27;</span>, data.<span class="hljs-title function_">toString</span>()));<br>  ws.<span class="hljs-title function_">send</span>(<span class="hljs-string">&#x27;welcome&#x27;</span>);<br>&#125;);<br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> ws = <span class="hljs-keyword">new</span> <span class="hljs-title class_">WebSocket</span>(<span class="hljs-string">&#x27;ws://localhost:3000&#x27;</span>);    <span class="hljs-comment">// 客户端（浏览器原生）</span><br>ws.<span class="hljs-property">onmessage</span> = <span class="hljs-function">(<span class="hljs-params">e</span>) =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(e.<span class="hljs-property">data</span>);<br>ws.<span class="hljs-title function_">send</span>(<span class="hljs-string">&#x27;hello&#x27;</span>);<br></code></pre></td></tr></table></figure>

<p>代价是<strong>脱离 HTTP 生态</strong>：没有状态码、缓存、Content-Type 语义，鉴权要塞进 URL 或首条消息，断线重连、心跳都要自己实现。</p>
<h2 id="五、怎么选"><a href="#五、怎么选" class="headerlink" title="五、怎么选"></a>五、怎么选</h2><table>
<thead>
<tr>
<th></th>
<th>HTTP</th>
<th>SSE</th>
<th>WebSocket</th>
</tr>
</thead>
<tbody><tr>
<td>通信方向</td>
<td>一问一答</td>
<td>服务端 → 客户端单向流</td>
<td>全双工</td>
</tr>
<tr>
<td>底层依赖</td>
<td>TCP</td>
<td>普通 HTTP 长响应（chunked）</td>
<td>TCP（HTTP 握手后换帧协议）</td>
</tr>
<tr>
<td>消息边界</td>
<td><code>Content-Length</code> &#x2F; chunked</td>
<td>事件间空行</td>
<td>帧头 Payload len</td>
</tr>
<tr>
<td>数据格式</td>
<td>任意</td>
<td>纯文本</td>
<td>文本 + 二进制</td>
</tr>
<tr>
<td>断线重连</td>
<td>—</td>
<td>内建（Last-Event-ID 续传）</td>
<td>自己写</td>
</tr>
<tr>
<td>典型场景</td>
<td>常规 API、静态资源</td>
<td>通知、行情、构建进度、AI 流式输出</td>
<td>聊天、协同编辑、联机游戏</td>
</tr>
</tbody></table>
<p><strong>普通的用 HTTP；只需要服务端推、客户端无需实时回话的用 SSE；双端都要高频互发的才上 WebSocket。</strong> 能用 SSE 就别上 WebSocket——它就是个不结束的 HTTP 响应，代理、CDN、鉴权全部白嫖 HTTP 生态。</p>
<h2 id="六、总结"><a href="#六、总结" class="headerlink" title="六、总结"></a>六、总结</h2><p>一条线索贯穿四个协议——<strong>在 TCP 无边界的字节流上，各自如何定义消息</strong>：</p>
<ul>
<li>TCP 用 seq&#x2F;ack + 重传交付有序字节，握手交换初始序号，挥手按方向各自关闭（FIN 只关写端，所以是四次）；</li>
<li>HTTP 用 <code>\r\n\r\n</code> 切头部、<code>Content-Length</code> &#x2F; chunked 切正文，换得一问一答的确定性；</li>
<li>SSE 借 chunked 让响应不结束，用空行切事件，把「响应」变成「事件流」；</li>
<li>WebSocket 用 Upgrade 握手接管连接，用帧头长度字段切消息，把「对话」变成「双向管道」。</li>
</ul>
<p>方向性上是三个台阶：一问一答 → 单向推送 → 全双工。理解了「边界」和「方向」这两个维度，四个协议就不再是名词，而是同一根管道上的四种用法。</p>
]]></content>
      <categories>
        <category>前端基础</category>
        <category>网络</category>
      </categories>
      <tags>
        <tag>SSE</tag>
        <tag>TCP</tag>
        <tag>HTTP</tag>
        <tag>WebSocket</tag>
      </tags>
  </entry>
  <entry>
    <title>Pinia 与 Vuex 状态管理总结：从单向数据流到组合式 Store</title>
    <url>/2026/09/10/pinia-vs-vuex/</url>
    <content><![CDATA[<p>这两个库解决的问题只有一个：<strong>多个组件共享同一份状态</strong>。但它们给出的答案，恰好对应了 Vue2 和 Vue3 两代技术栈的世界观——Vuex 是「中心化的单一仓库 + 单向数据流」，Pinia 是「一组全局的组合式函数」。</p>
<blockquote>
<p><strong>先说结论</strong>：Vuex 强制把「改状态」拆成 mutations（同步）和 actions（异步），是为了在 Options API 时代让 DevTools 能可靠追踪每一次变更；Vue3 的响应式系统本身就能观测到任何途径的修改，这层强制拆分失去了意义，于是 Pinia 把它删掉了，同时用「天然按 id 拆分的扁平 store」取代了 Vuex 的嵌套 modules。新项目（Vue3）直接用 Pinia，没有第二个选项；Vue2 老项目继续 Vuex，不必强行迁移。</p>
</blockquote>
<span id="more"></span>

<h2 id="一、为什么需要状态管理"><a href="#一、为什么需要状态管理" class="headerlink" title="一、为什么需要状态管理"></a>一、为什么需要状态管理</h2><p>组件化把页面拆成了树，但状态经常不属于任何一棵子树——登录态、用户信息、购物车、主题配置，都是「很多组件都要读写」的东西。不借助任何方案时，只有两条路：</p>
<p><strong>1. props 逐层透传（props drilling）</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs vue">&lt;!-- App.vue 知道 user，但真正用它的是 5 层之下的 Avatar.vue --&gt;<br>&lt;App :user=&quot;user&quot; /&gt;<br>  └─ &lt;Layout :user=&quot;user&quot; /&gt;<br>       └─ &lt;Sidebar :user=&quot;user&quot; /&gt;<br>            └─ &lt;Panel :user=&quot;user&quot; /&gt;<br>                 └─ &lt;Avatar :user=&quot;user&quot; /&gt;  &lt;!-- 中间三层只是搬运工 --&gt;<br></code></pre></td></tr></table></figure>

<p>中间组件被迫声明与自己无关的 props，数据流变成了体力劳动；而且兄弟组件之间这条路根本不通，得先把事件冒泡到公共祖先再传下来。</p>
<p><strong>2. 全局事件或全局变量</strong></p>
<p>挂 <code>window</code>、用 EventBus 面包屑式通知，状态散落在各处，没有统一的「单一数据源」（Single Source of Truth），排查问题时不知道值是谁改的。</p>
<p>状态管理库做的事因此很朴素：<strong>把共享状态提到组件树外的一个全局仓库里，任何组件都能直接读写，同时保留响应式和可追踪性</strong>。</p>
<p>但也要警惕反面：不是所有状态都该进全局仓库。表单草稿、弹窗开关、输入框焦点这类<strong>真正的局部状态</strong>，用 <code>ref</code>&#x2F;<code>reactive</code> 留在组件内就好；只是嫌透传麻烦的话，<code>provide/inject</code> 或一个模块级 <code>ref</code>（见 7.5）可能比引一个库更合适。<strong>先有共享需求，再上状态管理。</strong></p>
<h2 id="二、Vuex：中心化仓库与单向数据流"><a href="#二、Vuex：中心化仓库与单向数据流" class="headerlink" title="二、Vuex：中心化仓库与单向数据流"></a>二、Vuex：中心化仓库与单向数据流</h2><h3 id="2-1-心智模型"><a href="#2-1-心智模型" class="headerlink" title="2.1 心智模型"></a>2.1 心智模型</h3><p>Vuex 的核心是一张经典图：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">components ──dispatch──&gt; actions（可异步）──commit──&gt; mutations（必须同步）──&gt; state<br>    │                                                        │<br>    └──────────────── commit（直接同步改）───────────────────┘<br>                                 │<br>                          getters ──派生──&gt; components<br></code></pre></td></tr></table></figure>

<p>规则很明确：</p>
<ul>
<li><strong>state</strong>：唯一数据源，挂在 <code>store.state</code> 上；</li>
<li><strong>getters</strong>：state 的计算属性（缓存）；</li>
<li><strong>mutations</strong>：<strong>唯一合法的修改入口</strong>，必须是同步函数，第一个参数是 state；</li>
<li><strong>actions</strong>：提交 mutation，可以异步（发请求、定时器），第一个参数是上下文对象；</li>
<li>组件里 <code>commit(&#39;xxx&#39;)</code> 改状态，<code>dispatch(&#39;xxx&#39;)</code> 触发异步流程。</li>
</ul>
<h3 id="2-2-完整示例"><a href="#2-2-完整示例" class="headerlink" title="2.2 完整示例"></a>2.2 完整示例</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// store/index.js</span><br><span class="hljs-keyword">import</span> &#123; createStore &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;vuex&#x27;</span><br><br><span class="hljs-keyword">const</span> userModule = &#123;<br>  <span class="hljs-attr">namespaced</span>: <span class="hljs-literal">true</span>, <span class="hljs-comment">// 关键：开启命名空间，否则所有模块的 mutations/getters 都注册在全局</span><br>  <span class="hljs-attr">state</span>: <span class="hljs-function">() =&gt;</span> (&#123;<br>    <span class="hljs-attr">token</span>: <span class="hljs-string">&#x27;&#x27;</span>,<br>    <span class="hljs-attr">userInfo</span>: <span class="hljs-literal">null</span>,<br>  &#125;),<br>  <span class="hljs-attr">getters</span>: &#123;<br>    <span class="hljs-attr">isLoggedIn</span>: <span class="hljs-function">(<span class="hljs-params">state</span>) =&gt;</span> <span class="hljs-title class_">Boolean</span>(state.<span class="hljs-property">token</span>),<br>  &#125;,<br>  <span class="hljs-attr">mutations</span>: &#123;<br>    <span class="hljs-title function_">SET_TOKEN</span>(<span class="hljs-params">state, token</span>) &#123;<br>      state.<span class="hljs-property">token</span> = token<br>    &#125;,<br>    <span class="hljs-title function_">SET_USER_INFO</span>(<span class="hljs-params">state, info</span>) &#123;<br>      state.<span class="hljs-property">userInfo</span> = info<br>    &#125;,<br>  &#125;,<br>  <span class="hljs-attr">actions</span>: &#123;<br>    <span class="hljs-keyword">async</span> <span class="hljs-title function_">login</span>(<span class="hljs-params">&#123; commit &#125;, &#123; username, password &#125;</span>) &#123;<br>      <span class="hljs-keyword">const</span> &#123; token, userInfo &#125; = <span class="hljs-keyword">await</span> api.<span class="hljs-title function_">login</span>(username, password)<br>      <span class="hljs-title function_">commit</span>(<span class="hljs-string">&#x27;SET_TOKEN&#x27;</span>, token)<br>      <span class="hljs-title function_">commit</span>(<span class="hljs-string">&#x27;SET_USER_INFO&#x27;</span>, userInfo)<br>    &#125;,<br>  &#125;,<br>&#125;<br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">default</span> <span class="hljs-title function_">createStore</span>(&#123;<br>  <span class="hljs-attr">modules</span>: &#123; <span class="hljs-attr">user</span>: userModule &#125;,<br>&#125;)<br></code></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs vue">&lt;script&gt;<br>import &#123; mapState, mapGetters &#125; from &#x27;vuex&#x27;<br><br>export default &#123;<br>  computed: &#123;<br>    // 借助命名空间字符串 + 辅助函数<br>    ...mapState(&#x27;user&#x27;, [&#x27;userInfo&#x27;]),<br>    ...mapGetters(&#x27;user&#x27;, [&#x27;isLoggedIn&#x27;]),<br>  &#125;,<br>  methods: &#123;<br>    // 没有辅助函数时，是字符串路径<br>    handleLogin() &#123;<br>      this.$store.dispatch(&#x27;user/login&#x27;, &#123; username: &#x27;admin&#x27;, password: &#x27;xxxxxxx&#x27; &#125;)<br>    &#125;,<br>  &#125;,<br>&#125;<br>&lt;/script&gt;<br></code></pre></td></tr></table></figure>

<h3 id="2-3-为什么强制-mutations-必须同步"><a href="#2-3-为什么强制-mutations-必须同步" class="headerlink" title="2.3 为什么强制 mutations 必须同步"></a>2.3 为什么强制 mutations 必须同步</h3><p>这是 Vuex 最常被抱怨的设计，但它不是洁癖，是<strong>工程上的必要</strong>：</p>
<p>DevTools 的时间旅行（time travel）依赖「每个 mutation 执行完，拍一次状态快照」。如果 mutation 里有 <code>setTimeout</code>，快照拍下的会是<strong>中间状态</strong>，回放时状态对不上，整个调试模型就垮了。所以 Vuex 干脆用约定（严格模式下直接报错）堵死异步 mutation：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// 开发环境开启严格模式后，绕过 mutation 直接改 state 会抛错</span><br>store.<span class="hljs-property">state</span>.<span class="hljs-property">user</span>.<span class="hljs-property">token</span> = <span class="hljs-string">&#x27;xxx&#x27;</span> <span class="hljs-comment">// ❌ Error: [vuex] do not mutate vuex store state outside mutation handlers</span><br></code></pre></td></tr></table></figure>

<p>理解了这一点就理解了 Vuex 的一半：<strong>mutations&#x2F;actions 的分裂，是给 DevTools 交的「税」</strong>。</p>
<h3 id="2-4-Vuex-的五个痛点"><a href="#2-4-Vuex-的五个痛点" class="headerlink" title="2.4 Vuex 的五个痛点"></a>2.4 Vuex 的五个痛点</h3><ol>
<li><strong>mutations &#x2F; actions 样板代码</strong>。一个登录要写两个 mutation + 一个 action；同步逻辑被迫三处跳转。明明只是 <code>state.token = token</code> 一行的事。</li>
<li><strong>modules 嵌套 + 字符串路径</strong>。<code>dispatch(&#39;user/login&#39;)</code>、<code>mapState(&#39;user&#39;, [...])</code>、子模块里取兄弟模块要 <code>rootState</code> &#x2F; <code>rootGetters</code>——类型和重构都不友好，改名只能全局搜字符串。</li>
<li><strong>TypeScript 支持弱</strong>。Vue2 的 <code>this.$store</code> 没有类型上下文，需要手写 <code>declare module &#39;vuex&#39;</code> 类型增强和 InjectionKey；<code>mapState</code> 一族辅助函数基本放弃推导，getters 的返回类型要逐个手标。</li>
<li><strong>单入口大仓库</strong>。所有模块集中注册进一个 store，代码分割、按需加载都别扭；<code>store.state.user.xxx</code> 的深层访问把模块结构焊死。</li>
<li><strong>action 上下文对象心智负担</strong>。<code>{ state, commit, dispatch, rootState, rootGetters }</code> 一堆参数，嵌套模块里 <code>dispatch(&#39;other/action&#39;, null, { root: true })</code> 这类调用极易写错。</li>
</ol>
<p>Vuex4（Vue3 版）只是把内核换成 <code>reactive()</code>，这些 API 层面的债原样保留——官方也明确说了 Vuex 进入维护模式，Vue3 的答案是 Pinia。</p>
<h2 id="三、Pinia：把-store-写成组合式函数"><a href="#三、Pinia：把-store-写成组合式函数" class="headerlink" title="三、Pinia：把 store 写成组合式函数"></a>三、Pinia：把 store 写成组合式函数</h2><h3 id="3-1-设计哲学"><a href="#3-1-设计哲学" class="headerlink" title="3.1 设计哲学"></a>3.1 设计哲学</h3><p>Pinia（「菠萝」）不再维护一棵模块树，而是：<strong>每个 store 就是一个带响应式的全局对象，靠 <code>defineStore(id, ...)</code> 的 id 天然拆分</strong>。它建立在 Vue3 响应式 API 之上，没有引入自己的响应式系统——这是它能把 API 砍薄的根本原因。</p>
<h3 id="3-2-两种写法"><a href="#3-2-两种写法" class="headerlink" title="3.2 两种写法"></a>3.2 两种写法</h3><p><strong>Option Store</strong>（长得像 Vuex 模块，迁移友好）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// stores/user.js</span><br><span class="hljs-keyword">import</span> &#123; defineStore &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;pinia&#x27;</span><br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> useUserStore = <span class="hljs-title function_">defineStore</span>(<span class="hljs-string">&#x27;user&#x27;</span>, &#123;<br>  <span class="hljs-attr">state</span>: <span class="hljs-function">() =&gt;</span> (&#123;<br>    <span class="hljs-attr">token</span>: <span class="hljs-string">&#x27;&#x27;</span>,<br>    <span class="hljs-attr">userInfo</span>: <span class="hljs-literal">null</span>,<br>  &#125;),<br>  <span class="hljs-attr">getters</span>: &#123;<br>    <span class="hljs-attr">isLoggedIn</span>: <span class="hljs-function">(<span class="hljs-params">state</span>) =&gt;</span> <span class="hljs-title class_">Boolean</span>(state.<span class="hljs-property">token</span>),<br>    <span class="hljs-comment">// getter 间互访用 this（需要显式标返回类型）</span><br>    <span class="hljs-title function_">displayName</span>(<span class="hljs-params">state</span>) &#123;<br>      <span class="hljs-keyword">return</span> <span class="hljs-variable language_">this</span>.<span class="hljs-property">userInfo</span>?.<span class="hljs-property">name</span> ?? <span class="hljs-string">&#x27;游客&#x27;</span><br>    &#125;,<br>  &#125;,<br>  <span class="hljs-attr">actions</span>: &#123;<br>    <span class="hljs-comment">// ✅ 同步、异步都在 action 里，改 state 直接改</span><br>    <span class="hljs-keyword">async</span> <span class="hljs-title function_">login</span>(<span class="hljs-params">username, password</span>) &#123;<br>      <span class="hljs-keyword">const</span> &#123; token, userInfo &#125; = <span class="hljs-keyword">await</span> api.<span class="hljs-title function_">login</span>(username, password)<br>      <span class="hljs-variable language_">this</span>.<span class="hljs-property">token</span> = token<br>      <span class="hljs-variable language_">this</span>.<span class="hljs-property">userInfo</span> = userInfo<br>    &#125;,<br>    <span class="hljs-title function_">logout</span>(<span class="hljs-params"></span>) &#123;<br>      <span class="hljs-variable language_">this</span>.$reset() <span class="hljs-comment">// 恢复到 state() 的初始值</span><br>    &#125;,<br>  &#125;,<br>&#125;)<br></code></pre></td></tr></table></figure>

<p><strong>Setup Store</strong>（组合式 API 写法，推荐新代码使用）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// stores/user.js</span><br><span class="hljs-keyword">import</span> &#123; defineStore &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;pinia&#x27;</span><br><span class="hljs-keyword">import</span> &#123; ref, computed &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;vue&#x27;</span><br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> useUserStore = <span class="hljs-title function_">defineStore</span>(<span class="hljs-string">&#x27;user&#x27;</span>, <span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-comment">// ref()   → state</span><br>  <span class="hljs-keyword">const</span> token = <span class="hljs-title function_">ref</span>(<span class="hljs-string">&#x27;&#x27;</span>)<br>  <span class="hljs-keyword">const</span> userInfo = <span class="hljs-title function_">ref</span>(<span class="hljs-literal">null</span>)<br><br>  <span class="hljs-comment">// computed() → getters</span><br>  <span class="hljs-keyword">const</span> isLoggedIn = <span class="hljs-title function_">computed</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-title class_">Boolean</span>(token.<span class="hljs-property">value</span>))<br><br>  <span class="hljs-comment">// function  → actions</span><br>  <span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">login</span>(<span class="hljs-params">username, password</span>) &#123;<br>    <span class="hljs-keyword">const</span> res = <span class="hljs-keyword">await</span> api.<span class="hljs-title function_">login</span>(username, password)<br>    token.<span class="hljs-property">value</span> = res.<span class="hljs-property">token</span><br>    userInfo.<span class="hljs-property">value</span> = res.<span class="hljs-property">userInfo</span><br>  &#125;<br><br>  <span class="hljs-keyword">return</span> &#123; token, userInfo, isLoggedIn, login &#125;<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>Setup 写法的额外收益：store 内可以自由使用 <code>watch</code>、<code>provide/inject</code>、路由实例甚至其他组合式函数，且能按需只暴露部分能力（比如不 return 某个 ref，它就成了 store 的「私有状态」）。一个约定：<strong>state 用 <code>ref</code>，getter 用 <code>computed</code>，action 用普通函数</strong>。</p>
<h3 id="3-3-组件里使用"><a href="#3-3-组件里使用" class="headerlink" title="3.3 组件里使用"></a>3.3 组件里使用</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs vue">&lt;script setup&gt;<br>import &#123; storeToRefs &#125; from &#x27;pinia&#x27;<br>import &#123; useUserStore &#125; from &#x27;@/stores/user&#x27;<br><br>const userStore = useUserStore()<br><br>// ⚠️ 直接解构会丢响应性（见 5.1）<br>const &#123; token, userInfo &#125; = storeToRefs(userStore)<br>// action 是普通函数，可以直接解构<br>const &#123; login &#125; = userStore<br><br>// 直接读写<br>userStore.token = &#x27;new-token&#x27;<br>await login(&#x27;a&#x27;, &#x27;b&#x27;)<br>&lt;/script&gt;<br></code></pre></td></tr></table></figure>

<p>注意连 <code>$store</code> 都没有了——store 本身就是那个对象，<code>userStore.token</code> 直接访问。</p>
<h3 id="3-4-为什么删掉-mutations-还能时间旅行"><a href="#3-4-为什么删掉-mutations-还能时间旅行" class="headerlink" title="3.4 为什么删掉 mutations 还能时间旅行"></a>3.4 为什么删掉 mutations 还能时间旅行</h3><p>这是 Pinia 最核心的一个「为什么」。</p>
<p>Vuex 时代，追踪变更是靠「你承诺只通过 mutation 改」，DevTools 钩在 mutation 的执行边界上拍快照。Pinia 换了思路：<strong>state 本身就是 <code>reactive</code> 对象，任何途径的修改（action 里改、组件里改、<code>$patch</code> 批量改）都会触发响应式依赖</strong>。Pinia 通过 <code>$subscribe</code> 订阅这层响应式变化，把每次变更连同时间戳记录为事件序列交给 DevTools——不需要你先承诺「从哪个门进」，<strong>门本身消失了，但每个访客仍然被登记</strong>。</p>
<p>所以严格模式也没有存在的必要了：直接 <code>store.token = &#39;x&#39;</code> 不会破坏任何追踪能力，DevTools 照样能看到这次变更。代价只是「变更入口分散、不利于约束团队规范」这类软性问题，由团队规范而不是框架来管。</p>
<h3 id="3-5-实例-API：-patch-reset-subscribe-onAction"><a href="#3-5-实例-API：-patch-reset-subscribe-onAction" class="headerlink" title="3.5 实例 API：$patch &#x2F; $reset &#x2F; $subscribe &#x2F; $onAction"></a>3.5 实例 API：$patch &#x2F; $reset &#x2F; $subscribe &#x2F; $onAction</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> userStore = <span class="hljs-title function_">useUserStore</span>()<br><br><span class="hljs-comment">// $patch：批量修改，触发一次订阅通知（对象式与函数式）</span><br>userStore.$patch(&#123; <span class="hljs-attr">token</span>: <span class="hljs-string">&#x27;a&#x27;</span>, <span class="hljs-attr">userInfo</span>: &#123; <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;k&#x27;</span> &#125; &#125;)<br>userStore.$patch(<span class="hljs-function">(<span class="hljs-params">state</span>) =&gt;</span> &#123;<br>  state.<span class="hljs-property">userInfo</span>.<span class="hljs-property">name</span> = <span class="hljs-string">&#x27;k&#x27;</span><br>  state.<span class="hljs-property">token</span> = <span class="hljs-string">&#x27;a&#x27;</span><br>&#125;)<br><br><span class="hljs-comment">// $reset：重置为初始 state（⚠️ 仅 Option Store 自带；</span><br><span class="hljs-comment">// Setup Store 没有 state() 工厂，需自己实现一个 reset action）</span><br><br><span class="hljs-comment">// $subscribe：监听 state 变化（持久化的基础）</span><br>userStore.$subscribe(<span class="hljs-function">(<span class="hljs-params">mutation, state</span>) =&gt;</span> &#123;<br>  <span class="hljs-comment">// mutation.type: &#x27;direct&#x27; | &#x27;patch object&#x27; | &#x27;patch function&#x27;</span><br>  <span class="hljs-comment">// mutation.storeId: &#x27;user&#x27;</span><br>  <span class="hljs-variable language_">localStorage</span>.<span class="hljs-title function_">setItem</span>(<span class="hljs-string">&#x27;user&#x27;</span>, <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">stringify</span>(state))<br>&#125;, &#123; <span class="hljs-attr">detached</span>: <span class="hljs-literal">true</span> &#125;) <span class="hljs-comment">// detached: 组件卸载后仍继续订阅</span><br><br><span class="hljs-comment">// $onAction：拦截 action 的执行（日志、埋点、错误上报）</span><br><span class="hljs-keyword">const</span> unsubscribe = userStore.$onAction(&#123;<br>  <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;login&#x27;</span>,<br>  <span class="hljs-attr">after</span>: <span class="hljs-function">(<span class="hljs-params">result</span>) =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">`<span class="hljs-subst">$&#123;name&#125;</span> 成功`</span>, result),<br>  <span class="hljs-attr">onError</span>: <span class="hljs-function">(<span class="hljs-params">err</span>) =&gt;</span> <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">error</span>(<span class="hljs-string">`<span class="hljs-subst">$&#123;name&#125;</span> 失败`</span>, err),<br>&#125;)<br></code></pre></td></tr></table></figure>

<h3 id="3-6-原理速览"><a href="#3-6-原理速览" class="headerlink" title="3.6 原理速览"></a>3.6 原理速览</h3><p>Setup Store 的实现简单到可以口述：<code>defineStore(id, setup)</code> 内部用 <code>effectScope()</code> 开一个独立作用域，在里面执行你的 setup 函数——<code>ref</code> 就是 state，<code>computed</code> 就是 getters，返回的对象整体包一层 <code>reactive</code>（所以 <code>userStore.token</code> 会自动解包 ref）。<code>effectScope</code> 保证这些副作用可以被统一追踪和销毁，与任何组件实例<strong>无耦合</strong>——这就是 store 能活在组件外、且支持 HMR（热更新时销毁旧 scope 重建）的原因。相比之下，Vuex3 是把 state 塞进一个隐藏的 <code>new Vue({ data })</code> 实例、Vuex4 换成 <code>reactive()</code>，历史包袱一目了然。</p>
<h2 id="四、逐项对比"><a href="#四、逐项对比" class="headerlink" title="四、逐项对比"></a>四、逐项对比</h2><table>
<thead>
<tr>
<th>维度</th>
<th>Vuex 4</th>
<th>Pinia</th>
</tr>
</thead>
<tbody><tr>
<td>心智模型</td>
<td>单一大仓库 + 嵌套 modules</td>
<td>扁平的多 store，按 id 拆分</td>
</tr>
<tr>
<td>修改入口</td>
<td>必须经 mutations（同步）&#x2F; actions（异步）</td>
<td>action 或任意处直接改，<code>$patch</code> 批量</td>
</tr>
<tr>
<td>TypeScript</td>
<td>弱，需手写类型增强</td>
<td>全链路自动推导</td>
</tr>
<tr>
<td>组合式 API</td>
<td>先天不适配（Vuex4 仅兼容运行）</td>
<td>原生即为组合式设计</td>
</tr>
<tr>
<td>模块间互访</td>
<td><code>rootState</code> &#x2F; <code>rootGetters</code> &#x2F; <code>{ root: true }</code></td>
<td>直接 <code>useOtherStore()</code>，或 getter&#x2F;action 里组合</td>
</tr>
<tr>
<td>代码分割</td>
<td>单入口，别扭</td>
<td>每个 store 独立文件，天然友好</td>
</tr>
<tr>
<td>体积</td>
<td>约数 KB（gzip）</td>
<td>约 1KB（gzip）</td>
</tr>
<tr>
<td>DevTools</td>
<td>时间旅行、时间线</td>
<td>同样支持，且 store 树更直观</td>
</tr>
<tr>
<td>插件</td>
<td>store 插件</td>
<td>context 更丰富（含 <code>$subscribe</code>&#x2F;<code>$onAction</code> 钩子）</td>
</tr>
<tr>
<td>严格模式</td>
<td>有（dev 下抓非法修改）</td>
<td>无，也不需要（见 3.4）</td>
</tr>
<tr>
<td>Vue 版本</td>
<td>Vue2（Vuex3）&#x2F; Vue3（Vuex4）</td>
<td>Vue3；Vue 2.7 也可用</td>
</tr>
</tbody></table>
<p>几个值得展开的点：</p>
<p><strong>TypeScript 是体验差距最大的地方。</strong> Pinia 里从 <code>state</code> 到 <code>getters</code> 到组件里的 <code>storeToRefs</code>，类型全程自动流动，改一个接口字段，所有消费处编译器立刻标红。Vuex 里类型在 <code>commit(&#39;user/SET_TOKEN&#39;, ...)</code> 这种字符串处断裂，只能靠自律。</p>
<p><strong>store 之间可以像普通模块一样组合</strong>，这是 Vuex 做不到的：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// stores/cart.js 里使用 user store</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> useCartStore = <span class="hljs-title function_">defineStore</span>(<span class="hljs-string">&#x27;cart&#x27;</span>, <span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-keyword">const</span> items = <span class="hljs-title function_">ref</span>([])<br><br>  <span class="hljs-keyword">const</span> userStore = <span class="hljs-title function_">useUserStore</span>() <span class="hljs-comment">// 在 getter/action 执行时调用即可</span><br><br>  <span class="hljs-keyword">const</span> totalPrice = <span class="hljs-title function_">computed</span>(<span class="hljs-function">() =&gt;</span><br>    userStore.<span class="hljs-property">userInfo</span>?.<span class="hljs-property">vip</span><br>      ? items.<span class="hljs-property">value</span>.<span class="hljs-title function_">reduce</span>(<span class="hljs-function">(<span class="hljs-params">s, i</span>) =&gt;</span> s + i.<span class="hljs-property">price</span> * <span class="hljs-number">0.9</span>, <span class="hljs-number">0</span>)<br>      : items.<span class="hljs-property">value</span>.<span class="hljs-title function_">reduce</span>(<span class="hljs-function">(<span class="hljs-params">s, i</span>) =&gt;</span> s + i.<span class="hljs-property">price</span>, <span class="hljs-number">0</span>),<br>  )<br><br>  <span class="hljs-keyword">return</span> &#123; items, totalPrice &#125;<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>注意：在 setup store 的<strong>顶层</strong>调用其他 store 是允许的（只要那时 pinia 已激活），跨 store 循环依赖时则要放到 getter&#x2F;action 内部延迟调用。</p>
<h2 id="五、实战技巧与易错点"><a href="#五、实战技巧与易错点" class="headerlink" title="五、实战技巧与易错点"></a>五、实战技巧与易错点</h2><h3 id="5-1-解构丢响应性：必须用-storeToRefs"><a href="#5-1-解构丢响应性：必须用-storeToRefs" class="headerlink" title="5.1 解构丢响应性：必须用 storeToRefs"></a>5.1 解构丢响应性：必须用 storeToRefs</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> &#123; token &#125; = <span class="hljs-title function_">useUserStore</span>() <span class="hljs-comment">// ❌ token 变成一次性的纯字符串</span><br></code></pre></td></tr></table></figure>

<p>原因和 <code>props</code> 解构一样：<code>store.token</code> 是对 reactive 对象属性的<strong>读取</strong>，拿到的是值的快照，丢失了与源对象的联系。<code>storeToRefs</code> 的实现本质就是对 state 和 getters 逐个 <code>toRef</code>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> &#123; token &#125; = <span class="hljs-title function_">storeToRefs</span>(userStore) <span class="hljs-comment">// ✅ Ref&lt;string&gt;，保持响应</span><br><span class="hljs-keyword">const</span> &#123; login &#125; = userStore              <span class="hljs-comment">// ✅ action 是普通函数，随便解构</span><br></code></pre></td></tr></table></figure>

<h3 id="5-2-在组件外使用-store"><a href="#5-2-在组件外使用-store" class="headerlink" title="5.2 在组件外使用 store"></a>5.2 在组件外使用 store</h3><p>store 依赖已安装的 pinia 实例，而模块顶层的执行时机早于 <code>app.use(pinia)</code>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// ❌ 模块加载时立即调用，此时 pinia 还没激活</span><br><span class="hljs-keyword">const</span> userStore = <span class="hljs-title function_">useUserStore</span>() <span class="hljs-comment">// getActivePinia was called with no active Pinia</span><br><br><span class="hljs-comment">// ✅ 方案一：延迟到运行时调用（路由守卫、axios 拦截器的回调本身就是延迟执行的）</span><br>router.<span class="hljs-title function_">beforeEach</span>(<span class="hljs-function">(<span class="hljs-params">to</span>) =&gt;</span> &#123;<br>  <span class="hljs-keyword">const</span> userStore = <span class="hljs-title function_">useUserStore</span>() <span class="hljs-comment">// 此时 app 已启动，OK</span><br>  <span class="hljs-keyword">if</span> (to.<span class="hljs-property">meta</span>.<span class="hljs-property">auth</span> &amp;&amp; !userStore.<span class="hljs-property">isLoggedIn</span>) <span class="hljs-keyword">return</span> <span class="hljs-string">&#x27;/login&#x27;</span><br>&#125;)<br><br><span class="hljs-comment">// ✅ 方案二：模块顶层就要用，显式传入 pinia 实例</span><br><span class="hljs-keyword">import</span> &#123; pinia &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;./pinia&#x27;</span> <span class="hljs-comment">// 单独导出的 pinia 实例</span><br><span class="hljs-keyword">const</span> userStore = <span class="hljs-title function_">useUserStore</span>(pinia)<br></code></pre></td></tr></table></figure>

<p>方案二是 SSR 和单元测试（每次 createTestingPinia）场景的标准做法。</p>
<h3 id="5-3-持久化：一个-20-行的插件"><a href="#5-3-持久化：一个-20-行的插件" class="headerlink" title="5.3 持久化：一个 20 行的插件"></a>5.3 持久化：一个 20 行的插件</h3><p>Pinia 插件是一个函数，作用于每个 store，返回值会被合并进 store（可用于添加 <code>$reset</code> 增强等）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// plugins/persist.js</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">persist</span>(<span class="hljs-params">&#123; store &#125;</span>) &#123;<br>  <span class="hljs-keyword">const</span> saved = <span class="hljs-variable language_">localStorage</span>.<span class="hljs-title function_">getItem</span>(store.<span class="hljs-property">$id</span>)<br>  <span class="hljs-keyword">if</span> (saved) store.$patch(<span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">parse</span>(saved))<br><br>  store.$subscribe(<span class="hljs-function">(<span class="hljs-params">_mutation, state</span>) =&gt;</span> &#123;<br>    <span class="hljs-variable language_">localStorage</span>.<span class="hljs-title function_">setItem</span>(store.<span class="hljs-property">$id</span>, <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">stringify</span>(state))<br>  &#125;)<br>&#125;<br><br><span class="hljs-comment">// main.js</span><br>pinia.<span class="hljs-title function_">use</span>(persist)<br></code></pre></td></tr></table></figure>

<p>生产环境一般直接用 <code>pinia-plugin-persistedstate</code>，支持按 store 配置 <code>persist: { paths: [&#39;token&#39;] }</code> 做字段级持久化——原理就是上面这段代码加配置化。</p>
<h3 id="5-4-Setup-Store-的私有状态与-reset"><a href="#5-4-Setup-Store-的私有状态与-reset" class="headerlink" title="5.4 Setup Store 的私有状态与 $reset"></a>5.4 Setup Store 的私有状态与 $reset</h3><p>setup 函数里不 return 的变量就是 store 的私有状态，外界（包括 DevTools）看不到；代价是 <code>$reset</code> 失效，需要自己写：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> useUserStore = <span class="hljs-title function_">defineStore</span>(<span class="hljs-string">&#x27;user&#x27;</span>, <span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-keyword">const</span> token = <span class="hljs-title function_">ref</span>(<span class="hljs-string">&#x27;&#x27;</span>)<br><br>  <span class="hljs-keyword">function</span> <span class="hljs-title function_">$reset</span>(<span class="hljs-params"></span>) &#123;<br>    token.<span class="hljs-property">value</span> = <span class="hljs-string">&#x27;&#x27;</span><br>  &#125;<br>  <span class="hljs-keyword">return</span> &#123; token, $reset &#125;<br>&#125;)<br></code></pre></td></tr></table></figure>

<h3 id="5-5-「轻量替代品」：模块级-ref"><a href="#5-5-「轻量替代品」：模块级-ref" class="headerlink" title="5.5 「轻量替代品」：模块级 ref"></a>5.5 「轻量替代品」：模块级 ref</h3><p>如果只是两三个组件共享一个小状态，可以不引库：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// composables/useTheme.js</span><br><span class="hljs-keyword">import</span> &#123; ref &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;vue&#x27;</span><br><span class="hljs-keyword">const</span> theme = <span class="hljs-title function_">ref</span>(<span class="hljs-string">&#x27;light&#x27;</span>) <span class="hljs-comment">// 模块作用域，天然全局单例</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">useTheme</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">const</span> <span class="hljs-title function_">toggle</span> = (<span class="hljs-params"></span>) =&gt; (theme.<span class="hljs-property">value</span> = theme.<span class="hljs-property">value</span> === <span class="hljs-string">&#x27;light&#x27;</span> ? <span class="hljs-string">&#x27;dark&#x27;</span> : <span class="hljs-string">&#x27;light&#x27;</span>)<br>  <span class="hljs-keyword">return</span> &#123; theme, toggle &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>它的本质就是一个「手写的 setup store」。区别在于没有 DevTools 集成、<code>$patch</code>、插件体系和 SSR 安全——状态规模一上去，这些就会从「nice to have」变成刚需，那时再换 Pinia，迁移成本几乎为零（因为写法同构）。</p>
<h2 id="六、Vuex-→-Pinia-迁移指南"><a href="#六、Vuex-→-Pinia-迁移指南" class="headerlink" title="六、Vuex → Pinia 迁移指南"></a>六、Vuex → Pinia 迁移指南</h2><p>两个库可以<strong>共存过渡</strong>（Vuex 照常 <code>app.use(store)</code>，Pinia 照常 <code>app.use(pinia)</code>），建议按模块逐个迁移、迁移完删除。</p>
<p>映射关系：</p>
<table>
<thead>
<tr>
<th>Vuex</th>
<th>Pinia (Option)</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>state: () =&gt; ({...})</code></td>
<td><code>state: () =&gt; ({...})</code></td>
<td>一致</td>
</tr>
<tr>
<td><code>getters</code></td>
<td><code>getters</code></td>
<td>去掉 namespaced 概念</td>
</tr>
<tr>
<td><code>mutations</code> + <code>actions</code></td>
<td><code>actions</code></td>
<td>合并；<code>SET_XXX</code> 大写命名可顺手改成语义化方法</td>
</tr>
<tr>
<td><code>modules: { user }</code></td>
<td><code>defineStore(&#39;user&#39;, ...)</code> 独立文件</td>
<td>嵌套模块拍平</td>
</tr>
<tr>
<td><code>this.$store.commit(&#39;user/login&#39;)</code></td>
<td><code>userStore.login()</code></td>
<td>字符串 → 方法调用</td>
</tr>
<tr>
<td><code>mapState(&#39;user&#39;, [...])</code></td>
<td><code>storeToRefs(useUserStore())</code></td>
<td></td>
</tr>
<tr>
<td><code>store.dispatch(&#39;user/login&#39;)</code></td>
<td><code>userStore.login()</code></td>
<td></td>
</tr>
</tbody></table>
<p>迁移前的 user 模块（Vuex）和迁移后（Pinia）对照：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// ── Vuex ──</span><br><span class="hljs-keyword">const</span> userModule = &#123;<br>  <span class="hljs-attr">namespaced</span>: <span class="hljs-literal">true</span>,<br>  <span class="hljs-attr">state</span>: <span class="hljs-function">() =&gt;</span> (&#123; <span class="hljs-attr">token</span>: <span class="hljs-string">&#x27;&#x27;</span> &#125;),<br>  <span class="hljs-attr">mutations</span>: &#123;<br>    <span class="hljs-title function_">SET_TOKEN</span>(<span class="hljs-params">state, token</span>) &#123; state.<span class="hljs-property">token</span> = token &#125;,<br>    <span class="hljs-title function_">CLEAR_TOKEN</span>(<span class="hljs-params">state</span>) &#123; state.<span class="hljs-property">token</span> = <span class="hljs-string">&#x27;&#x27;</span> &#125;,<br>  &#125;,<br>  <span class="hljs-attr">actions</span>: &#123;<br>    <span class="hljs-keyword">async</span> <span class="hljs-title function_">login</span>(<span class="hljs-params">&#123; commit &#125;, form</span>) &#123;<br>      <span class="hljs-keyword">const</span> &#123; token &#125; = <span class="hljs-keyword">await</span> api.<span class="hljs-title function_">login</span>(form)<br>      <span class="hljs-title function_">commit</span>(<span class="hljs-string">&#x27;SET_TOKEN&#x27;</span>, token)<br>    &#125;,<br>    <span class="hljs-title function_">logout</span>(<span class="hljs-params">&#123; commit &#125;</span>) &#123;<br>      <span class="hljs-title function_">commit</span>(<span class="hljs-string">&#x27;CLEAR_TOKEN&#x27;</span>)<br>    &#125;,<br>  &#125;,<br>&#125;<br><br><span class="hljs-comment">// ── Pinia ──</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> useUserStore = <span class="hljs-title function_">defineStore</span>(<span class="hljs-string">&#x27;user&#x27;</span>, &#123;<br>  <span class="hljs-attr">state</span>: <span class="hljs-function">() =&gt;</span> (&#123; <span class="hljs-attr">token</span>: <span class="hljs-string">&#x27;&#x27;</span> &#125;),<br>  <span class="hljs-attr">actions</span>: &#123;<br>    <span class="hljs-keyword">async</span> <span class="hljs-title function_">login</span>(<span class="hljs-params">form</span>) &#123;<br>      <span class="hljs-variable language_">this</span>.<span class="hljs-property">token</span> = (<span class="hljs-keyword">await</span> api.<span class="hljs-title function_">login</span>(form)).<span class="hljs-property">token</span><br>    &#125;,<br>    <span class="hljs-title function_">logout</span>(<span class="hljs-params"></span>) &#123;<br>      <span class="hljs-variable language_">this</span>.$reset()<br>    &#125;,<br>  &#125;,<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>代码量减半不是重点，重点是<strong>字符串派发全部变成了可跳转、可推导类型的方法调用</strong>。</p>
<h2 id="七、高频面试题速答"><a href="#七、高频面试题速答" class="headerlink" title="七、高频面试题速答"></a>七、高频面试题速答</h2><p><strong>1. 为什么 Pinia 去掉 mutations？</strong><br>因为 mutations 存在的唯一理由（同步边界保证 DevTools 快照可靠）在 Vue3 响应式系统下不再必要：state 是 <code>reactive</code> 对象，任何途径的修改都可被 <code>$subscribe</code> 观测并记录，DevTools 能力无损。</p>
<p><strong>2. Pinia 为什么不需要 strict 模式？</strong><br>同上。Vuex 的严格模式防的是「绕过 mutation 导致 DevTools 看不见」；Pinia 里不存在看不见的修改，直接改 state 只是风格问题，不是正确性问题。</p>
<p><strong>3. 直接解构 store 为什么丢响应性？</strong><br><code>const { token } = store</code> 是对 reactive 属性的一次性读取，拿到原始值。要用 <code>storeToRefs</code>（内部 <code>toRef</code> 保持引用联系）；action 是普通函数不受影响。</p>
<p><strong>4. Pinia 和 Vuex 会长期并存吗？</strong><br>不会。Pinia 官方定位就是「下一代 Vuex」，Vuex 已进入维护模式（只修 bug 不加特性），Vue 官方文档状态管理章节只推荐 Pinia。</p>
<p><strong>5. 没有嵌套 modules，大型项目几十个 store 不会乱吗？</strong><br>「扁平 + 组合」优于「树形 + 命名空间」：模块间依赖显式化为 <code>useOtherStore()</code> 调用，跨域复用靠组合而非 <code>rootState</code>；按业务域拆文件（<code>stores/user.js</code>、<code>stores/cart.js</code>）后，目录即架构。</p>
<p><strong>6. $patch 和直接赋值有什么区别？</strong><br><code>$patch</code> 把多次修改合并为<strong>一次</strong>订阅通知，并且以 <code>patch object</code> 类型记录进 DevTools；批量更新时用它可减少订阅回调（如持久化写 localStorage）的触发次数。</p>
<h2 id="八、总结"><a href="#八、总结" class="headerlink" title="八、总结"></a>八、总结</h2><table>
<thead>
<tr>
<th></th>
<th>一句话</th>
</tr>
</thead>
<tbody><tr>
<td>Vuex 的本质</td>
<td>用「约定 + 强制」换可追踪性：mutations 是给 DevTools 交的税</td>
</tr>
<tr>
<td>Pinia 的本质</td>
<td>响应式系统本身可观测，税不用交了；store 即组合式函数，模块化即文件</td>
</tr>
<tr>
<td>选型</td>
<td>Vue3 新项目 → Pinia（唯一答案）；Vue2 存量 → Vuex4 不必动；简单共享 → 模块级 ref &#x2F; provide-inject</td>
</tr>
</tbody></table>
<p>真正值得带走的不是 API 对照表，而是这条主线：<strong>状态管理库的一切设计，都是围绕「如何让共享状态的变化可观测、可追踪」展开的</strong>——Vuex 用纪律实现它，Pinia 用代理实现的响应式系统实现它。理解了这一点，下一代的方案再怎么变，你都能在十分钟内看懂它。</p>
]]></content>
      <categories>
        <category>Vue</category>
        <category>状态管理</category>
      </categories>
      <tags>
        <tag>Vue3</tag>
        <tag>Pinia</tag>
        <tag>Vue2</tag>
        <tag>Vuex</tag>
        <tag>状态管理</tag>
      </tags>
  </entry>
  <entry>
    <title>小程序性能优化实战清单：分包、骨架屏、长列表</title>
    <url>/2026/09/09/miniprogram-performance/</url>
    <content><![CDATA[<p>小程序的性能问题，拆开就是一条启动链路上的三个阶段：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">下载代码包 → 注入 → 首屏渲染 → 运行时交互<br>     ↑           ↑         ↑           ↑<br>   分包         分包      骨架屏      长列表 / setData<br></code></pre></td></tr></table></figure>

<p>三板斧各治一段：<strong>分包治下载、骨架屏治感知、长列表治运行时</strong>。本文以微信小程序为例，每条给结论和能直接抄的代码。动手前先跑一遍开发者工具的「体验评分」面板，让数据告诉你卡在哪一段。</p>
<hr>
<h2 id="一、分包：主包只留”进门必须的”"><a href="#一、分包：主包只留”进门必须的”" class="headerlink" title="一、分包：主包只留”进门必须的”"></a>一、分包：主包只留”进门必须的”</h2><h3 id="1-1-为什么要分包"><a href="#1-1-为什么要分包" class="headerlink" title="1.1 为什么要分包"></a>1.1 为什么要分包</h3><p>小程序<strong>先下载主包、再渲染首页</strong>，主包越大，用户看到的第一个像素越晚。硬性限制：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">单个分包 / 主包 ≤ 2M        整个小程序（所有分包合计）≤ 30M<br></code></pre></td></tr></table></figure>

<p>主包超 2M 直接没法上传，所以分包不是可选项，是必修课。</p>
<h3 id="1-2-基础配置：app-json"><a href="#1-2-基础配置：app-json" class="headerlink" title="1.2 基础配置：app.json"></a>1.2 基础配置：app.json</h3><figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-punctuation">&#123;</span><br>  <span class="hljs-attr">&quot;pages&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">[</span><br>    <span class="hljs-string">&quot;pages/index/index&quot;</span><span class="hljs-punctuation">,</span><br>    <span class="hljs-string">&quot;pages/login/login&quot;</span><br>  <span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-attr">&quot;subpackages&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">[</span><br>    <span class="hljs-punctuation">&#123;</span><br>      <span class="hljs-attr">&quot;root&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;packageOrder&quot;</span><span class="hljs-punctuation">,</span><br>      <span class="hljs-attr">&quot;pages&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;pages/list/index&quot;</span><span class="hljs-punctuation">,</span> <span class="hljs-string">&quot;pages/detail/index&quot;</span><span class="hljs-punctuation">]</span><br>    <span class="hljs-punctuation">&#125;</span><span class="hljs-punctuation">,</span><br>    <span class="hljs-punctuation">&#123;</span><br>      <span class="hljs-attr">&quot;root&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;packageActivity&quot;</span><span class="hljs-punctuation">,</span><br>      <span class="hljs-attr">&quot;pages&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;pages/seckill/index&quot;</span><span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span><br>      <span class="hljs-attr">&quot;independent&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-literal"><span class="hljs-keyword">true</span></span><br>    <span class="hljs-punctuation">&#125;</span><br>  <span class="hljs-punctuation">]</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-attr">&quot;preloadRule&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">&#123;</span><br>    <span class="hljs-attr">&quot;pages/index/index&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">&#123;</span><br>      <span class="hljs-attr">&quot;network&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;wifi&quot;</span><span class="hljs-punctuation">,</span><br>      <span class="hljs-attr">&quot;packages&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">[</span><span class="hljs-string">&quot;packageOrder&quot;</span><span class="hljs-punctuation">]</span><br>    <span class="hljs-punctuation">&#125;</span><br>  <span class="hljs-punctuation">&#125;</span><br><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure>

<p>三个配置三件事：</p>
<ul>
<li><strong>subpackages</strong>：订单、活动等非首屏页面全部搬出主包。跳转时才下载对应分包（有几百毫秒的加载态，所以要配合预下载）</li>
<li><strong>preloadRule</strong>：用户在首页闲着时，提前把”下一步大概率要去的分包”拉下来，跳转时零等待</li>
<li><strong>independent（独立分包）</strong>：不依赖主包、单独运行。用户从分享卡片直接进活动页时，<strong>完全不下载主包</strong>，秒开——适合活动、落地页</li>
</ul>
<h3 id="1-3-三条铁律"><a href="#1-3-三条铁律" class="headerlink" title="1.3 三条铁律"></a>1.3 三条铁律</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① tabBar 页面和首页必须在主包，塞不进分包<br>② 分包可以引用主包的组件和工具，反之不行<br>   → 公共代码放主包，业务代码放分包<br>③ 图片、字体等静态资源走 CDN，不要打进包里<br>   → 主包体积超标，九成是图片干的<br></code></pre></td></tr></table></figure>

<h3 id="1-4-分包异步化：跨包用组件不拖累主包"><a href="#1-4-分包异步化：跨包用组件不拖累主包" class="headerlink" title="1.4 分包异步化：跨包用组件不拖累主包"></a>1.4 分包异步化：跨包用组件不拖累主包</h3><p>偶尔需要”主包页面用分包里的组件&#x2F;函数”，用分包异步化，用到时才加载：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// 异步引用其他分包的 JS 模块</span><br><span class="hljs-keyword">const</span> utils = <span class="hljs-keyword">await</span> <span class="hljs-built_in">require</span>.<span class="hljs-title function_">async</span>(<span class="hljs-string">&#x27;../packageOrder/utils.js&#x27;</span>)<br></code></pre></td></tr></table></figure>

<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-comment">// 主包页面的 json：引用分包组件 + 占位组件兜底</span><br><span class="hljs-punctuation">&#123;</span><br>  <span class="hljs-attr">&quot;usingComponents&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">&#123;</span> <span class="hljs-attr">&quot;hello&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;../packageActivity/components/hello&quot;</span> <span class="hljs-punctuation">&#125;</span><span class="hljs-punctuation">,</span><br>  <span class="hljs-attr">&quot;componentPlaceholder&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">&#123;</span> <span class="hljs-attr">&quot;hello&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;view&quot;</span> <span class="hljs-punctuation">&#125;</span><br><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure>

<p>组件没加载完时先渲染占位符，加载完自动替换。</p>
<hr>
<h2 id="二、骨架屏：把”白屏等待”变成”正在加载”"><a href="#二、骨架屏：把”白屏等待”变成”正在加载”" class="headerlink" title="二、骨架屏：把”白屏等待”变成”正在加载”"></a>二、骨架屏：把”白屏等待”变成”正在加载”</h2><h3 id="2-1-原则"><a href="#2-1-原则" class="headerlink" title="2.1 原则"></a>2.1 原则</h3><p>感知性能比真实性能更值钱：<strong>白屏超过 300ms 就该有骨架屏</strong>。两条底线：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 骨架的结构、尺寸必须和真实内容一致——否则内容一回来页面跳动，比白屏更糟<br>② 骨架是纯静态的：不依赖任何接口数据，样式内联在包里，零网络请求<br></code></pre></td></tr></table></figure>

<p>偷懒方案：开发者工具模拟器面板「…」菜单里有<strong>一键生成骨架屏</strong>，会按当前页面结构生成 <code>skeleton.wxml/wxss</code>，适合快速上线；结构精细的页面建议手写，可控得多。</p>
<h3 id="2-2-手写骨架屏"><a href="#2-2-手写骨架屏" class="headerlink" title="2.2 手写骨架屏"></a>2.2 手写骨架屏</h3><figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-comment">&lt;!-- pages/order/list.wxml --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">block</span> <span class="hljs-attr">wx:if</span>=<span class="hljs-string">&quot;&#123;&#123;loading&#125;&#125;&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">view</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;sk-card&quot;</span> <span class="hljs-attr">wx:for</span>=<span class="hljs-string">&quot;&#123;&#123;[1, 2, 3]&#125;&#125;&quot;</span> <span class="hljs-attr">wx:key</span>=<span class="hljs-string">&quot;*this&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">view</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;sk sk-avatar&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">view</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">view</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;sk sk-line w-60&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">view</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">view</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;sk sk-line w-90&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">view</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">view</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">block</span>&gt;</span><br><br><span class="hljs-tag">&lt;<span class="hljs-name">block</span> <span class="hljs-attr">wx:else</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">view</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;card&quot;</span> <span class="hljs-attr">wx:for</span>=<span class="hljs-string">&quot;&#123;&#123;list&#125;&#125;&quot;</span> <span class="hljs-attr">wx:key</span>=<span class="hljs-string">&quot;id&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">image</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;avatar&quot;</span> <span class="hljs-attr">src</span>=<span class="hljs-string">&quot;&#123;&#123;item.avatar&#125;&#125;&quot;</span> /&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">view</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;title&quot;</span>&gt;</span>&#123;&#123;item.title&#125;&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">view</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">view</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;desc&quot;</span>&gt;</span>&#123;&#123;item.desc&#125;&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">view</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">view</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">block</span>&gt;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-comment">/* shimmer 动画：一道高光从左到右扫过去 */</span><br><span class="hljs-selector-class">.sk</span> &#123;<br>  <span class="hljs-attribute">background</span>: <span class="hljs-built_in">linear-gradient</span>(<span class="hljs-number">90deg</span>, <span class="hljs-number">#f2f2f2</span> <span class="hljs-number">25%</span>, <span class="hljs-number">#e8e8e8</span> <span class="hljs-number">37%</span>, <span class="hljs-number">#f2f2f2</span> <span class="hljs-number">63%</span>);<br>  <span class="hljs-attribute">background-size</span>: <span class="hljs-number">400%</span> <span class="hljs-number">100%</span>;<br>  <span class="hljs-attribute">animation</span>: sk-shimmer <span class="hljs-number">1.4s</span> ease infinite;<br>&#125;<br><span class="hljs-keyword">@keyframes</span> sk-shimmer &#123;<br>  <span class="hljs-number">0%</span>   &#123; <span class="hljs-attribute">background-position</span>: <span class="hljs-number">100%</span> <span class="hljs-number">0</span>; &#125;<br>  <span class="hljs-number">100%</span> &#123; <span class="hljs-attribute">background-position</span>: <span class="hljs-number">0</span> <span class="hljs-number">0</span>; &#125;<br>&#125;<br><br><span class="hljs-selector-class">.sk-card</span>  &#123; <span class="hljs-attribute">display</span>: flex; <span class="hljs-attribute">flex-wrap</span>: wrap; <span class="hljs-attribute">padding</span>: <span class="hljs-number">24</span>rpx; &#125;<br><span class="hljs-selector-class">.sk-avatar</span> &#123; <span class="hljs-attribute">width</span>: <span class="hljs-number">88</span>rpx; <span class="hljs-attribute">height</span>: <span class="hljs-number">88</span>rpx; <span class="hljs-attribute">border-radius</span>: <span class="hljs-number">50%</span>; &#125;<br><span class="hljs-selector-class">.sk-line</span>  &#123; <span class="hljs-attribute">height</span>: <span class="hljs-number">32</span>rpx; <span class="hljs-attribute">margin</span>: <span class="hljs-number">8</span>rpx <span class="hljs-number">0</span>; <span class="hljs-attribute">border-radius</span>: <span class="hljs-number">8</span>rpx; &#125;<br><span class="hljs-selector-class">.w-60</span> &#123; <span class="hljs-attribute">width</span>: <span class="hljs-number">60%</span>; &#125;<br><span class="hljs-selector-class">.w-90</span> &#123; <span class="hljs-attribute">width</span>: <span class="hljs-number">90%</span>; &#125;<br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// pages/order/list.js</span><br><span class="hljs-title class_">Page</span>(&#123;<br>  <span class="hljs-attr">data</span>: &#123; <span class="hljs-attr">loading</span>: <span class="hljs-literal">true</span>, <span class="hljs-attr">list</span>: [] &#125;,<br><br>  <span class="hljs-keyword">async</span> <span class="hljs-title function_">onLoad</span>(<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>      <span class="hljs-keyword">const</span> list = <span class="hljs-keyword">await</span> <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">fetchList</span>()<br>      <span class="hljs-comment">// 一次性切换：骨架 → 内容，避免半渲染状态</span><br>      <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">setData</span>(&#123; list, <span class="hljs-attr">loading</span>: <span class="hljs-literal">false</span> &#125;)<br>    &#125; <span class="hljs-keyword">catch</span> (e) &#123;<br>      <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">setData</span>(&#123; <span class="hljs-attr">loading</span>: <span class="hljs-literal">false</span> &#125;)   <span class="hljs-comment">// 失败也要撤掉骨架，展示错误态</span><br>    &#125;<br>  &#125;<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>一个容易忽略的点：请求要放在 <code>onLoad</code> 里尽早发出（骨架屏亮着的同时数据在路上），而不是 <code>onReady</code> 之后再发，否则骨架只是把白屏变漂亮了，总耗时一点没少。</p>
<hr>
<h2 id="三、长列表：瓶颈不在渲染，在-setData"><a href="#三、长列表：瓶颈不在渲染，在-setData" class="headerlink" title="三、长列表：瓶颈不在渲染，在 setData"></a>三、长列表：瓶颈不在渲染，在 setData</h2><h3 id="3-1-先搞清楚卡在哪"><a href="#3-1-先搞清楚卡在哪" class="headerlink" title="3.1 先搞清楚卡在哪"></a>3.1 先搞清楚卡在哪</h3><p>小程序逻辑层（JS）和渲染层（WebView）是两个线程，<code>setData</code> 是两线程之间<strong>序列化 + 桥接传输</strong>：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">setData(&#123; list &#125;)  →  序列化整个 list  →  跨线程传输  →  渲染层 diff<br>                          ↑<br>              list 有 1000 条时，每调一次全量传一次，页面必卡<br></code></pre></td></tr></table></figure>

<p>所以优化方向就一句话：<strong>让每次 setData 传输的数据尽量小、调用频率尽量低</strong>。</p>
<h3 id="3-2-第一层：分页加载-防抖守卫（必做）"><a href="#3-2-第一层：分页加载-防抖守卫（必做）" class="headerlink" title="3.2 第一层：分页加载 + 防抖守卫（必做）"></a>3.2 第一层：分页加载 + 防抖守卫（必做）</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-title class_">Page</span>(&#123;<br>  <span class="hljs-attr">data</span>: &#123; <span class="hljs-attr">list</span>: [], <span class="hljs-attr">loading</span>: <span class="hljs-literal">false</span>, <span class="hljs-attr">finished</span>: <span class="hljs-literal">false</span> &#125;,<br>  <span class="hljs-attr">page</span>: <span class="hljs-number">1</span>,<br><br>  <span class="hljs-keyword">async</span> <span class="hljs-title function_">onReachBottom</span>(<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-comment">// 守卫：请求中 / 没有更多时，直接忽略触底事件</span><br>    <span class="hljs-keyword">if</span> (<span class="hljs-variable language_">this</span>.<span class="hljs-property">data</span>.<span class="hljs-property">loading</span> || <span class="hljs-variable language_">this</span>.<span class="hljs-property">data</span>.<span class="hljs-property">finished</span>) <span class="hljs-keyword">return</span><br>    <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">setData</span>(&#123; <span class="hljs-attr">loading</span>: <span class="hljs-literal">true</span> &#125;)<br><br>    <span class="hljs-keyword">const</span> &#123; list, hasMore &#125; = <span class="hljs-keyword">await</span> <span class="hljs-title function_">fetchPage</span>(<span class="hljs-variable language_">this</span>.<span class="hljs-property">page</span>++)<br>    <span class="hljs-keyword">if</span> (!hasMore) <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">setData</span>(&#123; <span class="hljs-attr">finished</span>: <span class="hljs-literal">true</span> &#125;)<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">appendList</span>(list)<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">setData</span>(&#123; <span class="hljs-attr">loading</span>: <span class="hljs-literal">false</span> &#125;)<br>  &#125;<br>&#125;)<br></code></pre></td></tr></table></figure>

<h3 id="3-3-第二层：差量更新，别全量推（核心）"><a href="#3-3-第二层：差量更新，别全量推（核心）" class="headerlink" title="3.3 第二层：差量更新，别全量推（核心）"></a>3.3 第二层：差量更新，别全量推（核心）</h3><figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// ❌ 全量：整个 list 重新序列化、重新传输</span><br><span class="hljs-variable language_">this</span>.<span class="hljs-title function_">setData</span>(&#123; <span class="hljs-attr">list</span>: <span class="hljs-variable language_">this</span>.<span class="hljs-property">data</span>.<span class="hljs-property">list</span>.<span class="hljs-title function_">concat</span>(batch) &#125;)<br><br><span class="hljs-comment">// ✅ 追加：只传新增的段</span><br><span class="hljs-title function_">appendList</span>(<span class="hljs-params">batch</span>) &#123;<br>  <span class="hljs-keyword">const</span> patch = &#123;&#125;<br>  batch.<span class="hljs-title function_">forEach</span>(<span class="hljs-function">(<span class="hljs-params">item, i</span>) =&gt;</span> &#123;<br>    patch[<span class="hljs-string">`list[<span class="hljs-subst">$&#123;<span class="hljs-variable language_">this</span>.data.list.length + i&#125;</span>]`</span>] = item<br>  &#125;)<br>  <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">setData</span>(patch)<br>&#125;<br><br><span class="hljs-comment">// ✅ 改单项：只传变化的字段（如给第 3 条设置加载态）</span><br><span class="hljs-variable language_">this</span>.<span class="hljs-title function_">setData</span>(&#123; [<span class="hljs-string">`list[3].status`</span>]: <span class="hljs-string">&#x27;loading&#x27;</span> &#125;)<br></code></pre></td></tr></table></figure>

<p>配套的三条习惯：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① data 只放渲染需要的数据，纯逻辑变量挂 this 上（不进 setData）<br>② 高频事件里别 setData：onPageScroll / bindscroll 每秒几十次，<br>   需要的话做节流，或改用 WXS 响应事件<br>③ 列表项封装成自定义组件，item 内部状态变化只 setData 组件自己，<br>   不惊动整页<br></code></pre></td></tr></table></figure>

<h3 id="3-4-第四位之后的内容不渲染：懒渲染"><a href="#3-4-第四位之后的内容不渲染：懒渲染" class="headerlink" title="3.4 第四位之后的内容不渲染：懒渲染"></a>3.4 第四位之后的内容不渲染：懒渲染</h3><p>信息流里一屏只见 3~4 条，用户往下滚才需要后面的内容。用 <code>IntersectionObserver</code> 只渲染进入视口的 item，DOM 数量直接砍到 1&#x2F;10：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">view</span> <span class="hljs-attr">wx:for</span>=<span class="hljs-string">&quot;&#123;&#123;list&#125;&#125;&quot;</span> <span class="hljs-attr">wx:key</span>=<span class="hljs-string">&quot;id&quot;</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;item-observer item-&#123;&#123;item.visible ? &#x27;show&#x27; : &#x27;placeholder&#x27;&#125;&#125;&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">block</span> <span class="hljs-attr">wx:if</span>=<span class="hljs-string">&quot;&#123;&#123;item.visible&#125;&#125;&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">order-card</span> <span class="hljs-attr">item</span>=<span class="hljs-string">&quot;&#123;&#123;item&#125;&#125;&quot;</span> /&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">block</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">view</span> <span class="hljs-attr">wx:else</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;ph&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">view</span>&gt;</span>  <span class="hljs-comment">&lt;!-- 固定高度的占位，防止滚动抖动 --&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">view</span>&gt;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-title function_">onLoad</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-variable language_">this</span>.<span class="hljs-property">observer</span> = wx.<span class="hljs-title function_">createIntersectionObserver</span>(<span class="hljs-variable language_">this</span>, &#123; <span class="hljs-attr">observeAll</span>: <span class="hljs-literal">true</span> &#125;)<br>  <span class="hljs-comment">// 比可视区域多观察上下三屏，滚动时提前渲染</span><br>  <span class="hljs-variable language_">this</span>.<span class="hljs-property">observer</span>.<span class="hljs-title function_">relativeViewport</span>(&#123; <span class="hljs-attr">bottom</span>: <span class="hljs-number">3000</span> &#125;).<span class="hljs-title function_">observe</span>(<span class="hljs-string">&#x27;.item-observer&#x27;</span>, <span class="hljs-function">(<span class="hljs-params">res</span>) =&gt;</span> &#123;<br>    <span class="hljs-keyword">const</span> &#123; index &#125; = res.<span class="hljs-property">dataset</span><br>    <span class="hljs-keyword">if</span> (res.<span class="hljs-property">intersectionRatio</span> &gt; <span class="hljs-number">0</span> &amp;&amp; !<span class="hljs-variable language_">this</span>.<span class="hljs-property">data</span>.<span class="hljs-property">list</span>[index].<span class="hljs-property">visible</span>) &#123;<br>      <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">setData</span>(&#123; [<span class="hljs-string">`list[<span class="hljs-subst">$&#123;index&#125;</span>].visible`</span>]: <span class="hljs-literal">true</span> &#125;)   <span class="hljs-comment">// 又是差量更新</span><br>    &#125;<br>  &#125;)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>给每个 item 加 <code>data-index=&quot;&#123;&#123;index&#125;&#125;&quot;</code>，回调里就能拿到索引。</p>
<h3 id="3-5-万级列表：官方虚拟列表-recycle-view"><a href="#3-5-万级列表：官方虚拟列表-recycle-view" class="headerlink" title="3.5 万级列表：官方虚拟列表 recycle-view"></a>3.5 万级列表：官方虚拟列表 recycle-view</h3><p>商品库、消息记录这种几千上万条的列表，用官方 <code>miniprogram-recycle-view</code>——<strong>只渲染可视区域 ± 缓冲区的节点</strong>，滚出屏幕的节点回收复用，无论数据多少条，渲染节点数恒定：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">npm install miniprogram-recycle-view<br></code></pre></td></tr></table></figure>

<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-comment">// 页面 json</span><br><span class="hljs-punctuation">&#123;</span><br>  <span class="hljs-attr">&quot;usingComponents&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">&#123;</span><br>    <span class="hljs-attr">&quot;recycle-view&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;miniprogram-recycle-view/recycle-view&quot;</span><span class="hljs-punctuation">,</span><br>    <span class="hljs-attr">&quot;recycle-item&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;miniprogram-recycle-view/recycle-item&quot;</span><br>  <span class="hljs-punctuation">&#125;</span><br><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-comment">&lt;!-- 页面 wxml --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">recycle-view</span> <span class="hljs-attr">batch</span>=<span class="hljs-string">&quot;&#123;&#123;batchSetData&#125;&#125;&quot;</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;recycleId&quot;</span> <span class="hljs-attr">enable-back-to-top</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">recycle-item</span> <span class="hljs-attr">wx:for</span>=<span class="hljs-string">&quot;&#123;&#123;recycleList&#125;&#125;&quot;</span> <span class="hljs-attr">wx:key</span>=<span class="hljs-string">&quot;id&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">order-card</span> <span class="hljs-attr">item</span>=<span class="hljs-string">&quot;&#123;&#123;item&#125;&#125;&quot;</span> /&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">recycle-item</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">recycle-view</span>&gt;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">const</span> createRecycleContext = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;miniprogram-recycle-view&#x27;</span>)<br><br><span class="hljs-title class_">Page</span>(&#123;<br>  <span class="hljs-title function_">onLoad</span>(<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">ctx</span> = <span class="hljs-title function_">createRecycleContext</span>(<span class="hljs-variable language_">this</span>, &#123;<br>      <span class="hljs-attr">id</span>: <span class="hljs-string">&#x27;recycleId&#x27;</span>,            <span class="hljs-comment">// 对应 wxml 里的 id</span><br>      <span class="hljs-attr">dataKey</span>: <span class="hljs-string">&#x27;recycleList&#x27;</span>,     <span class="hljs-comment">// 对应 wxml 里 wx:for 的数组名</span><br>      <span class="hljs-attr">page</span>: <span class="hljs-variable language_">this</span>,<br>      <span class="hljs-attr">itemSize</span>: &#123; <span class="hljs-attr">width</span>: <span class="hljs-number">710</span>, <span class="hljs-attr">height</span>: <span class="hljs-number">120</span> &#125;   <span class="hljs-comment">// 单位 rpx，定高才能算滚动</span><br>    &#125;)<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">fetchPage</span>(<span class="hljs-number">1</span>).<span class="hljs-title function_">then</span>(<span class="hljs-function"><span class="hljs-params">list</span> =&gt;</span> <span class="hljs-variable language_">this</span>.<span class="hljs-property">ctx</span>.<span class="hljs-title function_">append</span>(list))<br>  &#125;,<br><br>  <span class="hljs-keyword">async</span> <span class="hljs-title function_">onReachBottom</span>(<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-keyword">const</span> list = <span class="hljs-keyword">await</span> <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">fetchPage</span>(++<span class="hljs-variable language_">this</span>.<span class="hljs-property">page</span>)<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">ctx</span>.<span class="hljs-title function_">append</span>(list)              <span class="hljs-comment">// 追加也只传增量</span><br>    <span class="hljs-comment">// this.ctx.splice(start, n, ...items)   删改用 splice，同样差量</span><br>  &#125;<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>限制要知道：<code>recycle-view</code> 要求<strong>条目定高</strong>（不定高的列表要先估算高度），这是它换性能的代价。几百条以内的列表，用 3.2~3.4 的组合就够了，不必上虚拟列表。</p>
<hr>
<h2 id="四、速查表"><a href="#四、速查表" class="headerlink" title="四、速查表"></a>四、速查表</h2><table>
<thead>
<tr>
<th>阶段</th>
<th>手段</th>
<th>关键点</th>
</tr>
</thead>
<tbody><tr>
<td>下载</td>
<td>分包</td>
<td>单包 ≤ 2M、tabBar 页在主包、图片走 CDN</td>
</tr>
<tr>
<td>下载</td>
<td>preloadRule 预下载</td>
<td>用户在首页时拉下一个分包，跳转零等待</td>
</tr>
<tr>
<td>下载</td>
<td>独立分包</td>
<td>分享直进的活动页不下载主包，秒开</td>
</tr>
<tr>
<td>下载</td>
<td>分包异步化</td>
<td><code>require.async</code> + <code>componentPlaceholder</code>，跨包不拖主包</td>
</tr>
<tr>
<td>首屏</td>
<td>骨架屏</td>
<td>尺寸与真实内容一致防跳动；纯静态零请求；onLoad 尽早发请求</td>
</tr>
<tr>
<td>运行时</td>
<td>setData 差量</td>
<td>路径更新 <code>list[i].field</code>，单次 ≤ 1M，别高频调用</td>
</tr>
<tr>
<td>运行时</td>
<td>懒渲染</td>
<td>IntersectionObserver，视口外加占位不渲染</td>
</tr>
<tr>
<td>运行时</td>
<td>虚拟列表</td>
<td>recycle-view，节点数恒定，条目需定高</td>
</tr>
<tr>
<td>运行时</td>
<td>图片</td>
<td><code>&lt;image lazy-load&gt;</code>，列表图用缩略图尺寸</td>
</tr>
</tbody></table>
<p><strong>落地顺序</strong>：先跑体验评分定位阶段 → 分包保启动（硬性要求）→ 骨架屏保感知（半天工作量）→ 分页 + 差量 setData（性价比最高）→ 真有万级列表再上 recycle-view。</p>
<hr>
<h3 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h3><ul>
<li><a href="https://developers.weixin.qq.com/miniprogram/dev/framework/subpackages/basic.html">微信官方文档 - 使用分包</a></li>
<li><a href="https://developers.weixin.qq.com/miniprogram/dev/framework/subpackages/preload.html">微信官方文档 - 分包预下载</a></li>
<li><a href="https://developers.weixin.qq.com/miniprogram/dev/framework/performance/tips/runtime_setData.html">微信官方文档 - setData 性能</a></li>
<li><a href="https://github.com/wechat-miniprogram/recycle-view">miniprogram-recycle-view（官方虚拟列表组件）</a></li>
</ul>
]]></content>
      <categories>
        <category>前端工程化</category>
        <category>小程序</category>
      </categories>
      <tags>
        <tag>性能优化</tag>
        <tag>微信小程序</tag>
      </tags>
  </entry>
  <entry>
    <title>SPA 首屏优化：减体积、抢网络、缩白屏</title>
    <url>/2026/09/10/spa-first-screen-optimization/</url>
    <content><![CDATA[<p>SPA 首屏慢的根源是结构性的：HTML 里只有一个空的 <code>&lt;div id=&quot;app&quot;&gt;</code>，<strong>一切内容都要等 JS 下载、解析、执行完才能出现</strong>。所以优化逃不出三个方向：</p>
<blockquote>
<p><strong>减体积</strong>——让首屏只下载首屏需要的代码；<strong>抢网络</strong>——让必须传的字节更快到达；<strong>缩白屏</strong>——让 JS 接管之前，用户就有东西可看。</p>
</blockquote>
<p>先把时间线摆出来，慢在哪一段，就用哪一段的手段：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">导航开始 ─► TTFB ─► HTML 到达 ─► JS/CSS 下载 ─► 解析执行 ─► 首屏渲染 ─► 首屏接口返回 ─► 内容完整<br>             │                    │               │            │              │<br>          服务端慢？           体积/网络？        长任务？      接口串行？<br>          (后端/SSR)          (减体积/抢网络)    (拆任务)     (并行/预取)<br></code></pre></td></tr></table></figure>

<span id="more"></span>

<h2 id="一、先量化，再优化"><a href="#一、先量化，再优化" class="headerlink" title="一、先量化，再优化"></a>一、先量化，再优化</h2><p>不测量就优化，等于蒙眼开药。三个核心指标：</p>
<ul>
<li><strong>FCP</strong>（First Contentful Paint）：首次画出任何内容的时间，直接反映白屏期长短；</li>
<li><strong>LCP</strong>（Largest Contentful Paint）：最大内容元素出现的时间，<strong>低于 2.5s 算合格</strong>，首屏优化的主目标；</li>
<li><strong>INP</strong>：交互响应延迟，替代了旧的 TTI，主要衡量「能不能用」而非「能不能看」。</li>
</ul>
<p>工具就两个：<strong>Lighthouse</strong> 跑总分和机会清单，<strong>Chrome Performance 面板</strong>看瀑布图定位具体瓶颈。另外 Performance 面板里的 <strong>Coverage</strong> 标签值得专门一提——它能统计首屏 JS 里「下载了但没执行」的代码占比，这个数字通常是 60% 以上，这就是代码分割的空间。</p>
<p>线上监控用 <code>web-vitals</code> 库上报真实用户数据：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-keyword">import</span> &#123; onLCP, onFCP, onINP &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;web-vitals&#x27;</span>;<br><br><span class="hljs-title function_">onLCP</span>(<span class="hljs-function"><span class="hljs-params">metric</span> =&gt;</span> <span class="hljs-title function_">report</span>(<span class="hljs-string">&#x27;LCP&#x27;</span>, metric.<span class="hljs-property">value</span>));<br><span class="hljs-title function_">onFCP</span>(<span class="hljs-function"><span class="hljs-params">metric</span> =&gt;</span> <span class="hljs-title function_">report</span>(<span class="hljs-string">&#x27;FCP&#x27;</span>, metric.<span class="hljs-property">value</span>));<br><span class="hljs-title function_">onINP</span>(<span class="hljs-function"><span class="hljs-params">metric</span> =&gt;</span> <span class="hljs-title function_">report</span>(<span class="hljs-string">&#x27;INP&#x27;</span>, metric.<span class="hljs-property">value</span>));<br></code></pre></td></tr></table></figure>

<h2 id="二、减体积：让首屏只背首屏的债"><a href="#二、减体积：让首屏只背首屏的债" class="headerlink" title="二、减体积：让首屏只背首屏的债"></a>二、减体积：让首屏只背首屏的债</h2><h3 id="路由级代码分割（收益最大）"><a href="#路由级代码分割（收益最大）" class="headerlink" title="路由级代码分割（收益最大）"></a>路由级代码分割（收益最大）</h3><p>SPA 最常见的问题是<strong>全站代码打进一个 bundle</strong>——用户只看首页，却下载了设置页、订单页、编辑器组件。路由懒加载让每个路由变成独立 chunk，按需加载：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// React</span><br><span class="hljs-keyword">import</span> &#123; lazy, <span class="hljs-title class_">Suspense</span> &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;react&#x27;</span>;<br><br><span class="hljs-keyword">const</span> <span class="hljs-title class_">Settings</span> = <span class="hljs-title function_">lazy</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;./pages/Settings&#x27;</span>));<br><br><span class="hljs-comment">// 用 Suspense 包住路由出口，加载期间显示 fallback</span><br><span class="language-xml"><span class="hljs-tag">&lt;<span class="hljs-name">Suspense</span> <span class="hljs-attr">fallback</span>=<span class="hljs-string">&#123;</span>&lt;<span class="hljs-attr">PageSkeleton</span> /&gt;</span>&#125;&gt;</span><br><span class="language-xml">  <span class="hljs-tag">&lt;<span class="hljs-name">Settings</span> /&gt;</span></span><br><span class="language-xml"><span class="hljs-tag">&lt;/<span class="hljs-name">Suspense</span>&gt;</span></span><br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// Vue Router</span><br><span class="hljs-keyword">const</span> routes = [<br>  &#123; <span class="hljs-attr">path</span>: <span class="hljs-string">&#x27;/settings&#x27;</span>, <span class="hljs-attr">component</span>: <span class="hljs-function">() =&gt;</span> <span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;@/views/Settings.vue&#x27;</span>) &#125;,<br>];<br></code></pre></td></tr></table></figure>

<p>注意理解它对首屏的意义：首页 chunk 本身没变小，而是<strong>其他路由不再拖累首页</strong>。首屏 JS 从「全站体积」降到「首页体积」，大项目里常常是 1MB 到 300KB 的差距。</p>
<h3 id="依赖分包-长缓存"><a href="#依赖分包-长缓存" class="headerlink" title="依赖分包 + 长缓存"></a>依赖分包 + 长缓存</h3><p>把稳定的第三方依赖拆成单独的 vendor chunk，配合文件名 contenthash，<strong>业务代码天天发版，vendor 缓存常年有效</strong>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// vite.config.js</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">default</span> &#123;<br>  <span class="hljs-attr">build</span>: &#123;<br>    <span class="hljs-attr">rollupOptions</span>: &#123;<br>      <span class="hljs-attr">output</span>: &#123;<br>        <span class="hljs-attr">manualChunks</span>: &#123;<br>          <span class="hljs-attr">vue</span>: [<span class="hljs-string">&#x27;vue&#x27;</span>, <span class="hljs-string">&#x27;vue-router&#x27;</span>, <span class="hljs-string">&#x27;pinia&#x27;</span>],<br>          <span class="hljs-attr">vendor</span>: [<span class="hljs-string">&#x27;axios&#x27;</span>, <span class="hljs-string">&#x27;dayjs&#x27;</span>],<br>        &#125;,<br>      &#125;,<br>    &#125;,<br>  &#125;,<br>&#125;;<br></code></pre></td></tr></table></figure>

<p>Webpack 对应 <code>splitChunks.cacheGroups</code>，思路相同。拆的粒度别太细——chunk 过多在 HTTP&#x2F;1.1 下反而拖慢，几十 KB 一个比较合理。</p>
<h3 id="替换与按需引入重型依赖"><a href="#替换与按需引入重型依赖" class="headerlink" title="替换与按需引入重型依赖"></a>替换与按需引入重型依赖</h3><p>几个经典的重灾区：</p>
<ul>
<li><strong>moment → dayjs</strong>：moment 加上全量 locale 能有几百 KB，dayjs 核心 2KB，API 几乎一致；</li>
<li><strong>lodash</strong>：<code>import debounce from &#39;lodash/debounce&#39;</code> 按文件引入，或直接用 <code>lodash-es</code> 让 tree shaking 生效；</li>
<li><strong>UI 组件库按需加载</strong>：Element Plus 配 <code>unplugin-vue-components</code>，antd v5 已默认支持 tree shaking。</li>
</ul>
<h3 id="让-Tree-Shaking-真正生效"><a href="#让-Tree-Shaking-真正生效" class="headerlink" title="让 Tree Shaking 真正生效"></a>让 Tree Shaking 真正生效</h3><p>两个前提，缺一个都摇不动：<strong>依赖必须是 ESM 格式</strong>（CJS 的 <code>require</code> 是运行时动态的，静态分析不了）；库的 <code>package.json</code> 要正确声明 <code>sideEffects</code>。判断方法很简单——构建产物里搜一个你从没 import 过的导出，居然在，就是没摇掉。</p>
<h2 id="三、网络层：让字节更快到达"><a href="#三、网络层：让字节更快到达" class="headerlink" title="三、网络层：让字节更快到达"></a>三、网络层：让字节更快到达</h2><h3 id="压缩：gzip-是底线，brotli-更好"><a href="#压缩：gzip-是底线，brotli-更好" class="headerlink" title="压缩：gzip 是底线，brotli 更好"></a>压缩：gzip 是底线，brotli 更好</h3><p>文本资源不压缩就上线属于事故。gzip 能砍掉约 70% 体积，brotli 再省 15%~20%：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><code class="hljs nginx"><span class="hljs-attribute">gzip</span> <span class="hljs-literal">on</span>;<br><span class="hljs-attribute">gzip_comp_level</span> <span class="hljs-number">6</span>;<br><span class="hljs-attribute">gzip_min_length</span> <span class="hljs-number">1k</span>;<br><span class="hljs-attribute">gzip_types</span> text/css application/javascript application/json image/svg+xml;<br></code></pre></td></tr></table></figure>

<p>更好的做法是<strong>构建时预压缩</strong>（<code>vite-plugin-compression</code> 生成 <code>.gz</code> &#x2F; <code>.br</code> 文件），Nginx 用 <code>gzip_static</code> 直接发文件，省掉运行时压缩的 CPU。</p>
<h3 id="CDN-分域部署-缓存策略"><a href="#CDN-分域部署-缓存策略" class="headerlink" title="CDN + 分域部署 + 缓存策略"></a>CDN + 分域部署 + 缓存策略</h3><p>静态资源上 CDN 是基本盘。缓存策略记住一条铁律：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><code class="hljs nginx"><span class="hljs-comment"># 带 hash 的静态资源：缓存一年，永不协商</span><br><span class="hljs-section">location</span> <span class="hljs-regexp">~* \.(js|css|woff2|png|jpg)$</span> &#123;<br>  <span class="hljs-attribute">add_header</span> Cache-Control <span class="hljs-string">&quot;public, max-age=31536000, immutable&quot;</span>;<br>&#125;<br><br><span class="hljs-comment"># HTML 是资源的&quot;索引&quot;，必须每次校验，否则发版用户看不到</span><br><span class="hljs-section">location</span> = /index.html &#123;<br>  <span class="hljs-attribute">add_header</span> Cache-Control <span class="hljs-string">&quot;no-cache&quot;</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>逻辑闭环是：HTML 不缓存 → 每次拿到最新 hash → hash 变了才下载新 JS，没变走缓存。这样<strong>二次访问的首屏几乎只剩 HTML 一个请求</strong>。</p>
<h3 id="资源提示：把等待提前变成准备"><a href="#资源提示：把等待提前变成准备" class="headerlink" title="资源提示：把等待提前变成准备"></a>资源提示：把等待提前变成准备</h3><figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-comment">&lt;!-- 对关键域名提前完成 DNS + TCP + TLS 握手，能省几百毫秒 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">link</span> <span class="hljs-attr">rel</span>=<span class="hljs-string">&quot;preconnect&quot;</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;https://cdn.example.com&quot;</span> <span class="hljs-attr">crossorigin</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">link</span> <span class="hljs-attr">rel</span>=<span class="hljs-string">&quot;preconnect&quot;</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;https://api.example.com&quot;</span>&gt;</span><br><br><span class="hljs-comment">&lt;!-- 提前加载 ESM 入口及其 import 依赖，浏览器并行去取 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">link</span> <span class="hljs-attr">rel</span>=<span class="hljs-string">&quot;modulepreload&quot;</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;/assets/index-a1b2c3.js&quot;</span>&gt;</span><br><br><span class="hljs-comment">&lt;!-- 关键字体必须 preload，且 crossorigin 不能少，否则加载两次 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">link</span> <span class="hljs-attr">rel</span>=<span class="hljs-string">&quot;preload&quot;</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;/fonts/main.woff2&quot;</span> <span class="hljs-attr">as</span>=<span class="hljs-string">&quot;font&quot;</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;font/woff2&quot;</span> <span class="hljs-attr">crossorigin</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p><code>dns-prefetch</code> 是 <code>preconnect</code> 的降级版（只做 DNS 解析），给不重要域名用。<code>prefetch</code>（空闲时预取下一页 chunk）慎用——抢占移动端带宽。</p>
<h2 id="四、白屏期：让用户尽早”看见点什么”"><a href="#四、白屏期：让用户尽早”看见点什么”" class="headerlink" title="四、白屏期：让用户尽早”看见点什么”"></a>四、白屏期：让用户尽早”看见点什么”</h2><p>前面解决的是「快」，这一节解决「体感」——时间没变，但用户从空白变成看到布局，焦虑感完全不同。</p>
<h3 id="骨架屏：性价比之王"><a href="#骨架屏：性价比之王" class="headerlink" title="骨架屏：性价比之王"></a>骨架屏：性价比之王</h3><p>SPA 的白屏来自 <code>#app</code> 里什么都没有。最简单的方案：<strong>直接把骨架屏写进 <code>index.html</code></strong>，JS 挂载完成后会自然替换掉它：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;app&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;skeleton&quot;</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;sk-nav&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;sk-banner&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br>    <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;sk-content&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br>  <span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight css"><table><tr><td class="code"><pre><code class="hljs css"><span class="hljs-selector-class">.sk-banner</span> &#123;<br>  <span class="hljs-attribute">height</span>: <span class="hljs-number">200px</span>;<br>  <span class="hljs-attribute">border-radius</span>: <span class="hljs-number">8px</span>;<br>  <span class="hljs-attribute">background</span>: <span class="hljs-built_in">linear-gradient</span>(<span class="hljs-number">90deg</span>, <span class="hljs-number">#f2f2f2</span> <span class="hljs-number">25%</span>, <span class="hljs-number">#e8e8e8</span> <span class="hljs-number">37%</span>, <span class="hljs-number">#f2f2f2</span> <span class="hljs-number">63%</span>);<br>  <span class="hljs-attribute">background-size</span>: <span class="hljs-number">400%</span> <span class="hljs-number">100%</span>;<br>  <span class="hljs-attribute">animation</span>: shimmer <span class="hljs-number">1.4s</span> ease infinite;<br>&#125;<br><span class="hljs-keyword">@keyframes</span> shimmer &#123;<br>  <span class="hljs-number">0%</span>   &#123; <span class="hljs-attribute">background-position</span>: <span class="hljs-number">100%</span> <span class="hljs-number">0</span>; &#125;<br>  <span class="hljs-number">100%</span> &#123; <span class="hljs-attribute">background-position</span>: -<span class="hljs-number">100%</span> <span class="hljs-number">0</span>; &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="内联关键-CSS"><a href="#内联关键-CSS" class="headerlink" title="内联关键 CSS"></a>内联关键 CSS</h3><p>CSS 不加载完，浏览器不渲染任何东西（这是渲染阻塞）。把<strong>首屏必须的样式</strong>（布局、header、骨架屏）内联进 HTML，其余 CSS 异步加载：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">style</span>&gt;</span><span class="language-css"><span class="hljs-comment">/* 首屏关键样式，可由 critical 等工具构建时抽取 */</span></span><span class="hljs-tag">&lt;/<span class="hljs-name">style</span>&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">link</span> <span class="hljs-attr">rel</span>=<span class="hljs-string">&quot;preload&quot;</span> <span class="hljs-attr">as</span>=<span class="hljs-string">&quot;style&quot;</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;/css/main-hash.css&quot;</span></span><br><span class="hljs-tag">      <span class="hljs-attr">onload</span>=<span class="hljs-string">&quot;this.rel=&#x27;stylesheet&#x27;&quot;</span>&gt;</span><br></code></pre></td></tr></table></figure>

<h3 id="首屏数据：别让接口排队"><a href="#首屏数据：别让接口排队" class="headerlink" title="首屏数据：别让接口排队"></a>首屏数据：别让接口排队</h3><p>很多项目的时间线是「等 JS 加载完 → 执行 → 才发第一个接口」——数据请求被串行在了资源加载后面。解法按侵入程度排序：</p>
<ol>
<li><strong>接口并行</strong>：多个首屏请求用 <code>Promise.all</code> 同时发，别嵌套 await；</li>
<li><strong>数据随 HTML 下发</strong>：服务端把首屏数据注入 <code>window.__INITIAL_STATE__</code>，JS 拿来就用，省一轮 RTT；</li>
<li><strong>SSR</strong>：连渲染都在服务端做完，见下。</li>
</ol>
<h3 id="SSR-SSG-预渲染：终极方案"><a href="#SSR-SSG-预渲染：终极方案" class="headerlink" title="SSR &#x2F; SSG &#x2F; 预渲染：终极方案"></a>SSR &#x2F; SSG &#x2F; 预渲染：终极方案</h3><table>
<thead>
<tr>
<th>方案</th>
<th>原理</th>
<th>适用场景</th>
<th>成本</th>
</tr>
</thead>
<tbody><tr>
<td>预渲染（prerender）</td>
<td>构建时用无头浏览器把首屏 HTML 生成好</td>
<td>纯静态站、营销页</td>
<td>低</td>
</tr>
<tr>
<td>SSR（Nuxt &#x2F; Next）</td>
<td>每次请求服务端渲染 HTML 再注水</td>
<td>动态内容、强 SEO 需求</td>
<td>高（服务器、双端逻辑）</td>
</tr>
<tr>
<td>SSG</td>
<td>构建时生成全部页面</td>
<td>博客、文档站</td>
<td>中</td>
</tr>
<tr>
<td>骨架屏</td>
<td>不减少实际时间，改善体感</td>
<td>所有 SPA 的兜底</td>
<td>极低</td>
</tr>
</tbody></table>
<p>SSR 把白屏问题连根拔掉，但也引入服务器成本、注水（hydration）开销和双端兼容问题——<strong>先把前两节的做了，SSR 往往就不需要了</strong>。</p>
<h2 id="五、别忽略图片"><a href="#五、别忽略图片" class="headerlink" title="五、别忽略图片"></a>五、别忽略图片</h2><p>图片常常占首屏传输的大头，而且优化全是声明式的：</p>
<ul>
<li><strong>格式</strong>：WebP 起步，CDN 支持 AVIF 更好（同画质再小 20%）；</li>
<li><strong>懒加载</strong>：非首屏图片一行搞定 <code>&lt;img loading=&quot;lazy&quot; decoding=&quot;async&quot;&gt;</code>；</li>
<li><strong>防 CLS</strong>：写死 <code>width</code> &#x2F; <code>height</code>（或 CSS <code>aspect-ratio</code>），图片加载才不会把内容顶下去；</li>
<li><strong>按需尺寸</strong>：用 CDN 的裁剪参数按屏幕宽度取图，手机别下 2000px 的图，配 <code>srcset</code> 让浏览器自己选。</li>
</ul>
<h2 id="六、总结：一张表-实践顺序"><a href="#六、总结：一张表-实践顺序" class="headerlink" title="六、总结：一张表 + 实践顺序"></a>六、总结：一张表 + 实践顺序</h2><table>
<thead>
<tr>
<th>手段</th>
<th>解决的问题</th>
<th>收益量级</th>
<th>成本</th>
</tr>
</thead>
<tbody><tr>
<td>路由懒加载 + 依赖分包</td>
<td>首屏 JS 体积</td>
<td>⭐⭐⭐</td>
<td>半天，纯代码</td>
</tr>
<tr>
<td>替换重型依赖（moment 等）</td>
<td>体积</td>
<td>⭐⭐</td>
<td>视替换范围</td>
</tr>
<tr>
<td>gzip &#x2F; brotli</td>
<td>传输体积（约 -70%）</td>
<td>⭐⭐⭐</td>
<td>一行配置</td>
</tr>
<tr>
<td>CDN + 强缓存策略</td>
<td>传输 + 二次访问</td>
<td>⭐⭐⭐</td>
<td>需运维配合</td>
</tr>
<tr>
<td>preconnect &#x2F; modulepreload</td>
<td>连接与下载串行等待</td>
<td>⭐⭐</td>
<td>几行 HTML</td>
</tr>
<tr>
<td>骨架屏 + 内联关键 CSS</td>
<td>白屏体感</td>
<td>⭐⭐</td>
<td>半天</td>
</tr>
<tr>
<td>首屏数据预取 &#x2F; 并行</td>
<td>接口串行</td>
<td>⭐⭐</td>
<td>视接口情况</td>
</tr>
<tr>
<td>SSR &#x2F; 预渲染</td>
<td>白屏期（根治）</td>
<td>⭐⭐⭐</td>
<td>重构级</td>
</tr>
</tbody></table>
<p>推荐的实践顺序：<strong>先量化（Lighthouse + Coverage 找到瓶颈）→ 代码分割 + 按需引入（不依赖任何人）→ 压缩 + CDN + 缓存头（拉上运维）→ 骨架屏 + 关键 CSS → 最后才考虑 SSR</strong>。绝大多数项目做完前四步，LCP 就能从 4s+ 进 2.5s 以内——先把便宜的收益吃完，再动贵的刀。</p>
<hr>
<p>最后一个调试技巧：在 DevTools 的 Network 面板把网速限到 Slow 3G，再看一次瀑布图——很多「明明做了优化还是慢」的问题，答案都藏在那条被拉长的时间线里。</p>
]]></content>
      <categories>
        <category>前端基础</category>
        <category>性能优化</category>
      </categories>
      <tags>
        <tag>性能优化</tag>
        <tag>SPA</tag>
      </tags>
  </entry>
  <entry>
    <title>SPA 首屏优化落地实录：大屏项目的路由分割、依赖分包与异步引擎加载</title>
    <url>/2026/09/10/spa-first-screen-practice/</url>
    <content><![CDATA[<p>上一篇<a href="/2026/09/10/spa-first-screen-optimization/">《SPA 首屏优化：减体积、抢网络、缩白屏》</a>把理论清单列全了。这篇是它的落地篇——我拿手头一个数据可视化大屏项目（Vue 3 + Vite + MapLibre GL）逐条对照那份清单，<strong>有代码支撑的讲透实现，没落地的在文末如实记账</strong>。</p>
<p>先说这个项目的特殊性，它决定了优化的重心：大屏首屏最大的包袱不是业务代码，而是<strong>地图引擎</strong>——maplibre-gl 光 JS 就数百 KB，还有一份全量 CSS；其次才是 Element Plus 和一堆面板组件。所以整个优化围绕一句话展开：<strong>首屏关键路径上只留「能让地图出现的最小集合」，其余全部异步化</strong>。</p>
<p>先给核对结论，再逐条展开：</p>
<table>
<thead>
<tr>
<th>理论手段</th>
<th>项目落地</th>
<th>位置</th>
</tr>
</thead>
<tbody><tr>
<td>路由级代码分割</td>
<td>✅ 全部路由懒加载（含布局组件）</td>
<td><code>router/index.ts</code></td>
</tr>
<tr>
<td>依赖分包 + 长缓存</td>
<td>✅ manualChunks 五路分包 + contenthash</td>
<td><code>vite.config.ts</code></td>
</tr>
<tr>
<td>重型依赖按需&#x2F;异步引入</td>
<td>✅ 地图引擎 JS&#x2F;CSS&#x2F;协议全部动态 import</td>
<td><code>main.ts</code> &#x2F; <code>MapContainer.vue</code></td>
</tr>
<tr>
<td>组件级懒加载</td>
<td>✅ defineAsyncComponent 挂面板</td>
<td><code>views/Home</code></td>
</tr>
<tr>
<td>UI 库按需引入</td>
<td>⚠️ 图标按需注册，组件全量（见文末）</td>
<td><code>main.ts</code></td>
</tr>
<tr>
<td>资源提示 preconnect</td>
<td>✅ 底图 CDN 域名预连接</td>
<td><code>index.html</code></td>
</tr>
<tr>
<td>数据预取&#x2F;缓存</td>
<td>✅ 底图样式预取 + force-cache</td>
<td><code>stores/map.ts</code></td>
</tr>
<tr>
<td>首屏接口并行</td>
<td>✅ Promise.all + 单例防重</td>
<td><code>stores/layer.ts</code></td>
</tr>
<tr>
<td>白屏期反馈</td>
<td>✅ 分阶段加载进度（组件级）</td>
<td><code>MapContainer.vue</code></td>
</tr>
</tbody></table>
<span id="more"></span>

<h2 id="一、减体积：三层异步化"><a href="#一、减体积：三层异步化" class="headerlink" title="一、减体积：三层异步化"></a>一、减体积：三层异步化</h2><h3 id="1-路由懒加载：连布局都不放进首屏"><a href="#1-路由懒加载：连布局都不放进首屏" class="headerlink" title="1. 路由懒加载：连布局都不放进首屏"></a>1. 路由懒加载：连布局都不放进首屏</h3><p>理论篇说过，路由分割的收益是「其他路由不再拖累首页」。这个项目做得比较彻底——<strong>所有路由组件，包括多个路由共享的 MainLayout，全部走动态 import</strong>：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">const</span> <span class="hljs-title function_">MainLayout</span> = (<span class="hljs-params"></span>) =&gt; <span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;@/layouts/MainLayout.vue&#x27;</span>)<br><br><span class="hljs-keyword">const</span> <span class="hljs-attr">routes</span>: <span class="hljs-title class_">RouteRecordRaw</span>[] = [<br>  &#123;<br>    <span class="hljs-attr">path</span>: <span class="hljs-string">&#x27;/login&#x27;</span>,<br>    <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;Login&#x27;</span>,<br>    <span class="hljs-attr">component</span>: <span class="hljs-function">() =&gt;</span> <span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;@/views/Login/index.vue&#x27;</span>),<br>    <span class="hljs-attr">meta</span>: &#123; <span class="hljs-attr">requiresAuth</span>: <span class="hljs-literal">false</span>, <span class="hljs-attr">title</span>: <span class="hljs-string">&#x27;登录&#x27;</span> &#125;,<br>  &#125;,<br>  &#123;<br>    <span class="hljs-attr">path</span>: <span class="hljs-string">&#x27;/&#x27;</span>,<br>    <span class="hljs-attr">component</span>: <span class="hljs-title class_">MainLayout</span>,<br>    <span class="hljs-attr">children</span>: [<br>      &#123; <span class="hljs-attr">path</span>: <span class="hljs-string">&#x27;&#x27;</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;Home&#x27;</span>, <span class="hljs-attr">component</span>: <span class="hljs-function">() =&gt;</span> <span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;@/views/Home/index.vue&#x27;</span>) &#125;,<br>    ],<br>  &#125;,<br>  &#123;<br>    <span class="hljs-attr">path</span>: <span class="hljs-string">&#x27;/system&#x27;</span>,<br>    <span class="hljs-attr">component</span>: <span class="hljs-title class_">MainLayout</span>,<br>    <span class="hljs-attr">children</span>: [<br>      &#123; <span class="hljs-attr">path</span>: <span class="hljs-string">&#x27;user&#x27;</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;SystemUser&#x27;</span>, <span class="hljs-attr">component</span>: <span class="hljs-function">() =&gt;</span> <span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;@/views/System/User.vue&#x27;</span>) &#125;,<br>      <span class="hljs-comment">// 设施管理、样式配置、分析报表……十余个路由，全部同款写法</span><br>    ],<br>  &#125;,<br>]<br></code></pre></td></tr></table></figure>

<p>两个细节：MainLayout 被多个路由引用时，Rollup 会自动提取成共享 chunk，各路由并行预加载，不会重复下载；管理后台那十几个页面（用户&#x2F;角色&#x2F;日志&#x2F;数据上传）对只看大屏首屏的用户来说<strong>一个字节都不该下载</strong>——懒加载后它们各自躺在独立的 chunk 里，访问到才拉取。</p>
<h3 id="2-依赖分包：五路-manualChunks-contenthash"><a href="#2-依赖分包：五路-manualChunks-contenthash" class="headerlink" title="2. 依赖分包：五路 manualChunks + contenthash"></a>2. 依赖分包：五路 manualChunks + contenthash</h3><figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// vite.config.ts</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">default</span> <span class="hljs-title function_">defineConfig</span>(&#123;<br>  <span class="hljs-attr">build</span>: &#123;<br>    <span class="hljs-attr">cssCodeSplit</span>: <span class="hljs-literal">true</span>,               <span class="hljs-comment">// CSS 代码分割</span><br>    <span class="hljs-attr">chunkSizeWarningLimit</span>: <span class="hljs-number">1000</span>,<br>    <span class="hljs-attr">rollupOptions</span>: &#123;<br>      <span class="hljs-attr">output</span>: &#123;<br>        <span class="hljs-comment">// 手动分包策略</span><br>        <span class="hljs-attr">manualChunks</span>: &#123;<br>          <span class="hljs-string">&#x27;vue-vendor&#x27;</span>: [<span class="hljs-string">&#x27;vue&#x27;</span>, <span class="hljs-string">&#x27;vue-router&#x27;</span>, <span class="hljs-string">&#x27;pinia&#x27;</span>],        <span class="hljs-comment">// 框架核心</span><br>          <span class="hljs-string">&#x27;element-plus&#x27;</span>: [<span class="hljs-string">&#x27;element-plus&#x27;</span>, <span class="hljs-string">&#x27;@element-plus/icons-vue&#x27;</span>],  <span class="hljs-comment">// UI 库</span><br>          <span class="hljs-string">&#x27;maplibre&#x27;</span>: [<span class="hljs-string">&#x27;maplibre-gl&#x27;</span>],                          <span class="hljs-comment">// 地图引擎</span><br>          <span class="hljs-string">&#x27;chat-ui&#x27;</span>: [<span class="hljs-string">&#x27;vue-element-plus-x&#x27;</span>],                    <span class="hljs-comment">// AI 聊天组件</span><br>          <span class="hljs-string">&#x27;http&#x27;</span>: [<span class="hljs-string">&#x27;axios&#x27;</span>],                                    <span class="hljs-comment">// HTTP 工具</span><br>        &#125;,<br>        <span class="hljs-comment">// 文件命名策略：contenthash 打头</span><br>        <span class="hljs-attr">chunkFileNames</span>: <span class="hljs-string">&#x27;js/[name]-[hash].js&#x27;</span>,<br>        <span class="hljs-attr">entryFileNames</span>: <span class="hljs-string">&#x27;js/[name]-[hash].js&#x27;</span>,<br>        <span class="hljs-attr">assetFileNames</span>: <span class="hljs-string">&#x27;[ext]/[name]-[hash].[ext]&#x27;</span>,<br>      &#125;,<br>    &#125;,<br>    <span class="hljs-attr">minify</span>: <span class="hljs-string">&#x27;esbuild&#x27;</span>,<br>  &#125;,<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>分包的划分逻辑是<strong>变更频率</strong>：<code>vue-vendor</code> 几乎永不发版（缓存常年有效），<code>element-plus</code> 跟随 UI 库升级偶尔变，业务 chunk 天天变。配合 <code>[hash]</code> 文件名，理论篇那条「HTML 不缓存 → 拿最新 hash → 没变的 chunk 走强缓存」的闭环就有了物理基础——改一行业务代码，用户重新下载的只有业务 chunk 和 HTML。</p>
<p><code>&#39;http&#39;: [&#39;axios&#39;]</code> 这种小分包值得单说：axios 本身只有十几 KB，单独拆出来不是为了体积，而是它被<strong>入口和所有异步路由共同依赖</strong>，不拆就会被打进某个大 chunk，别的 chunk 引用它时重复打包。拆出来变成公共依赖，谁加载都命中同一个文件（和同一份缓存）。</p>
<h3 id="3-地图引擎异步加载：本项目最值钱的一刀"><a href="#3-地图引擎异步加载：本项目最值钱的一刀" class="headerlink" title="3. 地图引擎异步加载：本项目最值钱的一刀"></a>3. 地图引擎异步加载：本项目最值钱的一刀</h3><p>maplibre-gl 是全项目最大的单体依赖。三处异步化把它彻底移出首屏关键路径：</p>
<p><strong>CSS 不阻塞首屏</strong>——入口文件里用动态 import，样式变成异步 chunk，浏览器不会因为它阻塞渲染：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// main.ts</span><br><span class="hljs-comment">// maplibre-gl CSS 懒加载，避免阻塞首屏</span><br><span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;maplibre-gl/dist/maplibre-gl.css&#x27;</span>)<br></code></pre></td></tr></table></figure>

<p><strong>JS 在地图容器挂载时才加载</strong>——用模块级变量做单例缓存，加载一次后续复用：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// MapContainer.vue</span><br><span class="hljs-keyword">let</span> <span class="hljs-attr">maplibregl</span>: <span class="hljs-keyword">typeof</span> <span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;maplibre-gl&#x27;</span>).<span class="hljs-property">default</span> | <span class="hljs-literal">null</span> = <span class="hljs-literal">null</span><br><br><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">loadMapLibre</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">if</span> (!maplibregl) &#123;<br>    <span class="hljs-keyword">const</span> mod = <span class="hljs-keyword">await</span> <span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;maplibre-gl&#x27;</span>)   <span class="hljs-comment">// 数百 KB 的引擎，此刻才下载</span><br>    maplibregl = mod.<span class="hljs-property">default</span><br>  &#125;<br>  <span class="hljs-keyword">return</span> maplibregl<br>&#125;<br><br><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">initMap</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">if</span> (!mapRef.<span class="hljs-property">value</span>) <span class="hljs-keyword">return</span><br>  loadingStage.<span class="hljs-property">value</span> = <span class="hljs-string">&#x27;加载地图引擎...&#x27;</span><br>  <span class="hljs-keyword">const</span> maplibre = <span class="hljs-keyword">await</span> <span class="hljs-title function_">loadMapLibre</span>()        <span class="hljs-comment">// ← 关键路径上的唯一大件</span><br>  <span class="hljs-keyword">await</span> <span class="hljs-title function_">ensurePmtilesProtocol</span>(maplibre)       <span class="hljs-comment">// pmtiles 库同样是动态 import</span><br>  <span class="hljs-comment">// ...创建地图实例</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>配套的瓦片协议库也一样</strong>——PMTiles 的 Protocol 也是 <code>await import(&#39;pmtiles&#39;)</code> 按需加载，不用瓦片协议的用户路径不触发。</p>
<p>这一组操作的净效果：首屏 HTML 之后的关键 JS 从「全量 bundle」变成「vue-vendor + 入口 + Home 页」，地图引擎在网络空闲期与地图初始化流程并行下载。用户感知到的顺序不再是「等引擎下载完才开始渲染页面」，而是「页面先出来，地图区域显示加载中，引擎到了地图补位」。</p>
<h3 id="4-面板组件：defineAsyncComponent-按需挂载"><a href="#4-面板组件：defineAsyncComponent-按需挂载" class="headerlink" title="4. 面板组件：defineAsyncComponent 按需挂载"></a>4. 面板组件：defineAsyncComponent 按需挂载</h3><p>大屏首屏由地图容器 + 三个浮层面板（图层、事件、AI 助手）组成。面板不是首屏关键内容，全部异步：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// views/Home/index.vue</span><br><span class="hljs-comment">// 懒加载非首屏关键组件</span><br><span class="hljs-keyword">const</span> <span class="hljs-title class_">LayerPanel</span> = <span class="hljs-title function_">defineAsyncComponent</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;@/components/layout/LayerPanel.vue&#x27;</span>))<br><span class="hljs-keyword">const</span> <span class="hljs-title class_">EventPanel</span> = <span class="hljs-title function_">defineAsyncComponent</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;@/components/layout/EventPanel.vue&#x27;</span>))<br><span class="hljs-keyword">const</span> <span class="hljs-title class_">AIAssistant</span> = <span class="hljs-title function_">defineAsyncComponent</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-keyword">import</span>(<span class="hljs-string">&#x27;@/components/ai/AIAssistant.vue&#x27;</span>))<br></code></pre></td></tr></table></figure>

<p>配合路由懒加载，这是两级异步：<strong>路由级</strong>（进首页才加载 Home）+ <strong>组件级</strong>（Home 渲染后才加载面板）。图层面板里还有整棵图层树的渲染逻辑和搜索过滤，拆出去后 Home 的首帧又轻了一截。</p>
<h3 id="5-图标按需注册"><a href="#5-图标按需注册" class="headerlink" title="5. 图标按需注册"></a>5. 图标按需注册</h3><p>项目没有引 unplugin 自动按需，但图标走的是手动按需：从 <code>@element-plus/icons-vue</code> 具名导入需要的六十来个图标注册为全局组件，未引用的图标靠 tree shaking 摇掉：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">import</span> &#123; <span class="hljs-title class_">Monitor</span>, <span class="hljs-title class_">Camera</span>, <span class="hljs-title class_">FullScreen</span>, <span class="hljs-title class_">Plus</span>, <span class="hljs-comment">/* ... */</span> <span class="hljs-title class_">Grid</span>, <span class="hljs-title class_">List</span> &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;@element-plus/icons-vue&#x27;</span><br><br><span class="hljs-keyword">const</span> <span class="hljs-attr">icons</span>: <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-built_in">any</span>&gt; = &#123; <span class="hljs-title class_">Monitor</span>, <span class="hljs-title class_">Camera</span>, <span class="hljs-title class_">FullScreen</span>, <span class="hljs-title class_">Plus</span>, <span class="hljs-comment">/* ... */</span> &#125;<br><span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> [name, component] <span class="hljs-keyword">of</span> <span class="hljs-title class_">Object</span>.<span class="hljs-title function_">entries</span>(icons)) &#123;<br>  app.<span class="hljs-title function_">component</span>(name, component)<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="二、抢网络：预连接与预取"><a href="#二、抢网络：预连接与预取" class="headerlink" title="二、抢网络：预连接与预取"></a>二、抢网络：预连接与预取</h2><h3 id="preconnect：给底图-CDN-提前握手"><a href="#preconnect：给底图-CDN-提前握手" class="headerlink" title="preconnect：给底图 CDN 提前握手"></a>preconnect：给底图 CDN 提前握手</h3><p>大屏底图样式 JSON 和矢量瓦片全部来自 <code>basemaps.cartocdn.com</code>，首屏必然访问。在 HTML 里提前完成 DNS + TCP + TLS：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-comment">&lt;!-- index.html --&gt;</span><br><span class="hljs-comment">&lt;!-- DNS 预解析 + 预连接，加速外部资源加载 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">link</span> <span class="hljs-attr">rel</span>=<span class="hljs-string">&quot;preconnect&quot;</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;https://basemaps.cartocdn.com&quot;</span> /&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">link</span> <span class="hljs-attr">rel</span>=<span class="hljs-string">&quot;dns-prefetch&quot;</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;https://basemaps.cartocdn.com&quot;</span> /&gt;</span><br></code></pre></td></tr></table></figure>

<p>两行 HTML，省掉地图初始化时的握手串行等待。<code>dns-prefetch</code> 是 <code>preconnect</code> 的降级兜底，给老浏览器一份保险。选这个域名是有依据的——<strong>只 preconnect 首屏确定要访问的域</strong>，乱加反而挤占连接数。</p>
<h3 id="预取底图样式-强缓存"><a href="#预取底图样式-强缓存" class="headerlink" title="预取底图样式 + 强缓存"></a>预取底图样式 + 强缓存</h3><p>切换底图要拉取新的 style JSON，这个项目在地图加载完成后顺手把<strong>所有底图的样式预取</strong>了一遍，且强制走 HTTP 缓存：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// stores/map.ts</span><br><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">preloadStyleJson</span>(<span class="hljs-params"><span class="hljs-attr">styleId</span>: <span class="hljs-built_in">string</span></span>): <span class="hljs-title class_">Promise</span>&lt;<span class="hljs-built_in">object</span> | <span class="hljs-literal">null</span>&gt; &#123;<br>  <span class="hljs-keyword">if</span> (_styleJsonCache.<span class="hljs-title function_">has</span>(styleId)) <span class="hljs-keyword">return</span> _styleJsonCache.<span class="hljs-title function_">get</span>(styleId)!   <span class="hljs-comment">// 内存缓存</span><br>  <span class="hljs-keyword">const</span> target = availableStyles.<span class="hljs-property">value</span>.<span class="hljs-title function_">find</span>(<span class="hljs-function"><span class="hljs-params">s</span> =&gt;</span> s.<span class="hljs-property">id</span> === styleId)<br>  <span class="hljs-keyword">if</span> (target.<span class="hljs-property">type</span> === <span class="hljs-string">&#x27;raster&#x27;</span>) &#123;<br>    <span class="hljs-keyword">const</span> rasterStyle = <span class="hljs-title function_">buildRasterStyle</span>(target)    <span class="hljs-comment">// 栅格底图本地构造，零请求</span><br>    _styleJsonCache.<span class="hljs-title function_">set</span>(styleId, rasterStyle)<br>    <span class="hljs-keyword">return</span> rasterStyle<br>  &#125;<br>  <span class="hljs-keyword">const</span> resp = <span class="hljs-keyword">await</span> <span class="hljs-title function_">fetch</span>(target.<span class="hljs-property">url</span>, &#123; <span class="hljs-attr">cache</span>: <span class="hljs-string">&#x27;force-cache&#x27;</span> &#125;)  <span class="hljs-comment">// 有缓存就不发请求</span><br>  <span class="hljs-keyword">const</span> json = <span class="hljs-keyword">await</span> resp.<span class="hljs-title function_">json</span>()<br>  _styleJsonCache.<span class="hljs-title function_">set</span>(styleId, json)<br>  <span class="hljs-keyword">return</span> json<br>&#125;<br><br><span class="hljs-comment">// 地图 style.load 之后，空闲期预取全部底图</span><br>mapStore.<span class="hljs-title function_">preloadAllStyles</span>()<br></code></pre></td></tr></table></figure>

<p>三层缓存各司其职：内存 Map 管会话内复用，<code>force-cache</code> 管跨会话的 HTTP 缓存，栅格底图直接本地构造 JSON 连请求都不发。用户点切换底图时样式已是本地数据，切换零延迟。</p>
<h2 id="三、首屏数据：并行、防重、缓存"><a href="#三、首屏数据：并行、防重、缓存" class="headerlink" title="三、首屏数据：并行、防重、缓存"></a>三、首屏数据：并行、防重、缓存</h2><p>理论篇强调「别让接口排队」。首页初始化要同时拿图层树、全局样式、叠放顺序三份数据，全部 <code>Promise.all</code> 并行：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// stores/layer.ts</span><br><span class="hljs-keyword">const</span> [treeRes, , globalOrderResult] = <span class="hljs-keyword">await</span> <span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">all</span>([<br>  <span class="hljs-title function_">getLayerTree</span>(),<br>  <span class="hljs-title function_">loadGlobalStyle</span>(),                        <span class="hljs-comment">// 内部还有 sessionStorage 缓存兜底</span><br>  <span class="hljs-title function_">getGlobalOrder</span>().<span class="hljs-title function_">catch</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-literal">null</span>),       <span class="hljs-comment">// 弱依赖：失败不阻塞主流程</span><br>])<br></code></pre></td></tr></table></figure>

<p>两个额外细节：</p>
<ul>
<li><strong>弱依赖降级</strong>：<code>getGlobalOrder().catch(() =&gt; null)</code>——排序接口挂了不该拖死整个图层树加载，拿到 null 就用默认排序；</li>
<li><strong>单例防重</strong>：首页和图层面板的 <code>onMounted</code> 都会触发初始化，用 Promise 缓存挡住第二次请求：</li>
</ul>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">let</span> <span class="hljs-attr">_homeInitPromise</span>: <span class="hljs-title class_">Promise</span>&lt;<span class="hljs-built_in">void</span>&gt; | <span class="hljs-literal">null</span> = <span class="hljs-literal">null</span><br><br><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">initHomeLayerData</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">if</span> (_homeInitPromise) <span class="hljs-keyword">return</span> _homeInitPromise   <span class="hljs-comment">// 已经在跑，直接复用</span><br>  _homeInitPromise = (<span class="hljs-title function_">async</span> () =&gt; &#123;<br>    <span class="hljs-keyword">await</span> <span class="hljs-title function_">loadLayers</span>()<br>    <span class="hljs-keyword">await</span> <span class="hljs-title function_">loadGraphLayers</span>()<br>  &#125;)()<br>  <span class="hljs-keyword">return</span> _homeInitPromise<br>&#125;<br></code></pre></td></tr></table></figure>

<p>全局样式还会写进 sessionStorage——刷新页面时跳过 <code>getGlobalStyle</code> 请求，直接从会话缓存构建样式缓存。首屏接口从 3 个降到 2 个。</p>
<h2 id="四、白屏期：分阶段进度反馈"><a href="#四、白屏期：分阶段进度反馈" class="headerlink" title="四、白屏期：分阶段进度反馈"></a>四、白屏期：分阶段进度反馈</h2><p>项目没有做 index.html 级的骨架屏，但在<strong>地图容器内部</strong>做了分阶段的加载反馈——地图区域不是干等，而是持续告诉用户「现在进行到哪了」：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">const</span> loadingProgress = <span class="hljs-title function_">ref</span>(<span class="hljs-number">0</span>)<br><span class="hljs-keyword">const</span> loadingStage = <span class="hljs-title function_">ref</span>(<span class="hljs-string">&#x27;正在初始化地图引擎...&#x27;</span>)<br><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">startLoadingProgress</span>(<span class="hljs-params"></span>) &#123;<br>  loadingProgress.<span class="hljs-property">value</span> = <span class="hljs-number">5</span><br>  loadingStage.<span class="hljs-property">value</span> = <span class="hljs-string">&#x27;加载地图样式...&#x27;</span><br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">advanceProgress</span>(<span class="hljs-params"><span class="hljs-attr">percent</span>: <span class="hljs-built_in">number</span>, <span class="hljs-attr">stage</span>?: <span class="hljs-built_in">string</span></span>) &#123;<br>    loadingProgress.<span class="hljs-property">value</span> = <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">min</span>(percent, <span class="hljs-number">99</span>)   <span class="hljs-comment">// 封顶 99，完成交给真实事件</span><br>    <span class="hljs-keyword">if</span> (stage) loadingStage.<span class="hljs-property">value</span> = stage<br>  &#125;<br>&#125;<br><br><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">initMap</span>(<span class="hljs-params"></span>) &#123;<br>  loadingStage.<span class="hljs-property">value</span> = <span class="hljs-string">&#x27;加载地图引擎...&#x27;</span><br>  <span class="hljs-keyword">const</span> maplibre = <span class="hljs-keyword">await</span> <span class="hljs-title function_">loadMapLibre</span>()<br>  <span class="hljs-keyword">const</span> advanceProgress = <span class="hljs-title function_">startLoadingProgress</span>()<br>  <span class="hljs-comment">// ... 底图加载中按节点推进：advanceProgress(40, &#x27;加载底图瓦片...&#x27;)</span><br>  <span class="hljs-comment">// style.load 后：loadingStage.value = &#x27;底图加载完成&#x27;</span><br>&#125;<br></code></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">class</span>=<span class="hljs-string">&quot;loading-stage&quot;</span>&gt;</span>&#123;&#123; loadingStage &#125;&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><span class="hljs-comment">&lt;!-- 进度条按 loadingProgress 渲染 --&gt;</span><br></code></pre></td></tr></table></figure>

<p>一个小而讲究的设计：进度<strong>封顶 99%</strong>——异步加载的真实耗时不可预测，进度条冲到 100 却还卡在那儿，比停在 99 更让人焦虑。100% 只能由「地图真正加载完成」这个事件来置位。这是「体感优化」里典型的心理学小把戏，和骨架屏同一族：时间没变，等待变得可解释了。</p>
<p>另外详情弹窗里的图片用了原生懒加载，一行声明式：<img :src="img" loading="lazy" />，非首屏图片不占首屏带宽。</p>
<h2 id="五、诚实的账：理论有、项目还没做的"><a href="#五、诚实的账：理论有、项目还没做的" class="headerlink" title="五、诚实的账：理论有、项目还没做的"></a>五、诚实的账：理论有、项目还没做的</h2><ul>
<li><strong>web-vitals 上报</strong>：没有。目前优化验收靠 Lighthouse 手跑，缺真实用户数据——LCP 在客户内网环境是什么水平，其实是笔糊涂账。这在监控篇补课；</li>
<li><strong>Element Plus 组件按需引入</strong>：<code>app.use(ElementPlus)</code> 是全量引入，全量 CSS 也进了首屏。manualChunks 把它隔离成了独立 chunk（不阻塞关键路径的并行下载），但<strong>总量没减</strong>。按 unplugin-vue-components 按需引入预计还能再砍掉一大块，属于已识别未执行；</li>
<li><strong>服务端 gzip&#x2F;brotli、CDN、缓存头</strong>：前端仓库之外，需要部署侧配合，代码里无从体现；</li>
<li><strong>index.html 骨架屏 &#x2F; 内联关键 CSS &#x2F; SSR</strong>：未做。大屏场景白屏期被「地图加载进度」组件覆盖了大半，且内网部署暂无 SEO 需求，优先级排后——先吃便宜的收益，贵的刀最后动。</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>这张大屏的首屏优化，本质是把理论清单按项目特点<strong>排了优先级</strong>：</p>
<table>
<thead>
<tr>
<th>手段</th>
<th>代码位置</th>
<th>对首屏的意义</th>
</tr>
</thead>
<tbody><tr>
<td>路由全懒加载</td>
<td><code>router/index.ts</code></td>
<td>管理页零字节进首屏</td>
</tr>
<tr>
<td>manualChunks 五路分包</td>
<td><code>vite.config.ts</code></td>
<td>长缓存物理基础 + 公共依赖不重复打包</td>
</tr>
<tr>
<td>地图引擎三件动态 import</td>
<td><code>main.ts</code> &#x2F; <code>MapContainer.vue</code></td>
<td>数百 KB 最大件移出关键路径</td>
</tr>
<tr>
<td>defineAsyncComponent 面板</td>
<td><code>views/Home/index.vue</code></td>
<td>首帧只渲染地图容器</td>
</tr>
<tr>
<td>preconnect 底图 CDN</td>
<td><code>index.html</code></td>
<td>握手与 JS 下载并行</td>
</tr>
<tr>
<td>样式预取 + force-cache</td>
<td><code>stores/map.ts</code></td>
<td>切底图零网络等待</td>
</tr>
<tr>
<td>Promise.all + 单例防重</td>
<td><code>stores/layer.ts</code></td>
<td>接口不排队、不重复</td>
</tr>
<tr>
<td>分阶段加载进度</td>
<td><code>MapContainer.vue</code></td>
<td>白屏期可解释</td>
</tr>
</tbody></table>
<p><strong>一句话记住</strong>：首屏优化落地时别平均用力——先找出项目里<strong>最大的那块依赖</strong>（这里是地图引擎），把它异步化；再用分包和缓存把「不变的」和「常变的」分开；最后让等待变得可解释。理论清单是地图，往哪走要看自己项目的地形。</p>
]]></content>
      <categories>
        <category>前端基础</category>
        <category>性能优化</category>
      </categories>
      <tags>
        <tag>vue3</tag>
        <tag>性能优化</tag>
        <tag>SPA</tag>
        <tag>vite</tag>
      </tags>
  </entry>
  <entry>
    <title>泛型与条件类型实战：写一个类型安全的请求封装</title>
    <url>/2026/09/10/ts-type-safe-request/</url>
    <content><![CDATA[<p>这篇文章只做一件事：<strong>把一个 <code>any</code> 满天飞的请求封装，一步步改造成调用处零手写类型、零类型断言的类型安全封装</strong>。所有「类型体操」都不为炫技——每一步都对应一个真实的工程痛点。</p>
<blockquote>
<p><strong>先说结论</strong>：类型安全的请求封装，核心思路不是「在调用处标注类型」，而是<strong>让 API 定义表成为唯一数据源</strong>——<code>url</code>、<code>method</code>、参数类型、返回类型都写在一张表里，然后用 <code>typeof + keyof + 索引访问</code>取到某个 API 的定义，用<strong>条件类型 + <code>infer</code></strong> 从定义里把参数和返回值「解构」出来，用<strong>条件类型 + 可变元组</strong>表达「有 params 必传、没有就不能传」。调用处一个尖括号都不写，类型全部自动流动，而且<strong>写错参数名、传错类型、忘记写返回类型，都在编译期报错</strong>。</p>
</blockquote>
<span id="more"></span>

<h2 id="一、痛点：一个典型的-any-封装"><a href="#一、痛点：一个典型的-any-封装" class="headerlink" title="一、痛点：一个典型的 any 封装"></a>一、痛点：一个典型的 any 封装</h2><p>几乎所有项目都有这个文件：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// utils/request.ts —— 祖传版本</span><br><span class="hljs-keyword">import</span> axios <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;axios&#x27;</span><br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> get&lt;T = <span class="hljs-built_in">any</span>&gt;(<span class="hljs-attr">url</span>: <span class="hljs-built_in">string</span>, <span class="hljs-attr">params</span>?: <span class="hljs-built_in">any</span>): <span class="hljs-title class_">Promise</span>&lt;T&gt; &#123;<br>  <span class="hljs-keyword">return</span> axios.<span class="hljs-title function_">get</span>(url, &#123; params &#125;).<span class="hljs-title function_">then</span>(<span class="hljs-function">(<span class="hljs-params">res</span>) =&gt;</span> res.<span class="hljs-property">data</span>.<span class="hljs-property">data</span>)<br>&#125;<br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> post&lt;T = <span class="hljs-built_in">any</span>&gt;(<span class="hljs-attr">url</span>: <span class="hljs-built_in">string</span>, <span class="hljs-attr">data</span>?: <span class="hljs-built_in">any</span>): <span class="hljs-title class_">Promise</span>&lt;T&gt; &#123;<br>  <span class="hljs-keyword">return</span> axios.<span class="hljs-title function_">post</span>(url, data).<span class="hljs-title function_">then</span>(<span class="hljs-function">(<span class="hljs-params">res</span>) =&gt;</span> res.<span class="hljs-property">data</span>.<span class="hljs-property">data</span>)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>用它的时候：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// 类型是我「自称」的 —— 运行时到底返回什么，编译器一无所知</span><br><span class="hljs-keyword">const</span> user = <span class="hljs-keyword">await</span> get&lt;<span class="hljs-title class_">User</span>&gt;(<span class="hljs-string">&#x27;/users/1&#x27;</span>)<br><br><span class="hljs-comment">// 这些全部静默通过，编译期毫无反应：</span><br><span class="hljs-keyword">await</span> get&lt;<span class="hljs-title class_">User</span>&gt;(<span class="hljs-string">&#x27;/userss/1&#x27;</span>)            <span class="hljs-comment">// url 拼错，靠肉眼</span><br><span class="hljs-keyword">await</span> get&lt;<span class="hljs-title class_">User</span>&gt;(<span class="hljs-string">&#x27;/users/1&#x27;</span>, &#123; <span class="hljs-attr">id</span>: <span class="hljs-string">&#x27;x&#x27;</span> &#125;) <span class="hljs-comment">// params 是 any，传什么都不报错</span><br><span class="hljs-keyword">const</span> u = <span class="hljs-keyword">await</span> <span class="hljs-title function_">get</span>(<span class="hljs-string">&#x27;/users/1&#x27;</span>)          <span class="hljs-comment">// 忘写泛型 → T = any → user.name 也变成 any</span><br></code></pre></td></tr></table></figure>

<p>问题的根源有三个：</p>
<ol>
<li><strong>类型声明和接口是两份东西</strong>：<code>get&lt;User&gt;</code> 里的 <code>User</code> 是调用时的「口头声明」，与后端接口没有绑定关系，改了后端 TS 不知道；</li>
<li><strong>url、参数、返回值三者无关联</strong>：同一个接口的信息散落在调用处，没有单一数据源；</li>
<li><strong>泛型默认值是 <code>any</code></strong>：忘了写就静默退化，类型安全形同虚设。</li>
</ol>
<p>目标形态先亮出来：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">const</span> user = <span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;getUser&#x27;</span>, &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">1</span> &#125;)   <span class="hljs-comment">// ✅ Promise&lt;User&gt;，自动推导</span><br><span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;logout&#x27;</span>)                            <span class="hljs-comment">// ✅ 没有 params，不传也不许传</span><br><span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;getUser&#x27;</span>, &#123; <span class="hljs-attr">id</span>: <span class="hljs-string">&#x27;1&#x27;</span> &#125;)              <span class="hljs-comment">// ❌ 编译错误：id 应为 number</span><br><span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;getUser&#x27;</span>)                           <span class="hljs-comment">// ❌ 编译错误：缺少参数 &#123; id: number &#125;</span><br><span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;getUserr&#x27;</span>, &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">1</span> &#125;)               <span class="hljs-comment">// ❌ 编译错误：API 名不在表里</span><br></code></pre></td></tr></table></figure>

<p>注意调用处<strong>没有一处尖括号、没有一处 <code>as</code></strong>。下面开始一步步推。</p>
<h2 id="二、十五分钟前置：类型层面的编程语言"><a href="#二、十五分钟前置：类型层面的编程语言" class="headerlink" title="二、十五分钟前置：类型层面的编程语言"></a>二、十五分钟前置：类型层面的编程语言</h2><p>后面要用的类型语法就五组，先各用一个最小例子讲清。已经熟的可以直接跳到第三节，忘了再回来查。</p>
<h3 id="2-1-泛型：类型的「函数参数」"><a href="#2-1-泛型：类型的「函数参数」" class="headerlink" title="2.1 泛型：类型的「函数参数」"></a>2.1 泛型：类型的「函数参数」</h3><p>值有函数（<code>x =&gt; x + 1</code>），类型也有「函数」——泛型：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> identity&lt;T&gt;(<span class="hljs-attr">x</span>: T): T &#123; <span class="hljs-keyword">return</span> x &#125;<br><span class="hljs-comment">// identity&lt;number&gt;(1) —— T 是「传入的类型实参」</span><br><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">Box</span>&lt;T&gt; = &#123; <span class="hljs-attr">value</span>: T &#125;<br><span class="hljs-keyword">type</span> <span class="hljs-title class_">NumBox</span> = <span class="hljs-title class_">Box</span>&lt;<span class="hljs-built_in">number</span>&gt; <span class="hljs-comment">// &#123; value: number &#125;</span><br></code></pre></td></tr></table></figure>

<p><strong>泛型的本质是类型的参数化</strong>：把类型当作输入，输出新的类型&#x2F;函数签名。</p>
<h3 id="2-2-约束、keyof-与索引访问"><a href="#2-2-约束、keyof-与索引访问" class="headerlink" title="2.2 约束、keyof 与索引访问"></a>2.2 约束、keyof 与索引访问</h3><p><code>extends</code> 给泛型加约束——「T 至少得是什么」：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// K extends keyof T：K 只能是 T 的属性名之一，且与 obj 的键关联</span><br><span class="hljs-keyword">function</span> pick&lt;T, K <span class="hljs-keyword">extends</span> keyof T&gt;(<span class="hljs-attr">obj</span>: T, <span class="hljs-attr">key</span>: K): T[K] &#123;<br>  <span class="hljs-keyword">return</span> obj[key]<br>&#125;<br><br><span class="hljs-title function_">pick</span>(&#123; <span class="hljs-attr">id</span>: <span class="hljs-number">1</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;k&#x27;</span> &#125;, <span class="hljs-string">&#x27;name&#x27;</span>) <span class="hljs-comment">// ✅ 返回 string</span><br><span class="hljs-title function_">pick</span>(&#123; <span class="hljs-attr">id</span>: <span class="hljs-number">1</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;k&#x27;</span> &#125;, <span class="hljs-string">&#x27;age&#x27;</span>)  <span class="hljs-comment">// ❌ &#x27;age&#x27; 不在 keyof 范围内</span><br></code></pre></td></tr></table></figure>

<p>三个关键工具：<code>keyof T</code> 取属性名<strong>联合</strong>（<code>&#39;id&#39; | &#39;name&#39;</code>）；<code>T[K]</code> 是<strong>索引访问类型</strong>，取属性的类型；<code>K extends keyof T</code> 把两个参数的类型关联起来——<strong>关联，是类型安全的全部秘密</strong>。</p>
<h3 id="2-3-条件类型与-infer：类型层面的三元与模式匹配"><a href="#2-3-条件类型与-infer：类型层面的三元与模式匹配" class="headerlink" title="2.3 条件类型与 infer：类型层面的三元与模式匹配"></a>2.3 条件类型与 infer：类型层面的三元与模式匹配</h3><figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// 条件类型：类型层面的 A ? B : C</span><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">IsString</span>&lt;T&gt; = T <span class="hljs-keyword">extends</span> <span class="hljs-built_in">string</span> ? <span class="hljs-literal">true</span> : <span class="hljs-literal">false</span><br><span class="hljs-keyword">type</span> A = <span class="hljs-title class_">IsString</span>&lt;<span class="hljs-string">&#x27;hi&#x27;</span>&gt;  <span class="hljs-comment">// true</span><br><span class="hljs-keyword">type</span> B = <span class="hljs-title class_">IsString</span>&lt;<span class="hljs-number">42</span>&gt;    <span class="hljs-comment">// false</span><br><br><span class="hljs-comment">// infer：在「匹配」的同时声明一个待推断的类型变量</span><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">ElementOf</span>&lt;T&gt; = T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Array</span>&lt;infer E&gt; ? E : <span class="hljs-built_in">never</span><br><span class="hljs-keyword">type</span> C = <span class="hljs-title class_">ElementOf</span>&lt;<span class="hljs-built_in">string</span>[]&gt;   <span class="hljs-comment">// string</span><br><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">Unwrap</span>&lt;T&gt; = T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Promise</span>&lt;infer V&gt; ? V : T<br><span class="hljs-keyword">type</span> D = <span class="hljs-title class_">Unwrap</span>&lt;<span class="hljs-title class_">Promise</span>&lt;<span class="hljs-title class_">User</span>&gt;&gt; <span class="hljs-comment">// User</span><br></code></pre></td></tr></table></figure>

<p><code>infer E</code> 的含义：<strong>如果 T 能看成 <code>Array&lt;某个类型&gt;</code>，就把那个类型捕获为 E</strong>。这就是类型层面的「解构赋值」，是本文的主角。</p>
<h3 id="2-4-分布式条件类型"><a href="#2-4-分布式条件类型" class="headerlink" title="2.4 分布式条件类型"></a>2.4 分布式条件类型</h3><p>最后一个容易被忽略的规则：<strong>当条件类型的判断对象是「裸类型参数」且传入联合类型时，会把联合拆开逐个判断，再合并结果</strong>：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">type</span> <span class="hljs-title class_">ToArray</span>&lt;T&gt; = T <span class="hljs-keyword">extends</span> <span class="hljs-built_in">unknown</span> ? T[] : <span class="hljs-built_in">never</span><br><span class="hljs-keyword">type</span> R = <span class="hljs-title class_">ToArray</span>&lt;<span class="hljs-string">&#x27;a&#x27;</span> | <span class="hljs-number">1</span>&gt; <span class="hljs-comment">// &#x27;a&#x27;[] | 1[]，不是 (&#x27;a&#x27; | 1)[]</span><br><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">IsNever</span>&lt;T&gt; = T <span class="hljs-keyword">extends</span> <span class="hljs-built_in">never</span> ? <span class="hljs-literal">true</span> : <span class="hljs-literal">false</span><br><span class="hljs-keyword">type</span> X = <span class="hljs-title class_">IsNever</span>&lt;<span class="hljs-built_in">never</span>&gt; <span class="hljs-comment">// never！空联合分配后还是空 —— 所以判断 never 要用 [T] extends [never]</span><br></code></pre></td></tr></table></figure>

<p>想阻止分配，把两边都包上方括号：<code>[T] extends [U] ? X : Y</code>。第三节先记住结论，第九节讲它在我们封装里的实际影响。</p>
<h2 id="三、v1：泛型让返回值有类型——但类型写了两遍"><a href="#三、v1：泛型让返回值有类型——但类型写了两遍" class="headerlink" title="三、v1：泛型让返回值有类型——但类型写了两遍"></a>三、v1：泛型让返回值有类型——但类型写了两遍</h2><p>第一步改造，起码让返回值不要 <code>any</code>：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">interface</span> <span class="hljs-title class_">ApiEnvelope</span>&lt;T = <span class="hljs-built_in">unknown</span>&gt; &#123;<br>  <span class="hljs-attr">code</span>: <span class="hljs-built_in">number</span><br>  <span class="hljs-attr">message</span>: <span class="hljs-built_in">string</span><br>  <span class="hljs-attr">data</span>: T<br>&#125;<br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> request&lt;T&gt;(<br>  <span class="hljs-attr">url</span>: <span class="hljs-built_in">string</span>,<br>  <span class="hljs-attr">config</span>?: &#123; <span class="hljs-attr">params</span>?: <span class="hljs-built_in">object</span>; <span class="hljs-attr">data</span>?: <span class="hljs-built_in">object</span> &#125;,<br>): <span class="hljs-title class_">Promise</span>&lt;T&gt; &#123;<br>  <span class="hljs-keyword">const</span> res = <span class="hljs-keyword">await</span> axios.<span class="hljs-property">post</span>&lt;<span class="hljs-title class_">ApiEnvelope</span>&lt;T&gt;&gt;(url, config?.<span class="hljs-property">data</span> ?? config?.<span class="hljs-property">params</span>)<br>  <span class="hljs-keyword">if</span> (res.<span class="hljs-property">data</span>.<span class="hljs-property">code</span> !== <span class="hljs-number">0</span>) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(res.<span class="hljs-property">data</span>.<span class="hljs-property">message</span>)<br>  <span class="hljs-keyword">return</span> res.<span class="hljs-property">data</span>.<span class="hljs-property">data</span><br>&#125;<br><br><span class="hljs-keyword">const</span> user = <span class="hljs-keyword">await</span> request&lt;<span class="hljs-title class_">User</span>&gt;(<span class="hljs-string">&#x27;/users/1&#x27;</span>) <span class="hljs-comment">// 有类型了，但 User 和 &#x27;/users/1&#x27; 仍是两张皮</span><br></code></pre></td></tr></table></figure>

<p>进步有限：<code>User</code> 仍然是调用处的口头声明，url 仍然是裸字符串。<strong>类型没有和「接口定义」绑定，一切标注都可以骗人</strong>。真正的问题浮出水面：需要一个地方，把 url、参数、返回值<strong>写在一起</strong>。</p>
<h2 id="四、v2：API-定义表——唯一数据源"><a href="#四、v2：API-定义表——唯一数据源" class="headerlink" title="四、v2：API 定义表——唯一数据源"></a>四、v2：API 定义表——唯一数据源</h2><p>把所有接口收进一张表，用「值」承载「类型」：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">interface</span> <span class="hljs-title class_">User</span> &#123;<br>  <span class="hljs-attr">id</span>: <span class="hljs-built_in">number</span><br>  <span class="hljs-attr">name</span>: <span class="hljs-built_in">string</span><br>&#125;<br><br><span class="hljs-keyword">interface</span> <span class="hljs-title class_">ApiDef</span> &#123;<br>  <span class="hljs-attr">url</span>: <span class="hljs-built_in">string</span><br>  <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;GET&#x27;</span> | <span class="hljs-string">&#x27;POST&#x27;</span> | <span class="hljs-string">&#x27;PUT&#x27;</span> | <span class="hljs-string">&#x27;DELETE&#x27;</span><br>  <span class="hljs-attr">params</span>?: <span class="hljs-built_in">object</span>  <span class="hljs-comment">// query / 路径参数的类型占位</span><br>  <span class="hljs-attr">data</span>?: <span class="hljs-built_in">object</span>    <span class="hljs-comment">// 请求体的类型占位</span><br>  <span class="hljs-attr">resp</span>?: <span class="hljs-built_in">unknown</span>   <span class="hljs-comment">// 返回数据的类型占位</span><br>&#125;<br><br><span class="hljs-keyword">const</span> api = &#123;<br>  <span class="hljs-attr">getUser</span>: &#123;<br>    <span class="hljs-attr">url</span>: <span class="hljs-string">&#x27;/users/:id&#x27;</span>,<br>    <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;GET&#x27;</span>,<br>    <span class="hljs-attr">params</span>: &#123;&#125; <span class="hljs-keyword">as</span> &#123; <span class="hljs-attr">id</span>: <span class="hljs-built_in">number</span> &#125;, <span class="hljs-comment">// 值是空对象，类型是标注 —— 纯粹的「类型占位符」</span><br>    <span class="hljs-attr">resp</span>: &#123;&#125; <span class="hljs-keyword">as</span> <span class="hljs-title class_">User</span>,<br>  &#125;,<br>  <span class="hljs-attr">updateUser</span>: &#123;<br>    <span class="hljs-attr">url</span>: <span class="hljs-string">&#x27;/users&#x27;</span>,<br>    <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;PUT&#x27;</span>,<br>    <span class="hljs-attr">data</span>: &#123;&#125; <span class="hljs-keyword">as</span> &#123; <span class="hljs-attr">name</span>: <span class="hljs-built_in">string</span> &#125;,<br>    <span class="hljs-attr">resp</span>: &#123;&#125; <span class="hljs-keyword">as</span> <span class="hljs-title class_">User</span>,<br>  &#125;,<br>  <span class="hljs-attr">login</span>: &#123;<br>    <span class="hljs-attr">url</span>: <span class="hljs-string">&#x27;/login&#x27;</span>,<br>    <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;POST&#x27;</span>,<br>    <span class="hljs-attr">data</span>: &#123;&#125; <span class="hljs-keyword">as</span> &#123; <span class="hljs-attr">username</span>: <span class="hljs-built_in">string</span>; <span class="hljs-attr">password</span>: <span class="hljs-built_in">string</span> &#125;,<br>    <span class="hljs-attr">resp</span>: &#123;&#125; <span class="hljs-keyword">as</span> &#123; <span class="hljs-attr">token</span>: <span class="hljs-built_in">string</span> &#125;,<br>  &#125;,<br>  <span class="hljs-attr">logout</span>: &#123;<br>    <span class="hljs-attr">url</span>: <span class="hljs-string">&#x27;/logout&#x27;</span>,<br>    <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;POST&#x27;</span>,<br>    <span class="hljs-comment">// 没写 params/data/resp —— 没有参数、没有返回值</span><br>  &#125;,<br>&#125; <span class="hljs-keyword">satisfies</span> <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-title class_">ApiDef</span>&gt; <span class="hljs-comment">// ⚠️ 用 satisfies，不要用冒号标注，原因见下</span><br></code></pre></td></tr></table></figure>

<p>三个关键点：</p>
<p><strong>1. <code>{} as { id: number }</code> 是类型占位符。</strong> 值永远是空对象（运行时无意义），类型才是我们想要的。它是把「类型」存进「值世界」的容器——后面用 <code>infer</code> 再把类型取出来。</p>
<p><strong>2. <code>satisfies</code> 而不是 <code>: Record&lt;string, ApiDef&gt;</code>。</strong> 如果写冒号标注，<code>keyof typeof api</code> 会塌缩成 <code>string</code>（因为 <code>Record&lt;string, ...&gt;</code> 的键是任意 string），后面所有按 API 名的索引全部失去约束；而 <code>satisfies</code> 只<strong>校验</strong>形状（每个条目确实是 ApiDef），<strong>不改变推导出的字面量类型</strong>——<code>keyof typeof api</code> 精确地是 <code>&#39;getUser&#39; | &#39;updateUser&#39; | &#39;login&#39; | &#39;logout&#39;</code>。这是 TS 4.9 引入该操作符最典型的使用场景。</p>
<p><strong>3. 可选属性 + 缺席的键，是后面条件类型分支的依据。</strong> <code>logout</code> 没写 <code>params</code>，就是「无参数」的表达。</p>
<p>现在，「getUser 长什么样」这个问题有了唯一答案：<code>(typeof api)[&#39;getUser&#39;]</code>。<strong>类型层面</strong>的索引访问：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">type</span> <span class="hljs-title class_">ApiOf</span>&lt;K <span class="hljs-keyword">extends</span> keyof <span class="hljs-keyword">typeof</span> api&gt; = (<span class="hljs-keyword">typeof</span> api)[K]<br><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">GetUserDef</span> = <span class="hljs-title class_">ApiOf</span>&lt;<span class="hljs-string">&#x27;getUser&#x27;</span>&gt;<br><span class="hljs-comment">// &#123; url: &#x27;/users/:id&#x27;; method: &#x27;GET&#x27;; params: &#123; id: number &#125;; resp: User &#125;</span><br></code></pre></td></tr></table></figure>

<h2 id="五、v3：条件类型-infer，把-params-resp「解构」出来"><a href="#五、v3：条件类型-infer，把-params-resp「解构」出来" class="headerlink" title="五、v3：条件类型 + infer，把 params &#x2F; resp「解构」出来"></a>五、v3：条件类型 + infer，把 params &#x2F; resp「解构」出来</h2><p>定义表有了，接下来写三个工具类型，从定义里提取参数和返回值——<code>infer</code> 的主战场：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// 定义里有 resp 键 → 提取它的类型；没有 → void（表示「没有返回值」）</span><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">Resp</span>&lt;T&gt; = T <span class="hljs-keyword">extends</span> &#123; <span class="hljs-attr">resp</span>: infer R &#125; ? R : <span class="hljs-built_in">void</span><br><br><span class="hljs-comment">// 同理提取 params 与 data</span><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">Param</span>&lt;T&gt; = T <span class="hljs-keyword">extends</span> &#123; <span class="hljs-attr">params</span>: infer P &#125; ? P : <span class="hljs-literal">undefined</span><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">Body</span>&lt;T&gt; = T <span class="hljs-keyword">extends</span> &#123; <span class="hljs-attr">data</span>: infer D &#125; ? D : <span class="hljs-literal">undefined</span><br><br><span class="hljs-keyword">type</span> <span class="hljs-variable constant_">R1</span> = <span class="hljs-title class_">Resp</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;<span class="hljs-string">&#x27;getUser&#x27;</span>&gt;&gt; <span class="hljs-comment">// User ✅</span><br><span class="hljs-keyword">type</span> <span class="hljs-variable constant_">R2</span> = <span class="hljs-title class_">Resp</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;<span class="hljs-string">&#x27;logout&#x27;</span>&gt;&gt;  <span class="hljs-comment">// void ✅</span><br><span class="hljs-keyword">type</span> <span class="hljs-variable constant_">R3</span> = <span class="hljs-title class_">Param</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;<span class="hljs-string">&#x27;getUser&#x27;</span>&gt;&gt; <span class="hljs-comment">// &#123; id: number &#125; ✅</span><br></code></pre></td></tr></table></figure>

<p>逐行看 <code>Resp</code> 在 <code>&#39;getUser&#39;</code> 上的求值过程：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Resp&lt;&#123; url: &#x27;/users/:id&#x27;; method: &#x27;GET&#x27;; params: &#123; id: number &#125;; resp: User &#125;&gt;<br>  → &#123; url: ...; resp: User &#125; extends &#123; resp: infer R &#125; ? R : void<br>  → 匹配成功，R 捕获为 User<br>  → User<br></code></pre></td></tr></table></figure>

<p>为什么 <code>logout</code> 走 <code>void</code> 分支？因为 <code>{ url: &#39;/logout&#39;; method: &#39;POST&#39; }</code> <strong>不能</strong>赋给 <code>{ resp: 任意 }</code>——缺键即不匹配。<strong>「键的缺席」就这样变成了「类型的分支」</strong>，这就是 API 表里「没写就是没有」的类型学解释。</p>
<p>先写一个能用的版本（参数统一可选，下一节再收紧）：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> request&lt;K <span class="hljs-keyword">extends</span> keyof <span class="hljs-keyword">typeof</span> api&gt;(<br>  <span class="hljs-attr">name</span>: K,<br>  <span class="hljs-attr">payload</span>?: <span class="hljs-title class_">Param</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;K&gt;&gt; &amp; <span class="hljs-title class_">Body</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;K&gt;&gt;,<br>): <span class="hljs-title class_">Promise</span>&lt;<span class="hljs-title class_">Resp</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;K&gt;&gt;&gt; &#123;<br>  <span class="hljs-keyword">const</span> def = api[name]<br>  <span class="hljs-keyword">const</span> res = <span class="hljs-keyword">await</span> axios.<span class="hljs-property">post</span>&lt;<span class="hljs-title class_">ApiEnvelope</span>&lt;<span class="hljs-title class_">Resp</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;K&gt;&gt;&gt;&gt;(def.<span class="hljs-property">url</span>, payload)<br>  <span class="hljs-keyword">if</span> (res.<span class="hljs-property">data</span>.<span class="hljs-property">code</span> !== <span class="hljs-number">0</span>) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ApiError</span>(res.<span class="hljs-property">data</span>.<span class="hljs-property">code</span>, res.<span class="hljs-property">data</span>.<span class="hljs-property">message</span>)<br>  <span class="hljs-keyword">return</span> res.<span class="hljs-property">data</span>.<span class="hljs-property">data</span><br>&#125;<br><br><span class="hljs-keyword">const</span> user = <span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;getUser&#x27;</span>, &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">1</span> &#125;) <span class="hljs-comment">// ✅ Promise&lt;User&gt;，无尖括号</span><br></code></pre></td></tr></table></figure>

<p>返回值类型已经完全自动了。但还差一口气：<code>request(&#39;logout&#39;, { 任意垃圾 })</code> 现在也能过——参数是可选的 <code>undefined &amp; {...}</code>，约束太松。</p>
<h2 id="六、v4：条件类型-可变元组——「有参数必传，没参数不许传」"><a href="#六、v4：条件类型-可变元组——「有参数必传，没参数不许传」" class="headerlink" title="六、v4：条件类型 + 可变元组——「有参数必传，没参数不许传」"></a>六、v4：条件类型 + 可变元组——「有参数必传，没参数不许传」</h2><p>目标：<code>getUser</code> 必须传 <code>{ id: number }</code>；<code>logout</code> 一个参数都不许传。用<strong>可变元组 + 条件类型</strong>把「要不要参数」变成类型的一部分：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// 「参数列表」本身是一个条件类型：</span><br><span class="hljs-comment">// 定义里有 params → 参数列表是 [P]；否则是空元组 []</span><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">Args</span>&lt;T&gt; =<br>  T <span class="hljs-keyword">extends</span> &#123; <span class="hljs-attr">params</span>: infer P &#125; ? [<span class="hljs-attr">payload</span>: P] :<br>  T <span class="hljs-keyword">extends</span> &#123; <span class="hljs-attr">data</span>: infer D &#125;   ? [<span class="hljs-attr">payload</span>: D] :<br>  []<br><br><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> request&lt;K <span class="hljs-keyword">extends</span> keyof <span class="hljs-keyword">typeof</span> api&gt;(<br>  <span class="hljs-attr">name</span>: K,<br>  ...<span class="hljs-attr">args</span>: <span class="hljs-title class_">Args</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;K&gt;&gt;            <span class="hljs-comment">// ⭐ 剩余参数的类型由 API 定义决定</span><br>): <span class="hljs-title class_">Promise</span>&lt;<span class="hljs-title class_">Resp</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;K&gt;&gt;&gt; &#123;<br>  <span class="hljs-keyword">const</span> def = api[name]<br>  <span class="hljs-keyword">const</span> payload = args[<span class="hljs-number">0</span>] <span class="hljs-keyword">as</span> <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-built_in">unknown</span>&gt; | <span class="hljs-literal">undefined</span> <span class="hljs-comment">// 实现细节，见第七节</span><br>  <span class="hljs-keyword">const</span> url = <span class="hljs-title function_">fillPath</span>(def.<span class="hljs-property">url</span>, payload)<br>  <span class="hljs-keyword">const</span> res = <span class="hljs-keyword">await</span> axios.<span class="hljs-property">post</span>&lt;<span class="hljs-title class_">ApiEnvelope</span>&lt;<span class="hljs-title class_">Resp</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;K&gt;&gt;&gt;&gt;(url, payload)<br>  <span class="hljs-keyword">if</span> (res.<span class="hljs-property">data</span>.<span class="hljs-property">code</span> !== <span class="hljs-number">0</span>) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ApiError</span>(res.<span class="hljs-property">data</span>.<span class="hljs-property">code</span>, res.<span class="hljs-property">data</span>.<span class="hljs-property">message</span>)<br>  <span class="hljs-keyword">return</span> res.<span class="hljs-property">data</span>.<span class="hljs-property">data</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>调用处效果：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">const</span> user = <span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;getUser&#x27;</span>, &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">1</span> &#125;)    <span class="hljs-comment">// ✅ Promise&lt;User&gt;</span><br><span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;getUser&#x27;</span>)                             <span class="hljs-comment">// ❌ 缺少参数 payload: &#123; id: number &#125;</span><br><span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;getUser&#x27;</span>, &#123; <span class="hljs-attr">id</span>: <span class="hljs-string">&#x27;1&#x27;</span> &#125;)                <span class="hljs-comment">// ❌ id 应为 number</span><br><span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;getUser&#x27;</span>, &#123; <span class="hljs-attr">userId</span>: <span class="hljs-number">1</span> &#125;)              <span class="hljs-comment">// ❌ 对象字面量只能声明已知属性</span><br><span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;logout&#x27;</span>)                              <span class="hljs-comment">// ✅ 参数列表是 []，不传</span><br><span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;logout&#x27;</span>, &#123;&#125;)                          <span class="hljs-comment">// ❌ Expected 1 arguments, but got 2（只剩 API 名这一个参数位）</span><br></code></pre></td></tr></table></figure>

<p>原理：TS 4.0 的可变元组允许 <code>...args: SomeTupleType</code>，而<strong>元组类型可以是条件类型的求值结果</strong>。当 K 被推断为 <code>&#39;getUser&#39;</code> 时，<code>Args&lt;ApiOf&lt;&#39;getUser&#39;&gt;&gt;</code> 求值为 <code>[payload: { id: number }]</code>，函数签名「长出」一个必选参数；K 是 <code>&#39;logout&#39;</code> 时求值为 <code>[]</code>，签名就是零参数函数。<strong>同一个函数，按第一个参数（API 名）的不同，呈现不同的参数列表</strong>——这就是重载的「类型级实现」，而且是自动维护的：往表里加一个 API，它就自动获得正确的签名，不需要手写 overload。</p>
<blockquote>
<p>约定：一张表里的条目要么走 <code>params</code>（GET 类），要么走 <code>data</code>（POST&#x2F;PUT 类），不混用；真要同时用，把 <code>Args</code> 第一行换成 <code>[params: P, body: D]</code> 的双元素元组即可。</p>
</blockquote>
<h2 id="七、进阶：模板字面量类型，提取-REST-路径参数"><a href="#七、进阶：模板字面量类型，提取-REST-路径参数" class="headerlink" title="七、进阶：模板字面量类型，提取 REST 路径参数"></a>七、进阶：模板字面量类型，提取 REST 路径参数</h2><p>表里 <code>url: &#39;/users/:id&#39;</code> 还有一个没兑现的类型承诺——<code>:id</code> 应该被替换。先用<strong>模板字面量类型 + 递归条件类型</strong>把路径变量名提取成联合：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// 递归扫描字符串：`$&#123;前缀&#125;:$&#123;变量&#125;/$&#123;剩余&#125;` → 拆出变量继续扫右边</span><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">PathVars</span>&lt;S <span class="hljs-keyword">extends</span> <span class="hljs-built_in">string</span>&gt; =<br>  S <span class="hljs-keyword">extends</span> <span class="hljs-string">`<span class="hljs-subst">$&#123;<span class="hljs-built_in">string</span>&#125;</span>:<span class="hljs-subst">$&#123;infer V&#125;</span>/<span class="hljs-subst">$&#123;infer Rest&#125;</span>`</span><br>    ? V | <span class="hljs-title class_">PathVars</span>&lt;<span class="hljs-title class_">Rest</span>&gt;<br>    : S <span class="hljs-keyword">extends</span> <span class="hljs-string">`<span class="hljs-subst">$&#123;<span class="hljs-built_in">string</span>&#125;</span>:<span class="hljs-subst">$&#123;infer V&#125;</span>`</span><br>      ? V<br>      : <span class="hljs-built_in">never</span><br><br><span class="hljs-keyword">type</span> <span class="hljs-variable constant_">P1</span> = <span class="hljs-title class_">PathVars</span>&lt;<span class="hljs-string">&#x27;/users/:id&#x27;</span>&gt;          <span class="hljs-comment">// &#x27;id&#x27;</span><br><span class="hljs-keyword">type</span> <span class="hljs-variable constant_">P2</span> = <span class="hljs-title class_">PathVars</span>&lt;<span class="hljs-string">&#x27;/users/:id/posts/:pid&#x27;</span>&gt; <span class="hljs-comment">// &#x27;id&#x27; | &#x27;pid&#x27;</span><br><span class="hljs-keyword">type</span> <span class="hljs-variable constant_">P3</span> = <span class="hljs-title class_">PathVars</span>&lt;<span class="hljs-string">&#x27;/users&#x27;</span>&gt;               <span class="hljs-comment">// never —— 没有路径变量</span><br></code></pre></td></tr></table></figure>

<p>用它能给「填路径」函数做精确签名——<strong>参数的形状由路径字符串决定</strong>：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> fillPath&lt;S <span class="hljs-keyword">extends</span> <span class="hljs-built_in">string</span>&gt;(<br>  <span class="hljs-attr">path</span>: S,<br>  <span class="hljs-attr">vars</span>: <span class="hljs-title class_">Record</span>&lt;<span class="hljs-title class_">PathVars</span>&lt;S&gt;, <span class="hljs-built_in">string</span>&gt;,<br>): <span class="hljs-built_in">string</span> &#123;<br>  <span class="hljs-keyword">return</span> path.<span class="hljs-title function_">replace</span>(<span class="hljs-regexp">/:(\w+)/g</span>, <span class="hljs-function">(<span class="hljs-params">_, <span class="hljs-attr">key</span>: <span class="hljs-built_in">string</span></span>) =&gt;</span> &#123;<br>    <span class="hljs-keyword">const</span> v = (vars <span class="hljs-keyword">as</span> <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-built_in">string</span> | <span class="hljs-literal">undefined</span>&gt;)[key]<br>    <span class="hljs-keyword">if</span> (v === <span class="hljs-literal">undefined</span>) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">`[request] 缺少路径参数: <span class="hljs-subst">$&#123;key&#125;</span>`</span>)<br>    <span class="hljs-keyword">return</span> <span class="hljs-built_in">encodeURIComponent</span>(v)<br>  &#125;)<br>&#125;<br><br><span class="hljs-title function_">fillPath</span>(<span class="hljs-string">&#x27;/users/:id&#x27;</span>, &#123; <span class="hljs-attr">id</span>: <span class="hljs-string">&#x27;1&#x27;</span> &#125;)  <span class="hljs-comment">// ✅</span><br><span class="hljs-title function_">fillPath</span>(<span class="hljs-string">&#x27;/users/:id&#x27;</span>, &#123;&#125;)           <span class="hljs-comment">// ❌ 缺少 id</span><br><span class="hljs-title function_">fillPath</span>(<span class="hljs-string">&#x27;/users/:id&#x27;</span>, &#123; <span class="hljs-attr">foo</span>: <span class="hljs-string">&#x27;1&#x27;</span> &#125;) <span class="hljs-comment">// ❌ 多余属性</span><br><span class="hljs-title function_">fillPath</span>(<span class="hljs-string">&#x27;/users&#x27;</span>, &#123; <span class="hljs-attr">id</span>: <span class="hljs-string">&#x27;1&#x27;</span> &#125;)      <span class="hljs-comment">// ✅ 通过（见下）</span><br></code></pre></td></tr></table></figure>

<p>一个实测过的边界：<code>Record&lt;never, string&gt;</code> 会求值为 <code>{}</code>——全局最宽的对象类型，<strong>连多余属性检查都不会触发</strong>。所以「没有路径变量的接口传了 vars」拦不住，只拦得住反方向（有变量没传、传错名）。这是模板字面量守卫的真实局限：类型约束做不到的地方，靠 <code>fillPath</code> 实现里那行 <code>v === undefined</code> 的运行时检查兜底——<strong>编译期安全网和运行时兜底是互补关系，不是替代关系</strong>。</p>
<p>在 <code>request</code> 实现里调用它时，<code>payload</code> 是 <code>{ id: number }</code> 而 <code>fillPath</code> 要 <code>Record&lt;&#39;id&#39;, string&gt;</code>，实现处用了一个 <code>as</code> 桥接（顺手 <code>String(v)</code>）。这里要坦诚一个工程事实：<strong>类型安全的封装内部一定有几处断言，价值在于把不安全关进笼子——边界处（调用方）绝对安全，笼子里（实现方）集中审查</strong>。这与运行时校验（zod &#x2F; valibot）互补：TS 管编译期，schema 管网络另一端的不确定性。</p>
<h2 id="八、失败路径：让错误也变成类型的一部分"><a href="#八、失败路径：让错误也变成类型的一部分" class="headerlink" title="八、失败路径：让错误也变成类型的一部分"></a>八、失败路径：让错误也变成类型的一部分</h2><p>成功路径安全了，失败路径还是 <code>throw new Error(message)</code>——调用方只能拿到 <code>string</code>。给错误建类型：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">class</span> <span class="hljs-title class_">ApiError</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_ inherited__">Error</span> &#123;<br>  <span class="hljs-keyword">readonly</span> <span class="hljs-attr">code</span>: <span class="hljs-built_in">number</span><br>  <span class="hljs-title function_">constructor</span>(<span class="hljs-params"><span class="hljs-attr">code</span>: <span class="hljs-built_in">number</span>, <span class="hljs-attr">message</span>: <span class="hljs-built_in">string</span></span>) &#123;<br>    <span class="hljs-variable language_">super</span>(message)<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">name</span> = <span class="hljs-string">&#x27;ApiError&#x27;</span><br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">code</span> = code<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>调用方用 <code>instanceof</code> 收窄：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">try</span> &#123;<br>  <span class="hljs-keyword">const</span> user = <span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;getUser&#x27;</span>, &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">1</span> &#125;)<br>&#125; <span class="hljs-keyword">catch</span> (e) &#123;<br>  <span class="hljs-keyword">if</span> (e <span class="hljs-keyword">instanceof</span> <span class="hljs-title class_">ApiError</span>) &#123;<br>    e.<span class="hljs-property">code</span>       <span class="hljs-comment">// number —— 401 跳登录、429 提示稍后再试</span><br>    <span class="hljs-keyword">if</span> (e.<span class="hljs-property">code</span> === <span class="hljs-number">401</span>) router.<span class="hljs-title function_">push</span>(<span class="hljs-string">&#x27;/login&#x27;</span>)<br>  &#125;<br>  <span class="hljs-comment">// 不是 ApiError 的（网络层、取消请求），交给全局兜底</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>另外两个与 <code>never</code> &#x2F; <code>void</code> 相关的取舍：</p>
<ul>
<li><strong><code>Resp</code> 的兜底用 <code>void</code> 而不是 <code>any</code>&#x2F;<code>unknown</code></strong>：表里忘写 <code>resp</code> 时，返回 <code>Promise&lt;void&gt;</code>，调用方一旦取值立刻编译报错——错误被「放大」而不是被吞掉。用 <code>any</code> 则静默通过，等于给忘写返回类型开绿灯。</li>
<li><strong>穷尽检查</strong>：如果后续按 <code>method</code> 分发（GET 拼 query、POST 走 body），在 switch 里用 <code>never</code> 兜底，新增 method 常量时漏分支会直接编译失败：</li>
</ul>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> <span class="hljs-title function_">send</span>(<span class="hljs-params"><span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;GET&#x27;</span> | <span class="hljs-string">&#x27;POST&#x27;</span> | <span class="hljs-string">&#x27;PUT&#x27;</span> | <span class="hljs-string">&#x27;DELETE&#x27;</span></span>) &#123;<br>  <span class="hljs-keyword">switch</span> (method) &#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;GET&#x27;</span>: <span class="hljs-keyword">return</span> <span class="hljs-comment">/* ... */</span><br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;POST&#x27;</span>: <span class="hljs-keyword">return</span> <span class="hljs-comment">/* ... */</span><br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;PUT&#x27;</span>: <span class="hljs-keyword">return</span> <span class="hljs-comment">/* ... */</span><br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;DELETE&#x27;</span>: <span class="hljs-keyword">return</span> <span class="hljs-comment">/* ... */</span><br>    <span class="hljs-attr">default</span>: &#123;<br>      <span class="hljs-keyword">const</span> <span class="hljs-attr">_exhaustive</span>: <span class="hljs-built_in">never</span> = method <span class="hljs-comment">// 漏掉任何一个分支，这里编译报错</span><br>      <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">&#x27;unreachable&#x27;</span>)<br>    &#125;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h2 id="九、三个最容易踩的坑"><a href="#九、三个最容易踩的坑" class="headerlink" title="九、三个最容易踩的坑"></a>九、三个最容易踩的坑</h2><p><strong>坑一：手滑把 <code>satisfies</code> 写成类型标注。</strong></p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">const</span> <span class="hljs-attr">api</span>: <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-title class_">ApiDef</span>&gt; = &#123; ... &#125;          <span class="hljs-comment">// ❌ keyof typeof api → string，全部塌缩</span><br><span class="hljs-keyword">const</span> api = &#123; ... &#125; <span class="hljs-keyword">satisfies</span> <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-title class_">ApiDef</span>&gt; <span class="hljs-comment">// ✅ 校验形状，保留字面量推导</span><br></code></pre></td></tr></table></figure>

<p>塌缩后的症状很典型：<code>request(&#39;任意字符串&#39;)</code> 都不报错。看到这个现象先检查表的定义方式。</p>
<p><strong>坑二：分布式条件类型在意料之外拆开联合。</strong></p>
<p>本文的 <code>Resp&lt;T&gt;</code>&#x2F;<code>Args&lt;T&gt;</code> 之所以安全，是因为 <code>ApiOf&lt;K&gt;</code> 在调用时总是<strong>单个</strong>对象类型。但如果哪天写出 <code>Resp&lt;ApiOf&lt;keyof typeof api&gt;&gt;</code>（想「批量」处理所有 API），裸 <code>T</code> 会把四个定义的联合拆开逐个判断、再合并成联合——<code>Args</code> 会得到 <code>[payload: ...] | []</code>，可变元组变成「或」语义，约束直接失效。<strong>想禁止分配，写 <code>[T] extends [{ params: infer P }]</code></strong>。判断标准就一条：条件类型的检查对象是不是「裸的类型参数」。</p>
<p><strong>坑三：可选属性 + <code>infer</code> 的 <code>undefined</code> 污染。</strong></p>
<p>如果表里写的是 <code>params?: {} as { id: number }</code>（可选键），<code>T extends { params: infer P }</code> 推出的 <code>P</code> 是 <code>{ id: number } | undefined</code>，<code>Args</code> 会要求你传一个「可能是 undefined 的参数」，约束变糊。这就是第四节坚持<strong>要么写键、要么不写键</strong>（不用 <code>?</code>）的原因。若必须处理可选键，用守卫分支把 undefined 剥掉：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">type</span> <span class="hljs-title class_">Param</span>&lt;T&gt; =<br>  T <span class="hljs-keyword">extends</span> &#123; <span class="hljs-attr">params</span>: infer P &#125;<br>    ? <span class="hljs-literal">undefined</span> <span class="hljs-keyword">extends</span> P    <span class="hljs-comment">// P 里掺了 undefined（可选键）吗</span><br>      ? <span class="hljs-built_in">never</span>               <span class="hljs-comment">// 当作「不强制传」</span><br>      : P<br>    : <span class="hljs-built_in">never</span><br></code></pre></td></tr></table></figure>

<h2 id="十、完整实现与总结"><a href="#十、完整实现与总结" class="headerlink" title="十、完整实现与总结"></a>十、完整实现与总结</h2><figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// ── types.ts ──</span><br><span class="hljs-keyword">interface</span> <span class="hljs-title class_">ApiEnvelope</span>&lt;T = <span class="hljs-built_in">unknown</span>&gt; &#123;<br>  <span class="hljs-attr">code</span>: <span class="hljs-built_in">number</span><br>  <span class="hljs-attr">message</span>: <span class="hljs-built_in">string</span><br>  <span class="hljs-attr">data</span>: T<br>&#125;<br><br><span class="hljs-keyword">interface</span> <span class="hljs-title class_">ApiDef</span> &#123;<br>  <span class="hljs-attr">url</span>: <span class="hljs-built_in">string</span><br>  <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;GET&#x27;</span> | <span class="hljs-string">&#x27;POST&#x27;</span> | <span class="hljs-string">&#x27;PUT&#x27;</span> | <span class="hljs-string">&#x27;DELETE&#x27;</span><br>  <span class="hljs-attr">params</span>?: <span class="hljs-built_in">object</span><br>  <span class="hljs-attr">data</span>?: <span class="hljs-built_in">object</span><br>  <span class="hljs-attr">resp</span>?: <span class="hljs-built_in">unknown</span><br>&#125;<br><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">ApiOf</span>&lt;K <span class="hljs-keyword">extends</span> keyof <span class="hljs-keyword">typeof</span> api&gt; = (<span class="hljs-keyword">typeof</span> api)[K]<br><span class="hljs-keyword">type</span> <span class="hljs-title class_">Resp</span>&lt;T&gt; = T <span class="hljs-keyword">extends</span> &#123; <span class="hljs-attr">resp</span>: infer R &#125; ? R : <span class="hljs-built_in">void</span><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">Args</span>&lt;T&gt; =<br>  T <span class="hljs-keyword">extends</span> &#123; <span class="hljs-attr">params</span>: infer P &#125; ? [<span class="hljs-attr">payload</span>: P] :<br>  T <span class="hljs-keyword">extends</span> &#123; <span class="hljs-attr">data</span>: infer D &#125;   ? [<span class="hljs-attr">payload</span>: D] :<br>  []<br><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">PathVars</span>&lt;S <span class="hljs-keyword">extends</span> <span class="hljs-built_in">string</span>&gt; =<br>  S <span class="hljs-keyword">extends</span> <span class="hljs-string">`<span class="hljs-subst">$&#123;<span class="hljs-built_in">string</span>&#125;</span>:<span class="hljs-subst">$&#123;infer V&#125;</span>/<span class="hljs-subst">$&#123;infer Rest&#125;</span>`</span><br>    ? V | <span class="hljs-title class_">PathVars</span>&lt;<span class="hljs-title class_">Rest</span>&gt;<br>    : S <span class="hljs-keyword">extends</span> <span class="hljs-string">`<span class="hljs-subst">$&#123;<span class="hljs-built_in">string</span>&#125;</span>:<span class="hljs-subst">$&#123;infer V&#125;</span>`</span><br>      ? V<br>      : <span class="hljs-built_in">never</span><br><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">ApiError</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_ inherited__">Error</span> &#123;<br>  <span class="hljs-title function_">constructor</span>(<span class="hljs-params"><span class="hljs-keyword">readonly</span> <span class="hljs-attr">code</span>: <span class="hljs-built_in">number</span>, <span class="hljs-attr">message</span>: <span class="hljs-built_in">string</span></span>) &#123;<br>    <span class="hljs-variable language_">super</span>(message)<br>    <span class="hljs-variable language_">this</span>.<span class="hljs-property">name</span> = <span class="hljs-string">&#x27;ApiError&#x27;</span><br>  &#125;<br>&#125;<br><br><span class="hljs-comment">// ── api.ts ──（表即文档：url / method / 参数 / 返回值一处可见）</span><br><span class="hljs-keyword">const</span> api = &#123;<br>  <span class="hljs-attr">getUser</span>:   &#123; <span class="hljs-attr">url</span>: <span class="hljs-string">&#x27;/users/:id&#x27;</span>, <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;GET&#x27;</span>,  <span class="hljs-attr">params</span>: &#123;&#125; <span class="hljs-keyword">as</span> &#123; <span class="hljs-attr">id</span>: <span class="hljs-built_in">number</span> &#125;, <span class="hljs-attr">resp</span>: &#123;&#125; <span class="hljs-keyword">as</span> <span class="hljs-title class_">User</span> &#125;,<br>  <span class="hljs-attr">updateUser</span>:&#123; <span class="hljs-attr">url</span>: <span class="hljs-string">&#x27;/users&#x27;</span>,     <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;PUT&#x27;</span>,  <span class="hljs-attr">data</span>: &#123;&#125; <span class="hljs-keyword">as</span> &#123; <span class="hljs-attr">name</span>: <span class="hljs-built_in">string</span> &#125;, <span class="hljs-attr">resp</span>: &#123;&#125; <span class="hljs-keyword">as</span> <span class="hljs-title class_">User</span> &#125;,<br>  <span class="hljs-attr">login</span>:     &#123; <span class="hljs-attr">url</span>: <span class="hljs-string">&#x27;/login&#x27;</span>,     <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;POST&#x27;</span>, <span class="hljs-attr">data</span>: &#123;&#125; <span class="hljs-keyword">as</span> &#123; <span class="hljs-attr">username</span>: <span class="hljs-built_in">string</span>; <span class="hljs-attr">password</span>: <span class="hljs-built_in">string</span> &#125;, <span class="hljs-attr">resp</span>: &#123;&#125; <span class="hljs-keyword">as</span> &#123; <span class="hljs-attr">token</span>: <span class="hljs-built_in">string</span> &#125; &#125;,<br>  <span class="hljs-attr">logout</span>:    &#123; <span class="hljs-attr">url</span>: <span class="hljs-string">&#x27;/logout&#x27;</span>,    <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;POST&#x27;</span> &#125;,<br>&#125; <span class="hljs-keyword">satisfies</span> <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-title class_">ApiDef</span>&gt;<br><br><span class="hljs-comment">// ── request.ts ──</span><br><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> request&lt;K <span class="hljs-keyword">extends</span> keyof <span class="hljs-keyword">typeof</span> api&gt;(<br>  <span class="hljs-attr">name</span>: K,<br>  ...<span class="hljs-attr">args</span>: <span class="hljs-title class_">Args</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;K&gt;&gt;<br>): <span class="hljs-title class_">Promise</span>&lt;<span class="hljs-title class_">Resp</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;K&gt;&gt;&gt; &#123;<br>  <span class="hljs-keyword">const</span> def = api[name]<br>  <span class="hljs-keyword">const</span> payload = args[<span class="hljs-number">0</span>] <span class="hljs-keyword">as</span> <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-built_in">unknown</span>&gt; | <span class="hljs-literal">undefined</span><br>  <span class="hljs-keyword">const</span> url = <span class="hljs-title function_">fillPath</span>(def.<span class="hljs-property">url</span>, payload <span class="hljs-keyword">as</span> <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, <span class="hljs-built_in">string</span>&gt;)<br>  <span class="hljs-keyword">const</span> res = <span class="hljs-keyword">await</span> axios.<span class="hljs-property">post</span>&lt;<span class="hljs-title class_">ApiEnvelope</span>&lt;<span class="hljs-title class_">Resp</span>&lt;<span class="hljs-title class_">ApiOf</span>&lt;K&gt;&gt;&gt;&gt;(url, payload)<br>  <span class="hljs-keyword">if</span> (res.<span class="hljs-property">data</span>.<span class="hljs-property">code</span> !== <span class="hljs-number">0</span>) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ApiError</span>(res.<span class="hljs-property">data</span>.<span class="hljs-property">code</span>, res.<span class="hljs-property">data</span>.<span class="hljs-property">message</span>)<br>  <span class="hljs-keyword">return</span> res.<span class="hljs-property">data</span>.<span class="hljs-property">data</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>回头看这趟演进，每一步都是一个可迁移的思路：</p>
<table>
<thead>
<tr>
<th>痛点</th>
<th>用到的类型能力</th>
<th>迁移到别处的场景</th>
</tr>
</thead>
<tbody><tr>
<td>返回值是 <code>any</code></td>
<td>泛型 <code>request&lt;T&gt;</code></td>
<td>所有容器 &#x2F; 工具函数</td>
</tr>
<tr>
<td>类型与接口两张皮</td>
<td><code>typeof + keyof + 索引访问</code>，表为唯一数据源</td>
<td>配置表、路由表、i18n key</td>
</tr>
<tr>
<td>参数可传可不传</td>
<td>条件类型 + <code>infer</code> 提取</td>
<td>从现有类型解构出想要的片段</td>
</tr>
<tr>
<td>有参数必传 &#x2F; 无参禁传</td>
<td>条件类型 + 可变元组</td>
<td>任何「按前者决定后者」的签名</td>
</tr>
<tr>
<td><code>:id</code> 无约束</td>
<td>模板字面量 + 递归条件类型</td>
<td>事件名、路由 path、CSS 变量名</td>
</tr>
<tr>
<td>错误是裸 <code>string</code></td>
<td><code>ApiError</code> + <code>instanceof</code> 收窄</td>
<td>所有可预期的失败路径</td>
</tr>
</tbody></table>
<p><strong>类型安全的本质不是「多写类型」，而是「建立关联」</strong>：把 API 名和它的定义关联、把定义和参数列表关联、把 url 字符串和路径变量关联。<code>infer</code>、条件类型、模板字面量只是建立关联的胶水。想清楚了这一点，再看 <code>Partial</code>、<code>Pick</code>、<code>Awaited</code> 这些内置工具类型的源码（它们全是十几行的条件类型），就能读出每一条「关联」是为哪个痛点服务的——也就到了随手给自己写工具类型的时候。</p>
]]></content>
      <categories>
        <category>TypeScript</category>
        <category>实战</category>
      </categories>
      <tags>
        <tag>TypeScript</tag>
        <tag>泛型</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Boot 项目实战复盘：big-event 文章管理系统（JWT + Redis + MyBatis + MinIO）</title>
    <url>/2026/09/10/springboot-big-event-project/</url>
    <content><![CDATA[<p>学 Spring Boot 的时候，知识点是一个一个学的：依赖注入、拦截器、注解校验、MyBatis……每个都能看懂，但始终没回答一个问题——<strong>它们在真实项目里是怎么协同工作的</strong>。big-event 是我学完 Java 后按课程完成的第一个完整后端项目，一个文章管理系统，从建表到接口全部跑通。</p>
<blockquote>
<p><strong>单个知识点和完整项目之间的差距，在于「横切关注点」怎么组织</strong>：认证放在拦截器里统一做，而不是每个接口手写一遍；当前用户身份用 ThreadLocal 传递，而不是在参数里层层透传；异常和响应用全局处理器统一格式，而不是各接口自理。这个项目把这四件事各做了一遍，本文就以「一次请求的生命周期」为主线来复盘。</p>
</blockquote>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">                    big-event 文章管理系统<br>┌─────────────────────────────────────────────────────┐<br>│  用户模块      分类模块      文章模块      文件上传     │<br>│  注册/登录     CRUD         CRUD+分页    本地/MinIO   │<br>├─────────────────────────────────────────────────────┤<br>│  横切关注点：拦截器认证 / ThreadLocal / 全局异常 /    │<br>│              统一响应 Result / Bean Validation       │<br>├─────────────────────────────────────────────────────┤<br>│  MyBatis（注解 + XML 动态 SQL）   JWT + Redis         │<br>├─────────────────────────────────────────────────────┤<br>│  MySQL                Redis              MinIO      │<br>└─────────────────────────────────────────────────────┘<br></code></pre></td></tr></table></figure>

<span id="more"></span>

<h2 id="一、项目概览"><a href="#一、项目概览" class="headerlink" title="一、项目概览"></a>一、项目概览</h2><p>big-event 是一个三模块的 REST 接口服务：用户（注册、登录、个人信息、头像、改密）、文章分类（CRUD）、文章（CRUD + 条件分页），外加文件上传。技术栈如下：</p>
<table>
<thead>
<tr>
<th>技术</th>
<th>版本</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td>Spring Boot</td>
<td>4.1.0</td>
<td>基础框架，Java 17</td>
</tr>
<tr>
<td>MyBatis</td>
<td>4.0.1 starter</td>
<td>ORM，注解与 XML 混用</td>
</tr>
<tr>
<td>MySQL</td>
<td>-</td>
<td>三张表：user &#x2F; category &#x2F; article</td>
</tr>
<tr>
<td>java-jwt</td>
<td>4.4.0</td>
<td>生成 &#x2F; 解析 token</td>
</tr>
<tr>
<td>Redis</td>
<td>spring-data-redis</td>
<td>token 白名单，实现服务端主动失效</td>
</tr>
<tr>
<td>PageHelper</td>
<td>4.1.1</td>
<td>物理分页</td>
</tr>
<tr>
<td>MinIO</td>
<td>8.5.17</td>
<td>对象存储，存头像等文件</td>
</tr>
<tr>
<td>springdoc-openapi</td>
<td>3.0.0</td>
<td>Swagger 接口文档</td>
</tr>
<tr>
<td>Lombok + Validation</td>
<td>-</td>
<td>简化 POJO &#x2F; 参数校验</td>
</tr>
</tbody></table>
<p>包结构是经典的三层架构：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">com.big_event<br>├── contorller/     # 接口层：参数校验、调用 service、返回 Result<br>├── service/impl/   # 业务层：补时间戳、取当前用户、组织查询条件<br>├── mapper/         # 持久层：MyBatis 注解 + 一个 XML（动态 SQL）<br>├── pojo/           # User / Category / Article / Result / PageBean<br>├── interceptors/   # LoginInterceptor：token 双重校验<br>├── config/         # WebConfig / MinioConfig / SwaggerConfig<br>├── anno/           # @State 自定义校验注解、@AutoFill（未完成）<br>├── validation/     # StateValidation：注解的校验器实现<br>├── exception/      # GlobalExceptionHandler<br>└── utils/          # JwtUtil / Md5Util / ThreadLocalUtil<br></code></pre></td></tr></table></figure>

<h2 id="二、一次请求的生命周期"><a href="#二、一次请求的生命周期" class="headerlink" title="二、一次请求的生命周期"></a>二、一次请求的生命周期</h2><p><strong>先给结论，再给论证</strong>：这个项目里最值得复盘的不是某个接口，而是每个请求都要走的这条固定链路。</p>
<p>以「查询我的文章列表」为例，一个带着 <code>Authorization</code> 头的请求进来之后：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">HTTP 请求（携带 Authorization: &lt;token&gt;）<br>   │<br>   ▼<br>LoginInterceptor.preHandle<br>   ├── Redis 里还有这个 token 吗？── 没有 ──► 401，请求结束<br>   └── JwtUtil.parseToken 校验签名/有效期 ──► claims 存入 ThreadLocal<br>   │<br>   ▼<br>Controller（@Validated 参数校验，自定义 @State 注解）<br>   │<br>   ▼<br>Service（业务逻辑，ThreadLocalUtil.get() 拿当前用户 id）<br>   │<br>   ▼<br>Mapper（MyBatis：注解 SQL / XML 动态 SQL）──► MySQL<br>   │<br>   ▼<br>正常：Result.success(data)<br>异常：GlobalExceptionHandler 兜底 ──► Result.error(msg)<br>   │<br>   ▼<br>afterCompletion ──► ThreadLocalUtil.remove()（清理线程上下文）<br></code></pre></td></tr></table></figure>

<p>链路的最后一步是<strong>统一响应</strong>。所有接口都返回 <code>Result&lt;T&gt;</code>，前端只需要认一种结构：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Result</span>&lt;T&gt; &#123;<br>    <span class="hljs-keyword">private</span> Integer code;   <span class="hljs-comment">// 200 成功，1 失败</span><br>    <span class="hljs-keyword">private</span> String message;<br>    <span class="hljs-keyword">private</span> T data;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;E&gt; <span class="hljs-type">Result</span>&lt;E&gt; <span class="hljs-title function_">success</span><span class="hljs-params">(E data)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Result</span>&lt;E&gt;(<span class="hljs-number">200</span>, <span class="hljs-string">&quot;操作成功&quot;</span>, data);<br>    &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; <span class="hljs-type">Result</span>&lt;T&gt; <span class="hljs-title function_">error</span><span class="hljs-params">(String message)</span> &#123;<br>        <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Result</span>&lt;T&gt;(<span class="hljs-number">1</span>, message, <span class="hljs-literal">null</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>配套的是<strong>全局异常处理</strong>。没有它之前，任何一次参数校验失败都会抛出 <code>MethodArgumentNotValidException</code>，前端收到的是一整个堆栈页；有了它，所有未被捕获的异常都收敛成统一 JSON：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@RestControllerAdvice</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">GlobalExceptionHandler</span> &#123;<br>    <span class="hljs-meta">@ExceptionHandler(Exception.class)</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">Result</span> <span class="hljs-title function_">handleException</span><span class="hljs-params">(Exception e)</span> &#123;<br>        e.printStackTrace();<br>        <span class="hljs-keyword">return</span> Result.error(StringUtils.hasLength(e.getMessage())<br>                ? e.getMessage() : <span class="hljs-string">&quot;操作失败&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>拦截器本身在 <code>WebConfig</code> 里注册，登录、注册、文件上传和 Swagger 文档这些无需认证的路径显式排除：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java">registry.addInterceptor(loginInterceptor).excludePathPatterns(<br>    <span class="hljs-string">&quot;/user/login&quot;</span>, <span class="hljs-string">&quot;/user/register&quot;</span>,<br>    <span class="hljs-string">&quot;/minio/**&quot;</span>, <span class="hljs-string">&quot;/swagger-ui/**&quot;</span>, <span class="hljs-string">&quot;/swagger-ui.html&quot;</span>,<br>    <span class="hljs-string">&quot;/v3/api-docs/**&quot;</span>, <span class="hljs-string">&quot;/v3/api-docs&quot;</span><br>);<br></code></pre></td></tr></table></figure>

<h2 id="三、登录认证：JWT-Redis-双重校验"><a href="#三、登录认证：JWT-Redis-双重校验" class="headerlink" title="三、登录认证：JWT + Redis 双重校验"></a>三、登录认证：JWT + Redis 双重校验</h2><p><strong>这是整个项目最核心的一条链路。</strong> 单独用 JWT 有一个绕不过去的问题：token 一旦签发，在过期之前始终有效，服务端没有任何办法作废它——用户改了密码、被封号，旧 token 依然能畅通无阻。解法是把「token 是否有效」的裁决权收回服务端：<strong>JWT 负责携带身份，Redis 负责决定生死</strong>。</p>
<h3 id="3-1-登录：签发-token-并登记到-Redis"><a href="#3-1-登录：签发-token-并登记到-Redis" class="headerlink" title="3.1 登录：签发 token 并登记到 Redis"></a>3.1 登录：签发 token 并登记到 Redis</h3><p>密码校验通过后，两件事一起做：用 JWT 把 <code>id</code>、<code>username</code> 打包进 token（有效期 12 小时），同时把 token 存进 Redis（TTL 1 小时）：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">if</span> (Md5Util.md5(password).equals(loginUser.getPassword())) &#123;<br>    <span class="hljs-type">Map</span>&lt;String, Object&gt; <span class="hljs-variable">claims</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">HashMap</span>&lt;&gt;();<br>    claims.put(<span class="hljs-string">&quot;id&quot;</span>, loginUser.getId());<br>    claims.put(<span class="hljs-string">&quot;username&quot;</span>, loginUser.getUsername());<br>    <span class="hljs-type">String</span> <span class="hljs-variable">token</span> <span class="hljs-operator">=</span> JwtUtil.genToken(claims);<br><br>    <span class="hljs-comment">// 把 token 存储到 Redis 中，作为「当前有效」的白名单</span><br>    <span class="hljs-type">ValueOperations</span>&lt;String, String&gt; <span class="hljs-variable">operations</span> <span class="hljs-operator">=</span> stringRedisTemplate.opsForValue();<br>    operations.set(token, token, Duration.ofHours(<span class="hljs-number">1</span>));<br>    <span class="hljs-keyword">return</span> Result.success(token);<br>&#125;<br></code></pre></td></tr></table></figure>

<p>JWT 的生成与解析工具很薄，HMAC256 签名 + 过期时间：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">JwtUtil</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">String</span> <span class="hljs-variable">KEY</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;itheima&quot;</span>;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">String</span> <span class="hljs-title function_">genToken</span><span class="hljs-params">(Map&lt;String, Object&gt; claims)</span> &#123;<br>        <span class="hljs-keyword">return</span> JWT.create()<br>                .withClaim(<span class="hljs-string">&quot;claims&quot;</span>, claims)<br>                .withExpiresAt(<span class="hljs-keyword">new</span> <span class="hljs-title class_">Date</span>(System.currentTimeMillis() + <span class="hljs-number">1000</span> * <span class="hljs-number">60</span> * <span class="hljs-number">60</span> * <span class="hljs-number">12</span>))<br>                .sign(Algorithm.HMAC256(KEY));<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">Map</span>&lt;String, Object&gt; <span class="hljs-title function_">parseToken</span><span class="hljs-params">(String token)</span> &#123;<br>        <span class="hljs-keyword">return</span> JWT.require(Algorithm.HMAC256(KEY))<br>                .build().verify(token)<br>                .getClaim(<span class="hljs-string">&quot;claims&quot;</span>).asMap();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="3-2-拦截器：先查-Redis，再解析-JWT"><a href="#3-2-拦截器：先查-Redis，再解析-JWT" class="headerlink" title="3.2 拦截器：先查 Redis，再解析 JWT"></a>3.2 拦截器：先查 Redis，再解析 JWT</h3><p>每个受保护的请求都要过这道闸。注意校验顺序：<strong>先查 Redis 再解析 JWT</strong>——Redis 里没有，说明服务端已经作废了这个 token，直接 401：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Component</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">LoginInterceptor</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">HandlerInterceptor</span> &#123;<br><br>    <span class="hljs-meta">@Autowired</span><br>    <span class="hljs-keyword">private</span> StringRedisTemplate stringRedisTemplate;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">preHandle</span><span class="hljs-params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        <span class="hljs-type">String</span> <span class="hljs-variable">token</span> <span class="hljs-operator">=</span> request.getHeader(<span class="hljs-string">&quot;Authorization&quot;</span>);<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-comment">// 从 Redis 中获取 token，没有说明已失效（被作废或过期）</span><br>            <span class="hljs-type">ValueOperations</span>&lt;String, String&gt; <span class="hljs-variable">operations</span> <span class="hljs-operator">=</span> stringRedisTemplate.opsForValue();<br>            <span class="hljs-type">String</span> <span class="hljs-variable">redisToken</span> <span class="hljs-operator">=</span> operations.get(token);<br>            <span class="hljs-keyword">if</span> (redisToken == <span class="hljs-literal">null</span>) &#123;<br>                <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">RuntimeException</span>();<br>            &#125;<br><br>            <span class="hljs-type">Map</span>&lt;String, Object&gt; <span class="hljs-variable">claims</span> <span class="hljs-operator">=</span> JwtUtil.parseToken(token);<br>            <span class="hljs-comment">// 把业务数据存储到 ThreadLocal 中</span><br>            ThreadLocalUtil.set(claims);<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>            response.setStatus(<span class="hljs-number">401</span>);   <span class="hljs-comment">// http 响应状态码 401</span><br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">void</span> <span class="hljs-title function_">afterCompletion</span><span class="hljs-params">(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex)</span> <span class="hljs-keyword">throws</span> Exception &#123;<br>        ThreadLocalUtil.remove();     <span class="hljs-comment">// 清空，防止内存泄漏</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="3-3-ThreadLocal：把用户身份「随身携带」"><a href="#3-3-ThreadLocal：把用户身份「随身携带」" class="headerlink" title="3.3 ThreadLocal：把用户身份「随身携带」"></a>3.3 ThreadLocal：把用户身份「随身携带」</h3><p>拦截器解析出的用户信息，Controller 和 Service 是怎么拿到的？答案是不用参数传，用 <code>ThreadLocal</code> 存取——同一个请求由同一个线程处理，线程内部全局可见：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ThreadLocalUtil</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> <span class="hljs-type">ThreadLocal</span> <span class="hljs-variable">THREAD_LOCAL</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadLocal</span>();<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> &lt;T&gt; <span class="hljs-type">T</span> <span class="hljs-title function_">get</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> (T) THREAD_LOCAL.get();<br>    &#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">void</span> <span class="hljs-title function_">set</span><span class="hljs-params">(Object value)</span> &#123;<br>        THREAD_LOCAL.set(value);<br>    &#125;<br>    <span class="hljs-comment">// 清除，防止内存泄漏</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-type">void</span> <span class="hljs-title function_">remove</span><span class="hljs-params">()</span> &#123;<br>        THREAD_LOCAL.remove();<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>于是业务代码里取当前用户就一行：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">Map</span>&lt;String, Object&gt; <span class="hljs-variable">map</span> <span class="hljs-operator">=</span> ThreadLocalUtil.get();<br><span class="hljs-type">Integer</span> <span class="hljs-variable">userId</span> <span class="hljs-operator">=</span> (Integer) map.get(<span class="hljs-string">&quot;id&quot;</span>);<br></code></pre></td></tr></table></figure>

<p>这里有一个<strong>必须理解的坑</strong>：Tomcat 处理请求用的是线程池，线程是复用的。Entry 的 key 是弱引用会被 GC，但 value 是强引用——如果不在请求结束时 <code>remove()</code>，上一个请求的用户信息会残留在线程里，轻则串号（A 看到 B 的数据），重则内存泄漏。所以 <code>afterCompletion</code> 里的 <code>ThreadLocalUtil.remove()</code> 不是可选的收尾，而是正确性的一部分。</p>
<h3 id="3-4-改密码-强制下线"><a href="#3-4-改密码-强制下线" class="headerlink" title="3.4 改密码 &#x3D; 强制下线"></a>3.4 改密码 &#x3D; 强制下线</h3><p>有了 Redis 这一层，「修改密码后强制重新登录」变得非常自然——把当前 token 从 Redis 删掉即可，下次请求在 3.2 的第一道检查就会被拒：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 2.调用 service 完成更新密码</span><br>userService.updatePwd(newPwd);<br><br><span class="hljs-comment">// 删除 Redis 中对应的 token</span><br><span class="hljs-type">ValueOperations</span>&lt;String, String&gt; <span class="hljs-variable">operations</span> <span class="hljs-operator">=</span> stringRedisTemplate.opsForValue();<br>operations.getOperations().delete(token);<br></code></pre></td></tr></table></figure>

<p>纯 JWT 方案做不到这一点，这就是引入 Redis 的全部理由。</p>
<h2 id="四、参数校验体系：从内置注解到自定义注解"><a href="#四、参数校验体系：从内置注解到自定义注解" class="headerlink" title="四、参数校验体系：从内置注解到自定义注解"></a>四、参数校验体系：从内置注解到自定义注解</h2><p>项目里参数校验分三个层次，复杂度依次递进。</p>
<p><strong>第一层：内置注解直接标在参数上。</strong> 注册接口的用户名密码规则，一条 <code>@Pattern</code> 搞定（配合类上的 <code>@Validated</code>），替代了之前手写的一长串 if：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-type">Result</span> <span class="hljs-title function_">register</span><span class="hljs-params">(<span class="hljs-meta">@Pattern(regexp = &quot;^\\S&#123;5,16&#125;$&quot;)</span> String username,</span><br><span class="hljs-params">                       <span class="hljs-meta">@Pattern(regexp = &quot;^\\S&#123;5,16&#125;$&quot;)</span> String password)</span> &#123;<br></code></pre></td></tr></table></figure>

<p><strong>第二层：分组校验，一个 POJO 两套规则。</strong> 分类的「新增」不需要 id（数据库自增），「修改」必须带 id。为同一个 POJO 声明两个分组，让 <code>id</code> 只在 Update 组生效：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Data</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Category</span> &#123;<br>    <span class="hljs-meta">@NotNull(groups = Update.class)</span>   <span class="hljs-comment">// 只在修改时要求必填</span><br>    <span class="hljs-keyword">private</span> Integer id;<br>    <span class="hljs-meta">@NotEmpty</span><br>    <span class="hljs-keyword">private</span> String categoryName;<br>    <span class="hljs-meta">@NotEmpty</span><br>    <span class="hljs-keyword">private</span> String categoryAlias;<br><br>    <span class="hljs-comment">// 没指定 groups 的校验项默认属于 Default 分组</span><br>    <span class="hljs-comment">// 分组之间可以继承：Add extends Default，则 Add 组拥有 Default 的全部校验项</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Add</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Default</span> &#123;&#125;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">Update</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Default</span> &#123;&#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>Controller 里指定用哪套规则：<code>@Validated(Category.Add.class)</code> 新增、<code>@Validated(Category.Update.class)</code> 修改。</p>
<p><strong>第三层：自定义校验注解。</strong> 文章的 <code>state</code> 字段只允许「已发布」或「草稿」，内置注解表达不了「枚举值集合」这种语义（<code>@Pattern</code> 写中文正则太绕），于是自己写一个 <code>@State</code>。自定义校验注解三个要素——注解本身、校验器、注册关联：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 注解：用 @Constraint 指定校验器</span><br><span class="hljs-meta">@Documented</span><br><span class="hljs-meta">@Target(&#123;FIELD&#125;)</span><br><span class="hljs-meta">@Retention(RUNTIME)</span><br><span class="hljs-meta">@Constraint(validatedBy = &#123;StateValidation.class&#125;)</span><br><span class="hljs-keyword">public</span> <span class="hljs-meta">@interface</span> State &#123;<br>    <span class="hljs-type">String</span> <span class="hljs-title function_">message</span><span class="hljs-params">()</span> <span class="hljs-keyword">default</span> <span class="hljs-string">&quot;state参数的值只能是发布或者草稿&quot;</span>;<br>    <span class="hljs-type">Class</span>&lt;?&gt;[] <span class="hljs-title function_">groups</span><span class="hljs-params">()</span> <span class="hljs-keyword">default</span> &#123;&#125;;<br>    <span class="hljs-type">Class</span>&lt;? extends Payload&gt;[] <span class="hljs-title function_">payload</span><span class="hljs-params">()</span> <span class="hljs-keyword">default</span> &#123;&#125;;<br>&#125;<br><br><span class="hljs-comment">// 校验器：实现 ConstraintValidator&lt;注解类型, 被校验字段类型&gt;</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">StateValidation</span> <span class="hljs-keyword">implements</span> <span class="hljs-title class_">ConstraintValidator</span>&lt;State, String&gt; &#123;<br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-type">boolean</span> <span class="hljs-title function_">isValid</span><span class="hljs-params">(String value, ConstraintValidatorContext context)</span> &#123;<br>        <span class="hljs-keyword">if</span> (value == <span class="hljs-literal">null</span>) &#123;<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        &#125;<br>        <span class="hljs-keyword">return</span> value.equals(<span class="hljs-string">&quot;发布&quot;</span>) || value.equals(<span class="hljs-string">&quot;草稿&quot;</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>POJO 上的用法和内置注解完全一致，业务代码零感知：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@State</span><br><span class="hljs-keyword">private</span> String state;<br></code></pre></td></tr></table></figure>

<h2 id="五、文章列表：PageHelper-分页-MyBatis-动态-SQL"><a href="#五、文章列表：PageHelper-分页-MyBatis-动态-SQL" class="headerlink" title="五、文章列表：PageHelper 分页 + MyBatis 动态 SQL"></a>五、文章列表：PageHelper 分页 + MyBatis 动态 SQL</h2><p>文章列表接口的要求是：<strong>按分类、状态可选筛选，只看自己的文章，按更新时间倒序，分页返回</strong>。两个组件配合完成。</p>
<h3 id="5-1-PageHelper：一行开启物理分页"><a href="#5-1-PageHelper：一行开启物理分页" class="headerlink" title="5.1 PageHelper：一行开启物理分页"></a>5.1 PageHelper：一行开启物理分页</h3><p>PageHelper 的用法非常「隐式」——在查询前调一行 <code>startPage</code>，它就会拦截<strong>紧随其后的那一条</strong> MyBatis 查询，自动改写成 <code>limit</code> 语句并附带一次 count 查询：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-type">PageBean</span>&lt;Article&gt; <span class="hljs-title function_">list</span><span class="hljs-params">(Integer pageNum, Integer pageSize,</span><br><span class="hljs-params">                              Integer categoryId, String state)</span> &#123;<br>    <span class="hljs-type">PageBean</span>&lt;Article&gt; <span class="hljs-variable">pb</span> <span class="hljs-operator">=</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">PageBean</span>&lt;&gt;();<br><br>    <span class="hljs-comment">// 开启分页查询（只对下一条 SQL 生效）</span><br>    PageHelper.startPage(pageNum, pageSize);<br><br>    <span class="hljs-type">Map</span>&lt;String, Object&gt; <span class="hljs-variable">map</span> <span class="hljs-operator">=</span> ThreadLocalUtil.get();<br>    <span class="hljs-type">Integer</span> <span class="hljs-variable">userId</span> <span class="hljs-operator">=</span> (Integer) map.get(<span class="hljs-string">&quot;id&quot;</span>);<br>    <span class="hljs-type">List</span>&lt;Article&gt; <span class="hljs-variable">as</span> <span class="hljs-operator">=</span> articleMapper.list(userId, categoryId, state);<br><br>    <span class="hljs-comment">// Page 中提供了获取总记录数和当前页数据的方法</span><br>    <span class="hljs-type">Page</span>&lt;Article&gt; <span class="hljs-variable">p</span> <span class="hljs-operator">=</span> (Page&lt;Article&gt;) as;<br>    pb.setTotal(p.getTotal());<br>    pb.setItems(p.getResult());<br>    <span class="hljs-keyword">return</span> pb;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>返回值收敛到统一的分页 VO，前端不用关心 PageHelper 的存在：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Data</span><br><span class="hljs-meta">@NoArgsConstructor</span><br><span class="hljs-meta">@AllArgsConstructor</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">PageBean</span>&lt;T&gt; &#123;<br>    <span class="hljs-keyword">private</span> Long total;      <span class="hljs-comment">// 总条数</span><br>    <span class="hljs-keyword">private</span> List&lt;T&gt; items;   <span class="hljs-comment">// 当前页数据集合</span><br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="5-2-动态-SQL：XML-登场"><a href="#5-2-动态-SQL：XML-登场" class="headerlink" title="5.2 动态 SQL：XML 登场"></a>5.2 动态 SQL：XML 登场</h3><p>这个查询的筛选条件是可选的，SQL 得按参数动态拼接——这正是注解 SQL 的短板、XML 的主场：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><code class="hljs xml"><span class="hljs-tag">&lt;<span class="hljs-name">select</span> <span class="hljs-attr">id</span>=<span class="hljs-string">&quot;list&quot;</span> <span class="hljs-attr">resultType</span>=<span class="hljs-string">&quot;com.big_event.pojo.Article&quot;</span>&gt;</span><br>    select id, title, content, cover_img, state, category_id,<br>           create_user, create_time, update_time<br>    from article<br>    <span class="hljs-tag">&lt;<span class="hljs-name">where</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">if</span> <span class="hljs-attr">test</span>=<span class="hljs-string">&quot;categoryId != null&quot;</span>&gt;</span><br>            category_id = #&#123;categoryId&#125;<br>        <span class="hljs-tag">&lt;/<span class="hljs-name">if</span>&gt;</span><br>        <span class="hljs-tag">&lt;<span class="hljs-name">if</span> <span class="hljs-attr">test</span>=<span class="hljs-string">&quot;state != null and state != &#x27;&#x27;&quot;</span>&gt;</span><br>            and state = #&#123;state&#125;<br>        <span class="hljs-tag">&lt;/<span class="hljs-name">if</span>&gt;</span><br>        and create_user = #&#123;userId&#125;<br>    <span class="hljs-tag">&lt;/<span class="hljs-name">where</span>&gt;</span><br>    order by update_time desc<br><span class="hljs-tag">&lt;/<span class="hljs-name">select</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p><code>&lt;where&gt;</code> 标签会自动处理首个 <code>and</code> 的拼接问题；<code>create_user = #{userId}</code> 不在 <code>&lt;if&gt;</code> 里——<strong>数据隔离是硬条件，不是筛选项</strong>。项目里 UserMapper、CategoryMapper 全用注解（<code>@Select</code>&#x2F;<code>@Insert</code>），只有这条多条件查询用了 XML，这个取舍是对的：简单 SQL 注解直观，动态 SQL 交给 XML。</p>
<p>另外一个小但重要的配置——数据库下划线命名到 Java 驼峰命名的自动映射，不然 <code>cover_img</code> 映射不到 <code>coverImg</code>：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">mybatis:</span><br>  <span class="hljs-attr">configuration:</span><br>    <span class="hljs-attr">map-underscore-to-camel-case:</span> <span class="hljs-literal">true</span><br></code></pre></td></tr></table></figure>

<h2 id="六、文件上传：从本地磁盘到-MinIO"><a href="#六、文件上传：从本地磁盘到-MinIO" class="headerlink" title="六、文件上传：从本地磁盘到 MinIO"></a>六、文件上传：从本地磁盘到 MinIO</h2><p>项目里写了两个版本的上传接口，放在一起对比正好能说明<strong>为什么要用对象存储</strong>。</p>
<h3 id="6-1-本地磁盘版：能用，但处处是坑"><a href="#6-1-本地磁盘版：能用，但处处是坑" class="headerlink" title="6.1 本地磁盘版：能用，但处处是坑"></a>6.1 本地磁盘版：能用，但处处是坑</h3><p>第一个版本把文件写到本机目录，文件名用 UUID 防覆盖。问题很快浮现：路径硬编码（换台机器就失效）、多实例部署时文件散落在各台机器上不共享、还得额外写静态资源映射才能让 URL 访问到磁盘文件。它的问题不在代码，在架构。</p>
<h3 id="6-2-MinIO-版：配置、建桶、上传三步走"><a href="#6-2-MinIO-版：配置、建桶、上传三步走" class="headerlink" title="6.2 MinIO 版：配置、建桶、上传三步走"></a>6.2 MinIO 版：配置、建桶、上传三步走</h3><p>MinIO 是开源的 S3 兼容对象存储，一个独立服务专门管文件，应用只管调 API。第一步是把连接配置做成类型安全的属性绑定：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><code class="hljs yaml"><span class="hljs-attr">minio:</span><br>  <span class="hljs-attr">endpoint:</span> <span class="hljs-string">http://localhost:9000</span>   <span class="hljs-comment"># API 地址（注意是 9000，不是控制台 9001）</span><br>  <span class="hljs-attr">access-key:</span> <span class="hljs-string">minioadmin</span><br>  <span class="hljs-attr">secret-key:</span> <span class="hljs-string">minioadmin123</span><br>  <span class="hljs-attr">bucket:</span> <span class="hljs-string">big-event</span>                 <span class="hljs-comment"># 存储桶名称，不存在会自动创建</span><br></code></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@ConfigurationProperties(prefix = &quot;minio&quot;)</span><br><span class="hljs-meta">@Data</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">MinioProperties</span> &#123;<br>    <span class="hljs-keyword">private</span> String endpoint;<br>    <span class="hljs-keyword">private</span> String accessKey;<br>    <span class="hljs-keyword">private</span> String secretKey;<br>    <span class="hljs-keyword">private</span> String bucket;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>第二步是注册 <code>MinioClient</code>，并且<strong>在启动时自动初始化桶</strong>：不存在则创建，再统一设置成「公开读」策略——桶里的对象可以直接通过 URL 访问，无需签名：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-meta">@Bean</span><br><span class="hljs-keyword">public</span> <span class="hljs-type">MinioClient</span> <span class="hljs-title function_">minioClient</span><span class="hljs-params">()</span> &#123;<br>    <span class="hljs-type">MinioClient</span> <span class="hljs-variable">client</span> <span class="hljs-operator">=</span> MinioClient.builder()<br>            .endpoint(endpoint)<br>            .credentials(properties.getAccessKey(), properties.getSecretKey())<br>            .build();<br>    <span class="hljs-comment">// 客户端就绪后，立刻初始化 bucket（保证「先有客户端、再操作桶」的顺序）</span><br>    initBucket(client);<br>    <span class="hljs-keyword">return</span> client;<br>&#125;<br><br><span class="hljs-keyword">private</span> <span class="hljs-type">void</span> <span class="hljs-title function_">initBucket</span><span class="hljs-params">(MinioClient client)</span> &#123;<br>    <span class="hljs-keyword">try</span> &#123;<br>        <span class="hljs-type">boolean</span> <span class="hljs-variable">exists</span> <span class="hljs-operator">=</span> client.bucketExists(<br>                BucketExistsArgs.builder().bucket(properties.getBucket()).build());<br>        <span class="hljs-keyword">if</span> (!exists) &#123;<br>            client.makeBucket(MakeBucketArgs.builder().bucket(properties.getBucket()).build());<br>        &#125;<br>        <span class="hljs-comment">// 公开读策略：允许任何人执行 s3:GetObject</span><br>        <span class="hljs-comment">// Version 必须是固定的 &quot;2012-10-17&quot;（S3 策略语言的版本标识符，不是日期），</span><br>        <span class="hljs-comment">// 写成其它值 MinIO 会拒绝该策略，导致桶保持私有、图片无法直接访问</span><br>        <span class="hljs-type">String</span> <span class="hljs-variable">policy</span> <span class="hljs-operator">=</span> <span class="hljs-string">&quot;&quot;&quot;</span><br><span class="hljs-string">                &#123;</span><br><span class="hljs-string">                  &quot;Version&quot;: &quot;2012-10-17&quot;,</span><br><span class="hljs-string">                  &quot;Statement&quot;: [&#123;</span><br><span class="hljs-string">                      &quot;Effect&quot;: &quot;Allow&quot;,</span><br><span class="hljs-string">                      &quot;Principal&quot;: &#123;&quot;AWS&quot;: [&quot;*&quot;]&#125;,</span><br><span class="hljs-string">                      &quot;Action&quot;: [&quot;s3:GetObject&quot;],</span><br><span class="hljs-string">                      &quot;Resource&quot;: [&quot;arn:aws:s3:::%s/*&quot;]</span><br><span class="hljs-string">                  &#125;]</span><br><span class="hljs-string">                &#125;</span><br><span class="hljs-string">                &quot;&quot;&quot;</span>.formatted(properties.getBucket());<br>        client.setBucketPolicy(<br>                SetBucketPolicyArgs.builder().bucket(properties.getBucket()).config(policy).build());<br>    &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>        <span class="hljs-comment">// 初始化失败只打日志，不阻塞应用启动</span><br>        System.err.println(<span class="hljs-string">&quot;[MinIO] 初始化 bucket 失败：&quot;</span> + e.getMessage());<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>有一个细节值得记下：无论桶是新建还是已存在，策略都<strong>重新应用一次</strong>——这样即使某次启动时设置策略失败、桶停留在私有状态，下次启动也能自动修正。</p>
<p>第三步是上传接口本身，UUID 生成唯一对象名（保留原始扩展名），流式上传后拼接可直接访问的 URL：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><code class="hljs java"><span class="hljs-type">String</span> <span class="hljs-variable">objectName</span> <span class="hljs-operator">=</span> UUID.randomUUID().toString().replace(<span class="hljs-string">&quot;-&quot;</span>, <span class="hljs-string">&quot;&quot;</span>) + suffix;<br><br>minioClient.putObject(<br>        PutObjectArgs.builder()<br>                .bucket(minioProperties.getBucket())<br>                .object(objectName)<br>                .stream(file.getInputStream(), file.getSize(), -<span class="hljs-number">1</span>)  <span class="hljs-comment">// -1 让 SDK 自动定分片大小</span><br>                .contentType(file.getContentType())<br>                .build());<br><br><span class="hljs-comment">// 桶是公开读，这个 URL 可以直接在浏览器打开</span><br><span class="hljs-type">String</span> <span class="hljs-variable">url</span> <span class="hljs-operator">=</span> endpoint + <span class="hljs-string">&quot;/&quot;</span> + minioProperties.getBucket() + <span class="hljs-string">&quot;/&quot;</span> + objectName;<br></code></pre></td></tr></table></figure>

<p>对比一下两个版本：</p>
<table>
<thead>
<tr>
<th></th>
<th>本地磁盘</th>
<th>MinIO</th>
</tr>
</thead>
<tbody><tr>
<td>部署耦合</td>
<td>路径硬编码，换机失效</td>
<td>独立服务，配置驱动</td>
</tr>
<tr>
<td>多实例</td>
<td>文件不共享</td>
<td>所有实例访问同一存储</td>
</tr>
<tr>
<td>URL 访问</td>
<td>需额外静态资源映射</td>
<td>公开读策略，直接可访问</td>
</tr>
<tr>
<td>容量</td>
<td>受单机磁盘限制</td>
<td>横向扩展</td>
</tr>
</tbody></table>
<h2 id="七、不足与改进"><a href="#七、不足与改进" class="headerlink" title="七、不足与改进"></a>七、不足与改进</h2><p>复盘要看做得对的，也要看留了坑的。以下是这个项目当前明确的问题和对应的改进方向：</p>
<p><strong>密码用了裸 MD5。</strong> MD5 是快速哈希，彩虹表可以秒查常见口令。改进方向是 BCrypt：自带随机盐、可调慢速因子，同一个密码每次哈希结果都不同。学习项目可以接受 MD5，但值得在第一次写时就养成用 BCrypt 的习惯。</p>
<p><strong>Redis TTL（1 小时）短于 JWT 有效期（12 小时）。</strong> 两个过期时间不一致，实际效果是 token 一小时就失效，用户被迫频繁重新登录——JWT 的 12 小时成了摆设。应该让两者对齐，或者干脆让 JWT 只做载体、有效期完全交给 Redis 管理。</p>
<p><strong>登录没有作废旧 token。</strong> 每次登录都往 Redis 新增一个 token，旧 token 只要没过期依然有效，Redis 里会积累一个用户的多个有效 token。更严谨的做法是登录时按用户维度删旧 token，或者改用 <code>userId → token</code> 的结构存储。</p>
<p><strong>自动填充没做完。</strong> 项目里已经建了 <code>@AutoFill</code> 注解和 <code>OperationType</code>（INSERT&#x2F;UPDATE）枚举，但 AOP 依赖和切面类都没写，<code>createTime</code>&#x2F;<code>updateTime</code> 目前是每个 ServiceImpl 里手动 set 的。补一个切面拦截标了 <code>@AutoFill</code> 的 mapper 方法、通过反射统一填充公共字段，是一个很好的下一步练习——这也是把这个项目从「跟教程」推向「自己设计」的一步。</p>
<p><strong>异常处理粒度太粗。</strong> <code>GlobalExceptionHandler</code> 只有一个 <code>Exception.class</code> 兜底，参数校验异常和业务异常返回的信息格式并不友好。可以按异常类型细分：<code>MethodArgumentNotValidException</code> 提取第一条字段错误、自定义 <code>BusinessException</code> 区分业务语义。</p>
<h2 id="八、总结"><a href="#八、总结" class="headerlink" title="八、总结"></a>八、总结</h2><p>用一条主线收束全文：<strong>big-event 的价值不在于功能多，而在于每个功能都踩在一条完整的工程链路上</strong>。</p>
<ul>
<li>认证：JWT 携带身份 + Redis 掌握生死，拦截器统一校验，<code>ThreadLocal</code> 全链路传递用户上下文，<code>afterCompletion</code> 里 <code>remove()</code> 防串号防泄漏；</li>
<li>校验：内置注解 → 分组校验（一个 POJO 两套规则）→ 自定义 <code>@State</code> 注解（注解 + 校验器 + <code>@Constraint</code> 关联），复杂度随语义递进；</li>
<li>数据：PageHelper 一行开启物理分页，<code>&lt;where&gt;/&lt;if&gt;</code> 动态 SQL 应对可选筛选，<code>Result</code>&#x2F;<code>PageBean</code> 统一响应结构，<code>@RestControllerAdvice</code> 兜底所有异常；</li>
<li>存储：文件从本地磁盘迁到 MinIO，<code>@ConfigurationProperties</code> 类型安全配置，启动时自动建桶 + 公开读策略，上传接口 UUID 防覆盖。</li>
</ul>
<!-- 下一步的计划：补上 AOP 自动填充、把 MD5 换成 BCrypt、对齐两边的过期时间。坑都记在上一节了，改完再来一篇续集。 -->
]]></content>
      <categories>
        <category>后端</category>
        <category>Spring Boot</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>Spring Boot</tag>
        <tag>JWT</tag>
        <tag>Redis</tag>
        <tag>MyBatis</tag>
        <tag>MinIO</tag>
      </tags>
  </entry>
  <entry>
    <title>Vue2 与 Vue3 响应式原理对比：从 Object.defineProperty 到 Proxy</title>
    <url>/2026/09/09/vue-responsive-contrast/</url>
    <content><![CDATA[<h1 id="Vue2-与-Vue3-响应式原理对比：从-Object-defineProperty-到-Proxy"><a href="#Vue2-与-Vue3-响应式原理对比：从-Object-defineProperty-到-Proxy" class="headerlink" title="Vue2 与 Vue3 响应式原理对比：从 Object.defineProperty 到 Proxy"></a>Vue2 与 Vue3 响应式原理对比：从 Object.defineProperty 到 Proxy</h1><p>Vue 最核心的能力之一，就是<strong>响应式</strong>。</p>
<p>在使用 Vue 开发应用时，我们通常只需要修改数据：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">count</span>++<br></code></pre></td></tr></table></figure>

<p>页面就会自动发生变化。</p>
<p>开发者并不需要手动查找 DOM，再修改对应的节点。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">修改数据<br>   ↓<br>响应式系统感知变化<br>   ↓<br>找到依赖该数据的逻辑<br>   ↓<br>重新执行相关逻辑<br>   ↓<br>视图更新<br></code></pre></td></tr></table></figure>

<p>这背后就是 Vue 响应式系统所解决的问题。</p>
<p>Vue2 和 Vue3 都实现了这套机制，但底层实现存在比较明显的区别：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Vue2<br>Object.defineProperty<br>        ↓<br>   getter / setter<br>        ↓<br>   Dep / Watcher<br><br>Vue3<br>      Proxy<br>        ↓<br>  track / trigger<br>        ↓<br>      effect<br></code></pre></td></tr></table></figure>

<p>从 Vue2 到 Vue3，并不只是简单地把一个 API 换成另一个 API，而是响应式系统整体设计的一次变化。</p>
<p>本文从 <code>Object.defineProperty</code> 开始，逐步理解 Vue2 的实现方式，再来看 Vue3 为什么引入 <code>Proxy</code>，以及两套响应式系统之间的区别。</p>
<hr>
<h2 id="一、响应式系统解决了什么问题？"><a href="#一、响应式系统解决了什么问题？" class="headerlink" title="一、响应式系统解决了什么问题？"></a>一、响应式系统解决了什么问题？</h2><p>先抛开 Vue，只考虑一个最简单的问题。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> state = &#123;<br>  <span class="hljs-attr">count</span>: <span class="hljs-number">0</span><br>&#125;<br><br>state.<span class="hljs-property">count</span>++<br><br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(state.<span class="hljs-property">count</span>)<br></code></pre></td></tr></table></figure>

<p>JavaScript 本身当然可以感知：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">state.count 被修改了<br></code></pre></td></tr></table></figure>

<p>但是 JavaScript 并不知道：</p>
<blockquote>
<p>哪些代码依赖 <code>state.count</code>？</p>
</blockquote>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">render</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(state.<span class="hljs-property">count</span>)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这里 <code>render()</code> 明显依赖 <code>state.count</code>。</p>
<p>如果我们希望：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">count</span>++<br></code></pre></td></tr></table></figure>

<p>之后自动执行：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">render</span>()<br></code></pre></td></tr></table></figure>

<p>就需要建立一种关系：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">state.count<br>    ↓<br>    render<br></code></pre></td></tr></table></figure>

<p>也就是：</p>
<blockquote>
<p><strong>数据和使用数据的副作用之间建立依赖关系。</strong></p>
</blockquote>
<p>因此，一个响应式系统最核心的两个过程就是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">读取数据<br>   ↓<br>收集依赖<br><br>修改数据<br>   ↓<br>触发依赖<br></code></pre></td></tr></table></figure>

<p>Vue3 中把这两个过程抽象成了两个非常重要的概念：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">track</span>()<br><span class="hljs-title function_">trigger</span>()<br></code></pre></td></tr></table></figure>

<p>可以简单理解为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">track   → 收集依赖<br>trigger → 触发更新<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="二、Vue2：Object-defineProperty"><a href="#二、Vue2：Object-defineProperty" class="headerlink" title="二、Vue2：Object.defineProperty"></a>二、Vue2：Object.defineProperty</h1><p>Vue2 的响应式系统主要建立在：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title class_">Object</span>.<span class="hljs-title function_">defineProperty</span>()<br></code></pre></td></tr></table></figure>

<p>之上。</p>
<p>这是 JavaScript 提供的一个用于修改对象属性描述符的 API。</p>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> user = &#123;<br>  <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;Tom&#x27;</span><br>&#125;<br><br><span class="hljs-title class_">Object</span>.<span class="hljs-title function_">defineProperty</span>(user, <span class="hljs-string">&#x27;name&#x27;</span>, &#123;<br>  <span class="hljs-title function_">get</span>(<span class="hljs-params"></span>) &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;读取 name&#x27;</span>)<br><br>    <span class="hljs-keyword">return</span> <span class="hljs-string">&#x27;Tom&#x27;</span><br>  &#125;,<br><br>  <span class="hljs-title function_">set</span>(<span class="hljs-params">value</span>) &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;修改 name：&#x27;</span>, value)<br>  &#125;<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>当执行：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">user.<span class="hljs-property">name</span><br></code></pre></td></tr></table></figure>

<p>会进入：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">get</span>()<br></code></pre></td></tr></table></figure>

<p>而：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">user.<span class="hljs-property">name</span> = <span class="hljs-string">&#x27;Jerry&#x27;</span><br></code></pre></td></tr></table></figure>

<p>则会进入：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">set</span>()<br></code></pre></td></tr></table></figure>

<p>Vue2 正是利用这个特性，把普通对象属性转换成响应式属性。</p>
<hr>
<h1 id="三、Vue2-如何将对象转换成响应式？"><a href="#三、Vue2-如何将对象转换成响应式？" class="headerlink" title="三、Vue2 如何将对象转换成响应式？"></a>三、Vue2 如何将对象转换成响应式？</h1><p>可以把 Vue2 的过程简化成：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">defineReactive</span>(<span class="hljs-params">obj, key, value</span>) &#123;<br>  <span class="hljs-title class_">Object</span>.<span class="hljs-title function_">defineProperty</span>(obj, key, &#123;<br>    <span class="hljs-attr">enumerable</span>: <span class="hljs-literal">true</span>,<br>    <span class="hljs-attr">configurable</span>: <span class="hljs-literal">true</span>,<br><br>    <span class="hljs-title function_">get</span>(<span class="hljs-params"></span>) &#123;<br>      <span class="hljs-keyword">return</span> value<br>    &#125;,<br><br>    <span class="hljs-title function_">set</span>(<span class="hljs-params">newValue</span>) &#123;<br>      value = newValue<br>    &#125;<br>  &#125;)<br>&#125;<br></code></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> state = &#123;<br>  <span class="hljs-attr">count</span>: <span class="hljs-number">0</span><br>&#125;<br><br><span class="hljs-title function_">defineReactive</span>(state, <span class="hljs-string">&#x27;count&#x27;</span>, state.<span class="hljs-property">count</span>)<br></code></pre></td></tr></table></figure>

<p>此时：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">count</span><br></code></pre></td></tr></table></figure>

<p>会进入 getter。</p>
<p>而：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">count</span> = <span class="hljs-number">10</span><br></code></pre></td></tr></table></figure>

<p>会进入 setter。</p>
<p>从这里开始，Vue 就获得了一个“监听数据变化”的入口。</p>
<hr>
<h1 id="四、getter-和-setter-为什么能够实现响应式？"><a href="#四、getter-和-setter-为什么能够实现响应式？" class="headerlink" title="四、getter 和 setter 为什么能够实现响应式？"></a>四、getter 和 setter 为什么能够实现响应式？</h1><p>仅仅知道数据被读取和修改还不够。</p>
<p>Vue 还需要知道：</p>
<blockquote>
<p><strong>谁读取了这个数据？</strong></p>
</blockquote>
<p>例如组件：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">render</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">return</span> <span class="hljs-string">`&lt;div&gt;<span class="hljs-subst">$&#123;state.count&#125;</span>&lt;/div&gt;`</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>第一次执行 <code>render()</code> 时：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">render()<br>  ↓<br>读取 state.count<br>  ↓<br>触发 getter<br>  ↓<br>记录 render<br></code></pre></td></tr></table></figure>

<p>于是就可以建立：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">state.count<br>    ↓<br>  render<br></code></pre></td></tr></table></figure>

<p>当数据发生变化：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">count</span> = <span class="hljs-number">10</span><br></code></pre></td></tr></table></figure>

<p>执行：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">setter<br>  ↓<br>找到依赖<br>  ↓<br>重新执行 render()<br></code></pre></td></tr></table></figure>

<p>最终：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">数据变化<br>   ↓<br>setter<br>   ↓<br>通知依赖<br>   ↓<br>重新执行相关逻辑<br>   ↓<br>视图更新<br></code></pre></td></tr></table></figure>

<p>这就是 Vue2 响应式的基本思路。</p>
<hr>
<h1 id="五、Dep：依赖管理"><a href="#五、Dep：依赖管理" class="headerlink" title="五、Dep：依赖管理"></a>五、Dep：依赖管理</h1><p>Vue2 中有一个非常重要的概念：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Dep<br></code></pre></td></tr></table></figure>

<p>可以把它理解成：</p>
<blockquote>
<p><strong>一个属性对应的依赖管理器。</strong></p>
</blockquote>
<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">count<br>  ↓<br>Dep<br>  ↓<br>Watcher A<br>Watcher B<br>Watcher C<br></code></pre></td></tr></table></figure>

<p>当 <code>count</code> 被读取时，Vue 会把当前正在执行的 Watcher 添加进去。</p>
<p>可以简化为：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">get</span>(<span class="hljs-params"></span>) &#123;<br>  dep.<span class="hljs-title function_">depend</span>()<br><br>  <span class="hljs-keyword">return</span> value<br>&#125;<br></code></pre></td></tr></table></figure>

<p>当 <code>count</code> 被修改时：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">set</span>(<span class="hljs-params">newValue</span>) &#123;<br>  value = newValue<br><br>  dep.<span class="hljs-title function_">notify</span>()<br>&#125;<br></code></pre></td></tr></table></figure>

<p>于是整个流程变成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">     count<br>       │<br> Object.defineProperty<br>       │<br>  ┌────┴────┐<br>  ↓         ↓<br> get       set<br>  ↓         ↓<br>depend    notify<br>  ↓         ↓<br>  Dep      Dep<br>  │         │<br>  └────┬────┘<br>       ↓<br>    Watcher<br>       ↓<br>     update<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="六、Watcher：具体的依赖"><a href="#六、Watcher：具体的依赖" class="headerlink" title="六、Watcher：具体的依赖"></a>六、Watcher：具体的依赖</h1><p>如果说：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Dep<br></code></pre></td></tr></table></figure>

<p>负责保存依赖，那么：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Watcher<br></code></pre></td></tr></table></figure>

<p>就可以理解成一个具体的依赖。</p>
<p>例如组件渲染：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">render</span>(<span class="hljs-params"></span>) &#123;<br>  <span class="hljs-keyword">return</span> state.<span class="hljs-property">count</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>可以把这个渲染逻辑看成一个 Watcher。</p>
<p>第一次执行：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Watcher<br>   ↓<br>render()<br>   ↓<br>读取 count<br>   ↓<br>count.getter<br>   ↓<br>Dep 收集 Watcher<br></code></pre></td></tr></table></figure>

<p>之后：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">count</span>++<br></code></pre></td></tr></table></figure>

<p>就会：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">setter<br>  ↓<br>Dep.notify()<br>  ↓<br>Watcher.update()<br>  ↓<br>重新执行 render<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="七、Vue2-响应式的完整流程"><a href="#七、Vue2-响应式的完整流程" class="headerlink" title="七、Vue2 响应式的完整流程"></a>七、Vue2 响应式的完整流程</h1><p>把前面的内容串起来：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">        数据对象<br>           │<br>           ↓<br>Object.defineProperty<br>           │<br>     getter / setter<br>       ↙         ↘<br>     get          set<br>      ↓            ↓<br> 依赖收集       派发更新<br>      ↓            ↓<br>     Dep          Dep<br>      ↓            ↓<br>   Watcher ←───────┘<br>      ↓<br>  重新执行<br>      ↓<br>  视图更新<br></code></pre></td></tr></table></figure>

<p>因此 Vue2 的核心链路可以概括成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">读取<br> ↓<br>getter<br> ↓<br>Dep.depend()<br> ↓<br>收集 Watcher<br><br>修改<br> ↓<br>setter<br> ↓<br>Dep.notify()<br> ↓<br>Watcher 更新<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="八、Vue2-为什么需要递归处理对象？"><a href="#八、Vue2-为什么需要递归处理对象？" class="headerlink" title="八、Vue2 为什么需要递归处理对象？"></a>八、Vue2 为什么需要递归处理对象？</h1><p>考虑一个嵌套对象：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> state = &#123;<br>  <span class="hljs-attr">user</span>: &#123;<br>    <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;Tom&#x27;</span>,<br>    <span class="hljs-attr">age</span>: <span class="hljs-number">18</span><br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p>如果只对第一层：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">user</span><br></code></pre></td></tr></table></figure>

<p>进行响应式处理，那么：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">user</span>.<span class="hljs-property">name</span><br></code></pre></td></tr></table></figure>

<p>仍然无法完整地被监听。</p>
<p>因此 Vue2 会在初始化阶段递归处理对象：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">state<br> ├── user<br> │    ├── name<br> │    └── age<br> └── ...<br></code></pre></td></tr></table></figure>

<p>每一个需要响应式处理的属性都需要建立对应的 getter &#x2F; setter。</p>
<p>这也是 Vue2 初始化阶段需要遍历数据的重要原因之一。</p>
<hr>
<h1 id="九、Vue2-的局限：新增属性"><a href="#九、Vue2-的局限：新增属性" class="headerlink" title="九、Vue2 的局限：新增属性"></a>九、Vue2 的局限：新增属性</h1><p><code>Object.defineProperty</code> 最大的设计限制之一，就是：</p>
<blockquote>
<p><strong>它需要针对具体属性进行定义。</strong></p>
</blockquote>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> user = &#123;<br>  <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;Tom&#x27;</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>Vue 初始化时可能已经处理了：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">name<br> ↓<br>getter / setter<br></code></pre></td></tr></table></figure>

<p>但是后来：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">user.<span class="hljs-property">age</span> = <span class="hljs-number">18</span><br></code></pre></td></tr></table></figure>

<p><code>age</code> 是后来新增的属性。</p>
<p>Vue2 并没有提前对：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">age<br></code></pre></td></tr></table></figure>

<p>执行：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title class_">Object</span>.<span class="hljs-title function_">defineProperty</span>()<br></code></pre></td></tr></table></figure>

<p>因此无法自动建立对应的响应式关系。</p>
<p>也就是说：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">初始化<br><br>user.name<br>    ↓<br>defineProperty<br>    ↓<br>响应式<br><br><br>运行过程中<br><br>user.age = 18<br>    ↓<br>新增属性<br>    ↓<br>没有对应 getter / setter<br></code></pre></td></tr></table></figure>

<p>因此 Vue2 提供了：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title class_">Vue</span>.<span class="hljs-title function_">set</span>()<br></code></pre></td></tr></table></figure>

<p>以及：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-variable language_">this</span>.$set()<br></code></pre></td></tr></table></figure>

<p>用于处理这种情况。</p>
<hr>
<h1 id="十、Vue2-的删除属性问题"><a href="#十、Vue2-的删除属性问题" class="headerlink" title="十、Vue2 的删除属性问题"></a>十、Vue2 的删除属性问题</h1><p>类似地：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">delete</span> user.<span class="hljs-property">name</span><br></code></pre></td></tr></table></figure>

<p>也不会自然触发之前定义好的 setter。</p>
<p>因为 setter 只负责：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">属性被重新赋值<br></code></pre></td></tr></table></figure>

<p>而：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">delete</span><br></code></pre></td></tr></table></figure>

<p>是另外一种对象操作。</p>
<p>所以 Vue2 同样提供：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title class_">Vue</span>.<span class="hljs-title function_">delete</span>()<br></code></pre></td></tr></table></figure>

<p>来处理响应式对象中的属性删除。</p>
<hr>
<h1 id="十一、Vue2-对数组的特殊处理"><a href="#十一、Vue2-对数组的特殊处理" class="headerlink" title="十一、Vue2 对数组的特殊处理"></a>十一、Vue2 对数组的特殊处理</h1><p>数组是 Vue2 响应式系统中比较特殊的一部分。</p>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> list = [<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>]<br></code></pre></td></tr></table></figure>

<p>如果执行：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">list.<span class="hljs-title function_">push</span>(<span class="hljs-number">4</span>)<br></code></pre></td></tr></table></figure>

<p>Vue2 需要知道：</p>
<blockquote>
<p>数组发生变化了。</p>
</blockquote>
<p>因此 Vue2 对一些数组变异方法进行了特殊处理：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">push<br>pop<br>shift<br>unshift<br>splice<br>sort<br>reverse<br></code></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">list.<span class="hljs-title function_">push</span>(<span class="hljs-number">4</span>)<br></code></pre></td></tr></table></figure>

<p>Vue2 会通过重写后的数组方法，在执行原始操作后通知依赖。</p>
<p>可以理解为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">list.push()<br>   ↓<br>Vue 重写的方法<br>   ↓<br>原始 push<br>   ↓<br>数组发生变化<br>   ↓<br>通知依赖<br></code></pre></td></tr></table></figure>

<p>这也是为什么 Vue2 的数组响应式实现相比普通对象更加复杂。</p>
<hr>
<h1 id="十二、Vue2-响应式的整体限制"><a href="#十二、Vue2-响应式的整体限制" class="headerlink" title="十二、Vue2 响应式的整体限制"></a>十二、Vue2 响应式的整体限制</h1><p>因此，Vue2 的响应式系统可以总结为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Object.defineProperty<br>        ↓<br>针对属性进行劫持<br>        ↓<br>需要初始化时遍历对象<br>        ↓<br>需要递归处理嵌套对象<br>        ↓<br>新增 / 删除属性存在限制<br>        ↓<br>数组需要特殊处理<br></code></pre></td></tr></table></figure>

<p>这些限制并不是 Vue2 本身设计得不好，而是：</p>
<blockquote>
<p><strong>Object.defineProperty 本身的能力决定了这种实现方式存在边界。</strong></p>
</blockquote>
<p>随着 Vue 的使用场景越来越复杂，这些问题也逐渐成为响应式系统进一步发展的限制。</p>
<hr>
<h1 id="十三、Vue3：Proxy"><a href="#十三、Vue3：Proxy" class="headerlink" title="十三、Vue3：Proxy"></a>十三、Vue3：Proxy</h1><p>Vue3 的响应式系统换成了：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title class_">Proxy</span><br></code></pre></td></tr></table></figure>

<p>Proxy 和 <code>Object.defineProperty</code> 最大的区别是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Object.defineProperty<br>        ↓<br>代理具体属性<br><br>Proxy<br>        ↓<br>代理整个对象<br></code></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> state = &#123;<br>  <span class="hljs-attr">count</span>: <span class="hljs-number">0</span><br>&#125;<br><br><span class="hljs-keyword">const</span> proxy = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Proxy</span>(state, &#123;<br>  <span class="hljs-title function_">get</span>(<span class="hljs-params">target, key</span>) &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;读取：&#x27;</span>, key)<br><br>    <span class="hljs-keyword">return</span> target[key]<br>  &#125;,<br><br>  <span class="hljs-title function_">set</span>(<span class="hljs-params">target, key, value</span>) &#123;<br>    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;修改：&#x27;</span>, key, value)<br><br>    target[key] = value<br><br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span><br>  &#125;<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>现在访问：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">proxy.<span class="hljs-property">count</span><br></code></pre></td></tr></table></figure>

<p>会进入：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">get</span>()<br></code></pre></td></tr></table></figure>

<p>修改：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">proxy.<span class="hljs-property">count</span> = <span class="hljs-number">10</span><br></code></pre></td></tr></table></figure>

<p>会进入：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">set</span>()<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="十四、Proxy-为什么能够解决新增属性问题？"><a href="#十四、Proxy-为什么能够解决新增属性问题？" class="headerlink" title="十四、Proxy 为什么能够解决新增属性问题？"></a>十四、Proxy 为什么能够解决新增属性问题？</h1><p>关键就在于：</p>
<blockquote>
<p><strong>Proxy 代理的是整个对象，而不是某一个已经存在的属性。</strong></p>
</blockquote>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> state = &#123;&#125;<br><br><span class="hljs-keyword">const</span> proxy = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Proxy</span>(state, &#123;<br>  <span class="hljs-title function_">set</span>(<span class="hljs-params">target, key, value</span>) &#123;<br>    target[key] = value<br><br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span><br>  &#125;<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>此时：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">proxy.<span class="hljs-property">name</span> = <span class="hljs-string">&#x27;Tom&#x27;</span><br></code></pre></td></tr></table></figure>

<p>即使：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">name<br></code></pre></td></tr></table></figure>

<p>之前根本不存在，也依然会进入：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">set</span>()<br></code></pre></td></tr></table></figure>

<p>因为 Proxy 代理的是整个对象。</p>
<p>所以：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">proxy.name = &#x27;Tom&#x27;<br>        ↓<br>      Proxy<br>        ↓<br>       set<br>        ↓<br>    设置属性<br>        ↓<br>    触发响应式<br></code></pre></td></tr></table></figure>

<p>不再需要额外的：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title class_">Vue</span>.<span class="hljs-title function_">set</span>()<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="十五、Proxy-可以拦截更多操作"><a href="#十五、Proxy-可以拦截更多操作" class="headerlink" title="十五、Proxy 可以拦截更多操作"></a>十五、Proxy 可以拦截更多操作</h1><p>Proxy 不仅能够拦截：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">get<br>set<br></code></pre></td></tr></table></figure>

<p>还可以处理很多其他操作：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">get<br>set<br>has<br>deleteProperty<br>ownKeys<br>defineProperty<br>getOwnPropertyDescriptor<br>...<br></code></pre></td></tr></table></figure>

<p>例如：</p>
<h3 id="读取属性"><a href="#读取属性" class="headerlink" title="读取属性"></a>读取属性</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">proxy.<span class="hljs-property">name</span><br></code></pre></td></tr></table></figure>

<p>触发：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">get<br></code></pre></td></tr></table></figure>

<h3 id="设置属性"><a href="#设置属性" class="headerlink" title="设置属性"></a>设置属性</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">proxy.<span class="hljs-property">name</span> = <span class="hljs-string">&#x27;Jerry&#x27;</span><br></code></pre></td></tr></table></figure>

<p>触发：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">set<br></code></pre></td></tr></table></figure>

<h3 id="删除属性"><a href="#删除属性" class="headerlink" title="删除属性"></a>删除属性</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">delete</span> proxy.<span class="hljs-property">name</span><br></code></pre></td></tr></table></figure>

<p>触发：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">deleteProperty<br></code></pre></td></tr></table></figure>

<h3 id="判断属性是否存在"><a href="#判断属性是否存在" class="headerlink" title="判断属性是否存在"></a>判断属性是否存在</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-string">&#x27;name&#x27;</span> <span class="hljs-keyword">in</span> proxy<br></code></pre></td></tr></table></figure>

<p>触发：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">has<br></code></pre></td></tr></table></figure>

<p>因此 Proxy 为 Vue3 建立更加完整的响应式系统提供了更好的基础。</p>
<hr>
<h1 id="十六、Vue3-的-reactive"><a href="#十六、Vue3-的-reactive" class="headerlink" title="十六、Vue3 的 reactive"></a>十六、Vue3 的 reactive</h1><p>Vue3 中：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">reactive</span>()<br></code></pre></td></tr></table></figure>

<p>就是创建响应式对象的重要 API。</p>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> state = <span class="hljs-title function_">reactive</span>(&#123;<br>  <span class="hljs-attr">count</span>: <span class="hljs-number">0</span><br>&#125;)<br></code></pre></td></tr></table></figure>

<p>从概念上可以理解成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">普通对象<br>   ↓<br>reactive()<br>   ↓<br>Proxy<br>   ↓<br>响应式对象<br></code></pre></td></tr></table></figure>

<p>当：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">count</span><br></code></pre></td></tr></table></figure>

<p>被读取时：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Proxy.get<br>   ↓<br>track()<br></code></pre></td></tr></table></figure>

<p>当：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">count</span>++<br></code></pre></td></tr></table></figure>

<p>被修改时：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Proxy.set<br>   ↓<br>trigger()<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="十七、track：依赖收集"><a href="#十七、track：依赖收集" class="headerlink" title="十七、track：依赖收集"></a>十七、track：依赖收集</h1><p>Vue3 将依赖收集抽象成：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">track</span>()<br></code></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">effect</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(state.<span class="hljs-property">count</span>)<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>执行 <code>effect</code> 时：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">effect()<br>  ↓<br>读取 state.count<br>  ↓<br>Proxy.get<br>  ↓<br>track()<br>  ↓<br>记录当前 effect<br></code></pre></td></tr></table></figure>

<p>此时 Vue 就知道：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">state.count<br>     ↓<br>当前 effect<br></code></pre></td></tr></table></figure>

<p>建立了依赖关系。</p>
<hr>
<h1 id="十八、trigger：触发更新"><a href="#十八、trigger：触发更新" class="headerlink" title="十八、trigger：触发更新"></a>十八、trigger：触发更新</h1><p>当：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">count</span>++<br></code></pre></td></tr></table></figure>

<p>发生时：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Proxy.set<br>   ↓<br>trigger()<br>   ↓<br>找到 count 对应的依赖<br>   ↓<br>重新执行 effect<br></code></pre></td></tr></table></figure>

<p>于是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">读取：<br><br>state.count<br>    ↓<br>track<br>    ↓<br>收集 effect<br><br><br>修改：<br><br>state.count++<br>    ↓<br>trigger<br>    ↓<br>执行 effect<br></code></pre></td></tr></table></figure>

<p>这就是 Vue3 响应式系统最核心的两个过程。</p>
<hr>
<h1 id="十九、effect：副作用函数"><a href="#十九、effect：副作用函数" class="headerlink" title="十九、effect：副作用函数"></a>十九、effect：副作用函数</h1><p>Vue3 中可以使用：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">effect</span>()<br></code></pre></td></tr></table></figure>

<p>来描述一个会受到响应式数据影响的副作用。</p>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> state = <span class="hljs-title function_">reactive</span>(&#123;<br>  <span class="hljs-attr">count</span>: <span class="hljs-number">0</span><br>&#125;)<br><br><span class="hljs-title function_">effect</span>(<span class="hljs-function">() =&gt;</span> &#123;<br>  <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(state.<span class="hljs-property">count</span>)<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>第一次执行：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">effect<br> ↓<br>读取 count<br> ↓<br>track<br> ↓<br>收集依赖<br></code></pre></td></tr></table></figure>

<p>之后：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">count</span>++<br></code></pre></td></tr></table></figure>

<p>就会：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">set<br> ↓<br>trigger<br> ↓<br>找到 effect<br> ↓<br>重新执行<br></code></pre></td></tr></table></figure>

<p>可以把 <code>effect</code> 简单理解成：</p>
<blockquote>
<p><strong>一个会根据响应式数据变化而重新执行的函数。</strong></p>
</blockquote>
<p>Vue 组件渲染、计算属性以及部分其他响应式能力，都建立在类似的副作用机制之上。</p>
<hr>
<h1 id="二十、Vue3-如何保存依赖关系？"><a href="#二十、Vue3-如何保存依赖关系？" class="headerlink" title="二十、Vue3 如何保存依赖关系？"></a>二十、Vue3 如何保存依赖关系？</h1><p>随着响应式数据变多，Vue 需要解决一个问题：</p>
<blockquote>
<p>一个对象的多个属性，分别被哪些 effect 使用？</p>
</blockquote>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> state = <span class="hljs-title function_">reactive</span>(&#123;<br>  <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;Tom&#x27;</span>,<br>  <span class="hljs-attr">age</span>: <span class="hljs-number">18</span><br>&#125;)<br></code></pre></td></tr></table></figure>

<p>可能存在：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">state.name → effectA<br>state.age  → effectB<br></code></pre></td></tr></table></figure>

<p>因此 Vue3 使用了一种非常典型的依赖结构：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">WeakMap<br>   ↓<br> target<br>   ↓<br> Map<br>   ↓<br> key<br>   ↓<br> Set<br>   ↓<br> effect<br></code></pre></td></tr></table></figure>

<p>可以表示成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">WeakMap<br>│<br>└── state<br>     │<br>     └── Map<br>          │<br>          ├── name → Set(effectA)<br>          │<br>          └── age  → Set(effectB)<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="二十一、为什么是-WeakMap-→-Map-→-Set？"><a href="#二十一、为什么是-WeakMap-→-Map-→-Set？" class="headerlink" title="二十一、为什么是 WeakMap → Map → Set？"></a>二十一、为什么是 WeakMap → Map → Set？</h1><p>三个数据结构分别解决不同的问题。</p>
<h2 id="WeakMap"><a href="#WeakMap" class="headerlink" title="WeakMap"></a>WeakMap</h2><p>保存：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">响应式对象<br> ↓<br>依赖信息<br></code></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">state → depsMap<br></code></pre></td></tr></table></figure>

<p>WeakMap 的 key 必须是对象，非常适合用来关联响应式对象。</p>
<hr>
<h2 id="Map"><a href="#Map" class="headerlink" title="Map"></a>Map</h2><p>一个对象可能拥有多个属性：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">state.<span class="hljs-property">name</span><br>state.<span class="hljs-property">age</span><br>state.<span class="hljs-property">count</span><br></code></pre></td></tr></table></figure>

<p>所以需要进一步记录：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">name<br>age<br>count<br></code></pre></td></tr></table></figure>

<p>对应的依赖。</p>
<p>于是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Map<br> ↓<br>属性 key<br> ↓<br>依赖集合<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="Set"><a href="#Set" class="headerlink" title="Set"></a>Set</h2><p>一个属性可能同时被多个 effect 使用：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">state.count<br>   ↓<br>effectA<br>effectB<br>effectC<br></code></pre></td></tr></table></figure>

<p>Set 非常适合保存这些 effect，并且可以避免重复依赖。</p>
<p>因此最终形成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">WeakMap<br>  ↓<br>对象<br>  ↓<br>Map<br>  ↓<br>属性<br>  ↓<br>Set<br>  ↓<br>effect<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="二十二、Vue3-响应式完整流程"><a href="#二十二、Vue3-响应式完整流程" class="headerlink" title="二十二、Vue3 响应式完整流程"></a>二十二、Vue3 响应式完整流程</h1><p>把这些概念组合起来：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">             reactive()<br>                 ↓<br>               Proxy<br>                 ↓<br>       ┌─────────┴─────────┐<br>       ↓                   ↓<br>      get                 set<br>       ↓                   ↓<br>    track()             trigger()<br>       ↓                   ↓<br>   依赖收集             查找依赖<br>       ↓                   ↓<br>WeakMap → Map → Set       effect<br>                           ↓<br>                       重新执行<br></code></pre></td></tr></table></figure>

<p>完整过程就是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 创建响应式对象<br><br>reactive()<br>    ↓<br>Proxy<br><br><br>② 读取数据<br><br>state.count<br>    ↓<br>Proxy.get<br>    ↓<br>track()<br>    ↓<br>记录 effect<br><br><br>③ 修改数据<br><br>state.count++<br>    ↓<br>Proxy.set<br>    ↓<br>trigger()<br>    ↓<br>找到对应 effect<br>    ↓<br>重新执行<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="二十三、Vue2-与-Vue3-的实现对比"><a href="#二十三、Vue2-与-Vue3-的实现对比" class="headerlink" title="二十三、Vue2 与 Vue3 的实现对比"></a>二十三、Vue2 与 Vue3 的实现对比</h1><p>将两套响应式系统放在一起看：</p>
<table>
<thead>
<tr>
<th></th>
<th>Vue2</th>
<th>Vue3</th>
</tr>
</thead>
<tbody><tr>
<td>核心机制</td>
<td><code>Object.defineProperty</code></td>
<td><code>Proxy</code></td>
</tr>
<tr>
<td>代理范围</td>
<td>单个属性</td>
<td>整个对象</td>
</tr>
<tr>
<td>读取拦截</td>
<td>getter</td>
<td>Proxy <code>get</code></td>
</tr>
<tr>
<td>修改拦截</td>
<td>setter</td>
<td>Proxy <code>set</code></td>
</tr>
<tr>
<td>删除属性</td>
<td>需要额外处理</td>
<td>可以拦截</td>
</tr>
<tr>
<td>新增属性</td>
<td>需要额外处理</td>
<td>可以拦截</td>
</tr>
<tr>
<td>数组</td>
<td>重写部分数组方法</td>
<td>Proxy 进行代理</td>
</tr>
<tr>
<td>依赖管理</td>
<td>Dep &#x2F; Watcher</td>
<td>track &#x2F; trigger &#x2F; effect</td>
</tr>
<tr>
<td>依赖结构</td>
<td>以 Dep 为核心</td>
<td>WeakMap → Map → Set</td>
</tr>
<tr>
<td>响应式创建</td>
<td>初始化时遍历</td>
<td>访问时通过 Proxy 代理</td>
</tr>
</tbody></table>
<p>这里最值得注意的并不是 API 名称变化，而是<strong>响应式系统的观察粒度发生了变化</strong>：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Vue2<br>属性<br> ↓<br>getter / setter<br><br>Vue3<br>对象<br> ↓<br>Proxy<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="二十四、为什么-Vue3-不再需要-Vue-set？"><a href="#二十四、为什么-Vue3-不再需要-Vue-set？" class="headerlink" title="二十四、为什么 Vue3 不再需要 Vue.set？"></a>二十四、为什么 Vue3 不再需要 Vue.set？</h1><p>Vue2：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-variable language_">this</span>.$set(user, <span class="hljs-string">&#x27;age&#x27;</span>, <span class="hljs-number">18</span>)<br></code></pre></td></tr></table></figure>

<p>是因为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Object.defineProperty<br>        ↓<br>只能处理已经定义的属性<br></code></pre></td></tr></table></figure>

<p>而 Vue3：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">user.<span class="hljs-property">age</span> = <span class="hljs-number">18</span><br></code></pre></td></tr></table></figure>

<p>本身就可以被：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Proxy.set<br></code></pre></td></tr></table></figure>

<p>拦截。</p>
<p>所以：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Vue2<br><br>新增属性<br> ↓<br>需要额外 API<br> ↓<br>Vue.set<br><br><br>Vue3<br><br>新增属性<br> ↓<br>Proxy.set<br> ↓<br>响应式系统处理<br></code></pre></td></tr></table></figure>

<p>因此 Vue3 移除了 <code>Vue.set</code> 和 <code>Vue.delete</code> 这类 API。</p>
<hr>
<h1 id="二十五、Proxy-并不等于响应式"><a href="#二十五、Proxy-并不等于响应式" class="headerlink" title="二十五、Proxy 并不等于响应式"></a>二十五、Proxy 并不等于响应式</h1><p>这里有一个容易混淆的地方。</p>
<p>很多时候会直接说：</p>
<blockquote>
<p>Vue3 使用 Proxy 实现响应式。</p>
</blockquote>
<p>这句话从整体上来说没有问题，但从原理上看并不完整。</p>
<p>Proxy 本身只是 JavaScript 提供的<strong>代理机制</strong>。</p>
<p>例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> proxy = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Proxy</span>(target, &#123;<br>  <span class="hljs-title function_">get</span>(<span class="hljs-params">target, key</span>) &#123;<br>    <span class="hljs-keyword">return</span> target[key]<br>  &#125;<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>这并不会自动产生响应式。</p>
<p>Vue3 真正的响应式系统是多个机制组合起来的：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Proxy<br>  ↓<br>拦截对象操作<br>  ↓<br>track / trigger<br>  ↓<br>依赖管理<br>  ↓<br>effect<br>  ↓<br>重新执行副作用<br></code></pre></td></tr></table></figure>

<p>所以更准确地说：</p>
<blockquote>
<p><strong>Vue3 使用 Proxy 作为响应式系统的拦截基础，再通过 track、trigger 和 effect 建立完整的依赖追踪机制。</strong></p>
</blockquote>
<hr>
<h1 id="二十六、从-defineProperty-到-Proxy"><a href="#二十六、从-defineProperty-到-Proxy" class="headerlink" title="二十六、从 defineProperty 到 Proxy"></a>二十六、从 defineProperty 到 Proxy</h1><p>如果把 Vue2 到 Vue3 的变化抽象一下，可以看到一个非常清晰的演进过程。</p>
<h3 id="Vue2"><a href="#Vue2" class="headerlink" title="Vue2"></a>Vue2</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">对象<br> ↓<br>遍历属性<br> ↓<br>defineProperty<br> ↓<br>getter / setter<br> ↓<br>Dep<br> ↓<br>Watcher<br></code></pre></td></tr></table></figure>

<p>这种方式的问题是：</p>
<blockquote>
<p>需要提前知道有哪些属性。</p>
</blockquote>
<hr>
<h3 id="Vue3"><a href="#Vue3" class="headerlink" title="Vue3"></a>Vue3</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">对象<br> ↓<br>Proxy<br> ↓<br>运行时拦截操作<br> ↓<br>track / trigger<br> ↓<br>effect<br></code></pre></td></tr></table></figure>

<p>Proxy 让响应式系统从：</p>
<blockquote>
<p><strong>提前对属性进行处理</strong></p>
</blockquote>
<p>变成了：</p>
<blockquote>
<p><strong>运行时对对象操作进行拦截。</strong></p>
</blockquote>
<p>这也是 Vue3 响应式设计变化中非常重要的一点。</p>
<hr>
<h1 id="二十七、两套系统的核心思想其实没有改变"><a href="#二十七、两套系统的核心思想其实没有改变" class="headerlink" title="二十七、两套系统的核心思想其实没有改变"></a>二十七、两套系统的核心思想其实没有改变</h1><p>虽然 Vue2 和 Vue3 使用了不同的技术实现，但响应式系统最核心的思想其实一直没有变化：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">       数据<br>        │<br>    ┌───┴───┐<br>    ↓       ↓<br>   读取     修改<br>    ↓       ↓<br>收集依赖   触发更新<br>    ↓       ↓<br>   依赖 ←───┘<br></code></pre></td></tr></table></figure>

<p>变化的是实现方式：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Vue2<br><br>Object.defineProperty<br>        ↓<br>      getter<br>        ↓<br>       Dep<br>        ↓<br>     Watcher<br>        ↓<br>      update<br></code></pre></td></tr></table></figure>

<p>Vue3：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Proxy<br>  ↓<br>get<br>  ↓<br>track<br>  ↓<br>effect<br><br>Proxy<br>  ↓<br>set<br>  ↓<br>trigger<br>  ↓<br>effect<br></code></pre></td></tr></table></figure>

<p>因此，理解 Vue 响应式时，与其记住大量 API，不如先抓住这条主线：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">读取 → 收集依赖<br>修改 → 触发依赖<br></code></pre></td></tr></table></figure>

<hr>
<h1 id="二十八、总结"><a href="#二十八、总结" class="headerlink" title="二十八、总结"></a>二十八、总结</h1><p>Vue2 到 Vue3 的响应式变化，可以浓缩成一句话：</p>
<blockquote>
<p><strong>Vue2 基于 <code>Object.defineProperty</code> 对对象属性进行劫持，Vue3 则通过 <code>Proxy</code> 对整个对象进行代理，并重新设计了依赖追踪机制。</strong></p>
</blockquote>
<p>Vue2 的核心结构：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Object.defineProperty<br>        ↓<br>getter / setter<br>        ↓<br>Dep<br>        ↓<br>Watcher<br></code></pre></td></tr></table></figure>

<p>Vue3 的核心结构：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Proxy<br> ↓<br>get / set<br> ↓<br>track / trigger<br> ↓<br>effect<br> ↓<br>WeakMap → Map → Set<br></code></pre></td></tr></table></figure>

<p>而两者最终解决的都是同一个问题：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">数据发生变化<br>      ↓<br>找到依赖该数据的逻辑<br>      ↓<br>重新执行<br>      ↓<br>完成视图更新<br></code></pre></td></tr></table></figure>

<p>从这个角度来看，Vue3 的响应式升级并不是简单的 API 替换，而是从<strong>属性级别的数据劫持</strong>逐渐演进到了<strong>对象级别的运行时代理和依赖追踪</strong>。</p>
<p>这也为 Vue3 后续的 <code>ref</code>、<code>reactive</code>、<code>computed</code>、<code>watch</code> 以及 Composition API 等能力提供了更加统一的响应式基础。</p>
<hr>
]]></content>
      <categories>
        <category>Vue</category>
        <category>源码</category>
      </categories>
      <tags>
        <tag>Vue3</tag>
        <tag>Vue2</tag>
        <tag>响应式</tag>
        <tag>Proxy</tag>
        <tag>Object.defineProperty</tag>
      </tags>
  </entry>
  <entry>
    <title>TypeScript 进阶地图：从「会用」到「会设计类型」</title>
    <url>/2026/09/10/typescript-advanced-map/</url>
    <content><![CDATA[<p>先对齐一个观察：团队里 TS 写了两年的人，多数停在「会用」——变量有标注、接口有 interface、组件 props 有类型，遇到搞不定的场景一个 <code>any</code> 逃之夭夭。这不是姿势问题，是<strong>没有完成一次视角切换</strong>：</p>
<blockquote>
<p><strong>会用 TS 的人把类型当「描述」：给已经写好的代码贴标签。</strong><br><strong>会设计类型的人把类型当「约束」：先想清楚问题的合法状态空间，让非法状态在类型层面就无法表示。</strong></p>
</blockquote>
<p>这篇就把从前者到后者的路径画成一张地图。结论先放这：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">「会用」→「会设计」不是学更多语法，而是换一个问题：<br>  从「这个值是什么类型」 换成 「这个类型允许哪些值存在」<br></code></pre></td></tr></table></figure>

<span id="more"></span>

<h2 id="一、分界线长什么样：一个所有前端都写过的例子"><a href="#一、分界线长什么样：一个所有前端都写过的例子" class="headerlink" title="一、分界线长什么样：一个所有前端都写过的例子"></a>一、分界线长什么样：一个所有前端都写过的例子</h2><p>请求状态。三个字段的写法人人写过：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// 「会用」版：三个独立字段</span><br><span class="hljs-keyword">interface</span> <span class="hljs-title class_">RequestState</span>&lt;T&gt; &#123;<br>  <span class="hljs-attr">data</span>: T | <span class="hljs-literal">null</span><br>  <span class="hljs-attr">error</span>: <span class="hljs-title class_">Error</span> | <span class="hljs-literal">null</span><br>  <span class="hljs-attr">loading</span>: <span class="hljs-built_in">boolean</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>看起来无害，算一下状态空间：<code>data</code> 两种 × <code>error</code> 两种 × <code>loading</code> 两种 &#x3D; <strong>8 种组合，其中 4 种是非法的</strong>——<code>loading: true</code> 的同时还挂着 <code>error</code>？<code>data</code> 和 <code>error</code> 同时有值？类型系统全都放行，于是每个消费组件都得自己写防御：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">if</span> (state.<span class="hljs-property">loading</span> &amp;&amp; !state.<span class="hljs-property">error</span> &amp;&amp; state.<span class="hljs-property">data</span> === <span class="hljs-literal">null</span>) &#123; ... &#125; <span class="hljs-comment">// 防御性条件越堆越长</span><br></code></pre></td></tr></table></figure>

<p>「会设计」的写法是把状态空间直接建进类型：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// 「会设计」版：判别联合（discriminated union）</span><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">RequestState</span>&lt;T&gt; =<br>  | &#123; <span class="hljs-attr">status</span>: <span class="hljs-string">&#x27;idle&#x27;</span> &#125;<br>  | &#123; <span class="hljs-attr">status</span>: <span class="hljs-string">&#x27;loading&#x27;</span> &#125;<br>  | &#123; <span class="hljs-attr">status</span>: <span class="hljs-string">&#x27;success&#x27;</span>; <span class="hljs-attr">data</span>: T &#125;<br>  | &#123; <span class="hljs-attr">status</span>: <span class="hljs-string">&#x27;error&#x27;</span>; <span class="hljs-attr">error</span>: <span class="hljs-title class_">Error</span> &#125;<br></code></pre></td></tr></table></figure>

<p>恰好 4 个成员、每个成员携带且仅携带自己需要的字段，非法组合<strong>不是被运行时判断挡住，而是根本无法构造</strong>。这就是那句名言的意思——<em>make illegal states unrepresentable</em>。</p>
<p>记住这个例子，下面每往上一层，都是在给这类设计提供工具。</p>
<h2 id="二、地图总览"><a href="#二、地图总览" class="headerlink" title="二、地图总览"></a>二、地图总览</h2><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">┌─────────────────────────────────────────────────────────┐<br>│ ⑤ 设计层   判别联合 / Branded Types / 类型驱动开发        │ ← 让非法状态不可表示<br>│ ④ 边界层   unknown 治理 / 运行时守卫 / 声明合并           │ ← 给系统砌墙<br>│ ③ 推导层   条件类型 / infer / 模板字符串类型              │ ← 类型级模式匹配<br>│ ② 组合层   泛型抽象 / keyof / 映射类型                   │ ← 类型级函数<br>│ ① 收窄层   控制流分析 / 类型守卫 / satisfies / as const  │ ← 让推导跑起来<br>└─────────────────────────────────────────────────────────┘<br></code></pre></td></tr></table></figure>

<p>五层的关系是<strong>递进依赖</strong>：不掌握收窄，判别联合的 switch 写不完整；不懂 keyof，泛型约束就没有表达力；不理解条件类型，Omit 这种日常工具类型永远是个黑盒。下面逐层展开。</p>
<h2 id="三、①-收窄层：把类型推导当成队友，而不是对手"><a href="#三、①-收窄层：把类型推导当成队友，而不是对手" class="headerlink" title="三、① 收窄层：把类型推导当成队友，而不是对手"></a>三、① 收窄层：把类型推导当成队友，而不是对手</h2><p>这一层的核心认知：<strong>TS 的类型检查不是「逐行标注」，而是跟着控制流走的推导系统</strong>。你写的每个 <code>if</code>、<code>return</code>、<code>??</code> 都在给编译器喂信息。</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> <span class="hljs-title function_">format</span>(<span class="hljs-params"><span class="hljs-attr">value</span>: <span class="hljs-built_in">string</span> | <span class="hljs-built_in">number</span></span>) &#123;<br>  <span class="hljs-keyword">if</span> (<span class="hljs-keyword">typeof</span> value === <span class="hljs-string">&#x27;string&#x27;</span>) &#123;<br>    <span class="hljs-keyword">return</span> value.<span class="hljs-title function_">trim</span>()          <span class="hljs-comment">// 这里已经被收窄为 string</span><br>  &#125;<br>  <span class="hljs-keyword">return</span> value.<span class="hljs-title function_">toFixed</span>(<span class="hljs-number">2</span>)        <span class="hljs-comment">// 剩余分支自动收窄为 number</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>进阶者要掌握的四个抓手：</p>
<p><strong>1. 判别联合 + <code>never</code> 穷尽检查</strong>。第一节的状态类型，消费端这样写：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> <span class="hljs-title function_">render</span>(<span class="hljs-params"><span class="hljs-attr">state</span>: <span class="hljs-title class_">RequestState</span>&lt;<span class="hljs-title class_">User</span>[]&gt;</span>) &#123;<br>  <span class="hljs-keyword">switch</span> (state.<span class="hljs-property">status</span>) &#123;<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;idle&#x27;</span>:    <span class="hljs-keyword">return</span> <span class="hljs-title function_">placeholder</span>()<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;loading&#x27;</span>: <span class="hljs-keyword">return</span> <span class="hljs-title function_">spinner</span>()<br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;success&#x27;</span>: <span class="hljs-keyword">return</span> <span class="hljs-title function_">list</span>(state.<span class="hljs-property">data</span>)      <span class="hljs-comment">// 此分支里 data 一定存在</span><br>    <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;error&#x27;</span>:   <span class="hljs-keyword">return</span> <span class="hljs-title function_">errorBox</span>(state.<span class="hljs-property">error</span>)<br>    <span class="hljs-attr">default</span>: &#123;<br>      <span class="hljs-keyword">const</span> <span class="hljs-attr">_exhaustive</span>: <span class="hljs-built_in">never</span> = state           <span class="hljs-comment">// 漏写任何分支，这一行编译报错</span><br>      <span class="hljs-keyword">return</span> _exhaustive<br>    &#125;<br>  &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>never</code> 是空集——只有所有可能都被前面分支吃掉，<code>state</code> 才可能被收窄成 <code>never</code>。这个模式的价值在于：<strong>将来给联合加一个新状态，所有消费处的漏改会在编译期集体爆炸</strong>，而不是上线后白屏一处。</p>
<p><strong>2. 自定义守卫：<code>x is T</code></strong>。当收窄逻辑复杂到编译器推不出来，就把判断封装成语义化的守卫函数：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> <span class="hljs-title function_">isApiError</span>(<span class="hljs-params"><span class="hljs-attr">x</span>: <span class="hljs-built_in">unknown</span></span>): x is &#123; <span class="hljs-attr">code</span>: <span class="hljs-built_in">number</span>; <span class="hljs-attr">message</span>: <span class="hljs-built_in">string</span> &#125; &#123;<br>  <span class="hljs-keyword">return</span> <span class="hljs-keyword">typeof</span> x === <span class="hljs-string">&#x27;object&#x27;</span> &amp;&amp; x !== <span class="hljs-literal">null</span><br>      &amp;&amp; <span class="hljs-string">&#x27;code&#x27;</span> <span class="hljs-keyword">in</span> x &amp;&amp; <span class="hljs-string">&#x27;message&#x27;</span> <span class="hljs-keyword">in</span> x<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>3. <code>satisfies</code>：校验但不拓宽</strong>（TS 4.9+）。标注和推导之间的第三条路：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">const</span> theme = &#123;<br>  <span class="hljs-attr">dark</span>:  &#123; <span class="hljs-attr">bg</span>: <span class="hljs-string">&#x27;#111&#x27;</span>, <span class="hljs-attr">fg</span>: <span class="hljs-string">&#x27;#eee&#x27;</span> &#125;,<br>  <span class="hljs-attr">light</span>: &#123; <span class="hljs-attr">bg</span>: <span class="hljs-string">&#x27;#eee&#x27;</span>, <span class="hljs-attr">fg</span>: <span class="hljs-string">&#x27;#111&#x27;</span> &#125;,<br>&#125; <span class="hljs-keyword">satisfies</span> <span class="hljs-title class_">Record</span>&lt;<span class="hljs-built_in">string</span>, &#123; <span class="hljs-attr">bg</span>: <span class="hljs-built_in">string</span>; <span class="hljs-attr">fg</span>: <span class="hljs-built_in">string</span> &#125;&gt;<br><br>theme.<span class="hljs-property">dark</span>.<span class="hljs-property">bg</span>   <span class="hljs-comment">// ✅ 若写成「: Record&lt;...&gt;」标注，这里会因键被拓宽而报错</span><br></code></pre></td></tr></table></figure>

<p><strong>4. <code>as const</code></strong>：把推导从「宽松的容器」切换到「精确的字面量」，是后面模板字符串类型和 branded 类型的前置条件。</p>
<h2 id="四、②-组合层：泛型不是「类型的参数」，是类型级的函数"><a href="#四、②-组合层：泛型不是「类型的参数」，是类型级的函数" class="headerlink" title="四、② 组合层：泛型不是「类型的参数」，是类型级的函数"></a>四、② 组合层：泛型不是「类型的参数」，是类型级的函数</h2><p>初学者对泛型的理解停在「调用时要传一个类型进去」，这是把它当语法糖。「会设计」的理解是：<strong>泛型是抽象在类型世界的投影——函数值是值的抽象，泛型是类型的抽象</strong>。</p>
<p>分水岭例子，一行看懂 <code>keyof</code> + 索引访问 + 泛型约束的组合拳：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> pick&lt;T, K <span class="hljs-keyword">extends</span> keyof T&gt;(<span class="hljs-attr">obj</span>: T, <span class="hljs-attr">key</span>: K): T[K] &#123;<br>  <span class="hljs-keyword">return</span> obj[key]<br>&#125;<br><br><span class="hljs-keyword">const</span> u = &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">1</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;kxl&#x27;</span>, <span class="hljs-attr">vip</span>: <span class="hljs-literal">true</span> &#125;<br><span class="hljs-title function_">pick</span>(u, <span class="hljs-string">&#x27;name&#x27;</span>)   <span class="hljs-comment">// 类型是 string，key 打错字直接编译报错</span><br></code></pre></td></tr></table></figure>

<p><code>K extends keyof T</code> 读作「K 只能是 T 的键」，<code>T[K]</code> 读作「按 K 查表取值」。三个零件拼起来，就实现了「属性名的类型安全」——这在 JavaScript 里只能靠运行时报错兜底。</p>
<p>这一层要建立的肌肉：</p>
<ul>
<li><strong><code>keyof</code> &#x2F; <code>T[K]</code> &#x2F; <code>typeof x</code></strong>：类型的查表三件套。<code>typeof config</code> 从值反查类型，让配置对象和类型永远不漂移；</li>
<li><strong>映射类型</strong>：<code>{ [K in keyof T]: ... }</code> 是对类型的 for 循环，所有 <code>Partial</code>、<code>Readonly</code> 的本质；</li>
<li><strong>泛型参数能省则省</strong>：能被推导的就不要让调用者手传。<code>pick(u, &#39;name&#39;)</code> 不需要写 <code>pick&lt;typeof u, &#39;name&#39;&gt;(...)</code>——<strong>签名设计的目标是让调用方不可能传错，而不是让调用方传一堆</strong>。</li>
</ul>
<h2 id="五、③-推导层：条件类型-infer，类型级别的模式匹配"><a href="#五、③-推导层：条件类型-infer，类型级别的模式匹配" class="headerlink" title="五、③ 推导层：条件类型 + infer，类型级别的模式匹配"></a>五、③ 推导层：条件类型 + <code>infer</code>，类型级别的模式匹配</h2><p>这一层是「会设计」的分水岭，也是「类型体操」的重灾区。核心就一句话：</p>
<blockquote>
<p><strong>条件类型是类型层面的三元表达式，<code>infer</code> 是它在模式里声明的「捕获变量」。</strong></p>
</blockquote>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">type</span> <span class="hljs-title class_">UnwrapPromise</span>&lt;T&gt; = T <span class="hljs-keyword">extends</span> <span class="hljs-title class_">Promise</span>&lt;infer U&gt; ? U : T<br><span class="hljs-keyword">type</span> <span class="hljs-title class_">Awaited1</span> = <span class="hljs-title class_">UnwrapPromise</span>&lt;<span class="hljs-title class_">Promise</span>&lt;<span class="hljs-built_in">number</span>&gt;&gt;   <span class="hljs-comment">// number</span><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">ElementOf</span>&lt;T&gt; = T <span class="hljs-title function_">extends</span> (infer E)[] ? E : <span class="hljs-built_in">never</span><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">Item</span> = <span class="hljs-title class_">ElementOf</span>&lt;<span class="hljs-built_in">string</span>[]&gt;                  <span class="hljs-comment">// string</span><br></code></pre></td></tr></table></figure>

<p>读法：<code>T extends Promise&lt;infer U&gt;</code> 不是在问「T 是不是 Promise」——<code>infer U</code> 相当于在匹配的<strong>结构里挖一个洞</strong>，把洞里的类型绑到 U 上。这是 <code>ReturnType</code>、<code>Parameters</code> 全家的原理。</p>
<p>理解了这两个零件，<strong>标准库的工具类型就全透明了</strong>，每个都不超过五行：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">type</span> <span class="hljs-title class_">MyPartial</span>&lt;T&gt;  = &#123; [K <span class="hljs-keyword">in</span> keyof T]?: T[K] &#125;<br><span class="hljs-keyword">type</span> <span class="hljs-title class_">MyPick</span>&lt;T, K <span class="hljs-keyword">extends</span> keyof T&gt; = &#123; [P <span class="hljs-keyword">in</span> K]: T[P] &#125;<br><span class="hljs-keyword">type</span> <span class="hljs-title class_">MyReturnType</span>&lt;T&gt; = T <span class="hljs-title function_">extends</span> (...<span class="hljs-attr">args</span>: <span class="hljs-built_in">never</span>[]) =&gt; infer R ? R : <span class="hljs-built_in">never</span><br></code></pre></td></tr></table></figure>

<p>再进半步是<strong>键重映射 + 模板字符串类型</strong>（TS 4.1+），可以生成一族新键名：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">type</span> <span class="hljs-title class_">Getters</span>&lt;T&gt; = &#123;<br>  [K <span class="hljs-keyword">in</span> keyof T <span class="hljs-keyword">as</span> <span class="hljs-string">`get<span class="hljs-subst">$&#123;Capitalize&lt;K &amp; <span class="hljs-built_in">string</span>&gt;&#125;</span>`</span>]: <span class="hljs-function">() =&gt;</span> T[K]<br>&#125;<br><br><span class="hljs-keyword">interface</span> <span class="hljs-title class_">Point</span> &#123; <span class="hljs-attr">x</span>: <span class="hljs-built_in">number</span>; <span class="hljs-attr">y</span>: <span class="hljs-built_in">number</span> &#125;<br><span class="hljs-keyword">type</span> <span class="hljs-title class_">PointGetters</span> = <span class="hljs-title class_">Getters</span>&lt;<span class="hljs-title class_">Point</span>&gt;<br><span class="hljs-comment">// &#123; getX: () =&gt; number; getY: () =&gt; number &#125;</span><br></code></pre></td></tr></table></figure>

<p>这一层还有个必踩的坑要提前知道：<strong>裸类型参数遇到联合会分发</strong>。<code>ElementOf&lt;string[] | number[]&gt;</code> 得到的是 <code>string | number</code>（逐成员求值再合并），不是 <code>string[] | number[]</code> 的元素。想关掉分发，用方括号包一层 <code>[T] extends [never[]] ? ... : ...</code>。很多「条件类型行为诡异」的 bug，拆开看都是分发在暗中生效。</p>
<h2 id="六、④-边界层：系统里最危险的地方，恰好是-TS-管不到的地方"><a href="#六、④-边界层：系统里最危险的地方，恰好是-TS-管不到的地方" class="headerlink" title="六、④ 边界层：系统里最危险的地方，恰好是 TS 管不到的地方"></a>六、④ 边界层：系统里最危险的地方，恰好是 TS 管不到的地方</h2><p>类型只在编译期存在，而系统的边缘——网络响应、<code>JSON.parse</code>、<code>localStorage</code>、第三方 JS——<strong>天然没有类型</strong>。这里的抉择区分了三种人：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">any     = 双向放弃：既不检查我给它，也不检查它给我<br>unknown = 「我知道这里没类型，但我拒绝假装它有」→ 强制收窄后才能用<br>never   = 空集，穷尽检查的哨兵<br></code></pre></td></tr></table></figure>

<p>「会设计」的边界一定是 <code>unknown</code> + 运行时守卫的组合：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> request&lt;T&gt;(<br>  <span class="hljs-attr">url</span>: <span class="hljs-built_in">string</span>,<br>  <span class="hljs-attr">guard</span>: <span class="hljs-function">(<span class="hljs-params"><span class="hljs-attr">x</span>: <span class="hljs-built_in">unknown</span></span>) =&gt;</span> x is T,          <span class="hljs-comment">// 类型安全的最后一公里 = 一个守卫函数</span><br>): <span class="hljs-title class_">Promise</span>&lt;T&gt; &#123;<br>  <span class="hljs-keyword">const</span> <span class="hljs-attr">raw</span>: <span class="hljs-built_in">unknown</span> = <span class="hljs-keyword">await</span> <span class="hljs-title function_">fetch</span>(url).<span class="hljs-title function_">then</span>(<span class="hljs-function"><span class="hljs-params">r</span> =&gt;</span> r.<span class="hljs-title function_">json</span>())<br>  <span class="hljs-keyword">if</span> (!<span class="hljs-title function_">guard</span>(raw)) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">`响应结构不符: <span class="hljs-subst">$&#123;url&#125;</span>`</span>)<br>  <span class="hljs-keyword">return</span> raw<br>&#125;<br><br><span class="hljs-keyword">const</span> isUser = (<span class="hljs-attr">x</span>: <span class="hljs-built_in">unknown</span>): x is <span class="hljs-title class_">User</span> =&gt;<br>  <span class="hljs-keyword">typeof</span> x === <span class="hljs-string">&#x27;object&#x27;</span> &amp;&amp; x !== <span class="hljs-literal">null</span> &amp;&amp; <span class="hljs-string">&#x27;id&#x27;</span> <span class="hljs-keyword">in</span> x &amp;&amp; <span class="hljs-string">&#x27;name&#x27;</span> <span class="hljs-keyword">in</span> x<br><br><span class="hljs-keyword">const</span> user = <span class="hljs-keyword">await</span> <span class="hljs-title function_">request</span>(<span class="hljs-string">&#x27;/api/user&#x27;</span>, isUser)   <span class="hljs-comment">// user: User，且经过验证</span><br></code></pre></td></tr></table></figure>

<p>注意这个设计的妙处：<strong>守卫函数是唯一同时被「运行时」和「类型系统」信任的凭证</strong>——运行时执行它，类型系统认它的返回值签名。后端改了字段，错误在守卫处当场爆炸，而不是渲染层读到 <code>undefined</code> 才炸。</p>
<p>配套的两个习惯：<code>catch (e)</code> 的 <code>e</code> 按 <code>unknown</code> 处理（<code>useUnknownInCatchVariables</code>）；给无类型的第三方包补 <code>.d.ts</code>，用模块扩充（<code>declare module</code>）给已声明类型打补丁。</p>
<h2 id="七、⑤-设计层：把「业务不变量」编码进类型"><a href="#七、⑤-设计层：把「业务不变量」编码进类型" class="headerlink" title="七、⑤ 设计层：把「业务不变量」编码进类型"></a>七、⑤ 设计层：把「业务不变量」编码进类型</h2><p>到这一层，类型不再是代码的附属品，而是<strong>领域建模工具</strong>。两个代表武器：</p>
<p><strong>1. 判别联合建模状态机</strong>。第一节的 <code>RequestState</code> 推广开：向导表单的步骤、支付单的生命周期、组件的生命周期，凡是「状态 + 各状态的专属负载」都该是一个联合。写代码前先画状态转移表，再把表翻译成类型——这就是<strong>类型驱动开发</strong>：类型签名先于实现存在，它是给自己和同事的设计文档。</p>
<p><strong>2. Branded Types：给同构类型发身份证</strong>。TS 是结构化类型系统——<code>string</code> 和 <code>string</code> 永远兼容，于是用户 ID 和订单 ID 可以互相赋值，编译器毫无意见：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">declare</span> <span class="hljs-keyword">const</span> <span class="hljs-attr">brand</span>: unique <span class="hljs-built_in">symbol</span><br><span class="hljs-keyword">type</span> <span class="hljs-title class_">UserId</span>  = <span class="hljs-built_in">string</span> &amp; &#123; <span class="hljs-keyword">readonly</span> [brand]: <span class="hljs-string">&#x27;UserId&#x27;</span> &#125;<br><span class="hljs-keyword">type</span> <span class="hljs-title class_">OrderId</span> = <span class="hljs-built_in">string</span> &amp; &#123; <span class="hljs-keyword">readonly</span> [brand]: <span class="hljs-string">&#x27;OrderId&#x27;</span> &#125;<br><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">getUser</span>(<span class="hljs-params"><span class="hljs-attr">id</span>: <span class="hljs-title class_">UserId</span></span>) &#123; ... &#125;<br><br><span class="hljs-title function_">getUser</span>(orderId)   <span class="hljs-comment">// ❌ 编译报错：结构相同也不行</span><br><span class="hljs-title function_">getUser</span>(<span class="hljs-string">&#x27;123&#x27;</span>)     <span class="hljs-comment">// ❌ 裸字符串也不行</span><br></code></pre></td></tr></table></figure>

<p>唯一合法的进入方式是走「铸造」出口，把校验收拢在一处：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">function</span> <span class="hljs-title function_">toUserId</span>(<span class="hljs-params"><span class="hljs-attr">raw</span>: <span class="hljs-built_in">string</span></span>): <span class="hljs-title class_">UserId</span> &#123;<br>  <span class="hljs-keyword">if</span> (!<span class="hljs-regexp">/^\d+$/</span>.<span class="hljs-title function_">test</span>(raw)) <span class="hljs-keyword">throw</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Error</span>(<span class="hljs-string">&#x27;非法的用户 ID&#x27;</span>)<br>  <span class="hljs-keyword">return</span> raw <span class="hljs-keyword">as</span> <span class="hljs-title class_">UserId</span>            <span class="hljs-comment">// 系统里唯一一个 as</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>从此「传错 ID」这类事故从运行时日志变成编辑器红线。经验法则：<strong>同构类型一旦跨模块流动、且有自己的校验规则，就值得 brand 一下</strong>——ID、毫秒时间戳、已消毒的 HTML、经纬度，都是典型候选。</p>
<h2 id="八、练习路径，和「该在哪停」"><a href="#八、练习路径，和「该在哪停」" class="headerlink" title="八、练习路径，和「该在哪停」"></a>八、练习路径，和「该在哪停」</h2><p>每一层配一个刻意练习方式：</p>
<table>
<thead>
<tr>
<th>层</th>
<th>解锁能力</th>
<th>练习方式</th>
</tr>
</thead>
<tbody><tr>
<td>① 收窄</td>
<td>读懂并信任推导、穷尽检查</td>
<td>把旧代码里的防御性 if 换成判别联合，删掉 null 检查</td>
</tr>
<tr>
<td>② 组合</td>
<td>写出类型安全的工具函数</td>
<td>手写 pick&#x2F;omit&#x2F;restrictTo，不许查资料</td>
</tr>
<tr>
<td>③ 推导</td>
<td>读懂一切工具类型源码</td>
<td><strong>通读 <code>lib.es5.d.ts</code></strong>——最被低估的教材，全部核心工具类型都在里面，每个不超十行</td>
</tr>
<tr>
<td>④ 边界</td>
<td>any 的持续治理</td>
<td>给项目里每个 <code>any</code> 分类：能收窄的收窄、真没型的写守卫、第三方的补声明</td>
</tr>
<tr>
<td>⑤ 设计</td>
<td>用类型承载业务规则</td>
<td>找一个真实的「传错参数」事故，用判别联合或 brand 重构到它不可能再犯</td>
</tr>
</tbody></table>
<p>最后是「度」的问题——这一层往上就是类型体操的深渊（四五行 <code>infer</code> 嵌套、递归类型展开成 AST 变换），要不要走下去？我的判断标准只有一条：</p>
<blockquote>
<p><strong>类型代码没有运行时开销，但有认知开销。一个高级类型值不值得写，看它能不能让调用方「不看实现就知道自己用错了」——做不到这点的类型体操，是在用团队的未来给自己的智商付费。</strong></p>
</blockquote>
<p>团队项目里，⑤ 层的判别联合和 branded 值得推行，③ 层的炫技推导尽量封在底层库里并由注释护航。</p>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>回到开头的分界线。五层地图爬完，变化的不是语法量，是动笔顺序：</p>
<blockquote>
<p><strong>「会用」是写完代码再补类型——类型是给编译器的交代；「会设计」是先画状态空间再写实现——类型是给问题的建模。前者让编译通过，后者让错误无处容身。</strong></p>
</blockquote>
<p>这篇是地图，后面的坑位已经排好：泛型与条件类型的完整实战（一个类型安全的请求封装）、tsconfig 关键配置逐项讲清、存量项目的 any 治理。异步相关的类型边界（<code>Promise&lt;T&gt;</code> 的推导与 <code>await</code> 的 unwrap）可以配合<a href="/2026/09/09/javascript-promise/">《JavaScript Promise》</a>食用。</p>
]]></content>
      <categories>
        <category>前端基础</category>
        <category>typescript</category>
      </categories>
      <tags>
        <tag>typescript</tag>
        <tag>泛型</tag>
        <tag>类型系统</tag>
      </tags>
  </entry>
  <entry>
    <title>虚拟 DOM diff 算法与 key 的正确用法</title>
    <url>/2026/09/09/vue-diff-and-key/</url>
    <content><![CDATA[<h1 id="虚拟-DOM-diff-算法与-key-的正确用法"><a href="#虚拟-DOM-diff-算法与-key-的正确用法" class="headerlink" title="虚拟 DOM diff 算法与 key 的正确用法"></a>虚拟 DOM diff 算法与 key 的正确用法</h1><p>使用 Vue 或 React 时，几乎每个人都写过这样的代码：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">li</span> <span class="hljs-attr">v-for</span>=<span class="hljs-string">&quot;item in list&quot;</span> <span class="hljs-attr">:key</span>=<span class="hljs-string">&quot;item.id&quot;</span>&gt;</span><br>  &#123;&#123; item.name &#125;&#125;<br><span class="hljs-tag">&lt;/<span class="hljs-name">li</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>也几乎每个人都被编辑器警告过：</p>
<blockquote>
<p>Elements in iteration expect to have ‘v-bind:key’ directives.</p>
</blockquote>
<p>于是 <code>key</code> 变成了一种”加上就不报错”的咒语。</p>
<p>但如果追问几个问题，很多人就答不上来了：</p>
<blockquote>
<p>key 到底参与了什么过程？</p>
<p>为什么不能用 index 当 key？</p>
<p>为什么用 <code>Math.random()</code> 当 key 反而更糟？</p>
</blockquote>
<p>这些问题全部指向同一个东西：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">diff 算法<br></code></pre></td></tr></table></figure>

<p>本文从虚拟 DOM 的更新流程开始，先理解 diff 为什么存在、它做了哪些简化，再看 Vue2 的双端比较和 Vue3 的最长递增子序列，最后回到 key 的正确用法。</p>
<hr>
<h2 id="一、diff-算法解决了什么问题？"><a href="#一、diff-算法解决了什么问题？" class="headerlink" title="一、diff 算法解决了什么问题？"></a>一、diff 算法解决了什么问题？</h2><p>框架普遍采用虚拟 DOM 的更新方式：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">数据变化<br>   ↓<br>重新执行渲染逻辑<br>   ↓<br>生成一棵新的虚拟 DOM 树<br>   ↓<br>和旧的虚拟 DOM 树对比<br>   ↓<br>计算出最小的真实 DOM 操作<br>   ↓<br>更新页面<br></code></pre></td></tr></table></figure>

<p>其中”新旧对比”这一步，就是 diff：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧虚拟 DOM 树     新虚拟 DOM 树<br>     A                 A<br>   ┌─┴─┐             ┌─┴─┐<br>   B   C             B   D<br>                       └──→ C 去哪了？<br></code></pre></td></tr></table></figure>

<p>diff 的目标不是”找出所有不同”，而是：</p>
<blockquote>
<p><strong>用尽可能少的真实 DOM 操作，把旧树变成新树。</strong></p>
</blockquote>
<p>为什么如此在意 DOM 操作的数量？</p>
<p>因为创建和修改真实 DOM 的代价，远高于操作普通 JavaScript 对象：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">操作虚拟 DOM（普通 JS 对象）<br>        ↓<br>    速度快<br><br>操作真实 DOM<br>        ↓<br>可能引发样式计算、布局、绘制<br>        ↓<br>    代价高<br></code></pre></td></tr></table></figure>

<p>所以一次典型的更新流程是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 数据变化，生成新 vnode<br>② 新旧 vnode 做 diff（纯 JS 计算，便宜）<br>③ 根据 diff 结果，只对发生变化的部分做真实 DOM 操作（昂贵）<br></code></pre></td></tr></table></figure>

<p>diff 本质上是一个<strong>性能优化手段</strong>：用廉价的 JS 计算换昂贵的 DOM 操作。</p>
<hr>
<h2 id="二、为什么不比较整棵树？"><a href="#二、为什么不比较整棵树？" class="headerlink" title="二、为什么不比较整棵树？"></a>二、为什么不比较整棵树？</h2><p>如果把两棵树当作任意的树结构做完整的”树编辑距离”对比，最优解法的时间复杂度是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">O(n³)<br></code></pre></td></tr></table></figure>

<p>1000 个节点意味着十亿级别的比较，完全不可用。</p>
<p>所以 Vue 和 React 都没有做”完整的树对比”，而是基于几个经验假设，把复杂度降到了线性：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">O(n³) → O(n)<br></code></pre></td></tr></table></figure>

<p>这些假设来自对真实页面更新模式的观察。</p>
<hr>
<h2 id="三、diff-的三条基本策略"><a href="#三、diff-的三条基本策略" class="headerlink" title="三、diff 的三条基本策略"></a>三、diff 的三条基本策略</h2><h3 id="策略一：只做同层比较"><a href="#策略一：只做同层比较" class="headerlink" title="策略一：只做同层比较"></a>策略一：只做同层比较</h3><p>diff 只会比较同一层级的节点：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">   旧树                新树<br>    ul                  ul<br>  ┌──┴──┐             ┌──┴──┐<br>  li    li            li    span<br>  ↓     ↓             ↓      ↓<br>同层比较 ✓           同层比较：li vs span<br>                           ↓<br>                       类型不同<br>                           ↓<br>                     不再深入对比子树<br></code></pre></td></tr></table></figure>

<p>跨层级的节点移动，会被当作：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧位置删除 + 新位置创建<br></code></pre></td></tr></table></figure>

<p>而不是”移动”。</p>
<p>这看起来不够聪明，但背后的判断是：</p>
<blockquote>
<p><strong>真实页面中，跨层级移动节点是极少见的。</strong></p>
</blockquote>
<p>为极少见的场景把复杂度抬回 O(n³)，不划算。</p>
<h3 id="策略二：类型不同，直接重建"><a href="#策略二：类型不同，直接重建" class="headerlink" title="策略二：类型不同，直接重建"></a>策略二：类型不同，直接重建</h3><p>如果新旧节点的标签类型不同：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-comment">&lt;!-- 旧 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>hello<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span><br><br><span class="hljs-comment">&lt;!-- 新 --&gt;</span><br><span class="hljs-tag">&lt;<span class="hljs-name">span</span>&gt;</span>hello<span class="hljs-tag">&lt;/<span class="hljs-name">span</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>diff 不会去对比 <code>&lt;div&gt;</code> 和 <code>&lt;span&gt;</code> 内部的差异，而是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧 div 及其整个子树<br>        ↓<br>      删除<br><br>新 span 及其整个子树<br>        ↓<br>      创建<br></code></pre></td></tr></table></figure>

<p>理由同样来自经验：</p>
<blockquote>
<p><strong>不同类型的元素，渲染出的内容通常完全不同，细对比不如重建。</strong></p>
</blockquote>
<h3 id="策略三：用-key-标识节点"><a href="#策略三：用-key-标识节点" class="headerlink" title="策略三：用 key 标识节点"></a>策略三：用 key 标识节点</h3><p>对于同一层级的列表：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">li</span> <span class="hljs-attr">v-for</span>=<span class="hljs-string">&quot;item in list&quot;</span> <span class="hljs-attr">:key</span>=<span class="hljs-string">&quot;item.id&quot;</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>key 用来回答 diff 中最关键的问题：</p>
<blockquote>
<p><strong>新列表里的这个节点，是不是旧列表里那个节点的”同一个”？</strong></p>
</blockquote>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">key 相同 + 类型相同<br>        ↓<br>    认为是同一个节点<br>        ↓<br>    复用旧节点，只更新差异<br>    （必要时移动位置）<br></code></pre></td></tr></table></figure>

<p>这三条策略合在一起，把”任意的树对比”简化成了”同层级的线性扫描”。</p>
<hr>
<h2 id="四、如何判断两个节点”相同”？"><a href="#四、如何判断两个节点”相同”？" class="headerlink" title="四、如何判断两个节点”相同”？"></a>四、如何判断两个节点”相同”？</h2><p>Vue 源码里有一个函数：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">sameVnode</span>(a, b)<br></code></pre></td></tr></table></figure>

<p>它决定两个 vnode 能否复用。简化后的核心判断是：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">sameVnode</span>(<span class="hljs-params">a, b</span>) &#123;<br>  <span class="hljs-keyword">return</span> (<br>    a.<span class="hljs-property">key</span> === b.<span class="hljs-property">key</span> &amp;&amp;   <span class="hljs-comment">// key 相同</span><br>    a.<span class="hljs-property">tag</span> === b.<span class="hljs-property">tag</span>      <span class="hljs-comment">// 标签相同</span><br>  )<br>&#125;<br></code></pre></td></tr></table></figure>

<p>实际源码还包含一些细节，比如 <code>&lt;input&gt;</code> 会额外比较 type：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">(tag === <span class="hljs-string">&#x27;input&#x27;</span> &amp;&amp; a.<span class="hljs-property">type</span> === b.<span class="hljs-property">type</span>)<br></code></pre></td></tr></table></figure>

<p>但主线就两条：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">key 相同<br>  +<br>标签相同<br>    ↓<br>判定为同一节点<br>    ↓<br>进入复用流程（patchVnode）<br></code></pre></td></tr></table></figure>

<p>注意一个容易忽略的点：</p>
<blockquote>
<p><strong>key 是节点身份的一部分，而不是附加属性。</strong></p>
</blockquote>
<p>key 不同，哪怕标签、内容全都一样，也会被认为是两个不同的节点。</p>
<hr>
<h2 id="五、没有-key-的世界：就地复用"><a href="#五、没有-key-的世界：就地复用" class="headerlink" title="五、没有 key 的世界：就地复用"></a>五、没有 key 的世界：就地复用</h2><p>先看反例：如果不写 key，diff 会怎么做？</p>
<p>没有 key 时，Vue 对子节点的处理策略是<strong>就地复用（in-place patch）</strong>：</p>
<p>按索引一一配对：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧：  li[0]   li[1]   li[2]<br>       ↓       ↓       ↓<br>新：  li[0]   li[1]<br><br>索引 0 → 索引 0<br>索引 1 → 索引 1<br>索引 2 → 没有配对，删除<br></code></pre></td></tr></table></figure>

<p>也就是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">不看节点是谁，只看排在第几个<br></code></pre></td></tr></table></figure>

<p>对于”纯展示、顺序永远不变”的列表，这样做的效率非常高——改哪个索引就 patch 哪个。</p>
<p>但一旦列表发生<strong>头部的增删、排序、过滤</strong>，问题就来了。</p>
<p>假设列表从：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">A  B  C<br></code></pre></td></tr></table></figure>

<p>变成了：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">B  C<br></code></pre></td></tr></table></figure>

<p>（头部删除了 A）</p>
<p>就地复用的处理方式是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">索引 0：A 的节点 → patch 成 B 的内容<br>索引 1：B 的节点 → patch 成 C 的内容<br>索引 2：C 的节点 → 删除<br></code></pre></td></tr></table></figure>

<p>本来只需要”删掉第一个节点”，现在变成了：</p>
<blockquote>
<p><strong>剩下每个节点都要 patch 一遍，还要多删一个节点。</strong></p>
</blockquote>
<p>方向对了，但全在原地打补丁。key 要解决的，正是这个问题。</p>
<hr>
<h2 id="六、key-到底是什么？"><a href="#六、key-到底是什么？" class="headerlink" title="六、key 到底是什么？"></a>六、key 到底是什么？</h2><p>有了前面的铺垫，可以给 key 一个准确的定义了：</p>
<blockquote>
<p><strong>key 是 vnode 的身份标识，diff 用它在新旧两组子节点之间建立配对关系。</strong></p>
</blockquote>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧子节点                新子节点<br>key=1  A               key=2  B<br>key=2  B               key=3  C<br>key=3  C               key=1  A<br><br>diff：不看位置，看 key<br><br>key=1 → key=1  复用，移动<br>key=2 → key=2  复用，移动<br>key=3 → key=3  复用，移动<br></code></pre></td></tr></table></figure>

<p>key 带来的能力有两个：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">能力一：复用<br>  key 相同的节点只做属性/子节点的增量更新，<br>  而不是销毁重建<br><br>能力二：移动<br>  节点在列表中的位置变了，<br>  diff 能识别出&quot;这是移动&quot;，而不是&quot;删除 + 新建&quot;<br></code></pre></td></tr></table></figure>

<p>这也是为什么 key 必须满足两个条件：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 唯一：同一列表内不能重复<br>② 稳定：每次渲染同一个数据应得到同一个 key<br></code></pre></td></tr></table></figure>

<p>key 不是给开发者看的，是给 diff 算法做<strong>节点匹配</strong>用的。</p>
<p>接下来看 diff 算法具体怎么利用 key。</p>
<hr>
<h2 id="七、Vue2-的-diff：双端比较"><a href="#七、Vue2-的-diff：双端比较" class="headerlink" title="七、Vue2 的 diff：双端比较"></a>七、Vue2 的 diff：双端比较</h2><p>Vue2 对两组子节点（oldChildren &#x2F; newChildren）的 diff，核心函数是：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-title function_">updateChildren</span>()<br></code></pre></td></tr></table></figure>

<p>它维护了四个指针：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">let</span> oldStartIdx = <span class="hljs-number">0</span>                 <span class="hljs-comment">// 旧列表头部</span><br><span class="hljs-keyword">let</span> oldEndIdx = oldChildren.<span class="hljs-property">length</span> - <span class="hljs-number">1</span>  <span class="hljs-comment">// 旧列表尾部</span><br><br><span class="hljs-keyword">let</span> newStartIdx = <span class="hljs-number">0</span>                 <span class="hljs-comment">// 新列表头部</span><br><span class="hljs-keyword">let</span> newEndIdx = newChildren.<span class="hljs-property">length</span> - <span class="hljs-number">1</span>  <span class="hljs-comment">// 新列表尾部</span><br></code></pre></td></tr></table></figure>

<p>以及四个对应的节点：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">oldStartVnode   <span class="hljs-comment">// 旧头</span><br>oldEndVnode     <span class="hljs-comment">// 旧尾</span><br>newStartVnode   <span class="hljs-comment">// 新头</span><br>newEndVnode     <span class="hljs-comment">// 新尾</span><br></code></pre></td></tr></table></figure>

<p>示意图：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧： [oldStart ... oldEnd]<br>新： [newStart ... newEnd]<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="八、双端比较的四次命中"><a href="#八、双端比较的四次命中" class="headerlink" title="八、双端比较的四次命中"></a>八、双端比较的四次命中</h2><p>每一轮比较，按顺序尝试四种配对：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① oldStart vs newStart   （头对头）<br>② oldEnd   vs newEnd     （尾对尾）<br>③ oldStart vs newEnd     （旧头对 新尾）<br>④ oldEnd   vs newStart   （旧尾对 新头）<br></code></pre></td></tr></table></figure>

<h3 id="①-头对头命中"><a href="#①-头对头命中" class="headerlink" title="① 头对头命中"></a>① 头对头命中</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧： [A, B, C]<br>新： [A, B, D]<br><br>A vs A 命中<br>   ↓<br>patchVnode(A, A)<br>   ↓<br>双头指针各前进一步<br></code></pre></td></tr></table></figure>

<h3 id="②-尾对尾命中"><a href="#②-尾对尾命中" class="headerlink" title="② 尾对尾命中"></a>② 尾对尾命中</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧： [A, B, C]<br>新： [D, B, C]<br><br>C vs C 命中<br>   ↓<br>patchVnode<br>   ↓<br>双尾指针各后退一步<br></code></pre></td></tr></table></figure>

<h3 id="③-旧头对上新尾：节点被移到了末尾"><a href="#③-旧头对上新尾：节点被移到了末尾" class="headerlink" title="③ 旧头对上新尾：节点被移到了末尾"></a>③ 旧头对上新尾：节点被移到了末尾</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧： [A, B, C]<br>新： [B, C, A]<br><br>A（旧头）vs A（新尾）命中<br>   ↓<br>patchVnode<br>   ↓<br>把 A 移动到旧尾之后<br>   ↓<br>oldStartIdx++，newEndIdx--<br></code></pre></td></tr></table></figure>

<h3 id="④-旧尾对上新头：节点被移到了开头"><a href="#④-旧尾对上新头：节点被移到了开头" class="headerlink" title="④ 旧尾对上新头：节点被移到了开头"></a>④ 旧尾对上新头：节点被移到了开头</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧： [A, B, C]<br>新： [C, A, B]<br><br>C（旧尾）vs C（新头）命中<br>   ↓<br>patchVnode<br>   ↓<br>把 C 移动到旧头之前<br>   ↓<br>oldEndIdx--，newStartIdx++<br></code></pre></td></tr></table></figure>

<p>四种命中共用一个前提：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">sameVnode() 成立（key 相同 + 标签相同）<br></code></pre></td></tr></table></figure>

<p>任何一种命中后，都回到循环开头，进行下一轮。</p>
<hr>
<h2 id="九、四头都配不上：keyMap-兜底"><a href="#九、四头都配不上：keyMap-兜底" class="headerlink" title="九、四头都配不上：keyMap 兜底"></a>九、四头都配不上：keyMap 兜底</h2><p>如果四次比较都没命中：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧： [D, E, A, B, C]<br>新： [E, C, D, A]<br><br>E 不在旧列表的头，也不在尾<br>        ↓<br>四种快速路径全部失效<br></code></pre></td></tr></table></figure>

<p>这时 Vue2 会用 key 建立索引表：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-comment">// 旧子节点：key → 索引</span><br>keyMap = &#123;<br>  <span class="hljs-attr">D</span>: <span class="hljs-number">0</span>,<br>  <span class="hljs-attr">E</span>: <span class="hljs-number">1</span>,<br>  <span class="hljs-attr">A</span>: <span class="hljs-number">2</span>,<br>  <span class="hljs-attr">B</span>: <span class="hljs-number">3</span>,<br>  <span class="hljs-attr">C</span>: <span class="hljs-number">4</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>然后拿<strong>新头节点</strong>的 key 去查：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">newStart = E<br>    ↓<br>keyMap[&#x27;E&#x27;] = 1<br>    ↓<br>旧列表索引 1 的节点也是 E<br>    ↓<br>sameVnode 成立<br>    ↓<br>patchVnode + 把 E 移动到旧头之前<br></code></pre></td></tr></table></figure>

<p>如果 keyMap 里查不到：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">newStart 的 key 在旧列表中不存在<br>        ↓<br>这是一个全新节点<br>        ↓<br>createElm 直接创建并插入<br></code></pre></td></tr></table></figure>

<p>这一步是 key 最直接的用武之地：</p>
<blockquote>
<p><strong>没有 key，keyMap 无从建起，兜底路径整个失效。</strong></p>
</blockquote>
<hr>
<h2 id="十、收尾：多退少补"><a href="#十、收尾：多退少补" class="headerlink" title="十、收尾：多退少补"></a>十、收尾：多退少补</h2><p>指针不断靠近，循环结束的条件是某一侧的头指针越过尾指针：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">while</span> (oldStartIdx &lt;= oldEndIdx &amp;&amp; newStartIdx &lt;= newEndIdx)<br></code></pre></td></tr></table></figure>

<p>结束后只剩两种情况：</p>
<h3 id="旧的先用完：新节点有剩余"><a href="#旧的先用完：新节点有剩余" class="headerlink" title="旧的先用完：新节点有剩余"></a>旧的先用完：新节点有剩余</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧： []        （已全部处理）<br>新： [X, Y]    （还没处理）<br><br>        ↓<br>批量创建 X、Y 并插入<br></code></pre></td></tr></table></figure>

<h3 id="新的先用完：旧节点有剩余"><a href="#新的先用完：旧节点有剩余" class="headerlink" title="新的先用完：旧节点有剩余"></a>新的先用完：旧节点有剩余</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧： [M, N]    （还没处理）<br>新： []        （已全部处理）<br><br>        ↓<br>批量删除 M、N<br></code></pre></td></tr></table></figure>

<p>到这里，Vue2 的 diff 就完整了：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">updateChildren<br>   ↓<br>头头 / 尾尾 / 头尾 / 尾头 四次快速比较<br>   ↓<br>都不命中 → keyMap 兜底<br>   ↓<br>循环结束 → 多退少补<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="十一、Vue3-的-diff：预处理-最长递增子序列"><a href="#十一、Vue3-的-diff：预处理-最长递增子序列" class="headerlink" title="十一、Vue3 的 diff：预处理 + 最长递增子序列"></a>十一、Vue3 的 diff：预处理 + 最长递增子序列</h2><p>Vue3 重写了这部分算法，思路变成了：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 预处理：从头、从尾同步相同的节点<br>② 中间&quot;乱序部分&quot;用 key 建映射<br>③ 用最长递增子序列决定哪些节点需要移动<br></code></pre></td></tr></table></figure>

<h3 id="第一步：从头部同步"><a href="#第一步：从头部同步" class="headerlink" title="第一步：从头部同步"></a>第一步：从头部同步</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧： [A, B, C, D, E]<br>新： [A, B, D, E, C]<br><br>A vs A ✓  patch，指针右移<br>B vs B ✓  patch，指针右移<br>C vs D ✗  停止<br></code></pre></td></tr></table></figure>

<h3 id="第二步：从尾部同步"><a href="#第二步：从尾部同步" class="headerlink" title="第二步：从尾部同步"></a>第二步：从尾部同步</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧剩余： [C, D, E]<br>新剩余： [D, E, C]<br><br>E vs E ✓  patch，指针左移<br>D vs D ✓  patch，指针左移<br>C vs C ✗  停止<br></code></pre></td></tr></table></figure>

<p>经过两轮预处理，真正需要”动脑子”的只剩：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">旧剩余： [C]<br>新剩余： [C]<br></code></pre></td></tr></table></figure>

<p>很多真实场景（尾部追加、头部删除）在预处理阶段就直接消化完了，根本走不到复杂逻辑。</p>
<p>这两步预处理借鉴自 ivi &#x2F; inferno 两个库。</p>
<h3 id="第三步：处理中间乱序部分"><a href="#第三步：处理中间乱序部分" class="headerlink" title="第三步：处理中间乱序部分"></a>第三步：处理中间乱序部分</h3><p>对剩下的部分，Vue3 做三件事：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 为新节点建立 keyToNewIndexMap<br>    key → 新索引<br><br>② 遍历旧节点：<br>    能在 map 中查到 → patch，记录&quot;新索引 → 旧索引&quot;<br>    查不到          → 删除<br><br>③ 根据&quot;新索引 → 旧索引&quot;数组，<br>  求最长递增子序列（LIS）<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="十二、为什么需要最长递增子序列？"><a href="#十二、为什么需要最长递增子序列？" class="headerlink" title="十二、为什么需要最长递增子序列？"></a>十二、为什么需要最长递增子序列？</h2><p>这是 Vue3 diff 最精妙的一步。</p>
<p>“新索引 → 旧索引”数组描述了：<strong>新列表中每个位置上的节点，原来在旧列表的什么位置。</strong></p>
<p>举例：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">新顺序：  D    E    A    B    C<br>旧位置：  4    5    1    2    3<br></code></pre></td></tr></table></figure>

<p>这个数组 <code>[4, 5, 1, 2, 3]</code> 的最长递增子序列是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">[1, 2, 3]  （对应 A、B、C）<br></code></pre></td></tr></table></figure>

<p>它的含义是：</p>
<blockquote>
<p><strong>A、B、C 在新旧列表中的相对顺序没有变化。</strong></p>
</blockquote>
<p>于是 Vue3 的决策是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">处于最长递增子序列中的节点 → 不动<br>不处于其中的节点          → 移动<br></code></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">D、E → 需要移动<br>A、B、C → 保持不动<br></code></pre></td></tr></table></figure>

<p>为什么这样最优？</p>
<p>因为<strong>递增子序列内的节点相对顺序已经正确</strong>，不需要任何移动；剩下的节点插入到正确位置即可。这样把 DOM 移动次数降到最少。</p>
<p>对比一下 Vue2 和 Vue3 的思路差异：</p>
<table>
<thead>
<tr>
<th></th>
<th>Vue2</th>
<th>Vue3</th>
</tr>
</thead>
<tbody><tr>
<td>核心策略</td>
<td>双端比较</td>
<td>预处理 + key 映射 + LIS</td>
</tr>
<tr>
<td>快速路径</td>
<td>头尾四次比较</td>
<td>头部 &#x2F; 尾部同步</td>
</tr>
<tr>
<td>乱序处理</td>
<td>keyMap 找到就移动</td>
<td>LIS 保证只移动必须移动的</td>
</tr>
<tr>
<td>移动次数</td>
<td>较优</td>
<td>理论最优</td>
</tr>
</tbody></table>
<p>注意：两者的<strong>匹配依据没有变</strong>，都是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">sameVnode = key 相同 + 标签相同<br></code></pre></td></tr></table></figure>

<p>变的是”找到匹配之后，如何安排移动”的策略。</p>
<hr>
<h2 id="十三、经典翻车现场：index-作-key"><a href="#十三、经典翻车现场：index-作-key" class="headerlink" title="十三、经典翻车现场：index 作 key"></a>十三、经典翻车现场：index 作 key</h2><p>终于可以正面回答那个经典问题了。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">li</span> <span class="hljs-attr">v-for</span>=<span class="hljs-string">&quot;(item, index) in list&quot;</span> <span class="hljs-attr">:key</span>=<span class="hljs-string">&quot;index&quot;</span>&gt;</span><br>  &#123;&#123; item.name &#125;&#125;<br>  <span class="hljs-tag">&lt;<span class="hljs-name">input</span> <span class="hljs-attr">type</span>=<span class="hljs-string">&quot;text&quot;</span> /&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">li</span>&gt;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">list = [<br>  &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">1</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;张三&#x27;</span> &#125;,<br>  &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">2</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;李四&#x27;</span> &#125;,<br>  &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">3</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;王五&#x27;</span> &#125;<br>]<br></code></pre></td></tr></table></figure>

<p>用户在三个输入框里分别输入了：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">张三 → 输入了 &quot;111&quot;<br>李四 → 输入了 &quot;222&quot;<br>王五 → 输入了 &quot;333&quot;<br></code></pre></td></tr></table></figure>

<p>现在删除第一个元素，list 变成：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">list = [<br>  &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">2</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;李四&#x27;</span> &#125;,<br>  &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">3</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;王五&#x27;</span> &#125;<br>]<br></code></pre></td></tr></table></figure>

<h3 id="用-index-作-key-时发生了什么"><a href="#用-index-作-key-时发生了什么" class="headerlink" title="用 index 作 key 时发生了什么"></a>用 index 作 key 时发生了什么</h3><p>删除前，节点的 key 是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">张三 → key = 0<br>李四 → key = 1<br>王五 → key = 2<br></code></pre></td></tr></table></figure>

<p>删除后，key 重新计算：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">李四 → key = 0<br>王五 → key = 1<br></code></pre></td></tr></table></figure>

<p>于是 diff 做出的配对是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">新 key=0（李四） ← 旧 key=0（张三的节点）<br>新 key=1（王五） ← 旧 key=1（李四的节点）<br>旧 key=2（王五的节点） → 没有配对 → 删除<br></code></pre></td></tr></table></figure>

<p>patch 时发生的事情：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 文本子节点更新：<br>   &quot;张三&quot; → &quot;李四&quot; ✓<br>   &quot;李四&quot; → &quot;王五&quot; ✓<br><br>② input 是非受控的原生 DOM 状态：<br>   patch 只更新 vnode 描述的属性，<br>   不会去动用户输入的 value<br></code></pre></td></tr></table></figure>

<p>最终页面呈现：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">李四  [111]   ← 张三的输入框，被复用了<br>王五  [222]   ← 李四的输入框，被复用了<br>              ← 王五的输入框（333）被删掉了<br></code></pre></td></tr></table></figure>

<p><strong>文本是对的，输入框全错位了。</strong></p>
<h3 id="用-id-作-key-时发生了什么"><a href="#用-id-作-key-时发生了什么" class="headerlink" title="用 id 作 key 时发生了什么"></a>用 id 作 key 时发生了什么</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">新 key=2（李四） ← 旧 key=2（李四的节点）  完全相同，无需 patch<br>新 key=1（张三的节点） → 没有配对 → 删除<br></code></pre></td></tr></table></figure>

<p>结果：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">李四  [222]  ✓ 复用原节点，连 patch 都省了<br>王五  [333]  ✓<br></code></pre></td></tr></table></figure>

<p>除了问题本身，还有性能差距：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">index 作 key：所有剩余节点都要 patch + 删一个节点<br>id   作 key：只删除一个节点，其他节点原样复用<br></code></pre></td></tr></table></figure>

<p>一句话总结 index 作 key 的两个问题：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 性能：本可复用的节点被迫全部 patch<br>② 正确性：节点内部的状态（输入框、动画、组件内部状态）<br>   会错位到别的数据上<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="十四、比-index-更糟：随机数作-key"><a href="#十四、比-index-更糟：随机数作-key" class="headerlink" title="十四、比 index 更糟：随机数作 key"></a>十四、比 index 更糟：随机数作 key</h2><p>既然 key 要唯一，那这样写行不行？</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">li</span> <span class="hljs-attr">v-for</span>=<span class="hljs-string">&quot;item in list&quot;</span> <span class="hljs-attr">:key</span>=<span class="hljs-string">&quot;Math.random()&quot;</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>不行，而且比 index 更糟。</p>
<p><code>Math.random()</code> <strong>每次渲染都会生成新值</strong>：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">第一次渲染：  key = 0.8231, 0.1947, 0.5533<br>第二次渲染：  key = 0.2764, 0.9012, 0.4417<br></code></pre></td></tr></table></figure>

<p>对 diff 来说：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">新列表的每个 key<br>        ↓<br>在旧列表中都找不到<br>        ↓<br>判定为&quot;全新节点&quot;<br>        ↓<br>整个列表：全部删除 + 全部重建<br></code></pre></td></tr></table></figure>

<p>后果：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 性能：diff 的复用能力完全归零，<br>   每次更新都等价于重渲染整个列表<br><br>② 正确性：所有节点内部状态全部丢失<br>   ——输入框被清空、组件被重新 mounted、<br>   过渡动画从头播放<br></code></pre></td></tr></table></figure>

<p>key 的两个要求”唯一、稳定”，随机数只满足唯一：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">index    → 稳定但不指向数据（会错位）<br>random   → 唯一但不稳定（全部重建）<br>正确的 key → 唯一且稳定，来自数据本身<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="十五、key-的正确用法"><a href="#十五、key-的正确用法" class="headerlink" title="十五、key 的正确用法"></a>十五、key 的正确用法</h2><h3 id="基本原则"><a href="#基本原则" class="headerlink" title="基本原则"></a>基本原则</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">key 应该来自数据本身的、稳定唯一的标识<br></code></pre></td></tr></table></figure>

<p>首选后端返回的业务 id：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">li</span> <span class="hljs-attr">v-for</span>=<span class="hljs-string">&quot;item in list&quot;</span> <span class="hljs-attr">:key</span>=<span class="hljs-string">&quot;item.id&quot;</span>&gt;</span><br></code></pre></td></tr></table></figure>

<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">list = [<br>  &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">1001</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;张三&#x27;</span> &#125;,<br>  &#123; <span class="hljs-attr">id</span>: <span class="hljs-number">1002</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;李四&#x27;</span> &#125;<br>]<br></code></pre></td></tr></table></figure>

<h3 id="没有-id-怎么办"><a href="#没有-id-怎么办" class="headerlink" title="没有 id 怎么办"></a>没有 id 怎么办</h3><p>在数据<strong>创建时</strong>（而不是渲染时）生成一次，之后保持不变：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">import</span> &#123; nanoid &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;nanoid&#x27;</span><br><br>list.<span class="hljs-property">value</span> = rawData.<span class="hljs-title function_">map</span>(<span class="hljs-function"><span class="hljs-params">item</span> =&gt;</span> (&#123;<br>  ...item,<br>  <span class="hljs-attr">uid</span>: <span class="hljs-title function_">nanoid</span>()   <span class="hljs-comment">// 创建时生成一次，之后不再变化</span><br>&#125;))<br></code></pre></td></tr></table></figure>

<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">li</span> <span class="hljs-attr">v-for</span>=<span class="hljs-string">&quot;item in list&quot;</span> <span class="hljs-attr">:key</span>=<span class="hljs-string">&quot;item.uid&quot;</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>关键区别：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">渲染时生成  → 每次渲染都变 → 等价于随机数<br>创建时生成  → 只生成一次   → 稳定<br></code></pre></td></tr></table></figure>

<h3 id="key-的类型要求"><a href="#key-的类型要求" class="headerlink" title="key 的类型要求"></a>key 的类型要求</h3><p>key 只接受字符串、数字、symbol：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html">:key=&quot;item.id&quot;          ✓ 数字<br>:key=&quot;item.uuid&quot;        ✓ 字符串<br>:key=&quot;item&quot;             ✗ 对象（会被转成 &#x27;[object Object]&#x27;，全部相同）<br></code></pre></td></tr></table></figure>

<h3 id="中的-key"><a href="#中的-key" class="headerlink" title="&lt;template v-for&gt; 中的 key"></a><code>&lt;template v-for&gt;</code> 中的 key</h3><p>Vue3 中，<code>v-for</code> 写在 <code>&lt;template&gt;</code> 上时，key 要放在 <code>&lt;template&gt;</code> 上：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">template</span> <span class="hljs-attr">v-for</span>=<span class="hljs-string">&quot;item in list&quot;</span> <span class="hljs-attr">:key</span>=<span class="hljs-string">&quot;item.id&quot;</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">dt</span>&gt;</span>&#123;&#123; item.name &#125;&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">dt</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">dd</span>&gt;</span>&#123;&#123; item.desc &#125;&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">dd</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">template</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>而不是放在子元素上（这是与 Vue2 不同的地方）。</p>
<hr>
<h2 id="十六、什么时候可以用-index？"><a href="#十六、什么时候可以用-index？" class="headerlink" title="十六、什么时候可以用 index？"></a>十六、什么时候可以用 index？</h2><p>index 并非绝对禁区。满足<strong>全部</strong>以下条件时，index 作 key 是安全的：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 列表是纯展示，不包含表单类元素或带状态的组件<br>② 列表不会重排（sort）、过滤（filter）<br>③ 增删只发生在尾部，或数据本身永不变化<br></code></pre></td></tr></table></figure>

<p>比如完全静态的导航、配置驱动的纯文案列表：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">nav</span>&gt;</span><br>  <span class="hljs-tag">&lt;<span class="hljs-name">a</span> <span class="hljs-attr">v-for</span>=<span class="hljs-string">&quot;(item, index) in menus&quot;</span> <span class="hljs-attr">:key</span>=<span class="hljs-string">&quot;index&quot;</span>&gt;</span><br>    &#123;&#123; item.label &#125;&#125;<br>  <span class="hljs-tag">&lt;/<span class="hljs-name">a</span>&gt;</span><br><span class="hljs-tag">&lt;/<span class="hljs-name">nav</span>&gt;</span><br></code></pre></td></tr></table></figure>

<p>但实际项目中，列表”以后会加搜索功能”的概率远超预期，而 index 作 key 的错误又是<strong>隐性</strong>的——不报错、不警告，只在特定操作后悄悄错位。</p>
<p>所以工程上的建议很简单：</p>
<blockquote>
<p><strong>一律使用稳定 id，把 index 留给”确定永远不会变”的静态列表。</strong></p>
</blockquote>
<hr>
<h2 id="十七、React-中的-diff-与-key"><a href="#十七、React-中的-diff-与-key" class="headerlink" title="十七、React 中的 diff 与 key"></a>十七、React 中的 diff 与 key</h2><p>同样的思想也适用于 React。</p>
<p>React 的 reconciliation 同样基于两条假设：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 不同类型的元素 → 推倒重建<br>② 同层比较，通过 key 标识子节点<br></code></pre></td></tr></table></figure>

<p>使用方式也一致：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><code class="hljs jsx">&#123;list.<span class="hljs-title function_">map</span>(<span class="hljs-function"><span class="hljs-params">item</span> =&gt;</span> (<br>  <span class="language-xml"><span class="hljs-tag">&lt;<span class="hljs-name">li</span> <span class="hljs-attr">key</span>=<span class="hljs-string">&#123;item.id&#125;</span>&gt;</span>&#123;item.name&#125;<span class="hljs-tag">&lt;/<span class="hljs-name">li</span>&gt;</span></span><br>))&#125;<br></code></pre></td></tr></table></figure>

<p>React 中 index 作 key 的后果和 Vue 一致：列表头部插入元素时状态错位、输入框内容张冠李戴。</p>
<p>实现层面的主要差异在于”乱序部分的移动策略”：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Vue2：双端比较<br>Vue3：预处理 + 最长递增子序列<br>React：单向遍历 + 已放置节点索引（lastPlacedIndex）<br></code></pre></td></tr></table></figure>

<p>React 不做双端比较，从左到右遍历新列表，用”最后一个已放置节点的位置”判断当前节点是需要移动还是原地复用；对乱序程度高的列表，React 的移动次数通常多于 Vue3。</p>
<p>对这些差异，理解到这一层就够了：</p>
<blockquote>
<p><strong>不同框架的 diff 策略有差异，但 key 的语义完全一致——节点身份标识。</strong></p>
</blockquote>
<hr>
<h2 id="十八、总结"><a href="#十八、总结" class="headerlink" title="十八、总结"></a>十八、总结</h2><p>把全文的主线串起来：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">虚拟 DOM 更新需要对比新旧树<br>        ↓<br>完整对比是 O(n³)，不可行<br>        ↓<br>三条简化：同层比较 / 类型不同重建 / key 标识<br>        ↓<br>key 成为节点复用与移动的唯一依据<br></code></pre></td></tr></table></figure>

<p>两代 Vue 的 diff 实现：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Vue2 updateChildren<br>  四指针双端比较<br>  + keyMap 兜底<br>  + 多退少补<br><br>Vue3 patchKeyedChildren<br>  头尾预处理<br>  + keyToNewIndexMap<br>  + 最长递增子序列（最少移动）<br></code></pre></td></tr></table></figure>

<p>key 的正确用法，浓缩成一张表：</p>
<table>
<thead>
<tr>
<th>key 写法</th>
<th>唯一</th>
<th>稳定</th>
<th>结果</th>
</tr>
</thead>
<tbody><tr>
<td><code>item.id</code></td>
<td>✓</td>
<td>✓</td>
<td>复用 + 最少移动，正确且高效</td>
</tr>
<tr>
<td><code>index</code></td>
<td>✓</td>
<td>✓</td>
<td>位置不指向数据：patch 放大、状态错位</td>
</tr>
<tr>
<td><code>Math.random()</code></td>
<td>✓</td>
<td>✗</td>
<td>每次渲染全量重建，状态全丢</td>
</tr>
<tr>
<td>不写 key</td>
<td>—</td>
<td>—</td>
<td>就地复用：乱序场景下问题同 index</td>
</tr>
</tbody></table>
<p>最后回到那个根本问题——key 是什么？</p>
<blockquote>
<p><strong>key 是给 diff 算法看的节点身份证：diff 靠它在新旧列表之间配对节点，从而做到”能复用的不重建，能不动的少移动”。</strong></p>
</blockquote>
<p>身份证的要点从来只有两个：<strong>唯一，且始终属于同一个人。</strong></p>
<hr>
]]></content>
      <categories>
        <category>Vue</category>
        <category>源码</category>
      </categories>
      <tags>
        <tag>性能优化</tag>
        <tag>Vue</tag>
        <tag>diff</tag>
        <tag>key</tag>
        <tag>虚拟DOM</tag>
      </tags>
  </entry>
  <entry>
    <title>Vite vs Webpack：迁移成本与收益评估——老项目值不值得换一次引擎</title>
    <url>/2026/09/10/vite-webpack-migration/</url>
    <content><![CDATA[<p>「Vite 为什么快」已经写过了（见<a href="/2026/09/10/webpack-vs-rollup/">《Webpack 与 Rollup》</a>末尾），这篇聊一个更折磨人的问题：<strong>已经在 Webpack 上稳定运行的项目，要不要迁 Vite？</strong></p>
<p>先给结论，再给论证：</p>
<blockquote>
<p><strong>新项目不用讨论，默认 Vite。老项目是一道算术题：你买的是「每天的开发等待时间」，付的是「一次性的工程成本 + 生态摩擦」。团队还在高频迭代，这笔账大概率划算；项目进入维护期，一分钱都不该花。</strong></p>
</blockquote>
<p>下面把两边的账都拆开算。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">迁移收益 ≈ 冷启动提速 + HMR 提速 × 每日次数 × 团队人数（持续性收益）<br>迁移成本 ≈ 配置翻译 + 环境变量改造 + 无对等物插件重写 + 回归验证（一次性成本）<br></code></pre></td></tr></table></figure>

<span id="more"></span>

<h2 id="一、先对齐：差异发生在开发态，不在生产态"><a href="#一、先对齐：差异发生在开发态，不在生产态" class="headerlink" title="一、先对齐：差异发生在开发态，不在生产态"></a>一、先对齐：差异发生在开发态，不在生产态</h2><p>很多迁移决策吵错了地方。两家真正的架构分野在 dev server：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Webpack dev：bundle 模式<br>  启动 = 从 entry 全量打包 → 起服务 → 打包完才能看到页面<br>  HMR  = 增量重新打包受影响模块 → 生成补丁 → 推给浏览器<br>  特点：所有等待都和项目规模正相关<br><br>Vite dev：unbundled 模式<br>  启动 = 只起服务（不打包）→ 浏览器用原生 ESM 按需 import<br>  依赖 = esbuild 预打包（node_modules 里的 CJS 转 ESM）<br>  HMR  = 只重编译当前模块，浏览器重新 import 那一个文件<br>  特点：等待和项目规模基本无关<br></code></pre></td></tr></table></figure>

<p>一个关键推论，直接决定后面所有账怎么算：</p>
<blockquote>
<p><strong>迁移 Vite 的收益几乎全部落在开发态。生产构建那边，Vite 的内核是 Rollup，产物质量和 Webpack 是同一水平线（甚至 CSS 代码分割默认做得更好），但构建速度未必更快——大型项目上 Rollup 常常比 Webpack 慢。</strong></p>
</blockquote>
<p>所以如果你的痛点是「CI 构建 too slow」，迁 Vite 大概率解决不了，那是另一个问题（见<a href="/2026/09/09/webpack-performance/">《Webpack 性能优化速查》</a>）。如果你的痛点是「改一行代码等八秒」，继续往下算。</p>
<h2 id="二、收益盘点：你到底在买什么"><a href="#二、收益盘点：你到底在买什么" class="headerlink" title="二、收益盘点：你到底在买什么"></a>二、收益盘点：你到底在买什么</h2><h3 id="1-冷启动：量级差异，不是百分比差异"><a href="#1-冷启动：量级差异，不是百分比差异" class="headerlink" title="1. 冷启动：量级差异，不是百分比差异"></a>1. 冷启动：量级差异，不是百分比差异</h3><p>Webpack 大型项目 dev 冷启动普遍在 30 秒到几分钟（取决定于入口数和 loader 数量）；Vite 通常秒级返回（首次要预打包依赖，大 monorepo 依赖下也会慢，但 <code>node_modules/.vite</code> 有缓存）。</p>
<p>量级差异意味着质变：<strong>分钟级的启动时间会改变人的行为</strong>——早上起一次服务挂到下班、不敢随便重启、切分支先去倒杯水。秒级启动把这些行为成本清零了。</p>
<h3 id="2-HMR-与项目规模解耦：这才是收益的大头"><a href="#2-HMR-与项目规模解耦：这才是收益的大头" class="headerlink" title="2. HMR 与项目规模解耦：这才是收益的大头"></a>2. HMR 与项目规模解耦：这才是收益的大头</h3><p>冷启动一天一次，HMR 一天几十次。Webpack 的 HMR 随项目膨胀线性变慢（增量打包也要走 loader 流水线）；Vite 的 HMR 只重编当前文件，项目长多大它都是毫秒级。</p>
<p><strong>这笔账要乘上人数</strong>：10 个人的团队，每人每天 50 次保存，每次从 5 秒变 0.5 秒，一天就是接近一小时的纯等待消失。这是迁移收益里最大、也最难被「跑个 benchmark」感知的一项——它是以「体感」的形式逐日兑付的。</p>
<h3 id="3-配置面缩小"><a href="#3-配置面缩小" class="headerlink" title="3. 配置面缩小"></a>3. 配置面缩小</h3><p>以 Vue 项目为例，一份典型 <code>vue.config.js</code>&#x2F;<code>webpack.config.js</code>（chain 配 loader、splitChunks、插件开关）对应到 <code>vite.config.ts</code>，通常能缩到几十行——CSS 处理、HTML 模板、静态资源、dev proxy 都是内置能力：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// vite.config.ts —— 这就是大部分中型项目接近全量的配置</span><br><span class="hljs-keyword">import</span> &#123; defineConfig &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;vite&#x27;</span><br><span class="hljs-keyword">import</span> vue <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;@vitejs/plugin-vue&#x27;</span><br><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">default</span> <span class="hljs-title function_">defineConfig</span>(&#123;<br>  <span class="hljs-attr">plugins</span>: [<span class="hljs-title function_">vue</span>()],<br>  <span class="hljs-attr">resolve</span>: &#123; <span class="hljs-attr">alias</span>: &#123; <span class="hljs-string">&#x27;@&#x27;</span>: <span class="hljs-string">&#x27;/src&#x27;</span> &#125; &#125;,<br>  <span class="hljs-attr">server</span>: &#123; <span class="hljs-attr">proxy</span>: &#123; <span class="hljs-string">&#x27;/api&#x27;</span>: <span class="hljs-string">&#x27;http://localhost:8080&#x27;</span> &#125; &#125;,<br>  <span class="hljs-attr">build</span>: &#123;<br>    <span class="hljs-attr">rollupOptions</span>: &#123;<br>      <span class="hljs-attr">output</span>: &#123; <span class="hljs-attr">manualChunks</span>: &#123; <span class="hljs-attr">vendor</span>: [<span class="hljs-string">&#x27;vue&#x27;</span>, <span class="hljs-string">&#x27;vue-router&#x27;</span>, <span class="hljs-string">&#x27;pinia&#x27;</span>] &#125; &#125;,<br>    &#125;,<br>  &#125;,<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>配置少不只是好看——<strong>配置少意味着故障面小</strong>。Webpack 的很多诡异问题（loader 顺序、缓存失效、chain 写法版本差异）在 Vite 里直接没有存在的土壤。</p>
<h3 id="4-附带收益"><a href="#4-附带收益" class="headerlink" title="4. 附带收益"></a>4. 附带收益</h3><ul>
<li><strong>vitest 与 Vite 共享同一份配置</strong>（alias、插件、transform 原生复用），从 jest 迁过去后测试和构建终于用同一套模块解析，<code>moduleNameMapper</code> 双份维护的坑消失；</li>
<li>生产产物出自 Rollup 内核，自带更干净的 tree shaking 和按模块的 CSS 分割；</li>
<li>插件生态活跃度肉眼可见地偏向 Vite，新框架的官方脚手架基本只出 Vite 版。</li>
</ul>
<h3 id="收益的边界（诚实记账）"><a href="#收益的边界（诚实记账）" class="headerlink" title="收益的边界（诚实记账）"></a>收益的边界（诚实记账）</h3><ul>
<li><strong>生产构建不保证变快</strong>。Rollup 在超大项目上可能比 Webpack 慢，好在 <code>build</code> 不像 dev 那样每天发生几十次；</li>
<li><strong>dev 和 prod 是两个引擎</strong>（esbuild &#x2F; Rollup），存在 dev 好好的、prod 构建报错或行为不一致的坑（CJS interop 差异、动态 import 语义边界），所以迁移验收必须两个都测，后文会写；</li>
<li>首次冷启动要预打包依赖，monorepo + 重依赖时并不「秒开」；</li>
<li><strong>所有收益的前提是团队还在活跃迭代</strong>。没有人在写代码的项目，dev 体验再好也没有兑付渠道。</li>
</ul>
<h2 id="三、成本盘点：你要付什么"><a href="#三、成本盘点：你要付什么" class="headerlink" title="三、成本盘点：你要付什么"></a>三、成本盘点：你要付什么</h2><p>按工作量从大到小排，别按直觉排——直觉会告诉你配置翻译最难，实际上环境变量和回归验证才是。</p>
<h3 id="1-环境变量体系：最琐碎、最容易漏的一项"><a href="#1-环境变量体系：最琐碎、最容易漏的一项" class="headerlink" title="1. 环境变量体系：最琐碎、最容易漏的一项"></a>1. 环境变量体系：最琐碎、最容易漏的一项</h3><p>Webpack 生态写 <code>process.env.XXX</code>，Vite 的标准是 <code>import.meta.env.XXX</code>。坑在于 <code>process.env</code> 的散布范围远超源码：</p>
<ul>
<li><code>src/</code> 里所有直接引用（量大，但grep 能扫干净）；</li>
<li><strong>CI 脚本、Dockerfile entrypoint、package.json scripts</strong> 里 grep <code>process.env</code> 的地方；</li>
<li>第三方依赖内部读 <code>process.env.NODE_ENV</code> 之外的变量（这类最阴险，运行时才炸）；</li>
<li>测试框架的 setup 文件。</li>
</ul>
<p>正确做法不是全局替换，而是<strong>收敛出口</strong>（见下一节的迁移路径第一步）。</p>
<h3 id="2-loader-插件翻译对照表"><a href="#2-loader-插件翻译对照表" class="headerlink" title="2. loader &#x2F; 插件翻译对照表"></a>2. loader &#x2F; 插件翻译对照表</h3><p>大部分是机械翻译，一行对一行：</p>
<table>
<thead>
<tr>
<th>Webpack 侧</th>
<th>Vite 侧</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td><code>babel-loader</code></td>
<td>不需要（esbuild 转译）</td>
<td>旧浏览器支持用 <code>@vitejs/plugin-legacy</code></td>
</tr>
<tr>
<td><code>css-loader</code> + <code>style-loader</code></td>
<td>内置</td>
<td>无对应配置</td>
</tr>
<tr>
<td><code>sass-loader</code> &#x2F; <code>less-loader</code></td>
<td>内置 + <code>css.preprocessorOptions</code></td>
<td>装好 sass&#x2F;less 即用</td>
</tr>
<tr>
<td><code>postcss-loader</code> + <code>autoprefixer</code></td>
<td><code>postcss.config.js</code> 原样识别</td>
<td>无成本</td>
</tr>
<tr>
<td><code>file-loader</code> &#x2F; <code>url-loader</code></td>
<td><code>assetsInlineLimit</code> + <code>new URL(..., import.meta.url)</code></td>
<td>小资源内联阈值一个配置管完</td>
</tr>
<tr>
<td><code>copy-webpack-plugin</code></td>
<td><code>vite-plugin-static-copy</code></td>
<td>public 目录能覆盖大部分场景</td>
</tr>
<tr>
<td><code>DefinePlugin</code></td>
<td><code>define</code></td>
<td>键名要手动包 <code>JSON.stringify</code></td>
</tr>
<tr>
<td><code>html-webpack-plugin</code></td>
<td>内置</td>
<td>模板放根目录 <code>index.html</code></td>
</tr>
<tr>
<td><code>splitChunks</code></td>
<td><code>build.rollupOptions.output.manualChunks</code></td>
<td>心智从「缓存组」换成「入口分块」，分包实践我写在<a href="/2026/09/10/spa-first-screen-practice/">大屏优化实录</a>里</td>
</tr>
<tr>
<td><code>resolve.alias</code></td>
<td><code>resolve.alias</code></td>
<td>几乎原名翻译</td>
</tr>
<tr>
<td><code>devServer.proxy</code></td>
<td><code>server.proxy</code></td>
<td>配置结构基本一致</td>
</tr>
</tbody></table>
<p><strong>真正没有对等物的是下面这几个，它们才构成硬成本：</strong></p>
<table>
<thead>
<tr>
<th>Webpack 能力</th>
<th>Vite 现状</th>
<th>应对</th>
</tr>
</thead>
<tbody><tr>
<td><code>ProvidePlugin</code>（免 import 全局变量）</td>
<td>无对等物</td>
<td>改源码，显式 import。长痛不如短痛，但就是要动人肉改</td>
</tr>
<tr>
<td>自定义 loader &#x2F; 自研插件</td>
<td>需按 Vite 插件模型重写</td>
<td>数量 × 复杂度决定这笔账的大小</td>
</tr>
<tr>
<td>Module Federation</td>
<td>原生不友好</td>
<td><code>@module-federation/enhanced</code> 可用但属于新路线；重度使用建议不迁（见决策表）</td>
</tr>
<tr>
<td><code>worker-loader</code> 深度用法</td>
<td><code>?worker</code> &#x2F; <code>new Worker(new URL(...))</code></td>
<td>语法重写，逻辑可保留</td>
</tr>
<tr>
<td>qiankun 微前端子应用</td>
<td>能跑，有适配成本</td>
<td>官方有 Vite 适配方案，但签名和沙箱要逐一验</td>
</tr>
</tbody></table>
<h3 id="3-CJS-遗留依赖"><a href="#3-CJS-遗留依赖" class="headerlink" title="3. CJS 遗留依赖"></a>3. CJS 遗留依赖</h3><p>Vite 开发态是 ESM 世界，CJS 依赖靠预打包转换。少数老包（深度依赖 <code>require</code> 动态拼接、或把依赖藏在奇怪入口里的）会在 dev 启动时报错，解法是报错驱动：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-keyword">export</span> <span class="hljs-keyword">default</span> <span class="hljs-title function_">defineConfig</span>(&#123;<br>  <span class="hljs-attr">optimizeDeps</span>: &#123;<br>    <span class="hljs-comment">// 谁报 &quot;does not provide an export named ...&quot; 就把谁加进来</span><br>    <span class="hljs-attr">include</span>: [<span class="hljs-string">&#x27;legacy-charts&#x27;</span>, <span class="hljs-string">&#x27;some-sdk/dist/entry&#x27;</span>],<br>  &#125;,<br>&#125;)<br></code></pre></td></tr></table></figure>

<p>这类问题单项都不难，难在数量不可预知——迁移排期时给这里留 20% 的 buffer。</p>
<h3 id="4-测试链路"><a href="#4-测试链路" class="headerlink" title="4. 测试链路"></a>4. 测试链路</h3><p>jest 的 transform 和 Webpack 无关，理论上可以原样保留。但保留意味着 alias、环境变量在 jest 和 Vite 两边各配一份。<strong>通常的做法是顺势换 vitest</strong>，配置共享后这一项从成本变成收益——代价是测试 API 的迁移工作量（兼容 jest 语法，大头在 mock 和 setup）。</p>
<h3 id="5-验证成本：隐性大头"><a href="#5-验证成本：隐性大头" class="headerlink" title="5. 验证成本：隐性大头"></a>5. 验证成本：隐性大头</h3><p>构建产物从 Webpack 换成 Rollup 输出，<strong>模块图、CSS 拆分、懒加载边界全部重画</strong>。哪怕配置语义等价，也不存在「配置对了产物就一定对」的保证（还记得双引擎问题吗）。所以一次完整的回归测试是刚性成本——功能回归 + 关键页面性能对比。很多团队迁移超期，超的正是这一段。</p>
<h3 id="工作量量级（经验值，非精确报价）"><a href="#工作量量级（经验值，非精确报价）" class="headerlink" title="工作量量级（经验值，非精确报价）"></a>工作量量级（经验值，非精确报价）</h3><p>以一个 30<del>50 个路由、标准技术栈（Vue&#x2F;React + sass + 常规插件）的项目、1</del>2 名熟悉两边工具的工程师为例：</p>
<table>
<thead>
<tr>
<th>阶段</th>
<th>工作量</th>
</tr>
</thead>
<tbody><tr>
<td>环境变量收敛 + 配置骨架</td>
<td>1~2 天</td>
</tr>
<tr>
<td>loader&#x2F;插件翻译 + CJS 依赖报错清理</td>
<td>2~4 天</td>
</tr>
<tr>
<td>ProvidePlugin &#x2F; worker 等源码级改动</td>
<td>视数量，0.5~3 天</td>
</tr>
<tr>
<td>测试链路（顺势换 vitest）</td>
<td>1~2 天</td>
</tr>
<tr>
<td>双引擎验证 + 功能回归</td>
<td>2~5 天</td>
</tr>
<tr>
<td><strong>合计</strong></td>
<td><strong>约 1~3 周</strong></td>
</tr>
</tbody></table>
<p>重度依赖 MF &#x2F; 自定义 loader 的项目不在此表射程内，那种情况先看第五节的第三条路。</p>
<h2 id="四、迁移路径：怎么把成本压到最低"><a href="#四、迁移路径：怎么把成本压到最低" class="headerlink" title="四、迁移路径：怎么把成本压到最低"></a>四、迁移路径：怎么把成本压到最低</h2><h3 id="1-配置从零手写，不要机器翻译"><a href="#1-配置从零手写，不要机器翻译" class="headerlink" title="1. 配置从零手写，不要机器翻译"></a>1. 配置从零手写，不要机器翻译</h3><p>两套配置的心智模型不同（bundle 流水线 vs 原生 ESM 服务），翻译出来的必然是塞满 <code>optimizeDeps</code> 和奇怪 <code>define</code> 的四不像。正确姿势是按上一节的对照表，从最小骨架开始，缺什么补什么。</p>
<h3 id="2-第一步做「环境变量收敛」，而且要在迁移前做"><a href="#2-第一步做「环境变量收敛」，而且要在迁移前做" class="headerlink" title="2. 第一步做「环境变量收敛」，而且要在迁移前做"></a>2. 第一步做「环境变量收敛」，而且要在迁移前做</h3><p>全项目扫 <code>process.env</code>，收敛到单一出口：</p>
<figure class="highlight ts"><table><tr><td class="code"><pre><code class="hljs ts"><span class="hljs-comment">// src/env.ts —— 全项目只允许从这里读环境变量</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> env = &#123;<br>  <span class="hljs-attr">apiBase</span>: <span class="hljs-keyword">import</span>.<span class="hljs-property">meta</span>.<span class="hljs-property">env</span>.<span class="hljs-property">VITE_API_BASE</span> <span class="hljs-keyword">as</span> <span class="hljs-built_in">string</span>,<br>  <span class="hljs-attr">mode</span>: <span class="hljs-keyword">import</span>.<span class="hljs-property">meta</span>.<span class="hljs-property">env</span>.<span class="hljs-property">MODE</span>,<br>  <span class="hljs-attr">isProd</span>: <span class="hljs-keyword">import</span>.<span class="hljs-property">meta</span>.<span class="hljs-property">env</span>.<span class="hljs-property">PROD</span>,<br>&#125;<br></code></pre></td></tr></table></figure>

<p>这一步的价值在于：<strong>即使最后决定不迁移，它也让下一次迁移（或任何构建工具变更）的成本降一个量级</strong>。环境变量散在几百个文件里，才是「锁死在某个工具上」的真正原因。</p>
<h3 id="3-报错驱动地处理-CJS，而不是预判"><a href="#3-报错驱动地处理-CJS，而不是预判" class="headerlink" title="3. 报错驱动地处理 CJS，而不是预判"></a>3. 报错驱动地处理 CJS，而不是预判</h3><p>不要试图提前列完问题依赖清单，起服务、看报错、<code>optimizeDeps.include</code>、重启，循环到干净为止。半天到一天通常能清完。</p>
<h3 id="4-双引擎验收，产物对比兜底"><a href="#4-双引擎验收，产物对比兜底" class="headerlink" title="4. 双引擎验收，产物对比兜底"></a>4. 双引擎验收，产物对比兜底</h3><p>dev 能跑不等于 prod 能用（esbuild 和 Rollup 是两个引擎）。验收要两条线：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">功能线：dev 跑一遍 + build &amp;&amp; preview 跑一遍，重点回归懒加载路由和动态 import<br>产物线：构建产物体积表对比（总包 + 各 chunk），<br>       体积异常膨胀通常意味着某个依赖没被正确预打包或 tree shaking 失效<br></code></pre></td></tr></table></figure>

<h3 id="5-保留回滚窗口"><a href="#5-保留回滚窗口" class="headerlink" title="5. 保留回滚窗口"></a>5. 保留回滚窗口</h3><p>SPA 没法「半个应用迁过去」，切换是一次性的。做法：</p>
<ul>
<li>在独立分支完成全部迁移，主干不动；</li>
<li>切换后<strong>旧的 <code>webpack.config</code> 保留至少一到两周不删</strong>——这不是心理安慰，回滚时你要的是「原样可构建」，而不是「重新对齐过的旧配置」；</li>
<li>CI 里可以让旧构建任务先并行跑一段，产物留档。</li>
</ul>
<h3 id="验收清单"><a href="#验收清单" class="headerlink" title="验收清单"></a>验收清单</h3><table>
<thead>
<tr>
<th>指标</th>
<th>通过标准</th>
</tr>
</thead>
<tbody><tr>
<td>dev 冷启动</td>
<td>秒级返回（首启除外，缓存后计）</td>
</tr>
<tr>
<td>HMR 延迟</td>
<td>保存到可视变化 &lt; 1s</td>
</tr>
<tr>
<td>build 时间</td>
<td>不显著劣化（对比旧构建）</td>
</tr>
<tr>
<td>产物体积</td>
<td>总体积不膨胀，chunk 分布合理</td>
</tr>
<tr>
<td>功能回归</td>
<td>路由懒加载、动态 import、微前端&#x2F;SSR（如有）逐一过</td>
</tr>
</tbody></table>
<h2 id="五、决策框架：一张表算清账"><a href="#五、决策框架：一张表算清账" class="headerlink" title="五、决策框架：一张表算清账"></a>五、决策框架：一张表算清账</h2><p>把前面的账收拢成一个定性公式：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">持续收益 = 团队人数 × 每日迭代次数 × 单次等待削减量 × 剩余维护年限<br>一次性成本 = 迁移人日 + 生态摩擦（无对等物部分）+ 验证成本<br>值得迁 ⟺ 持续收益明显 &gt; 一次性成本，且没有不可替代的生态依赖<br></code></pre></td></tr></table></figure>

<p>落到具体场景：</p>
<table>
<thead>
<tr>
<th>项目状态</th>
<th>决策</th>
<th>理由</th>
</tr>
</thead>
<tbody><tr>
<td>新项目</td>
<td>Vite，不用讨论</td>
<td>没有沉没成本，收益全额兑付</td>
</tr>
<tr>
<td>维护期老项目（偶尔改 bug）</td>
<td><strong>不迁</strong></td>
<td>收益没有兑付渠道。要提速用 esbuild-loader &#x2F; swc-loader 换掉 babel，成本一天，见<a href="/2026/09/09/webpack-performance/">Webpack 优化速查</a></td>
</tr>
<tr>
<td>活跃迭代 + 标准技术栈</td>
<td><strong>迁</strong></td>
<td>上面 1~3 周的成本，通常一两个月内从开发等待里赚回</td>
</tr>
<tr>
<td>活跃迭代 + 重度 MF &#x2F; qiankun &#x2F; 自定义 loader 成堆</td>
<td>第三条路：<strong>Rspack</strong></td>
<td>保配置、换引擎（Rust 实现），迁移成本约十分之一，拿走大部分性能收益</td>
</tr>
</tbody></table>
<p>Rspack 这条路值得单独说一句：它走 Webpack 兼容路线，loader 和配置大部分原样可跑，本质上是「承认你的配置资产，只换掉慢的内核」。对配置体量巨大的存量项目，它常常比 Vite 迁移更理性——代价是留在 Webpack 心智模型里，享受不到 unbundled dev 那种和规模解耦的 HMR（它通过原生 Rust 编译把 bundle 模式做到极快来逼近）。</p>
<h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>一句话收拢全文：</p>
<blockquote>
<p><strong>Vite 迁移买的不是更快的构建，而是「不等待的开发节奏」。这笔钱只有在团队每天都在等的时候才值得付——所以它本质上是团队规模和迭代频率的函数，而不是构建工具本身的函数。</strong></p>
</blockquote>
<p>另外把视角拉远一点：Vite 团队正在用 Rolldown（Rust 重写的 Rollup）替换生产内核，目标是收敛 dev&#x2F;prod 双引擎的差异；Rspack 在兼容路线上狂奔。两个方向殊途同归——<strong>构建工具正在从「配置工程」变成「默认即得」</strong>。今天做的环境变量收敛、出口统一这些迁移准备，在任何一个未来里都不亏。</p>
<p>相关的旧文：理念层面的分工看<a href="/2026/09/10/webpack-vs-rollup/">《Webpack 与 Rollup：一个为应用而生，一个为库而生》</a>，Webpack 自身体质优化的完整清单看<a href="/2026/09/09/webpack-performance/">《Webpack 性能优化速查》</a>，迁移后怎么做分包和首屏可以对照<a href="/2026/09/10/spa-first-screen-practice/">《SPA 首屏优化落地实录》</a>。</p>
]]></content>
      <categories>
        <category>前端工程化</category>
        <category>构建工具</category>
      </categories>
      <tags>
        <tag>Vite</tag>
        <tag>工程化</tag>
        <tag>Webpack</tag>
        <tag>构建工具</tag>
        <tag>Rspack</tag>
      </tags>
  </entry>
  <entry>
    <title>Webpack 性能优化速查：构建速度与产物体积，一张表讲完</title>
    <url>/2026/09/09/webpack-performance/</url>
    <content><![CDATA[<p>Webpack 的优化手段几十条，但目标只有两个：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">构建速度  → 优化的是开发体验（等多久能跑起来）<br>产物体积  → 优化的是用户首屏（下载多少 JS 才能看）<br></code></pre></td></tr></table></figure>

<p>本文不铺原理，每条只留<strong>结论 + 最小配置</strong>，按「收益 &#x2F; 成本」排序。以 webpack5 为准，webpack4 的差异随文标注。</p>
<hr>
<h2 id="0-先测量，再优化"><a href="#0-先测量，再优化" class="headerlink" title="0. 先测量，再优化"></a>0. 先测量，再优化</h2><p>没有数据的一切优化都是猜。两件工具先装上：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-comment">// 体积构成：谁把包撑大的</span><br><span class="hljs-keyword">const</span> &#123; <span class="hljs-title class_">BundleAnalyzerPlugin</span> &#125; = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;webpack-bundle-analyzer&#x27;</span>)<br><br><span class="hljs-comment">// 各阶段耗时：时间花在哪了</span><br><span class="hljs-keyword">const</span> <span class="hljs-title class_">SpeedMeasurePlugin</span> = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;speed-measure-webpack-plugin&#x27;</span>)<br></code></pre></td></tr></table></figure>

<p>90% 的项目分析完会发现：体积大头是 <code>node_modules</code> 里的重复依赖和没被 tree shaking 掉的组件库；耗时大头是 babel 对 <code>node_modules</code> 的无谓转译。下面的手段就是逐个干掉它们。</p>
<hr>
<h2 id="一、构建速度（开发体验）"><a href="#一、构建速度（开发体验）" class="headerlink" title="一、构建速度（开发体验）"></a>一、构建速度（开发体验）</h2><h3 id="1-持久化缓存-——-webpack5-收益最大的一条"><a href="#1-持久化缓存-——-webpack5-收益最大的一条" class="headerlink" title="1. 持久化缓存 —— webpack5 收益最大的一条"></a>1. 持久化缓存 —— webpack5 收益最大的一条</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-comment">// webpack.config.js</span><br><span class="hljs-attr">cache</span>: &#123; <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;filesystem&#x27;</span> &#125;   <span class="hljs-comment">// webpack5 默认开发环境已开启</span><br></code></pre></td></tr></table></figure>

<p>二次构建从分钟级降到秒级。<strong>webpack4 对应物</strong>：<code>babel-loader</code> 的 <code>cacheDirectory: true</code>、<code>cache-loader</code>、以及已过时的 DllPlugin——webpack5 一个配置全部替代。</p>
<h3 id="2-别让-loader-碰-node-modules"><a href="#2-别让-loader-碰-node-modules" class="headerlink" title="2. 别让 loader 碰 node_modules"></a>2. 别让 loader 碰 node_modules</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">&#123;<br>  <span class="hljs-attr">test</span>: <span class="hljs-regexp">/\.js$/</span>,<br>  <span class="hljs-attr">exclude</span>: <span class="hljs-regexp">/node_modules/</span>,        <span class="hljs-comment">// 依赖通常已是编译产物</span><br>  <span class="hljs-attr">include</span>: path.<span class="hljs-title function_">resolve</span>(__dirname, <span class="hljs-string">&#x27;src&#x27;</span>)<br>&#125;<br></code></pre></td></tr></table></figure>

<p><code>node_modules</code> 往往是业务代码的几十倍体积，babel&#x2F;ESLint 多看它一眼都是浪费。</p>
<h3 id="3-resolve-提效"><a href="#3-resolve-提效" class="headerlink" title="3. resolve 提效"></a>3. resolve 提效</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-attr">resolve</span>: &#123;<br>  <span class="hljs-attr">extensions</span>: [<span class="hljs-string">&#x27;.js&#x27;</span>, <span class="hljs-string">&#x27;.ts&#x27;</span>, <span class="hljs-string">&#x27;.jsx&#x27;</span>, <span class="hljs-string">&#x27;.tsx&#x27;</span>, <span class="hljs-string">&#x27;.json&#x27;</span>],  <span class="hljs-comment">// 每多一个，import 无后缀时多试一轮</span><br>  <span class="hljs-attr">alias</span>: &#123; <span class="hljs-string">&#x27;@&#x27;</span>: path.<span class="hljs-title function_">resolve</span>(__dirname, <span class="hljs-string">&#x27;src&#x27;</span>) &#125;,<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="4-oneOf：命中即止"><a href="#4-oneOf：命中即止" class="headerlink" title="4. oneOf：命中即止"></a>4. oneOf：命中即止</h3><p><code>module.rules</code> 默认每个文件把所有规则试一遍；包一层 <code>oneOf</code> 后命中一条就停：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-attr">module</span>: &#123; <span class="hljs-attr">rules</span>: [&#123; <span class="hljs-attr">oneOf</span>: [ <span class="hljs-comment">/* js / css / 图片规则 */</span> ] &#125;] &#125;<br></code></pre></td></tr></table></figure>

<p>一个 <code>.js</code> 文件不该再被 css 规则白测一遍。</p>
<h3 id="5-换编译器：babel-→-esbuild-SWC"><a href="#5-换编译器：babel-→-esbuild-SWC" class="headerlink" title="5. 换编译器：babel → esbuild &#x2F; SWC"></a>5. 换编译器：babel → esbuild &#x2F; SWC</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript">&#123; <span class="hljs-attr">test</span>: <span class="hljs-regexp">/\.js$/</span>, <span class="hljs-attr">exclude</span>: <span class="hljs-regexp">/node_modules/</span>, <span class="hljs-attr">loader</span>: <span class="hljs-string">&#x27;esbuild-loader&#x27;</span>, <span class="hljs-attr">options</span>: &#123; <span class="hljs-attr">target</span>: <span class="hljs-string">&#x27;es2015&#x27;</span> &#125; &#125;<br></code></pre></td></tr></table></figure>

<p>转译速度差一个数量级以上。项目越大收益越明显。</p>
<h3 id="6-其他两条"><a href="#6-其他两条" class="headerlink" title="6. 其他两条"></a>6. 其他两条</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">devtool：开发环境用 &#x27;eval-cheap-module-source-map&#x27;，<br>        生产环境按需，别全量 source-map<br>多进程：thread-loader 只对转译量大 + 多核的项目有效，<br>        小项目进程通信开销反而更慢 —— 别无脑加<br></code></pre></td></tr></table></figure>

<hr>
<h2 id="二、产物体积（用户首屏）"><a href="#二、产物体积（用户首屏）" class="headerlink" title="二、产物体积（用户首屏）"></a>二、产物体积（用户首屏）</h2><h3 id="1-Tree-Shaking-的三个前提"><a href="#1-Tree-Shaking-的三个前提" class="headerlink" title="1. Tree Shaking 的三个前提"></a>1. Tree Shaking 的三个前提</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">① 源码用 ESM（import/export）—— CommonJS 摇不动<br>② package.json 标注副作用：<br>   &quot;sideEffects&quot;: false           // 全部无副作用<br>   &quot;sideEffects&quot;: [&quot;*.css&quot;]       // css 这类不能摇<br>③ production mode —— 自动开启 usedExports + 压缩删除<br></code></pre></td></tr></table></figure>

<p>组件库引入后体积没变小，八成是这三条里缺了一条。</p>
<h3 id="2-路由懒加载-——-体积优化里最立竿见影的一条"><a href="#2-路由懒加载-——-体积优化里最立竿见影的一条" class="headerlink" title="2. 路由懒加载 —— 体积优化里最立竿见影的一条"></a>2. 路由懒加载 —— 体积优化里最立竿见影的一条</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">const</span> <span class="hljs-title function_">Home</span> = (<span class="hljs-params"></span>) =&gt; <span class="hljs-keyword">import</span>(<span class="hljs-comment">/* webpackChunkName: &quot;home&quot; */</span> <span class="hljs-string">&#x27;./views/Home.vue&#x27;</span>)<br></code></pre></td></tr></table></figure>

<p>首屏只加载首屏的代码，其他页面按需。SPA 不做这一条，谈别的都是细节。</p>
<h3 id="3-splitChunks-拆公共依赖"><a href="#3-splitChunks-拆公共依赖" class="headerlink" title="3. splitChunks 拆公共依赖"></a>3. splitChunks 拆公共依赖</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-attr">optimization</span>: &#123;<br>  <span class="hljs-attr">splitChunks</span>: &#123;<br>    <span class="hljs-attr">chunks</span>: <span class="hljs-string">&#x27;all&#x27;</span>,<br>    <span class="hljs-attr">cacheGroups</span>: &#123;<br>      <span class="hljs-attr">vendors</span>: &#123;<br>        <span class="hljs-attr">test</span>: <span class="hljs-regexp">/[\\/]node_modules[\\/]/</span>,<br>        <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;vendors&#x27;</span>,<br>        <span class="hljs-attr">priority</span>: -<span class="hljs-number">10</span>,<br>      &#125;,<br>    &#125;,<br>  &#125;,<br>&#125;<br></code></pre></td></tr></table></figure>

<p>业务代码天天改，vendor 单独成块才能吃到浏览器长效缓存（见第三节）。</p>
<h3 id="4-大而稳的库走-CDN（externals）"><a href="#4-大而稳的库走-CDN（externals）" class="headerlink" title="4. 大而稳的库走 CDN（externals）"></a>4. 大而稳的库走 CDN（externals）</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-attr">externals</span>: &#123; <span class="hljs-attr">vue</span>: <span class="hljs-string">&#x27;Vue&#x27;</span>, <span class="hljs-attr">echarts</span>: <span class="hljs-string">&#x27;echarts&#x27;</span> &#125;   <span class="hljs-comment">// 打包时排除，运行时读全局变量</span><br></code></pre></td></tr></table></figure>

<p>权衡：externals 会<strong>失去 tree shaking 和版本锁定</strong>，只适合”确实用得多且稳定”的大库；偶尔用的库老老实实打包。</p>
<h3 id="5-传输压缩：gzip-brotli"><a href="#5-传输压缩：gzip-brotli" class="headerlink" title="5. 传输压缩：gzip &#x2F; brotli"></a>5. 传输压缩：gzip &#x2F; brotli</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">new</span> <span class="hljs-title class_">CompressionPlugin</span>(&#123; <span class="hljs-attr">algorithm</span>: <span class="hljs-string">&#x27;brotliCompress&#x27;</span> &#125;)  <span class="hljs-comment">// 构建时预压缩</span><br></code></pre></td></tr></table></figure>

<figure class="highlight nginx"><table><tr><td class="code"><pre><code class="hljs nginx"><span class="hljs-comment"># nginx 直接吐预压缩文件，省掉实时压缩的 CPU</span><br><span class="hljs-attribute">brotli_static</span> <span class="hljs-literal">on</span>;<br><span class="hljs-attribute">gzip_static</span> <span class="hljs-literal">on</span>;<br></code></pre></td></tr></table></figure>

<p>brotli 比 gzip 再小 15%~20%，两者可以并存做兜底。</p>
<h3 id="6-静态资源"><a href="#6-静态资源" class="headerlink" title="6. 静态资源"></a>6. 静态资源</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-comment">// webpack5 asset modules：file-loader/url-loader 已无需安装</span><br>&#123; <span class="hljs-attr">test</span>: <span class="hljs-regexp">/\.png$/</span>,<br>  <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;asset&#x27;</span>,<br>  <span class="hljs-attr">parser</span>: &#123; <span class="hljs-attr">dataUrlCondition</span>: &#123; <span class="hljs-attr">maxSize</span>: <span class="hljs-number">4</span> * <span class="hljs-number">1024</span> &#125; &#125; &#125;  <span class="hljs-comment">// 4KB 内联 base64</span><br></code></pre></td></tr></table></figure>

<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">小图标  → 内联或雪碧图 / iconfont<br>大图    → 压缩 + webp/avif，首图考虑懒加载<br></code></pre></td></tr></table></figure>

<h3 id="7-依赖瘦身-——-常被忽略的大头"><a href="#7-依赖瘦身-——-常被忽略的大头" class="headerlink" title="7. 依赖瘦身 —— 常被忽略的大头"></a>7. 依赖瘦身 —— 常被忽略的大头</h3><figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">组件库按需引入（unplugin-vue-components / babel-plugin-import）<br>lodash        → lodash-es（配合 tree shaking）或按路径引入<br>moment（~70KB）→ dayjs（~2KB）<br>重复依赖      → npm ls &lt;pkg&gt; 查多版本，resolve.alias 收敛到一份<br></code></pre></td></tr></table></figure>

<p><code>BundleAnalyzerPlugin</code> 里看到同一个库出现两份，就是这条没做。</p>
<hr>
<h2 id="三、浏览器缓存与加载"><a href="#三、浏览器缓存与加载" class="headerlink" title="三、浏览器缓存与加载"></a>三、浏览器缓存与加载</h2><h3 id="1-contenthash-长效缓存"><a href="#1-contenthash-长效缓存" class="headerlink" title="1. contenthash + 长效缓存"></a>1. contenthash + 长效缓存</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-attr">output</span>: &#123;<br>  <span class="hljs-attr">filename</span>: <span class="hljs-string">&#x27;[name].[contenthash:8].js&#x27;</span>,   <span class="hljs-comment">// 内容变才变 hash</span><br>  <span class="hljs-attr">clean</span>: <span class="hljs-literal">true</span>,                              <span class="hljs-comment">// 每次构建清旧文件</span><br>&#125;<br></code></pre></td></tr></table></figure>

<p>配合 nginx 对带 hash 的资源给 <code>Cache-Control: max-age=31536000, immutable</code>。</p>
<h3 id="2-runtimeChunk：别让业务代码污染-vendor-的-hash"><a href="#2-runtimeChunk：别让业务代码污染-vendor-的-hash" class="headerlink" title="2. runtimeChunk：别让业务代码污染 vendor 的 hash"></a>2. runtimeChunk：别让业务代码污染 vendor 的 hash</h3><p>webpack 的运行时（模块加载器）默认混在入口 chunk 里，业务代码一改，runtime 变，vendor 引用跟着变，hash 全失效。抽出来：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-attr">optimization</span>: &#123; <span class="hljs-attr">runtimeChunk</span>: <span class="hljs-string">&#x27;single&#x27;</span> &#125;<br></code></pre></td></tr></table></figure>

<p>改业务代码时，<code>vendors.js</code> 的 hash 纹丝不动，用户不用重新下载几百 KB 依赖——这是长效缓存真正生效的前提。</p>
<h3 id="3-资源提示：preload-prefetch"><a href="#3-资源提示：preload-prefetch" class="headerlink" title="3. 资源提示：preload &#x2F; prefetch"></a>3. 资源提示：preload &#x2F; prefetch</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><code class="hljs javascript"><span class="hljs-keyword">import</span>(<span class="hljs-comment">/* webpackPreload: true */</span> <span class="hljs-string">&#x27;./critical-module&#x27;</span>)   <span class="hljs-comment">// 与主包并行，当前页就要用</span><br><span class="hljs-keyword">import</span>(<span class="hljs-comment">/* webpackPrefetch: true */</span> <span class="hljs-string">&#x27;./next-page&#x27;</span>)        <span class="hljs-comment">// 浏览器空闲时偷偷加载下一页</span><br></code></pre></td></tr></table></figure>

<p>一句话区分：<strong>preload 给当前导航，prefetch 给下一步操作</strong>。滥用 prefetch 会浪费用户流量。</p>
<h3 id="4-分包不是越细越好"><a href="#4-分包不是越细越好" class="headerlink" title="4. 分包不是越细越好"></a>4. 分包不是越细越好</h3><p>HTTP&#x2F;2 下请求数不再昂贵，但每个 chunk 有压缩下限和请求开销；几百个碎 chunk 不如适度聚合。<code>splitChunks.maxSize</code> 按需用，别为拆而拆。</p>
<hr>
<h2 id="四、速查表"><a href="#四、速查表" class="headerlink" title="四、速查表"></a>四、速查表</h2><table>
<thead>
<tr>
<th>目标</th>
<th>手段</th>
<th>关键配置</th>
<th>备注</th>
</tr>
</thead>
<tbody><tr>
<td>构建</td>
<td>持久化缓存</td>
<td><code>cache: { type: &#39;filesystem&#39; }</code></td>
<td>webpack5 最大杀器，wp4 用 cache-loader&#x2F;Dll</td>
</tr>
<tr>
<td>构建</td>
<td>缩小 loader 范围</td>
<td><code>exclude / include</code></td>
<td>无成本，默认就该做</td>
</tr>
<tr>
<td>构建</td>
<td>命中即止</td>
<td><code>oneOf</code></td>
<td>无成本</td>
</tr>
<tr>
<td>构建</td>
<td>换编译器</td>
<td><code>esbuild-loader</code> &#x2F; SWC</td>
<td>大项目收益显著</td>
</tr>
<tr>
<td>体积</td>
<td>Tree Shaking</td>
<td>ESM + <code>sideEffects</code> + production</td>
<td>三个前提缺一不可</td>
</tr>
<tr>
<td>体积</td>
<td>路由懒加载</td>
<td><code>() =&gt; import()</code></td>
<td>立竿见影，必做</td>
</tr>
<tr>
<td>体积</td>
<td>拆 vendor</td>
<td><code>splitChunks.cacheGroups</code></td>
<td>配合长效缓存</td>
</tr>
<tr>
<td>体积</td>
<td>CDN 外置</td>
<td><code>externals</code></td>
<td>失去 tree shaking，慎用</td>
</tr>
<tr>
<td>体积</td>
<td>预压缩</td>
<td><code>CompressionPlugin</code> + <code>brotli_static</code></td>
<td>比 gzip 再小 15%+</td>
</tr>
<tr>
<td>体积</td>
<td>依赖瘦身</td>
<td>按需引入 &#x2F; dayjs &#x2F; 去重</td>
<td>最容易被忽略的大头</td>
</tr>
<tr>
<td>加载</td>
<td>长效缓存</td>
<td><code>contenthash</code> + <code>runtimeChunk: &#39;single&#39;</code></td>
<td>两者配套才生效</td>
</tr>
<tr>
<td>加载</td>
<td>资源提示</td>
<td><code>webpackPreload/Prefetch</code></td>
<td>preload 给当前页，prefetch 给下一页</td>
</tr>
</tbody></table>
<p><strong>落地的顺序</strong>：先上 analyzer 看数据 → 构建侧做缓存 + 范围（几乎零成本）→ 体积侧做懒加载 + tree shaking + 依赖瘦身 → 最后补 hash 缓存与资源提示。对绝大多数项目，做完这张表就够了，剩下的交给 CDN 和 HTTP&#x2F;2。</p>
]]></content>
      <categories>
        <category>前端工程化</category>
        <category>webpack</category>
      </categories>
      <tags>
        <tag>性能优化</tag>
        <tag>Webpack</tag>
        <tag>Tree Shaking</tag>
      </tags>
  </entry>
  <entry>
    <title>Webpack 与 Rollup：一个为应用而生，一个为库而生</title>
    <url>/2026/09/10/webpack-vs-rollup/</url>
    <content><![CDATA[<p>这两个工具经常被放在一起比，但它们出生时回答的是<strong>两个不同的问题</strong>：</p>
<blockquote>
<p><strong>Webpack（2012）：怎么把一个「应用」跑进浏览器？</strong> 于是万物皆模块（JS&#x2F;CSS&#x2F;图片&#x2F;字体），loader 负责转换，dev-server + HMR 负责开发体验，产物自带运行时。<br><strong>Rollup（2015）：怎么把一个「库」发布得干净？</strong> 于是原生 ESM 静态分析，产物几乎没有运行时，多格式输出（ESM&#x2F;CJS&#x2F;UMD），下游打包器还能继续 tree shaking。</p>
</blockquote>
<p>选型口诀可以先记住：<strong>写应用用 Webpack，发库用 Rollup</strong>。下面解释为什么。</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">Webpack 产物 = 运行时(__webpack_require__) + 模块注册表(每个模块包一层函数)<br>Rollup 产物 = 所有模块摊平进同一个作用域，看起来像一份手写的代码<br></code></pre></td></tr></table></figure>

<span id="more"></span>

<h2 id="一、出身决定性格"><a href="#一、出身决定性格" class="headerlink" title="一、出身决定性格"></a>一、出身决定性格</h2><p>Webpack 诞生于 CommonJS 时代，核心洞见是「<strong>任何资源都是模块</strong>」+「<strong>动态依赖可以切分出按需加载的代码块</strong>」。它的战场是应用开发：入口一堆、资源一堆、要热更新、要按路由分包。</p>
<p>Rollup 由 Rich Harris（Svelte 的作者）在 2015 年创建，比 Webpack 晚三年，正好踩在 ES Module 标准落地之后。它的洞见是：<strong>ESM 是静态的——import&#x2F;export 在编译期就能确定，不需要运行时</strong>。于是它可以做到 Webpack 当时做不到的事：把模块直接摊平合并，产出干净得像手写的代码。tree shaking 这个词就是随 Rollup 火起来的。</p>
<p>后来的事实也印证了分工：React、Vue 3、D3 的构建产物都出自 Rollup；而 create-react-app、Vue CLI 时代的应用脚手架清一色是 Webpack。</p>
<h2 id="二、产物对比：一眼看懂本质区别"><a href="#二、产物对比：一眼看懂本质区别" class="headerlink" title="二、产物对比：一眼看懂本质区别"></a>二、产物对比：一眼看懂本质区别</h2><p>同样的源码，看两家的产物（Webpack 产物做了简化，保留结构）：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// ---------- 源码 ----------</span><br><span class="hljs-comment">// utils.js</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">a</span>(<span class="hljs-params"></span>) &#123; <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>; &#125;<br><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">b</span>(<span class="hljs-params"></span>) &#123; <span class="hljs-keyword">return</span> <span class="hljs-number">2</span>; &#125;<br><br><span class="hljs-comment">// main.js</span><br><span class="hljs-keyword">import</span> &#123; a &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;./utils.js&#x27;</span>;<br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-title function_">a</span>());<br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// ---------- Webpack 产物（简化） ----------</span><br>(<span class="hljs-function">() =&gt;</span> &#123; <span class="hljs-comment">// webpackBootstrap 运行时</span><br>  <span class="hljs-keyword">const</span> modules = &#123;<br>    <span class="hljs-string">&#x27;./src/utils.js&#x27;</span>: <span class="hljs-function">(<span class="hljs-params"><span class="hljs-variable language_">module</span>, <span class="hljs-built_in">exports</span></span>) =&gt;</span> &#123;<br>      <span class="hljs-built_in">exports</span>.<span class="hljs-property">a</span> = <span class="hljs-function">() =&gt;</span> <span class="hljs-number">1</span>;<br>      <span class="hljs-built_in">exports</span>.<span class="hljs-property">b</span> = <span class="hljs-function">() =&gt;</span> <span class="hljs-number">2</span>;<br>    &#125;,<br>    <span class="hljs-string">&#x27;./src/main.js&#x27;</span>: <span class="hljs-function">(<span class="hljs-params"><span class="hljs-variable language_">module</span>, <span class="hljs-built_in">exports</span>, __webpack_require__</span>) =&gt;</span> &#123;<br>      <span class="hljs-keyword">const</span> &#123; a &#125; = <span class="hljs-title function_">__webpack_require__</span>(<span class="hljs-string">&#x27;./src/utils.js&#x27;</span>);<br>      <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-title function_">a</span>());<br>    &#125;,<br>  &#125;;<br>  <span class="hljs-comment">// ...模块缓存、require 实现、执行入口</span><br>&#125;)();<br></code></pre></td></tr></table></figure>

<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// ---------- Rollup 产物 ----------</span><br><span class="hljs-keyword">function</span> <span class="hljs-title function_">a</span>(<span class="hljs-params"></span>) &#123; <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>; &#125;<br><br><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-title function_">a</span>());<br></code></pre></td></tr></table></figure>

<p>差异一目了然：</p>
<ul>
<li><strong>Webpack 给每个模块包一层函数</strong>，用字符串 id 索引成一张注册表，再配一个运行时（模块缓存、require 实现、异步加载逻辑）负责按需执行——这是为了让<strong>任何模块格式（包括 CommonJS、AMD）在任何环境</strong>都能跑，代价是体积和一层间接性。</li>
<li><strong>Rollup 把所有模块摊平到同一个作用域</strong>（scope hoisting，作用域提升），没有模块边界、没有运行时。<code>b</code> 这种没用的导出直接被摇掉了。</li>
</ul>
<p>一个比喻：Webpack 的产物像<strong>带发动机的整车</strong>，开箱能跑但多了一堆底盘；Rollup 的产物像<strong>精密加工过的零件</strong>，本身几乎零冗余。</p>
<h2 id="三、Tree-Shaking：都能摇，干净程度不同"><a href="#三、Tree-Shaking：都能摇，干净程度不同" class="headerlink" title="三、Tree Shaking：都能摇，干净程度不同"></a>三、Tree Shaking：都能摇，干净程度不同</h2><p>Webpack 2（2016）之后就支持 tree shaking，但它是「半路出家」——必须先把 CommonJS 转成兼容格式，静态分析天然打折。实践中有两个前提，缺一个就摇不动：</p>
<ul>
<li>依赖必须是 <strong>ESM</strong>（CJS 的 <code>require</code> 是运行时动态调用，无法静态分析）；</li>
<li>库要在 <code>package.json</code> 里正确声明 <strong><code>sideEffects</code></strong>，否则 Webpack 不敢删「看起来没用到」的文件（万一有 <code>polyfill</code> 这类副作用呢）。</li>
</ul>
<p>Rollup 从第一天就围绕 ESM 静态分析设计，配合 scope hoisting，摇得更快更彻底，也不需要 <code>sideEffects</code> 这类补救字段。同样的库源码，Rollup 的 ESM 产物通常更小。</p>
<p>顺带一提，Webpack 4 之后也做了 scope hoisting（<code>ModuleConcatenationPlugin</code>，生产模式默认开启），差距在缩小——但「以 CJS 兼容为底座」和「以 ESM 纯净为底座」的出身差异，决定了它永远要多处理一层兼容逻辑。</p>
<h2 id="四、代码分割：Webpack-的主场"><a href="#四、代码分割：Webpack-的主场" class="headerlink" title="四、代码分割：Webpack 的主场"></a>四、代码分割：Webpack 的主场</h2><p>应用开发的核心需求是<strong>按路由&#x2F;按需分包</strong>，这正是 Webpack 的看家本领：</p>
<ul>
<li>多入口、<code>import()</code> 动态导入、<code>splitChunks</code> 公共依赖抽取，策略丰富且成熟；</li>
<li>和 HMR、懒加载失败重试、preload&#x2F;prefetch 魔法注释等应用级能力配套完整。</li>
</ul>
<p>Rollup 也支持代码分割（多入口 + 动态导入会生成共享 chunk），但它的分割策略是<strong>面向多入口的库</strong>设计的——比如同时打包出 <code>core</code> 和 <code>full</code> 两个入口，共享部分自动提取。拿它做复杂应用的分包（按路由切、vendor 策略、runtime chunk），配置别扭，优化空间也远不如 Webpack。Rollup 文档自己都说：它不是主要为应用构建设计的。</p>
<p>开发体验同理：webpack-dev-server 的 HMR（改代码不丢状态、页面不整刷）是应用开发刚需；Rollup 只有 watch 重新打包，没有 HMR——因为<strong>库开发根本不需要跑一个完整应用，写个 demo 页用 ESM 直接 import 源码就够了</strong>。</p>
<h2 id="五、生态与扩展模型：全能-vs-纯净"><a href="#五、生态与扩展模型：全能-vs-纯净" class="headerlink" title="五、生态与扩展模型：全能 vs 纯净"></a>五、生态与扩展模型：全能 vs 纯净</h2><p><strong>Webpack 的扩展是「双轨制」</strong>：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// loader 管「转换」：让 webpack 认识非 JS 资源</span><br><span class="hljs-attr">module</span>: &#123;<br>  <span class="hljs-attr">rules</span>: [<br>    &#123; <span class="hljs-attr">test</span>: <span class="hljs-regexp">/\.css$/</span>, <span class="hljs-attr">use</span>: [<span class="hljs-string">&#x27;style-loader&#x27;</span>, <span class="hljs-string">&#x27;css-loader&#x27;</span>] &#125;,<br>    &#123; <span class="hljs-attr">test</span>: <span class="hljs-regexp">/\.(png|woff2)$/</span>, <span class="hljs-attr">type</span>: <span class="hljs-string">&#x27;asset/resource&#x27;</span> &#125;,<br>  ],<br>&#125;,<br><span class="hljs-comment">// plugin 管「介入构建生命周期」：基于 Tapable 的事件钩子</span><br><span class="hljs-attr">plugins</span>: [<span class="hljs-keyword">new</span> <span class="hljs-title class_">HtmlWebpackPlugin</span>()],<br></code></pre></td></tr></table></figure>

<p>loader 让 CSS、图片、字体都成为一等公民，plugin 钩子覆盖构建的每个阶段——这套体系庞大到「没有 webpack 做不到的事，只有你没配对的 loader」。</p>
<p><strong>Rollup 只有一种插件接口</strong>（<code>load</code>&#x2F;<code>transform</code>&#x2F;<code>resolveId</code> 等钩子），设计干净得多，但能力边界也明显：资源处理要自己拼插件；最经典的痛点是<strong>依赖里全是 CommonJS 时，必须配 <code>@rollup/plugin-commonjs</code> 做转换</strong>，遇到写法刁钻的 CJS 依赖还可能踩坑——毕竟把不纯净的东西洗纯净，本来就是它的逆命题。</p>
<h2 id="六、多格式产物：发库用-Rollup-的根本原因"><a href="#六、多格式产物：发库用-Rollup-的根本原因" class="headerlink" title="六、多格式产物：发库用 Rollup 的根本原因"></a>六、多格式产物：发库用 Rollup 的根本原因</h2><p>一个库的用户可能用 ESM import、可能用 CJS require、可能直接 <code>&lt;script&gt;</code> 引入。Rollup 一份配置输出全家桶：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">// rollup.config.js</span><br><span class="hljs-keyword">export</span> <span class="hljs-keyword">default</span> &#123;<br>  <span class="hljs-attr">input</span>: <span class="hljs-string">&#x27;src/index.js&#x27;</span>,<br>  <span class="hljs-attr">output</span>: [<br>    &#123; <span class="hljs-attr">file</span>: <span class="hljs-string">&#x27;dist/index.esm.js&#x27;</span>, <span class="hljs-attr">format</span>: <span class="hljs-string">&#x27;es&#x27;</span> &#125;,   <span class="hljs-comment">// 给打包器：可继续 tree shaking</span><br>    &#123; <span class="hljs-attr">file</span>: <span class="hljs-string">&#x27;dist/index.cjs&#x27;</span>,    <span class="hljs-attr">format</span>: <span class="hljs-string">&#x27;cjs&#x27;</span> &#125;,  <span class="hljs-comment">// 给 Node.js</span><br>    &#123; <span class="hljs-attr">file</span>: <span class="hljs-string">&#x27;dist/index.umd.js&#x27;</span>, <span class="hljs-attr">format</span>: <span class="hljs-string">&#x27;umd&#x27;</span>, <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;MyLib&#x27;</span> &#125;, <span class="hljs-comment">// 给 &lt;script&gt;</span><br>  ],<br>&#125;;<br></code></pre></td></tr></table></figure>

<figure class="highlight json"><table><tr><td class="code"><pre><code class="hljs json"><span class="hljs-punctuation">&#123;</span><br>  <span class="hljs-attr">&quot;exports&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">&#123;</span><br>    <span class="hljs-attr">&quot;.&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-punctuation">&#123;</span><br>      <span class="hljs-attr">&quot;import&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;./dist/index.esm.js&quot;</span><span class="hljs-punctuation">,</span><br>      <span class="hljs-attr">&quot;require&quot;</span><span class="hljs-punctuation">:</span> <span class="hljs-string">&quot;./dist/index.cjs&quot;</span><br>    <span class="hljs-punctuation">&#125;</span><br>  <span class="hljs-punctuation">&#125;</span><br><span class="hljs-punctuation">&#125;</span><br></code></pre></td></tr></table></figure>

<p>关键洞察是：<strong>库的产物会被下游的打包器再构建一次</strong>。ESM 产物进了用户的 bundle 后还能被继续摇树；而 Webpack 产物里那层 <code>__webpack_require__</code> 包装会阻止下游分析，等于把树「焊死」了——库体积有多大，用户就得全量背多重。这就是 React、Vue 都选 Rollup 的根本原因：<strong>库的产物不是终点，而是下游构建的原料</strong>。</p>
<p>（Webpack 也能通过 <code>output.library.type</code> 输出 umd 等格式，能做，但不优雅。）</p>
<h2 id="七、选型决策-新时代的变化"><a href="#七、选型决策-新时代的变化" class="headerlink" title="七、选型决策 + 新时代的变化"></a>七、选型决策 + 新时代的变化</h2><table>
<thead>
<tr>
<th>维度</th>
<th>Webpack</th>
<th>Rollup</th>
</tr>
</thead>
<tbody><tr>
<td>设计目标</td>
<td>应用打包</td>
<td>库打包</td>
</tr>
<tr>
<td>模块机制</td>
<td>运行时 + 模块函数包装</td>
<td>ESM 静态合并（scope hoisting）</td>
</tr>
<tr>
<td>产物体积</td>
<td>较大（含运行时）</td>
<td>接近手写，几乎零冗余</td>
</tr>
<tr>
<td>Tree shaking</td>
<td>支持，依赖配置与依赖格式</td>
<td>更彻底</td>
</tr>
<tr>
<td>代码分割</td>
<td>强，策略成熟</td>
<td>较弱，面向多入口库</td>
</tr>
<tr>
<td>资源处理</td>
<td>loader 全家桶，一等公民</td>
<td>需插件拼装</td>
</tr>
<tr>
<td>HMR</td>
<td>内置（dev-server）</td>
<td>无，只有 watch</td>
</tr>
<tr>
<td>产物格式</td>
<td>以浏览器 bundle 为主</td>
<td>es &#x2F; cjs &#x2F; umd &#x2F; iife 多格式</td>
</tr>
<tr>
<td>CJS 依赖</td>
<td>原生丝滑</td>
<td>需 commonjs 插件，有坑</td>
</tr>
<tr>
<td>适用场景</td>
<td>复杂应用</td>
<td>组件库 &#x2F; 工具库</td>
</tr>
</tbody></table>
<p>最后是 2026 年的补充答案——这个问题的「现代默认选项」其实是 <strong>Vite</strong>：开发态用 esbuild 预打包依赖 + 浏览器原生 ESM 按需编译，生产构建的内核恰恰就是 Rollup。所以新项目很少直接手写 Webpack 配置了，但底层还是这两个工具的理念在分工：<strong>Vite 开发体验是新的，生产构建的纯净依然姓 Rollup</strong>。另外 Rust 军团（Rspack 走 Webpack 兼容路线、Rolldown 用 Rust 重写 Rollup 理念）正在接管性能问题，可以理解为：<strong>Webpack 的全能与 Rollup 的纯净这场理念之争没有结束，只是换了更快的引擎继续打</strong>。</p>
<p>对面试和实战，记这句话就够：<strong>应用要的是「开发爽 + 分包灵活」，库要的是「产物纯 + 可被下游摇树」——前者选 Webpack（或 Vite 的开发体验），后者选 Rollup。</strong></p>
]]></content>
      <categories>
        <category>前端基础</category>
        <category>工程化</category>
      </categories>
      <tags>
        <tag>工程化</tag>
        <tag>Webpack</tag>
        <tag>构建工具</tag>
        <tag>Rollup</tag>
      </tags>
  </entry>
</search>
